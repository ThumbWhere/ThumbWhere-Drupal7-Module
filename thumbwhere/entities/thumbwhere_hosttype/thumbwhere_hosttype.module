<?php

// ******************************************************************
// This file was generated by autogen_model_implementation_drupal.xsl
// ******************************************************************

// Include the ThumbWhere SDK
require_once(dirname(__FILE__) . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . 'dependencies' . DIRECTORY_SEPARATOR . 'tw' . DIRECTORY_SEPARATOR . 'sdk.class.php');

/**
 * @file
 * Module for the ThumbWhere HostType Entity
 *
 *
 * NOTE: This has been autogenerated.
 * 
 */

/**
 * Implement hook_entity_info().
 *
 * We define two entities here - the actual entity that will hold our domain
 * specific information and an entity that holds information about the different
 * types of entities. See here: http://drupal.org/node/977380 for a discussion on this
 * choice.
 */
function thumbwhere_hosttype_entity_info() {
  $return['thumbwhere_hosttype'] = array(

    // The human-readable name of the type.
    'label' => t('ThumbWhereHostType'),

    // (used by DrupalDefaultEntityController) The name of the entity type's base table.
    'base table' => 'thumbwhere_hosttype',

    // fieldable: Set to TRUE if you want your entity type to accept fields being attached to it.
    'fieldable' => TRUE,

  // Declare the endty keys..
    'entity keys' => array(
      'id' => 'pk_hosttype',
      'label' => 'name',
    ),

    // A function taking an entity and an entity type as arguments and returning the label of the entity.
    // The entity label is the main string associated with an entity; for example, the title of a node or
    // the subject of a comment. If there is an entity object property that defines the label, use the
    // 'label' element of the 'entity keys' return value component to provide this information (see below).
    // If more complex logic is needed to determine the label of an entity, you can instead specify a callback
    // function here, which will be called to determine the entity label. See also the entity_label()
    // function, which implements this logic.
    'label callback' => 'entity_class_label',

    // A function taking an entity as argument and returning the uri elements of the entity, e.g. 'path' and 'options'.
    // The actual entity uri can be constructed by passing these elements to url().
    'uri callback' => 'entity_class_uri',

    //
    // Entity API Components
    //

    // The module whwre we can find our classes
    'module' => 'thumbwhere_hosttype',

    // The entity class and controller class extend the classes provided by the Entity API
    'entity class' => 'ThumbWhereHostType',

    // Which Entity API contoller
    'controller class' => 'ThumbWhereHostTypeController',

    // The information below is used by the ThumbWhereHostTypeUIController (which extends the EntityDefaultUIController)
    'admin ui' => array(
      'path' => 'admin/thumbwhere/thumbwhere_hosttypes',
      'file' => 'thumbwhere_hosttype.admin.inc',
      'controller class' => 'ThumbWhereHostTypeUIController',
      'menu wildcard' => '%thumbwhere_hosttype',
    ),
  );

  return $return;
}


/**
 * Implements hook_permission().
 */
function thumbwhere_hosttype_permission() {

  // We set up permisssions to manage entity types, manage all entities and the
  // permissions for each individual entity
  $permissions = array(
    'administer thumbwhere_hosttypes' => array(
      'title' => t('Administer thumbwhere_hosttypes'),
      'description' => t('Edit and delete all thumbwhere_hosttypes'),
    ),
  'send thumbwhere hosttype notifications' => array(
          'title' => t('Send ThumbWhere Notifications'),
          'description' => t('Allow the caller to call the ThumbWhere HostType Entity Notifications Endpoint.'),
          'restrict access' => TRUE,
      ),    
  );

  return $permissions;
}


/**
 * Determines whether the given user has access to a thumbwhere_hosttype.
 *
 * @param $op
 *   The operation being performed. One of 'view', 'update', 'create', 'delete'
 *   or just 'edit' (being the same as 'create' or 'update').
 * @param $thumbwhere_hosttype
 *   Optionally a thumbwhere_hosttype or a thumbwhere_hosttype type to check access for. If nothing is
 *   given, access for all thumbwhere_hosttypes is determined.
 * @param $account
 *   The user to check for. Leave it to NULL to check for the global user.
 * @return boolean
 *   Whether access is allowed or not.
 */
function thumbwhere_hosttype_access($op, $thumbwhere_hosttype = NULL, $account = NULL) {

  //if (user_access('administer thumbwhere_hosttypes', $account)) {
  //  return TRUE;
 // }

  //return FALSE;
  return TRUE;
}




/**
 * Fetch a thumbwhere_hosttype object. Make sure that the wildcard you choose
 * in the thumbwhere_hosttype entity definition fits the function name here.
 *
 * @param $thumbwhere_hosttype_id
 *   Integer specifying the thumbwhere_hosttype id.
 * @param $reset
 *   A boolean indicating that the internal cache should be reset.
 * @return
 *   A fully-loaded $thumbwhere_hosttype object or FALSE if it cannot be loaded.
 *
 * @see thumbwhere_hosttype_load_multiple()
 */
function thumbwhere_hosttype_load($thumbwhere_hosttype_id, $reset = FALSE) {

  $thumbwhere_hosttypes = entity_load('thumbwhere_hosttype', array($thumbwhere_hosttype_id), array(), $reset);

  return reset($thumbwhere_hosttypes);
}


/**
 * Deletes a thumbwhere_hosttype.
 */
function thumbwhere_hosttype_delete(ThumbWhereHostType $thumbwhere_hosttype) {

  $thumbwhere_hosttype->delete();
}

/**
 * Create a thumbwhere_hosttype object.
 */
function thumbwhere_hosttype_create($values = array()) {

  return entity_get_controller('thumbwhere_hosttype')->create($values);
}


/**
 * URI callback for thumbwhere_hosttypes
 */
function thumbwhere_hosttype_uri(ThumbWhereHostType $thumbwhere_hosttype) {

  return array(
    'path' => 'thumbwhere_hosttype/' . $thumbwhere_hosttype->pk_hosttype,
  );
}


/**
 * Menu title callback for showing individual entities
 */
function thumbwhere_hosttype_page_title(ThumbWhereHostType $thumbwhere_hosttype) {

	return $thumbwhere_hosttype->name;
}


/**
 * Sets up content to show an individual thumbwhere_hosttype
 * @todo - get rid of drupal_set_title();
 */
function thumbwhere_hosttype_page_view($thumbwhere_hosttype, $view_mode = 'full') {



  $controller = entity_get_controller('thumbwhere_hosttype');
  $content = $controller->view(array($thumbwhere_hosttype->pk_hosttype => $thumbwhere_hosttype));
  drupal_set_title($thumbwhere_hosttype->name);
  return $content;
}


/**
 * Implements hook_views_api().
 */
function thumbwhere_hosttype_views_api() {



  return array(
    'api' => 3,
    'path' => drupal_get_path('module', 'thumbwhere_hosttype') . '/views',
  );
}


/**
 * Implement hook_theme().
 */
function thumbwhere_hosttype_theme() {



  return array(
    'thumbwhere_hosttype_add_list' => array(
      'variables' => array('content' => array()),
      'file' => 'thumbwhere_hosttype.admin.inc',
    ),
    'thumbwhere_hosttype' => array(
      'render element' => 'elements',
      'template' => 'thumbwhere_hosttype',
    )
  );
}


/**
 * Implements hook_menu_local_tasks_alter().
 */
function thumbwhere_hosttype_menu_local_tasks_alter(&$data, $router_item, $root_path) {

  // Add action link 'admin/structure/thumbwhere_hosttypes/add' on 'admin/structure/thumbwhere_hosttypes'.
  if ($root_path == 'admin/thumbwhere/thumbwhere_hosttypes') {
    $item = menu_get_item('admin/thumbwhere/thumbwhere_hosttypes/add');
    if ($item['access']) {
      $data['actions']['output'][] = array(
        '#theme' => 'menu_local_action',
        '#link' => $item,
      );
    }
  }
}


/**
 * The class used for thumbwhere_hosttype entities
 */
class ThumbWhereHostType extends Entity {

  public function __construct($values = array()) {
    parent::__construct($values, 'thumbwhere_hosttype');
  }

  protected function defaultLabel() {
    return $this->name;
  }

  protected function defaultUri() {
    return array('path' => 'thumbwhere_hosttype/' . $this->pk_hosttype);
  }


}



/**
 * The Controller for ThumbWhereHostType entities
 */
class ThumbWhereHostTypeController extends EntityAPIController {
  public function __construct($entityType) {
    parent::__construct($entityType);
  }
}



/**
* Implements hook_entity_presave().
*
* Act on an HostType entity before it is about to be created or updated.
*
* @param $entity
*   The entity object.
* @param $entity_type
*   The type of entity being saved (i.e. node, user, comment).
*/
function thumbwhere_hosttype_entity_presave($entity, $entity_type) {

  // We only look after what we want...
  if ($entity_type == 'thumbwhere_hosttype')  {
  	
 	/////////////////////////////////////////////////////
    //
    // Should we not propagate this to ThumbWhere? 
    //
    if (isset($entity->do_not_propagate)) {
      if ($entity->do_not_propagate) {
        if (twCanDebug()) {
          if ($entity->is_new) {
            if (twCanDebug()){
              debug("DO NOT PROPAGATE 'hosttype' presave *CREATE* not being sent to ThumbWhere server.");
          	}
          }
          else {
            if (twCanDebug()) {
              debug("DO NOT PROPAGATE 'hosttype' presave *UPDATE* not being sent to ThumbWhere server.");
            }
          }
        }

        // To cause an infinite loop of updates, comment out this line.
        return;
      }
      else if (twCanDebug()) {
        debug("OK PROAOGATE 'hosttype' presave to ThumbWhere server.");
      }
    }
    else if (twCanDebug()) {
      debug("DO NOT PROPAGATE IS NOT SET");
    }
    //
    //
    ////////////////////////////////////////////////////////  	

  
    // .. then we create a new thumbwhere_hosttype entity...
      // Instantiate the ThumbWhereContent class

    $twAdminAPI = new ThumbWhereAPIAdmin();

    //if (twCanDebug()) debug($twContentAPI);

    //if (twCanDebug()) debug($entity);

    //
    // Collate and validate our fields
    //

    $fields = array();
    // Compulsory so..
    if (empty($entity->name)) {
      throw new Exception('name is mandatory.');
    }
    else {
      $fields['name'] = $entity->name;
    }
		
	//
	// Ensure that we have the 'thumbwhere_api_key' as we are going to pass that through in a context
	//
    if (variable_get('thumbwhere_api_key') == '') {
    	throw new Exception('ThumbWhere resource for \'HostType\' requires that variable \'thumbwhere_api_key\' is set.');
    }
    
              		
    //
    // If this hosttype entity is new....
    //
               
    if ($entity->is_new) {
                

      //
      // ... then we create the hosttype entity in ThumbWhere
      //

      $hosttype = $twAdminAPI->create_host_type($context = array(
                  'key' => variable_get('thumbwhere_api_key')
          ), $fields);
            
       if (twCanDebug()) debug($hosttype);

      //if (twCanDebug()) debug($host_type);

      $attributes = $hosttype->body->host_type->attributes();

      if (twCanDebug()) debug('newly created id = ' . $attributes['id']);

      // Assign new id.
      $entity->pk_hosttype = intval($attributes['id']);

      if (twCanDebug()) debug('$entity->pk_hosttype = ' . $entity->pk_hosttype);
    }
    else
    {
          	

      //
      // .. otherwise we update the hosttype entity in ThumbWhere
      //

      $hosttype = $twAdminAPI->update_host_type($id=$entity->pk_hosttype,$context = array(
                  'key' => variable_get('thumbwhere_api_key')
          ), $fields);
            
        
    }
          	
    
  }
}

/**
 * Implements thumbwhere_hosttype_entity_insert().
 *
 * Act on entities when inserted.
 *
 * @param $entity
 *   The entity object
 * @param $type
 *   The type of entity being inserted (i.e. node, user, comment).
 */
function thumbwhere_hosttype_entity_insert($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_hosttype') return;

}


/**
* Implements thumbwhere_hosttype_entity_delete().
*
* Act on entities when inserted.
*
* @param $entity
*   The entity object
* @param $type
*   The type of entity being deleted (i.e. node, user, comment).
*/
function thumbwhere_hosttype_entity_delete($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_hosttype') return;


}
