<?php

// ******************************************************************
// This file was generated by autogen_model_implementation_drupal.xsl
// ******************************************************************

// Include the ThumbWhere SDK
require_once(dirname(__FILE__) . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . 'dependencies' . DIRECTORY_SEPARATOR . 'tw' . DIRECTORY_SEPARATOR . 'sdk.class.php');

/**
 * @file
 * Module for the ThumbWhere ContentMetadata Entity
 *
 *
 * NOTE: This has been autogenerated.
 * 
 */

/**
 * Implement hook_entity_info().
 *
 * We define two entities here - the actual entity that will hold our domain
 * specific information and an entity that holds information about the different
 * types of entities. See here: http://drupal.org/node/977380 for a discussion on this
 * choice.
 */
function thumbwhere_contentmetadata_entity_info() {
  $return['thumbwhere_contentmetadata'] = array(

    // The human-readable name of the type.
    'label' => t('ThumbWhereContentMetadata'),

    // (used by DrupalDefaultEntityController) The name of the entity type's base table.
    'base table' => 'thumbwhere_contentmetadata',

    // fieldable: Set to TRUE if you want your entity type to accept fields being attached to it.
    'fieldable' => TRUE,

  // Declare the endty keys..
    'entity keys' => array(
      'id' => 'pk_contentmetadata',
      'label' => 'pk_contentmetadata',
    ),

    // A function taking an entity and an entity type as arguments and returning the label of the entity.
    // The entity label is the main string associated with an entity; for example, the title of a node or
    // the subject of a comment. If there is an entity object property that defines the label, use the
    // 'label' element of the 'entity keys' return value component to provide this information (see below).
    // If more complex logic is needed to determine the label of an entity, you can instead specify a callback
    // function here, which will be called to determine the entity label. See also the entity_label()
    // function, which implements this logic.
    'label callback' => 'entity_class_label',

    // A function taking an entity as argument and returning the uri elements of the entity, e.g. 'path' and 'options'.
    // The actual entity uri can be constructed by passing these elements to url().
    'uri callback' => 'entity_class_uri',

    //
    // Entity API Components
    //

    // The module whwre we can find our classes
    'module' => 'thumbwhere_contentmetadata',

    // The entity class and controller class extend the classes provided by the Entity API
    'entity class' => 'ThumbWhereContentMetadata',

    // Which Entity API contoller
    'controller class' => 'ThumbWhereContentMetadataController',

    // The information below is used by the ThumbWhereContentMetadataUIController (which extends the EntityDefaultUIController)
    'admin ui' => array(
      'path' => 'admin/thumbwhere/thumbwhere_contentmetadatas',
      'file' => 'thumbwhere_contentmetadata.admin.inc',
      'controller class' => 'ThumbWhereContentMetadataUIController',
      'menu wildcard' => '%thumbwhere_contentmetadata',
    ),
  );

  return $return;
}


/**
 * Implements hook_permission().
 */
function thumbwhere_contentmetadata_permission() {

  // We set up permisssions to manage entity types, manage all entities and the
  // permissions for each individual entity
  $permissions = array(
    'administer thumbwhere_contentmetadatas' => array(
      'title' => t('Administer thumbwhere_contentmetadatas'),
      'description' => t('Administer all thumbwhere_contentmetadatas entities'),
      //'restrict access' => TRUE,
    ),
  'send thumbwhere contentmetadata notifications' => array(
      'title' => t('Send ThumbWhere Notifications'),
      'description' => t('Allow the caller to call the ThumbWhere ContentMetadata Entity Notifications Endpoint.'),
      //'restrict access' => TRUE,
    ),    
  );

  return $permissions;
}


/**
 * Determines whether the given user has access to a thumbwhere_contentmetadata.
 *
 * @param $op
 *   The operation being performed. One of 'view', 'update', 'create', 'delete'
 *   or just 'edit' (being the same as 'create' or 'update').
 * @param $thumbwhere_contentmetadata
 *   Optionally a thumbwhere_contentmetadata or a thumbwhere_contentmetadata type to check access for. If nothing is
 *   given, access for all thumbwhere_contentmetadatas is determined.
 * @param $account
 *   The user to check for. Leave it to NULL to check for the global user.
 * @return boolean
 *   Whether access is allowed or not.
 */
function thumbwhere_contentmetadata_access($op, $thumbwhere_contentmetadata = NULL, $account = NULL) {

  if (user_access('administer thumbwhere_contentmetadatas', $account)) {
    return TRUE;
  }
 
  return FALSE;
}




/**
 * Fetch a thumbwhere_contentmetadata object. Make sure that the wildcard you choose
 * in the thumbwhere_contentmetadata entity definition fits the function name here.
 *
 * @param $thumbwhere_contentmetadata_id
 *   Integer specifying the thumbwhere_contentmetadata id.
 * @param $reset
 *   A boolean indicating that the internal cache should be reset.
 * @return
 *   A fully-loaded $thumbwhere_contentmetadata object or FALSE if it cannot be loaded.
 *
 * @see thumbwhere_contentmetadata_load_multiple()
 */
function thumbwhere_contentmetadata_load($thumbwhere_contentmetadata_id, $reset = FALSE) {

  $thumbwhere_contentmetadatas = entity_load('thumbwhere_contentmetadata', array($thumbwhere_contentmetadata_id), array(), $reset);

  return reset($thumbwhere_contentmetadatas);
}


/**
 * Deletes a thumbwhere_contentmetadata.
 */
function thumbwhere_contentmetadata_delete(ThumbWhereContentMetadata $thumbwhere_contentmetadata) {

  $thumbwhere_contentmetadata->delete();
}

/**
 * Create a thumbwhere_contentmetadata object.
 */
function thumbwhere_contentmetadata_create($values = array()) {

  return entity_get_controller('thumbwhere_contentmetadata')->create($values);
}


/**
 * URI callback for thumbwhere_contentmetadatas
 */
function thumbwhere_contentmetadata_uri(ThumbWhereContentMetadata $thumbwhere_contentmetadata) {

  return array(
    'path' => 'thumbwhere_contentmetadata/' . $thumbwhere_contentmetadata->pk_contentmetadata,
  );
}


/**
 * Menu title callback for showing individual entities
 */
function thumbwhere_contentmetadata_page_title(ThumbWhereContentMetadata $thumbwhere_contentmetadata) {

	return $thumbwhere_contentmetadata->pk_contentmetadata;
}


/**
 * Sets up content to show an individual thumbwhere_contentmetadata
 * @todo - get rid of drupal_set_title();
 */
function thumbwhere_contentmetadata_page_view($thumbwhere_contentmetadata, $view_mode = 'full') {



  $controller = entity_get_controller('thumbwhere_contentmetadata');
  $content = $controller->view(array($thumbwhere_contentmetadata->pk_contentmetadata => $thumbwhere_contentmetadata));
  drupal_set_title($thumbwhere_contentmetadata->pk_contentmetadata);
  return $content;
}


/**
 * Implements hook_views_api().
 */
function thumbwhere_contentmetadata_views_api() {



  return array(
    'api' => 3,
    'path' => drupal_get_path('module', 'thumbwhere_contentmetadata') . '/views',
  );
}


/**
 * Implement hook_theme().
 */
function thumbwhere_contentmetadata_theme() {



  return array(
    'thumbwhere_contentmetadata_add_list' => array(
      'variables' => array('content' => array()),
      'file' => 'thumbwhere_contentmetadata.admin.inc',
    ),
    'thumbwhere_contentmetadata' => array(
      'render element' => 'elements',
      'template' => 'thumbwhere_contentmetadata',
    )
  );
}


/**
 * Implements hook_menu_local_tasks_alter().
 */
function thumbwhere_contentmetadata_menu_local_tasks_alter(&$data, $router_item, $root_path) {

  // Add action link 'admin/structure/thumbwhere_contentmetadatas/add' on 'admin/structure/thumbwhere_contentmetadatas'.
  if ($root_path == 'admin/thumbwhere/thumbwhere_contentmetadatas') {
    $item = menu_get_item('admin/thumbwhere/thumbwhere_contentmetadatas/add');
    if ($item['access']) {
      $data['actions']['output'][] = array(
        '#theme' => 'menu_local_action',
        '#link' => $item,
      );
    }
  }
}


/**
 * The class used for thumbwhere_contentmetadata entities
 */
class ThumbWhereContentMetadata extends Entity {

  public function __construct($values = array()) {
    parent::__construct($values, 'thumbwhere_contentmetadata');
  }

  protected function defaultLabel() {
    return $this->pk_contentmetadata;
  }

  protected function defaultUri() {
    return array('path' => 'thumbwhere_contentmetadata/' . $this->pk_contentmetadata);
  }


}



/**
 * The Controller for ThumbWhereContentMetadata entities
 */
class ThumbWhereContentMetadataController extends EntityAPIController {
  public function __construct($entityType) {
    parent::__construct($entityType);
  }
}



/**
* Implements hook_entity_presave().
*
* Act on an ContentMetadata entity before it is about to be created or updated.
*
* @param $entity
*   The entity object.
* @param $entity_type
*   The type of entity being saved (i.e. node, user, comment).
*/
function thumbwhere_contentmetadata_entity_presave($entity, $entity_type) {

  // We only look after what we want...
  if ($entity_type == 'thumbwhere_contentmetadata')  {
  	
    // Should we not propagate this to ThumbWhere? 
    if (isset($entity->do_not_propagate)) {
      if ($entity->do_not_propagate) {
        // To cause an infinite loop of updates, comment out this line.
        return;
      }
    }

  
    // .. then we create a new thumbwhere_contentmetadata entity...
      // Instantiate the ThumbWhereContent class

    $twContentAPI = new ThumbWhereAPIContent();


    //
    // Collate and validate our fields
    //

    $fields = array();

    // Nullable so we only pass it through to create_content_metadata if we have it..
    if (isset($entity->fk_contentgenre)) {
      $fields['contentgenre'] = $entity->fk_contentgenre;
    }

    // Nullable so we only pass it through to create_content_metadata if we have it..
    if (isset($entity->fk_contentclassification)) {
      $fields['contentclassification'] = $entity->fk_contentclassification;
    }

    // Nullable so we only pass it through to create_content_metadata if we have it..
    if (!empty($entity->description)) {
      $fields['description'] = $entity->description;
    }

    // Nullable so we only pass it through to create_content_metadata if we have it..
    if (!empty($entity->comment)) {
      $fields['comment'] = $entity->comment;
    }

    // Nullable so we only pass it through to create_content_metadata if we have it..
    if (!empty($entity->year)) {
      $fields['year'] = $entity->year;
    }

    // Nullable so we only pass it through to create_content_metadata if we have it..
    if (isset($entity->compilation)) {
      $fields['compilation'] = $entity->compilation;
    }

    // Not nullable but we have a default value
    if (empty($entity->advisory)) {
      $fields['advisory'] = '';
    }
    else {
      $fields['advisory'] = $entity->advisory;
    }
		
	//
	// Ensure that we have the 'thumbwhere_api_key' as we are going to pass that through in a context
	//
  if (variable_get('thumbwhere_api_key') == '') {
    throw new Exception('ThumbWhere resource calls for \'ContentMetadata\' requires that variable \'thumbwhere_api_key\' is set.');
  }

	//
	// Ensure that we have the 'thumbwhere_api_key' as we are going to pass that through in a context
	//
  if (!is_numeric(variable_get('thumbwhere_host_id'))) {
    throw new Exception('ThumbWhere resource calls for \'ContentMetadata\' requires that variable \'thumbwhere_host_id\' is set.');
  }


              		
    //
    // If this contentmetadata entity is new....
    //
               
    if ($entity->is_new) {
                

      //
      // ... then we create the contentmetadata entity in ThumbWhere
      //

      $contentmetadata = $twContentAPI->create_content_metadata($context = array(
                  'key' => variable_get('thumbwhere_api_key'),
                  'origin' => variable_get('thumbwhere_host_id'),
          ), $fields);

      $attributes = $contentmetadata->body->content_metadata->attributes();

      //if (twCanDebug()) debug('newly created id = ' . t($attributes['id']));

      // Assign new id.
      $entity->pk_contentmetadata = intval($attributes['id']);

      //if (twCanDebug()) debug(t('$entity->pk_contentmetadata = ' . $entity->pk_contentmetadata));
    }
    else
    {
          	

      //
      // .. otherwise we update the contentmetadata entity in ThumbWhere
      //

      $contentmetadata = $twContentAPI->update_content_metadata($id=$entity->pk_contentmetadata,$context = array(
                  'key' => variable_get('thumbwhere_api_key'),
                  'origin' => variable_get('thumbwhere_host_id'),
          ), $fields);
            
        
    }
          	
    
  }
}

/**
 * Implements thumbwhere_contentmetadata_entity_insert().
 *
 * Act on entities when inserted.
 *
 * @param $entity
 *   The entity object
 * @param $type
 *   The type of entity being inserted (i.e. node, user, comment).
 */
function thumbwhere_contentmetadata_entity_insert($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_contentmetadata') return;

}


/**
* Implements thumbwhere_contentmetadata_entity_delete().
*
* Act on entities when inserted.
*
* @param $entity
*   The entity object
* @param $type
*   The type of entity being deleted (i.e. node, user, comment).
*/
function thumbwhere_contentmetadata_entity_delete($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_contentmetadata') return;


}
