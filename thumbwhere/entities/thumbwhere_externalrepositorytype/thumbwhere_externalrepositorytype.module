<?php

// ******************************************************************
// This file was generated by autogen_model_implementation_drupal.xsl
// ******************************************************************

// Include the ThumbWhere SDK
require_once(dirname(__FILE__) . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . 'dependencies' . DIRECTORY_SEPARATOR . 'tw' . DIRECTORY_SEPARATOR . 'sdk.class.php');

/**
 * @file
 * Module for the ThumbWhere ExternalRepositoryType Entity
 *
 *
 * NOTE: This has been autogenerated.
 * 
 */

/**
 * Implement hook_entity_info().
 *
 * We define two entities here - the actual entity that will hold our domain
 * specific information and an entity that holds information about the different
 * types of entities. See here: http://drupal.org/node/977380 for a discussion on this
 * choice.
 */
function thumbwhere_externalrepositorytype_entity_info() {
  $return['thumbwhere_externalrepositorytype'] = array(

    // The human-readable name of the type.
    'label' => t('ThumbWhereExternalRepositoryType'),

    // (used by DrupalDefaultEntityController) The name of the entity type's base table.
    'base table' => 'thumbwhere_externalrepositorytype',

    // fieldable: Set to TRUE if you want your entity type to accept fields being attached to it.
    'fieldable' => TRUE,

  // Declare the endty keys..
    'entity keys' => array(
      'id' => 'pk_externalrepositorytype',
      'label' => 'name',
    ),

    // A function taking an entity and an entity type as arguments and returning the label of the entity.
    // The entity label is the main string associated with an entity; for example, the title of a node or
    // the subject of a comment. If there is an entity object property that defines the label, use the
    // 'label' element of the 'entity keys' return value component to provide this information (see below).
    // If more complex logic is needed to determine the label of an entity, you can instead specify a callback
    // function here, which will be called to determine the entity label. See also the entity_label()
    // function, which implements this logic.
    'label callback' => 'entity_class_label',

    // A function taking an entity as argument and returning the uri elements of the entity, e.g. 'path' and 'options'.
    // The actual entity uri can be constructed by passing these elements to url().
    'uri callback' => 'entity_class_uri',

    //
    // Entity API Components
    //

    // The module whwre we can find our classes
    'module' => 'thumbwhere_externalrepositorytype',

    // The entity class and controller class extend the classes provided by the Entity API
    'entity class' => 'ThumbWhereExternalRepositoryType',

    // Which Entity API contoller
    'controller class' => 'ThumbWhereExternalRepositoryTypeController',

    // The information below is used by the ThumbWhereExternalRepositoryTypeUIController (which extends the EntityDefaultUIController)
    'admin ui' => array(
      'path' => 'admin/thumbwhere/thumbwhere_externalrepositorytypes',
      'file' => 'thumbwhere_externalrepositorytype.admin.inc',
      'controller class' => 'ThumbWhereExternalRepositoryTypeUIController',
      'menu wildcard' => '%thumbwhere_externalrepositorytype',
    ),
  );

  return $return;
}


/**
 * Implements hook_permission().
 */
function thumbwhere_externalrepositorytype_permission() {

  // We set up permisssions to manage entity types, manage all entities and the
  // permissions for each individual entity
  $permissions = array(
    'administer thumbwhere_externalrepositorytypes' => array(
      'title' => t('Administer thumbwhere_externalrepositorytypes'),
      'description' => t('Edit and delete all thumbwhere_externalrepositorytypes'),
    ),
  'send thumbwhere externalrepositorytype notifications' => array(
          'title' => t('Send ThumbWhere Notifications'),
          'description' => t('Allow the caller to call the ThumbWhere ExternalRepositoryType Entity Notifications Endpoint.'),
          'restrict access' => TRUE,
      ),    
  );

  return $permissions;
}


/**
 * Determines whether the given user has access to a thumbwhere_externalrepositorytype.
 *
 * @param $op
 *   The operation being performed. One of 'view', 'update', 'create', 'delete'
 *   or just 'edit' (being the same as 'create' or 'update').
 * @param $thumbwhere_externalrepositorytype
 *   Optionally a thumbwhere_externalrepositorytype or a thumbwhere_externalrepositorytype type to check access for. If nothing is
 *   given, access for all thumbwhere_externalrepositorytypes is determined.
 * @param $account
 *   The user to check for. Leave it to NULL to check for the global user.
 * @return boolean
 *   Whether access is allowed or not.
 */
function thumbwhere_externalrepositorytype_access($op, $thumbwhere_externalrepositorytype = NULL, $account = NULL) {

  //if (user_access('administer thumbwhere_externalrepositorytypes', $account)) {
  //  return TRUE;
 // }

  //return FALSE;
  return TRUE;
}




/**
 * Fetch a thumbwhere_externalrepositorytype object. Make sure that the wildcard you choose
 * in the thumbwhere_externalrepositorytype entity definition fits the function name here.
 *
 * @param $thumbwhere_externalrepositorytype_id
 *   Integer specifying the thumbwhere_externalrepositorytype id.
 * @param $reset
 *   A boolean indicating that the internal cache should be reset.
 * @return
 *   A fully-loaded $thumbwhere_externalrepositorytype object or FALSE if it cannot be loaded.
 *
 * @see thumbwhere_externalrepositorytype_load_multiple()
 */
function thumbwhere_externalrepositorytype_load($thumbwhere_externalrepositorytype_id, $reset = FALSE) {

  $thumbwhere_externalrepositorytypes = entity_load('thumbwhere_externalrepositorytype', array($thumbwhere_externalrepositorytype_id), array(), $reset);

  return reset($thumbwhere_externalrepositorytypes);
}


/**
 * Deletes a thumbwhere_externalrepositorytype.
 */
function thumbwhere_externalrepositorytype_delete(ThumbWhereExternalRepositoryType $thumbwhere_externalrepositorytype) {

  $thumbwhere_externalrepositorytype->delete();
}

/**
 * Create a thumbwhere_externalrepositorytype object.
 */
function thumbwhere_externalrepositorytype_create($values = array()) {

  return entity_get_controller('thumbwhere_externalrepositorytype')->create($values);
}


/**
 * URI callback for thumbwhere_externalrepositorytypes
 */
function thumbwhere_externalrepositorytype_uri(ThumbWhereExternalRepositoryType $thumbwhere_externalrepositorytype) {

  return array(
    'path' => 'thumbwhere_externalrepositorytype/' . $thumbwhere_externalrepositorytype->pk_externalrepositorytype,
  );
}


/**
 * Menu title callback for showing individual entities
 */
function thumbwhere_externalrepositorytype_page_title(ThumbWhereExternalRepositoryType $thumbwhere_externalrepositorytype) {

	return $thumbwhere_externalrepositorytype->name;
}


/**
 * Sets up content to show an individual thumbwhere_externalrepositorytype
 * @todo - get rid of drupal_set_title();
 */
function thumbwhere_externalrepositorytype_page_view($thumbwhere_externalrepositorytype, $view_mode = 'full') {



  $controller = entity_get_controller('thumbwhere_externalrepositorytype');
  $content = $controller->view(array($thumbwhere_externalrepositorytype->pk_externalrepositorytype => $thumbwhere_externalrepositorytype));
  drupal_set_title($thumbwhere_externalrepositorytype->name);
  return $content;
}


/**
 * Implements hook_views_api().
 */
function thumbwhere_externalrepositorytype_views_api() {



  return array(
    'api' => 3,
    'path' => drupal_get_path('module', 'thumbwhere_externalrepositorytype') . '/views',
  );
}


/**
 * Implement hook_theme().
 */
function thumbwhere_externalrepositorytype_theme() {



  return array(
    'thumbwhere_externalrepositorytype_add_list' => array(
      'variables' => array('content' => array()),
      'file' => 'thumbwhere_externalrepositorytype.admin.inc',
    ),
    'thumbwhere_externalrepositorytype' => array(
      'render element' => 'elements',
      'template' => 'thumbwhere_externalrepositorytype',
    )
  );
}


/**
 * Implements hook_menu_local_tasks_alter().
 */
function thumbwhere_externalrepositorytype_menu_local_tasks_alter(&$data, $router_item, $root_path) {

  // Add action link 'admin/structure/thumbwhere_externalrepositorytypes/add' on 'admin/structure/thumbwhere_externalrepositorytypes'.
  if ($root_path == 'admin/thumbwhere/thumbwhere_externalrepositorytypes') {
    $item = menu_get_item('admin/thumbwhere/thumbwhere_externalrepositorytypes/add');
    if ($item['access']) {
      $data['actions']['output'][] = array(
        '#theme' => 'menu_local_action',
        '#link' => $item,
      );
    }
  }
}


/**
 * The class used for thumbwhere_externalrepositorytype entities
 */
class ThumbWhereExternalRepositoryType extends Entity {

  public function __construct($values = array()) {
    parent::__construct($values, 'thumbwhere_externalrepositorytype');
  }

  protected function defaultLabel() {
    return $this->name;
  }

  protected function defaultUri() {
    return array('path' => 'thumbwhere_externalrepositorytype/' . $this->pk_externalrepositorytype);
  }


}



/**
 * The Controller for ThumbWhereExternalRepositoryType entities
 */
class ThumbWhereExternalRepositoryTypeController extends EntityAPIController {
  public function __construct($entityType) {
    parent::__construct($entityType);
  }
}



/**
* Implements hook_entity_presave().
*
* Act on an ExternalRepositoryType entity before it is about to be created or updated.
*
* @param $entity
*   The entity object.
* @param $entity_type
*   The type of entity being saved (i.e. node, user, comment).
*/
function thumbwhere_externalrepositorytype_entity_presave($entity, $entity_type) {

  // We only look after what we want...
  if ($entity_type == 'thumbwhere_externalrepositorytype')  {
  	
 	/////////////////////////////////////////////////////
    //
    // Should we not propagate this to ThumbWhere? 
    //
    if (isset($entity->do_not_propagate)) {
      if ($entity->do_not_propagate) {
        if (twCanDebug()) {
          if ($entity->is_new) {
            if (twCanDebug()){
              debug("DO NOT PROPAGATE 'externalrepositorytype' presave *CREATE* not being sent to ThumbWhere server.");
          	}
          }
          else {
            if (twCanDebug()) {
              debug("DO NOT PROPAGATE 'externalrepositorytype' presave *UPDATE* not being sent to ThumbWhere server.");
            }
          }
        }

        // To cause an infinite loop of updates, comment out this line.
        return;
      }
      else if (twCanDebug()) {
        debug("OK PROAOGATE 'externalrepositorytype' presave to ThumbWhere server.");
      }
    }
    else if (twCanDebug()) {
      debug("DO NOT PROPAGATE IS NOT SET");
    }
    //
    //
    ////////////////////////////////////////////////////////  	

  
    // .. then we create a new thumbwhere_externalrepositorytype entity...
      // Instantiate the ThumbWhereContent class

    $twAdminAPI = new ThumbWhereAPIAdmin();

    //if (twCanDebug()) debug($twContentAPI);

    //if (twCanDebug()) debug($entity);

    //
    // Collate and validate our fields
    //

    $fields = array();
    // Compulsory so..
    if (empty($entity->name)) {
      throw new Exception('name is mandatory.');
    }
    else {
      $fields['name'] = $entity->name;
    }
		
	//
	// Ensure that we have the 'thumbwhere_api_key' as we are going to pass that through in a context
	//
  if (variable_get('thumbwhere_api_key') == '') {
    throw new Exception('ThumbWhere resource calls for \'ExternalRepositoryType\' requires that variable \'thumbwhere_api_key\' is set.');
  }

	//
	// Ensure that we have the 'thumbwhere_api_key' as we are going to pass that through in a context
	//
  if (!is_numeric(variable_get('thumbwhere_host_id'))) {
    throw new Exception('ThumbWhere resource calls for \'ExternalRepositoryType\' requires that variable \'thumbwhere_host_id\' is set.');
  }


              		
    //
    // If this externalrepositorytype entity is new....
    //
               
    if ($entity->is_new) {
                

      //
      // ... then we create the externalrepositorytype entity in ThumbWhere
      //

      $externalrepositorytype = $twAdminAPI->create_external_repository_type($context = array(
                  'key' => variable_get('thumbwhere_api_key'),
                  'origin' => variable_get('thumbwhere_host_id'),
          ), $fields);
            
       if (twCanDebug()) debug($externalrepositorytype);

      //if (twCanDebug()) debug($external_repository_type);

      $attributes = $externalrepositorytype->body->external_repository_type->attributes();

      if (twCanDebug()) debug('newly created id = ' . $attributes['id']);

      // Assign new id.
      $entity->pk_externalrepositorytype = intval($attributes['id']);

      if (twCanDebug()) debug('$entity->pk_externalrepositorytype = ' . $entity->pk_externalrepositorytype);
    }
    else
    {
          	

      //
      // .. otherwise we update the externalrepositorytype entity in ThumbWhere
      //

      $externalrepositorytype = $twAdminAPI->update_external_repository_type($id=$entity->pk_externalrepositorytype,$context = array(
                  'key' => variable_get('thumbwhere_api_key'),
                  'origin' => variable_get('thumbwhere_host_id'),
          ), $fields);
            
        
    }
          	
    
  }
}

/**
 * Implements thumbwhere_externalrepositorytype_entity_insert().
 *
 * Act on entities when inserted.
 *
 * @param $entity
 *   The entity object
 * @param $type
 *   The type of entity being inserted (i.e. node, user, comment).
 */
function thumbwhere_externalrepositorytype_entity_insert($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_externalrepositorytype') return;

}


/**
* Implements thumbwhere_externalrepositorytype_entity_delete().
*
* Act on entities when inserted.
*
* @param $entity
*   The entity object
* @param $type
*   The type of entity being deleted (i.e. node, user, comment).
*/
function thumbwhere_externalrepositorytype_entity_delete($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_externalrepositorytype') return;


}
