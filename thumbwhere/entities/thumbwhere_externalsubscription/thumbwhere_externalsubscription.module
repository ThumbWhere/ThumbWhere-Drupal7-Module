<?php

// ******************************************************************
// This file was generated by autogen_model_implementation_drupal.xsl
// ******************************************************************

// Include the ThumbWhere SDK
require_once(dirname(__FILE__) . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . 'dependencies' . DIRECTORY_SEPARATOR . 'tw' . DIRECTORY_SEPARATOR . 'sdk.class.php');

/**
 * @file
 * Module for the ThumbWhere ExternalSubscription Entity
 *
 *
 * NOTE: This has been autogenerated.
 * 
 */

/**
 * Implement hook_entity_info().
 *
 * We define two entities here - the actual entity that will hold our domain
 * specific information and an entity that holds information about the different
 * types of entities. See here: http://drupal.org/node/977380 for a discussion on this
 * choice.
 */
function thumbwhere_externalsubscription_entity_info() {
  $return['thumbwhere_externalsubscription'] = array(

    // The human-readable name of the type.
    'label' => t('ThumbWhereExternalSubscription'),

    // (used by DrupalDefaultEntityController) The name of the entity type's base table.
    'base table' => 'thumbwhere_externalsubscription',

    // fieldable: Set to TRUE if you want your entity type to accept fields being attached to it.
    'fieldable' => TRUE,

  // Declare the endty keys..
    'entity keys' => array(
      'id' => 'pk_externalsubscription',
      'label' => 'pk_externalsubscription',
    ),

    // A function taking an entity and an entity type as arguments and returning the label of the entity.
    // The entity label is the main string associated with an entity; for example, the title of a node or
    // the subject of a comment. If there is an entity object property that defines the label, use the
    // 'label' element of the 'entity keys' return value component to provide this information (see below).
    // If more complex logic is needed to determine the label of an entity, you can instead specify a callback
    // function here, which will be called to determine the entity label. See also the entity_label()
    // function, which implements this logic.
    'label callback' => 'entity_class_label',

    // A function taking an entity as argument and returning the uri elements of the entity, e.g. 'path' and 'options'.
    // The actual entity uri can be constructed by passing these elements to url().
    'uri callback' => 'entity_class_uri',

    //
    // Entity API Components
    //

    // The module whwre we can find our classes
    'module' => 'thumbwhere_externalsubscription',

    // The entity class and controller class extend the classes provided by the Entity API
    'entity class' => 'ThumbWhereExternalSubscription',

    // Which Entity API contoller
    'controller class' => 'ThumbWhereExternalSubscriptionController',

    // The information below is used by the ThumbWhereExternalSubscriptionUIController (which extends the EntityDefaultUIController)
    'admin ui' => array(
      'path' => 'admin/thumbwhere/thumbwhere_externalsubscriptions',
      'file' => 'thumbwhere_externalsubscription.admin.inc',
      'controller class' => 'ThumbWhereExternalSubscriptionUIController',
      'menu wildcard' => '%thumbwhere_externalsubscription',
    ),
  );

  return $return;
}


/**
 * Implements hook_permission().
 */
function thumbwhere_externalsubscription_permission() {

  // We set up permisssions to manage entity types, manage all entities and the
  // permissions for each individual entity
  $permissions = array(
    'administer thumbwhere_externalsubscriptions' => array(
      'title' => t('Administer thumbwhere_externalsubscriptions'),
      'description' => t('Edit and delete all thumbwhere_externalsubscriptions'),
    ),
  'send thumbwhere externalsubscription notifications' => array(
          'title' => t('Send ThumbWhere Notifications'),
          'description' => t('Allow the caller to call the ThumbWhere ExternalSubscription Entity Notifications Endpoint.'),
          'restrict access' => TRUE,
      ),    
  );

  return $permissions;
}


/**
 * Determines whether the given user has access to a thumbwhere_externalsubscription.
 *
 * @param $op
 *   The operation being performed. One of 'view', 'update', 'create', 'delete'
 *   or just 'edit' (being the same as 'create' or 'update').
 * @param $thumbwhere_externalsubscription
 *   Optionally a thumbwhere_externalsubscription or a thumbwhere_externalsubscription type to check access for. If nothing is
 *   given, access for all thumbwhere_externalsubscriptions is determined.
 * @param $account
 *   The user to check for. Leave it to NULL to check for the global user.
 * @return boolean
 *   Whether access is allowed or not.
 */
function thumbwhere_externalsubscription_access($op, $thumbwhere_externalsubscription = NULL, $account = NULL) {

  //if (user_access('administer thumbwhere_externalsubscriptions', $account)) {
  //  return TRUE;
 // }

  //return FALSE;
  return TRUE;
}




/**
 * Fetch a thumbwhere_externalsubscription object. Make sure that the wildcard you choose
 * in the thumbwhere_externalsubscription entity definition fits the function name here.
 *
 * @param $thumbwhere_externalsubscription_id
 *   Integer specifying the thumbwhere_externalsubscription id.
 * @param $reset
 *   A boolean indicating that the internal cache should be reset.
 * @return
 *   A fully-loaded $thumbwhere_externalsubscription object or FALSE if it cannot be loaded.
 *
 * @see thumbwhere_externalsubscription_load_multiple()
 */
function thumbwhere_externalsubscription_load($thumbwhere_externalsubscription_id, $reset = FALSE) {

  $thumbwhere_externalsubscriptions = entity_load('thumbwhere_externalsubscription', array($thumbwhere_externalsubscription_id), array(), $reset);

  return reset($thumbwhere_externalsubscriptions);
}


/**
 * Deletes a thumbwhere_externalsubscription.
 */
function thumbwhere_externalsubscription_delete(ThumbWhereExternalSubscription $thumbwhere_externalsubscription) {

  $thumbwhere_externalsubscription->delete();
}

/**
 * Create a thumbwhere_externalsubscription object.
 */
function thumbwhere_externalsubscription_create($values = array()) {

  return entity_get_controller('thumbwhere_externalsubscription')->create($values);
}


/**
 * URI callback for thumbwhere_externalsubscriptions
 */
function thumbwhere_externalsubscription_uri(ThumbWhereExternalSubscription $thumbwhere_externalsubscription) {

  return array(
    'path' => 'thumbwhere_externalsubscription/' . $thumbwhere_externalsubscription->pk_externalsubscription,
  );
}


/**
 * Menu title callback for showing individual entities
 */
function thumbwhere_externalsubscription_page_title(ThumbWhereExternalSubscription $thumbwhere_externalsubscription) {

	return $thumbwhere_externalsubscription->pk_externalsubscription;
}


/**
 * Sets up content to show an individual thumbwhere_externalsubscription
 * @todo - get rid of drupal_set_title();
 */
function thumbwhere_externalsubscription_page_view($thumbwhere_externalsubscription, $view_mode = 'full') {



  $controller = entity_get_controller('thumbwhere_externalsubscription');
  $content = $controller->view(array($thumbwhere_externalsubscription->pk_externalsubscription => $thumbwhere_externalsubscription));
  drupal_set_title($thumbwhere_externalsubscription->pk_externalsubscription);
  return $content;
}


/**
 * Implements hook_views_api().
 */
function thumbwhere_externalsubscription_views_api() {



  return array(
    'api' => 3,
    'path' => drupal_get_path('module', 'thumbwhere_externalsubscription') . '/views',
  );
}


/**
 * Implement hook_theme().
 */
function thumbwhere_externalsubscription_theme() {



  return array(
    'thumbwhere_externalsubscription_add_list' => array(
      'variables' => array('content' => array()),
      'file' => 'thumbwhere_externalsubscription.admin.inc',
    ),
    'thumbwhere_externalsubscription' => array(
      'render element' => 'elements',
      'template' => 'thumbwhere_externalsubscription',
    )
  );
}


/**
 * Implements hook_menu_local_tasks_alter().
 */
function thumbwhere_externalsubscription_menu_local_tasks_alter(&$data, $router_item, $root_path) {

  // Add action link 'admin/structure/thumbwhere_externalsubscriptions/add' on 'admin/structure/thumbwhere_externalsubscriptions'.
  if ($root_path == 'admin/thumbwhere/thumbwhere_externalsubscriptions') {
    $item = menu_get_item('admin/thumbwhere/thumbwhere_externalsubscriptions/add');
    if ($item['access']) {
      $data['actions']['output'][] = array(
        '#theme' => 'menu_local_action',
        '#link' => $item,
      );
    }
  }
}


/**
 * The class used for thumbwhere_externalsubscription entities
 */
class ThumbWhereExternalSubscription extends Entity {

  public function __construct($values = array()) {
    parent::__construct($values, 'thumbwhere_externalsubscription');
  }

  protected function defaultLabel() {
    return $this->pk_externalsubscription;
  }

  protected function defaultUri() {
    return array('path' => 'thumbwhere_externalsubscription/' . $this->pk_externalsubscription);
  }


}



/**
 * The Controller for ThumbWhereExternalSubscription entities
 */
class ThumbWhereExternalSubscriptionController extends EntityAPIController {
  public function __construct($entityType) {
    parent::__construct($entityType);
  }
}



/**
* Implements hook_entity_presave().
*
* Act on an ExternalSubscription entity before it is about to be created or updated.
*
* @param $entity
*   The entity object.
* @param $entity_type
*   The type of entity being saved (i.e. node, user, comment).
*/
function thumbwhere_externalsubscription_entity_presave($entity, $entity_type) {

  // We only look after what we want...
  if ($entity_type == 'thumbwhere_externalsubscription')  {
  	
  	
  	
 	/////////////////////////////////////////////////////
    //
    // Should we not propagate this to ThumbWhere? 
    //
    if (isset($entity->do_not_propagate)) {
      if ($entity->do_not_propagate) {
        if (twCanDebug()) {
          if ($entity->is_new) {
            if (twCanDebug()){
              debug("DO NOT PROPAGATE 'externalsubscription' presave *CREATE*");
          	}
          }
          else {
            if (twCanDebug()) {
              debug("DO NOT PROPAGATE 'externalsubscription' presave *UPDATE*");
            }
          }
        }

        // To cause an infinite loop of updates, comment out this line.
        return;
      }
      else if (twCanDebug()) {
        debug("OK PROAOGATE 'externalsubscription' presave to ThumbWhere Server");
      }
    }
    else if (twCanDebug()) {
      debug("DO NOT PROPAGATE IS NOT SET");
    }
    //
    //
    ////////////////////////////////////////////////////////  	

  
    // .. then we create a new thumbwhere_externalsubscription entity...
      // Instantiate the ThumbWhereContent class

    $twAdminAPI = new ThumbWhereAPIAdmin();

    //if (twCanDebug()) debug($twContentAPI);

    //if (twCanDebug()) debug($entity);

    //
    // Collate and validate our fields
    //

    $fields = array();
    // Compulsory so..
    if (!isset($entity->fk_externalrepository)) {
      throw new Exception('externalrepository is mandatory.');
    }
    else {
      $fields['externalrepository'] = $entity->fk_externalrepository;
    }
    // Compulsory so..
    if (!isset($entity->fk_repositoryresource)) {
      throw new Exception('repositoryresource is mandatory.');
    }
    else {
      $fields['repositoryresource'] = $entity->fk_repositoryresource;
    }

    // Not nullable but we have a default value
    if (!isset($entity->active)) {
      $fields['active'] = 1;
    }
    else {
      $fields['active'] = $entity->active;
    }
		
	//
	// Ensure that we have the 'thumbwhere_api_key' as we are going to pass that through in a context
	//
    if (variable_get('thumbwhere_api_key') == '') {
    	throw new Exception('ThumbWhere resource for \'ExternalSubscription\' requires that variable \'thumbwhere_api_key\' is set.');
    }
    
              		
    //
    // If this externalsubscription entity is new....
    //
               
    if ($entity->is_new) {
                

      //
      // ... then we create the externalsubscription entity in ThumbWhere
      //

      $externalsubscription = $twAdminAPI->create_external_repository_resource_subscription($context = array(
                  'key' => variable_get('thumbwhere_api_key')
          ), $fields);
            
       if (twCanDebug()) debug($externalsubscription);

      //if (twCanDebug()) debug($external_repository_resource_subscription);

      $attributes = $externalsubscription->body->external_repository_resource_subscription->attributes();

      if (twCanDebug()) debug('newly created id = ' . $attributes['id']);

      // Assign new id.
      $entity->pk_externalsubscription = intval($attributes['id']);

      if (twCanDebug()) debug('$entity->pk_externalsubscription = ' . $entity->pk_externalsubscription);
    }
    else
    {
          	

      //
      // .. otherwise we update the externalsubscription entity in ThumbWhere
      //

      $externalsubscription = $twAdminAPI->update_external_repository_resource_subscription($id=$entity->pk_externalsubscription,$context = array(
                  'key' => variable_get('thumbwhere_api_key')
          ), $fields);
            
        
    }
          	
    
  }
}

/**
 * Implements thumbwhere_externalsubscription_entity_insert().
 *
 * Act on entities when inserted.
 *
 * @param $entity
 *   The entity object
 * @param $type
 *   The type of entity being inserted (i.e. node, user, comment).
 */
function thumbwhere_externalsubscription_entity_insert($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_externalsubscription') return;

}


/**
* Implements thumbwhere_externalsubscription_entity_delete().
*
* Act on entities when inserted.
*
* @param $entity
*   The entity object
* @param $type
*   The type of entity being deleted (i.e. node, user, comment).
*/
function thumbwhere_externalsubscription_entity_delete($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_externalsubscription') return;


}
