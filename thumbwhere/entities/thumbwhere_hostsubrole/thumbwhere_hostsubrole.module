<?php

// ******************************************************************
// This file was generated by autogen_model_implementation_drupal.xsl
// ******************************************************************

// Include the ThumbWhere SDK
require_once(dirname(__FILE__) . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . 'dependencies' . DIRECTORY_SEPARATOR . 'tw' . DIRECTORY_SEPARATOR . 'sdk.class.php');

/**
 * @file
 * Module for the ThumbWhere HostSubRole Entity
 *
 *
 * NOTE: This has been autogenerated.
 * 
 */

/**
 * Implement hook_entity_info().
 *
 * We define two entities here - the actual entity that will hold our domain
 * specific information and an entity that holds information about the different
 * types of entities. See here: http://drupal.org/node/977380 for a discussion on this
 * choice.
 */
function thumbwhere_hostsubrole_entity_info() {
  $return['thumbwhere_hostsubrole'] = array(

    // The human-readable name of the type.
    'label' => t('ThumbWhereHostSubRole'),

    // (used by DrupalDefaultEntityController) The name of the entity type's base table.
    'base table' => 'thumbwhere_hostsubrole',

    // fieldable: Set to TRUE if you want your entity type to accept fields being attached to it.
    'fieldable' => TRUE,

  // Declare the endty keys..
    'entity keys' => array(
      'id' => 'pk_hostsubrole',
      'label' => 'pk_subrole',
    ),

    // A function taking an entity and an entity type as arguments and returning the label of the entity.
    // The entity label is the main string associated with an entity; for example, the title of a node or
    // the subject of a comment. If there is an entity object property that defines the label, use the
    // 'label' element of the 'entity keys' return value component to provide this information (see below).
    // If more complex logic is needed to determine the label of an entity, you can instead specify a callback
    // function here, which will be called to determine the entity label. See also the entity_label()
    // function, which implements this logic.
    'label callback' => 'entity_class_label',

    // A function taking an entity as argument and returning the uri elements of the entity, e.g. 'path' and 'options'.
    // The actual entity uri can be constructed by passing these elements to url().
    'uri callback' => 'entity_class_uri',

    //
    // Entity API Components
    //

    // The module whwre we can find our classes
    'module' => 'thumbwhere_hostsubrole',

    // The entity class and controller class extend the classes provided by the Entity API
    'entity class' => 'ThumbWhereHostSubRole',

    // Which Entity API contoller
    'controller class' => 'ThumbWhereHostSubRoleController',

    // The information below is used by the ThumbWhereHostSubRoleUIController (which extends the EntityDefaultUIController)
    'admin ui' => array(
      'path' => 'admin/thumbwhere/thumbwhere_hostsubroles',
      'file' => 'thumbwhere_hostsubrole.admin.inc',
      'controller class' => 'ThumbWhereHostSubRoleUIController',
      'menu wildcard' => '%thumbwhere_hostsubrole',
    ),
  );

  return $return;
}


/**
 * Implements hook_permission().
 */
function thumbwhere_hostsubrole_permission() {

  // We set up permisssions to manage entity types, manage all entities and the
  // permissions for each individual entity
  $permissions = array(
    'administer thumbwhere_hostsubroles' => array(
      'title' => t('Administer thumbwhere_hostsubroles'),
      'description' => t('Administer all thumbwhere_hostsubroles entities'),
      //'restrict access' => TRUE,
    ),
  'send thumbwhere hostsubrole notifications' => array(
      'title' => t('Send ThumbWhere Notifications'),
      'description' => t('Allow the caller to call the ThumbWhere HostSubRole Entity Notifications Endpoint.'),
      //'restrict access' => TRUE,
    ),    
  );

  return $permissions;
}


/**
 * Determines whether the given user has access to a thumbwhere_hostsubrole.
 *
 * @param $op
 *   The operation being performed. One of 'view', 'update', 'create', 'delete'
 *   or just 'edit' (being the same as 'create' or 'update').
 * @param $thumbwhere_hostsubrole
 *   Optionally a thumbwhere_hostsubrole or a thumbwhere_hostsubrole type to check access for. If nothing is
 *   given, access for all thumbwhere_hostsubroles is determined.
 * @param $account
 *   The user to check for. Leave it to NULL to check for the global user.
 * @return boolean
 *   Whether access is allowed or not.
 */
function thumbwhere_hostsubrole_access($op, $thumbwhere_hostsubrole = NULL, $account = NULL) {

  if (user_access('administer thumbwhere_hostsubroles', $account)) {
    return TRUE;
  }
 
  return FALSE;
}




/**
 * Fetch a thumbwhere_hostsubrole object. Make sure that the wildcard you choose
 * in the thumbwhere_hostsubrole entity definition fits the function name here.
 *
 * @param $thumbwhere_hostsubrole_id
 *   Integer specifying the thumbwhere_hostsubrole id.
 * @param $reset
 *   A boolean indicating that the internal cache should be reset.
 * @return
 *   A fully-loaded $thumbwhere_hostsubrole object or FALSE if it cannot be loaded.
 *
 * @see thumbwhere_hostsubrole_load_multiple()
 */
function thumbwhere_hostsubrole_load($thumbwhere_hostsubrole_id, $reset = FALSE) {

  $thumbwhere_hostsubroles = entity_load('thumbwhere_hostsubrole', array($thumbwhere_hostsubrole_id), array(), $reset);

  return reset($thumbwhere_hostsubroles);
}


/**
 * Deletes a thumbwhere_hostsubrole.
 */
function thumbwhere_hostsubrole_delete(ThumbWhereHostSubRole $thumbwhere_hostsubrole) {

  $thumbwhere_hostsubrole->delete();
}

/**
 * Create a thumbwhere_hostsubrole object.
 */
function thumbwhere_hostsubrole_create($values = array()) {

  return entity_get_controller('thumbwhere_hostsubrole')->create($values);
}


/**
 * URI callback for thumbwhere_hostsubroles
 */
function thumbwhere_hostsubrole_uri(ThumbWhereHostSubRole $thumbwhere_hostsubrole) {

  return array(
    'path' => 'thumbwhere_hostsubrole/' . $thumbwhere_hostsubrole->pk_hostsubrole,
  );
}


/**
 * Menu title callback for showing individual entities
 */
function thumbwhere_hostsubrole_page_title(ThumbWhereHostSubRole $thumbwhere_hostsubrole) {

	return $thumbwhere_hostsubrole->pk_subrole;
}


/**
 * Sets up content to show an individual thumbwhere_hostsubrole
 * @todo - get rid of drupal_set_title();
 */
function thumbwhere_hostsubrole_page_view($thumbwhere_hostsubrole, $view_mode = 'full') {



  $controller = entity_get_controller('thumbwhere_hostsubrole');
  $content = $controller->view(array($thumbwhere_hostsubrole->pk_hostsubrole => $thumbwhere_hostsubrole));
  drupal_set_title($thumbwhere_hostsubrole->pk_subrole);
  return $content;
}


/**
 * Implements hook_views_api().
 */
function thumbwhere_hostsubrole_views_api() {



  return array(
    'api' => 3,
    'path' => drupal_get_path('module', 'thumbwhere_hostsubrole') . '/views',
  );
}


/**
 * Implement hook_theme().
 */
function thumbwhere_hostsubrole_theme() {



  return array(
    'thumbwhere_hostsubrole_add_list' => array(
      'variables' => array('content' => array()),
      'file' => 'thumbwhere_hostsubrole.admin.inc',
    ),
    'thumbwhere_hostsubrole' => array(
      'render element' => 'elements',
      'template' => 'thumbwhere_hostsubrole',
    )
  );
}


/**
 * Implements hook_menu_local_tasks_alter().
 */
function thumbwhere_hostsubrole_menu_local_tasks_alter(&$data, $router_item, $root_path) {

  // Add action link 'admin/structure/thumbwhere_hostsubroles/add' on 'admin/structure/thumbwhere_hostsubroles'.
  if ($root_path == 'admin/thumbwhere/thumbwhere_hostsubroles') {
    $item = menu_get_item('admin/thumbwhere/thumbwhere_hostsubroles/add');
    if ($item['access']) {
      $data['actions']['output'][] = array(
        '#theme' => 'menu_local_action',
        '#link' => $item,
      );
    }
  }
}


/**
 * The class used for thumbwhere_hostsubrole entities
 */
class ThumbWhereHostSubRole extends Entity {

  public function __construct($values = array()) {
    parent::__construct($values, 'thumbwhere_hostsubrole');
  }

  protected function defaultLabel() {
    return $this->pk_subrole;
  }

  protected function defaultUri() {
    return array('path' => 'thumbwhere_hostsubrole/' . $this->pk_hostsubrole);
  }


}



/**
 * The Controller for ThumbWhereHostSubRole entities
 */
class ThumbWhereHostSubRoleController extends EntityAPIController {
  public function __construct($entityType) {
    parent::__construct($entityType);
  }
}



/**
* Implements hook_entity_presave().
*
* Act on an HostSubRole entity before it is about to be created or updated.
*
* @param $entity
*   The entity object.
* @param $entity_type
*   The type of entity being saved (i.e. node, user, comment).
*/
function thumbwhere_hostsubrole_entity_presave($entity, $entity_type) {

  // We only look after what we want...
  if ($entity_type == 'thumbwhere_hostsubrole')  {
  	
    // Should we not propagate this to ThumbWhere? 
    if (isset($entity->do_not_propagate)) {
      if ($entity->do_not_propagate) {
        // To cause an infinite loop of updates, comment out this line.
        return;
      }
    }

  
    // .. then we create a new thumbwhere_hostsubrole entity...
      // Instantiate the ThumbWhereContent class

    $twAdminAPI = new ThumbWhereAPIAdmin();


    //
    // Collate and validate our fields
    //

    $fields = array();
    // Compulsory so..
    if (!isset($entity->fk_subroletype)) {
      throw new Exception('subroletype is mandatory.');
    }
    else {
      $fields['subroletype'] = $entity->fk_subroletype;
    }

    // Nullable so we only pass it through to create_host_role_subrole if we have it..
    if (isset($entity->fk_hostrole)) {
      $fields['hostrole'] = $entity->fk_hostrole;
    }
		
	//
	// Ensure that we have the 'thumbwhere_api_key' as we are going to pass that through in a context
	//
  if (variable_get('thumbwhere_api_key') == '') {
    throw new Exception('ThumbWhere resource calls for \'HostSubRole\' requires that variable \'thumbwhere_api_key\' is set.');
  }

	//
	// Ensure that we have the 'thumbwhere_api_key' as we are going to pass that through in a context
	//
  if (!is_numeric(variable_get('thumbwhere_host_id'))) {
    throw new Exception('ThumbWhere resource calls for \'HostSubRole\' requires that variable \'thumbwhere_host_id\' is set.');
  }


              		
    //
    // If this hostsubrole entity is new....
    //
               
    if ($entity->is_new) {
                

      //
      // ... then we create the hostsubrole entity in ThumbWhere
      //

      $hostsubrole = $twAdminAPI->create_host_role_subrole($context = array(
                  'key' => variable_get('thumbwhere_api_key'),
                  'origin' => variable_get('thumbwhere_host_id'),
          ), $fields);

      $attributes = $hostsubrole->body->host_role_subrole->attributes();

      //if (twCanDebug()) debug('newly created id = ' . t($attributes['id']));

      // Assign new id.
      $entity->pk_hostsubrole = intval($attributes['id']);

      //if (twCanDebug()) debug(t('$entity->pk_hostsubrole = ' . $entity->pk_hostsubrole));
    }
    else
    {
          	

      //
      // .. otherwise we update the hostsubrole entity in ThumbWhere
      //

      $hostsubrole = $twAdminAPI->update_host_role_subrole($id=$entity->pk_hostsubrole,$context = array(
                  'key' => variable_get('thumbwhere_api_key'),
                  'origin' => variable_get('thumbwhere_host_id'),
          ), $fields);
            
        
    }
          	
    
  }
}

/**
 * Implements thumbwhere_hostsubrole_entity_insert().
 *
 * Act on entities when inserted.
 *
 * @param $entity
 *   The entity object
 * @param $type
 *   The type of entity being inserted (i.e. node, user, comment).
 */
function thumbwhere_hostsubrole_entity_insert($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_hostsubrole') return;

}


/**
* Implements thumbwhere_hostsubrole_entity_delete().
*
* Act on entities when inserted.
*
* @param $entity
*   The entity object
* @param $type
*   The type of entity being deleted (i.e. node, user, comment).
*/
function thumbwhere_hostsubrole_entity_delete($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_hostsubrole') return;


}
