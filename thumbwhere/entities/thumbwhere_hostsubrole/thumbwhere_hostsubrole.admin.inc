<?php

/**
 * @file
 * ThumbWhereHostSubRole editing UI.
 *
 * We make very little use of the EntityAPI interface for this - preferring instead to use
 * views. That offers more flexibility to change a UI that will, more often than not,
 * be end-user facing.
 *
 * NOTE: This file was generated by autogen_model_implementation_drupal.xsl
 */

 
/**
 * Handle the notification for content_ingext
 */
function _thumbwhere_handle_host_role_subrole_notify_xml($notification) {

  //if (twCanDebug()) debug('PROCESS NOTIFICATION');
  //if (twCanDebug()) debug($notification);

  //
  // Get the operation
  //

  if (!isset($notification->attributes()->op)) {
    drupal_add_http_header('Status', '501 unable to fulfil request..');
    $err = 'We were expecting an \'op\', attribute on the \'host_role_subrole\', element.';
    print($err);
   	watchdog('thumbwhere_hostsubrole', $err ,array(), WATCHDOG_ERROR);
    exit(0);
  }

  $op = $notification->attributes()->op;

  //if (twCanDebug()) debug('op');
  //if (twCanDebug()) debug($op);

  //
  // If this is a notification then $op should be 'notify'
  //

  if (!isset($notification->attributes()->notification)) {
    drupal_add_http_header('Status', '501 unable to fulfil request..');
    $err = 'We were expecting an \'notification\', attribute on the \'host_role_subrole\', element.';
    print($err);
   	watchdog('thumbwhere_hostsubrole', $err ,array(), WATCHDOG_ERROR);
    exit(0);
  }

  $notification_op = $notification->attributes()->notification;

  //if (twCanDebug()) debug('notification');
  //if (twCanDebug()) debug($notification_op);


  //
  // Get the context
  //

  if (!isset($notification->attributes()->key)) {
    drupal_add_http_header('Status', '412 unable to fulfil request..');
    $err = 'We were expecting a \'key\', attribute on the \'host_role_subrole\', element.';
    print($err);
    watchdog('thumbwhere_hostsubrole', $err ,array(), WATCHDOG_ERROR);
    exit(0);
  }

  $context_key =  $notification->attributes()->key;

  // Start a transaction
  //$transaction = db_transaction('hostsubrole_notify');
  $transaction = null;

  //
  // Now based on the operation..
  //

  switch($notification_op)
  {
  
  
      case 'create':
      {
      		
		
		    //
		    // We want to time this
		    //
			
		    $start_time = microtime(TRUE);
			
        //
        // What resource are we creating?
        //
      
        //
        // Get the id
        //
		
        if (!isset($notification->attributes()->id)) {
          drupal_add_http_header('Status', '412 Unable to fulfil request.');
          $err = 'We were expecting an \'id\', attribute on the \'host_role_subrole\', element when we are handling a \'create\' notification..';
          print($err);
          watchdog('thumbwhere_hostsubrole', $err ,array(), WATCHDOG_ERROR);
          exit(0);
        }

        $id = intval($notification->attributes()->id);

        //if (twCanDebug()) debug('id');
        //if (twCanDebug()) debug(t($id));

        //
        // Get the origin
        //
		
		    $origin = -1;
		    
        if (isset($notification->attributes()->origin)) {        
          $origin = intval($notification->attributes()->origin);

          //if (twCanDebug()) debug('origin');
          //if (twCanDebug()) debug(t($origin));

          if ($origin == variable_get('thumbwhere_host_id',-1)) {
            $err = 'Origin of this notification ' . $origin  . ' is the same as this host. \'host_role_subrole\', Dropping this \'create\' notification.';
            watchdog('thumbwhere_hostsubrole', $err ,array(), WATCHDOG_ERROR);
              
            // We act all idempotent like its all good. 'create', when it already exists, is ignored. 
            drupal_add_http_header('Content-Type', 'text/xml');
            print('<response>');
            print('<host_role_subrole op="' . $op . '" id="' . $id . '" origin="' . $origin . '" >');
            print('<status>success</status>');
            print('</host_role_subrole>');
            print('</response>');
            exit(0);               
          }
        }
              
        watchdog('thumbwhere_hostsubrole', 'Notification: \'create\' hostsubrole #<a href="/admin/thumbwhere/thumbwhere_hostsubroles/thumbwhere_hostsubrole/' . $id .  '/edit">\'' . $id . '\'</a>  origin=\'' . $origin . '\'.',array(), WATCHDOG_NOTICE);
      
        //
        // Get the fields
        //
          
        $entities = entity_load('thumbwhere_hostsubrole', array($id));

        if (count($entities) != 0) {
          //drupal_add_http_header('Status', '409 Resource already exists.');
          $err = 'Error while servicing \'create\' notification. Resource \'host_role_subrole\' already exists with an id of \'' . $id . '\'.';
           //print($err);
          // When ORIGIN (TW-171) is sorted out then this can be re-anbled
          watchdog('thumbwhere_hostsubrole', $err ,array(), WATCHDOG_ERROR);

          	
          // We act all idempotent like its all good. 'create', when it already exists, is ignored. 
          drupal_add_http_header('Content-Type', 'text/xml');
          print('<response>');
          print('<host_role_subrole op="' . $op . '" id="' . $id . '" origin="' . $origin . '" >');
          print('<status>success</status>');
          print('</host_role_subrole>');
          print('</response>');
          exit(0);          	
          
        }

        // Getting a reference to the entity we asked for
        $entityfields = array();

        //
        // We are only updating what has been passed through.
        // No test for missing values.
        // TODO: Validate incoming values.
        //


        $entityfields['pk_hostsubrole'] = $id;	

        if (isset($notification->subroletype)) {
          $entityfields['fk_subroletype'] = intval($notification->subroletype);
        }

        if (isset($notification->hostrole)) {
          $entityfields['fk_hostrole'] = intval($notification->hostrole);
        }

        if (!isset($notification->attributes()->key)) {
                  }

        // We are being notified, so we do not want to ship this out again as a notification...
        // Finally write the entity.
        $entity = entity_create('thumbwhere_hostsubrole', $entityfields);
        $entity->do_not_propagate = TRUE;
        $entity->save($transaction);

        //
        // Send the final response
        //
        
        $duration = round(((microtime(TRUE) - $start_time) * 1000.0),3); 

        drupal_add_http_header('Content-Type', 'text/xml');
        print('<response duration="' . $duration . 'ms">');
        print('<host_role_subrole op="' . $op . '" id="' . $id . '" origin="' . $origin . '" >');
        print('<status>success</status>');
        print('</host_role_subrole>');
        print('</response>');
        exit(0);			
		
      break;
      }
  
  
    case 'update':
      {
      
    		//
    		// We want to time this
    		//
    			
    		$start_time = microtime(TRUE);
      
        //
        // What resource are we updating?
        //
        
      
        //
        // Get the id
        //
		
        if (!isset($notification->attributes()->id)) {
          drupal_add_http_header('Status', '412 Unable to fulfil request.');
          $err = 'We were expecting an \'id\', attribute on the \'host_role_subrole\', element when we are performing an \'update\' notification..';
          print($err);
          watchdog('thumbwhere_hostsubrole', $err ,array(), WATCHDOG_ERROR);
          exit(0);
        }

        $id = intval($notification->attributes()->id);
		

        //if (twCanDebug()) debug('id');
        //if (twCanDebug()) debug($id);

            
        //
        // Get the origin
        //
		
		    $origin = -1;
		    
        if (isset($notification->attributes()->origin)) {                
          $origin = intval($notification->attributes()->origin);

          //if (twCanDebug()) debug('origin');
          //if (twCanDebug()) debug($origin);

          if ($origin == variable_get('thumbwhere_host_id',-1)) {
            $err = 'Origin of this notification ' . $origin  . ' is the same as this host. \'host_role_subrole\', Dropping this \'update\' notification.';
            watchdog('thumbwhere_hostsubrole', $err ,array(), WATCHDOG_ERROR);
              
            // We act all idempotent like its all good. 'create', when it already exists, is ignored. 
            drupal_add_http_header('Content-Type', 'text/xml');
            print('<response>');
            print('<host_role_subrole op="' . $op . '" id="' . $id . '" origin="' . $origin . '">');
            print('<status>success</status>');
            print('</host_role_subrole>');
            print('</response>');
            exit(0);               
          }
        }
          
        watchdog('thumbwhere_hostsubrole', 'Notification: \'' . $notification_op . '\' hostsubrole #<a href="/admin/thumbwhere/thumbwhere_hostsubroles/thumbwhere_hostsubrole/' . $id .  '/edit"> \'' . $id .  '\'</a>  origin=\'' . $origin . '\'.',array(), WATCHDOG_NOTICE);
       
        //
        // Get the fields
        //
          
        $entities = entity_load('thumbwhere_hostsubrole', array($id));

        if (count($entities) == 0) {
          //drupal_add_http_header('Status', '409 No such resource exists.');
          $err = 'No such \'host_role_subrole\' resource exists with an id of \'' . $id . '\'.';
          //print($err);
          watchdog('thumbwhere_hostsubrole',$err,array(), WATCHDOG_ERROR);
          //exit(0);

////////////////////////////////////////////////////////////////////////
//
// ... for now we will just create the local copy
//
          watchdog('thumbwhere_hostsubrole','creating local version of missing entity',array(), WATCHDOG_ERROR);
		
		
		    //
		    // We want to time this
		    //
			
		    $start_time = microtime(TRUE);
			
        //
        // What resource are we creating?
        //
      
        //
        // Get the id
        //
		
        if (!isset($notification->attributes()->id)) {
          drupal_add_http_header('Status', '412 Unable to fulfil request.');
          $err = 'We were expecting an \'id\', attribute on the \'host_role_subrole\', element when we are handling a \'update\' notification..';
          print($err);
          watchdog('thumbwhere_hostsubrole', $err ,array(), WATCHDOG_ERROR);
          exit(0);
        }

        $id = intval($notification->attributes()->id);

        //if (twCanDebug()) debug('id');
        //if (twCanDebug()) debug(t($id));

        //
        // Get the origin
        //
		
		    $origin = -1;
		    
        if (isset($notification->attributes()->origin)) {        
          $origin = intval($notification->attributes()->origin);

          //if (twCanDebug()) debug('origin');
          //if (twCanDebug()) debug(t($origin));

          if ($origin == variable_get('thumbwhere_host_id',-1)) {
            $err = 'Origin of this notification ' . $origin  . ' is the same as this host. \'host_role_subrole\', Dropping this \'update\' notification.';
            watchdog('thumbwhere_hostsubrole', $err ,array(), WATCHDOG_ERROR);
              
            // We act all idempotent like its all good. 'create', when it already exists, is ignored. 
            drupal_add_http_header('Content-Type', 'text/xml');
            print('<response>');
            print('<host_role_subrole op="' . $op . '" id="' . $id . '" origin="' . $origin . '" >');
            print('<status>success</status>');
            print('</host_role_subrole>');
            print('</response>');
            exit(0);               
          }
        }
              
        watchdog('thumbwhere_hostsubrole', 'Notification: \'update\' hostsubrole #<a href="/admin/thumbwhere/thumbwhere_hostsubroles/thumbwhere_hostsubrole/' . $id .  '/edit">\'' . $id . '\'</a>  origin=\'' . $origin . '\'.',array(), WATCHDOG_NOTICE);
      
        //
        // Get the fields
        //
          
        $entities = entity_load('thumbwhere_hostsubrole', array($id));

        if (count($entities) != 0) {
          //drupal_add_http_header('Status', '409 Resource already exists.');
          $err = 'Error while servicing \'update\' notification. Resource \'host_role_subrole\' already exists with an id of \'' . $id . '\'.';
           //print($err);
          // When ORIGIN (TW-171) is sorted out then this can be re-anbled
          watchdog('thumbwhere_hostsubrole', $err ,array(), WATCHDOG_ERROR);

          	
          // We act all idempotent like its all good. 'create', when it already exists, is ignored. 
          drupal_add_http_header('Content-Type', 'text/xml');
          print('<response>');
          print('<host_role_subrole op="' . $op . '" id="' . $id . '" origin="' . $origin . '" >');
          print('<status>success</status>');
          print('</host_role_subrole>');
          print('</response>');
          exit(0);          	
          
        }

        // Getting a reference to the entity we asked for
        $entityfields = array();

        //
        // We are only updating what has been passed through.
        // No test for missing values.
        // TODO: Validate incoming values.
        //


        $entityfields['pk_hostsubrole'] = $id;	

        if (isset($notification->subroletype)) {
          $entityfields['fk_subroletype'] = intval($notification->subroletype);
        }

        if (isset($notification->hostrole)) {
          $entityfields['fk_hostrole'] = intval($notification->hostrole);
        }

        if (!isset($notification->attributes()->key)) {
                  }

        // We are being notified, so we do not want to ship this out again as a notification...
        // Finally write the entity.
        $entity = entity_create('thumbwhere_hostsubrole', $entityfields);
        $entity->do_not_propagate = TRUE;
        $entity->save($transaction);

        //
        // Send the final response
        //
        
        $duration = round(((microtime(TRUE) - $start_time) * 1000.0),3); 

        drupal_add_http_header('Content-Type', 'text/xml');
        print('<response duration="' . $duration . 'ms">');
        print('<host_role_subrole op="' . $op . '" id="' . $id . '" origin="' . $origin . '" >');
        print('<status>success</status>');
        print('</host_role_subrole>');
        print('</response>');
        exit(0);			
		
//
//
//
////////////////////////////////////////////////////////////////////////
        }

        // Getting a reference to the entity we asked for
        $entity = $entities[$id];

        //
        // We are only updating what has been passed through.
        // No test for missing values.
        // TODO: Validate incoming values.
        //


        if (isset($notification->subroletype)) {
          $entity->fk_subroletype = intval($notification->subroletype);
        }

        if (isset($notification->hostrole)) {
          $entity->fk_hostrole = intval($notification->hostrole);
        }

        if (!isset($notification->attributes()->key)) {
                  }

        // We are being notified, so we do not want to ship this out again as a notification...
        $entity->do_not_propagate = TRUE;

        // Finally write the entity.
        //entity_save('thumbwhere_hostsubrole', $entity);
        $entity->save($transaction);

        //
        // Send the final response
        //
        
        $duration = round(((microtime(TRUE) - $start_time) * 1000.0),3);

        drupal_add_http_header('Content-Type', 'text/xml');
        print('<response  duration="' . $duration . '" >');
        print('<host_role_subrole op="' . $op . '" notification="' . $notification_op . '" id="' . $id . '" >');
        print('<status>success</status>');
        print('</host_role_subrole>');
        print('</response>');
        exit(0);
      }

    default:
      {
        drupal_add_http_header('Status', '501 Unable to fulfil request. Unhandled operation \'' . $op . '\'.');
        print('We don\'t handle the \'' . $notification_op . '\', notification operation on the \'host_role_subrole\', element.');
        exit(0);
      }
  }
}



/**
 * Menu callback: React to a notification for hostsubrole.
 */
function _thumbwhere_host_role_subrole_notify() {

  
  if (twCanDebug()) {
    watchdog('thumbwhere_host_role_subrole', 'incoming notification.');
  }

  $numargs = func_num_args();
  $arg_list = func_get_args();

  //////////////////////////////////////////////////
  //
  //

  switch ($_SERVER['REQUEST_METHOD']) {

    case 'POST': {
        if ($numargs == 1) {
          if ($arg_list[0] == 'host_role_subrole') {

            //
            // Get the POST data as XML
            //

            $post_xml = new SimpleXMLElement('php://input', LIBXML_NOWARNING | (defined('LIBXML_COMPACT') ? LIBXML_COMPACT : 0), true);

            // We expect 'action' as the root element. TODO: I would rather this was the more generic 'request'
            if ($post_xml->getName() != 'action') {
              drupal_add_http_header('Status', '404 not found.');
              print('We were expecting \'action\', as the root element, not \'' . $post_xml->getName() . '\'.');
              exit(0);
            }

            /*
             *  SimpleXMLElement::__set_state(array(
             *      'host_role_subrole' =>
             *      SimpleXMLElement::__set_state(array(
             *       '@attributes' =>
             *      array (
             *        'key' => '8e9ff318-3cd7-4593-99de-4c37c0fb335a',
             *        'op' => 'create',
             *      ),
             *    )),
             *  ))
             */


            // We expect 'action' as the root element.
            if (!isset($post_xml->host_role_subrole)) {
              drupal_add_http_header('Status', '404 not found.');
              print('We were expecting a \'host_role_subrole\', element in the action payload.');
              exit(0);
            }

            // Handle the incoming notification for content ingest
            _thumbwhere_handle_host_role_subrole_notify_xml($post_xml->host_role_subrole);

          }
          else {
            drupal_add_http_header('Status', '404 not found.');
            print('Invalid resource');
            exit(0);
          }
        }
        else {
          drupal_add_http_header('Status', '404 not found.');
          print('Invalid notification request url format.');
          exit(0);
        }
      }
      break;

    case 'GET': {

        if ($numargs == 1) {

          // If we are returning the index.xml file for the webservice.
          if ($arg_list[0] == 'index.xml') {
            // return index.xml
            drupal_add_http_header('Content-Type', 'text/xml');
            readfile(dirname(__FILE__) . '/index.xml');
            exit(0);
          }
          else {
            drupal_add_http_header('Status', '404 not found.');
            print('File not found.');
            exit(0);
          }
        }
        else {
          drupal_add_http_header('Status', '404 not found.');
          print('Invalid notification request url format.');
          exit(0);
        }
      }
      break;

    default: {
        drupal_add_http_header('Status', '404 not found.');
        exit(0);
      }
      break;
  }

  //
  //
  ////////////////////////////////////////////////////////////////////////////////////
 
}





/**
 * HostSubRole UI controller.
 */
class ThumbWhereHostSubRoleUIController extends EntityDefaultUIController {

  /**
   * Overrides hook_menu() defaults. Main reason for doing this is that
   * parent class hook_menu() is optimized for entity type administration.
   */
  public function hook_menu() {

    $items = array();
    
    // Add a notification page...
    $items['thumbwhere/host_role_subrole/notify'] = array(
        'title' => 'Notifications Callback for "HostSubRole" Entity',
        'page callback' => '_thumbwhere_host_role_subrole_notify',
        'access arguments' => array(
            'send thumbwhere hostsubrole notifications'
        ),
        'file' => 'thumbwhere_hostsubrole.admin.inc',
        'file path' => drupal_get_path('module', $this->entityInfo['module'])
    );    
    
    $id_count = count(explode('/', $this->path));
    $wildcard = isset($this->entityInfo['admin ui']['menu wildcard']) ? $this->entityInfo['admin ui']['menu wildcard'] : '%' . $this->entityType;

    $items[$this->path] = array(
      'title' => 'HostSubRole',
      'description' => 'Add edit and update thumbwhere_hostsubroles entities.',
      'page callback' => 'system_admin_menu_block_page',
      'access arguments' => array('access administration pages'),
      'file path' => drupal_get_path('module', 'system'),
      'file' => 'system.admin.inc',
    );

    // Change the overview menu type for the list of thumbwhere_hostsubroles.
    $items[$this->path]['type'] = MENU_LOCAL_TASK;

    // Change the add page menu to multiple types of entities
    $items[$this->path . '/add'] = array(
      //'title' => 'Add a HostSubRole',
      'title' => 'Add',
      'description' => 'Add a new HostSubRole',
      'page callback' => 'thumbwhere_hostsubrole_form_wrapper',
      'page arguments' => array(thumbwhere_hostsubrole_create(array('type' => 'thumbwhere_hostsubrole'))),
      'access callback' => 'thumbwhere_hostsubrole_access',
      'access arguments' => array('edit', 'edit ' . 'thumbwhere_hostsubrole'),
      'file' => 'thumbwhere_hostsubrole.admin.inc',
      'file path' => drupal_get_path('module', $this->entityInfo['module'])
    );


    // Loading and editing thumbwhere_hostsubrole entities
    $items[$this->path . '/thumbwhere_hostsubrole/' . $wildcard] = array(
      'page callback' => 'thumbwhere_hostsubrole_form_wrapper',
      'page arguments' => array($id_count + 1),
      'access callback' => 'thumbwhere_hostsubrole_access',
      'access arguments' => array('edit', $id_count + 1),
      'weight' => 0,
      'context' => MENU_CONTEXT_PAGE | MENU_CONTEXT_INLINE,
      'file' => 'thumbwhere_hostsubrole.admin.inc',
      'file path' => drupal_get_path('module', $this->entityInfo['module'])
    );
    
    $items[$this->path . '/thumbwhere_hostsubrole/' . $wildcard . '/edit'] = array(
      'title' => 'Edit',
      'access callback' => 'thumbwhere_hostsubrole_access',
      'access arguments' => array('edit', $id_count + 1),      
      'title' => 'Edit',
      'type' => MENU_DEFAULT_LOCAL_TASK,
      'weight' => -10,
      'context' => MENU_CONTEXT_PAGE | MENU_CONTEXT_INLINE,
    );

    $items[$this->path . '/thumbwhere_hostsubrole/' . $wildcard . '/delete'] = array(
      'title' => 'Delete',
      'page callback' => 'thumbwhere_hostsubrole_delete_form_wrapper',
      'page arguments' => array($id_count + 1),
      'access callback' => 'thumbwhere_hostsubrole_access',
      'access arguments' => array('edit', $id_count + 1),
      'type' => MENU_LOCAL_TASK,
      'context' => MENU_CONTEXT_INLINE,
      'weight' => 10,
      'file' => 'thumbwhere_hostsubrole.admin.inc',
      'file path' => drupal_get_path('module', $this->entityInfo['module'])
    );

    // Menu item for viewing thumbwhere_hostsubroles
    /*
    $items['thumbwhere_hostsubrole/' . $wildcard] = array(
      //'title' => 'Title',
      'title callback' => 'thumbwhere_hostsubrole_page_title',
      'title arguments' => array(1),
      'page callback' => 'thumbwhere_hostsubrole_page_view',
      'page arguments' => array(1),
      'access callback' => 'thumbwhere_hostsubrole_access',
      'access arguments' => array('view', 1),
      'type' => MENU_CALLBACK,
    );
    */
    return $items;
  }


  /**
   * Create the markup for the add ThumbWhereHostSubRole Entities page within the class
   * so it can easily be extended/overriden.
   */
  public function addPage() {
    $item = menu_get_item();
    $content = system_admin_menu_block($item);

    if (count($content) == 1) {
      $item = array_shift($content);
      drupal_goto($item['href']);
    }

    return theme('thumbwhere_hostsubrole_add_list', array('content' => $content));
  }

}


/**
 * Form callback wrapper: create or edit a thumbwhere_hostsubrole.
 *
 * @param $thumbwhere_hostsubrole
 *   The thumbwhere_hostsubrole object being edited by this form.
 *
 * @see thumbwhere_hostsubrole_edit_form()
 */
function thumbwhere_hostsubrole_form_wrapper($thumbwhere_hostsubrole) {
  // Add the breadcrumb for the form's location.
  thumbwhere_hostsubrole_set_breadcrumb();
  return drupal_get_form('thumbwhere_hostsubrole_edit_form', $thumbwhere_hostsubrole);
}


/**
 * Form callback wrapper: delete a thumbwhere_hostsubrole.
 *
 * @param $thumbwhere_hostsubrole
 *   The thumbwhere_hostsubrole object being edited by this form.
 *
 * @see thumbwhere_hostsubrole_edit_form()
 */
function thumbwhere_hostsubrole_delete_form_wrapper($thumbwhere_hostsubrole) {
  // Add the breadcrumb for the form's location.
  //thumbwhere_hostsubrole_set_breadcrumb();
  return drupal_get_form('thumbwhere_hostsubrole_delete_form', $thumbwhere_hostsubrole);
}


/**
 * Form callback: create or edit a thumbwhere_hostsubrole.
 *
 * @param $thumbwhere_hostsubrole
 *   The thumbwhere_hostsubrole object to edit or for a create form an empty thumbwhere_hostsubrole object
 *     with only a thumbwhere_hostsubrole type defined.
 */
function thumbwhere_hostsubrole_edit_form($form, &$form_state, $thumbwhere_hostsubrole) {

  // Add the default field elements.
  $form['fk_subroletype'] = array(
    '#type' => 'textfield',
    '#title' => t('ThumbWhereHostSubRole fk_subroletype'),
    '#default_value' => isset($thumbwhere_hostsubrole->fk_subroletype) ? $thumbwhere_hostsubrole->fk_subroletype : '',
    //'#maxlength' => 255,
   /// '#required' => TRUE,
    '#weight' => -5,
    '#autocomplete_path' => 'entity-autocomplete/thumbwhere_hostsubroletype',
  );
  // Add the default field elements.
  $form['fk_hostrole'] = array(
    '#type' => 'textfield',
    '#title' => t('ThumbWhereHostSubRole fk_hostrole'),
    '#default_value' => isset($thumbwhere_hostsubrole->fk_hostrole) ? $thumbwhere_hostsubrole->fk_hostrole : '',
    //'#maxlength' => 255,
   /// '#required' => TRUE,
    '#weight' => -5,
    '#autocomplete_path' => 'entity-autocomplete/thumbwhere_hostrole',
  );



  $form['data']['#tree'] = TRUE;

  /*
  $form['data']['sample_data'] = array(
    '#type' => 'checkbox',
    '#title' => t('An interesting thumbwhere_hostsubrole switch'),
    '#default_value' => isset($thumbwhere_hostsubrole->data['sample_data']) ? $thumbwhere_hostsubrole->data['sample_data'] : 1,
  );
  */


  // Add the field related form elements.
  $form_state['thumbwhere_hostsubrole'] = $thumbwhere_hostsubrole;
  field_attach_form('thumbwhere_hostsubrole', $thumbwhere_hostsubrole, $form, $form_state);

  $form['actions'] = array(
    '#type' => 'container',
    '#attributes' => array('class' => array('form-actions')),
    '#weight' => 400,
  );

  // We add the form's #submit array to this button along with the actual submit
  // handler to preserve any submit handlers added by a form callback_wrapper.
  $submit = array();

  if (!empty($form['#submit'])) {
    $submit += $form['#submit'];
  }

  $form['actions']['submit'] = array(
    '#type' => 'submit',
    '#value' => t('Save thumbwhere_hostsubrole'),
    '#submit' => $submit + array('thumbwhere_hostsubrole_edit_form_submit'),
  );

  // Do we show the delete button?
  if (!empty($thumbwhere_hostsubrole->pk_hostsubrole)) {
    $form['actions']['delete'] = array(
      '#type' => 'submit',
      '#value' => t('Delete thumbwhere_hostsubrole'),
      '#suffix' => l(t('Cancel'), 'admin/thumbwhere/thumbwhere_hostsubroles'),
      '#submit' => $submit + array('thumbwhere_hostsubrole_form_submit_delete'),
      '#weight' => 45,
    );
  }

  // We append the validate handler to #validate in case a form callback_wrapper
  // is used to add validate handlers earlier.
  $form['#validate'][] = 'thumbwhere_hostsubrole_edit_form_validate';
  return $form;
}


/**
 * Form API validate callback for the thumbwhere_hostsubrole form
 */
function thumbwhere_hostsubrole_edit_form_validate(&$form, &$form_state) {


  // No validation for pk


  // Convert fk autocomplete for fk_subroletype
  $value = $form['fk_subroletype']['#value'];
  // If we have something, escape the auto-completion encoding.
  if (!empty($value)) { 
    $value = entity_autocomplete_get_id($value);
    form_set_value($form['fk_subroletype'], $value,$form_state);
  }
  //if (twCanDebug()) debug(t($form['fk_subroletype']));
   //if (twCanDebug()) debug(t('fk_subroletype = ' . $value));
  // Validate fk fk_subroletype
  if (empty($value)) {
    form_set_error('fk_subroletype', t('Validation error, \'fk_subroletype\' is mandatory1.'));
    // throw new Exception('Field \'fk_subroletype\' in is mandatory');
  }

  // Convert fk autocomplete for fk_hostrole
  $value = $form['fk_hostrole']['#value'];
  // If we have something, escape the auto-completion encoding.
  if (!empty($value)) { 
    $value = entity_autocomplete_get_id($value);
    form_set_value($form['fk_hostrole'], $value,$form_state);
  }
  //if (twCanDebug()) debug(t($form['fk_hostrole']));
   //if (twCanDebug()) debug(t('fk_hostrole = ' . $value));
  // Validate fk fk_hostrole


              

  //form_set_value( array('#parents' => array('array_key_parent', 'array_key_to_replace')) , $value, $form_state);

   
   
  $thumbwhere_hostsubrole = $form_state['thumbwhere_hostsubrole'];

  // Notify field widgets to validate their data.
  field_attach_form_validate('thumbwhere_hostsubrole', $thumbwhere_hostsubrole, $form, $form_state);
    
    
  
}


/**
 * Form API submit callback for the thumbwhere_hostsubrole form.
 *
 * @todo remove hard-coded link
 */
function thumbwhere_hostsubrole_edit_form_submit(&$form, &$form_state) {

  $thumbwhere_hostsubrole = entity_ui_controller('thumbwhere_hostsubrole')->entityFormSubmitBuildEntity($form, $form_state);
  // Save the thumbwhere_hostsubrole and go back to the list of thumbwhere_hostsubroles

  // Add in created and changed times.
  if ($thumbwhere_hostsubrole->is_new = isset($thumbwhere_hostsubrole->is_new) ? $thumbwhere_hostsubrole->is_new : 0) {
    $thumbwhere_hostsubrole->created = time();
  }

  $thumbwhere_hostsubrole->changed = time();

  $thumbwhere_hostsubrole->save();
  $form_state['redirect'] = 'admin/thumbwhere/thumbwhere_hostsubroles';
}

/**
 * Form API submit callback for the delete button.
 *
 * @todo Remove hard-coded path
 */
function thumbwhere_hostsubrole_form_submit_delete(&$form, &$form_state) {
  $form_state['redirect'] = 'admin/thumbwhere/thumbwhere_hostsubroles/thumbwhere_hostsubrole/' . $form_state['thumbwhere_hostsubrole']->pk_hostsubrole . '/delete';
}





/**
 * Form callback: confirmation form for deleting a thumbwhere_hostsubrole.
 *
 * @param $thumbwhere_hostsubrole
 *   The thumbwhere_hostsubrole to delete
 *
 * @see confirm_form()
 */
function thumbwhere_hostsubrole_delete_form($form, &$form_state, $thumbwhere_hostsubrole) {
  $form_state['thumbwhere_hostsubrole'] = $thumbwhere_hostsubrole;

  $form['#submit'][] = 'thumbwhere_hostsubrole_delete_form_submit';

  $form = confirm_form($form,
    t('Are you sure you want to delete thumbwhere_hostsubrole %name?', array('%name' => $thumbwhere_hostsubrole->pk_subrole)),
    'admin/thumbwhere/thumbwhere_hostsubroles/thumbwhere_hostsubrole',
    '<p>' . t('This action cannot be undone.') . '</p>',
    t('Delete'),
    t('Cancel'),
    'confirm'
  );

  return $form;
}

/**
 * Submit callback for thumbwhere_hostsubrole_delete_form
 */
function thumbwhere_hostsubrole_delete_form_submit($form, &$form_state) {
  $thumbwhere_hostsubrole = $form_state['thumbwhere_hostsubrole'];

  thumbwhere_hostsubrole_delete($thumbwhere_hostsubrole);

  drupal_set_message(t('The thumbwhere_hostsubrole %name has been deleted.', array('%name' => $thumbwhere_hostsubrole->pk_subrole)));
  watchdog('thumbwhere_hostsubrole', 'Deleted thumbwhere_hostsubrole %name.', array('%name' => $thumbwhere_hostsubrole->pk_subrole));

  $form_state['redirect'] = 'admin/thumbwhere/thumbwhere_hostsubroles';
}



/**
 * Page to add ThumbWhereHostSubRole Entities.
 *
 * @todo Pass this through a proper theme function
 */
function thumbwhere_hostsubrole_add_page() {
  $controller = entity_ui_controller('thumbwhere_hostsubrole');
  return $controller->addPage();
}


/**
 * Displays the list of available thumbwhere_hostsubrole types for thumbwhere_hostsubrole creation.
 *
 * @ingroup themeable
 */
function theme_thumbwhere_hostsubrole_add_list($variables) {


  //$content = $variables['content'];
  $output = '';
  /*
  if ($content) {
    $output = '<dl class="thumbwhere_hostsubrole-type-list">';
    foreach ($content as $item) {
      $output .= '<dt>' . l($item['title'], $item['href']) . '</dt>';
      $output .= '<dd>' . filter_xss_admin($item['description']) . '</dd>';
    }
    $output .= '</dl>';
  }
  else {
    if (user_access('administer thumbwhere_hostsubrole types')) {
      $output = '<p>' . t('ThumbWhereHostSubRole Entities cannot be added because you have not created any thumbwhere_hostsubrole types yet. Go to the <a href="@create-thumbwhere_hostsubrole-type">thumbwhere_hostsubrole type creation page</a> to add a new thumbwhere_hostsubrole type.', array('@create-thumbwhere_hostsubrole-type' => url('admin/structure/thumbwhere_hostsubrole_types/add'))) . '</p>';
    }
    else {
      $output = '<p>' . t('No thumbwhere_hostsubrole types have been created yet for you to use.') . '</p>';
    }
  }
  */
  return $output;
}





/**
 * Sets the breadcrumb for administrative thumbwhere_hostsubrole pages.
 */
function thumbwhere_hostsubrole_set_breadcrumb() {
  $breadcrumb = array(
    l(t('Home'), '<front>'),
    l(t('Administration'), 'admin'),
    l(t('HostSubRole'), 'admin/content'),
    l(t('HostSubRole'), 'admin/thumbwhere/thumbwhere_hostsubroles'),
  );

  drupal_set_breadcrumb($breadcrumb);
}



