<?php

/**
 * @file
 * Sets up the base table for our entity and a table to store information about
 * the entity types.
 *
 * NOTE: This has been autogenerated.
 */

/**
 * Implements hook_uninstall().
 *
 * This hook is called when the module is installed.
 *
 * @ingroup thumbwhere_trophy
 */
function thumbwhere_trophy_uninstall() {

  // Delete the permissions
  foreach (_thumbwhere_trophy_get_permissions() as $role_name => $permissions) {
    // Find the role ID for this role
    $role = user_role_load_by_name($role_name);
    if ($role != false) {
      $rid = $role->rid;
      // Set the permissions for this role
      user_role_revoke_permissions($rid, $permissions);
    }
  }


  //
  // Uninstall all the field info
  //

  // Uninstall the Menu TODO: Do we even install a menu???
  if (module_exists('menu')) {
    if ($thumbwhere_trophy_menu = menu_load('thumbwhere_trophy')) {
      menu_delete($thumbwhere_trophy_menu);
    }
  }

  // Clear the everything cache
  drupal_flush_all_caches();

  // Rebuild the menu
  menu_rebuild();

}

/**
 * Implements hook_install().
 *
 * This hook is called when the module is installed.
 *
 * @ingroup thumbwhere_trophy
 */
function thumbwhere_trophy_install() {

 // Create the permissions
  foreach (_thumbwhere_trophy_get_permissions() as $role_name => $permissions) {
    // Find the role ID for this role
    $role = user_role_load_by_name($role_name);
    if ($role != false) {
      $rid = $role->rid;
      // Set the permissions for this role
      user_role_grant_permissions($rid, $permissions);
    }
  }


  // Clear the everything cache
  drupal_flush_all_caches();

  // Rebuild the menu
  menu_rebuild();

  // Entity provided by ThumbWhere gets a weight of 12. Storage = 11 and ThumbWere itself, 10
  //db_query("UPDATE {system} SET weight = 12 WHERE name = 'thumbwhere_trophy'");

}

/**
 * Implements hook_enable().
 *
 * Called when the module is enabled
 *
 * @ingroup thumbwhere_trophy
 */
function thumbwhere_trophy_enable() {

  // Clear the everything cache
  drupal_flush_all_caches();

  // Rebuild the menu
  menu_rebuild();

}

/**
 * Implements hook_disable().
 *
 * Called when the module is disabled
 *
 * @ingroup thumbwhere_trophy
 */
function thumbwhere_trophy_disable() {

  // Clear the everything cache
  drupal_flush_all_caches();

  // Rebuild the menu
  menu_rebuild();
}

/**
 * Implements hook_schema().
 *
 *
 * This is where we define the schema for our entity.
 *
 * This results in a table being created called 'thumbwhere_trophy'
 *
 * thumbwhere_trophy_id
 * The primary key used by
 *
 * pk_trophy
 * Used to map to the actual object in ThumbWhere.
 */
function thumbwhere_trophy_schema() {
  $t = get_t();

  $schema = array();

  $schema['thumbwhere_trophy'] = array(
  'description' => 'The base table for thumbwhere_trophy entities.' . 'singleton: There can only be one of these awarded, so if someone wins this, someone else will lose it. name: The name of the trophy.',
    'fields' => array(
          'pk_trophy' => array(
              'description' => 'ThumbWhere Id: ThumbWhere\'s Primary key this entity. Represents the id of this entity in the ThumbWhere \'ThumbWhereTrophys\' entity.',
              'type' => 'int',
              'size' => 'big',
              'not null' => TRUE,
              'default' => -1,
          ),
          'singleton' => array(
              'description' => 'The \'singleton\' checkbox field of the \'Trophy\' entity.',
              'type' => 'int',
              'size' => 'tiny',
              'not null' => FALSE,                  
          ),
          'name' => array(
              'description' => 'The \'name\' textfield field of the \'Trophy\' entity.',
              'type' => 'varchar',
              'size' => 'normal',
              'length' => 128,
              'not null' => FALSE,                  
          ),      
      ),
      'primary key' => array(
          'pk_trophy'      
      ),      
      'indexes' => array(
          'pk_trophy' => array(
              'pk_trophy'
          ),
      
      ),
  );

  return $schema;
}


/**
 * Returns a structured array defining the permission for each role.
 *
 * @return
 *  An associative array specifying the permissions for each role
 *
 * @ingroup track
 */
function _thumbwhere_trophy_get_permissions() {
  return array(
      'anonymous user' => array(
          'send thumbwhere trophy notifications',
      ),
      'authenticated user' => array(
          'send thumbwhere trophy notifications',
      ),      
      'administrator' => array(
          'send thumbwhere trophy notifications',
      ),
  );
}


