<?php

// ******************************************************************
// This file was generated by autogen_model_implementation_drupal.xsl
// ******************************************************************

// Include the ThumbWhere SDK
require_once(dirname(__FILE__) . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . 'dependencies' . DIRECTORY_SEPARATOR . 'tw' . DIRECTORY_SEPARATOR . 'sdk.class.php');

/**
 * @file
 * Module for the ThumbWhere Trophy Entity
 *
 *
 * NOTE: This has been autogenerated.
 * 
 */

/**
 * Implement hook_entity_info().
 *
 * We define two entities here - the actual entity that will hold our domain
 * specific information and an entity that holds information about the different
 * types of entities. See here: http://drupal.org/node/977380 for a discussion on this
 * choice.
 */
function thumbwhere_trophy_entity_info() {
  $return['thumbwhere_trophy'] = array(

    // The human-readable name of the type.
    'label' => t('ThumbWhereTrophy'),

    // (used by DrupalDefaultEntityController) The name of the entity type's base table.
    'base table' => 'thumbwhere_trophy',

    // fieldable: Set to TRUE if you want your entity type to accept fields being attached to it.
    'fieldable' => TRUE,

  // Declare the endty keys..
    'entity keys' => array(
      'id' => 'pk_trophy',
      'label' => 'name',
    ),

    // A function taking an entity and an entity type as arguments and returning the label of the entity.
    // The entity label is the main string associated with an entity; for example, the title of a node or
    // the subject of a comment. If there is an entity object property that defines the label, use the
    // 'label' element of the 'entity keys' return value component to provide this information (see below).
    // If more complex logic is needed to determine the label of an entity, you can instead specify a callback
    // function here, which will be called to determine the entity label. See also the entity_label()
    // function, which implements this logic.
    'label callback' => 'entity_class_label',

    // A function taking an entity as argument and returning the uri elements of the entity, e.g. 'path' and 'options'.
    // The actual entity uri can be constructed by passing these elements to url().
    'uri callback' => 'entity_class_uri',

    //
    // Entity API Components
    //

    // The module whwre we can find our classes
    'module' => 'thumbwhere_trophy',

    // The entity class and controller class extend the classes provided by the Entity API
    'entity class' => 'ThumbWhereTrophy',

    // Which Entity API contoller
    'controller class' => 'ThumbWhereTrophyController',

    // The information below is used by the ThumbWhereTrophyUIController (which extends the EntityDefaultUIController)
    'admin ui' => array(
      'path' => 'admin/thumbwhere/thumbwhere_trophys',
      'file' => 'thumbwhere_trophy.admin.inc',
      'controller class' => 'ThumbWhereTrophyUIController',
      'menu wildcard' => '%thumbwhere_trophy',
    ),
  );

  return $return;
}


/**
 * Implements hook_permission().
 */
function thumbwhere_trophy_permission() {

  // We set up permisssions to manage entity types, manage all entities and the
  // permissions for each individual entity
  $permissions = array(
    'administer thumbwhere_trophys' => array(
      'title' => t('Administer thumbwhere_trophys'),
      'description' => t('Edit and delete all thumbwhere_trophys'),
    ),
  'send thumbwhere trophy notifications' => array(
          'title' => t('Send ThumbWhere Notifications'),
          'description' => t('Allow the caller to call the ThumbWhere Trophy Entity Notifications Endpoint.'),
          'restrict access' => TRUE,
      ),    
  );

  return $permissions;
}


/**
 * Determines whether the given user has access to a thumbwhere_trophy.
 *
 * @param $op
 *   The operation being performed. One of 'view', 'update', 'create', 'delete'
 *   or just 'edit' (being the same as 'create' or 'update').
 * @param $thumbwhere_trophy
 *   Optionally a thumbwhere_trophy or a thumbwhere_trophy type to check access for. If nothing is
 *   given, access for all thumbwhere_trophys is determined.
 * @param $account
 *   The user to check for. Leave it to NULL to check for the global user.
 * @return boolean
 *   Whether access is allowed or not.
 */
function thumbwhere_trophy_access($op, $thumbwhere_trophy = NULL, $account = NULL) {

  //if (user_access('administer thumbwhere_trophys', $account)) {
  //  return TRUE;
 // }

  //return FALSE;
  return TRUE;
}




/**
 * Fetch a thumbwhere_trophy object. Make sure that the wildcard you choose
 * in the thumbwhere_trophy entity definition fits the function name here.
 *
 * @param $thumbwhere_trophy_id
 *   Integer specifying the thumbwhere_trophy id.
 * @param $reset
 *   A boolean indicating that the internal cache should be reset.
 * @return
 *   A fully-loaded $thumbwhere_trophy object or FALSE if it cannot be loaded.
 *
 * @see thumbwhere_trophy_load_multiple()
 */
function thumbwhere_trophy_load($thumbwhere_trophy_id, $reset = FALSE) {

  $thumbwhere_trophys = entity_load('thumbwhere_trophy', array($thumbwhere_trophy_id), array(), $reset);

  return reset($thumbwhere_trophys);
}


/**
 * Deletes a thumbwhere_trophy.
 */
function thumbwhere_trophy_delete(ThumbWhereTrophy $thumbwhere_trophy) {

  $thumbwhere_trophy->delete();
}

/**
 * Create a thumbwhere_trophy object.
 */
function thumbwhere_trophy_create($values = array()) {

  return entity_get_controller('thumbwhere_trophy')->create($values);
}


/**
 * URI callback for thumbwhere_trophys
 */
function thumbwhere_trophy_uri(ThumbWhereTrophy $thumbwhere_trophy) {

  return array(
    'path' => 'thumbwhere_trophy/' . $thumbwhere_trophy->pk_trophy,
  );
}


/**
 * Menu title callback for showing individual entities
 */
function thumbwhere_trophy_page_title(ThumbWhereTrophy $thumbwhere_trophy) {

	return $thumbwhere_trophy->name;
}


/**
 * Sets up content to show an individual thumbwhere_trophy
 * @todo - get rid of drupal_set_title();
 */
function thumbwhere_trophy_page_view($thumbwhere_trophy, $view_mode = 'full') {



  $controller = entity_get_controller('thumbwhere_trophy');
  $content = $controller->view(array($thumbwhere_trophy->pk_trophy => $thumbwhere_trophy));
  drupal_set_title($thumbwhere_trophy->name);
  return $content;
}


/**
 * Implements hook_views_api().
 */
function thumbwhere_trophy_views_api() {



  return array(
    'api' => 3,
    'path' => drupal_get_path('module', 'thumbwhere_trophy') . '/views',
  );
}


/**
 * Implement hook_theme().
 */
function thumbwhere_trophy_theme() {



  return array(
    'thumbwhere_trophy_add_list' => array(
      'variables' => array('content' => array()),
      'file' => 'thumbwhere_trophy.admin.inc',
    ),
    'thumbwhere_trophy' => array(
      'render element' => 'elements',
      'template' => 'thumbwhere_trophy',
    )
  );
}


/**
 * Implements hook_menu_local_tasks_alter().
 */
function thumbwhere_trophy_menu_local_tasks_alter(&$data, $router_item, $root_path) {

  // Add action link 'admin/structure/thumbwhere_trophys/add' on 'admin/structure/thumbwhere_trophys'.
  if ($root_path == 'admin/thumbwhere/thumbwhere_trophys') {
    $item = menu_get_item('admin/thumbwhere/thumbwhere_trophys/add');
    if ($item['access']) {
      $data['actions']['output'][] = array(
        '#theme' => 'menu_local_action',
        '#link' => $item,
      );
    }
  }
}


/**
 * The class used for thumbwhere_trophy entities
 */
class ThumbWhereTrophy extends Entity {

  public function __construct($values = array()) {
    parent::__construct($values, 'thumbwhere_trophy');
  }

  protected function defaultLabel() {
    return $this->name;
  }

  protected function defaultUri() {
    return array('path' => 'thumbwhere_trophy/' . $this->pk_trophy);
  }


}



/**
 * The Controller for ThumbWhereTrophy entities
 */
class ThumbWhereTrophyController extends EntityAPIController {
  public function __construct($entityType) {
    parent::__construct($entityType);
  }
}



/**
* Implements hook_entity_presave().
*
* Act on an Trophy entity before it is about to be created or updated.
*
* @param $entity
*   The entity object.
* @param $entity_type
*   The type of entity being saved (i.e. node, user, comment).
*/
function thumbwhere_trophy_entity_presave($entity, $entity_type) {

  // We only look after what we want...
  if ($entity_type == 'thumbwhere_trophy')  {
  	
  	
  	
 	/////////////////////////////////////////////////////
    //
    // Should we not propagate this to ThumbWhere? 
    //
    if (isset($entity->do_not_propagate)) {
      if ($entity->do_not_propagate) {
        if (twCanDebug()) {
          if ($entity->is_new) {
            debug("DO NOT PROPAGATE 'trophy' presave *CREATE*");
          }
          else {
            debug("DO NOT PROPAGATE 'trophy' presave *UPDATE*");
          }
        }

        // To cause an infinite loop of updates, comment out this line.
        return;
      }
      else if (twCanDebug()) {
        debug("OK PROAOGATE 'trophy' presave to ThumbWhere Server");
      }
    }
    else if (twCanDebug()) {
      debug("DO NOT PROPAGATE IS NOT SET");
    }
    //
    //
    ////////////////////////////////////////////////////////  	

  
    // .. then we create a new thumbwhere_trophy entity...
      // Instantiate the ThumbWhereContent class

    $twContentAPI = new ThumbWhereAPIContent();

    //if (twCanDebug()) debug($twContentAPI);

    //if (twCanDebug()) debug($entity);

    //
    // Collate and validate our fields
    //

    $fields = array();
    // Compulsory so..
    if (!isset($entity->singleton)) {
      throw new Exception('singleton is mandatory.');
    }
    else {
      $fields['singleton'] = $entity->singleton;
    }
    // Compulsory so..
    if (empty($entity->name)) {
      throw new Exception('name is mandatory.');
    }
    else {
      $fields['name'] = $entity->name;
    }
		
	//
	// Ensure that we have the 'thumbwhere_api_key' as we are going to pass that through in a context
	//
    if (variable_get('thumbwhere_api_key') == '') {
    	throw new Exception('ThumbWhere resource for \'Trophy\' requires that variable \'thumbwhere_api_key\' is set.');
    }
    
              		
    //
    // If this trophy entity is new....
    //
               
    if ($entity->is_new) {
                

      //
      // ... then we create the trophy entity in ThumbWhere
      //

      $trophy = $twContentAPI->create_trophy($context = array(
                  'key' => variable_get('thumbwhere_api_key')
          ), $fields);
            
       if (twCanDebug()) debug($trophy);

      //if (twCanDebug()) debug($trophy);

      $attributes = $trophy->body->trophy->attributes();

      if (twCanDebug()) debug('newly created id = ' . $attributes['id']);

      // Assign new id.
      $entity->pk_trophy = intval($attributes['id']);

      if (twCanDebug()) debug('$entity->pk_trophy = ' . $entity->pk_trophy);
    }
    else
    {
          	

      //
      // .. otherwise we update the trophy entity in ThumbWhere
      //

      $trophy = $twContentAPI->update_trophy($id=$entity->pk_trophy,$context = array(
                  'key' => variable_get('thumbwhere_api_key')
          ), $fields);
            
        
    }
          	
    
  }
}

/**
 * Implements thumbwhere_trophy_entity_insert().
 *
 * Act on entities when inserted.
 *
 * @param $entity
 *   The entity object
 * @param $type
 *   The type of entity being inserted (i.e. node, user, comment).
 */
function thumbwhere_trophy_entity_insert($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_trophy') return;

}


/**
* Implements thumbwhere_trophy_entity_delete().
*
* Act on entities when inserted.
*
* @param $entity
*   The entity object
* @param $type
*   The type of entity being deleted (i.e. node, user, comment).
*/
function thumbwhere_trophy_entity_delete($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_trophy') return;


}
