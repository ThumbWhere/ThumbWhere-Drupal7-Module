<?php

// ******************************************************************
// This file was generated by autogen_model_implementation_drupal.xsl
// ******************************************************************

// Include the ThumbWhere SDK
require_once(dirname(__FILE__) . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . 'dependencies' . DIRECTORY_SEPARATOR . 'tw' . DIRECTORY_SEPARATOR . 'sdk.class.php');

/**
 * @file
 * Module for the ThumbWhere Fan Entity
 *
 *
 * NOTE: This has been autogenerated.
 * 
 */

/**
 * Implement hook_entity_info().
 *
 * We define two entities here - the actual entity that will hold our domain
 * specific information and an entity that holds information about the different
 * types of entities. See here: http://drupal.org/node/977380 for a discussion on this
 * choice.
 */
function thumbwhere_fan_entity_info() {
  $return['thumbwhere_fan'] = array(

    // The human-readable name of the type.
    'label' => t('ThumbWhereFan'),

    // (used by DrupalDefaultEntityController) The name of the entity type's base table.
    'base table' => 'thumbwhere_fan',

    // fieldable: Set to TRUE if you want your entity type to accept fields being attached to it.
    'fieldable' => TRUE,

  // Declare the endty keys..
    'entity keys' => array(
      'id' => 'pk_fan',
      'label' => 'pk_fan',
    ),

    // A function taking an entity and an entity type as arguments and returning the label of the entity.
    // The entity label is the main string associated with an entity; for example, the title of a node or
    // the subject of a comment. If there is an entity object property that defines the label, use the
    // 'label' element of the 'entity keys' return value component to provide this information (see below).
    // If more complex logic is needed to determine the label of an entity, you can instead specify a callback
    // function here, which will be called to determine the entity label. See also the entity_label()
    // function, which implements this logic.
    'label callback' => 'entity_class_label',

    // A function taking an entity as argument and returning the uri elements of the entity, e.g. 'path' and 'options'.
    // The actual entity uri can be constructed by passing these elements to url().
    'uri callback' => 'entity_class_uri',

    //
    // Entity API Components
    //

    // The module whwre we can find our classes
    'module' => 'thumbwhere_fan',

    // The entity class and controller class extend the classes provided by the Entity API
    'entity class' => 'ThumbWhereFan',

    // Which Entity API contoller
    'controller class' => 'ThumbWhereFanController',

    // The information below is used by the ThumbWhereFanUIController (which extends the EntityDefaultUIController)
    'admin ui' => array(
      'path' => 'admin/thumbwhere/thumbwhere_fans',
      'file' => 'thumbwhere_fan.admin.inc',
      'controller class' => 'ThumbWhereFanUIController',
      'menu wildcard' => '%thumbwhere_fan',
    ),
  );

  return $return;
}


/**
 * Implements hook_permission().
 */
function thumbwhere_fan_permission() {

  // We set up permisssions to manage entity types, manage all entities and the
  // permissions for each individual entity
  $permissions = array(
    'administer thumbwhere_fans' => array(
      'title' => t('Administer thumbwhere_fans'),
      'description' => t('Administer all thumbwhere_fans entities'),
      //'restrict access' => TRUE,
    ),
  'send thumbwhere fan notifications' => array(
      'title' => t('Send ThumbWhere Notifications'),
      'description' => t('Allow the caller to call the ThumbWhere Fan Entity Notifications Endpoint.'),
      //'restrict access' => TRUE,
    ),    
  );

  return $permissions;
}


/**
 * Determines whether the given user has access to a thumbwhere_fan.
 *
 * @param $op
 *   The operation being performed. One of 'view', 'update', 'create', 'delete'
 *   or just 'edit' (being the same as 'create' or 'update').
 * @param $thumbwhere_fan
 *   Optionally a thumbwhere_fan or a thumbwhere_fan type to check access for. If nothing is
 *   given, access for all thumbwhere_fans is determined.
 * @param $account
 *   The user to check for. Leave it to NULL to check for the global user.
 * @return boolean
 *   Whether access is allowed or not.
 */
function thumbwhere_fan_access($op, $thumbwhere_fan = NULL, $account = NULL) {

  if (user_access('administer thumbwhere_fans', $account)) {
    return TRUE;
  }
 
  return FALSE;
}




/**
 * Fetch a thumbwhere_fan object. Make sure that the wildcard you choose
 * in the thumbwhere_fan entity definition fits the function name here.
 *
 * @param $thumbwhere_fan_id
 *   Integer specifying the thumbwhere_fan id.
 * @param $reset
 *   A boolean indicating that the internal cache should be reset.
 * @return
 *   A fully-loaded $thumbwhere_fan object or FALSE if it cannot be loaded.
 *
 * @see thumbwhere_fan_load_multiple()
 */
function thumbwhere_fan_load($thumbwhere_fan_id, $reset = FALSE) {

  $thumbwhere_fans = entity_load('thumbwhere_fan', array($thumbwhere_fan_id), array(), $reset);

  return reset($thumbwhere_fans);
}


/**
 * Deletes a thumbwhere_fan.
 */
function thumbwhere_fan_delete(ThumbWhereFan $thumbwhere_fan) {

  $thumbwhere_fan->delete();
}

/**
 * Create a thumbwhere_fan object.
 */
function thumbwhere_fan_create($values = array()) {

  return entity_get_controller('thumbwhere_fan')->create($values);
}


/**
 * URI callback for thumbwhere_fans
 */
function thumbwhere_fan_uri(ThumbWhereFan $thumbwhere_fan) {

  return array(
    'path' => 'thumbwhere_fan/' . $thumbwhere_fan->pk_fan,
  );
}


/**
 * Menu title callback for showing individual entities
 */
function thumbwhere_fan_page_title(ThumbWhereFan $thumbwhere_fan) {

	return $thumbwhere_fan->pk_fan;
}


/**
 * Sets up content to show an individual thumbwhere_fan
 * @todo - get rid of drupal_set_title();
 */
function thumbwhere_fan_page_view($thumbwhere_fan, $view_mode = 'full') {



  $controller = entity_get_controller('thumbwhere_fan');
  $content = $controller->view(array($thumbwhere_fan->pk_fan => $thumbwhere_fan));
  drupal_set_title($thumbwhere_fan->pk_fan);
  return $content;
}


/**
 * Implements hook_views_api().
 */
function thumbwhere_fan_views_api() {



  return array(
    'api' => 3,
    'path' => drupal_get_path('module', 'thumbwhere_fan') . '/views',
  );
}


/**
 * Implement hook_theme().
 */
function thumbwhere_fan_theme() {



  return array(
    'thumbwhere_fan_add_list' => array(
      'variables' => array('content' => array()),
      'file' => 'thumbwhere_fan.admin.inc',
    ),
    'thumbwhere_fan' => array(
      'render element' => 'elements',
      'template' => 'thumbwhere_fan',
    )
  );
}


/**
 * Implements hook_menu_local_tasks_alter().
 */
function thumbwhere_fan_menu_local_tasks_alter(&$data, $router_item, $root_path) {

  // Add action link 'admin/structure/thumbwhere_fans/add' on 'admin/structure/thumbwhere_fans'.
  if ($root_path == 'admin/thumbwhere/thumbwhere_fans') {
    $item = menu_get_item('admin/thumbwhere/thumbwhere_fans/add');
    if ($item['access']) {
      $data['actions']['output'][] = array(
        '#theme' => 'menu_local_action',
        '#link' => $item,
      );
    }
  }
}


/**
 * The class used for thumbwhere_fan entities
 */
class ThumbWhereFan extends Entity {

  public function __construct($values = array()) {
    parent::__construct($values, 'thumbwhere_fan');
  }

  protected function defaultLabel() {
    return $this->pk_fan;
  }

  protected function defaultUri() {
    return array('path' => 'thumbwhere_fan/' . $this->pk_fan);
  }


}



/**
 * The Controller for ThumbWhereFan entities
 */
class ThumbWhereFanController extends EntityAPIController {
  public function __construct($entityType) {
    parent::__construct($entityType);
  }
}



/**
* Implements hook_entity_presave().
*
* Act on an Fan entity before it is about to be created or updated.
*
* @param $entity
*   The entity object.
* @param $entity_type
*   The type of entity being saved (i.e. node, user, comment).
*/
function thumbwhere_fan_entity_presave($entity, $entity_type) {

  // We only look after what we want...
  if ($entity_type == 'thumbwhere_fan')  {
  	
    // Should we not propagate this to ThumbWhere? 
    if (isset($entity->do_not_propagate)) {
      if ($entity->do_not_propagate) {
        // To cause an infinite loop of updates, comment out this line.
        return;
      }
    }

  
    // .. then we create a new thumbwhere_fan entity...
      // Instantiate the ThumbWhereContent class

    $twContentAPI = new ThumbWhereAPIContent();


    //
    // Collate and validate our fields
    //

    $fields = array();
    // Compulsory so..
    if (!isset($entity->fk_actor)) {
      throw new Exception('actor is mandatory.');
    }
    else {
      $fields['actor'] = $entity->fk_actor;
    }
    // Compulsory so..
    if (!isset($entity->fk_thing)) {
      throw new Exception('thing is mandatory.');
    }
    else {
      $fields['thing'] = $entity->fk_thing;
    }

    // Nullable so we only pass it through to create_fan if we have it..
    if (isset($entity->fk_place)) {
      $fields['place'] = $entity->fk_place;
    }
    // Compulsory so..
    if (empty($entity->documentation)) {
      throw new Exception('documentation is mandatory.');
    }
    else {
      $fields['documentation'] = $entity->documentation;
    }
		
	//
	// Ensure that we have the 'thumbwhere_api_key' as we are going to pass that through in a context
	//
  if (variable_get('thumbwhere_api_key') == '') {
    throw new Exception('ThumbWhere resource calls for \'Fan\' requires that variable \'thumbwhere_api_key\' is set.');
  }

	//
	// Ensure that we have the 'thumbwhere_api_key' as we are going to pass that through in a context
	//
  if (!is_numeric(variable_get('thumbwhere_host_id'))) {
    throw new Exception('ThumbWhere resource calls for \'Fan\' requires that variable \'thumbwhere_host_id\' is set.');
  }


              		
    //
    // If this fan entity is new....
    //
               
    if ($entity->is_new) {
                

      //
      // ... then we create the fan entity in ThumbWhere
      //

      $fan = $twContentAPI->create_fan($context = array(
                  'key' => variable_get('thumbwhere_api_key'),
                  'origin' => variable_get('thumbwhere_host_id'),
          ), $fields);

      $attributes = $fan->body->fan->attributes();

      //if (twCanDebug()) debug('newly created id = ' . t($attributes['id']));

      // Assign new id.
      $entity->pk_fan = intval($attributes['id']);

      //if (twCanDebug()) debug(t('$entity->pk_fan = ' . $entity->pk_fan));
    }
    else
    {
          	

      //
      // .. otherwise we update the fan entity in ThumbWhere
      //

      $fan = $twContentAPI->update_fan($id=$entity->pk_fan,$context = array(
                  'key' => variable_get('thumbwhere_api_key'),
                  'origin' => variable_get('thumbwhere_host_id'),
          ), $fields);
            
        
    }
          	
    
  }
}

/**
 * Implements thumbwhere_fan_entity_insert().
 *
 * Act on entities when inserted.
 *
 * @param $entity
 *   The entity object
 * @param $type
 *   The type of entity being inserted (i.e. node, user, comment).
 */
function thumbwhere_fan_entity_insert($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_fan') return;

}


/**
* Implements thumbwhere_fan_entity_delete().
*
* Act on entities when inserted.
*
* @param $entity
*   The entity object
* @param $type
*   The type of entity being deleted (i.e. node, user, comment).
*/
function thumbwhere_fan_entity_delete($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_fan') return;


}
