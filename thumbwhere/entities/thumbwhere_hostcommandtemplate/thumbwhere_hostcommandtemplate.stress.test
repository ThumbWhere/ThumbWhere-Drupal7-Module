<?php

/**
 * @file
 * Stress tests for the HostCommandTemplate module
 *
 *
 * NOTE: This has been autogenerated.
 * 
 */

class ThumbWhereHostCommandTemplateStressTestCase extends DrupalWebTestCase {

  protected $privileged_user;

  public function getInfo() {
    return array(
        'name' => 'HostCommandTemplate Entity Stress Test',
        'description' => 'Creates HostCommandTemplates in lots of 10, 100(disabled), 1000(disabled) and outputs the times take and the average rate of creation.',
        'group' => 'ThumbWhereStress',
    );
  }

  public function setUp() {

    // Make sure we are using the testing profile
    $this->profile = 'testing';
    
    // We need temporary folder
    static $temporary_directory;
    $temporary_directory = 'C:\\TEMP\\';    

    // Set the critial thumbwhere config 
    variable_set('thumbwhere_api_url', 'localhost.thumbwhere.com');
    variable_set('thumbwhere_api_version', 'v1.1');
    variable_set('thumbwhere_api_key', '80bb71a1-131c-43c2-9bac-e27006051cd0');
    variable_set('thumbwhere_master_console', '0');
    variable_set('thumbwhere_api_log_debug', '0');
    variable_set('thumbwhere_api_log_trace', '0');
    variable_set('thumbwhere_machine_id', 1);    

    // Enable the module.
    parent::setUp('thumbwhere_hostcommandtemplate');
    	
    	

    $this->privileged_user = $this->drupalCreateUser(array());
    $this->drupalLogin($this->privileged_user);
  }

  /**
   * Create 100 contents..
   */
  function testStress100() {

    // Set the critial thumbwhere config 
    variable_set('thumbwhere_api_url', 'localhost.thumbwhere.com');
    variable_set('thumbwhere_api_version', 'v1.1');
    variable_set('thumbwhere_api_key', '80bb71a1-131c-43c2-9bac-e27006051cd0');
    variable_set('thumbwhere_master_console', '0');
    variable_set('thumbwhere_api_log_debug', '0');
    variable_set('thumbwhere_api_log_trace', '0');
    variable_set('thumbwhere_machine_id', 1);    


    $n = 100;

    $time_start = microtime(TRUE);
    
    $total = 0;
   

    for ($i = 0; $i < $n; $i++) {

      $entity = _thumbwhere_stresstest_create_HostCommandTemplate($this->privileged_user->uid,$this);
	
      if ($entity == null) {
        break;
      }
	  

      $entity->save();
      
      $total++;

    }
    
    if ($total < $n)
    {
    	$this->assertTrue(TRUE, 'Bailed because we can not create dependencies.');
    	return;
    }

    $time_end = microtime(TRUE);
    $time = $time_end - $time_start;

    $result = TRUE;
    $message = t("Created $total HostCommandTemplates in $time seconds = " . $total / $time . ' contents per second.');

    if (function_exists('drush_log')) {
      drush_log($message, 'metric');
      drush_log("##teamcity[buildStatisticValue key='hostcommandtemplateCreate" . $total . "' value='" . $total / $time . "']");
    }
    
    $this->assertTrue($result, $message);



  }


 
}
     

function _thumbwhere_stresstest_create_HostCommandTemplate($uid,$test) {

    global $_in_thumbwhere_stresstest_create_HostCommandTemplate;
    $_in_thumbwhere_stresstest_create_HostCommandTemplate = TRUE;
	
    //
    // Global values that we will use...
    //
    
    global $_global_thumbwhere_stresstest_HostRoleType;
    if (!isset( $_global_thumbwhere_stresstest_HostRoleType)){
      if (function_exists ('_thumbwhere_stresstest_create_HostRoleType')) {
        $_global_thumbwhere_stresstest_HostRoleType = _thumbwhere_stresstest_create_HostRoleType($uid,$test);
      }
      else {
        debug('bailing - function \'_thumbwhere_stresstest_create_HostRoleType\' is not loaded yet.');
        return NULL;
      }
    }
    global $_global_thumbwhere_stresstest_HostCommandTemplateType;
    if (!isset( $_global_thumbwhere_stresstest_HostCommandTemplateType)){
      if (function_exists ('_thumbwhere_stresstest_create_HostCommandTemplateType')) {
        $_global_thumbwhere_stresstest_HostCommandTemplateType = _thumbwhere_stresstest_create_HostCommandTemplateType($uid,$test);
      }
      else {
        debug('bailing - function \'_thumbwhere_stresstest_create_HostCommandTemplateType\' is not loaded yet.');
        return NULL;
      }
    }
          		
          		
    $entity = entity_create('thumbwhere_hostcommandtemplate', array(
        'uid' => $uid,
    		'name' => $test->randomName(),
        'hostroletype' => $_global_thumbwhere_stresstest_HostRoleType,
        'hostcommandtemplatetype' => $_global_thumbwhere_stresstest_HostCommandTemplateType,

      ));  
      
    $_in_thumbwhere_stresstest_create_HostCommandTemplate = FALSE;
    return $entity;
}
