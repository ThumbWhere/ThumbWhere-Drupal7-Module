<?php

// ******************************************************************
// This file was generated by autogen_model_implementation_drupal.xsl
// ******************************************************************

// Include the ThumbWhere SDK
require_once(dirname(__FILE__) . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . 'dependencies' . DIRECTORY_SEPARATOR . 'tw' . DIRECTORY_SEPARATOR . 'sdk.class.php');

/**
 * @file
 * Module for the ThumbWhere HostCommandTemplate Entity
 *
 *
 * NOTE: This has been autogenerated.
 * 
 */

/**
 * Implement hook_entity_info().
 *
 * We define two entities here - the actual entity that will hold our domain
 * specific information and an entity that holds information about the different
 * types of entities. See here: http://drupal.org/node/977380 for a discussion on this
 * choice.
 */
function thumbwhere_hostcommandtemplate_entity_info() {
  $return['thumbwhere_hostcommandtemplate'] = array(

    // The human-readable name of the type.
    'label' => t('ThumbWhereHostCommandTemplate'),

    // (used by DrupalDefaultEntityController) The name of the entity type's base table.
    'base table' => 'thumbwhere_hostcommandtemplate',

    // fieldable: Set to TRUE if you want your entity type to accept fields being attached to it.
    'fieldable' => TRUE,

  // Declare the endty keys..
    'entity keys' => array(
      'id' => 'pk_hostcommandtemplate',
      'label' => 'name',
    ),

    // A function taking an entity and an entity type as arguments and returning the label of the entity.
    // The entity label is the main string associated with an entity; for example, the title of a node or
    // the subject of a comment. If there is an entity object property that defines the label, use the
    // 'label' element of the 'entity keys' return value component to provide this information (see below).
    // If more complex logic is needed to determine the label of an entity, you can instead specify a callback
    // function here, which will be called to determine the entity label. See also the entity_label()
    // function, which implements this logic.
    'label callback' => 'entity_class_label',

    // A function taking an entity as argument and returning the uri elements of the entity, e.g. 'path' and 'options'.
    // The actual entity uri can be constructed by passing these elements to url().
    'uri callback' => 'entity_class_uri',

    //
    // Entity API Components
    //

    // The module whwre we can find our classes
    'module' => 'thumbwhere_hostcommandtemplate',

    // The entity class and controller class extend the classes provided by the Entity API
    'entity class' => 'ThumbWhereHostCommandTemplate',

    // Which Entity API contoller
    'controller class' => 'ThumbWhereHostCommandTemplateController',

    // The information below is used by the ThumbWhereHostCommandTemplateUIController (which extends the EntityDefaultUIController)
    'admin ui' => array(
      'path' => 'admin/thumbwhere/thumbwhere_hostcommandtemplates',
      'file' => 'thumbwhere_hostcommandtemplate.admin.inc',
      'controller class' => 'ThumbWhereHostCommandTemplateUIController',
      'menu wildcard' => '%thumbwhere_hostcommandtemplate',
    ),
  );

  return $return;
}


/**
 * Implements hook_permission().
 */
function thumbwhere_hostcommandtemplate_permission() {

  // We set up permisssions to manage entity types, manage all entities and the
  // permissions for each individual entity
  $permissions = array(
    'administer thumbwhere_hostcommandtemplates' => array(
      'title' => t('Administer thumbwhere_hostcommandtemplates'),
      'description' => t('Administer all thumbwhere_hostcommandtemplates entities'),
      //'restrict access' => TRUE,
    ),
  'send thumbwhere hostcommandtemplate notifications' => array(
      'title' => t('Send ThumbWhere Notifications'),
      'description' => t('Allow the caller to call the ThumbWhere HostCommandTemplate Entity Notifications Endpoint.'),
      //'restrict access' => TRUE,
    ),    
  );

  return $permissions;
}


/**
 * Determines whether the given user has access to a thumbwhere_hostcommandtemplate.
 *
 * @param $op
 *   The operation being performed. One of 'view', 'update', 'create', 'delete'
 *   or just 'edit' (being the same as 'create' or 'update').
 * @param $thumbwhere_hostcommandtemplate
 *   Optionally a thumbwhere_hostcommandtemplate or a thumbwhere_hostcommandtemplate type to check access for. If nothing is
 *   given, access for all thumbwhere_hostcommandtemplates is determined.
 * @param $account
 *   The user to check for. Leave it to NULL to check for the global user.
 * @return boolean
 *   Whether access is allowed or not.
 */
function thumbwhere_hostcommandtemplate_access($op, $thumbwhere_hostcommandtemplate = NULL, $account = NULL) {

  if (user_access('administer thumbwhere_hostcommandtemplates', $account)) {
    return TRUE;
  }
 
  return FALSE;
}




/**
 * Fetch a thumbwhere_hostcommandtemplate object. Make sure that the wildcard you choose
 * in the thumbwhere_hostcommandtemplate entity definition fits the function name here.
 *
 * @param $thumbwhere_hostcommandtemplate_id
 *   Integer specifying the thumbwhere_hostcommandtemplate id.
 * @param $reset
 *   A boolean indicating that the internal cache should be reset.
 * @return
 *   A fully-loaded $thumbwhere_hostcommandtemplate object or FALSE if it cannot be loaded.
 *
 * @see thumbwhere_hostcommandtemplate_load_multiple()
 */
function thumbwhere_hostcommandtemplate_load($thumbwhere_hostcommandtemplate_id, $reset = FALSE) {

  $thumbwhere_hostcommandtemplates = entity_load('thumbwhere_hostcommandtemplate', array($thumbwhere_hostcommandtemplate_id), array(), $reset);

  return reset($thumbwhere_hostcommandtemplates);
}


/**
 * Deletes a thumbwhere_hostcommandtemplate.
 */
function thumbwhere_hostcommandtemplate_delete(ThumbWhereHostCommandTemplate $thumbwhere_hostcommandtemplate) {

  $thumbwhere_hostcommandtemplate->delete();
}

/**
 * Create a thumbwhere_hostcommandtemplate object.
 */
function thumbwhere_hostcommandtemplate_create($values = array()) {

  return entity_get_controller('thumbwhere_hostcommandtemplate')->create($values);
}


/**
 * URI callback for thumbwhere_hostcommandtemplates
 */
function thumbwhere_hostcommandtemplate_uri(ThumbWhereHostCommandTemplate $thumbwhere_hostcommandtemplate) {

  return array(
    'path' => 'thumbwhere_hostcommandtemplate/' . $thumbwhere_hostcommandtemplate->pk_hostcommandtemplate,
  );
}


/**
 * Menu title callback for showing individual entities
 */
function thumbwhere_hostcommandtemplate_page_title(ThumbWhereHostCommandTemplate $thumbwhere_hostcommandtemplate) {

	return $thumbwhere_hostcommandtemplate->name;
}


/**
 * Sets up content to show an individual thumbwhere_hostcommandtemplate
 * @todo - get rid of drupal_set_title();
 */
function thumbwhere_hostcommandtemplate_page_view($thumbwhere_hostcommandtemplate, $view_mode = 'full') {



  $controller = entity_get_controller('thumbwhere_hostcommandtemplate');
  $content = $controller->view(array($thumbwhere_hostcommandtemplate->pk_hostcommandtemplate => $thumbwhere_hostcommandtemplate));
  drupal_set_title($thumbwhere_hostcommandtemplate->name);
  return $content;
}


/**
 * Implements hook_views_api().
 */
function thumbwhere_hostcommandtemplate_views_api() {



  return array(
    'api' => 3,
    'path' => drupal_get_path('module', 'thumbwhere_hostcommandtemplate') . '/views',
  );
}


/**
 * Implement hook_theme().
 */
function thumbwhere_hostcommandtemplate_theme() {



  return array(
    'thumbwhere_hostcommandtemplate_add_list' => array(
      'variables' => array('content' => array()),
      'file' => 'thumbwhere_hostcommandtemplate.admin.inc',
    ),
    'thumbwhere_hostcommandtemplate' => array(
      'render element' => 'elements',
      'template' => 'thumbwhere_hostcommandtemplate',
    )
  );
}


/**
 * Implements hook_menu_local_tasks_alter().
 */
function thumbwhere_hostcommandtemplate_menu_local_tasks_alter(&$data, $router_item, $root_path) {

  // Add action link 'admin/structure/thumbwhere_hostcommandtemplates/add' on 'admin/structure/thumbwhere_hostcommandtemplates'.
  if ($root_path == 'admin/thumbwhere/thumbwhere_hostcommandtemplates') {
    $item = menu_get_item('admin/thumbwhere/thumbwhere_hostcommandtemplates/add');
    if ($item['access']) {
      $data['actions']['output'][] = array(
        '#theme' => 'menu_local_action',
        '#link' => $item,
      );
    }
  }
}


/**
 * The class used for thumbwhere_hostcommandtemplate entities
 */
class ThumbWhereHostCommandTemplate extends Entity {

  public function __construct($values = array()) {
    parent::__construct($values, 'thumbwhere_hostcommandtemplate');
  }

  protected function defaultLabel() {
    return $this->name;
  }

  protected function defaultUri() {
    return array('path' => 'thumbwhere_hostcommandtemplate/' . $this->pk_hostcommandtemplate);
  }


}



/**
 * The Controller for ThumbWhereHostCommandTemplate entities
 */
class ThumbWhereHostCommandTemplateController extends EntityAPIController {
  public function __construct($entityType) {
    parent::__construct($entityType);
  }
}



/**
* Implements hook_entity_presave().
*
* Act on an HostCommandTemplate entity before it is about to be created or updated.
*
* @param $entity
*   The entity object.
* @param $entity_type
*   The type of entity being saved (i.e. node, user, comment).
*/
function thumbwhere_hostcommandtemplate_entity_presave($entity, $entity_type) {

  // We only look after what we want...
  if ($entity_type == 'thumbwhere_hostcommandtemplate')  {
  	
    // Should we not propagate this to ThumbWhere? 
    if (isset($entity->do_not_propagate)) {
      if ($entity->do_not_propagate) {
        // To cause an infinite loop of updates, comment out this line.
        return;
      }
    }

  
    // .. then we create a new thumbwhere_hostcommandtemplate entity...
      // Instantiate the ThumbWhereContent class

    $twAdminAPI = new ThumbWhereAPIAdmin();


    //
    // Collate and validate our fields
    //

    $fields = array();
    // Compulsory so..
    if (empty($entity->name)) {
      throw new Exception('name is mandatory.');
    }
    else {
      $fields['name'] = $entity->name;
    }
    // Compulsory so..
    if (!isset($entity->fk_hostroletype)) {
      throw new Exception('hostroletype is mandatory.');
    }
    else {
      $fields['hostroletype'] = $entity->fk_hostroletype;
    }
    // Compulsory so..
    if (!isset($entity->fk_hostcommandtemplatetype)) {
      throw new Exception('hostcommandtemplatetype is mandatory.');
    }
    else {
      $fields['hostcommandtemplatetype'] = $entity->fk_hostcommandtemplatetype;
    }
		
	//
	// Ensure that we have the 'thumbwhere_api_key' as we are going to pass that through in a context
	//
  if (variable_get('thumbwhere_api_key') == '') {
    throw new Exception('ThumbWhere resource calls for \'HostCommandTemplate\' requires that variable \'thumbwhere_api_key\' is set.');
  }

	//
	// Ensure that we have the 'thumbwhere_api_key' as we are going to pass that through in a context
	//
  if (!is_numeric(variable_get('thumbwhere_host_id'))) {
    throw new Exception('ThumbWhere resource calls for \'HostCommandTemplate\' requires that variable \'thumbwhere_host_id\' is set.');
  }


              		
    //
    // If this hostcommandtemplate entity is new....
    //
               
    if ($entity->is_new) {
                

      //
      // ... then we create the hostcommandtemplate entity in ThumbWhere
      //

      $hostcommandtemplate = $twAdminAPI->create_host_command_template($context = array(
                  'key' => variable_get('thumbwhere_api_key'),
                  'origin' => variable_get('thumbwhere_host_id'),
          ), $fields);

      $attributes = $hostcommandtemplate->body->host_command_template->attributes();

      //if (twCanDebug()) debug('newly created id = ' . t($attributes['id']));

      // Assign new id.
      $entity->pk_hostcommandtemplate = intval($attributes['id']);

      //if (twCanDebug()) debug(t('$entity->pk_hostcommandtemplate = ' . $entity->pk_hostcommandtemplate));
    }
    else
    {
          	

      //
      // .. otherwise we update the hostcommandtemplate entity in ThumbWhere
      //

      $hostcommandtemplate = $twAdminAPI->update_host_command_template($id=$entity->pk_hostcommandtemplate,$context = array(
                  'key' => variable_get('thumbwhere_api_key'),
                  'origin' => variable_get('thumbwhere_host_id'),
          ), $fields);
            
        
    }
          	
    
  }
}

/**
 * Implements thumbwhere_hostcommandtemplate_entity_insert().
 *
 * Act on entities when inserted.
 *
 * @param $entity
 *   The entity object
 * @param $type
 *   The type of entity being inserted (i.e. node, user, comment).
 */
function thumbwhere_hostcommandtemplate_entity_insert($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_hostcommandtemplate') return;

}


/**
* Implements thumbwhere_hostcommandtemplate_entity_delete().
*
* Act on entities when inserted.
*
* @param $entity
*   The entity object
* @param $type
*   The type of entity being deleted (i.e. node, user, comment).
*/
function thumbwhere_hostcommandtemplate_entity_delete($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_hostcommandtemplate') return;


}
