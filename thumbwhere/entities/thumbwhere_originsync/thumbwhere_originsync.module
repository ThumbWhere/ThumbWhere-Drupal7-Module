<?php

// ******************************************************************
// This file was generated by autogen_model_implementation_drupal.xsl
// ******************************************************************

// Include the ThumbWhere SDK
require_once(dirname(__FILE__) . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . 'dependencies' . DIRECTORY_SEPARATOR . 'tw' . DIRECTORY_SEPARATOR . 'sdk.class.php');

/**
 * @file
 * Module for the ThumbWhere OriginSync Entity
 *
 *
 * NOTE: This has been autogenerated.
 * 
 */

/**
 * Implement hook_entity_info().
 *
 * We define two entities here - the actual entity that will hold our domain
 * specific information and an entity that holds information about the different
 * types of entities. See here: http://drupal.org/node/977380 for a discussion on this
 * choice.
 */
function thumbwhere_originsync_entity_info() {
  $return['thumbwhere_originsync'] = array(

    // The human-readable name of the type.
    'label' => t('ThumbWhereOriginSync'),

    // (used by DrupalDefaultEntityController) The name of the entity type's base table.
    'base table' => 'thumbwhere_originsync',

    // fieldable: Set to TRUE if you want your entity type to accept fields being attached to it.
    'fieldable' => TRUE,

  // Declare the endty keys..
    'entity keys' => array(
      'id' => 'pk_originsync',
      'label' => 'pk_originsync',
    ),

    // A function taking an entity and an entity type as arguments and returning the label of the entity.
    // The entity label is the main string associated with an entity; for example, the title of a node or
    // the subject of a comment. If there is an entity object property that defines the label, use the
    // 'label' element of the 'entity keys' return value component to provide this information (see below).
    // If more complex logic is needed to determine the label of an entity, you can instead specify a callback
    // function here, which will be called to determine the entity label. See also the entity_label()
    // function, which implements this logic.
    'label callback' => 'entity_class_label',

    // A function taking an entity as argument and returning the uri elements of the entity, e.g. 'path' and 'options'.
    // The actual entity uri can be constructed by passing these elements to url().
    'uri callback' => 'entity_class_uri',

    //
    // Entity API Components
    //

    // The module whwre we can find our classes
    'module' => 'thumbwhere_originsync',

    // The entity class and controller class extend the classes provided by the Entity API
    'entity class' => 'ThumbWhereOriginSync',

    // Which Entity API contoller
    'controller class' => 'ThumbWhereOriginSyncController',

    // The information below is used by the ThumbWhereOriginSyncUIController (which extends the EntityDefaultUIController)
    'admin ui' => array(
      'path' => 'admin/thumbwhere/thumbwhere_originsyncs',
      'file' => 'thumbwhere_originsync.admin.inc',
      'controller class' => 'ThumbWhereOriginSyncUIController',
      'menu wildcard' => '%thumbwhere_originsync',
    ),
  );

  return $return;
}


/**
 * Implements hook_permission().
 */
function thumbwhere_originsync_permission() {

  // We set up permisssions to manage entity types, manage all entities and the
  // permissions for each individual entity
  $permissions = array(
    'administer thumbwhere_originsyncs' => array(
      'title' => t('Administer thumbwhere_originsyncs'),
      'description' => t('Edit and delete all thumbwhere_originsyncs'),
    ),
  'send thumbwhere originsync notifications' => array(
          'title' => t('Send ThumbWhere Notifications'),
          'description' => t('Allow the caller to call the ThumbWhere OriginSync Entity Notifications Endpoint.'),
          'restrict access' => TRUE,
      ),    
  );

  return $permissions;
}


/**
 * Determines whether the given user has access to a thumbwhere_originsync.
 *
 * @param $op
 *   The operation being performed. One of 'view', 'update', 'create', 'delete'
 *   or just 'edit' (being the same as 'create' or 'update').
 * @param $thumbwhere_originsync
 *   Optionally a thumbwhere_originsync or a thumbwhere_originsync type to check access for. If nothing is
 *   given, access for all thumbwhere_originsyncs is determined.
 * @param $account
 *   The user to check for. Leave it to NULL to check for the global user.
 * @return boolean
 *   Whether access is allowed or not.
 */
function thumbwhere_originsync_access($op, $thumbwhere_originsync = NULL, $account = NULL) {

  //if (user_access('administer thumbwhere_originsyncs', $account)) {
  //  return TRUE;
 // }

  //return FALSE;
  return TRUE;
}




/**
 * Fetch a thumbwhere_originsync object. Make sure that the wildcard you choose
 * in the thumbwhere_originsync entity definition fits the function name here.
 *
 * @param $thumbwhere_originsync_id
 *   Integer specifying the thumbwhere_originsync id.
 * @param $reset
 *   A boolean indicating that the internal cache should be reset.
 * @return
 *   A fully-loaded $thumbwhere_originsync object or FALSE if it cannot be loaded.
 *
 * @see thumbwhere_originsync_load_multiple()
 */
function thumbwhere_originsync_load($thumbwhere_originsync_id, $reset = FALSE) {

  $thumbwhere_originsyncs = entity_load('thumbwhere_originsync', array($thumbwhere_originsync_id), array(), $reset);

  return reset($thumbwhere_originsyncs);
}


/**
 * Deletes a thumbwhere_originsync.
 */
function thumbwhere_originsync_delete(ThumbWhereOriginSync $thumbwhere_originsync) {

  $thumbwhere_originsync->delete();
}

/**
 * Create a thumbwhere_originsync object.
 */
function thumbwhere_originsync_create($values = array()) {

  return entity_get_controller('thumbwhere_originsync')->create($values);
}


/**
 * URI callback for thumbwhere_originsyncs
 */
function thumbwhere_originsync_uri(ThumbWhereOriginSync $thumbwhere_originsync) {

  return array(
    'path' => 'thumbwhere_originsync/' . $thumbwhere_originsync->pk_originsync,
  );
}


/**
 * Menu title callback for showing individual entities
 */
function thumbwhere_originsync_page_title(ThumbWhereOriginSync $thumbwhere_originsync) {

	return $thumbwhere_originsync->pk_originsync;
}


/**
 * Sets up content to show an individual thumbwhere_originsync
 * @todo - get rid of drupal_set_title();
 */
function thumbwhere_originsync_page_view($thumbwhere_originsync, $view_mode = 'full') {



  $controller = entity_get_controller('thumbwhere_originsync');
  $content = $controller->view(array($thumbwhere_originsync->pk_originsync => $thumbwhere_originsync));
  drupal_set_title($thumbwhere_originsync->pk_originsync);
  return $content;
}


/**
 * Implements hook_views_api().
 */
function thumbwhere_originsync_views_api() {



  return array(
    'api' => 3,
    'path' => drupal_get_path('module', 'thumbwhere_originsync') . '/views',
  );
}


/**
 * Implement hook_theme().
 */
function thumbwhere_originsync_theme() {



  return array(
    'thumbwhere_originsync_add_list' => array(
      'variables' => array('content' => array()),
      'file' => 'thumbwhere_originsync.admin.inc',
    ),
    'thumbwhere_originsync' => array(
      'render element' => 'elements',
      'template' => 'thumbwhere_originsync',
    )
  );
}


/**
 * Implements hook_menu_local_tasks_alter().
 */
function thumbwhere_originsync_menu_local_tasks_alter(&$data, $router_item, $root_path) {

  // Add action link 'admin/structure/thumbwhere_originsyncs/add' on 'admin/structure/thumbwhere_originsyncs'.
  if ($root_path == 'admin/thumbwhere/thumbwhere_originsyncs') {
    $item = menu_get_item('admin/thumbwhere/thumbwhere_originsyncs/add');
    if ($item['access']) {
      $data['actions']['output'][] = array(
        '#theme' => 'menu_local_action',
        '#link' => $item,
      );
    }
  }
}


/**
 * The class used for thumbwhere_originsync entities
 */
class ThumbWhereOriginSync extends Entity {

  public function __construct($values = array()) {
    parent::__construct($values, 'thumbwhere_originsync');
  }

  protected function defaultLabel() {
    return $this->pk_originsync;
  }

  protected function defaultUri() {
    return array('path' => 'thumbwhere_originsync/' . $this->pk_originsync);
  }


}



/**
 * The Controller for ThumbWhereOriginSync entities
 */
class ThumbWhereOriginSyncController extends EntityAPIController {
  public function __construct($entityType) {
    parent::__construct($entityType);
  }
}



/**
* Implements hook_entity_presave().
*
* Act on an OriginSync entity before it is about to be created or updated.
*
* @param $entity
*   The entity object.
* @param $entity_type
*   The type of entity being saved (i.e. node, user, comment).
*/
function thumbwhere_originsync_entity_presave($entity, $entity_type) {

  // We only look after what we want...
  if ($entity_type == 'thumbwhere_originsync')  {
  	
  	
  	
 	/////////////////////////////////////////////////////
    //
    // Should we not propagate this to ThumbWhere? 
    //
    if (isset($entity->do_not_propagate)) {
      if ($entity->do_not_propagate) {
        if (twCanDebug()) {
          if ($entity->is_new) {
            if (twCanDebug()){
              debug("DO NOT PROPAGATE 'originsync' presave *CREATE*");
          	}
          }
          else {
            if (twCanDebug()) {
              debug("DO NOT PROPAGATE 'originsync' presave *UPDATE*");
            }
          }
        }

        // To cause an infinite loop of updates, comment out this line.
        return;
      }
      else if (twCanDebug()) {
        debug("OK PROAOGATE 'originsync' presave to ThumbWhere Server");
      }
    }
    else if (twCanDebug()) {
      debug("DO NOT PROPAGATE IS NOT SET");
    }
    //
    //
    ////////////////////////////////////////////////////////  	

  
    // .. then we create a new thumbwhere_originsync entity...
      // Instantiate the ThumbWhereContent class

    $twAdminAPI = new ThumbWhereAPIAdmin();

    //if (twCanDebug()) debug($twContentAPI);

    //if (twCanDebug()) debug($entity);

    //
    // Collate and validate our fields
    //

    $fields = array();
    // Compulsory so..
    if (!isset($entity->fk_origin)) {
      throw new Exception('origin is mandatory.');
    }
    else {
      $fields['origin'] = $entity->fk_origin;
    }
    // Compulsory so..
    if (empty($entity->resource)) {
      throw new Exception('resource is mandatory.');
    }
    else {
      $fields['resource'] = $entity->resource;
    }
    // Compulsory so..
    if (!isset($entity->last_sync)) {
      throw new Exception('last_sync is mandatory.');
    }
    else {
      $fields['last_sync'] = $entity->last_sync;
    }
    // Compulsory so..
    if (!isset($entity->max)) {
      throw new Exception('max is mandatory.');
    }
    else {
      $fields['max'] = $entity->max;
    }

    // Not nullable but we have a default value
    if (!isset($entity->complete)) {
      $fields['complete'] = 0;
    }
    else {
      $fields['complete'] = $entity->complete;
    }
		
	//
	// Ensure that we have the 'thumbwhere_api_key' as we are going to pass that through in a context
	//
    if (variable_get('thumbwhere_api_key') == '') {
    	throw new Exception('ThumbWhere resource for \'OriginSync\' requires that variable \'thumbwhere_api_key\' is set.');
    }
    
              		
    //
    // If this originsync entity is new....
    //
               
    if ($entity->is_new) {
                

      //
      // ... then we create the originsync entity in ThumbWhere
      //

      $originsync = $twAdminAPI->create_origin($context = array(
                  'key' => variable_get('thumbwhere_api_key')
          ), $fields);
            
       if (twCanDebug()) debug($originsync);

      //if (twCanDebug()) debug($origin);

      $attributes = $originsync->body->origin->attributes();

      if (twCanDebug()) debug('newly created id = ' . $attributes['id']);

      // Assign new id.
      $entity->pk_originsync = intval($attributes['id']);

      if (twCanDebug()) debug('$entity->pk_originsync = ' . $entity->pk_originsync);
    }
    else
    {
          	

      //
      // .. otherwise we update the originsync entity in ThumbWhere
      //

      $originsync = $twAdminAPI->update_origin($id=$entity->pk_originsync,$context = array(
                  'key' => variable_get('thumbwhere_api_key')
          ), $fields);
            
        
    }
          	
    
  }
}

/**
 * Implements thumbwhere_originsync_entity_insert().
 *
 * Act on entities when inserted.
 *
 * @param $entity
 *   The entity object
 * @param $type
 *   The type of entity being inserted (i.e. node, user, comment).
 */
function thumbwhere_originsync_entity_insert($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_originsync') return;

}


/**
* Implements thumbwhere_originsync_entity_delete().
*
* Act on entities when inserted.
*
* @param $entity
*   The entity object
* @param $type
*   The type of entity being deleted (i.e. node, user, comment).
*/
function thumbwhere_originsync_entity_delete($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_originsync') return;


}
