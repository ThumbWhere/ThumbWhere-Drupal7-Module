<?php

/**
 * @file
 * Stress tests for the HostCommand module
 *
 *
 * NOTE: This has been autogenerated.
 * 
 */

class ThumbWhereHostCommandStressTestCase extends DrupalWebTestCase {

  protected $privileged_user;

  public function getInfo() {
    return array(
        'name' => 'HostCommand Entity Stress Test',
        'description' => 'Creates HostCommands in lots of 10, 100(disabled), 1000(disabled) and outputs the times take and the average rate of creation.',
        'group' => 'ThumbWhereStress',
    );
  }

  public function setUp() {

    // Make sure we are using the testing profile
    $this->profile = 'testing';
    
    // We need temporary folder
    static $temporary_directory;
    $temporary_directory = 'C:\\TEMP\\';    

    // Set the critial thumbwhere config 
    variable_set('thumbwhere_api_url', 'localhost.thumbwhere.com');
    variable_set('thumbwhere_api_version', 'v1.1');
    variable_set('thumbwhere_api_key', '80bb71a1-131c-43c2-9bac-e27006051cd0');
    variable_set('thumbwhere_master_console', '0');
    variable_set('thumbwhere_api_log_debug', '0');
    variable_set('thumbwhere_api_log_trace', '0');
    variable_set('thumbwhere_machine_id', 1);    

    // Enable the module.
    parent::setUp('thumbwhere_hostcommand');
    	
    	

    $this->privileged_user = $this->drupalCreateUser(array());
    $this->drupalLogin($this->privileged_user);
  }

  /**
   * Create 100 contents..
   */
  function testStress100() {

    // Set the critial thumbwhere config 
    variable_set('thumbwhere_api_url', 'localhost.thumbwhere.com');
    variable_set('thumbwhere_api_version', 'v1.1');
    variable_set('thumbwhere_api_key', '80bb71a1-131c-43c2-9bac-e27006051cd0');
    variable_set('thumbwhere_master_console', '0');
    variable_set('thumbwhere_api_log_debug', '0');
    variable_set('thumbwhere_api_log_trace', '0');
    variable_set('thumbwhere_machine_id', 1);    


    $n = 100;

    $time_start = microtime(TRUE);
    
    $total = 0;
   

    for ($i = 0; $i < $n; $i++) {

      $entity = _thumbwhere_stresstest_create_HostCommand($this->privileged_user->uid,$this);
	
      if ($entity == null) {
        break;
      }
	  

      $entity->save();
      
      $total++;

    }
    
    if ($total < $n)
    {
    	$this->assertTrue(TRUE, 'Bailed because we can not create dependencies.');
    	return;
    }

    $time_end = microtime(TRUE);
    $time = $time_end - $time_start;

    $result = TRUE;
    $message = t("Created $total HostCommands in $time seconds = " . $total / $time . ' contents per second.');

    if (function_exists('drush_log')) {
      drush_log($message, 'metric');
      drush_log("##teamcity[buildStatisticValue key='hostcommandCreate" . $total . "' value='" . $total / $time . "']");
    }
    
    $this->assertTrue($result, $message);



  }


 
}
     

function _thumbwhere_stresstest_create_HostCommand($uid,$test) {

    global $_in_thumbwhere_stresstest_create_HostCommand;
    $_in_thumbwhere_stresstest_create_HostCommand = TRUE;
	
    //
    // Global values that we will use...
    //
    
    global $_global_thumbwhere_stresstest_Host;
    if (!isset( $_global_thumbwhere_stresstest_Host)){
      if (function_exists ('_thumbwhere_stresstest_create_Host')) {
        $_global_thumbwhere_stresstest_Host = _thumbwhere_stresstest_create_Host($uid,$test);
      }
      else {
        debug('bailing - function \'_thumbwhere_stresstest_create_Host\' is not loaded yet.');
        return NULL;
      }
    }
    global $_global_thumbwhere_stresstest_HostCommandTemplate;
    if (!isset( $_global_thumbwhere_stresstest_HostCommandTemplate)){
      if (function_exists ('_thumbwhere_stresstest_create_HostCommandTemplate')) {
        $_global_thumbwhere_stresstest_HostCommandTemplate = _thumbwhere_stresstest_create_HostCommandTemplate($uid,$test);
      }
      else {
        debug('bailing - function \'_thumbwhere_stresstest_create_HostCommandTemplate\' is not loaded yet.');
        return NULL;
      }
    }
          		
          		
    $entity = entity_create('thumbwhere_hostcommand', array(
        'uid' => $uid,
        'host' => $_global_thumbwhere_stresstest_Host,
        'hostcommandtemplate' => $_global_thumbwhere_stresstest_HostCommandTemplate,
    		'command' => $test->randomString(),
    		'running' => (rand(0,1) == 1 ? TRUE : FALSE),
    		'completed' => (rand(0,1) == 1 ? TRUE : FALSE),

      ));  
      
    $_in_thumbwhere_stresstest_create_HostCommand = FALSE;
    return $entity;
}
