<?php

// ******************************************************************
// This file was generated by autogen_model_implementation_drupal.xsl
// ******************************************************************

// Include the ThumbWhere SDK
require_once(dirname(__FILE__) . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . 'dependencies' . DIRECTORY_SEPARATOR . 'tw' . DIRECTORY_SEPARATOR . 'sdk.class.php');

/**
 * @file
 * Module for the ThumbWhere HostCommand Entity
 *
 *
 * NOTE: This has been autogenerated.
 * 
 */

/**
 * Implement hook_entity_info().
 *
 * We define two entities here - the actual entity that will hold our domain
 * specific information and an entity that holds information about the different
 * types of entities. See here: http://drupal.org/node/977380 for a discussion on this
 * choice.
 */
function thumbwhere_hostcommand_entity_info() {
  $return['thumbwhere_hostcommand'] = array(

    // The human-readable name of the type.
    'label' => t('ThumbWhereHostCommand'),

    // (used by DrupalDefaultEntityController) The name of the entity type's base table.
    'base table' => 'thumbwhere_hostcommand',

    // fieldable: Set to TRUE if you want your entity type to accept fields being attached to it.
    'fieldable' => TRUE,

  // Declare the endty keys..
    'entity keys' => array(
      'id' => 'pk_hostcommand',
      'label' => 'pk_hostcommand',
    ),

    // A function taking an entity and an entity type as arguments and returning the label of the entity.
    // The entity label is the main string associated with an entity; for example, the title of a node or
    // the subject of a comment. If there is an entity object property that defines the label, use the
    // 'label' element of the 'entity keys' return value component to provide this information (see below).
    // If more complex logic is needed to determine the label of an entity, you can instead specify a callback
    // function here, which will be called to determine the entity label. See also the entity_label()
    // function, which implements this logic.
    'label callback' => 'entity_class_label',

    // A function taking an entity as argument and returning the uri elements of the entity, e.g. 'path' and 'options'.
    // The actual entity uri can be constructed by passing these elements to url().
    'uri callback' => 'entity_class_uri',

    //
    // Entity API Components
    //

    // The module whwre we can find our classes
    'module' => 'thumbwhere_hostcommand',

    // The entity class and controller class extend the classes provided by the Entity API
    'entity class' => 'ThumbWhereHostCommand',

    // Which Entity API contoller
    'controller class' => 'ThumbWhereHostCommandController',

    // The information below is used by the ThumbWhereHostCommandUIController (which extends the EntityDefaultUIController)
    'admin ui' => array(
      'path' => 'admin/thumbwhere/thumbwhere_hostcommands',
      'file' => 'thumbwhere_hostcommand.admin.inc',
      'controller class' => 'ThumbWhereHostCommandUIController',
      'menu wildcard' => '%thumbwhere_hostcommand',
    ),
  );

  return $return;
}


/**
 * Implements hook_permission().
 */
function thumbwhere_hostcommand_permission() {

  // We set up permisssions to manage entity types, manage all entities and the
  // permissions for each individual entity
  $permissions = array(
    'administer thumbwhere_hostcommands' => array(
      'title' => t('Administer thumbwhere_hostcommands'),
      'description' => t('Edit and delete all thumbwhere_hostcommands'),
    ),
  'send thumbwhere hostcommand notifications' => array(
          'title' => t('Send ThumbWhere Notifications'),
          'description' => t('Allow the caller to call the ThumbWhere HostCommand Entity Notifications Endpoint.'),
          'restrict access' => TRUE,
      ),    
  );

  return $permissions;
}


/**
 * Determines whether the given user has access to a thumbwhere_hostcommand.
 *
 * @param $op
 *   The operation being performed. One of 'view', 'update', 'create', 'delete'
 *   or just 'edit' (being the same as 'create' or 'update').
 * @param $thumbwhere_hostcommand
 *   Optionally a thumbwhere_hostcommand or a thumbwhere_hostcommand type to check access for. If nothing is
 *   given, access for all thumbwhere_hostcommands is determined.
 * @param $account
 *   The user to check for. Leave it to NULL to check for the global user.
 * @return boolean
 *   Whether access is allowed or not.
 */
function thumbwhere_hostcommand_access($op, $thumbwhere_hostcommand = NULL, $account = NULL) {

  //if (user_access('administer thumbwhere_hostcommands', $account)) {
  //  return TRUE;
 // }

  //return FALSE;
  return TRUE;
}




/**
 * Fetch a thumbwhere_hostcommand object. Make sure that the wildcard you choose
 * in the thumbwhere_hostcommand entity definition fits the function name here.
 *
 * @param $thumbwhere_hostcommand_id
 *   Integer specifying the thumbwhere_hostcommand id.
 * @param $reset
 *   A boolean indicating that the internal cache should be reset.
 * @return
 *   A fully-loaded $thumbwhere_hostcommand object or FALSE if it cannot be loaded.
 *
 * @see thumbwhere_hostcommand_load_multiple()
 */
function thumbwhere_hostcommand_load($thumbwhere_hostcommand_id, $reset = FALSE) {

  $thumbwhere_hostcommands = entity_load('thumbwhere_hostcommand', array($thumbwhere_hostcommand_id), array(), $reset);

  return reset($thumbwhere_hostcommands);
}


/**
 * Deletes a thumbwhere_hostcommand.
 */
function thumbwhere_hostcommand_delete(ThumbWhereHostCommand $thumbwhere_hostcommand) {

  $thumbwhere_hostcommand->delete();
}

/**
 * Create a thumbwhere_hostcommand object.
 */
function thumbwhere_hostcommand_create($values = array()) {

  return entity_get_controller('thumbwhere_hostcommand')->create($values);
}


/**
 * URI callback for thumbwhere_hostcommands
 */
function thumbwhere_hostcommand_uri(ThumbWhereHostCommand $thumbwhere_hostcommand) {

  return array(
    'path' => 'thumbwhere_hostcommand/' . $thumbwhere_hostcommand->pk_hostcommand,
  );
}


/**
 * Menu title callback for showing individual entities
 */
function thumbwhere_hostcommand_page_title(ThumbWhereHostCommand $thumbwhere_hostcommand) {

	return $thumbwhere_hostcommand->pk_hostcommand;
}


/**
 * Sets up content to show an individual thumbwhere_hostcommand
 * @todo - get rid of drupal_set_title();
 */
function thumbwhere_hostcommand_page_view($thumbwhere_hostcommand, $view_mode = 'full') {



  $controller = entity_get_controller('thumbwhere_hostcommand');
  $content = $controller->view(array($thumbwhere_hostcommand->pk_hostcommand => $thumbwhere_hostcommand));
  drupal_set_title($thumbwhere_hostcommand->pk_hostcommand);
  return $content;
}


/**
 * Implements hook_views_api().
 */
function thumbwhere_hostcommand_views_api() {



  return array(
    'api' => 3,
    'path' => drupal_get_path('module', 'thumbwhere_hostcommand') . '/views',
  );
}


/**
 * Implement hook_theme().
 */
function thumbwhere_hostcommand_theme() {



  return array(
    'thumbwhere_hostcommand_add_list' => array(
      'variables' => array('content' => array()),
      'file' => 'thumbwhere_hostcommand.admin.inc',
    ),
    'thumbwhere_hostcommand' => array(
      'render element' => 'elements',
      'template' => 'thumbwhere_hostcommand',
    )
  );
}


/**
 * Implements hook_menu_local_tasks_alter().
 */
function thumbwhere_hostcommand_menu_local_tasks_alter(&$data, $router_item, $root_path) {

  // Add action link 'admin/structure/thumbwhere_hostcommands/add' on 'admin/structure/thumbwhere_hostcommands'.
  if ($root_path == 'admin/thumbwhere/thumbwhere_hostcommands') {
    $item = menu_get_item('admin/thumbwhere/thumbwhere_hostcommands/add');
    if ($item['access']) {
      $data['actions']['output'][] = array(
        '#theme' => 'menu_local_action',
        '#link' => $item,
      );
    }
  }
}


/**
 * The class used for thumbwhere_hostcommand entities
 */
class ThumbWhereHostCommand extends Entity {

  public function __construct($values = array()) {
    parent::__construct($values, 'thumbwhere_hostcommand');
  }

  protected function defaultLabel() {
    return $this->pk_hostcommand;
  }

  protected function defaultUri() {
    return array('path' => 'thumbwhere_hostcommand/' . $this->pk_hostcommand);
  }


}



/**
 * The Controller for ThumbWhereHostCommand entities
 */
class ThumbWhereHostCommandController extends EntityAPIController {
  public function __construct($entityType) {
    parent::__construct($entityType);
  }
}



/**
* Implements hook_entity_presave().
*
* Act on an HostCommand entity before it is about to be created or updated.
*
* @param $entity
*   The entity object.
* @param $entity_type
*   The type of entity being saved (i.e. node, user, comment).
*/
function thumbwhere_hostcommand_entity_presave($entity, $entity_type) {

  // We only look after what we want...
  if ($entity_type == 'thumbwhere_hostcommand')  {
  	
  	
  	
 	/////////////////////////////////////////////////////
    //
    // Should we not propagate this to ThumbWhere? 
    //
    if (isset($entity->do_not_propagate)) {
      if ($entity->do_not_propagate) {
        if (twCanDebug()) {
          if ($entity->is_new) {
            debug("DO NOT PROPAGATE 'hostcommand' presave *CREATE*");
          }
          else {
            debug("DO NOT PROPAGATE 'hostcommand' presave *UPDATE*");
          }
        }

        // To cause an infinite loop of updates, comment out this line.
        return;
      }
      else if (twCanDebug()) {
        debug("OK PROAOGATE 'hostcommand' presave to ThumbWhere Server");
      }
    }
    else if (twCanDebug()) {
      debug("DO NOT PROPAGATE IS NOT SET");
    }
    //
    //
    ////////////////////////////////////////////////////////  	

  
    // .. then we create a new thumbwhere_hostcommand entity...
      // Instantiate the ThumbWhereContent class

    $twAdminAPI = new ThumbWhereAPIAdmin();

    //if (twCanDebug()) debug($twContentAPI);

    //if (twCanDebug()) debug($entity);

    //
    // Collate and validate our fields
    //

    $fields = array();
    // Compulsory so..
    if (empty($entity->host)) {
      throw new Exception('host is compulsory.');
    }
    else {
      $fields['host'] = $entity->host;
    }
    // Compulsory so..
    if (empty($entity->hostcommandtemplate)) {
      throw new Exception('hostcommandtemplate is compulsory.');
    }
    else {
      $fields['hostcommandtemplate'] = $entity->hostcommandtemplate;
    }
    // Compulsory so..
    if (empty($entity->command)) {
      throw new Exception('command is compulsory.');
    }
    else {
      $fields['command'] = $entity->command;
    }
    // Compulsory so..
    if (empty($entity->running)) {
      throw new Exception('running is compulsory.');
    }
    else {
      $fields['running'] = $entity->running;
    }
    // Compulsory so..
    if (empty($entity->completed)) {
      throw new Exception('completed is compulsory.');
    }
    else {
      $fields['completed'] = $entity->completed;
    }
    
    //
    // If this hostcommand entity is new....
    //
    
    if ($entity->is_new) {
                

      //
      // ... then we create the hostcommand entity in ThumbWhere
      //

      $hostcommand = $twAdminAPI->create_host_command($context = array(
                  'key' => variable_get('thumbwhere_api_key')
          ), $fields);
            
       if (twCanDebug()) debug($hostcommand);

      //if (twCanDebug()) debug($host_command);

      $attributes = $hostcommand->body->host_command->attributes();

      if (twCanDebug()) debug('newly created id = ' . $attributes['id']);

      // Assign new id.
      $entity->pk_hostcommand = intval($attributes['id']);

      if (twCanDebug()) debug('$entity->pk_hostcommand = ' . $entity->pk_hostcommand);
    }
    else
    {
          	

      //
      // .. otherwise we update the hostcommand entity in ThumbWhere
      //

      $hostcommand = $twAdminAPI->update_host_command($id=$entity->pk_hostcommand,$context = array(
                  'key' => variable_get('thumbwhere_api_key')
          ), $fields);
            
        
    }
          	
    
  }
}

/**
 * Implements thumbwhere_hostcommand_entity_insert().
 *
 * Act on entities when inserted.
 *
 * @param $entity
 *   The entity object
 * @param $type
 *   The type of entity being inserted (i.e. node, user, comment).
 */
function thumbwhere_hostcommand_entity_insert($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_hostcommand') return;

}


/**
* Implements thumbwhere_hostcommand_entity_delete().
*
* Act on entities when inserted.
*
* @param $entity
*   The entity object
* @param $type
*   The type of entity being deleted (i.e. node, user, comment).
*/
function thumbwhere_hostcommand_entity_delete($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_hostcommand') return;


}
