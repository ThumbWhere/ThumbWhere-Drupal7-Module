<?php

// ******************************************************************
// This file was generated by autogen_model_implementation_drupal.xsl
// ******************************************************************

// Include the ThumbWhere SDK
require_once(dirname(__FILE__) . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . 'dependencies' . DIRECTORY_SEPARATOR . 'tw' . DIRECTORY_SEPARATOR . 'sdk.class.php');

/**
 * @file
 * Module for the ThumbWhere Program Entity
 *
 *
 * NOTE: This has been autogenerated.
 * 
 */

/**
 * Implement hook_entity_info().
 *
 * We define two entities here - the actual entity that will hold our domain
 * specific information and an entity that holds information about the different
 * types of entities. See here: http://drupal.org/node/977380 for a discussion on this
 * choice.
 */
function thumbwhere_program_entity_info() {
  $return['thumbwhere_program'] = array(

    // The human-readable name of the type.
    'label' => t('ThumbWhereProgram'),

    // (used by DrupalDefaultEntityController) The name of the entity type's base table.
    'base table' => 'thumbwhere_program',

    // fieldable: Set to TRUE if you want your entity type to accept fields being attached to it.
    'fieldable' => TRUE,

  // Declare the endty keys..
    'entity keys' => array(
      'id' => 'pk_program',
      'label' => 'name',
    ),

    // A function taking an entity and an entity type as arguments and returning the label of the entity.
    // The entity label is the main string associated with an entity; for example, the title of a node or
    // the subject of a comment. If there is an entity object property that defines the label, use the
    // 'label' element of the 'entity keys' return value component to provide this information (see below).
    // If more complex logic is needed to determine the label of an entity, you can instead specify a callback
    // function here, which will be called to determine the entity label. See also the entity_label()
    // function, which implements this logic.
    'label callback' => 'entity_class_label',

    // A function taking an entity as argument and returning the uri elements of the entity, e.g. 'path' and 'options'.
    // The actual entity uri can be constructed by passing these elements to url().
    'uri callback' => 'entity_class_uri',

    //
    // Entity API Components
    //

    // The module whwre we can find our classes
    'module' => 'thumbwhere_program',

    // The entity class and controller class extend the classes provided by the Entity API
    'entity class' => 'ThumbWhereProgram',

    // Which Entity API contoller
    'controller class' => 'ThumbWhereProgramController',

    // The information below is used by the ThumbWhereProgramUIController (which extends the EntityDefaultUIController)
    'admin ui' => array(
      'path' => 'admin/thumbwhere/thumbwhere_programs',
      'file' => 'thumbwhere_program.admin.inc',
      'controller class' => 'ThumbWhereProgramUIController',
      'menu wildcard' => '%thumbwhere_program',
    ),
  );

  return $return;
}


/**
 * Implements hook_permission().
 */
function thumbwhere_program_permission() {

  // We set up permisssions to manage entity types, manage all entities and the
  // permissions for each individual entity
  $permissions = array(
    'administer thumbwhere_programs' => array(
      'title' => t('Administer thumbwhere_programs'),
      'description' => t('Administer all thumbwhere_programs entities'),
      //'restrict access' => TRUE,
    ),
  'send thumbwhere program notifications' => array(
      'title' => t('Send ThumbWhere Notifications'),
      'description' => t('Allow the caller to call the ThumbWhere Program Entity Notifications Endpoint.'),
      //'restrict access' => TRUE,
    ),    
  );

  return $permissions;
}


/**
 * Determines whether the given user has access to a thumbwhere_program.
 *
 * @param $op
 *   The operation being performed. One of 'view', 'update', 'create', 'delete'
 *   or just 'edit' (being the same as 'create' or 'update').
 * @param $thumbwhere_program
 *   Optionally a thumbwhere_program or a thumbwhere_program type to check access for. If nothing is
 *   given, access for all thumbwhere_programs is determined.
 * @param $account
 *   The user to check for. Leave it to NULL to check for the global user.
 * @return boolean
 *   Whether access is allowed or not.
 */
function thumbwhere_program_access($op, $thumbwhere_program = NULL, $account = NULL) {

  if (user_access('administer thumbwhere_programs', $account)) {
    return TRUE;
  }
 
  return FALSE;
}




/**
 * Fetch a thumbwhere_program object. Make sure that the wildcard you choose
 * in the thumbwhere_program entity definition fits the function name here.
 *
 * @param $thumbwhere_program_id
 *   Integer specifying the thumbwhere_program id.
 * @param $reset
 *   A boolean indicating that the internal cache should be reset.
 * @return
 *   A fully-loaded $thumbwhere_program object or FALSE if it cannot be loaded.
 *
 * @see thumbwhere_program_load_multiple()
 */
function thumbwhere_program_load($thumbwhere_program_id, $reset = FALSE) {

  $thumbwhere_programs = entity_load('thumbwhere_program', array($thumbwhere_program_id), array(), $reset);

  return reset($thumbwhere_programs);
}


/**
 * Deletes a thumbwhere_program.
 */
function thumbwhere_program_delete(ThumbWhereProgram $thumbwhere_program) {

  $thumbwhere_program->delete();
}

/**
 * Create a thumbwhere_program object.
 */
function thumbwhere_program_create($values = array()) {

  return entity_get_controller('thumbwhere_program')->create($values);
}


/**
 * URI callback for thumbwhere_programs
 */
function thumbwhere_program_uri(ThumbWhereProgram $thumbwhere_program) {

  return array(
    'path' => 'thumbwhere_program/' . $thumbwhere_program->pk_program,
  );
}


/**
 * Menu title callback for showing individual entities
 */
function thumbwhere_program_page_title(ThumbWhereProgram $thumbwhere_program) {

	return $thumbwhere_program->name;
}


/**
 * Sets up content to show an individual thumbwhere_program
 * @todo - get rid of drupal_set_title();
 */
function thumbwhere_program_page_view($thumbwhere_program, $view_mode = 'full') {



  $controller = entity_get_controller('thumbwhere_program');
  $content = $controller->view(array($thumbwhere_program->pk_program => $thumbwhere_program));
  drupal_set_title($thumbwhere_program->name);
  return $content;
}


/**
 * Implements hook_views_api().
 */
function thumbwhere_program_views_api() {



  return array(
    'api' => 3,
    'path' => drupal_get_path('module', 'thumbwhere_program') . '/views',
  );
}


/**
 * Implement hook_theme().
 */
function thumbwhere_program_theme() {



  return array(
    'thumbwhere_program_add_list' => array(
      'variables' => array('content' => array()),
      'file' => 'thumbwhere_program.admin.inc',
    ),
    'thumbwhere_program' => array(
      'render element' => 'elements',
      'template' => 'thumbwhere_program',
    )
  );
}


/**
 * Implements hook_menu_local_tasks_alter().
 */
function thumbwhere_program_menu_local_tasks_alter(&$data, $router_item, $root_path) {

  // Add action link 'admin/structure/thumbwhere_programs/add' on 'admin/structure/thumbwhere_programs'.
  if ($root_path == 'admin/thumbwhere/thumbwhere_programs') {
    $item = menu_get_item('admin/thumbwhere/thumbwhere_programs/add');
    if ($item['access']) {
      $data['actions']['output'][] = array(
        '#theme' => 'menu_local_action',
        '#link' => $item,
      );
    }
  }
}


/**
 * The class used for thumbwhere_program entities
 */
class ThumbWhereProgram extends Entity {

  public function __construct($values = array()) {
    parent::__construct($values, 'thumbwhere_program');
  }

  protected function defaultLabel() {
    return $this->name;
  }

  protected function defaultUri() {
    return array('path' => 'thumbwhere_program/' . $this->pk_program);
  }


}



/**
 * The Controller for ThumbWhereProgram entities
 */
class ThumbWhereProgramController extends EntityAPIController {
  public function __construct($entityType) {
    parent::__construct($entityType);
  }
}



/**
* Implements hook_entity_presave().
*
* Act on an Program entity before it is about to be created or updated.
*
* @param $entity
*   The entity object.
* @param $entity_type
*   The type of entity being saved (i.e. node, user, comment).
*/
function thumbwhere_program_entity_presave($entity, $entity_type) {

  // We only look after what we want...
  if ($entity_type == 'thumbwhere_program')  {
  	
    // Should we not propagate this to ThumbWhere? 
    if (isset($entity->do_not_propagate)) {
      if ($entity->do_not_propagate) {
        // To cause an infinite loop of updates, comment out this line.
        return;
      }
    }

  
    // .. then we create a new thumbwhere_program entity...
      // Instantiate the ThumbWhereContent class

    $twProgramAPI = new ThumbWhereAPIProgram();


    //
    // Collate and validate our fields
    //

    $fields = array();
    // Compulsory so..
    if (empty($entity->name)) {
      throw new Exception('name is mandatory.');
    }
    else {
      $fields['name'] = $entity->name;
    }
		
	//
	// Ensure that we have the 'thumbwhere_api_key' as we are going to pass that through in a context
	//
  if (variable_get('thumbwhere_api_key') == '') {
    throw new Exception('ThumbWhere resource calls for \'Program\' requires that variable \'thumbwhere_api_key\' is set.');
  }

	//
	// Ensure that we have the 'thumbwhere_api_key' as we are going to pass that through in a context
	//
  if (!is_numeric(variable_get('thumbwhere_host_id'))) {
    throw new Exception('ThumbWhere resource calls for \'Program\' requires that variable \'thumbwhere_host_id\' is set.');
  }


              		
    //
    // If this program entity is new....
    //
               
    if ($entity->is_new) {
                

      //
      // ... then we create the program entity in ThumbWhere
      //

      $program = $twProgramAPI->create_program($context = array(
                  'key' => variable_get('thumbwhere_api_key'),
                  'origin' => variable_get('thumbwhere_host_id'),
          ), $fields);

      $attributes = $program->body->program->attributes();

      //if (twCanDebug()) debug('newly created id = ' . t($attributes['id']));

      // Assign new id.
      $entity->pk_program = intval($attributes['id']);

      //if (twCanDebug()) debug(t('$entity->pk_program = ' . $entity->pk_program));
    }
    else
    {
          	

      //
      // .. otherwise we update the program entity in ThumbWhere
      //

      $program = $twProgramAPI->update_program($id=$entity->pk_program,$context = array(
                  'key' => variable_get('thumbwhere_api_key'),
                  'origin' => variable_get('thumbwhere_host_id'),
          ), $fields);
            
        
    }
          	
    
  }
}

/**
 * Implements thumbwhere_program_entity_insert().
 *
 * Act on entities when inserted.
 *
 * @param $entity
 *   The entity object
 * @param $type
 *   The type of entity being inserted (i.e. node, user, comment).
 */
function thumbwhere_program_entity_insert($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_program') return;

}


/**
* Implements thumbwhere_program_entity_delete().
*
* Act on entities when inserted.
*
* @param $entity
*   The entity object
* @param $type
*   The type of entity being deleted (i.e. node, user, comment).
*/
function thumbwhere_program_entity_delete($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_program') return;


}
