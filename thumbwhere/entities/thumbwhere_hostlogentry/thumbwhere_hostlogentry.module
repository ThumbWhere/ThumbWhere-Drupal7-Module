<?php

// ******************************************************************
// This file was generated by autogen_model_implementation_drupal.xsl
// ******************************************************************

// Include the ThumbWhere SDK
require_once(dirname(__FILE__) . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . 'dependencies' . DIRECTORY_SEPARATOR . 'tw' . DIRECTORY_SEPARATOR . 'sdk.class.php');

/**
 * @file
 * Module for the ThumbWhere HostLogEntry Entity
 *
 *
 * NOTE: This has been autogenerated.
 * 
 */

/**
 * Implement hook_entity_info().
 *
 * We define two entities here - the actual entity that will hold our domain
 * specific information and an entity that holds information about the different
 * types of entities. See here: http://drupal.org/node/977380 for a discussion on this
 * choice.
 */
function thumbwhere_hostlogentry_entity_info() {
  $return['thumbwhere_hostlogentry'] = array(

    // The human-readable name of the type.
    'label' => t('ThumbWhereHostLogEntry'),

    // (used by DrupalDefaultEntityController) The name of the entity type's base table.
    'base table' => 'thumbwhere_hostlogentry',

    // fieldable: Set to TRUE if you want your entity type to accept fields being attached to it.
    'fieldable' => TRUE,

  // Declare the endty keys..
    'entity keys' => array(
      'id' => 'pk_hostlogentry',
      'label' => 'pk_hostlogentry',
    ),

    // A function taking an entity and an entity type as arguments and returning the label of the entity.
    // The entity label is the main string associated with an entity; for example, the title of a node or
    // the subject of a comment. If there is an entity object property that defines the label, use the
    // 'label' element of the 'entity keys' return value component to provide this information (see below).
    // If more complex logic is needed to determine the label of an entity, you can instead specify a callback
    // function here, which will be called to determine the entity label. See also the entity_label()
    // function, which implements this logic.
    'label callback' => 'entity_class_label',

    // A function taking an entity as argument and returning the uri elements of the entity, e.g. 'path' and 'options'.
    // The actual entity uri can be constructed by passing these elements to url().
    'uri callback' => 'entity_class_uri',

    //
    // Entity API Components
    //

    // The module whwre we can find our classes
    'module' => 'thumbwhere_hostlogentry',

    // The entity class and controller class extend the classes provided by the Entity API
    'entity class' => 'ThumbWhereHostLogEntry',

    // Which Entity API contoller
    'controller class' => 'ThumbWhereHostLogEntryController',

    // The information below is used by the ThumbWhereHostLogEntryUIController (which extends the EntityDefaultUIController)
    'admin ui' => array(
      'path' => 'admin/thumbwhere/thumbwhere_hostlogentrys',
      'file' => 'thumbwhere_hostlogentry.admin.inc',
      'controller class' => 'ThumbWhereHostLogEntryUIController',
      'menu wildcard' => '%thumbwhere_hostlogentry',
    ),
  );

  return $return;
}


/**
 * Implements hook_permission().
 */
function thumbwhere_hostlogentry_permission() {

  // We set up permisssions to manage entity types, manage all entities and the
  // permissions for each individual entity
  $permissions = array(
    'administer thumbwhere_hostlogentrys' => array(
      'title' => t('Administer thumbwhere_hostlogentrys'),
      'description' => t('Edit and delete all thumbwhere_hostlogentrys'),
    ),
  'send thumbwhere hostlogentry notifications' => array(
          'title' => t('Send ThumbWhere Notifications'),
          'description' => t('Allow the caller to call the ThumbWhere HostLogEntry Entity Notifications Endpoint.'),
          'restrict access' => TRUE,
      ),    
  );

  return $permissions;
}


/**
 * Determines whether the given user has access to a thumbwhere_hostlogentry.
 *
 * @param $op
 *   The operation being performed. One of 'view', 'update', 'create', 'delete'
 *   or just 'edit' (being the same as 'create' or 'update').
 * @param $thumbwhere_hostlogentry
 *   Optionally a thumbwhere_hostlogentry or a thumbwhere_hostlogentry type to check access for. If nothing is
 *   given, access for all thumbwhere_hostlogentrys is determined.
 * @param $account
 *   The user to check for. Leave it to NULL to check for the global user.
 * @return boolean
 *   Whether access is allowed or not.
 */
function thumbwhere_hostlogentry_access($op, $thumbwhere_hostlogentry = NULL, $account = NULL) {

  //if (user_access('administer thumbwhere_hostlogentrys', $account)) {
  //  return TRUE;
 // }

  //return FALSE;
  return TRUE;
}




/**
 * Fetch a thumbwhere_hostlogentry object. Make sure that the wildcard you choose
 * in the thumbwhere_hostlogentry entity definition fits the function name here.
 *
 * @param $thumbwhere_hostlogentry_id
 *   Integer specifying the thumbwhere_hostlogentry id.
 * @param $reset
 *   A boolean indicating that the internal cache should be reset.
 * @return
 *   A fully-loaded $thumbwhere_hostlogentry object or FALSE if it cannot be loaded.
 *
 * @see thumbwhere_hostlogentry_load_multiple()
 */
function thumbwhere_hostlogentry_load($thumbwhere_hostlogentry_id, $reset = FALSE) {

  $thumbwhere_hostlogentrys = entity_load('thumbwhere_hostlogentry', array($thumbwhere_hostlogentry_id), array(), $reset);

  return reset($thumbwhere_hostlogentrys);
}


/**
 * Deletes a thumbwhere_hostlogentry.
 */
function thumbwhere_hostlogentry_delete(ThumbWhereHostLogEntry $thumbwhere_hostlogentry) {

  $thumbwhere_hostlogentry->delete();
}

/**
 * Create a thumbwhere_hostlogentry object.
 */
function thumbwhere_hostlogentry_create($values = array()) {

  return entity_get_controller('thumbwhere_hostlogentry')->create($values);
}


/**
 * URI callback for thumbwhere_hostlogentrys
 */
function thumbwhere_hostlogentry_uri(ThumbWhereHostLogEntry $thumbwhere_hostlogentry) {

  return array(
    'path' => 'thumbwhere_hostlogentry/' . $thumbwhere_hostlogentry->pk_hostlogentry,
  );
}


/**
 * Menu title callback for showing individual entities
 */
function thumbwhere_hostlogentry_page_title(ThumbWhereHostLogEntry $thumbwhere_hostlogentry) {

	return $thumbwhere_hostlogentry->pk_hostlogentry;
}


/**
 * Sets up content to show an individual thumbwhere_hostlogentry
 * @todo - get rid of drupal_set_title();
 */
function thumbwhere_hostlogentry_page_view($thumbwhere_hostlogentry, $view_mode = 'full') {



  $controller = entity_get_controller('thumbwhere_hostlogentry');
  $content = $controller->view(array($thumbwhere_hostlogentry->pk_hostlogentry => $thumbwhere_hostlogentry));
  drupal_set_title($thumbwhere_hostlogentry->pk_hostlogentry);
  return $content;
}


/**
 * Implements hook_views_api().
 */
function thumbwhere_hostlogentry_views_api() {



  return array(
    'api' => 3,
    'path' => drupal_get_path('module', 'thumbwhere_hostlogentry') . '/views',
  );
}


/**
 * Implement hook_theme().
 */
function thumbwhere_hostlogentry_theme() {



  return array(
    'thumbwhere_hostlogentry_add_list' => array(
      'variables' => array('content' => array()),
      'file' => 'thumbwhere_hostlogentry.admin.inc',
    ),
    'thumbwhere_hostlogentry' => array(
      'render element' => 'elements',
      'template' => 'thumbwhere_hostlogentry',
    )
  );
}


/**
 * Implements hook_menu_local_tasks_alter().
 */
function thumbwhere_hostlogentry_menu_local_tasks_alter(&$data, $router_item, $root_path) {

  // Add action link 'admin/structure/thumbwhere_hostlogentrys/add' on 'admin/structure/thumbwhere_hostlogentrys'.
  if ($root_path == 'admin/thumbwhere/thumbwhere_hostlogentrys') {
    $item = menu_get_item('admin/thumbwhere/thumbwhere_hostlogentrys/add');
    if ($item['access']) {
      $data['actions']['output'][] = array(
        '#theme' => 'menu_local_action',
        '#link' => $item,
      );
    }
  }
}


/**
 * The class used for thumbwhere_hostlogentry entities
 */
class ThumbWhereHostLogEntry extends Entity {

  public function __construct($values = array()) {
    parent::__construct($values, 'thumbwhere_hostlogentry');
  }

  protected function defaultLabel() {
    return $this->pk_hostlogentry;
  }

  protected function defaultUri() {
    return array('path' => 'thumbwhere_hostlogentry/' . $this->pk_hostlogentry);
  }


}



/**
 * The Controller for ThumbWhereHostLogEntry entities
 */
class ThumbWhereHostLogEntryController extends EntityAPIController {
  public function __construct($entityType) {
    parent::__construct($entityType);
  }
}



/**
* Implements hook_entity_presave().
*
* Act on an HostLogEntry entity before it is about to be created or updated.
*
* @param $entity
*   The entity object.
* @param $entity_type
*   The type of entity being saved (i.e. node, user, comment).
*/
function thumbwhere_hostlogentry_entity_presave($entity, $entity_type) {

  // We only look after what we want...
  if ($entity_type == 'thumbwhere_hostlogentry')  {
  	
 	/////////////////////////////////////////////////////
    //
    // Should we not propagate this to ThumbWhere? 
    //
    if (isset($entity->do_not_propagate)) {
      if ($entity->do_not_propagate) {
        if (twCanDebug()) {
          if ($entity->is_new) {
            if (twCanDebug()){
              debug("DO NOT PROPAGATE 'hostlogentry' presave *CREATE* not being sent to ThumbWhere server.");
          	}
          }
          else {
            if (twCanDebug()) {
              debug("DO NOT PROPAGATE 'hostlogentry' presave *UPDATE* not being sent to ThumbWhere server.");
            }
          }
        }

        // To cause an infinite loop of updates, comment out this line.
        return;
      }
      else if (twCanDebug()) {
        debug("OK PROAOGATE 'hostlogentry' presave to ThumbWhere server.");
      }
    }
    else if (twCanDebug()) {
      debug("DO NOT PROPAGATE IS NOT SET");
    }
    //
    //
    ////////////////////////////////////////////////////////  	

  
    // .. then we create a new thumbwhere_hostlogentry entity...
      // Instantiate the ThumbWhereContent class

    $twAdminAPI = new ThumbWhereAPIAdmin();

    //if (twCanDebug()) debug($twContentAPI);

    //if (twCanDebug()) debug($entity);

    //
    // Collate and validate our fields
    //

    $fields = array();
    // Compulsory so..
    if (empty($entity->log)) {
      throw new Exception('log is mandatory.');
    }
    else {
      $fields['log'] = $entity->log;
    }
    // Compulsory so..
    if (!isset($entity->fk_hostcommand)) {
      throw new Exception('hostcommand is mandatory.');
    }
    else {
      $fields['hostcommand'] = $entity->fk_hostcommand;
    }
		
	//
	// Ensure that we have the 'thumbwhere_api_key' as we are going to pass that through in a context
	//
  if (variable_get('thumbwhere_api_key') == '') {
    throw new Exception('ThumbWhere resource calls for \'HostLogEntry\' requires that variable \'thumbwhere_api_key\' is set.');
  }

	//
	// Ensure that we have the 'thumbwhere_api_key' as we are going to pass that through in a context
	//
  if (!is_numeric(variable_get('thumbwhere_host_id'))) {
    throw new Exception('ThumbWhere resource calls for \'HostLogEntry\' requires that variable \'thumbwhere_host_id\' is set.');
  }


              		
    //
    // If this hostlogentry entity is new....
    //
               
    if ($entity->is_new) {
                

      //
      // ... then we create the hostlogentry entity in ThumbWhere
      //

      $hostlogentry = $twAdminAPI->create_host_log_entry($context = array(
                  'key' => variable_get('thumbwhere_api_key'),
                  'origin' => variable_get('thumbwhere_host_id'),
          ), $fields);
            
       if (twCanDebug()) debug($hostlogentry);

      //if (twCanDebug()) debug($host_log_entry);

      $attributes = $hostlogentry->body->host_log_entry->attributes();

      if (twCanDebug()) debug('newly created id = ' . $attributes['id']);

      // Assign new id.
      $entity->pk_hostlogentry = intval($attributes['id']);

      if (twCanDebug()) debug('$entity->pk_hostlogentry = ' . $entity->pk_hostlogentry);
    }
    else
    {
          	

      //
      // .. otherwise we update the hostlogentry entity in ThumbWhere
      //

      $hostlogentry = $twAdminAPI->update_host_log_entry($id=$entity->pk_hostlogentry,$context = array(
                  'key' => variable_get('thumbwhere_api_key'),
                  'origin' => variable_get('thumbwhere_host_id'),
          ), $fields);
            
        
    }
          	
    
  }
}

/**
 * Implements thumbwhere_hostlogentry_entity_insert().
 *
 * Act on entities when inserted.
 *
 * @param $entity
 *   The entity object
 * @param $type
 *   The type of entity being inserted (i.e. node, user, comment).
 */
function thumbwhere_hostlogentry_entity_insert($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_hostlogentry') return;

}


/**
* Implements thumbwhere_hostlogentry_entity_delete().
*
* Act on entities when inserted.
*
* @param $entity
*   The entity object
* @param $type
*   The type of entity being deleted (i.e. node, user, comment).
*/
function thumbwhere_hostlogentry_entity_delete($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_hostlogentry') return;


}
