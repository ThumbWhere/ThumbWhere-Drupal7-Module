<?php

/**
 * @file
 * Provides list of operations that are possible in the thumbwhere_producer editing view.
 *
 * NOTE: This has been autogenerated.
 * 	
 */

/**
 * This field handler aggregates operations that can be done on a thumbwhere_producer
 * under a single field providing a more flexible way to present them in a view
 */
class thumbwhere_producer_handler_thumbwhere_producer_operations_field extends views_handler_field {
  function construct() {
    parent::construct();

    $this->additional_fields['pk_producer'] = 'pk_producer';
  }

  function query() {
    $this->ensure_my_table();
    $this->add_additional_fields();
  }

  function render($values) {

    $links = menu_contextual_links('thumbwhere_producer', 'admin/content/thumbwhere_producers/thumbwhere_producer', array($this->get_value($values, 'pk_producer')));

    if (!empty($links)) {
      return theme('links', array('links' => $links, 'attributes' => array('class' => array('links', 'inline', 'operations'))));
    }
  }
}
