<?php

/**
 * @file
 * Sets up the base table for our entity and a table to store information about
 * the entity types.
 */

/**
 * Implements hook_uninstall().
 *
 * This hook is called when the module is installed.
 *
 * @ingroup thumbwhere_producer
 */
function thumbwhere_producer_uninstall() {

  ddl('_thumbwhere_producer_uninstall()');

  //
  // Uninstall all the field info
  //

  // Uninstall the Menu TODO: Do we even install a menu???
  if (module_exists('menu')) {
    if ($thumbwhere_producer_menu = menu_load('thumbwhere_producer')) {
      ddl('deleting thumbwhere_producer menu');
      menu_delete($thumbwhere_producer_menu);
    }
  }

  // Clear the everything cache
  drupal_flush_all_caches();

  // Rebuild the menu
  menu_rebuild();

}

/**
 * Implements hook_install().
 *
 * This hook is called when the module is installed.
 *
 * @ingroup thumbwhere_producer
 */
function thumbwhere_producer_install() {

  ddl('thumbwhere_producer_install()');

  // Clear the everything cache
  drupal_flush_all_caches();

  // Rebuild the menu
  menu_rebuild();

  // Entity provided by ThumbWhere gets a weight of 12. Storage = 11 and ThumbWere itself, 10
  //db_query("UPDATE {system} SET weight = 12 WHERE name = 'thumbwhere_producer'");

}

/**
 * Implements hook_enable().
 *
 * Called when the module is enabled
 *
 * @ingroup thumbwhere_producer
 */
function thumbwhere_producer_enable() {
  ddl('thumbwhere_producer_enable()');

  // Clear the everything cache
  drupal_flush_all_caches();

  // Rebuild the menu
  menu_rebuild();

}

/**
 * Implements hook_disable().
 *
 * Called when the module is disabled
 *
 * @ingroup thumbwhere_producer
 */
function thumbwhere_producer_disable() {

  // Clear the everything cache
  drupal_flush_all_caches();

  // Rebuild the menu
  menu_rebuild();
  }

/**
 * Implements hook_schema().
 *
 *
 * This is where we define the schema for our entity.
 *
 * This results in a table being created called 'thumbwhere_producer'
 *
 * thumbwhere_producer_id
 * The primary key used by
 *
 * thumbwhere_id
 * Used to map to the actual object in ThumbWhere.

 */
function thumbwhere_producer_schema() {
  $t = get_t();

  $schema = array();

  $schema['thumbwhere_producer'] = array(
      'description' => 'The base table for thumbwhere_producer entities.',
      'fields' => array(
          'thumbwhere_producer_id' => array(
              'description' => 'Primary Key: Identifier for a thumbwhere_producer.',
              'type' => 'serial',
              'unsigned' => TRUE,
              'not null' => TRUE,
          ),
          'thumbwhere_id' => array(
              'description' => 'ThumbWhere Id: ThumbWhere\'s id for this entity. Represents the id of this entity in the ThumbWhere \'ThumbWhereProducers\' entity.',
              'type' => 'int',
              'not null' => TRUE,
              'default' => -1,
          ),
          'name' => array(
              'description' => 'The name of the thumbwhere_producer - a human-readable identifier.',
              'type' => 'varchar',
              'length' => 255,
              'not null' => TRUE,
              'default' => '',
          ),
          'genre' => array(
              'description' => 'The name of the primary genre of this thumbwhere_producer.',
              'type' => 'varchar',
              'length' => 255,
              'not null' => TRUE,
              'default' => '',
          ),
      ),
      'primary key' => array(
          'thumbwhere_producer_id'
      ),
      'indexes' => array(
          'thumbwhere_id' => array(
              'thumbwhere_id'
          ),
      ),
  );

  return $schema;
}

