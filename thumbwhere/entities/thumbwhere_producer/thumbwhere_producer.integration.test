<?php

/**
 * @file
 * Tests for thumbwhere_producer_field_storage.module.
 *
 * Field_sql_storage.module implements the default back-end storage plugin
 * for the Field Strage API.
 */

/**
 * Tests field storage.
 */
class ThumbWhereProducerIntegrationTestCase extends DrupalWebTestCase {
  public static function getInfo() {
    return array(
      'name'  => 'Producer Entity Integration Tests',
      'description'  => "Test ThumbWhere Producer Entity module.",
      'group' => 'ThumbWhere_Integration_Tests'
    );
  }

  function setUp() {

    // Make sure we are using the testing profile
    $this->profile = 'testing';

    parent::setUp('thumbwhere_producer', 'field', 'field_test', 'text');
  }

  /**
   * Test that the field storage works.
   */
  function testSomething() {
      entity_create('thumbwhere_producer', array( 'name' => 'bob' ));
    }
}

