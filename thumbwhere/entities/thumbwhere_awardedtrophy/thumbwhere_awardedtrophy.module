<?php

// ******************************************************************
// This file was generated by autogen_model_implementation_drupal.xsl
// ******************************************************************

// Include the ThumbWhere SDK
require_once(dirname(__FILE__) . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . 'dependencies' . DIRECTORY_SEPARATOR . 'tw' . DIRECTORY_SEPARATOR . 'sdk.class.php');

/**
 * @file
 * Module for the ThumbWhere AwardedTrophy Entity
 *
 *
 * NOTE: This has been autogenerated.
 * 
 */

/**
 * Implement hook_entity_info().
 *
 * We define two entities here - the actual entity that will hold our domain
 * specific information and an entity that holds information about the different
 * types of entities. See here: http://drupal.org/node/977380 for a discussion on this
 * choice.
 */
function thumbwhere_awardedtrophy_entity_info() {
  $return['thumbwhere_awardedtrophy'] = array(

    // The human-readable name of the type.
    'label' => t('ThumbWhereAwardedTrophy'),

    // (used by DrupalDefaultEntityController) The name of the entity type's base table.
    'base table' => 'thumbwhere_awardedtrophy',

    // fieldable: Set to TRUE if you want your entity type to accept fields being attached to it.
    'fieldable' => TRUE,

  // Declare the endty keys..
    'entity keys' => array(
      'id' => 'pk_awardedtrophy',
      'label' => 'pk_awardedtrophy',
    ),

    // A function taking an entity and an entity type as arguments and returning the label of the entity.
    // The entity label is the main string associated with an entity; for example, the title of a node or
    // the subject of a comment. If there is an entity object property that defines the label, use the
    // 'label' element of the 'entity keys' return value component to provide this information (see below).
    // If more complex logic is needed to determine the label of an entity, you can instead specify a callback
    // function here, which will be called to determine the entity label. See also the entity_label()
    // function, which implements this logic.
    'label callback' => 'entity_class_label',

    // A function taking an entity as argument and returning the uri elements of the entity, e.g. 'path' and 'options'.
    // The actual entity uri can be constructed by passing these elements to url().
    'uri callback' => 'entity_class_uri',

    //
    // Entity API Components
    //

    // The module whwre we can find our classes
    'module' => 'thumbwhere_awardedtrophy',

    // The entity class and controller class extend the classes provided by the Entity API
    'entity class' => 'ThumbWhereAwardedTrophy',

    // Which Entity API contoller
    'controller class' => 'ThumbWhereAwardedTrophyController',

    // The information below is used by the ThumbWhereAwardedTrophyUIController (which extends the EntityDefaultUIController)
    'admin ui' => array(
      'path' => 'admin/thumbwhere/thumbwhere_awardedtrophys',
      'file' => 'thumbwhere_awardedtrophy.admin.inc',
      'controller class' => 'ThumbWhereAwardedTrophyUIController',
      'menu wildcard' => '%thumbwhere_awardedtrophy',
    ),
  );

  return $return;
}


/**
 * Implements hook_permission().
 */
function thumbwhere_awardedtrophy_permission() {

  // We set up permisssions to manage entity types, manage all entities and the
  // permissions for each individual entity
  $permissions = array(
    'administer thumbwhere_awardedtrophys' => array(
      'title' => t('Administer thumbwhere_awardedtrophys'),
      'description' => t('Edit and delete all thumbwhere_awardedtrophys'),
    ),
  'send thumbwhere awardedtrophy notifications' => array(
          'title' => t('Send ThumbWhere Notifications'),
          'description' => t('Allow the caller to call the ThumbWhere AwardedTrophy Entity Notifications Endpoint.'),
          'restrict access' => TRUE,
      ),    
  );

  return $permissions;
}


/**
 * Determines whether the given user has access to a thumbwhere_awardedtrophy.
 *
 * @param $op
 *   The operation being performed. One of 'view', 'update', 'create', 'delete'
 *   or just 'edit' (being the same as 'create' or 'update').
 * @param $thumbwhere_awardedtrophy
 *   Optionally a thumbwhere_awardedtrophy or a thumbwhere_awardedtrophy type to check access for. If nothing is
 *   given, access for all thumbwhere_awardedtrophys is determined.
 * @param $account
 *   The user to check for. Leave it to NULL to check for the global user.
 * @return boolean
 *   Whether access is allowed or not.
 */
function thumbwhere_awardedtrophy_access($op, $thumbwhere_awardedtrophy = NULL, $account = NULL) {

  //if (user_access('administer thumbwhere_awardedtrophys', $account)) {
  //  return TRUE;
 // }

  //return FALSE;
  return TRUE;
}




/**
 * Fetch a thumbwhere_awardedtrophy object. Make sure that the wildcard you choose
 * in the thumbwhere_awardedtrophy entity definition fits the function name here.
 *
 * @param $thumbwhere_awardedtrophy_id
 *   Integer specifying the thumbwhere_awardedtrophy id.
 * @param $reset
 *   A boolean indicating that the internal cache should be reset.
 * @return
 *   A fully-loaded $thumbwhere_awardedtrophy object or FALSE if it cannot be loaded.
 *
 * @see thumbwhere_awardedtrophy_load_multiple()
 */
function thumbwhere_awardedtrophy_load($thumbwhere_awardedtrophy_id, $reset = FALSE) {

  $thumbwhere_awardedtrophys = entity_load('thumbwhere_awardedtrophy', array($thumbwhere_awardedtrophy_id), array(), $reset);

  return reset($thumbwhere_awardedtrophys);
}


/**
 * Deletes a thumbwhere_awardedtrophy.
 */
function thumbwhere_awardedtrophy_delete(ThumbWhereAwardedTrophy $thumbwhere_awardedtrophy) {

  $thumbwhere_awardedtrophy->delete();
}

/**
 * Create a thumbwhere_awardedtrophy object.
 */
function thumbwhere_awardedtrophy_create($values = array()) {

  return entity_get_controller('thumbwhere_awardedtrophy')->create($values);
}


/**
 * URI callback for thumbwhere_awardedtrophys
 */
function thumbwhere_awardedtrophy_uri(ThumbWhereAwardedTrophy $thumbwhere_awardedtrophy) {

  return array(
    'path' => 'thumbwhere_awardedtrophy/' . $thumbwhere_awardedtrophy->pk_awardedtrophy,
  );
}


/**
 * Menu title callback for showing individual entities
 */
function thumbwhere_awardedtrophy_page_title(ThumbWhereAwardedTrophy $thumbwhere_awardedtrophy) {

	return $thumbwhere_awardedtrophy->pk_awardedtrophy;
}


/**
 * Sets up content to show an individual thumbwhere_awardedtrophy
 * @todo - get rid of drupal_set_title();
 */
function thumbwhere_awardedtrophy_page_view($thumbwhere_awardedtrophy, $view_mode = 'full') {



  $controller = entity_get_controller('thumbwhere_awardedtrophy');
  $content = $controller->view(array($thumbwhere_awardedtrophy->pk_awardedtrophy => $thumbwhere_awardedtrophy));
  drupal_set_title($thumbwhere_awardedtrophy->pk_awardedtrophy);
  return $content;
}


/**
 * Implements hook_views_api().
 */
function thumbwhere_awardedtrophy_views_api() {



  return array(
    'api' => 3,
    'path' => drupal_get_path('module', 'thumbwhere_awardedtrophy') . '/views',
  );
}


/**
 * Implement hook_theme().
 */
function thumbwhere_awardedtrophy_theme() {



  return array(
    'thumbwhere_awardedtrophy_add_list' => array(
      'variables' => array('content' => array()),
      'file' => 'thumbwhere_awardedtrophy.admin.inc',
    ),
    'thumbwhere_awardedtrophy' => array(
      'render element' => 'elements',
      'template' => 'thumbwhere_awardedtrophy',
    )
  );
}


/**
 * Implements hook_menu_local_tasks_alter().
 */
function thumbwhere_awardedtrophy_menu_local_tasks_alter(&$data, $router_item, $root_path) {

  // Add action link 'admin/structure/thumbwhere_awardedtrophys/add' on 'admin/structure/thumbwhere_awardedtrophys'.
  if ($root_path == 'admin/thumbwhere/thumbwhere_awardedtrophys') {
    $item = menu_get_item('admin/thumbwhere/thumbwhere_awardedtrophys/add');
    if ($item['access']) {
      $data['actions']['output'][] = array(
        '#theme' => 'menu_local_action',
        '#link' => $item,
      );
    }
  }
}


/**
 * The class used for thumbwhere_awardedtrophy entities
 */
class ThumbWhereAwardedTrophy extends Entity {

  public function __construct($values = array()) {
    parent::__construct($values, 'thumbwhere_awardedtrophy');
  }

  protected function defaultLabel() {
    return $this->pk_awardedtrophy;
  }

  protected function defaultUri() {
    return array('path' => 'thumbwhere_awardedtrophy/' . $this->pk_awardedtrophy);
  }


}



/**
 * The Controller for ThumbWhereAwardedTrophy entities
 */
class ThumbWhereAwardedTrophyController extends EntityAPIController {
  public function __construct($entityType) {
    parent::__construct($entityType);
  }
}



/**
* Implements hook_entity_presave().
*
* Act on an AwardedTrophy entity before it is about to be created or updated.
*
* @param $entity
*   The entity object.
* @param $entity_type
*   The type of entity being saved (i.e. node, user, comment).
*/
function thumbwhere_awardedtrophy_entity_presave($entity, $entity_type) {

  // We only look after what we want...
  if ($entity_type == 'thumbwhere_awardedtrophy')  {
  	
  	
  	
 	/////////////////////////////////////////////////////
    //
    // Should we not propagate this to ThumbWhere? 
    //
    if (isset($entity->do_not_propagate)) {
      if ($entity->do_not_propagate) {
        if (twCanDebug()) {
          if ($entity->is_new) {
            debug("DO NOT PROPAGATE 'awardedtrophy' presave *CREATE*");
          }
          else {
            debug("DO NOT PROPAGATE 'awardedtrophy' presave *UPDATE*");
          }
        }

        // To cause an infinite loop of updates, comment out this line.
        return;
      }
      else if (twCanDebug()) {
        debug("OK PROAOGATE 'awardedtrophy' presave to ThumbWhere Server");
      }
    }
    else if (twCanDebug()) {
      debug("DO NOT PROPAGATE IS NOT SET");
    }
    //
    //
    ////////////////////////////////////////////////////////  	

  
    // .. then we create a new thumbwhere_awardedtrophy entity...
      // Instantiate the ThumbWhereContent class

    $twContentAPI = new ThumbWhereAPIContent();

    //if (twCanDebug()) debug($twContentAPI);

    //if (twCanDebug()) debug($entity);

    //
    // Collate and validate our fields
    //

    $fields = array();
    // Compulsory so..
    if (empty($entity->fk_trophy)) {
      throw new Exception('trophy is compulsory.');
    }
    else {
      $fields['trophy'] = $entity->fk_trophy;
    }
    // Compulsory so..
    if (empty($entity->fk_actor)) {
      throw new Exception('actor is compulsory.');
    }
    else {
      $fields['actor'] = $entity->fk_actor;
    }
    // Compulsory so..
    if (empty($entity->fk_thing)) {
      throw new Exception('thing is compulsory.');
    }
    else {
      $fields['thing'] = $entity->fk_thing;
    }
    // Compulsory so..
    if (empty($entity->documentation)) {
      throw new Exception('documentation is compulsory.');
    }
    else {
      $fields['documentation'] = $entity->documentation;
    }
    
    //
    // If this awardedtrophy entity is new....
    //
    
    if ($entity->is_new) {
                

      //
      // ... then we create the awardedtrophy entity in ThumbWhere
      //

      $awardedtrophy = $twContentAPI->create_awarded_trophy($context = array(
                  'key' => variable_get('thumbwhere_api_key')
          ), $fields);
            
       if (twCanDebug()) debug($awardedtrophy);

      //if (twCanDebug()) debug($awarded_trophy);

      $attributes = $awardedtrophy->body->awarded_trophy->attributes();

      if (twCanDebug()) debug('newly created id = ' . $attributes['id']);

      // Assign new id.
      $entity->pk_awardedtrophy = intval($attributes['id']);

      if (twCanDebug()) debug('$entity->pk_awardedtrophy = ' . $entity->pk_awardedtrophy);
    }
    else
    {
          	

      //
      // .. otherwise we update the awardedtrophy entity in ThumbWhere
      //

      $awardedtrophy = $twContentAPI->update_awarded_trophy($id=$entity->pk_awardedtrophy,$context = array(
                  'key' => variable_get('thumbwhere_api_key')
          ), $fields);
            
        
    }
          	
    
  }
}

/**
 * Implements thumbwhere_awardedtrophy_entity_insert().
 *
 * Act on entities when inserted.
 *
 * @param $entity
 *   The entity object
 * @param $type
 *   The type of entity being inserted (i.e. node, user, comment).
 */
function thumbwhere_awardedtrophy_entity_insert($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_awardedtrophy') return;

}


/**
* Implements thumbwhere_awardedtrophy_entity_delete().
*
* Act on entities when inserted.
*
* @param $entity
*   The entity object
* @param $type
*   The type of entity being deleted (i.e. node, user, comment).
*/
function thumbwhere_awardedtrophy_entity_delete($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_awardedtrophy') return;


}
