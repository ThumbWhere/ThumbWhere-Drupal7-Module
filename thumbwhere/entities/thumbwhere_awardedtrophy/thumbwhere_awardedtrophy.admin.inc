<?php

/**
 * @file
 * ThumbWhereAwardedTrophy editing UI.
 *
 * We make very little use of the EntityAPI interface for this - preferring instead to use
 * views. That offers more flexibility to change a UI that will, more often than not,
 * be end-user facing.
 *
 * NOTE: This file was generated by autogen_model_implementation_drupal.xsl
 */

 
/**
 * Handle the notification for content_ingext
 */
function _thumbwhere_handle_awarded_trophy_notify_xml($notification) {

  if (twCanDebug()) debug('PROCESS NOTIFICATION');
  if (twCanDebug()) debug($notification);

  //
  // Get the operation
  //

  if (!isset($notification->attributes()->op)) {
    drupal_add_http_header('Status', '501 unable to fulfil request..');
    $err = 'We were expecting an \'op\', attribute on the \'awarded_trophy\', element.';
    print($err);
   	watchdog('thumbwhere_awardedtrophy', $err ,array(), WATCHDOG_ERROR);
    exit(0);
  }

  $op = $notification->attributes()->op;

  if (twCanDebug()) debug('op');
  if (twCanDebug()) debug($op);

  //
  // If this is a notification then $op should be 'notify'
  //

  if (!isset($notification->attributes()->notification)) {
    drupal_add_http_header('Status', '501 unable to fulfil request..');
    $err = 'We were expecting an \'notification\', attribute on the \'awarded_trophy\', element.';
    print($err);
   	watchdog('thumbwhere_awardedtrophy', $err ,array(), WATCHDOG_ERROR);
    exit(0);
  }

  $notification_op = $notification->attributes()->notification;

  if (twCanDebug()) debug('notification');
  if (twCanDebug()) debug($notification_op);


  //
  // Get the context
  //

  if (!isset($notification->attributes()->key)) {
    drupal_add_http_header('Status', '412 unable to fulfil request..');
    $err = 'We were expecting a \'key\', attribute on the \'awarded_trophy\', element.';
    print($err);
    watchdog('thumbwhere_awardedtrophy', $err ,array(), WATCHDOG_ERROR);
    exit(0);
  }

  $context_key =  $notification->attributes()->key;

  // Start a transaction
  //$transaction = db_transaction('awardedtrophy_notify');
  $transaction = null;

  //
  // Now based on the operation..
  //

  switch($notification_op)
  {
  
  
      case 'create':
      {
      		
		
		    //
		    // We want to time this
		    //
			
		    $start_time = microtime(TRUE);
			
        //
        // What resource are we creating?
        //
      
        //
        // Get the id
        //
		
        if (!isset($notification->attributes()->id)) {
          drupal_add_http_header('Status', '412 Unable to fulfil request.');
          $err = 'We were expecting an \'id\', attribute on the \'awarded_trophy\', element when we are handling a \'create\' notification..';
          print($err);
          watchdog('thumbwhere_awardedtrophy', $err ,array(), WATCHDOG_ERROR);
          exit(0);
        }

        $id = intval($notification->attributes()->id);
		

        if (twCanDebug()) debug('id');
        if (twCanDebug()) debug($id);


        //
        // Get the origin
        //
		
		    $origin = -1;
		    
        if (isset($notification->attributes()->origin)) {        
          $origin = intval($notification->attributes()->origin);

          if (twCanDebug()) debug('origin');
          if (twCanDebug()) debug($origin);

          if ($origin == variable_get('thumbwhere_host_id',-1)) {
            $err = 'Origin of this notification ' . $origin  . ' is the same as this host. \'awarded_trophy\', Dropping this \'create\' notification.';
            watchdog('thumbwhere_awardedtrophy', $err ,array(), WATCHDOG_ERROR);
              
            // We act all idempotent like its all good. 'create', when it already exists, is ignored. 
            drupal_add_http_header('Content-Type', 'text/xml');
            print('<response>');
            print('<awarded_trophy op="' . $op . '" id="' . $id . '" origin="' . $origin . '" >');
            print('<status>success</status>');
            print('</awarded_trophy>');
            print('</response>');
            exit(0);               
          }
        }
              
        watchdog('thumbwhere_awardedtrophy', 'Notification: \'create\' awardedtrophy #<a href="/admin/thumbwhere/thumbwhere_awardedtrophys/thumbwhere_awardedtrophy/' . $id .  '/edit">\'' . $id . '\'</a>  origin=\'' . $origin . '\'.',array(), WATCHDOG_NOTICE);
      
        //
        // Get the fields
        //
          
        $entities = entity_load('thumbwhere_awardedtrophy', array($id));

        if (twCanDebug()) debug('$entities');
        if (twCanDebug()) debug($entities);

        if (count($entities) != 0) {
          //drupal_add_http_header('Status', '409 Resource already exists.');
          $err = 'Error while servicing \'create\' notification. Resource \'awarded_trophy\' already exists with an id of \'' . $id . '\'.';
           //print($err);
          // When ORIGIN (TW-171) is sorted out then this can be re-anbled
          watchdog('thumbwhere_awardedtrophy', $err ,array(), WATCHDOG_ERROR);

          	
          // We act all idempotent like its all good. 'create', when it already exists, is ignored. 
          drupal_add_http_header('Content-Type', 'text/xml');
          print('<response>');
          print('<awarded_trophy op="' . $op . '" id="' . $id . '" origin="' . $origin . '" >');
          print('<status>success</status>');
          print('</awarded_trophy>');
          print('</response>');
          exit(0);          	
          
        }

        // Getting a reference to the entity we asked for
        $entityfields = array();

        //
        // We are only updating what has been passed through.
        // No test for missing values.
        // TODO: Validate incoming values.
        //


        $entityfields['pk_awardedtrophy'] = $id;	

        if (isset($notification->trophy)) {
          $entityfields['fk_trophy'] = intval($notification->trophy);
        }

        if (isset($notification->actor)) {
          $entityfields['fk_actor'] = intval($notification->actor);
        }

        if (isset($notification->thing)) {
          $entityfields['fk_thing'] = intval($notification->thing);
        }

        if (isset($notification->documentation)) {
          $entityfields['documentation'] = (string)($notification->documentation);
        }

        if (!isset($notification->attributes()->key)) {
          if (twCanDebug()) debug('$entities');
          if (twCanDebug()) debug($entities);
        }

        // We are being notified, so we do not want to ship this out again as a notification...
        // Finally write the entity.
        $entity = entity_create('thumbwhere_awardedtrophy', $entityfields);
        $entity->do_not_propagate = TRUE;
        $entity->save($transaction);

        //
        // Send the final response
        //
        
        $duration = round(((microtime(TRUE) - $start_time) * 1000.0),3); 

        drupal_add_http_header('Content-Type', 'text/xml');
        print('<response duration="' . $duration . 'ms">');
        print('<awarded_trophy op="' . $op . '" id="' . $id . '" origin="' . $origin . '" >');
        print('<status>success</status>');
        print('</awarded_trophy>');
        print('</response>');
        exit(0);			
		
      break;
      }
  
  
    case 'update':
      {
      
    		//
    		// We want to time this
    		//
    			
    		$start_time = microtime(TRUE);
      
        //
        // What resource are we updating?
        //
        
      
        //
        // Get the id
        //
		
        if (!isset($notification->attributes()->id)) {
          drupal_add_http_header('Status', '412 Unable to fulfil request.');
          $err = 'We were expecting an \'id\', attribute on the \'awarded_trophy\', element when we are performing an \'update\' notification..';
          print($err);
          watchdog('thumbwhere_awardedtrophy', $err ,array(), WATCHDOG_ERROR);
          exit(0);
        }

        $id = intval($notification->attributes()->id);
		

        if (twCanDebug()) debug('id');
        if (twCanDebug()) debug($id);

            
        //
        // Get the origin
        //
		
		    $origin = -1;
		    
        if (isset($notification->attributes()->origin)) {                
          $origin = intval($notification->attributes()->origin);

          if (twCanDebug()) debug('origin');
          if (twCanDebug()) debug($origin);

          if ($origin == variable_get('thumbwhere_host_id',-1)) {
            $err = 'Origin of this notification ' . $origin  . ' is the same as this host. \'awarded_trophy\', Dropping this \'update\' notification.';
            watchdog('thumbwhere_awardedtrophy', $err ,array(), WATCHDOG_ERROR);
              
            // We act all idempotent like its all good. 'create', when it already exists, is ignored. 
            drupal_add_http_header('Content-Type', 'text/xml');
            print('<response>');
            print('<awarded_trophy op="' . $op . '" id="' . $id . '" origin="' . $origin . '">');
            print('<status>success</status>');
            print('</awarded_trophy>');
            print('</response>');
            exit(0);               
          }
        }
          
        watchdog('thumbwhere_awardedtrophy', 'Notification: \'' . $notification_op . '\' awardedtrophy #<a href="/admin/thumbwhere/thumbwhere_awardedtrophys/thumbwhere_awardedtrophy/' . $id .  '/edit"> \'' . $id .  '\'</a>  origin=\'' . $origin . '\'.',array(), WATCHDOG_NOTICE);
       
        //
        // Get the fields
        //
          
        $entities = entity_load('thumbwhere_awardedtrophy', array($id));

        if (twCanDebug()) debug('$entities');
        if (twCanDebug()) debug($entities);

        if (count($entities) == 0) {
          //drupal_add_http_header('Status', '409 No such resource exists.');
          $err = 'No such \'awarded_trophy\' resource exists with an id of \'' . $id . '\'.';
          //print($err);
          watchdog('thumbwhere_awardedtrophy',$err,array(), WATCHDOG_ERROR);
          //exit(0);

////////////////////////////////////////////////////////////////////////
//
// ... for now we will just create the local copy
//
          watchdog('thumbwhere_awardedtrophy','creating local version of missing entity',array(), WATCHDOG_ERROR);
		
		
		    //
		    // We want to time this
		    //
			
		    $start_time = microtime(TRUE);
			
        //
        // What resource are we creating?
        //
      
        //
        // Get the id
        //
		
        if (!isset($notification->attributes()->id)) {
          drupal_add_http_header('Status', '412 Unable to fulfil request.');
          $err = 'We were expecting an \'id\', attribute on the \'awarded_trophy\', element when we are handling a \'update\' notification..';
          print($err);
          watchdog('thumbwhere_awardedtrophy', $err ,array(), WATCHDOG_ERROR);
          exit(0);
        }

        $id = intval($notification->attributes()->id);
		

        if (twCanDebug()) debug('id');
        if (twCanDebug()) debug($id);


        //
        // Get the origin
        //
		
		    $origin = -1;
		    
        if (isset($notification->attributes()->origin)) {        
          $origin = intval($notification->attributes()->origin);

          if (twCanDebug()) debug('origin');
          if (twCanDebug()) debug($origin);

          if ($origin == variable_get('thumbwhere_host_id',-1)) {
            $err = 'Origin of this notification ' . $origin  . ' is the same as this host. \'awarded_trophy\', Dropping this \'update\' notification.';
            watchdog('thumbwhere_awardedtrophy', $err ,array(), WATCHDOG_ERROR);
              
            // We act all idempotent like its all good. 'create', when it already exists, is ignored. 
            drupal_add_http_header('Content-Type', 'text/xml');
            print('<response>');
            print('<awarded_trophy op="' . $op . '" id="' . $id . '" origin="' . $origin . '" >');
            print('<status>success</status>');
            print('</awarded_trophy>');
            print('</response>');
            exit(0);               
          }
        }
              
        watchdog('thumbwhere_awardedtrophy', 'Notification: \'update\' awardedtrophy #<a href="/admin/thumbwhere/thumbwhere_awardedtrophys/thumbwhere_awardedtrophy/' . $id .  '/edit">\'' . $id . '\'</a>  origin=\'' . $origin . '\'.',array(), WATCHDOG_NOTICE);
      
        //
        // Get the fields
        //
          
        $entities = entity_load('thumbwhere_awardedtrophy', array($id));

        if (twCanDebug()) debug('$entities');
        if (twCanDebug()) debug($entities);

        if (count($entities) != 0) {
          //drupal_add_http_header('Status', '409 Resource already exists.');
          $err = 'Error while servicing \'update\' notification. Resource \'awarded_trophy\' already exists with an id of \'' . $id . '\'.';
           //print($err);
          // When ORIGIN (TW-171) is sorted out then this can be re-anbled
          watchdog('thumbwhere_awardedtrophy', $err ,array(), WATCHDOG_ERROR);

          	
          // We act all idempotent like its all good. 'create', when it already exists, is ignored. 
          drupal_add_http_header('Content-Type', 'text/xml');
          print('<response>');
          print('<awarded_trophy op="' . $op . '" id="' . $id . '" origin="' . $origin . '" >');
          print('<status>success</status>');
          print('</awarded_trophy>');
          print('</response>');
          exit(0);          	
          
        }

        // Getting a reference to the entity we asked for
        $entityfields = array();

        //
        // We are only updating what has been passed through.
        // No test for missing values.
        // TODO: Validate incoming values.
        //


        $entityfields['pk_awardedtrophy'] = $id;	

        if (isset($notification->trophy)) {
          $entityfields['fk_trophy'] = intval($notification->trophy);
        }

        if (isset($notification->actor)) {
          $entityfields['fk_actor'] = intval($notification->actor);
        }

        if (isset($notification->thing)) {
          $entityfields['fk_thing'] = intval($notification->thing);
        }

        if (isset($notification->documentation)) {
          $entityfields['documentation'] = (string)($notification->documentation);
        }

        if (!isset($notification->attributes()->key)) {
          if (twCanDebug()) debug('$entities');
          if (twCanDebug()) debug($entities);
        }

        // We are being notified, so we do not want to ship this out again as a notification...
        // Finally write the entity.
        $entity = entity_create('thumbwhere_awardedtrophy', $entityfields);
        $entity->do_not_propagate = TRUE;
        $entity->save($transaction);

        //
        // Send the final response
        //
        
        $duration = round(((microtime(TRUE) - $start_time) * 1000.0),3); 

        drupal_add_http_header('Content-Type', 'text/xml');
        print('<response duration="' . $duration . 'ms">');
        print('<awarded_trophy op="' . $op . '" id="' . $id . '" origin="' . $origin . '" >');
        print('<status>success</status>');
        print('</awarded_trophy>');
        print('</response>');
        exit(0);			
		
//
//
//
////////////////////////////////////////////////////////////////////////
        }

        // Getting a reference to the entity we asked for
        $entity = $entities[$id];

        //
        // We are only updating what has been passed through.
        // No test for missing values.
        // TODO: Validate incoming values.
        //


        if (isset($notification->trophy)) {
          $entity->fk_trophy = intval($notification->trophy);
        }

        if (isset($notification->actor)) {
          $entity->fk_actor = intval($notification->actor);
        }

        if (isset($notification->thing)) {
          $entity->fk_thing = intval($notification->thing);
        }

        if (isset($notification->documentation)) {
          $entity->documentation = (string)($notification->documentation);
        }

        if (!isset($notification->attributes()->key)) {
          if (twCanDebug()) debug('$entities');
          if (twCanDebug()) debug($entities);
        }

        // We are being notified, so we do not want to ship this out again as a notification...
        $entity->do_not_propagate = TRUE;

        // Finally write the entity.
        //entity_save('thumbwhere_awardedtrophy', $entity);
        $entity->save($transaction);

        //
        // Send the final response
        //
        
        $duration = round(((microtime(TRUE) - $start_time) * 1000.0),3);

        drupal_add_http_header('Content-Type', 'text/xml');
        print('<response  duration="' . $duration . '" >');
        print('<awarded_trophy op="' . $op . '" notification="' . $notification_op . '" id="' . $id . '" >');
        print('<status>success</status>');
        print('</awarded_trophy>');
        print('</response>');
        exit(0);
      }

    default:
      {
        drupal_add_http_header('Status', '501 Unable to fulfil request. Unhandled operation \'' . $op . '\'.');
        print('We don\'t handle the \'' . $notification_op . '\', notification operation on the \'awarded_trophy\', element.');
        exit(0);
      }
  }
}



/**
 * Menu callback: React to a notification for awardedtrophy.
 */
function _thumbwhere_awarded_trophy_notify() {

  if (twCanDebug()) {
    watchdog('thumbwhere_awarded_trophy', 'incoming notification.');
  }

  $numargs = func_num_args();
  $arg_list = func_get_args();

  
	if (twCanDebug()) debug('$numargs  = ' . $numargs);
	
	if (twCanDebug()) debug('$arg_list = ' . $arg_list);
	
	if (twCanDebug()) debug('$_REQUEST');
	if (twCanDebug()) debug($_REQUEST);
	
	if (twCanDebug()) debug('$_GET');
	if (twCanDebug()) debug($_GET);
	
	if (twCanDebug()) debug('$_POST');
	if (twCanDebug()) debug($_POST);
	
	//if (twCanDebug()) debug('$HTTP_RAW_POST_DATA');
	//if (twCanDebug()) debug($HTTP_RAW_POST_DATA);
	
	if (twCanDebug()) debug('$_SERVER');
	if (twCanDebug()) debug($_SERVER);
	
	//if (twCanDebug()) debug('$GLOBALS');
	//if (twCanDebug()) debug($GLOBALS);

  

  if (variable_get('thumbwhere_api_log_debug', 0) == 1) {
    foreach ($arg_list as $arg) {
      if (twCanDebug()) debug('$arg  = ' . $arg);
    }
  }


  //////////////////////////////////////////////////
  //
  //

  switch ($_SERVER['REQUEST_METHOD']) {

    case 'POST': {
        if ($numargs == 1) {
          if ($arg_list[0] == 'awarded_trophy') {

            //
            // Get the POST data as XML
            //

            $post_xml = new SimpleXMLElement('php://input', null, true);
            if (twCanDebug()) debug('$post_xml');
            if (twCanDebug()) debug($post_xml);

            if (twCanDebug()) debug('$post_xml->getName()');
            if (twCanDebug()) debug($post_xml->getName());

            // We expect 'action' as the root element. TODO: I would rather this was the more generic 'request'
            if ($post_xml->getName() != 'action') {
              drupal_add_http_header('Status', '404 not found.');
              print('We were expecting \'action\', as the root element, not \'' . $post_xml->getName() . '\'.');
              exit(0);
            }

            /*
             *  SimpleXMLElement::__set_state(array(
             *      'awarded_trophy' =>
             *      SimpleXMLElement::__set_state(array(
             *       '@attributes' =>
             *      array (
             *        'key' => '8e9ff318-3cd7-4593-99de-4c37c0fb335a',
             *        'op' => 'create',
             *      ),
             *    )),
             *  ))
             */


            // We expect 'action' as the root element.
            if (!isset($post_xml->awarded_trophy)) {
              drupal_add_http_header('Status', '404 not found.');
              print('We were expecting a \'awarded_trophy\', element in the action payload.');
              exit(0);
            }

            // Handle the incoming notification for content ingest
            _thumbwhere_handle_awarded_trophy_notify_xml($post_xml->awarded_trophy);

          }
          else {
            drupal_add_http_header('Status', '404 not found.');
            print('Invalid resource');
            exit(0);
          }
        }
        else {
          drupal_add_http_header('Status', '404 not found.');
          print('Invalid notification request url format.');
          exit(0);
        }
      }
      break;

    case 'GET': {

        if ($numargs == 1) {

          // If we are returning the index.xml file for the webservice.
          if ($arg_list[0] == 'index.xml') {
            // return index.xml
            drupal_add_http_header('Content-Type', 'text/xml');
            readfile(dirname(__FILE__) . '/index.xml');
            exit(0);
          }
          else {
            drupal_add_http_header('Status', '404 not found.');
            print('File not found.');
            exit(0);
          }
        }
        else {
          drupal_add_http_header('Status', '404 not found.');
          print('Invalid notification request url format.');
          exit(0);
        }
      }
      break;

    default: {
        drupal_add_http_header('Status', '404 not found.');
        exit(0);
      }
      break;
  }

  //
  //
  ////////////////////////////////////////////////////////////////////////////////////
 
}





/**
 * AwardedTrophy UI controller.
 */
class ThumbWhereAwardedTrophyUIController extends EntityDefaultUIController {

  /**
   * Overrides hook_menu() defaults. Main reason for doing this is that
   * parent class hook_menu() is optimized for entity type administration.
   */
  public function hook_menu() {

    $items = array();
    
    // Add a notification page...
    $items['thumbwhere/awarded_trophy/notify'] = array(
        'title' => 'Notifications Callback for "AwardedTrophy" Entity',
        'page callback' => '_thumbwhere_awarded_trophy_notify',
        'access arguments' => array(
            'send thumbwhere awardedtrophy notifications'
        ),
        'file' => 'thumbwhere_awardedtrophy.admin.inc',
        'file path' => drupal_get_path('module', $this->entityInfo['module'])
    );    
    
    $id_count = count(explode('/', $this->path));
    $wildcard = isset($this->entityInfo['admin ui']['menu wildcard']) ? $this->entityInfo['admin ui']['menu wildcard'] : '%' . $this->entityType;

    $items[$this->path] = array(
      'title' => 'AwardedTrophy',
      'description' => 'Add edit and update thumbwhere_awardedtrophys.',
      'page callback' => 'system_admin_menu_block_page',
      'access arguments' => array('access administration pages'),
      'file path' => drupal_get_path('module', 'system'),
      'file' => 'system.admin.inc',
    );

    // Change the overview menu type for the list of thumbwhere_awardedtrophys.
    $items[$this->path]['type'] = MENU_LOCAL_TASK;

    // Change the add page menu to multiple types of entities
    $items[$this->path . '/add'] = array(
      //'title' => 'Add a AwardedTrophy',
      'title' => 'Add',
      'description' => 'Add a new AwardedTrophy',
      'page callback' => 'thumbwhere_awardedtrophy_form_wrapper',
      'page arguments' => array(thumbwhere_awardedtrophy_create(array('type' => 'thumbwhere_awardedtrophy'))),
      'access callback' => 'thumbwhere_awardedtrophy_access',
      'access arguments' => array('edit', 'edit ' . 'thumbwhere_awardedtrophy'),
      'file' => 'thumbwhere_awardedtrophy.admin.inc',
      'file path' => drupal_get_path('module', $this->entityInfo['module'])
    );


/*
    // Change the add page menu to multiple types of entities
    $items[$this->path . '/add'] = array(
      //'title' => 'Add a AwardedTrophy',
      'title' => 'Add',
	  'description' => 'Add a new AwardedTrophy',
      'page callback'  => 'thumbwhere_awardedtrophy_add_page',
      'access callback'  => 'thumbwhere_awardedtrophy_access',
      'access arguments' => array('edit'),
      'type' => MENU_NORMAL_ITEM,
      'weight' => 20,
      'file' => 'thumbwhere_awardedtrophy.admin.inc',
      'file path' => drupal_get_path('module', $this->entityInfo['module'])

    );
*/    
/*
    $items[$this->path . '/add/' . 'thumbwhere_awardedtrophy'] = array(
      'title' => 'Add ' . 'ThumbWhereAwardedTrophy',
      'page callback' => 'thumbwhere_awardedtrophy_form_wrapper',
      'page arguments' => array(thumbwhere_awardedtrophy_create(array('type' => 'thumbwhere_awardedtrophy'))),
      'access callback' => 'thumbwhere_awardedtrophy_access',
      'access arguments' => array('edit', 'edit ' . 'thumbwhere_awardedtrophy'),
      'file' => 'thumbwhere_awardedtrophy.admin.inc',
      'file path' => drupal_get_path('module', $this->entityInfo['module'])
    );
*/
    // Loading and editing thumbwhere_awardedtrophy entities
    $items[$this->path . '/thumbwhere_awardedtrophy/' . $wildcard] = array(
      'page callback' => 'thumbwhere_awardedtrophy_form_wrapper',
      'page arguments' => array($id_count + 1),
      'access callback' => 'thumbwhere_awardedtrophy_access',
      'access arguments' => array('edit', $id_count + 1),
      'weight' => 0,
      'context' => MENU_CONTEXT_PAGE | MENU_CONTEXT_INLINE,
      'file' => 'thumbwhere_awardedtrophy.admin.inc',
      'file path' => drupal_get_path('module', $this->entityInfo['module'])
    );
    $items[$this->path . '/thumbwhere_awardedtrophy/' . $wildcard . '/edit'] = array(
      'title' => 'Edit',
      'type' => MENU_DEFAULT_LOCAL_TASK,
      'weight' => -10,
      'context' => MENU_CONTEXT_PAGE | MENU_CONTEXT_INLINE,
    );

    $items[$this->path . '/thumbwhere_awardedtrophy/' . $wildcard . '/delete'] = array(
      'title' => 'Delete',
      'page callback' => 'thumbwhere_awardedtrophy_delete_form_wrapper',
      'page arguments' => array($id_count + 1),
      'access callback' => 'thumbwhere_awardedtrophy_access',
      'access arguments' => array('edit', $id_count + 1),
      'type' => MENU_LOCAL_TASK,
      'context' => MENU_CONTEXT_INLINE,
      'weight' => 10,
      'file' => 'thumbwhere_awardedtrophy.admin.inc',
      'file path' => drupal_get_path('module', $this->entityInfo['module'])
    );

    // Menu item for viewing thumbwhere_awardedtrophys
    $items['thumbwhere_awardedtrophy/' . $wildcard] = array(
      //'title' => 'Title',
      'title callback' => 'thumbwhere_awardedtrophy_page_title',
      'title arguments' => array(1),
      'page callback' => 'thumbwhere_awardedtrophy_page_view',
      'page arguments' => array(1),
      'access callback' => 'thumbwhere_awardedtrophy_access',
      'access arguments' => array('view', 1),
      'type' => MENU_CALLBACK,
    );
    return $items;
  }


  /**
   * Create the markup for the add ThumbWhereAwardedTrophy Entities page within the class
   * so it can easily be extended/overriden.
   */
  public function addPage() {
    $item = menu_get_item();
    $content = system_admin_menu_block($item);

    if (count($content) == 1) {
      $item = array_shift($content);
      drupal_goto($item['href']);
    }

    return theme('thumbwhere_awardedtrophy_add_list', array('content' => $content));
  }

}


/**
 * Form callback wrapper: create or edit a thumbwhere_awardedtrophy.
 *
 * @param $thumbwhere_awardedtrophy
 *   The thumbwhere_awardedtrophy object being edited by this form.
 *
 * @see thumbwhere_awardedtrophy_edit_form()
 */
function thumbwhere_awardedtrophy_form_wrapper($thumbwhere_awardedtrophy) {
  // Add the breadcrumb for the form's location.
  thumbwhere_awardedtrophy_set_breadcrumb();
  return drupal_get_form('thumbwhere_awardedtrophy_edit_form', $thumbwhere_awardedtrophy);
}


/**
 * Form callback wrapper: delete a thumbwhere_awardedtrophy.
 *
 * @param $thumbwhere_awardedtrophy
 *   The thumbwhere_awardedtrophy object being edited by this form.
 *
 * @see thumbwhere_awardedtrophy_edit_form()
 */
function thumbwhere_awardedtrophy_delete_form_wrapper($thumbwhere_awardedtrophy) {
  // Add the breadcrumb for the form's location.
  //thumbwhere_awardedtrophy_set_breadcrumb();
  return drupal_get_form('thumbwhere_awardedtrophy_delete_form', $thumbwhere_awardedtrophy);
}


/**
 * Form callback: create or edit a thumbwhere_awardedtrophy.
 *
 * @param $thumbwhere_awardedtrophy
 *   The thumbwhere_awardedtrophy object to edit or for a create form an empty thumbwhere_awardedtrophy object
 *     with only a thumbwhere_awardedtrophy type defined.
 */
function thumbwhere_awardedtrophy_edit_form($form, &$form_state, $thumbwhere_awardedtrophy) {

  // Add the default field elements.
  $form['fk_trophy'] = array(
    '#type' => 'textfield',
    '#title' => t('ThumbWhereAwardedTrophy fk_trophy'),
    '#default_value' => isset($thumbwhere_awardedtrophy->fk_trophy) ? $thumbwhere_awardedtrophy->fk_trophy : '',
    //'#maxlength' => 255,
   /// '#required' => TRUE,
    '#weight' => -5,
    '#autocomplete_path' => 'entity-autocomplete/thumbwhere_trophy',
  );
  // Add the default field elements.
  $form['fk_actor'] = array(
    '#type' => 'textfield',
    '#title' => t('ThumbWhereAwardedTrophy fk_actor'),
    '#default_value' => isset($thumbwhere_awardedtrophy->fk_actor) ? $thumbwhere_awardedtrophy->fk_actor : '',
    //'#maxlength' => 255,
   /// '#required' => TRUE,
    '#weight' => -5,
    '#autocomplete_path' => 'entity-autocomplete/thumbwhere_actor',
  );
  // Add the default field elements.
  $form['fk_thing'] = array(
    '#type' => 'textfield',
    '#title' => t('ThumbWhereAwardedTrophy fk_thing'),
    '#default_value' => isset($thumbwhere_awardedtrophy->fk_thing) ? $thumbwhere_awardedtrophy->fk_thing : '',
    //'#maxlength' => 255,
   /// '#required' => TRUE,
    '#weight' => -5,
    '#autocomplete_path' => 'entity-autocomplete/thumbwhere_thing',
  );
  // Add the default field elements.
    $form['documentation'] = array(
    '#type' => 'textfield',
    '#title' => t('ThumbWhereAwardedTrophy documentation'),
    '#default_value' => isset($thumbwhere_awardedtrophy->documentation) ? $thumbwhere_awardedtrophy->documentation : '',
    // '#maxlength' => 255,
   // '#required' => TRUE,
    '#weight' => -5,
  );



  $form['data']['#tree'] = TRUE;

  /*
  $form['data']['sample_data'] = array(
    '#type' => 'checkbox',
    '#title' => t('An interesting thumbwhere_awardedtrophy switch'),
    '#default_value' => isset($thumbwhere_awardedtrophy->data['sample_data']) ? $thumbwhere_awardedtrophy->data['sample_data'] : 1,
  );
  */


  // Add the field related form elements.
  $form_state['thumbwhere_awardedtrophy'] = $thumbwhere_awardedtrophy;
  field_attach_form('thumbwhere_awardedtrophy', $thumbwhere_awardedtrophy, $form, $form_state);

  $form['actions'] = array(
    '#type' => 'container',
    '#attributes' => array('class' => array('form-actions')),
    '#weight' => 400,
  );

  // We add the form's #submit array to this button along with the actual submit
  // handler to preserve any submit handlers added by a form callback_wrapper.
  $submit = array();

  if (!empty($form['#submit'])) {
    $submit += $form['#submit'];
  }

  $form['actions']['submit'] = array(
    '#type' => 'submit',
    '#value' => t('Save thumbwhere_awardedtrophy'),
    '#submit' => $submit + array('thumbwhere_awardedtrophy_edit_form_submit'),
  );

  // Do we show the delete button?
  if (!empty($thumbwhere_awardedtrophy->pk_awardedtrophy)) {
    $form['actions']['delete'] = array(
      '#type' => 'submit',
      '#value' => t('Delete thumbwhere_awardedtrophy'),
      '#suffix' => l(t('Cancel'), 'admin/thumbwhere/thumbwhere_awardedtrophys'),
      '#submit' => $submit + array('thumbwhere_awardedtrophy_form_submit_delete'),
      '#weight' => 45,
    );
  }

  // We append the validate handler to #validate in case a form callback_wrapper
  // is used to add validate handlers earlier.
  $form['#validate'][] = 'thumbwhere_awardedtrophy_edit_form_validate';
  return $form;
}


/**
 * Form API validate callback for the thumbwhere_awardedtrophy form
 */
function thumbwhere_awardedtrophy_edit_form_validate(&$form, &$form_state) {
   
  //if (twCanDebug()) debug($form);   
  //if (twCanDebug()) debug($form_state);


  // No validation for pk


  // Convert fk autocomplete for fk_trophy
  $value = $form['fk_trophy']['#value'];
  // If we have something, escape the auto-completion encoding.
  if (!empty($value)) { 
    $value = entity_autocomplete_get_id($value);
    form_set_value($form['fk_trophy'], $value,$form_state);
  }
  //if (twCanDebug()) debug($form['fk_trophy']);
   if (twCanDebug()) debug('fk_trophy = ' . $value);
  // Validate fk fk_trophy
  if (empty($value)) {
    form_set_error('fk_trophy', t('Validation error, \'fk_trophy\' is mandatory1.'));
    // throw new Exception('Field \'fk_trophy\' in is mandatory');
  }

  // Convert fk autocomplete for fk_actor
  $value = $form['fk_actor']['#value'];
  // If we have something, escape the auto-completion encoding.
  if (!empty($value)) { 
    $value = entity_autocomplete_get_id($value);
    form_set_value($form['fk_actor'], $value,$form_state);
  }
  //if (twCanDebug()) debug($form['fk_actor']);
   if (twCanDebug()) debug('fk_actor = ' . $value);
  // Validate fk fk_actor
  if (empty($value)) {
    form_set_error('fk_actor', t('Validation error, \'fk_actor\' is mandatory1.'));
    // throw new Exception('Field \'fk_actor\' in is mandatory');
  }

  // Convert fk autocomplete for fk_thing
  $value = $form['fk_thing']['#value'];
  // If we have something, escape the auto-completion encoding.
  if (!empty($value)) { 
    $value = entity_autocomplete_get_id($value);
    form_set_value($form['fk_thing'], $value,$form_state);
  }
  //if (twCanDebug()) debug($form['fk_thing']);
   if (twCanDebug()) debug('fk_thing = ' . $value);
  // Validate fk fk_thing
  if (empty($value)) {
    form_set_error('fk_thing', t('Validation error, \'fk_thing\' is mandatory1.'));
    // throw new Exception('Field \'fk_thing\' in is mandatory');
  }

  // Validate normaldocumentation
  $value = $form['documentation']['#value'];
  // If we have no default value then we don't care much for checking for emptyness.
  if (empty($value)) {
    form_set_error('documentation', t('Validation error, \'documentation\' is mandatory2.'));
    //throw new Exception('Field \'documentation\' in is mandatory.');
  }

              

  //form_set_value( array('#parents' => array('array_key_parent', 'array_key_to_replace')) , $value, $form_state);

   
   
  $thumbwhere_awardedtrophy = $form_state['thumbwhere_awardedtrophy'];

  // Notify field widgets to validate their data.
  field_attach_form_validate('thumbwhere_awardedtrophy', $thumbwhere_awardedtrophy, $form, $form_state);
    
    
  
}


/**
 * Form API submit callback for the thumbwhere_awardedtrophy form.
 *
 * @todo remove hard-coded link
 */
function thumbwhere_awardedtrophy_edit_form_submit(&$form, &$form_state) {

  $thumbwhere_awardedtrophy = entity_ui_controller('thumbwhere_awardedtrophy')->entityFormSubmitBuildEntity($form, $form_state);
  // Save the thumbwhere_awardedtrophy and go back to the list of thumbwhere_awardedtrophys

  // Add in created and changed times.
  if ($thumbwhere_awardedtrophy->is_new = isset($thumbwhere_awardedtrophy->is_new) ? $thumbwhere_awardedtrophy->is_new : 0) {
    $thumbwhere_awardedtrophy->created = time();
  }

  $thumbwhere_awardedtrophy->changed = time();

  $thumbwhere_awardedtrophy->save($transaction);
  $form_state['redirect'] = 'admin/thumbwhere/thumbwhere_awardedtrophys';
}

/**
 * Form API submit callback for the delete button.
 *
 * @todo Remove hard-coded path
 */
function thumbwhere_awardedtrophy_form_submit_delete(&$form, &$form_state) {
  $form_state['redirect'] = 'admin/thumbwhere/thumbwhere_awardedtrophys/thumbwhere_awardedtrophy/' . $form_state['thumbwhere_awardedtrophy']->pk_awardedtrophy . '/delete';
}





/**
 * Form callback: confirmation form for deleting a thumbwhere_awardedtrophy.
 *
 * @param $thumbwhere_awardedtrophy
 *   The thumbwhere_awardedtrophy to delete
 *
 * @see confirm_form()
 */
function thumbwhere_awardedtrophy_delete_form($form, &$form_state, $thumbwhere_awardedtrophy) {
  $form_state['thumbwhere_awardedtrophy'] = $thumbwhere_awardedtrophy;

  $form['#submit'][] = 'thumbwhere_awardedtrophy_delete_form_submit';

  $form = confirm_form($form,
    t('Are you sure you want to delete thumbwhere_awardedtrophy %name?', array('%name' => $thumbwhere_awardedtrophy->pk_awardedtrophy)),
    'admin/thumbwhere/thumbwhere_awardedtrophys/thumbwhere_awardedtrophy',
    '<p>' . t('This action cannot be undone.') . '</p>',
    t('Delete'),
    t('Cancel'),
    'confirm'
  );

  return $form;
}

/**
 * Submit callback for thumbwhere_awardedtrophy_delete_form
 */
function thumbwhere_awardedtrophy_delete_form_submit($form, &$form_state) {
  $thumbwhere_awardedtrophy = $form_state['thumbwhere_awardedtrophy'];

  thumbwhere_awardedtrophy_delete($thumbwhere_awardedtrophy);

  drupal_set_message(t('The thumbwhere_awardedtrophy %name has been deleted.', array('%name' => $thumbwhere_awardedtrophy->pk_awardedtrophy)));
  watchdog('thumbwhere_awardedtrophy', 'Deleted thumbwhere_awardedtrophy %name.', array('%name' => $thumbwhere_awardedtrophy->pk_awardedtrophy));

  $form_state['redirect'] = 'admin/thumbwhere/thumbwhere_awardedtrophys';
}



/**
 * Page to add ThumbWhereAwardedTrophy Entities.
 *
 * @todo Pass this through a proper theme function
 */
function thumbwhere_awardedtrophy_add_page() {
  $controller = entity_ui_controller('thumbwhere_awardedtrophy');
  return $controller->addPage();
}


/**
 * Displays the list of available thumbwhere_awardedtrophy types for thumbwhere_awardedtrophy creation.
 *
 * @ingroup themeable
 */
function theme_thumbwhere_awardedtrophy_add_list($variables) {


  //$content = $variables['content'];
  $output = '';
  /*
  if ($content) {
    $output = '<dl class="thumbwhere_awardedtrophy-type-list">';
    foreach ($content as $item) {
      $output .= '<dt>' . l($item['title'], $item['href']) . '</dt>';
      $output .= '<dd>' . filter_xss_admin($item['description']) . '</dd>';
    }
    $output .= '</dl>';
  }
  else {
    if (user_access('administer thumbwhere_awardedtrophy types')) {
      $output = '<p>' . t('ThumbWhereAwardedTrophy Entities cannot be added because you have not created any thumbwhere_awardedtrophy types yet. Go to the <a href="@create-thumbwhere_awardedtrophy-type">thumbwhere_awardedtrophy type creation page</a> to add a new thumbwhere_awardedtrophy type.', array('@create-thumbwhere_awardedtrophy-type' => url('admin/structure/thumbwhere_awardedtrophy_types/add'))) . '</p>';
    }
    else {
      $output = '<p>' . t('No thumbwhere_awardedtrophy types have been created yet for you to use.') . '</p>';
    }
  }
  */
  return $output;
}





/**
 * Sets the breadcrumb for administrative thumbwhere_awardedtrophy pages.
 */
function thumbwhere_awardedtrophy_set_breadcrumb() {
  $breadcrumb = array(
    l(t('Home'), '<front>'),
    l(t('Administration'), 'admin'),
    l(t('AwardedTrophy'), 'admin/content'),
    l(t('AwardedTrophy'), 'admin/thumbwhere/thumbwhere_awardedtrophys'),
  );

  drupal_set_breadcrumb($breadcrumb);
}



