<?php

// ******************************************************************
// This file was generated by autogen_model_implementation_drupal.xsl
// ******************************************************************

// Include the ThumbWhere SDK
require_once(dirname(__FILE__) . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . 'dependencies' . DIRECTORY_SEPARATOR . 'tw' . DIRECTORY_SEPARATOR . 'sdk.class.php');

/**
 * @file
 * Module for the ThumbWhere HostTypeHostRoleType Entity
 *
 *
 * NOTE: This has been autogenerated.
 * 
 */

/**
 * Implement hook_entity_info().
 *
 * We define two entities here - the actual entity that will hold our domain
 * specific information and an entity that holds information about the different
 * types of entities. See here: http://drupal.org/node/977380 for a discussion on this
 * choice.
 */
function thumbwhere_hosttypehostroletype_entity_info() {
  $return['thumbwhere_hosttypehostroletype'] = array(

    // The human-readable name of the type.
    'label' => t('ThumbWhereHostTypeHostRoleType'),

    // (used by DrupalDefaultEntityController) The name of the entity type's base table.
    'base table' => 'thumbwhere_hosttypehostroletype',

    // fieldable: Set to TRUE if you want your entity type to accept fields being attached to it.
    'fieldable' => TRUE,

  // Declare the endty keys..
    'entity keys' => array(
      'id' => 'pk_hosttypehostroletype',
      'label' => 'pk_hosttypehostroletype',
    ),

    // A function taking an entity and an entity type as arguments and returning the label of the entity.
    // The entity label is the main string associated with an entity; for example, the title of a node or
    // the subject of a comment. If there is an entity object property that defines the label, use the
    // 'label' element of the 'entity keys' return value component to provide this information (see below).
    // If more complex logic is needed to determine the label of an entity, you can instead specify a callback
    // function here, which will be called to determine the entity label. See also the entity_label()
    // function, which implements this logic.
    'label callback' => 'entity_class_label',

    // A function taking an entity as argument and returning the uri elements of the entity, e.g. 'path' and 'options'.
    // The actual entity uri can be constructed by passing these elements to url().
    'uri callback' => 'entity_class_uri',

    //
    // Entity API Components
    //

    // The module whwre we can find our classes
    'module' => 'thumbwhere_hosttypehostroletype',

    // The entity class and controller class extend the classes provided by the Entity API
    'entity class' => 'ThumbWhereHostTypeHostRoleType',

    // Which Entity API contoller
    'controller class' => 'ThumbWhereHostTypeHostRoleTypeController',

    // The information below is used by the ThumbWhereHostTypeHostRoleTypeUIController (which extends the EntityDefaultUIController)
    'admin ui' => array(
      'path' => 'admin/thumbwhere/thumbwhere_hosttypehostroletypes',
      'file' => 'thumbwhere_hosttypehostroletype.admin.inc',
      'controller class' => 'ThumbWhereHostTypeHostRoleTypeUIController',
      'menu wildcard' => '%thumbwhere_hosttypehostroletype',
    ),
  );

  return $return;
}


/**
 * Implements hook_permission().
 */
function thumbwhere_hosttypehostroletype_permission() {

  // We set up permisssions to manage entity types, manage all entities and the
  // permissions for each individual entity
  $permissions = array(
    'administer thumbwhere_hosttypehostroletypes' => array(
      'title' => t('Administer thumbwhere_hosttypehostroletypes'),
      'description' => t('Edit and delete all thumbwhere_hosttypehostroletypes'),
    ),
  'send thumbwhere hosttypehostroletype notifications' => array(
          'title' => t('Send ThumbWhere Notifications'),
          'description' => t('Allow the caller to call the ThumbWhere HostTypeHostRoleType Entity Notifications Endpoint.'),
          'restrict access' => TRUE,
      ),    
  );

  return $permissions;
}


/**
 * Determines whether the given user has access to a thumbwhere_hosttypehostroletype.
 *
 * @param $op
 *   The operation being performed. One of 'view', 'update', 'create', 'delete'
 *   or just 'edit' (being the same as 'create' or 'update').
 * @param $thumbwhere_hosttypehostroletype
 *   Optionally a thumbwhere_hosttypehostroletype or a thumbwhere_hosttypehostroletype type to check access for. If nothing is
 *   given, access for all thumbwhere_hosttypehostroletypes is determined.
 * @param $account
 *   The user to check for. Leave it to NULL to check for the global user.
 * @return boolean
 *   Whether access is allowed or not.
 */
function thumbwhere_hosttypehostroletype_access($op, $thumbwhere_hosttypehostroletype = NULL, $account = NULL) {

  //if (user_access('administer thumbwhere_hosttypehostroletypes', $account)) {
  //  return TRUE;
 // }

  //return FALSE;
  return TRUE;
}




/**
 * Fetch a thumbwhere_hosttypehostroletype object. Make sure that the wildcard you choose
 * in the thumbwhere_hosttypehostroletype entity definition fits the function name here.
 *
 * @param $thumbwhere_hosttypehostroletype_id
 *   Integer specifying the thumbwhere_hosttypehostroletype id.
 * @param $reset
 *   A boolean indicating that the internal cache should be reset.
 * @return
 *   A fully-loaded $thumbwhere_hosttypehostroletype object or FALSE if it cannot be loaded.
 *
 * @see thumbwhere_hosttypehostroletype_load_multiple()
 */
function thumbwhere_hosttypehostroletype_load($thumbwhere_hosttypehostroletype_id, $reset = FALSE) {

  $thumbwhere_hosttypehostroletypes = entity_load('thumbwhere_hosttypehostroletype', array($thumbwhere_hosttypehostroletype_id), array(), $reset);

  return reset($thumbwhere_hosttypehostroletypes);
}


/**
 * Deletes a thumbwhere_hosttypehostroletype.
 */
function thumbwhere_hosttypehostroletype_delete(ThumbWhereHostTypeHostRoleType $thumbwhere_hosttypehostroletype) {

  $thumbwhere_hosttypehostroletype->delete();
}

/**
 * Create a thumbwhere_hosttypehostroletype object.
 */
function thumbwhere_hosttypehostroletype_create($values = array()) {

  return entity_get_controller('thumbwhere_hosttypehostroletype')->create($values);
}


/**
 * URI callback for thumbwhere_hosttypehostroletypes
 */
function thumbwhere_hosttypehostroletype_uri(ThumbWhereHostTypeHostRoleType $thumbwhere_hosttypehostroletype) {

  return array(
    'path' => 'thumbwhere_hosttypehostroletype/' . $thumbwhere_hosttypehostroletype->pk_hosttypehostroletype,
  );
}


/**
 * Menu title callback for showing individual entities
 */
function thumbwhere_hosttypehostroletype_page_title(ThumbWhereHostTypeHostRoleType $thumbwhere_hosttypehostroletype) {

	return $thumbwhere_hosttypehostroletype->pk_hosttypehostroletype;
}


/**
 * Sets up content to show an individual thumbwhere_hosttypehostroletype
 * @todo - get rid of drupal_set_title();
 */
function thumbwhere_hosttypehostroletype_page_view($thumbwhere_hosttypehostroletype, $view_mode = 'full') {



  $controller = entity_get_controller('thumbwhere_hosttypehostroletype');
  $content = $controller->view(array($thumbwhere_hosttypehostroletype->pk_hosttypehostroletype => $thumbwhere_hosttypehostroletype));
  drupal_set_title($thumbwhere_hosttypehostroletype->pk_hosttypehostroletype);
  return $content;
}


/**
 * Implements hook_views_api().
 */
function thumbwhere_hosttypehostroletype_views_api() {



  return array(
    'api' => 3,
    'path' => drupal_get_path('module', 'thumbwhere_hosttypehostroletype') . '/views',
  );
}


/**
 * Implement hook_theme().
 */
function thumbwhere_hosttypehostroletype_theme() {



  return array(
    'thumbwhere_hosttypehostroletype_add_list' => array(
      'variables' => array('content' => array()),
      'file' => 'thumbwhere_hosttypehostroletype.admin.inc',
    ),
    'thumbwhere_hosttypehostroletype' => array(
      'render element' => 'elements',
      'template' => 'thumbwhere_hosttypehostroletype',
    )
  );
}


/**
 * Implements hook_menu_local_tasks_alter().
 */
function thumbwhere_hosttypehostroletype_menu_local_tasks_alter(&$data, $router_item, $root_path) {

  // Add action link 'admin/structure/thumbwhere_hosttypehostroletypes/add' on 'admin/structure/thumbwhere_hosttypehostroletypes'.
  if ($root_path == 'admin/thumbwhere/thumbwhere_hosttypehostroletypes') {
    $item = menu_get_item('admin/thumbwhere/thumbwhere_hosttypehostroletypes/add');
    if ($item['access']) {
      $data['actions']['output'][] = array(
        '#theme' => 'menu_local_action',
        '#link' => $item,
      );
    }
  }
}


/**
 * The class used for thumbwhere_hosttypehostroletype entities
 */
class ThumbWhereHostTypeHostRoleType extends Entity {

  public function __construct($values = array()) {
    parent::__construct($values, 'thumbwhere_hosttypehostroletype');
  }

  protected function defaultLabel() {
    return $this->pk_hosttypehostroletype;
  }

  protected function defaultUri() {
    return array('path' => 'thumbwhere_hosttypehostroletype/' . $this->pk_hosttypehostroletype);
  }


}



/**
 * The Controller for ThumbWhereHostTypeHostRoleType entities
 */
class ThumbWhereHostTypeHostRoleTypeController extends EntityAPIController {
  public function __construct($entityType) {
    parent::__construct($entityType);
  }
}



/**
* Implements hook_entity_presave().
*
* Act on an HostTypeHostRoleType entity before it is about to be created or updated.
*
* @param $entity
*   The entity object.
* @param $entity_type
*   The type of entity being saved (i.e. node, user, comment).
*/
function thumbwhere_hosttypehostroletype_entity_presave($entity, $entity_type) {

  // We only look after what we want...
  if ($entity_type == 'thumbwhere_hosttypehostroletype')  {
  	
 	/////////////////////////////////////////////////////
    //
    // Should we not propagate this to ThumbWhere? 
    //
    if (isset($entity->do_not_propagate)) {
      if ($entity->do_not_propagate) {
        if (twCanDebug()) {
          if ($entity->is_new) {
            if (twCanDebug()){
              debug("DO NOT PROPAGATE 'hosttypehostroletype' presave *CREATE* not being sent to ThumbWhere server.");
          	}
          }
          else {
            if (twCanDebug()) {
              debug("DO NOT PROPAGATE 'hosttypehostroletype' presave *UPDATE* not being sent to ThumbWhere server.");
            }
          }
        }

        // To cause an infinite loop of updates, comment out this line.
        return;
      }
      else if (twCanDebug()) {
        debug("OK PROAOGATE 'hosttypehostroletype' presave to ThumbWhere server.");
      }
    }
    else if (twCanDebug()) {
      debug("DO NOT PROPAGATE IS NOT SET");
    }
    //
    //
    ////////////////////////////////////////////////////////  	

  
    // .. then we create a new thumbwhere_hosttypehostroletype entity...
      // Instantiate the ThumbWhereContent class

    $twAdminAPI = new ThumbWhereAPIAdmin();

    //if (twCanDebug()) debug($twContentAPI);

    //if (twCanDebug()) debug($entity);

    //
    // Collate and validate our fields
    //

    $fields = array();
    // Compulsory so..
    if (!isset($entity->fk_hosttype)) {
      throw new Exception('hosttype is mandatory.');
    }
    else {
      $fields['hosttype'] = $entity->fk_hosttype;
    }
    // Compulsory so..
    if (!isset($entity->fk_hostroletype)) {
      throw new Exception('hostroletype is mandatory.');
    }
    else {
      $fields['hostroletype'] = $entity->fk_hostroletype;
    }
		
	//
	// Ensure that we have the 'thumbwhere_api_key' as we are going to pass that through in a context
	//
    if (variable_get('thumbwhere_api_key') == '') {
    	throw new Exception('ThumbWhere resource for \'HostTypeHostRoleType\' requires that variable \'thumbwhere_api_key\' is set.');
    }
    
              		
    //
    // If this hosttypehostroletype entity is new....
    //
               
    if ($entity->is_new) {
                

      //
      // ... then we create the hosttypehostroletype entity in ThumbWhere
      //

      $hosttypehostroletype = $twAdminAPI->create_host_type_capable_of_host_role_type($context = array(
                  'key' => variable_get('thumbwhere_api_key')
          ), $fields);
            
       if (twCanDebug()) debug($hosttypehostroletype);

      //if (twCanDebug()) debug($host_type_capable_of_host_role_type);

      $attributes = $hosttypehostroletype->body->host_type_capable_of_host_role_type->attributes();

      if (twCanDebug()) debug('newly created id = ' . $attributes['id']);

      // Assign new id.
      $entity->pk_hosttypehostroletype = intval($attributes['id']);

      if (twCanDebug()) debug('$entity->pk_hosttypehostroletype = ' . $entity->pk_hosttypehostroletype);
    }
    else
    {
          	

      //
      // .. otherwise we update the hosttypehostroletype entity in ThumbWhere
      //

      $hosttypehostroletype = $twAdminAPI->update_host_type_capable_of_host_role_type($id=$entity->pk_hosttypehostroletype,$context = array(
                  'key' => variable_get('thumbwhere_api_key')
          ), $fields);
            
        
    }
          	
    
  }
}

/**
 * Implements thumbwhere_hosttypehostroletype_entity_insert().
 *
 * Act on entities when inserted.
 *
 * @param $entity
 *   The entity object
 * @param $type
 *   The type of entity being inserted (i.e. node, user, comment).
 */
function thumbwhere_hosttypehostroletype_entity_insert($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_hosttypehostroletype') return;

}


/**
* Implements thumbwhere_hosttypehostroletype_entity_delete().
*
* Act on entities when inserted.
*
* @param $entity
*   The entity object
* @param $type
*   The type of entity being deleted (i.e. node, user, comment).
*/
function thumbwhere_hosttypehostroletype_entity_delete($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_hosttypehostroletype') return;


}
