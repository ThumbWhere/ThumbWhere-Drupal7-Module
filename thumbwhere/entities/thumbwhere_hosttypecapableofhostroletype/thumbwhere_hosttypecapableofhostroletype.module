<?php

// ******************************************************************
// This file was generated by autogen_model_implementation_drupal.xsl
// ******************************************************************

// Include the ThumbWhere SDK
require_once(dirname(__FILE__) . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . 'dependencies' . DIRECTORY_SEPARATOR . 'tw' . DIRECTORY_SEPARATOR . 'sdk.class.php');

/**
 * @file
 * Module for the ThumbWhere HostTypeCapableOfHostRoleType Entity
 *
 *
 * NOTE: This has been autogenerated.
 * 
 */

/**
 * Implement hook_entity_info().
 *
 * We define two entities here - the actual entity that will hold our domain
 * specific information and an entity that holds information about the different
 * types of entities. See here: http://drupal.org/node/977380 for a discussion on this
 * choice.
 */
function thumbwhere_hosttypecapableofhostroletype_entity_info() {
  $return['thumbwhere_hosttypecapableofhostroletype'] = array(

    // The human-readable name of the type.
    'label' => t('ThumbWhereHostTypeCapableOfHostRoleType'),

    // (used by DrupalDefaultEntityController) The name of the entity type's base table.
    'base table' => 'thumbwhere_hosttypecapableofhostroletype',

    // fieldable: Set to TRUE if you want your entity type to accept fields being attached to it.
    'fieldable' => TRUE,

  // Declare the endty keys..
    'entity keys' => array(
      'id' => 'pk_hosttypecapableofhostroletype',
      'label' => 'pk_hostypecapableofhostroletype',
    ),

    // A function taking an entity and an entity type as arguments and returning the label of the entity.
    // The entity label is the main string associated with an entity; for example, the title of a node or
    // the subject of a comment. If there is an entity object property that defines the label, use the
    // 'label' element of the 'entity keys' return value component to provide this information (see below).
    // If more complex logic is needed to determine the label of an entity, you can instead specify a callback
    // function here, which will be called to determine the entity label. See also the entity_label()
    // function, which implements this logic.
    'label callback' => 'entity_class_label',

    // A function taking an entity as argument and returning the uri elements of the entity, e.g. 'path' and 'options'.
    // The actual entity uri can be constructed by passing these elements to url().
    'uri callback' => 'entity_class_uri',

    //
    // Entity API Components
    //

    // The module whwre we can find our classes
    'module' => 'thumbwhere_hosttypecapableofhostroletype',

    // The entity class and controller class extend the classes provided by the Entity API
    'entity class' => 'ThumbWhereHostTypeCapableOfHostRoleType',

    // Which Entity API contoller
    'controller class' => 'ThumbWhereHostTypeCapableOfHostRoleTypeController',

    // The information below is used by the ThumbWhereHostTypeCapableOfHostRoleTypeUIController (which extends the EntityDefaultUIController)
    'admin ui' => array(
      'path' => 'admin/thumbwhere/thumbwhere_hosttypecapableofhostroletypes',
      'file' => 'thumbwhere_hosttypecapableofhostroletype.admin.inc',
      'controller class' => 'ThumbWhereHostTypeCapableOfHostRoleTypeUIController',
      'menu wildcard' => '%thumbwhere_hosttypecapableofhostroletype',
    ),
  );

  return $return;
}


/**
 * Implements hook_permission().
 */
function thumbwhere_hosttypecapableofhostroletype_permission() {

  // We set up permisssions to manage entity types, manage all entities and the
  // permissions for each individual entity
  $permissions = array(
    'administer thumbwhere_hosttypecapableofhostroletypes' => array(
      'title' => t('Administer thumbwhere_hosttypecapableofhostroletypes'),
      'description' => t('Edit and delete all thumbwhere_hosttypecapableofhostroletypes'),
    ),
  'send thumbwhere hosttypecapableofhostroletype notifications' => array(
          'title' => t('Send ThumbWhere Notifications'),
          'description' => t('Allow the caller to call the ThumbWhere HostTypeCapableOfHostRoleType Entity Notifications Endpoint.'),
          'restrict access' => TRUE,
      ),    
  );

  return $permissions;
}


/**
 * Determines whether the given user has access to a thumbwhere_hosttypecapableofhostroletype.
 *
 * @param $op
 *   The operation being performed. One of 'view', 'update', 'create', 'delete'
 *   or just 'edit' (being the same as 'create' or 'update').
 * @param $thumbwhere_hosttypecapableofhostroletype
 *   Optionally a thumbwhere_hosttypecapableofhostroletype or a thumbwhere_hosttypecapableofhostroletype type to check access for. If nothing is
 *   given, access for all thumbwhere_hosttypecapableofhostroletypes is determined.
 * @param $account
 *   The user to check for. Leave it to NULL to check for the global user.
 * @return boolean
 *   Whether access is allowed or not.
 */
function thumbwhere_hosttypecapableofhostroletype_access($op, $thumbwhere_hosttypecapableofhostroletype = NULL, $account = NULL) {

  //if (user_access('administer thumbwhere_hosttypecapableofhostroletypes', $account)) {
  //  return TRUE;
 // }

  //return FALSE;
  return TRUE;
}




/**
 * Fetch a thumbwhere_hosttypecapableofhostroletype object. Make sure that the wildcard you choose
 * in the thumbwhere_hosttypecapableofhostroletype entity definition fits the function name here.
 *
 * @param $thumbwhere_hosttypecapableofhostroletype_id
 *   Integer specifying the thumbwhere_hosttypecapableofhostroletype id.
 * @param $reset
 *   A boolean indicating that the internal cache should be reset.
 * @return
 *   A fully-loaded $thumbwhere_hosttypecapableofhostroletype object or FALSE if it cannot be loaded.
 *
 * @see thumbwhere_hosttypecapableofhostroletype_load_multiple()
 */
function thumbwhere_hosttypecapableofhostroletype_load($thumbwhere_hosttypecapableofhostroletype_id, $reset = FALSE) {

  $thumbwhere_hosttypecapableofhostroletypes = entity_load('thumbwhere_hosttypecapableofhostroletype', array($thumbwhere_hosttypecapableofhostroletype_id), array(), $reset);

  return reset($thumbwhere_hosttypecapableofhostroletypes);
}


/**
 * Deletes a thumbwhere_hosttypecapableofhostroletype.
 */
function thumbwhere_hosttypecapableofhostroletype_delete(ThumbWhereHostTypeCapableOfHostRoleType $thumbwhere_hosttypecapableofhostroletype) {

  $thumbwhere_hosttypecapableofhostroletype->delete();
}

/**
 * Create a thumbwhere_hosttypecapableofhostroletype object.
 */
function thumbwhere_hosttypecapableofhostroletype_create($values = array()) {

  return entity_get_controller('thumbwhere_hosttypecapableofhostroletype')->create($values);
}


/**
 * URI callback for thumbwhere_hosttypecapableofhostroletypes
 */
function thumbwhere_hosttypecapableofhostroletype_uri(ThumbWhereHostTypeCapableOfHostRoleType $thumbwhere_hosttypecapableofhostroletype) {

  return array(
    'path' => 'thumbwhere_hosttypecapableofhostroletype/' . $thumbwhere_hosttypecapableofhostroletype->pk_hosttypecapableofhostroletype,
  );
}


/**
 * Menu title callback for showing individual entities
 */
function thumbwhere_hosttypecapableofhostroletype_page_title(ThumbWhereHostTypeCapableOfHostRoleType $thumbwhere_hosttypecapableofhostroletype) {

	return $thumbwhere_hosttypecapableofhostroletype->pk_hostypecapableofhostroletype;
}


/**
 * Sets up content to show an individual thumbwhere_hosttypecapableofhostroletype
 * @todo - get rid of drupal_set_title();
 */
function thumbwhere_hosttypecapableofhostroletype_page_view($thumbwhere_hosttypecapableofhostroletype, $view_mode = 'full') {



  $controller = entity_get_controller('thumbwhere_hosttypecapableofhostroletype');
  $content = $controller->view(array($thumbwhere_hosttypecapableofhostroletype->pk_hosttypecapableofhostroletype => $thumbwhere_hosttypecapableofhostroletype));
  drupal_set_title($thumbwhere_hosttypecapableofhostroletype->pk_hostypecapableofhostroletype);
  return $content;
}


/**
 * Implements hook_views_api().
 */
function thumbwhere_hosttypecapableofhostroletype_views_api() {



  return array(
    'api' => 3,
    'path' => drupal_get_path('module', 'thumbwhere_hosttypecapableofhostroletype') . '/views',
  );
}


/**
 * Implement hook_theme().
 */
function thumbwhere_hosttypecapableofhostroletype_theme() {



  return array(
    'thumbwhere_hosttypecapableofhostroletype_add_list' => array(
      'variables' => array('content' => array()),
      'file' => 'thumbwhere_hosttypecapableofhostroletype.admin.inc',
    ),
    'thumbwhere_hosttypecapableofhostroletype' => array(
      'render element' => 'elements',
      'template' => 'thumbwhere_hosttypecapableofhostroletype',
    )
  );
}


/**
 * Implements hook_menu_local_tasks_alter().
 */
function thumbwhere_hosttypecapableofhostroletype_menu_local_tasks_alter(&$data, $router_item, $root_path) {

  // Add action link 'admin/structure/thumbwhere_hosttypecapableofhostroletypes/add' on 'admin/structure/thumbwhere_hosttypecapableofhostroletypes'.
  if ($root_path == 'admin/thumbwhere/thumbwhere_hosttypecapableofhostroletypes') {
    $item = menu_get_item('admin/thumbwhere/thumbwhere_hosttypecapableofhostroletypes/add');
    if ($item['access']) {
      $data['actions']['output'][] = array(
        '#theme' => 'menu_local_action',
        '#link' => $item,
      );
    }
  }
}


/**
 * The class used for thumbwhere_hosttypecapableofhostroletype entities
 */
class ThumbWhereHostTypeCapableOfHostRoleType extends Entity {

  public function __construct($values = array()) {
    parent::__construct($values, 'thumbwhere_hosttypecapableofhostroletype');
  }

  protected function defaultLabel() {
    return $this->pk_hostypecapableofhostroletype;
  }

  protected function defaultUri() {
    return array('path' => 'thumbwhere_hosttypecapableofhostroletype/' . $this->pk_hosttypecapableofhostroletype);
  }


}



/**
 * The Controller for ThumbWhereHostTypeCapableOfHostRoleType entities
 */
class ThumbWhereHostTypeCapableOfHostRoleTypeController extends EntityAPIController {
  public function __construct($entityType) {
    parent::__construct($entityType);
  }
}



/**
* Implements hook_entity_presave().
*
* Act on an HostTypeCapableOfHostRoleType entity before it is about to be created or updated.
*
* @param $entity
*   The entity object.
* @param $entity_type
*   The type of entity being saved (i.e. node, user, comment).
*/
function thumbwhere_hosttypecapableofhostroletype_entity_presave($entity, $entity_type) {

  // We only look after what we want...
  if ($entity_type == 'thumbwhere_hosttypecapableofhostroletype')  {
  	
  	
  	
 	/////////////////////////////////////////////////////
    //
    // Should we not propagate this to ThumbWhere? 
    //
    if (isset($entity->do_not_propagate)) {
      if ($entity->do_not_propagate) {
        if (twCanDebug()) {
          if ($entity->is_new) {
            if (twCanDebug()){
              debug("DO NOT PROPAGATE 'hosttypecapableofhostroletype' presave *CREATE*");
          	}
          }
          else {
            if (twCanDebug()) {
              debug("DO NOT PROPAGATE 'hosttypecapableofhostroletype' presave *UPDATE*");
            }
          }
        }

        // To cause an infinite loop of updates, comment out this line.
        return;
      }
      else if (twCanDebug()) {
        debug("OK PROAOGATE 'hosttypecapableofhostroletype' presave to ThumbWhere Server");
      }
    }
    else if (twCanDebug()) {
      debug("DO NOT PROPAGATE IS NOT SET");
    }
    //
    //
    ////////////////////////////////////////////////////////  	

  
    // .. then we create a new thumbwhere_hosttypecapableofhostroletype entity...
      // Instantiate the ThumbWhereContent class

    $twAdminAPI = new ThumbWhereAPIAdmin();

    //if (twCanDebug()) debug($twContentAPI);

    //if (twCanDebug()) debug($entity);

    //
    // Collate and validate our fields
    //

    $fields = array();
    // Compulsory so..
    if (!isset($entity->fk_hosttype)) {
      throw new Exception('hosttype is mandatory.');
    }
    else {
      $fields['hosttype'] = $entity->fk_hosttype;
    }
    // Compulsory so..
    if (!isset($entity->fk_hostroletype)) {
      throw new Exception('hostroletype is mandatory.');
    }
    else {
      $fields['hostroletype'] = $entity->fk_hostroletype;
    }
		
	//
	// Ensure that we have the 'thumbwhere_api_key' as we are going to pass that through in a context
	//
    if (variable_get('thumbwhere_api_key') == '') {
    	throw new Exception('ThumbWhere resource for \'HostTypeCapableOfHostRoleType\' requires that variable \'thumbwhere_api_key\' is set.');
    }
    
              		
    //
    // If this hosttypecapableofhostroletype entity is new....
    //
               
    if ($entity->is_new) {
                

      //
      // ... then we create the hosttypecapableofhostroletype entity in ThumbWhere
      //

      $hosttypecapableofhostroletype = $twAdminAPI->create_host_type_capable_of_host_role_type($context = array(
                  'key' => variable_get('thumbwhere_api_key')
          ), $fields);
            
       if (twCanDebug()) debug($hosttypecapableofhostroletype);

      //if (twCanDebug()) debug($host_type_capable_of_host_role_type);

      $attributes = $hosttypecapableofhostroletype->body->host_type_capable_of_host_role_type->attributes();

      if (twCanDebug()) debug('newly created id = ' . $attributes['id']);

      // Assign new id.
      $entity->pk_hosttypecapableofhostroletype = intval($attributes['id']);

      if (twCanDebug()) debug('$entity->pk_hosttypecapableofhostroletype = ' . $entity->pk_hosttypecapableofhostroletype);
    }
    else
    {
          	

      //
      // .. otherwise we update the hosttypecapableofhostroletype entity in ThumbWhere
      //

      $hosttypecapableofhostroletype = $twAdminAPI->update_host_type_capable_of_host_role_type($id=$entity->pk_hosttypecapableofhostroletype,$context = array(
                  'key' => variable_get('thumbwhere_api_key')
          ), $fields);
            
        
    }
          	
    
  }
}

/**
 * Implements thumbwhere_hosttypecapableofhostroletype_entity_insert().
 *
 * Act on entities when inserted.
 *
 * @param $entity
 *   The entity object
 * @param $type
 *   The type of entity being inserted (i.e. node, user, comment).
 */
function thumbwhere_hosttypecapableofhostroletype_entity_insert($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_hosttypecapableofhostroletype') return;

}


/**
* Implements thumbwhere_hosttypecapableofhostroletype_entity_delete().
*
* Act on entities when inserted.
*
* @param $entity
*   The entity object
* @param $type
*   The type of entity being deleted (i.e. node, user, comment).
*/
function thumbwhere_hosttypecapableofhostroletype_entity_delete($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_hosttypecapableofhostroletype') return;


}
