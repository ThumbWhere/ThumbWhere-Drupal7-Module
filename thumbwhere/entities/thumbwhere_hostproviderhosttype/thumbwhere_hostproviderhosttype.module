<?php

// ******************************************************************
// This file was generated by autogen_model_implementation_drupal.xsl
// ******************************************************************

// Include the ThumbWhere SDK
require_once(dirname(__FILE__) . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . 'dependencies' . DIRECTORY_SEPARATOR . 'tw' . DIRECTORY_SEPARATOR . 'sdk.class.php');

/**
 * @file
 * Module for the ThumbWhere HostProviderHostType Entity
 *
 *
 * NOTE: This has been autogenerated.
 * 
 */

/**
 * Implement hook_entity_info().
 *
 * We define two entities here - the actual entity that will hold our domain
 * specific information and an entity that holds information about the different
 * types of entities. See here: http://drupal.org/node/977380 for a discussion on this
 * choice.
 */
function thumbwhere_hostproviderhosttype_entity_info() {
  $return['thumbwhere_hostproviderhosttype'] = array(

    // The human-readable name of the type.
    'label' => t('ThumbWhereHostProviderHostType'),

    // (used by DrupalDefaultEntityController) The name of the entity type's base table.
    'base table' => 'thumbwhere_hostproviderhosttype',

    // fieldable: Set to TRUE if you want your entity type to accept fields being attached to it.
    'fieldable' => TRUE,

  // Declare the endty keys..
    'entity keys' => array(
      'id' => 'pk_hostproviderhosttype',
      'label' => 'pk_hostproviderhosttype',
    ),

    // A function taking an entity and an entity type as arguments and returning the label of the entity.
    // The entity label is the main string associated with an entity; for example, the title of a node or
    // the subject of a comment. If there is an entity object property that defines the label, use the
    // 'label' element of the 'entity keys' return value component to provide this information (see below).
    // If more complex logic is needed to determine the label of an entity, you can instead specify a callback
    // function here, which will be called to determine the entity label. See also the entity_label()
    // function, which implements this logic.
    'label callback' => 'entity_class_label',

    // A function taking an entity as argument and returning the uri elements of the entity, e.g. 'path' and 'options'.
    // The actual entity uri can be constructed by passing these elements to url().
    'uri callback' => 'entity_class_uri',

    //
    // Entity API Components
    //

    // The module whwre we can find our classes
    'module' => 'thumbwhere_hostproviderhosttype',

    // The entity class and controller class extend the classes provided by the Entity API
    'entity class' => 'ThumbWhereHostProviderHostType',

    // Which Entity API contoller
    'controller class' => 'ThumbWhereHostProviderHostTypeController',

    // The information below is used by the ThumbWhereHostProviderHostTypeUIController (which extends the EntityDefaultUIController)
    'admin ui' => array(
      'path' => 'admin/thumbwhere/thumbwhere_hostproviderhosttypes',
      'file' => 'thumbwhere_hostproviderhosttype.admin.inc',
      'controller class' => 'ThumbWhereHostProviderHostTypeUIController',
      'menu wildcard' => '%thumbwhere_hostproviderhosttype',
    ),
  );

  return $return;
}


/**
 * Implements hook_permission().
 */
function thumbwhere_hostproviderhosttype_permission() {

  // We set up permisssions to manage entity types, manage all entities and the
  // permissions for each individual entity
  $permissions = array(
    'administer thumbwhere_hostproviderhosttypes' => array(
      'title' => t('Administer thumbwhere_hostproviderhosttypes'),
      'description' => t('Edit and delete all thumbwhere_hostproviderhosttypes'),
    ),
  'send thumbwhere hostproviderhosttype notifications' => array(
          'title' => t('Send ThumbWhere Notifications'),
          'description' => t('Allow the caller to call the ThumbWhere HostProviderHostType Entity Notifications Endpoint.'),
          'restrict access' => TRUE,
      ),    
  );

  return $permissions;
}


/**
 * Determines whether the given user has access to a thumbwhere_hostproviderhosttype.
 *
 * @param $op
 *   The operation being performed. One of 'view', 'update', 'create', 'delete'
 *   or just 'edit' (being the same as 'create' or 'update').
 * @param $thumbwhere_hostproviderhosttype
 *   Optionally a thumbwhere_hostproviderhosttype or a thumbwhere_hostproviderhosttype type to check access for. If nothing is
 *   given, access for all thumbwhere_hostproviderhosttypes is determined.
 * @param $account
 *   The user to check for. Leave it to NULL to check for the global user.
 * @return boolean
 *   Whether access is allowed or not.
 */
function thumbwhere_hostproviderhosttype_access($op, $thumbwhere_hostproviderhosttype = NULL, $account = NULL) {

  //if (user_access('administer thumbwhere_hostproviderhosttypes', $account)) {
  //  return TRUE;
 // }

  //return FALSE;
  return TRUE;
}




/**
 * Fetch a thumbwhere_hostproviderhosttype object. Make sure that the wildcard you choose
 * in the thumbwhere_hostproviderhosttype entity definition fits the function name here.
 *
 * @param $thumbwhere_hostproviderhosttype_id
 *   Integer specifying the thumbwhere_hostproviderhosttype id.
 * @param $reset
 *   A boolean indicating that the internal cache should be reset.
 * @return
 *   A fully-loaded $thumbwhere_hostproviderhosttype object or FALSE if it cannot be loaded.
 *
 * @see thumbwhere_hostproviderhosttype_load_multiple()
 */
function thumbwhere_hostproviderhosttype_load($thumbwhere_hostproviderhosttype_id, $reset = FALSE) {

  $thumbwhere_hostproviderhosttypes = entity_load('thumbwhere_hostproviderhosttype', array($thumbwhere_hostproviderhosttype_id), array(), $reset);

  return reset($thumbwhere_hostproviderhosttypes);
}


/**
 * Deletes a thumbwhere_hostproviderhosttype.
 */
function thumbwhere_hostproviderhosttype_delete(ThumbWhereHostProviderHostType $thumbwhere_hostproviderhosttype) {

  $thumbwhere_hostproviderhosttype->delete();
}

/**
 * Create a thumbwhere_hostproviderhosttype object.
 */
function thumbwhere_hostproviderhosttype_create($values = array()) {

  return entity_get_controller('thumbwhere_hostproviderhosttype')->create($values);
}


/**
 * URI callback for thumbwhere_hostproviderhosttypes
 */
function thumbwhere_hostproviderhosttype_uri(ThumbWhereHostProviderHostType $thumbwhere_hostproviderhosttype) {

  return array(
    'path' => 'thumbwhere_hostproviderhosttype/' . $thumbwhere_hostproviderhosttype->pk_hostproviderhosttype,
  );
}


/**
 * Menu title callback for showing individual entities
 */
function thumbwhere_hostproviderhosttype_page_title(ThumbWhereHostProviderHostType $thumbwhere_hostproviderhosttype) {

	return $thumbwhere_hostproviderhosttype->pk_hostproviderhosttype;
}


/**
 * Sets up content to show an individual thumbwhere_hostproviderhosttype
 * @todo - get rid of drupal_set_title();
 */
function thumbwhere_hostproviderhosttype_page_view($thumbwhere_hostproviderhosttype, $view_mode = 'full') {



  $controller = entity_get_controller('thumbwhere_hostproviderhosttype');
  $content = $controller->view(array($thumbwhere_hostproviderhosttype->pk_hostproviderhosttype => $thumbwhere_hostproviderhosttype));
  drupal_set_title($thumbwhere_hostproviderhosttype->pk_hostproviderhosttype);
  return $content;
}


/**
 * Implements hook_views_api().
 */
function thumbwhere_hostproviderhosttype_views_api() {



  return array(
    'api' => 3,
    'path' => drupal_get_path('module', 'thumbwhere_hostproviderhosttype') . '/views',
  );
}


/**
 * Implement hook_theme().
 */
function thumbwhere_hostproviderhosttype_theme() {



  return array(
    'thumbwhere_hostproviderhosttype_add_list' => array(
      'variables' => array('content' => array()),
      'file' => 'thumbwhere_hostproviderhosttype.admin.inc',
    ),
    'thumbwhere_hostproviderhosttype' => array(
      'render element' => 'elements',
      'template' => 'thumbwhere_hostproviderhosttype',
    )
  );
}


/**
 * Implements hook_menu_local_tasks_alter().
 */
function thumbwhere_hostproviderhosttype_menu_local_tasks_alter(&$data, $router_item, $root_path) {

  // Add action link 'admin/structure/thumbwhere_hostproviderhosttypes/add' on 'admin/structure/thumbwhere_hostproviderhosttypes'.
  if ($root_path == 'admin/thumbwhere/thumbwhere_hostproviderhosttypes') {
    $item = menu_get_item('admin/thumbwhere/thumbwhere_hostproviderhosttypes/add');
    if ($item['access']) {
      $data['actions']['output'][] = array(
        '#theme' => 'menu_local_action',
        '#link' => $item,
      );
    }
  }
}


/**
 * The class used for thumbwhere_hostproviderhosttype entities
 */
class ThumbWhereHostProviderHostType extends Entity {

  public function __construct($values = array()) {
    parent::__construct($values, 'thumbwhere_hostproviderhosttype');
  }

  protected function defaultLabel() {
    return $this->pk_hostproviderhosttype;
  }

  protected function defaultUri() {
    return array('path' => 'thumbwhere_hostproviderhosttype/' . $this->pk_hostproviderhosttype);
  }


}



/**
 * The Controller for ThumbWhereHostProviderHostType entities
 */
class ThumbWhereHostProviderHostTypeController extends EntityAPIController {
  public function __construct($entityType) {
    parent::__construct($entityType);
  }
}



/**
* Implements hook_entity_presave().
*
* Act on an HostProviderHostType entity before it is about to be created or updated.
*
* @param $entity
*   The entity object.
* @param $entity_type
*   The type of entity being saved (i.e. node, user, comment).
*/
function thumbwhere_hostproviderhosttype_entity_presave($entity, $entity_type) {

  // We only look after what we want...
  if ($entity_type == 'thumbwhere_hostproviderhosttype')  {
  	
  	
  	
 	/////////////////////////////////////////////////////
    //
    // Should we not propagate this to ThumbWhere? 
    //
    if (isset($entity->do_not_propagate)) {
      if ($entity->do_not_propagate) {
        if (twCanDebug()) {
          if ($entity->is_new) {
            if (twCanDebug()){
              debug("DO NOT PROPAGATE 'hostproviderhosttype' presave *CREATE*");
          	}
          }
          else {
            if (twCanDebug()) {
              debug("DO NOT PROPAGATE 'hostproviderhosttype' presave *UPDATE*");
            }
          }
        }

        // To cause an infinite loop of updates, comment out this line.
        return;
      }
      else if (twCanDebug()) {
        debug("OK PROAOGATE 'hostproviderhosttype' presave to ThumbWhere Server");
      }
    }
    else if (twCanDebug()) {
      debug("DO NOT PROPAGATE IS NOT SET");
    }
    //
    //
    ////////////////////////////////////////////////////////  	

  
    // .. then we create a new thumbwhere_hostproviderhosttype entity...
      // Instantiate the ThumbWhereContent class

    $twAdminAPI = new ThumbWhereAPIAdmin();

    //if (twCanDebug()) debug($twContentAPI);

    //if (twCanDebug()) debug($entity);

    //
    // Collate and validate our fields
    //

    $fields = array();
    // Compulsory so..
    if (!isset($entity->fk_hostprovider)) {
      throw new Exception('hostprovider is mandatory.');
    }
    else {
      $fields['hostprovider'] = $entity->fk_hostprovider;
    }
    // Compulsory so..
    if (!isset($entity->fk_hosttype)) {
      throw new Exception('hosttype is mandatory.');
    }
    else {
      $fields['hosttype'] = $entity->fk_hosttype;
    }
		
	//
	// Ensure that we have the 'thumbwhere_api_key' as we are going to pass that through in a context
	//
    if (variable_get('thumbwhere_api_key') == '') {
    	throw new Exception('ThumbWhere resource for \'HostProviderHostType\' requires that variable \'thumbwhere_api_key\' is set.');
    }
    
              		
    //
    // If this hostproviderhosttype entity is new....
    //
               
    if ($entity->is_new) {
                

      //
      // ... then we create the hostproviderhosttype entity in ThumbWhere
      //

      $hostproviderhosttype = $twAdminAPI->create_host_provider_host_type($context = array(
                  'key' => variable_get('thumbwhere_api_key')
          ), $fields);
            
       if (twCanDebug()) debug($hostproviderhosttype);

      //if (twCanDebug()) debug($host_provider_host_type);

      $attributes = $hostproviderhosttype->body->host_provider_host_type->attributes();

      if (twCanDebug()) debug('newly created id = ' . $attributes['id']);

      // Assign new id.
      $entity->pk_hostproviderhosttype = intval($attributes['id']);

      if (twCanDebug()) debug('$entity->pk_hostproviderhosttype = ' . $entity->pk_hostproviderhosttype);
    }
    else
    {
          	

      //
      // .. otherwise we update the hostproviderhosttype entity in ThumbWhere
      //

      $hostproviderhosttype = $twAdminAPI->update_host_provider_host_type($id=$entity->pk_hostproviderhosttype,$context = array(
                  'key' => variable_get('thumbwhere_api_key')
          ), $fields);
            
        
    }
          	
    
  }
}

/**
 * Implements thumbwhere_hostproviderhosttype_entity_insert().
 *
 * Act on entities when inserted.
 *
 * @param $entity
 *   The entity object
 * @param $type
 *   The type of entity being inserted (i.e. node, user, comment).
 */
function thumbwhere_hostproviderhosttype_entity_insert($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_hostproviderhosttype') return;

}


/**
* Implements thumbwhere_hostproviderhosttype_entity_delete().
*
* Act on entities when inserted.
*
* @param $entity
*   The entity object
* @param $type
*   The type of entity being deleted (i.e. node, user, comment).
*/
function thumbwhere_hostproviderhosttype_entity_delete($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_hostproviderhosttype') return;


}
