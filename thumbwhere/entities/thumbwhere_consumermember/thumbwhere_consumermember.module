<?php

// ******************************************************************
// This file was generated by autogen_model_implementation_drupal.xsl
// ******************************************************************

// Include the ThumbWhere SDK
require_once(dirname(__FILE__) . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . 'dependencies' . DIRECTORY_SEPARATOR . 'tw' . DIRECTORY_SEPARATOR . 'sdk.class.php');

/**
 * @file
 * Module for the ThumbWhere ConsumerMember Entity
 *
 *
 * NOTE: This has been autogenerated.
 * 
 */

/**
 * Implement hook_entity_info().
 *
 * We define two entities here - the actual entity that will hold our domain
 * specific information and an entity that holds information about the different
 * types of entities. See here: http://drupal.org/node/977380 for a discussion on this
 * choice.
 */
function thumbwhere_consumermember_entity_info() {
  $return['thumbwhere_consumermember'] = array(

    // The human-readable name of the type.
    'label' => t('ThumbWhereConsumerMember'),

    // (used by DrupalDefaultEntityController) The name of the entity type's base table.
    'base table' => 'thumbwhere_consumermember',

    // fieldable: Set to TRUE if you want your entity type to accept fields being attached to it.
    'fieldable' => TRUE,

  // Declare the endty keys..
    'entity keys' => array(
      'id' => 'pk_consumermember',
      'label' => 'pk_consumermember',
    ),

    // A function taking an entity and an entity type as arguments and returning the label of the entity.
    // The entity label is the main string associated with an entity; for example, the title of a node or
    // the subject of a comment. If there is an entity object property that defines the label, use the
    // 'label' element of the 'entity keys' return value component to provide this information (see below).
    // If more complex logic is needed to determine the label of an entity, you can instead specify a callback
    // function here, which will be called to determine the entity label. See also the entity_label()
    // function, which implements this logic.
    'label callback' => 'entity_class_label',

    // A function taking an entity as argument and returning the uri elements of the entity, e.g. 'path' and 'options'.
    // The actual entity uri can be constructed by passing these elements to url().
    'uri callback' => 'entity_class_uri',

    //
    // Entity API Components
    //

    // The module whwre we can find our classes
    'module' => 'thumbwhere_consumermember',

    // The entity class and controller class extend the classes provided by the Entity API
    'entity class' => 'ThumbWhereConsumerMember',

    // Which Entity API contoller
    'controller class' => 'ThumbWhereConsumerMemberController',

    // The information below is used by the ThumbWhereConsumerMemberUIController (which extends the EntityDefaultUIController)
    'admin ui' => array(
      'path' => 'admin/content/thumbwhere_consumermembers',
      'file' => 'thumbwhere_consumermember.admin.inc',
      'controller class' => 'ThumbWhereConsumerMemberUIController',
      'menu wildcard' => '%thumbwhere_consumermember',
    ),
  );

  return $return;
}


/**
 * Implements hook_permission().
 */
function thumbwhere_consumermember_permission() {

  // We set up permisssions to manage entity types, manage all entities and the
  // permissions for each individual entity
  $permissions = array(
    'administer thumbwhere_consumermembers' => array(
      'title' => t('Administer thumbwhere_consumermembers'),
      'description' => t('Edit and delete all thumbwhere_consumermembers'),
    ),
  'send thumbwhere consumermember notifications' => array(
          'title' => t('Send ThumbWhere Notifications'),
          'description' => t('Allow the caller to call the ThumbWhere ConsumerMember Entity Notifications Endpoint.'),
          'restrict access' => TRUE,
      ),    
  );

  return $permissions;
}


/**
 * Determines whether the given user has access to a thumbwhere_consumermember.
 *
 * @param $op
 *   The operation being performed. One of 'view', 'update', 'create', 'delete'
 *   or just 'edit' (being the same as 'create' or 'update').
 * @param $thumbwhere_consumermember
 *   Optionally a thumbwhere_consumermember or a thumbwhere_consumermember type to check access for. If nothing is
 *   given, access for all thumbwhere_consumermembers is determined.
 * @param $account
 *   The user to check for. Leave it to NULL to check for the global user.
 * @return boolean
 *   Whether access is allowed or not.
 */
function thumbwhere_consumermember_access($op, $thumbwhere_consumermember = NULL, $account = NULL) {

  //if (user_access('administer thumbwhere_consumermembers', $account)) {
  //  return TRUE;
 // }

  //return FALSE;
  return TRUE;
}




/**
 * Fetch a thumbwhere_consumermember object. Make sure that the wildcard you choose
 * in the thumbwhere_consumermember entity definition fits the function name here.
 *
 * @param $thumbwhere_consumermember_id
 *   Integer specifying the thumbwhere_consumermember id.
 * @param $reset
 *   A boolean indicating that the internal cache should be reset.
 * @return
 *   A fully-loaded $thumbwhere_consumermember object or FALSE if it cannot be loaded.
 *
 * @see thumbwhere_consumermember_load_multiple()
 */
function thumbwhere_consumermember_load($thumbwhere_consumermember_id, $reset = FALSE) {

  //

  $thumbwhere_consumermembers = entity_load('thumbwhere_consumermember', array($thumbwhere_consumermember_id), array(), $reset);

  return reset($thumbwhere_consumermembers);
}





/**
 * Load multiple thumbwhere_consumermembers based on certain conditions.
 *
 * @param $thumbwhere_consumermember_ids
 *   An array of thumbwhere_consumermember IDs.
 * @param $conditions
 *   An array of conditions to match against the {thumbwhere_consumermember} table.
 * @param $reset
 *   A boolean indicating that the internal cache should be reset.
 * @return
 *   An array of thumbwhere_consumermember objects, indexed by thumbwhere_consumermember_id.
 *
 * @see entity_load()
 * @see thumbwhere_consumermember_load()
 */
/*
function thumbwhere_consumermember_load_multiple($thumbwhere_consumermember_ids = array(), $conditions = array(), $reset = FALSE) {



  $loaded = entity_load('thumbwhere_consumermember', $thumbwhere_consumermember_ids, $conditions, $reset);

  return $loaded;
}
*/

/**
 * Deletes a thumbwhere_consumermember.
 */
function thumbwhere_consumermember_delete(ThumbWhereConsumerMember $thumbwhere_consumermember) {

  $thumbwhere_consumermember->delete();
}


/**
 * Delete multiple thumbwhere_consumermembers.
 *
 * @param $thumbwhere_consumermember_ids
 *   An array of thumbwhere_consumermember IDs.
 */
/*
function thumbwhere_consumermember_delete_multiple(array $thumbwhere_consumermember_ids) {



  entity_get_controller('thumbwhere_consumermember')->delete($thumbwhere_consumermember_ids);
}
*/

/**
 * Create a thumbwhere_consumermember object.
 */
function thumbwhere_consumermember_create($values = array()) {

  return entity_get_controller('thumbwhere_consumermember')->create($values);
}


/**
 * Saves a thumbwhere_consumermember to the database.
 *
 * @param $thumbwhere_consumermember
 *   The thumbwhere_consumermember object.
 */
/*
function thumbwhere_consumermember_save(ThumbWhereConsumerMember $thumbwhere_consumermember) {



  return $thumbwhere_consumermember->save();
}*/



/**
 * Insert a thumbwhere_consumermember
 *
 * @param $thumbwhere_consumermember
 *   The thumbwhere_consumermember object.
 */
/*
function thumbwhere_consumermember_insert(array $values = array()) {



  //return $thumbwhere_consumermember->create($values);
  return entity_get_controller('thumbwhere_consumermember')->insert($values);
}
*/






/**
 * URI callback for thumbwhere_consumermembers
 */
function thumbwhere_consumermember_uri(ThumbWhereConsumerMember $thumbwhere_consumermember) {



  return array(
    'path' => 'thumbwhere_consumermember/' . $thumbwhere_consumermember->pk_consumermember,
  );
}


/**
 * Menu title callback for showing individual entities
 */
function thumbwhere_consumermember_page_title(ThumbWhereConsumerMember $thumbwhere_consumermember) {

  

  return $thumbwhere_consumermember->pk_consumermember;
}


/**
 * Sets up content to show an individual thumbwhere_consumermember
 * @todo - get rid of drupal_set_title();
 */
function thumbwhere_consumermember_page_view($thumbwhere_consumermember, $view_mode = 'full') {



  $controller = entity_get_controller('thumbwhere_consumermember');
  $content = $controller->view(array($thumbwhere_consumermember->pk_consumermember => $thumbwhere_consumermember));
  drupal_set_title($thumbwhere_consumermember->pk_consumermember);
  return $content;
}


/**
 * Implements hook_views_api().
 */
function thumbwhere_consumermember_views_api() {



  return array(
    'api' => 3,
    'path' => drupal_get_path('module', 'thumbwhere_consumermember') . '/views',
  );
}


/**
 * Implement hook_theme().
 */
function thumbwhere_consumermember_theme() {



  return array(
    'thumbwhere_consumermember_add_list' => array(
      'variables' => array('content' => array()),
      'file' => 'thumbwhere_consumermember.admin.inc',
    ),
    'thumbwhere_consumermember' => array(
      'render element' => 'elements',
      'template' => 'thumbwhere_consumermember',
    )
  );
}


/**
 * Implements hook_menu_local_tasks_alter().
 */
function thumbwhere_consumermember_menu_local_tasks_alter(&$data, $router_item, $root_path) {

  // Add action link 'admin/structure/thumbwhere_consumermembers/add' on 'admin/structure/thumbwhere_consumermembers'.
  if ($root_path == 'admin/content/thumbwhere_consumermembers') {
    $item = menu_get_item('admin/content/thumbwhere_consumermembers/add');
    if ($item['access']) {
      $data['actions']['output'][] = array(
        '#theme' => 'menu_local_action',
        '#link' => $item,
      );
    }
  }
}


/**
 * The class used for thumbwhere_consumermember entities
 */
class ThumbWhereConsumerMember extends Entity {

  public function __construct($values = array()) {
    parent::__construct($values, 'thumbwhere_consumermember');
  }

  protected function defaultLabel() {
    return $this->pk_consumermember;
  }

  protected function defaultUri() {
    return array('path' => 'thumbwhere_consumermember/' . $this->pk_consumermember);
  }


}



/**
 * The Controller for ThumbWhereConsumerMember entities
 */
class ThumbWhereConsumerMemberController extends EntityAPIController {
  public function __construct($entityType) {
    parent::__construct($entityType);
  }


  /**
   * Create a thumbwhere_consumermember - we first set up the values that are specific
   * to our thumbwhere_consumermember schema but then also go through the EntityAPIController
   * function.
   *
   * @param $type
   *   The machine-readable type of the thumbwhere_consumermember.
   *
   * @return
   *   A thumbwhere_consumermember object with all default fields initialized.
   */
  /*
  public function create(array $values = array()) {



    //$values += array(
    //  'thumbwhere_id' => -1,
    //);

    return parent::create($values);
  }
  */


  /**
  * view
  * to our thumbwhere_consumermember schema but then also go through the EntityAPIController
  * function.
  *
  * @param $type
  *   The machine-readable type of the thumbwhere_consumermember.
  *
  * @return
  *   A thumbwhere_consumermember object with all default fields initialized.
  */
  /*
  public function view($entities, $view_mode = 'full', $langcode = NULL, $page = NULL) {



    $view = parent::view($entities, $view_mode, $langcode, $page);

    return $view;
  }
  */




  /**
  * query - Builds and executes the query for loading.
  *
  * @param $ids
  *   Array of ids
  *
  * @param $conditions
  *
  *
  *
  * @return
  *   A thumbwhere_consumermember object with all default fields initialized.
  */
  /*
  public function load($ids = array(), $conditions = array()) {



    $result = parent::load($ids, $conditions);

    //$result[1]->token = "my token";

    return $result;
  }
  */

  /**
  * Invoke
  *
  * @param $ids
  *   Array of ids
  *
  * @param $transaction
  *   If not null then the transaction this is taking part in
  *
  * @return
  *   A thumbwhere_consumermember object with all default fields initialized.
  */
  /*
  public function invoke($hook, $entity) {



    parent::invoke($hook, $entity);

  }
  */


  /**
  * Delete a thumbwhere_consumermember.
  *
  * @param $ids
  *   Array of ids
  *
  * @param $transaction
  *   If not null then the transaction this is taking part in
  *
  * @return
  *   A thumbwhere_consumermember object with all default fields initialized.
  */
  /*
  public function delete($ids, DatabaseTransaction $transaction = NULL) {



    parent::delete($ids, $transaction);

  }
  */


  /**
  * Save a thumbwhere_consumermember.
  *
  * @param $thumbwhere_consumermember
  *   The thumbwhere_consumermember being saved
  *
  * @param $transaction
  *   If not null then the transaction this is taking part in
  *
  * @return
  *   A thumbwhere_consumermember object with all default fields initialized.
  */
  /*
  public function save($thumbwhere_consumermember, DatabaseTransaction $transaction = NULL) {



    return parent::save($thumbwhere_consumermember, $transaction);
  }
  */


  /**
  * resetCache - Called when the cache is
  *
  * @param $ids
  *   Array of ids
  *
  * @return
  *   void
  */
  /*
  public function resetCache(array $ids = NULL) {



    parent::resetCache($ids);
  }
  */


  /**
  * query - Builds and executes the query for loading.
  *
  * @param $ids
  *   Array of ids
  *
  * @param $conditions
  *
  * @param $revision_id
  *
  *
  * @return
  *   A thumbwhere_consumermember object with all default fields initialized.
  */
  /*
  public function query($ids, $conditions, $revision_id = FALSE) {



    $result= parent::query($ids, $conditions, $revision_id);

    return $result;
  }
  */


  /**
   * Overriding the buldConsumerMember function to add entity specific fields
   */
  /*
  public function buildConsumerMember($entity, $view_mode = 'full', $langcode = NULL, $content = array()) {

    $content = parent::buildConsumerMember($entity, $view_mode, $langcode, $content);

    $content['thumbwhere_consumermember_sample_data'] =  array(
      '#markup' => theme('thumbwhere_consumermember_sample_data', array('thumbwhere_consumermember_sample_data' => check_plain($entity->data['sample_data']), 'thumbwhere_consumermember' => $entity)),
    );

    return $content;
  }*/
}


/**
 * Implements hook_field_storage_load().
 *
 * Load field from storage
 *
 * @todo This needs to be improved
 */
/*
function thumbwhere_consumermember_field_storage_load($entity_type, $entities, $age, $fields, $options) {


}
*/



/**
* Implements hook_entity_presave().
*
* Act on an ConsumerMember entity before it is about to be created or updated.
*
* @param $entity
*   The entity object.
* @param $entity_type
*   The type of entity being saved (i.e. node, user, comment).
*/
function thumbwhere_consumermember_entity_presave($entity, $entity_type) {

  // We only look after what we want...
  if ($entity_type == 'thumbwhere_consumermember')  {

    // If this is new
    if ($entity->is_new) {
  
      // .. then we create a new thumbwhere_consumermember entity...
      // .. then we create a new thumbwhere_ConsumerMember entity...
      // Instantiate the ThumbWhereContent class

      $twContentAPI = new ThumbWhereAPIContent();

      //if (twCanDebug()) debug($twContentAPI);

      //if (twCanDebug()) debug($entity);

      //
      // Collate and validate our fields
      //

      $fields = array();
      // Compulsory so..
      if (empty($entity->fk_consumer)) {
        throw new Exception('consumer is compulsory.');
      }
      else {
        $fields['consumer'] = $entity->fk_consumer;
      }
      // Compulsory so..
      if (empty($entity->fk_member)) {
        throw new Exception('member is compulsory.');
      }
      else {
        $fields['member'] = $entity->fk_member;
      }

      // Not nullable but we have a default value
      if (empty($entity->admin)) {
        $fields['admin'] = $entity->admin = 0;
      }
      else {
        $fields['admin'] = $entity->admin;
      }

      //
      // Now create the Entity in ThumbWhere
      //

      $consumermember = $twContentAPI->create_consumer_member($context = array(
                  'key' => variable_get('thumbwhere_api_key')
          ), $fields);
            
       if (twCanDebug()) debug($consumermember);

      //if (twCanDebug()) debug($consumer_member);

      $attributes = $consumermember->body->consumer_member->attributes();

      if (twCanDebug()) debug('newly created id = ' . $attributes['id']);

      // Assign new id.
      $entity->pk_consumermember = intval($attributes['id']);

      if (twCanDebug()) debug('$entity->pk_consumermember = ' . $entity->pk_consumermember);
    }
    else
    {
        // .. otherwise we calculate the changes and ship them off to thumbwhere
    }
  }
}

/**
 * Implements thumbwhere_consumermember_entity_insert().
 *
 * Act on entities when inserted.
 *
 * @param $entity
 *   The entity object
 * @param $type
 *   The type of entity being inserted (i.e. node, user, comment).
 */
function thumbwhere_consumermember_entity_insert($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_consumermember') return;

}


/**
* Implements thumbwhere_consumermember_entity_delete().
*
* Act on entities when inserted.
*
* @param $entity
*   The entity object
* @param $type
*   The type of entity being deleted (i.e. node, user, comment).
*/
function thumbwhere_consumermember_entity_delete($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_consumermember') return;


}
