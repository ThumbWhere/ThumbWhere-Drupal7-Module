<?php

/**
 * @file
 * Tests for thumbwhere_consumermember_field_storage.module.
 *
 * Field_sql_storage.module implements the default back-end storage plugin
 * for the Field Strage API.
 *
 * NOTE: This file was generated by autogen_model_implementation_drupal.xsl
 * 
 */

/**
 * Tests field storage.
 */
class ThumbWhereConsumerMemberIntegrationTestCase extends DrupalWebTestCase {
  public static function getInfo() {
    return array(
      'name'  => 'ConsumerMember Entity Integration Tests',
      'description'  => "Tests integration of the ThumbWhere ConsumerMember entity.",
      'group' => 'ThumbWhereIntegration'
    );
  }

  function setUp() {

    // We need temporary folder
    static $temporary_directory;
    $temporary_directory = 'C:\\TEMP\\';

    // Make sure we are using the testing profile
    $this->profile = 'testing';

    parent::setUp('thumbwhere_consumermember');
  }

  /**
   * Test that the field storage works.
   */
  function testSomething() {
      $entity = entity_create('thumbwhere_consumermember', array( 'name' => 'bob' ));

      $result = TRUE;
      $message = t("Test Passed.");
      $this->assertTrue($result, $message);

    }
}

