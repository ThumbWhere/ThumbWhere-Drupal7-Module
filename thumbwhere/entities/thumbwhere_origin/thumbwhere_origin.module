<?php

// ******************************************************************
// This file was generated by autogen_model_implementation_drupal.xsl
// ******************************************************************

// Include the ThumbWhere SDK
require_once(dirname(__FILE__) . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . 'dependencies' . DIRECTORY_SEPARATOR . 'tw' . DIRECTORY_SEPARATOR . 'sdk.class.php');

/**
 * @file
 * Module for the ThumbWhere Origin Entity
 *
 *
 * NOTE: This has been autogenerated.
 * 
 */

/**
 * Implement hook_entity_info().
 *
 * We define two entities here - the actual entity that will hold our domain
 * specific information and an entity that holds information about the different
 * types of entities. See here: http://drupal.org/node/977380 for a discussion on this
 * choice.
 */
function thumbwhere_origin_entity_info() {
  $return['thumbwhere_origin'] = array(

    // The human-readable name of the type.
    'label' => t('ThumbWhereOrigin'),

    // (used by DrupalDefaultEntityController) The name of the entity type's base table.
    'base table' => 'thumbwhere_origin',

    // fieldable: Set to TRUE if you want your entity type to accept fields being attached to it.
    'fieldable' => TRUE,

  // Declare the endty keys..
    'entity keys' => array(
      'id' => 'pk_origin',
      'label' => 'pk_origin',
    ),

    // A function taking an entity and an entity type as arguments and returning the label of the entity.
    // The entity label is the main string associated with an entity; for example, the title of a node or
    // the subject of a comment. If there is an entity object property that defines the label, use the
    // 'label' element of the 'entity keys' return value component to provide this information (see below).
    // If more complex logic is needed to determine the label of an entity, you can instead specify a callback
    // function here, which will be called to determine the entity label. See also the entity_label()
    // function, which implements this logic.
    'label callback' => 'entity_class_label',

    // A function taking an entity as argument and returning the uri elements of the entity, e.g. 'path' and 'options'.
    // The actual entity uri can be constructed by passing these elements to url().
    'uri callback' => 'entity_class_uri',

    //
    // Entity API Components
    //

    // The module whwre we can find our classes
    'module' => 'thumbwhere_origin',

    // The entity class and controller class extend the classes provided by the Entity API
    'entity class' => 'ThumbWhereOrigin',

    // Which Entity API contoller
    'controller class' => 'ThumbWhereOriginController',

    // The information below is used by the ThumbWhereOriginUIController (which extends the EntityDefaultUIController)
    'admin ui' => array(
      'path' => 'admin/thumbwhere/thumbwhere_origins',
      'file' => 'thumbwhere_origin.admin.inc',
      'controller class' => 'ThumbWhereOriginUIController',
      'menu wildcard' => '%thumbwhere_origin',
    ),
  );

  return $return;
}


/**
 * Implements hook_permission().
 */
function thumbwhere_origin_permission() {

  // We set up permisssions to manage entity types, manage all entities and the
  // permissions for each individual entity
  $permissions = array(
    'administer thumbwhere_origins' => array(
      'title' => t('Administer thumbwhere_origins'),
      'description' => t('Edit and delete all thumbwhere_origins'),
    ),
  'send thumbwhere origin notifications' => array(
          'title' => t('Send ThumbWhere Notifications'),
          'description' => t('Allow the caller to call the ThumbWhere Origin Entity Notifications Endpoint.'),
          'restrict access' => TRUE,
      ),    
  );

  return $permissions;
}


/**
 * Determines whether the given user has access to a thumbwhere_origin.
 *
 * @param $op
 *   The operation being performed. One of 'view', 'update', 'create', 'delete'
 *   or just 'edit' (being the same as 'create' or 'update').
 * @param $thumbwhere_origin
 *   Optionally a thumbwhere_origin or a thumbwhere_origin type to check access for. If nothing is
 *   given, access for all thumbwhere_origins is determined.
 * @param $account
 *   The user to check for. Leave it to NULL to check for the global user.
 * @return boolean
 *   Whether access is allowed or not.
 */
function thumbwhere_origin_access($op, $thumbwhere_origin = NULL, $account = NULL) {

  //if (user_access('administer thumbwhere_origins', $account)) {
  //  return TRUE;
 // }

  //return FALSE;
  return TRUE;
}




/**
 * Fetch a thumbwhere_origin object. Make sure that the wildcard you choose
 * in the thumbwhere_origin entity definition fits the function name here.
 *
 * @param $thumbwhere_origin_id
 *   Integer specifying the thumbwhere_origin id.
 * @param $reset
 *   A boolean indicating that the internal cache should be reset.
 * @return
 *   A fully-loaded $thumbwhere_origin object or FALSE if it cannot be loaded.
 *
 * @see thumbwhere_origin_load_multiple()
 */
function thumbwhere_origin_load($thumbwhere_origin_id, $reset = FALSE) {

  $thumbwhere_origins = entity_load('thumbwhere_origin', array($thumbwhere_origin_id), array(), $reset);

  return reset($thumbwhere_origins);
}


/**
 * Deletes a thumbwhere_origin.
 */
function thumbwhere_origin_delete(ThumbWhereOrigin $thumbwhere_origin) {

  $thumbwhere_origin->delete();
}

/**
 * Create a thumbwhere_origin object.
 */
function thumbwhere_origin_create($values = array()) {

  return entity_get_controller('thumbwhere_origin')->create($values);
}


/**
 * URI callback for thumbwhere_origins
 */
function thumbwhere_origin_uri(ThumbWhereOrigin $thumbwhere_origin) {

  return array(
    'path' => 'thumbwhere_origin/' . $thumbwhere_origin->pk_origin,
  );
}


/**
 * Menu title callback for showing individual entities
 */
function thumbwhere_origin_page_title(ThumbWhereOrigin $thumbwhere_origin) {

	return $thumbwhere_origin->pk_origin;
}


/**
 * Sets up content to show an individual thumbwhere_origin
 * @todo - get rid of drupal_set_title();
 */
function thumbwhere_origin_page_view($thumbwhere_origin, $view_mode = 'full') {



  $controller = entity_get_controller('thumbwhere_origin');
  $content = $controller->view(array($thumbwhere_origin->pk_origin => $thumbwhere_origin));
  drupal_set_title($thumbwhere_origin->pk_origin);
  return $content;
}


/**
 * Implements hook_views_api().
 */
function thumbwhere_origin_views_api() {



  return array(
    'api' => 3,
    'path' => drupal_get_path('module', 'thumbwhere_origin') . '/views',
  );
}


/**
 * Implement hook_theme().
 */
function thumbwhere_origin_theme() {



  return array(
    'thumbwhere_origin_add_list' => array(
      'variables' => array('content' => array()),
      'file' => 'thumbwhere_origin.admin.inc',
    ),
    'thumbwhere_origin' => array(
      'render element' => 'elements',
      'template' => 'thumbwhere_origin',
    )
  );
}


/**
 * Implements hook_menu_local_tasks_alter().
 */
function thumbwhere_origin_menu_local_tasks_alter(&$data, $router_item, $root_path) {

  // Add action link 'admin/structure/thumbwhere_origins/add' on 'admin/structure/thumbwhere_origins'.
  if ($root_path == 'admin/thumbwhere/thumbwhere_origins') {
    $item = menu_get_item('admin/thumbwhere/thumbwhere_origins/add');
    if ($item['access']) {
      $data['actions']['output'][] = array(
        '#theme' => 'menu_local_action',
        '#link' => $item,
      );
    }
  }
}


/**
 * The class used for thumbwhere_origin entities
 */
class ThumbWhereOrigin extends Entity {

  public function __construct($values = array()) {
    parent::__construct($values, 'thumbwhere_origin');
  }

  protected function defaultLabel() {
    return $this->pk_origin;
  }

  protected function defaultUri() {
    return array('path' => 'thumbwhere_origin/' . $this->pk_origin);
  }


}



/**
 * The Controller for ThumbWhereOrigin entities
 */
class ThumbWhereOriginController extends EntityAPIController {
  public function __construct($entityType) {
    parent::__construct($entityType);
  }
}



/**
* Implements hook_entity_presave().
*
* Act on an Origin entity before it is about to be created or updated.
*
* @param $entity
*   The entity object.
* @param $entity_type
*   The type of entity being saved (i.e. node, user, comment).
*/
function thumbwhere_origin_entity_presave($entity, $entity_type) {

  // We only look after what we want...
  if ($entity_type == 'thumbwhere_origin')  {
  	
  	
  	
 	/////////////////////////////////////////////////////
    //
    // Should we not propagate this to ThumbWhere? 
    //
    if (isset($entity->do_not_propagate)) {
      if ($entity->do_not_propagate) {
        if (twCanDebug()) {
          if ($entity->is_new) {
            if (twCanDebug()){
              debug("DO NOT PROPAGATE 'origin' presave *CREATE*");
          	}
          }
          else {
            if (twCanDebug()) {
              debug("DO NOT PROPAGATE 'origin' presave *UPDATE*");
            }
          }
        }

        // To cause an infinite loop of updates, comment out this line.
        return;
      }
      else if (twCanDebug()) {
        debug("OK PROAOGATE 'origin' presave to ThumbWhere Server");
      }
    }
    else if (twCanDebug()) {
      debug("DO NOT PROPAGATE IS NOT SET");
    }
    //
    //
    ////////////////////////////////////////////////////////  	

  
    // .. then we create a new thumbwhere_origin entity...
      // Instantiate the ThumbWhereContent class

    $twAdminAPI = new ThumbWhereAPIAdmin();

    //if (twCanDebug()) debug($twContentAPI);

    //if (twCanDebug()) debug($entity);

    //
    // Collate and validate our fields
    //

    $fields = array();
    // Compulsory so..
    if (!isset($entity->fk_host)) {
      throw new Exception('host is mandatory.');
    }
    else {
      $fields['host'] = $entity->fk_host;
    }
		
	//
	// Ensure that we have the 'thumbwhere_api_key' as we are going to pass that through in a context
	//
    if (variable_get('thumbwhere_api_key') == '') {
    	throw new Exception('ThumbWhere resource for \'Origin\' requires that variable \'thumbwhere_api_key\' is set.');
    }
    
              		
    //
    // If this origin entity is new....
    //
               
    if ($entity->is_new) {
                

      //
      // ... then we create the origin entity in ThumbWhere
      //

      $origin = $twAdminAPI->create_origin($context = array(
                  'key' => variable_get('thumbwhere_api_key')
          ), $fields);
            
       if (twCanDebug()) debug($origin);

      //if (twCanDebug()) debug($origin);

      $attributes = $origin->body->origin->attributes();

      if (twCanDebug()) debug('newly created id = ' . $attributes['id']);

      // Assign new id.
      $entity->pk_origin = intval($attributes['id']);

      if (twCanDebug()) debug('$entity->pk_origin = ' . $entity->pk_origin);
    }
    else
    {
          	

      //
      // .. otherwise we update the origin entity in ThumbWhere
      //

      $origin = $twAdminAPI->update_origin($id=$entity->pk_origin,$context = array(
                  'key' => variable_get('thumbwhere_api_key')
          ), $fields);
            
        
    }
          	
    
  }
}

/**
 * Implements thumbwhere_origin_entity_insert().
 *
 * Act on entities when inserted.
 *
 * @param $entity
 *   The entity object
 * @param $type
 *   The type of entity being inserted (i.e. node, user, comment).
 */
function thumbwhere_origin_entity_insert($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_origin') return;

}


/**
* Implements thumbwhere_origin_entity_delete().
*
* Act on entities when inserted.
*
* @param $entity
*   The entity object
* @param $type
*   The type of entity being deleted (i.e. node, user, comment).
*/
function thumbwhere_origin_entity_delete($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_origin') return;


}
