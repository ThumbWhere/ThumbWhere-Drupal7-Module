<?php

/**
 * @file
 * Provides list of operations that are possible in the thumbwhere_origin editing view.
 *
 * NOTE: This file was generated by autogen_model_implementation_drupal.xsl
 *   
 */

/**
 * This field handler aggregates operations that can be done on a thumbwhere_origin
 * under a single field providing a more flexible way to present them in a view
 */
class thumbwhere_origin_handler_thumbwhere_origin_operations_field extends views_handler_field {
  function construct() {
    parent::construct();

    $this->additional_fields['pk_origin'] = 'pk_origin';
  }

  function query() {
    $this->ensure_my_table();
    $this->add_additional_fields();
  }

  function render($values) {

    $links = menu_contextual_links('thumbwhere_origin', 'admin/thumbwhere/thumbwhere_origins/thumbwhere_origin', array($this->get_value($values, 'pk_origin')));

    if (!empty($links)) {
      return theme('links', array('links' => $links, 'attributes' => array('class' => array('links', 'inline', 'operations'))));
    }
  }
}
