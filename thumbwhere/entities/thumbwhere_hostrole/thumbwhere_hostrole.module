<?php

// ******************************************************************
// This file was generated by autogen_model_implementation_drupal.xsl
// ******************************************************************

// Include the ThumbWhere SDK
require_once(dirname(__FILE__) . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . 'dependencies' . DIRECTORY_SEPARATOR . 'tw' . DIRECTORY_SEPARATOR . 'sdk.class.php');

/**
 * @file
 * Module for the ThumbWhere HostRole Entity
 *
 *
 * NOTE: This has been autogenerated.
 * 
 */

/**
 * Implement hook_entity_info().
 *
 * We define two entities here - the actual entity that will hold our domain
 * specific information and an entity that holds information about the different
 * types of entities. See here: http://drupal.org/node/977380 for a discussion on this
 * choice.
 */
function thumbwhere_hostrole_entity_info() {
  $return['thumbwhere_hostrole'] = array(

    // The human-readable name of the type.
    'label' => t('ThumbWhereHostRole'),

    // (used by DrupalDefaultEntityController) The name of the entity type's base table.
    'base table' => 'thumbwhere_hostrole',

    // fieldable: Set to TRUE if you want your entity type to accept fields being attached to it.
    'fieldable' => TRUE,

  // Declare the endty keys..
    'entity keys' => array(
      'id' => 'pk_hostrole',
      'label' => 'pk_hostrole',
    ),

    // A function taking an entity and an entity type as arguments and returning the label of the entity.
    // The entity label is the main string associated with an entity; for example, the title of a node or
    // the subject of a comment. If there is an entity object property that defines the label, use the
    // 'label' element of the 'entity keys' return value component to provide this information (see below).
    // If more complex logic is needed to determine the label of an entity, you can instead specify a callback
    // function here, which will be called to determine the entity label. See also the entity_label()
    // function, which implements this logic.
    'label callback' => 'entity_class_label',

    // A function taking an entity as argument and returning the uri elements of the entity, e.g. 'path' and 'options'.
    // The actual entity uri can be constructed by passing these elements to url().
    'uri callback' => 'entity_class_uri',

    //
    // Entity API Components
    //

    // The module whwre we can find our classes
    'module' => 'thumbwhere_hostrole',

    // The entity class and controller class extend the classes provided by the Entity API
    'entity class' => 'ThumbWhereHostRole',

    // Which Entity API contoller
    'controller class' => 'ThumbWhereHostRoleController',

    // The information below is used by the ThumbWhereHostRoleUIController (which extends the EntityDefaultUIController)
    'admin ui' => array(
      'path' => 'admin/thumbwhere/thumbwhere_hostroles',
      'file' => 'thumbwhere_hostrole.admin.inc',
      'controller class' => 'ThumbWhereHostRoleUIController',
      'menu wildcard' => '%thumbwhere_hostrole',
    ),
  );

  return $return;
}


/**
 * Implements hook_permission().
 */
function thumbwhere_hostrole_permission() {

  // We set up permisssions to manage entity types, manage all entities and the
  // permissions for each individual entity
  $permissions = array(
    'administer thumbwhere_hostroles' => array(
      'title' => t('Administer thumbwhere_hostroles'),
      'description' => t('Edit and delete all thumbwhere_hostroles'),
    ),
  'send thumbwhere hostrole notifications' => array(
          'title' => t('Send ThumbWhere Notifications'),
          'description' => t('Allow the caller to call the ThumbWhere HostRole Entity Notifications Endpoint.'),
          //'restrict access' => TRUE,
      ),    
  );

  return $permissions;
}


/**
 * Determines whether the given user has access to a thumbwhere_hostrole.
 *
 * @param $op
 *   The operation being performed. One of 'view', 'update', 'create', 'delete'
 *   or just 'edit' (being the same as 'create' or 'update').
 * @param $thumbwhere_hostrole
 *   Optionally a thumbwhere_hostrole or a thumbwhere_hostrole type to check access for. If nothing is
 *   given, access for all thumbwhere_hostroles is determined.
 * @param $account
 *   The user to check for. Leave it to NULL to check for the global user.
 * @return boolean
 *   Whether access is allowed or not.
 */
function thumbwhere_hostrole_access($op, $thumbwhere_hostrole = NULL, $account = NULL) {

  //if (user_access('administer thumbwhere_hostroles', $account)) {
  //  return TRUE;
 // }

  //return FALSE;
  return TRUE;
}




/**
 * Fetch a thumbwhere_hostrole object. Make sure that the wildcard you choose
 * in the thumbwhere_hostrole entity definition fits the function name here.
 *
 * @param $thumbwhere_hostrole_id
 *   Integer specifying the thumbwhere_hostrole id.
 * @param $reset
 *   A boolean indicating that the internal cache should be reset.
 * @return
 *   A fully-loaded $thumbwhere_hostrole object or FALSE if it cannot be loaded.
 *
 * @see thumbwhere_hostrole_load_multiple()
 */
function thumbwhere_hostrole_load($thumbwhere_hostrole_id, $reset = FALSE) {

  $thumbwhere_hostroles = entity_load('thumbwhere_hostrole', array($thumbwhere_hostrole_id), array(), $reset);

  return reset($thumbwhere_hostroles);
}


/**
 * Deletes a thumbwhere_hostrole.
 */
function thumbwhere_hostrole_delete(ThumbWhereHostRole $thumbwhere_hostrole) {

  $thumbwhere_hostrole->delete();
}

/**
 * Create a thumbwhere_hostrole object.
 */
function thumbwhere_hostrole_create($values = array()) {

  return entity_get_controller('thumbwhere_hostrole')->create($values);
}


/**
 * URI callback for thumbwhere_hostroles
 */
function thumbwhere_hostrole_uri(ThumbWhereHostRole $thumbwhere_hostrole) {

  return array(
    'path' => 'thumbwhere_hostrole/' . $thumbwhere_hostrole->pk_hostrole,
  );
}


/**
 * Menu title callback for showing individual entities
 */
function thumbwhere_hostrole_page_title(ThumbWhereHostRole $thumbwhere_hostrole) {

	return $thumbwhere_hostrole->pk_hostrole;
}


/**
 * Sets up content to show an individual thumbwhere_hostrole
 * @todo - get rid of drupal_set_title();
 */
function thumbwhere_hostrole_page_view($thumbwhere_hostrole, $view_mode = 'full') {



  $controller = entity_get_controller('thumbwhere_hostrole');
  $content = $controller->view(array($thumbwhere_hostrole->pk_hostrole => $thumbwhere_hostrole));
  drupal_set_title($thumbwhere_hostrole->pk_hostrole);
  return $content;
}


/**
 * Implements hook_views_api().
 */
function thumbwhere_hostrole_views_api() {



  return array(
    'api' => 3,
    'path' => drupal_get_path('module', 'thumbwhere_hostrole') . '/views',
  );
}


/**
 * Implement hook_theme().
 */
function thumbwhere_hostrole_theme() {



  return array(
    'thumbwhere_hostrole_add_list' => array(
      'variables' => array('content' => array()),
      'file' => 'thumbwhere_hostrole.admin.inc',
    ),
    'thumbwhere_hostrole' => array(
      'render element' => 'elements',
      'template' => 'thumbwhere_hostrole',
    )
  );
}


/**
 * Implements hook_menu_local_tasks_alter().
 */
function thumbwhere_hostrole_menu_local_tasks_alter(&$data, $router_item, $root_path) {

  // Add action link 'admin/structure/thumbwhere_hostroles/add' on 'admin/structure/thumbwhere_hostroles'.
  if ($root_path == 'admin/thumbwhere/thumbwhere_hostroles') {
    $item = menu_get_item('admin/thumbwhere/thumbwhere_hostroles/add');
    if ($item['access']) {
      $data['actions']['output'][] = array(
        '#theme' => 'menu_local_action',
        '#link' => $item,
      );
    }
  }
}


/**
 * The class used for thumbwhere_hostrole entities
 */
class ThumbWhereHostRole extends Entity {

  public function __construct($values = array()) {
    parent::__construct($values, 'thumbwhere_hostrole');
  }

  protected function defaultLabel() {
    return $this->pk_hostrole;
  }

  protected function defaultUri() {
    return array('path' => 'thumbwhere_hostrole/' . $this->pk_hostrole);
  }


}



/**
 * The Controller for ThumbWhereHostRole entities
 */
class ThumbWhereHostRoleController extends EntityAPIController {
  public function __construct($entityType) {
    parent::__construct($entityType);
  }
}



/**
* Implements hook_entity_presave().
*
* Act on an HostRole entity before it is about to be created or updated.
*
* @param $entity
*   The entity object.
* @param $entity_type
*   The type of entity being saved (i.e. node, user, comment).
*/
function thumbwhere_hostrole_entity_presave($entity, $entity_type) {

  // We only look after what we want...
  if ($entity_type == 'thumbwhere_hostrole')  {
  	
 	/////////////////////////////////////////////////////
    //
    // Should we not propagate this to ThumbWhere? 
    //
    if (isset($entity->do_not_propagate)) {
      if ($entity->do_not_propagate) {
        if (twCanDebug()) {
          if ($entity->is_new) {
            if (twCanDebug()){
              debug("DO NOT PROPAGATE 'hostrole' presave *CREATE* not being sent to ThumbWhere server.");
          	}
          }
          else {
            if (twCanDebug()) {
              debug("DO NOT PROPAGATE 'hostrole' presave *UPDATE* not being sent to ThumbWhere server.");
            }
          }
        }

        // To cause an infinite loop of updates, comment out this line.
        return;
      }
      else if (twCanDebug()) {
        debug("OK PROAOGATE 'hostrole' presave to ThumbWhere server.");
      }
    }
    else if (twCanDebug()) {
      debug("DO NOT PROPAGATE IS NOT SET");
    }
    //
    //
    ////////////////////////////////////////////////////////  	

  
    // .. then we create a new thumbwhere_hostrole entity...
      // Instantiate the ThumbWhereContent class

    $twAdminAPI = new ThumbWhereAPIAdmin();

    //if (twCanDebug()) debug($twContentAPI);

    //if (twCanDebug()) debug($entity);

    //
    // Collate and validate our fields
    //

    $fields = array();
    // Compulsory so..
    if (!isset($entity->fk_hostroletype)) {
      throw new Exception('hostroletype is mandatory.');
    }
    else {
      $fields['hostroletype'] = $entity->fk_hostroletype;
    }

    // Nullable so we only pass it through to create_host_role if we have it..
    if (isset($entity->fk_host)) {
      $fields['host'] = $entity->fk_host;
    }
		
	//
	// Ensure that we have the 'thumbwhere_api_key' as we are going to pass that through in a context
	//
  if (variable_get('thumbwhere_api_key') == '') {
    throw new Exception('ThumbWhere resource calls for \'HostRole\' requires that variable \'thumbwhere_api_key\' is set.');
  }

	//
	// Ensure that we have the 'thumbwhere_api_key' as we are going to pass that through in a context
	//
  if (!is_numeric(variable_get('thumbwhere_host_id'))) {
    throw new Exception('ThumbWhere resource calls for \'HostRole\' requires that variable \'thumbwhere_host_id\' is set.');
  }


              		
    //
    // If this hostrole entity is new....
    //
               
    if ($entity->is_new) {
                

      //
      // ... then we create the hostrole entity in ThumbWhere
      //

      $hostrole = $twAdminAPI->create_host_role($context = array(
                  'key' => variable_get('thumbwhere_api_key'),
                  'origin' => variable_get('thumbwhere_host_id'),
          ), $fields);
            
       if (twCanDebug()) debug($hostrole);

      //if (twCanDebug()) debug($host_role);

      $attributes = $hostrole->body->host_role->attributes();

      if (twCanDebug()) debug('newly created id = ' . $attributes['id']);

      // Assign new id.
      $entity->pk_hostrole = intval($attributes['id']);

      if (twCanDebug()) debug('$entity->pk_hostrole = ' . $entity->pk_hostrole);
    }
    else
    {
          	

      //
      // .. otherwise we update the hostrole entity in ThumbWhere
      //

      $hostrole = $twAdminAPI->update_host_role($id=$entity->pk_hostrole,$context = array(
                  'key' => variable_get('thumbwhere_api_key'),
                  'origin' => variable_get('thumbwhere_host_id'),
          ), $fields);
            
        
    }
          	
    
  }
}

/**
 * Implements thumbwhere_hostrole_entity_insert().
 *
 * Act on entities when inserted.
 *
 * @param $entity
 *   The entity object
 * @param $type
 *   The type of entity being inserted (i.e. node, user, comment).
 */
function thumbwhere_hostrole_entity_insert($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_hostrole') return;

}


/**
* Implements thumbwhere_hostrole_entity_delete().
*
* Act on entities when inserted.
*
* @param $entity
*   The entity object
* @param $type
*   The type of entity being deleted (i.e. node, user, comment).
*/
function thumbwhere_hostrole_entity_delete($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_hostrole') return;


}
