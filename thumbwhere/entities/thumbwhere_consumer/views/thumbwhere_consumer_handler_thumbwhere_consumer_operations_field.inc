<?php

/**
 * @file
 * Provides list of operations that are possible in the thumbwhere_consumer editing view.
 *
 * NOTE: This file was generated by autogen_model_implementation_drupal.xsl
 *   
 */

/**
 * This field handler aggregates operations that can be done on a thumbwhere_consumer
 * under a single field providing a more flexible way to present them in a view
 */
class thumbwhere_consumer_handler_thumbwhere_consumer_operations_field extends views_handler_field {
  function construct() {
    parent::construct();

    $this->additional_fields['pk_consumer'] = 'pk_consumer';
  }

  function query() {
    $this->ensure_my_table();
    $this->add_additional_fields();
  }

  function render($values) {

    $links = menu_contextual_links('thumbwhere_consumer', 'admin/content/thumbwhere_consumers/thumbwhere_consumer', array($this->get_value($values, 'pk_consumer')));

    if (!empty($links)) {
      return theme('links', array('links' => $links, 'attributes' => array('class' => array('links', 'inline', 'operations'))));
    }
  }
}
