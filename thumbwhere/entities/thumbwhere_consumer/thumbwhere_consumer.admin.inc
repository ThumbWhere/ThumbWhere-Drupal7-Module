<?php

/**
 * @file
 * ThumbWhereConsumer editing UI.
 *
 * We make very little use of the EntityAPI interface for this - preferring instead to use
 * views. That offers more flexibility to change a UI that will, more often than not,
 * be end-user facing.
 *
 * NOTE: This file was generated by autogen_model_implementation_drupal.xsl
 */

 
/**
 * Handle the notification for content_ingext
 */
function _thumbwhere_handle_consumer_notify_xml($notification) {

  if (twCanDebug()) debug('PROCESS NOTIFICATION');
  if (twCanDebug()) debug($notification);

  //
  // Get the operation
  //

  if (!isset($notification->attributes()->op)) {
    drupal_add_http_header('Status', '501 unable to fulfil request..');
    $err = 'We were expecting an \'op\', attribute on the \'consumer\', element.';
    print($err);
   	watchdog('thumbwhere_consumer', $err ,array(), WATCHDOG_ERROR);
    exit(0);
  }

  $op = $notification->attributes()->op;

  if (twCanDebug()) debug('op');
  if (twCanDebug()) debug($op);

  //
  // If this is a notification then $op should be 'notify'
  //

  if (!isset($notification->attributes()->notification)) {
    drupal_add_http_header('Status', '501 unable to fulfil request..');
    $err = 'We were expecting an \'notification\', attribute on the \'consumer\', element.';
    print($err);
   	watchdog('thumbwhere_consumer', $err ,array(), WATCHDOG_ERROR);
    exit(0);
  }

  $notification_op = $notification->attributes()->notification;

  if (twCanDebug()) debug('notification');
  if (twCanDebug()) debug($notification_op);


  //
  // Get the context
  //

  if (!isset($notification->attributes()->key)) {
    drupal_add_http_header('Status', '412 unable to fulfil request..');
    $err = 'We were expecting a \'key\', attribute on the \'consumer\', element.';
    print($err);
    watchdog('thumbwhere_consumer', $err ,array(), WATCHDOG_ERROR);
    exit(0);
  }

  $context_key =  $notification->attributes()->key;

  // Start a transaction
  //$transaction = db_transaction('consumer_notify');
  $transaction = null;

  //
  // Now based on the operation..
  //

  switch($notification_op)
  {
  
  
      case 'create':
      {
      		
		
		    //
		    // We want to time this
		    //
			
		    $start_time = microtime(TRUE);
			
        //
        // What resource are we creating?
        //
      
        //
        // Get the id
        //
		
        if (!isset($notification->attributes()->id)) {
          drupal_add_http_header('Status', '412 Unable to fulfil request.');
          $err = 'We were expecting an \'id\', attribute on the \'consumer\', element when we are handling a \'create\' notification..';
          print($err);
          watchdog('thumbwhere_consumer', $err ,array(), WATCHDOG_ERROR);
          exit(0);
        }

        $id = intval($notification->attributes()->id);
		

        if (twCanDebug()) debug('id');
        if (twCanDebug()) debug($id);


        //
        // Get the origin
        //
		
		    $origin = -1;
		    
        if (isset($notification->attributes()->origin)) {        
          $origin = intval($notification->attributes()->origin);

          if (twCanDebug()) debug('origin');
          if (twCanDebug()) debug($origin);

          if ($origin == variable_get('thumbwhere_host_id',-1)) {
            $err = 'Origin of this notification ' . $origin  . ' is the same as this host. \'consumer\', Dropping this \'create\' notification.';
            watchdog('thumbwhere_consumer', $err ,array(), WATCHDOG_ERROR);
              
            // We act all idempotent like its all good. 'create', when it already exists, is ignored. 
            drupal_add_http_header('Content-Type', 'text/xml');
            print('<response>');
            print('<consumer op="' . $op . '" id="' . $id . '" origin="' . $origin . '" >');
            print('<status>success</status>');
            print('</consumer>');
            print('</response>');
            exit(0);               
          }
        }
              
        watchdog('thumbwhere_consumer', 'Notification: \'create\' consumer $<a href="/admin/thumbwhere/thumbwhere_consumers/thumbwhere_consumer/' . $id .  '/edit">\'' . $id . '\'</a>  origin=\'' . $origin . '\'.',array(), WATCHDOG_NOTICE);
      
        //
        // Get the fields
        //
          
        $entities = entity_load('thumbwhere_consumer', array($id));

        if (twCanDebug()) debug('$entities');
        if (twCanDebug()) debug($entities);

        if (count($entities) != 0) {
          //drupal_add_http_header('Status', '409 Resource already exists.');
          $err = 'Error while servicing \'create\' notification. Resource \'consumer\' already exists with an id of \'' . $id . '\'.';
           //print($err);
          // When ORIGIN (TW-171) is sorted out then this can be re-anbled
          watchdog('thumbwhere_consumer', $err ,array(), WATCHDOG_ERROR);

          	
          // We act all idempotent like its all good. 'create', when it already exists, is ignored. 
          drupal_add_http_header('Content-Type', 'text/xml');
          print('<response>');
          print('<consumer op="' . $op . '" id="' . $id . '" origin="' . $origin . '" >');
          print('<status>success</status>');
          print('</consumer>');
          print('</response>');
          exit(0);          	
          
        }

        // Getting a reference to the entity we asked for
        $entityfields = array();

        //
        // We are only updating what has been passed through.
        // No test for missing values.
        // TODO: Validate incoming values.
        //


        $entityfields['pk_consumer'] = $id;	

        if (isset($notification->place)) {
          $entityfields['fk_place'] = intval($notification->place);
        }

        if (isset($notification->reputation)) {
          $entityfields['fk_reputation'] = intval($notification->reputation);
        }

        if (isset($notification->name)) {
          $entityfields['name'] = (string)($notification->name);
        }

        if (isset($notification->external_id)) {
          $entityfields['external_id'] = (string)($notification->external_id);
        }

        if (!isset($notification->attributes()->key)) {
          if (twCanDebug()) debug('$entities');
          if (twCanDebug()) debug($entities);
        }

        // We are being notified, so we do not want to ship this out again as a notification...
        // Finally write the entity.
        $entity = entity_create('thumbwhere_consumer', $entityfields);
        $entity->do_not_propagate = TRUE;
        $entity->save($transaction);

        //
        // Send the final response
        //
        
        $duration = round(((microtime(TRUE) - $start_time) * 1000.0),3); 

        drupal_add_http_header('Content-Type', 'text/xml');
        print('<response duration="' . $duration . 'ms">');
        print('<consumer op="' . $op . '" id="' . $id . '" origin="' . $origin . '" >');
        print('<status>success</status>');
        print('</consumer>');
        print('</response>');
        exit(0);			
		
      break;
      }
  
  
    case 'update':
      {
      
    		//
    		// We want to time this
    		//
    			
    		$start_time = microtime(TRUE);
      
        //
        // What resource are we updating?
        //
        
      
        //
        // Get the id
        //
		
        if (!isset($notification->attributes()->id)) {
          drupal_add_http_header('Status', '412 Unable to fulfil request.');
          $err = 'We were expecting an \'id\', attribute on the \'consumer\', element when we are performing an \'update\' notification..';
          print($err);
          watchdog('thumbwhere_consumer', $err ,array(), WATCHDOG_ERROR);
          exit(0);
        }

        $id = intval($notification->attributes()->id);
		

        if (twCanDebug()) debug('id');
        if (twCanDebug()) debug($id);

            
        //
        // Get the origin
        //
		
		    $origin = -1;
		    
        if (isset($notification->attributes()->origin)) {                
          $origin = intval($notification->attributes()->origin);

          if (twCanDebug()) debug('origin');
          if (twCanDebug()) debug($origin);

          if ($origin == variable_get('thumbwhere_host_id',-1)) {
            $err = 'Origin of this notification ' . $origin  . ' is the same as this host. \'consumer\', Dropping this \'update\' notification.';
            watchdog('thumbwhere_consumer', $err ,array(), WATCHDOG_ERROR);
              
            // We act all idempotent like its all good. 'create', when it already exists, is ignored. 
            drupal_add_http_header('Content-Type', 'text/xml');
            print('<response>');
            print('<consumer op="' . $op . '" id="' . $id . '" origin="' . $origin . '">');
            print('<status>success</status>');
            print('</consumer>');
            print('</response>');
            exit(0);               
          }
        }
          
        watchdog('thumbwhere_consumer', 'Notification: \'' . $notification_op . '\' consumer #<a href="/admin/thumbwhere/thumbwhere_consumers/thumbwhere_consumer/' . $id .  '/edit"> \'' . $id .  '\'</a>  origin=\'' . $origin . '\'.',array(), WATCHDOG_NOTICE);
       
        //
        // Get the fields
        //
          
        $entities = entity_load('thumbwhere_consumer', array($id));

        if (twCanDebug()) debug('$entities');
        if (twCanDebug()) debug($entities);

        if (count($entities) == 0) {
          //drupal_add_http_header('Status', '409 No such resource exists.');
          $err = 'No such \'consumer\' resource exists with an id of \'' . $id . '\'.';
          //print($err);
          watchdog('thumbwhere_consumer',$err,array(), WATCHDOG_ERROR);
          //exit(0);

////////////////////////////////////////////////////////////////////////
//
// ... for now we will just create the local copy
//
          watchdog('thumbwhere_consumer','creating local version of missing entity',array(), WATCHDOG_ERROR);
		
		
		    //
		    // We want to time this
		    //
			
		    $start_time = microtime(TRUE);
			
        //
        // What resource are we creating?
        //
      
        //
        // Get the id
        //
		
        if (!isset($notification->attributes()->id)) {
          drupal_add_http_header('Status', '412 Unable to fulfil request.');
          $err = 'We were expecting an \'id\', attribute on the \'consumer\', element when we are handling a \'update\' notification..';
          print($err);
          watchdog('thumbwhere_consumer', $err ,array(), WATCHDOG_ERROR);
          exit(0);
        }

        $id = intval($notification->attributes()->id);
		

        if (twCanDebug()) debug('id');
        if (twCanDebug()) debug($id);


        //
        // Get the origin
        //
		
		    $origin = -1;
		    
        if (isset($notification->attributes()->origin)) {        
          $origin = intval($notification->attributes()->origin);

          if (twCanDebug()) debug('origin');
          if (twCanDebug()) debug($origin);

          if ($origin == variable_get('thumbwhere_host_id',-1)) {
            $err = 'Origin of this notification ' . $origin  . ' is the same as this host. \'consumer\', Dropping this \'update\' notification.';
            watchdog('thumbwhere_consumer', $err ,array(), WATCHDOG_ERROR);
              
            // We act all idempotent like its all good. 'create', when it already exists, is ignored. 
            drupal_add_http_header('Content-Type', 'text/xml');
            print('<response>');
            print('<consumer op="' . $op . '" id="' . $id . '" origin="' . $origin . '" >');
            print('<status>success</status>');
            print('</consumer>');
            print('</response>');
            exit(0);               
          }
        }
              
        watchdog('thumbwhere_consumer', 'Notification: \'update\' consumer $<a href="/admin/thumbwhere/thumbwhere_consumers/thumbwhere_consumer/' . $id .  '/edit">\'' . $id . '\'</a>  origin=\'' . $origin . '\'.',array(), WATCHDOG_NOTICE);
      
        //
        // Get the fields
        //
          
        $entities = entity_load('thumbwhere_consumer', array($id));

        if (twCanDebug()) debug('$entities');
        if (twCanDebug()) debug($entities);

        if (count($entities) != 0) {
          //drupal_add_http_header('Status', '409 Resource already exists.');
          $err = 'Error while servicing \'update\' notification. Resource \'consumer\' already exists with an id of \'' . $id . '\'.';
           //print($err);
          // When ORIGIN (TW-171) is sorted out then this can be re-anbled
          watchdog('thumbwhere_consumer', $err ,array(), WATCHDOG_ERROR);

          	
          // We act all idempotent like its all good. 'create', when it already exists, is ignored. 
          drupal_add_http_header('Content-Type', 'text/xml');
          print('<response>');
          print('<consumer op="' . $op . '" id="' . $id . '" origin="' . $origin . '" >');
          print('<status>success</status>');
          print('</consumer>');
          print('</response>');
          exit(0);          	
          
        }

        // Getting a reference to the entity we asked for
        $entityfields = array();

        //
        // We are only updating what has been passed through.
        // No test for missing values.
        // TODO: Validate incoming values.
        //


        $entityfields['pk_consumer'] = $id;	

        if (isset($notification->place)) {
          $entityfields['fk_place'] = intval($notification->place);
        }

        if (isset($notification->reputation)) {
          $entityfields['fk_reputation'] = intval($notification->reputation);
        }

        if (isset($notification->name)) {
          $entityfields['name'] = (string)($notification->name);
        }

        if (isset($notification->external_id)) {
          $entityfields['external_id'] = (string)($notification->external_id);
        }

        if (!isset($notification->attributes()->key)) {
          if (twCanDebug()) debug('$entities');
          if (twCanDebug()) debug($entities);
        }

        // We are being notified, so we do not want to ship this out again as a notification...
        // Finally write the entity.
        $entity = entity_create('thumbwhere_consumer', $entityfields);
        $entity->do_not_propagate = TRUE;
        $entity->save($transaction);

        //
        // Send the final response
        //
        
        $duration = round(((microtime(TRUE) - $start_time) * 1000.0),3); 

        drupal_add_http_header('Content-Type', 'text/xml');
        print('<response duration="' . $duration . 'ms">');
        print('<consumer op="' . $op . '" id="' . $id . '" origin="' . $origin . '" >');
        print('<status>success</status>');
        print('</consumer>');
        print('</response>');
        exit(0);			
		
//
//
//
////////////////////////////////////////////////////////////////////////
        }

        // Getting a reference to the entity we asked for
        $entity = $entities[$id];

        //
        // We are only updating what has been passed through.
        // No test for missing values.
        // TODO: Validate incoming values.
        //


        if (isset($notification->place)) {
          $entity->fk_place = intval($notification->place);
        }

        if (isset($notification->reputation)) {
          $entity->fk_reputation = intval($notification->reputation);
        }

        if (isset($notification->name)) {
          $entity->name = (string)($notification->name);
        }

        if (isset($notification->external_id)) {
          $entity->external_id = (string)($notification->external_id);
        }

        if (!isset($notification->attributes()->key)) {
          if (twCanDebug()) debug('$entities');
          if (twCanDebug()) debug($entities);
        }

        // We are being notified, so we do not want to ship this out again as a notification...
        $entity->do_not_propagate = TRUE;

        // Finally write the entity.
        //entity_save('thumbwhere_consumer', $entity);
        $entity->save($transaction);

        //
        // Send the final response
        //
        
        $duration = round(((microtime(TRUE) - $start_time) * 1000.0),3);

        drupal_add_http_header('Content-Type', 'text/xml');
        print('<response  duration="' . $duration . '" >');
        print('<consumer op="' . $op . '" notification="' . $notification_op . '" id="' . $id . '" >');
        print('<status>success</status>');
        print('</consumer>');
        print('</response>');
        exit(0);
      }

    default:
      {
        drupal_add_http_header('Status', '501 Unable to fulfil request. Unhandled operation \'' . $op . '\'.');
        print('We don\'t handle the \'' . $notification_op . '\', notification operation on the \'consumer\', element.');
        exit(0);
      }
  }
}



/**
 * Menu callback: React to a notification for consumer.
 */
function _thumbwhere_consumer_notify() {

  if (twCanDebug()) {
    watchdog('thumbwhere_consumer', 'incoming notification.');
  }

  $numargs = func_num_args();
  $arg_list = func_get_args();

  
	if (twCanDebug()) debug('$numargs  = ' . $numargs);
	
	if (twCanDebug()) debug('$arg_list = ' . $arg_list);
	
	if (twCanDebug()) debug('$_REQUEST');
	if (twCanDebug()) debug($_REQUEST);
	
	if (twCanDebug()) debug('$_GET');
	if (twCanDebug()) debug($_GET);
	
	if (twCanDebug()) debug('$_POST');
	if (twCanDebug()) debug($_POST);
	
	//if (twCanDebug()) debug('$HTTP_RAW_POST_DATA');
	//if (twCanDebug()) debug($HTTP_RAW_POST_DATA);
	
	if (twCanDebug()) debug('$_SERVER');
	if (twCanDebug()) debug($_SERVER);
	
	//if (twCanDebug()) debug('$GLOBALS');
	//if (twCanDebug()) debug($GLOBALS);

  

  if (variable_get('thumbwhere_api_log_debug', 0) == 1) {
    foreach ($arg_list as $arg) {
      if (twCanDebug()) debug('$arg  = ' . $arg);
    }
  }


  //////////////////////////////////////////////////
  //
  //

  switch ($_SERVER['REQUEST_METHOD']) {

    case 'POST': {
        if ($numargs == 1) {
          if ($arg_list[0] == 'consumer') {

            //
            // Get the POST data as XML
            //

            $post_xml = new SimpleXMLElement('php://input', null, true);
            if (twCanDebug()) debug('$post_xml');
            if (twCanDebug()) debug($post_xml);

            if (twCanDebug()) debug('$post_xml->getName()');
            if (twCanDebug()) debug($post_xml->getName());

            // We expect 'action' as the root element. TODO: I would rather this was the more generic 'request'
            if ($post_xml->getName() != 'action') {
              drupal_add_http_header('Status', '404 not found.');
              print('We were expecting \'action\', as the root element, not \'' . $post_xml->getName() . '\'.');
              exit(0);
            }

            /*
             *  SimpleXMLElement::__set_state(array(
             *      'consumer' =>
             *      SimpleXMLElement::__set_state(array(
             *       '@attributes' =>
             *      array (
             *        'key' => '8e9ff318-3cd7-4593-99de-4c37c0fb335a',
             *        'op' => 'create',
             *      ),
             *    )),
             *  ))
             */


            // We expect 'action' as the root element.
            if (!isset($post_xml->consumer)) {
              drupal_add_http_header('Status', '404 not found.');
              print('We were expecting a \'consumer\', element in the action payload.');
              exit(0);
            }

            // Handle the incoming notification for content ingest
            _thumbwhere_handle_consumer_notify_xml($post_xml->consumer);

          }
          else {
            drupal_add_http_header('Status', '404 not found.');
            print('Invalid resource');
            exit(0);
          }
        }
        else {
          drupal_add_http_header('Status', '404 not found.');
          print('Invalid notification request url format.');
          exit(0);
        }
      }
      break;

    case 'GET': {

        if ($numargs == 1) {

          // If we are returning the index.xml file for the webservice.
          if ($arg_list[0] == 'index.xml') {
            // return index.xml
            drupal_add_http_header('Content-Type', 'text/xml');
            readfile(dirname(__FILE__) . '/index.xml');
            exit(0);
          }
          else {
            drupal_add_http_header('Status', '404 not found.');
            print('File not found.');
            exit(0);
          }
        }
        else {
          drupal_add_http_header('Status', '404 not found.');
          print('Invalid notification request url format.');
          exit(0);
        }
      }
      break;

    default: {
        drupal_add_http_header('Status', '404 not found.');
        exit(0);
      }
      break;
  }

  //
  //
  ////////////////////////////////////////////////////////////////////////////////////
 
}





/**
 * Consumer UI controller.
 */
class ThumbWhereConsumerUIController extends EntityDefaultUIController {

  /**
   * Overrides hook_menu() defaults. Main reason for doing this is that
   * parent class hook_menu() is optimized for entity type administration.
   */
  public function hook_menu() {

    $items = array();
    
    // Add a notification page...
    $items['thumbwhere/consumer/notify'] = array(
        'title' => 'Notifications Callback for "Consumer" Entity',
        'page callback' => '_thumbwhere_consumer_notify',
        'access arguments' => array(
            'send thumbwhere consumer notifications'
        ),
        'file' => 'thumbwhere_consumer.admin.inc',
        'file path' => drupal_get_path('module', $this->entityInfo['module'])
    );    
    
    $id_count = count(explode('/', $this->path));
    $wildcard = isset($this->entityInfo['admin ui']['menu wildcard']) ? $this->entityInfo['admin ui']['menu wildcard'] : '%' . $this->entityType;

    $items[$this->path] = array(
      'title' => 'Consumer',
      'description' => 'Add edit and update thumbwhere_consumers.',
      'page callback' => 'system_admin_menu_block_page',
      'access arguments' => array('access administration pages'),
      'file path' => drupal_get_path('module', 'system'),
      'file' => 'system.admin.inc',
    );

    // Change the overview menu type for the list of thumbwhere_consumers.
    $items[$this->path]['type'] = MENU_LOCAL_TASK;

    // Change the add page menu to multiple types of entities
    $items[$this->path . '/add'] = array(
      //'title' => 'Add a Consumer',
      'title' => 'Add',
      'description' => 'Add a new Consumer',
      'page callback' => 'thumbwhere_consumer_form_wrapper',
      'page arguments' => array(thumbwhere_consumer_create(array('type' => 'thumbwhere_consumer'))),
      'access callback' => 'thumbwhere_consumer_access',
      'access arguments' => array('edit', 'edit ' . 'thumbwhere_consumer'),
      'file' => 'thumbwhere_consumer.admin.inc',
      'file path' => drupal_get_path('module', $this->entityInfo['module'])
    );


/*
    // Change the add page menu to multiple types of entities
    $items[$this->path . '/add'] = array(
      //'title' => 'Add a Consumer',
      'title' => 'Add',
	  'description' => 'Add a new Consumer',
      'page callback'  => 'thumbwhere_consumer_add_page',
      'access callback'  => 'thumbwhere_consumer_access',
      'access arguments' => array('edit'),
      'type' => MENU_NORMAL_ITEM,
      'weight' => 20,
      'file' => 'thumbwhere_consumer.admin.inc',
      'file path' => drupal_get_path('module', $this->entityInfo['module'])

    );
*/    
/*
    $items[$this->path . '/add/' . 'thumbwhere_consumer'] = array(
      'title' => 'Add ' . 'ThumbWhereConsumer',
      'page callback' => 'thumbwhere_consumer_form_wrapper',
      'page arguments' => array(thumbwhere_consumer_create(array('type' => 'thumbwhere_consumer'))),
      'access callback' => 'thumbwhere_consumer_access',
      'access arguments' => array('edit', 'edit ' . 'thumbwhere_consumer'),
      'file' => 'thumbwhere_consumer.admin.inc',
      'file path' => drupal_get_path('module', $this->entityInfo['module'])
    );
*/
    // Loading and editing thumbwhere_consumer entities
    $items[$this->path . '/thumbwhere_consumer/' . $wildcard] = array(
      'page callback' => 'thumbwhere_consumer_form_wrapper',
      'page arguments' => array($id_count + 1),
      'access callback' => 'thumbwhere_consumer_access',
      'access arguments' => array('edit', $id_count + 1),
      'weight' => 0,
      'context' => MENU_CONTEXT_PAGE | MENU_CONTEXT_INLINE,
      'file' => 'thumbwhere_consumer.admin.inc',
      'file path' => drupal_get_path('module', $this->entityInfo['module'])
    );
    $items[$this->path . '/thumbwhere_consumer/' . $wildcard . '/edit'] = array(
      'title' => 'Edit',
      'type' => MENU_DEFAULT_LOCAL_TASK,
      'weight' => -10,
      'context' => MENU_CONTEXT_PAGE | MENU_CONTEXT_INLINE,
    );

    $items[$this->path . '/thumbwhere_consumer/' . $wildcard . '/delete'] = array(
      'title' => 'Delete',
      'page callback' => 'thumbwhere_consumer_delete_form_wrapper',
      'page arguments' => array($id_count + 1),
      'access callback' => 'thumbwhere_consumer_access',
      'access arguments' => array('edit', $id_count + 1),
      'type' => MENU_LOCAL_TASK,
      'context' => MENU_CONTEXT_INLINE,
      'weight' => 10,
      'file' => 'thumbwhere_consumer.admin.inc',
      'file path' => drupal_get_path('module', $this->entityInfo['module'])
    );

    // Menu item for viewing thumbwhere_consumers
    $items['thumbwhere_consumer/' . $wildcard] = array(
      //'title' => 'Title',
      'title callback' => 'thumbwhere_consumer_page_title',
      'title arguments' => array(1),
      'page callback' => 'thumbwhere_consumer_page_view',
      'page arguments' => array(1),
      'access callback' => 'thumbwhere_consumer_access',
      'access arguments' => array('view', 1),
      'type' => MENU_CALLBACK,
    );
    return $items;
  }


  /**
   * Create the markup for the add ThumbWhereConsumer Entities page within the class
   * so it can easily be extended/overriden.
   */
  public function addPage() {
    $item = menu_get_item();
    $content = system_admin_menu_block($item);

    if (count($content) == 1) {
      $item = array_shift($content);
      drupal_goto($item['href']);
    }

    return theme('thumbwhere_consumer_add_list', array('content' => $content));
  }

}


/**
 * Form callback wrapper: create or edit a thumbwhere_consumer.
 *
 * @param $thumbwhere_consumer
 *   The thumbwhere_consumer object being edited by this form.
 *
 * @see thumbwhere_consumer_edit_form()
 */
function thumbwhere_consumer_form_wrapper($thumbwhere_consumer) {
  // Add the breadcrumb for the form's location.
  thumbwhere_consumer_set_breadcrumb();
  return drupal_get_form('thumbwhere_consumer_edit_form', $thumbwhere_consumer);
}


/**
 * Form callback wrapper: delete a thumbwhere_consumer.
 *
 * @param $thumbwhere_consumer
 *   The thumbwhere_consumer object being edited by this form.
 *
 * @see thumbwhere_consumer_edit_form()
 */
function thumbwhere_consumer_delete_form_wrapper($thumbwhere_consumer) {
  // Add the breadcrumb for the form's location.
  //thumbwhere_consumer_set_breadcrumb();
  return drupal_get_form('thumbwhere_consumer_delete_form', $thumbwhere_consumer);
}


/**
 * Form callback: create or edit a thumbwhere_consumer.
 *
 * @param $thumbwhere_consumer
 *   The thumbwhere_consumer object to edit or for a create form an empty thumbwhere_consumer object
 *     with only a thumbwhere_consumer type defined.
 */
function thumbwhere_consumer_edit_form($form, &$form_state, $thumbwhere_consumer) {

  // Add the default field elements.
  $form['fk_place'] = array(
    '#type' => 'textfield',
    '#title' => t('ThumbWhereConsumer fk_place'),
    '#default_value' => isset($thumbwhere_consumer->fk_place) ? $thumbwhere_consumer->fk_place : '',
    //'#maxlength' => 255,
   /// '#required' => TRUE,
    '#weight' => -5,
    '#autocomplete_path' => 'entity-autocomplete/thumbwhere_place',
  );
  // Add the default field elements.
  $form['fk_reputation'] = array(
    '#type' => 'textfield',
    '#title' => t('ThumbWhereConsumer fk_reputation'),
    '#default_value' => isset($thumbwhere_consumer->fk_reputation) ? $thumbwhere_consumer->fk_reputation : '',
    //'#maxlength' => 255,
   /// '#required' => TRUE,
    '#weight' => -5,
    '#autocomplete_path' => 'entity-autocomplete/thumbwhere_reputation',
  );
  // Add the default field elements.
    $form['name'] = array(
    '#type' => 'textfield',
    '#title' => t('ThumbWhereConsumer name'),
    '#default_value' => isset($thumbwhere_consumer->name) ? $thumbwhere_consumer->name : '',
    // '#maxlength' => 255,
   // '#required' => TRUE,
    '#weight' => -5,
  );
  // Add the default field elements.
    $form['external_id'] = array(
    '#type' => 'textfield',
    '#title' => t('ThumbWhereConsumer external_id'),
    '#default_value' => isset($thumbwhere_consumer->external_id) ? $thumbwhere_consumer->external_id : '',
    // '#maxlength' => 255,
   // '#required' => TRUE,
    '#weight' => -5,
  );



  $form['data']['#tree'] = TRUE;

  /*
  $form['data']['sample_data'] = array(
    '#type' => 'checkbox',
    '#title' => t('An interesting thumbwhere_consumer switch'),
    '#default_value' => isset($thumbwhere_consumer->data['sample_data']) ? $thumbwhere_consumer->data['sample_data'] : 1,
  );
  */


  // Add the field related form elements.
  $form_state['thumbwhere_consumer'] = $thumbwhere_consumer;
  field_attach_form('thumbwhere_consumer', $thumbwhere_consumer, $form, $form_state);

  $form['actions'] = array(
    '#type' => 'container',
    '#attributes' => array('class' => array('form-actions')),
    '#weight' => 400,
  );

  // We add the form's #submit array to this button along with the actual submit
  // handler to preserve any submit handlers added by a form callback_wrapper.
  $submit = array();

  if (!empty($form['#submit'])) {
    $submit += $form['#submit'];
  }

  $form['actions']['submit'] = array(
    '#type' => 'submit',
    '#value' => t('Save thumbwhere_consumer'),
    '#submit' => $submit + array('thumbwhere_consumer_edit_form_submit'),
  );

  // Do we show the delete button?
  if (!empty($thumbwhere_consumer->pk_consumer)) {
    $form['actions']['delete'] = array(
      '#type' => 'submit',
      '#value' => t('Delete thumbwhere_consumer'),
      '#suffix' => l(t('Cancel'), 'admin/thumbwhere/thumbwhere_consumers'),
      '#submit' => $submit + array('thumbwhere_consumer_form_submit_delete'),
      '#weight' => 45,
    );
  }

  // We append the validate handler to #validate in case a form callback_wrapper
  // is used to add validate handlers earlier.
  $form['#validate'][] = 'thumbwhere_consumer_edit_form_validate';
  return $form;
}


/**
 * Form API validate callback for the thumbwhere_consumer form
 */
function thumbwhere_consumer_edit_form_validate(&$form, &$form_state) {
   
  //if (twCanDebug()) debug($form);   
  //if (twCanDebug()) debug($form_state);


  // No validation for pk


  // Convert fk autocomplete for fk_place
  $value = $form['fk_place']['#value'];
  // If we have something, escape the auto-completion encoding.
  if (!empty($value)) { 
    $value = entity_autocomplete_get_id($value);
    form_set_value($form['fk_place'], $value,$form_state);
  }
  //if (twCanDebug()) debug($form['fk_place']);
   if (twCanDebug()) debug('fk_place = ' . $value);
  // Validate fk fk_place


  // Convert fk autocomplete for fk_reputation
  $value = $form['fk_reputation']['#value'];
  // If we have something, escape the auto-completion encoding.
  if (!empty($value)) { 
    $value = entity_autocomplete_get_id($value);
    form_set_value($form['fk_reputation'], $value,$form_state);
  }
  //if (twCanDebug()) debug($form['fk_reputation']);
   if (twCanDebug()) debug('fk_reputation = ' . $value);
  // Validate fk fk_reputation


  // Validate normalname
  $value = $form['name']['#value'];
  // If we have no default value then we don't care much for checking for emptyness.
  if (empty($value)) {
    form_set_error('name', t('Validation error, \'name\' is mandatory2.'));
    //throw new Exception('Field \'name\' in is mandatory.');
  }

  // Validate normalexternal_id
  $value = $form['external_id']['#value'];
  // If we have no default value then we don't care much for checking for emptyness.


              

  //form_set_value( array('#parents' => array('array_key_parent', 'array_key_to_replace')) , $value, $form_state);

   
   
  $thumbwhere_consumer = $form_state['thumbwhere_consumer'];

  // Notify field widgets to validate their data.
  field_attach_form_validate('thumbwhere_consumer', $thumbwhere_consumer, $form, $form_state);
    
    
  
}


/**
 * Form API submit callback for the thumbwhere_consumer form.
 *
 * @todo remove hard-coded link
 */
function thumbwhere_consumer_edit_form_submit(&$form, &$form_state) {

  $thumbwhere_consumer = entity_ui_controller('thumbwhere_consumer')->entityFormSubmitBuildEntity($form, $form_state);
  // Save the thumbwhere_consumer and go back to the list of thumbwhere_consumers

  // Add in created and changed times.
  if ($thumbwhere_consumer->is_new = isset($thumbwhere_consumer->is_new) ? $thumbwhere_consumer->is_new : 0) {
    $thumbwhere_consumer->created = time();
  }

  $thumbwhere_consumer->changed = time();

  $thumbwhere_consumer->save($transaction);
  $form_state['redirect'] = 'admin/thumbwhere/thumbwhere_consumers';
}

/**
 * Form API submit callback for the delete button.
 *
 * @todo Remove hard-coded path
 */
function thumbwhere_consumer_form_submit_delete(&$form, &$form_state) {
  $form_state['redirect'] = 'admin/thumbwhere/thumbwhere_consumers/thumbwhere_consumer/' . $form_state['thumbwhere_consumer']->pk_consumer . '/delete';
}





/**
 * Form callback: confirmation form for deleting a thumbwhere_consumer.
 *
 * @param $thumbwhere_consumer
 *   The thumbwhere_consumer to delete
 *
 * @see confirm_form()
 */
function thumbwhere_consumer_delete_form($form, &$form_state, $thumbwhere_consumer) {
  $form_state['thumbwhere_consumer'] = $thumbwhere_consumer;

  $form['#submit'][] = 'thumbwhere_consumer_delete_form_submit';

  $form = confirm_form($form,
    t('Are you sure you want to delete thumbwhere_consumer %name?', array('%name' => $thumbwhere_consumer->name)),
    'admin/thumbwhere/thumbwhere_consumers/thumbwhere_consumer',
    '<p>' . t('This action cannot be undone.') . '</p>',
    t('Delete'),
    t('Cancel'),
    'confirm'
  );

  return $form;
}

/**
 * Submit callback for thumbwhere_consumer_delete_form
 */
function thumbwhere_consumer_delete_form_submit($form, &$form_state) {
  $thumbwhere_consumer = $form_state['thumbwhere_consumer'];

  thumbwhere_consumer_delete($thumbwhere_consumer);

  drupal_set_message(t('The thumbwhere_consumer %name has been deleted.', array('%name' => $thumbwhere_consumer->name)));
  watchdog('thumbwhere_consumer', 'Deleted thumbwhere_consumer %name.', array('%name' => $thumbwhere_consumer->name));

  $form_state['redirect'] = 'admin/thumbwhere/thumbwhere_consumers';
}



/**
 * Page to add ThumbWhereConsumer Entities.
 *
 * @todo Pass this through a proper theme function
 */
function thumbwhere_consumer_add_page() {
  $controller = entity_ui_controller('thumbwhere_consumer');
  return $controller->addPage();
}


/**
 * Displays the list of available thumbwhere_consumer types for thumbwhere_consumer creation.
 *
 * @ingroup themeable
 */
function theme_thumbwhere_consumer_add_list($variables) {


  //$content = $variables['content'];
  $output = '';
  /*
  if ($content) {
    $output = '<dl class="thumbwhere_consumer-type-list">';
    foreach ($content as $item) {
      $output .= '<dt>' . l($item['title'], $item['href']) . '</dt>';
      $output .= '<dd>' . filter_xss_admin($item['description']) . '</dd>';
    }
    $output .= '</dl>';
  }
  else {
    if (user_access('administer thumbwhere_consumer types')) {
      $output = '<p>' . t('ThumbWhereConsumer Entities cannot be added because you have not created any thumbwhere_consumer types yet. Go to the <a href="@create-thumbwhere_consumer-type">thumbwhere_consumer type creation page</a> to add a new thumbwhere_consumer type.', array('@create-thumbwhere_consumer-type' => url('admin/structure/thumbwhere_consumer_types/add'))) . '</p>';
    }
    else {
      $output = '<p>' . t('No thumbwhere_consumer types have been created yet for you to use.') . '</p>';
    }
  }
  */
  return $output;
}





/**
 * Sets the breadcrumb for administrative thumbwhere_consumer pages.
 */
function thumbwhere_consumer_set_breadcrumb() {
  $breadcrumb = array(
    l(t('Home'), '<front>'),
    l(t('Administration'), 'admin'),
    l(t('Consumer'), 'admin/content'),
    l(t('Consumer'), 'admin/thumbwhere/thumbwhere_consumers'),
  );

  drupal_set_breadcrumb($breadcrumb);
}



