<?php

// ******************************************************************
// This file was generated by autogen_model_implementation_drupal.xsl
// ******************************************************************

// Include the ThumbWhere SDK
require_once(dirname(__FILE__) . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . 'dependencies' . DIRECTORY_SEPARATOR . 'tw' . DIRECTORY_SEPARATOR . 'sdk.class.php');

/**
 * @file
 * Module for the ThumbWhere MediaItem Entity
 *
 *
 * NOTE: This has been autogenerated.
 * 
 */

/**
 * Implement hook_entity_info().
 *
 * We define two entities here - the actual entity that will hold our domain
 * specific information and an entity that holds information about the different
 * types of entities. See here: http://drupal.org/node/977380 for a discussion on this
 * choice.
 */
function thumbwhere_mediaitem_entity_info() {
  $return['thumbwhere_mediaitem'] = array(

    // The human-readable name of the type.
    'label' => t('ThumbWhereMediaItem'),

    // (used by DrupalDefaultEntityController) The name of the entity type's base table.
    'base table' => 'thumbwhere_mediaitem',

    // fieldable: Set to TRUE if you want your entity type to accept fields being attached to it.
    'fieldable' => TRUE,

  // Declare the endty keys..
    'entity keys' => array(
      'id' => 'pk_mediaitem',
      'label' => 'title',
    ),

    // A function taking an entity and an entity type as arguments and returning the label of the entity.
    // The entity label is the main string associated with an entity; for example, the title of a node or
    // the subject of a comment. If there is an entity object property that defines the label, use the
    // 'label' element of the 'entity keys' return value component to provide this information (see below).
    // If more complex logic is needed to determine the label of an entity, you can instead specify a callback
    // function here, which will be called to determine the entity label. See also the entity_label()
    // function, which implements this logic.
    'label callback' => 'entity_class_label',

    // A function taking an entity as argument and returning the uri elements of the entity, e.g. 'path' and 'options'.
    // The actual entity uri can be constructed by passing these elements to url().
    'uri callback' => 'entity_class_uri',

    //
    // Entity API Components
    //

    // The module whwre we can find our classes
    'module' => 'thumbwhere_mediaitem',

    // The entity class and controller class extend the classes provided by the Entity API
    'entity class' => 'ThumbWhereMediaItem',

    // Which Entity API contoller
    'controller class' => 'ThumbWhereMediaItemController',

    // The information below is used by the ThumbWhereMediaItemUIController (which extends the EntityDefaultUIController)
    'admin ui' => array(
      'path' => 'admin/thumbwhere/thumbwhere_mediaitems',
      'file' => 'thumbwhere_mediaitem.admin.inc',
      'controller class' => 'ThumbWhereMediaItemUIController',
      'menu wildcard' => '%thumbwhere_mediaitem',
    ),
  );

  return $return;
}


/**
 * Implements hook_permission().
 */
function thumbwhere_mediaitem_permission() {

  // We set up permisssions to manage entity types, manage all entities and the
  // permissions for each individual entity
  $permissions = array(
    'administer thumbwhere_mediaitems' => array(
      'title' => t('Administer thumbwhere_mediaitems'),
      'description' => t('Edit and delete all thumbwhere_mediaitems'),
    ),
  'send thumbwhere mediaitem notifications' => array(
          'title' => t('Send ThumbWhere Notifications'),
          'description' => t('Allow the caller to call the ThumbWhere MediaItem Entity Notifications Endpoint.'),
          'restrict access' => TRUE,
      ),    
  );

  return $permissions;
}


/**
 * Determines whether the given user has access to a thumbwhere_mediaitem.
 *
 * @param $op
 *   The operation being performed. One of 'view', 'update', 'create', 'delete'
 *   or just 'edit' (being the same as 'create' or 'update').
 * @param $thumbwhere_mediaitem
 *   Optionally a thumbwhere_mediaitem or a thumbwhere_mediaitem type to check access for. If nothing is
 *   given, access for all thumbwhere_mediaitems is determined.
 * @param $account
 *   The user to check for. Leave it to NULL to check for the global user.
 * @return boolean
 *   Whether access is allowed or not.
 */
function thumbwhere_mediaitem_access($op, $thumbwhere_mediaitem = NULL, $account = NULL) {

  //if (user_access('administer thumbwhere_mediaitems', $account)) {
  //  return TRUE;
 // }

  //return FALSE;
  return TRUE;
}




/**
 * Fetch a thumbwhere_mediaitem object. Make sure that the wildcard you choose
 * in the thumbwhere_mediaitem entity definition fits the function name here.
 *
 * @param $thumbwhere_mediaitem_id
 *   Integer specifying the thumbwhere_mediaitem id.
 * @param $reset
 *   A boolean indicating that the internal cache should be reset.
 * @return
 *   A fully-loaded $thumbwhere_mediaitem object or FALSE if it cannot be loaded.
 *
 * @see thumbwhere_mediaitem_load_multiple()
 */
function thumbwhere_mediaitem_load($thumbwhere_mediaitem_id, $reset = FALSE) {

  $thumbwhere_mediaitems = entity_load('thumbwhere_mediaitem', array($thumbwhere_mediaitem_id), array(), $reset);

  return reset($thumbwhere_mediaitems);
}


/**
 * Deletes a thumbwhere_mediaitem.
 */
function thumbwhere_mediaitem_delete(ThumbWhereMediaItem $thumbwhere_mediaitem) {

  $thumbwhere_mediaitem->delete();
}

/**
 * Create a thumbwhere_mediaitem object.
 */
function thumbwhere_mediaitem_create($values = array()) {

  return entity_get_controller('thumbwhere_mediaitem')->create($values);
}


/**
 * URI callback for thumbwhere_mediaitems
 */
function thumbwhere_mediaitem_uri(ThumbWhereMediaItem $thumbwhere_mediaitem) {

  return array(
    'path' => 'thumbwhere_mediaitem/' . $thumbwhere_mediaitem->pk_mediaitem,
  );
}


/**
 * Menu title callback for showing individual entities
 */
function thumbwhere_mediaitem_page_title(ThumbWhereMediaItem $thumbwhere_mediaitem) {

	return $thumbwhere_mediaitem->title;
}


/**
 * Sets up content to show an individual thumbwhere_mediaitem
 * @todo - get rid of drupal_set_title();
 */
function thumbwhere_mediaitem_page_view($thumbwhere_mediaitem, $view_mode = 'full') {



  $controller = entity_get_controller('thumbwhere_mediaitem');
  $content = $controller->view(array($thumbwhere_mediaitem->pk_mediaitem => $thumbwhere_mediaitem));
  drupal_set_title($thumbwhere_mediaitem->title);
  return $content;
}


/**
 * Implements hook_views_api().
 */
function thumbwhere_mediaitem_views_api() {



  return array(
    'api' => 3,
    'path' => drupal_get_path('module', 'thumbwhere_mediaitem') . '/views',
  );
}


/**
 * Implement hook_theme().
 */
function thumbwhere_mediaitem_theme() {



  return array(
    'thumbwhere_mediaitem_add_list' => array(
      'variables' => array('content' => array()),
      'file' => 'thumbwhere_mediaitem.admin.inc',
    ),
    'thumbwhere_mediaitem' => array(
      'render element' => 'elements',
      'template' => 'thumbwhere_mediaitem',
    )
  );
}


/**
 * Implements hook_menu_local_tasks_alter().
 */
function thumbwhere_mediaitem_menu_local_tasks_alter(&$data, $router_item, $root_path) {

  // Add action link 'admin/structure/thumbwhere_mediaitems/add' on 'admin/structure/thumbwhere_mediaitems'.
  if ($root_path == 'admin/thumbwhere/thumbwhere_mediaitems') {
    $item = menu_get_item('admin/thumbwhere/thumbwhere_mediaitems/add');
    if ($item['access']) {
      $data['actions']['output'][] = array(
        '#theme' => 'menu_local_action',
        '#link' => $item,
      );
    }
  }
}


/**
 * The class used for thumbwhere_mediaitem entities
 */
class ThumbWhereMediaItem extends Entity {

  public function __construct($values = array()) {
    parent::__construct($values, 'thumbwhere_mediaitem');
  }

  protected function defaultLabel() {
    return $this->title;
  }

  protected function defaultUri() {
    return array('path' => 'thumbwhere_mediaitem/' . $this->pk_mediaitem);
  }


}



/**
 * The Controller for ThumbWhereMediaItem entities
 */
class ThumbWhereMediaItemController extends EntityAPIController {
  public function __construct($entityType) {
    parent::__construct($entityType);
  }
}



/**
* Implements hook_entity_presave().
*
* Act on an MediaItem entity before it is about to be created or updated.
*
* @param $entity
*   The entity object.
* @param $entity_type
*   The type of entity being saved (i.e. node, user, comment).
*/
function thumbwhere_mediaitem_entity_presave($entity, $entity_type) {

  // We only look after what we want...
  if ($entity_type == 'thumbwhere_mediaitem')  {
  	
 	/////////////////////////////////////////////////////
    //
    // Should we not propagate this to ThumbWhere? 
    //
    if (isset($entity->do_not_propagate)) {
      if ($entity->do_not_propagate) {
        if (twCanDebug()) {
          if ($entity->is_new) {
            if (twCanDebug()){
              debug("DO NOT PROPAGATE 'mediaitem' presave *CREATE* not being sent to ThumbWhere server.");
          	}
          }
          else {
            if (twCanDebug()) {
              debug("DO NOT PROPAGATE 'mediaitem' presave *UPDATE* not being sent to ThumbWhere server.");
            }
          }
        }

        // To cause an infinite loop of updates, comment out this line.
        return;
      }
      else if (twCanDebug()) {
        debug("OK PROAOGATE 'mediaitem' presave to ThumbWhere server.");
      }
    }
    else if (twCanDebug()) {
      debug("DO NOT PROPAGATE IS NOT SET");
    }
    //
    //
    ////////////////////////////////////////////////////////  	

  
    // .. then we create a new thumbwhere_mediaitem entity...
      // Instantiate the ThumbWhereContent class

    $twMediaAPI = new ThumbWhereAPIMedia();

    //if (twCanDebug()) debug($twContentAPI);

    //if (twCanDebug()) debug($entity);

    //
    // Collate and validate our fields
    //

    $fields = array();
    // Compulsory so..
    if (!isset($entity->fk_code)) {
      throw new Exception('code is mandatory.');
    }
    else {
      $fields['code'] = $entity->fk_code;
    }
    // Compulsory so..
    if (!isset($entity->fk_identity)) {
      throw new Exception('identity is mandatory.');
    }
    else {
      $fields['identity'] = $entity->fk_identity;
    }
    // Compulsory so..
    if (!isset($entity->fk_format)) {
      throw new Exception('format is mandatory.');
    }
    else {
      $fields['format'] = $entity->fk_format;
    }
    // Compulsory so..
    if (!isset($entity->fk_server)) {
      throw new Exception('server is mandatory.');
    }
    else {
      $fields['server'] = $entity->fk_server;
    }

    // Nullable so we only pass it through to create_mediaitem if we have it..
    if (isset($entity->fk_license)) {
      $fields['license'] = $entity->fk_license;
    }

    // Nullable so we only pass it through to create_mediaitem if we have it..
    if (isset($entity->fk_content)) {
      $fields['content'] = $entity->fk_content;
    }

    // Nullable so we only pass it through to create_mediaitem if we have it..
    if (isset($entity->fk_target)) {
      $fields['target'] = $entity->fk_target;
    }

    // Nullable so we only pass it through to create_mediaitem if we have it..
    if (!empty($entity->title)) {
      $fields['title'] = $entity->title;
    }

    // Nullable so we only pass it through to create_mediaitem if we have it..
    if (!empty($entity->body)) {
      $fields['body'] = $entity->body;
    }

    // Not nullable but we have a default value
    if (!isset($entity->bodyonly)) {
      $fields['bodyonly'] = 0;
    }
    else {
      $fields['bodyonly'] = $entity->bodyonly;
    }

    // Not nullable but we have a default value
    if (!isset($entity->comment)) {
      $fields['comment'] = 0;
    }
    else {
      $fields['comment'] = $entity->comment;
    }

    // Not nullable but we have a default value
    if (!isset($entity->usergenerated)) {
      $fields['usergenerated'] = 0;
    }
    else {
      $fields['usergenerated'] = $entity->usergenerated;
    }
    // Compulsory so..
    if (empty($entity->digest)) {
      throw new Exception('digest is mandatory.');
    }
    else {
      $fields['digest'] = $entity->digest;
    }

    // Not nullable but we have a default value
    if (!isset($entity->process_attempts)) {
      $fields['process_attempts'] = 0;
    }
    else {
      $fields['process_attempts'] = $entity->process_attempts;
    }

    // Not nullable but we have a default value
    if (!isset($entity->censored)) {
      $fields['censored'] = 0;
    }
    else {
      $fields['censored'] = $entity->censored;
    }

    // Not nullable but we have a default value
    if (!isset($entity->reviewed)) {
      $fields['reviewed'] = 0;
    }
    else {
      $fields['reviewed'] = $entity->reviewed;
    }

    // Not nullable but we have a default value
    if (!isset($entity->privacy)) {
      $fields['privacy'] = 0;
    }
    else {
      $fields['privacy'] = $entity->privacy;
    }

    // Not nullable but we have a default value
    if (!isset($entity->viewcount)) {
      $fields['viewcount'] = 0;
    }
    else {
      $fields['viewcount'] = $entity->viewcount;
    }

    // Not nullable but we have a default value
    if (!isset($entity->ratingdividend)) {
      $fields['ratingdividend'] = 0;
    }
    else {
      $fields['ratingdividend'] = $entity->ratingdividend;
    }

    // Not nullable but we have a default value
    if (!isset($entity->ratingdivisor)) {
      $fields['ratingdivisor'] = 0;
    }
    else {
      $fields['ratingdivisor'] = $entity->ratingdivisor;
    }

    // Not nullable but we have a default value
    if (!isset($entity->ratings)) {
      $fields['ratings'] = 0;
    }
    else {
      $fields['ratings'] = $entity->ratings;
    }
		
	//
	// Ensure that we have the 'thumbwhere_api_key' as we are going to pass that through in a context
	//
    if (variable_get('thumbwhere_api_key') == '') {
    	throw new Exception('ThumbWhere resource for \'MediaItem\' requires that variable \'thumbwhere_api_key\' is set.');
    }
    
              		
    //
    // If this mediaitem entity is new....
    //
               
    if ($entity->is_new) {
                

      //
      // ... then we create the mediaitem entity in ThumbWhere
      //

      $mediaitem = $twMediaAPI->create_mediaitem($context = array(
                  'key' => variable_get('thumbwhere_api_key')
          ), $fields);
            
       if (twCanDebug()) debug($mediaitem);

      //if (twCanDebug()) debug($mediaitem);

      $attributes = $mediaitem->body->mediaitem->attributes();

      if (twCanDebug()) debug('newly created id = ' . $attributes['id']);

      // Assign new id.
      $entity->pk_mediaitem = intval($attributes['id']);

      if (twCanDebug()) debug('$entity->pk_mediaitem = ' . $entity->pk_mediaitem);
    }
    else
    {
          	

      //
      // .. otherwise we update the mediaitem entity in ThumbWhere
      //

      $mediaitem = $twMediaAPI->update_mediaitem($id=$entity->pk_mediaitem,$context = array(
                  'key' => variable_get('thumbwhere_api_key')
          ), $fields);
            
        
    }
          	
    
  }
}

/**
 * Implements thumbwhere_mediaitem_entity_insert().
 *
 * Act on entities when inserted.
 *
 * @param $entity
 *   The entity object
 * @param $type
 *   The type of entity being inserted (i.e. node, user, comment).
 */
function thumbwhere_mediaitem_entity_insert($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_mediaitem') return;

}


/**
* Implements thumbwhere_mediaitem_entity_delete().
*
* Act on entities when inserted.
*
* @param $entity
*   The entity object
* @param $type
*   The type of entity being deleted (i.e. node, user, comment).
*/
function thumbwhere_mediaitem_entity_delete($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_mediaitem') return;


}
