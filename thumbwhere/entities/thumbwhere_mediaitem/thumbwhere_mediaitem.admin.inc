<?php

/**
 * @file
 * ThumbWhereMediaItem editing UI.
 *
 * We make very little use of the EntityAPI interface for this - preferring instead to use
 * views. That offers more flexibility to change a UI that will, more often than not,
 * be end-user facing.
 *
 * NOTE: This file was generated by autogen_model_implementation_drupal.xsl
 */


	

 
/**
 * Handle the notification for content_ingext
 */
function _thumbwhere_handle_mediaitem_notify_xml($notification) {

  //
  // Get the operation
  //

  if (!isset($notification->attributes()->op)) {
    drupal_add_http_header('Status', '501 unable to fulfull request..');
    $err = 'We were expecting an \'op\', attribute on the \'mediaitem\', element.';
    print($err);
   	watchdog('thumbwhere_mediaitem', $err ,array(), WATCHDOG_ERROR);
    exit(0);
  }

  $op = $notification->attributes()->op;

  if (twCanDebug()) debug('op');
  if (twCanDebug()) debug($op);

  //
  // Get the context
  //

  if (!isset($notification->attributes()->key)) {
    drupal_add_http_header('Status', '412 unable to fulfull request..');
    $err = 'We were expecting a \'key\', attribute on the \'mediaitem\', element.';
    print($err);
    watchdog('thumbwhere_mediaitem', $err ,array(), WATCHDOG_ERROR);
    exit(0);
  }

  $context_key =  $notification->attributes()->key;

  //
  // Now based on the operation..
  //

  switch($op)
  {
  
  
      case 'create':
      {
      		
		
		//
		// We want to time this
		//
			
		$start_time = microtime(TRUE);
			
        //
        // What resource are we creating?
        //
      
        //
        // Get the id
        //
		
        if (!isset($notification->attributes()->id)) {
          drupal_add_http_header('Status', '412 Unable to fulfull request.');
          $err = 'We were expecting an \'id\', attribute on the \'mediaitem\', element when we are performing an \'update\' notification..';
          print($err);
          watchdog('thumbwhere_mediaitem', $err ,array(), WATCHDOG_ERROR);
          exit(0);
        }

        $id = intval($notification->attributes()->id);
		

        if (twCanDebug()) debug('id');
        if (twCanDebug()) debug($id);

      
        //
        // Get the fields
        //
          
        $entities = entity_load('thumbwhere_mediaitem', array($id));

        if (twCanDebug()) debug('$entities');
        if (twCanDebug()) debug($entities);

        if (count($entities) != 0) {
          //drupal_add_http_header('Status', '409 Resource already exists.');
          $err = '\'mediaitem\' resource already exists with an id of \'' . $id . '\'.';
           //print($err);
          watchdog('thumbwhere_mediaitem', $err ,array(), WATCHDOG_ERROR);
          //exit(0);

          	
          drupal_add_http_header('Content-Type', 'text/xml');
          print('<response>');
          print('<mediaitem op="' . $op . '" id="' . $id . '" >');
          print('<status>success</status>');
          print('</mediaitem>');
          print('</response>');
          exit(0);          	
          
        }

        // Getting a reference to the entity we asked for
        $entityfields = array();

        //
        // We are only updating what has been passed through.
        // No test for missing values.
        // TODO: Validate incoming values.
        //


        $entityfields['pk_mediaitem'] = $id;	

        if (isset($notification->code)) {
          $entityfields['fk_code'] = intval($notification->code);
        }

        if (isset($notification->identity)) {
          $entityfields['fk_identity'] = intval($notification->identity);
        }

        if (isset($notification->format)) {
          $entityfields['fk_format'] = intval($notification->format);
        }

        if (isset($notification->server)) {
          $entityfields['fk_server'] = intval($notification->server);
        }

        if (isset($notification->license)) {
          $entityfields['fk_license'] = intval($notification->license);
        }

        if (isset($notification->content)) {
          $entityfields['fk_content'] = intval($notification->content);
        }

        if (isset($notification->target)) {
          $entityfields['fk_target'] = intval($notification->target);
        }

        if (isset($notification->title)) {

        }

        if (isset($notification->body)) {

        }

        if (isset($notification->bodyonly)) {
          if ((string)($notification->bodyonly) == 'true') {
            $entityfields['bodyonly'] = '1';
          }
          else {
            $entityfields['bodyonly'] = '0';
          }
        }

        if (isset($notification->comment)) {
          if ((string)($notification->comment) == 'true') {
            $entityfields['comment'] = '1';
          }
          else {
            $entityfields['comment'] = '0';
          }
        }

        if (isset($notification->usergenerated)) {
          if ((string)($notification->usergenerated) == 'true') {
            $entityfields['usergenerated'] = '1';
          }
          else {
            $entityfields['usergenerated'] = '0';
          }
        }

        if (isset($notification->digest)) {
          $entityfields['digest'] = (string)($notification->digest);
        }

        if (isset($notification->process_attempts)) {
          $entityfields['process_attempts'] = (int)intval($notification->process_attempts);
        }

        if (isset($notification->censored)) {
          if ((string)($notification->censored) == 'true') {
            $entityfields['censored'] = '1';
          }
          else {
            $entityfields['censored'] = '0';
          }
        }

        if (isset($notification->reviewed)) {
          if ((string)($notification->reviewed) == 'true') {
            $entityfields['reviewed'] = '1';
          }
          else {
            $entityfields['reviewed'] = '0';
          }
        }

        if (isset($notification->privacy)) {
          $entityfields['privacy'] = (int)intval($notification->privacy);
        }

        if (isset($notification->viewcount)) {
          $entityfields['viewcount'] = (int)intval($notification->viewcount);
        }

        if (isset($notification->ratingdividend)) {
          $entityfields['ratingdividend'] = (int)intval($notification->ratingdividend);
        }

        if (isset($notification->ratingdivisor)) {
          $entityfields['ratingdivisor'] = (int)intval($notification->ratingdivisor);
        }

        if (isset($notification->ratings)) {
          $entityfields['ratings'] = (int)intval($notification->ratings);
        }

        if (!isset($notification->attributes()->key)) {
          if (twCanDebug()) debug('$entities');
          if (twCanDebug()) debug($entities);
        }

        // We are being notified, so we do not want to ship this out again as a notification...
        // Finally write the entity.
        $entity = entity_create('thumbwhere_mediaitem', $entityfields);
        $entity->do_not_propagate = TRUE;
        $entity->save();

        //
        // Send the final response
        //
        
        $duration = round(((microtime(TRUE) - $start_time) * 1000.0),3); 

        drupal_add_http_header('Content-Type', 'text/xml');
        print('<response duration="' . $duration . 'ms">');
        print('<mediaitem op="' . $op . '" id="' . $id . '" >');
        print('<status>success</status>');
        print('</mediaitem>');
        print('</response>');
        exit(0);			
		
      
      }
  
  
    case 'update':
      {
      
		//
		// We want to time this
		//
			
		$start_time = microtime(TRUE);
      
        //
        // What resource are we updating?
        //
        
      
        //
        // Get the id
        //
		
        if (!isset($notification->attributes()->id)) {
          drupal_add_http_header('Status', '412 Unable to fulfull request.');
          $err = 'We were expecting an \'id\', attribute on the \'mediaitem\', element when we are performing an \'update\' notification..';
          print($err);
          watchdog('thumbwhere_mediaitem', $err ,array(), WATCHDOG_ERROR);
          exit(0);
        }

        $id = intval($notification->attributes()->id);
		

        if (twCanDebug()) debug('id');
        if (twCanDebug()) debug($id);

      
        //
        // Get the fields
        //
          
        $entities = entity_load('thumbwhere_mediaitem', array($id));

        if (twCanDebug()) debug('$entities');
        if (twCanDebug()) debug($entities);

        if (count($entities) == 0) {
          //drupal_add_http_header('Status', '409 No such resource exists.');
          $err = 'No such \'mediaitem\' resource exists with an id of \'' . $id . '\'.';
          //print($err);
          watchdog('thumbwhere_mediaitem',$err,array(), WATCHDOG_ERROR);
          //exit(0);

////////////////////////////////////////////////////////////////////////
//
// ... for now we will just create the local copy
//
          watchdog('thumbwhere_mediaitem','creating local version of missing entity',array(), WATCHDOG_ERROR);
		
		
		//
		// We want to time this
		//
			
		$start_time = microtime(TRUE);
			
        //
        // What resource are we creating?
        //
      
        //
        // Get the id
        //
		
        if (!isset($notification->attributes()->id)) {
          drupal_add_http_header('Status', '412 Unable to fulfull request.');
          $err = 'We were expecting an \'id\', attribute on the \'mediaitem\', element when we are performing an \'update\' notification..';
          print($err);
          watchdog('thumbwhere_mediaitem', $err ,array(), WATCHDOG_ERROR);
          exit(0);
        }

        $id = intval($notification->attributes()->id);
		

        if (twCanDebug()) debug('id');
        if (twCanDebug()) debug($id);

      
        //
        // Get the fields
        //
          
        $entities = entity_load('thumbwhere_mediaitem', array($id));

        if (twCanDebug()) debug('$entities');
        if (twCanDebug()) debug($entities);

        if (count($entities) != 0) {
          //drupal_add_http_header('Status', '409 Resource already exists.');
          $err = '\'mediaitem\' resource already exists with an id of \'' . $id . '\'.';
           //print($err);
          watchdog('thumbwhere_mediaitem', $err ,array(), WATCHDOG_ERROR);
          //exit(0);

          	
          drupal_add_http_header('Content-Type', 'text/xml');
          print('<response>');
          print('<mediaitem op="' . $op . '" id="' . $id . '" >');
          print('<status>success</status>');
          print('</mediaitem>');
          print('</response>');
          exit(0);          	
          
        }

        // Getting a reference to the entity we asked for
        $entityfields = array();

        //
        // We are only updating what has been passed through.
        // No test for missing values.
        // TODO: Validate incoming values.
        //


        $entityfields['pk_mediaitem'] = $id;	

        if (isset($notification->code)) {
          $entityfields['fk_code'] = intval($notification->code);
        }

        if (isset($notification->identity)) {
          $entityfields['fk_identity'] = intval($notification->identity);
        }

        if (isset($notification->format)) {
          $entityfields['fk_format'] = intval($notification->format);
        }

        if (isset($notification->server)) {
          $entityfields['fk_server'] = intval($notification->server);
        }

        if (isset($notification->license)) {
          $entityfields['fk_license'] = intval($notification->license);
        }

        if (isset($notification->content)) {
          $entityfields['fk_content'] = intval($notification->content);
        }

        if (isset($notification->target)) {
          $entityfields['fk_target'] = intval($notification->target);
        }

        if (isset($notification->title)) {

        }

        if (isset($notification->body)) {

        }

        if (isset($notification->bodyonly)) {
          if ((string)($notification->bodyonly) == 'true') {
            $entityfields['bodyonly'] = '1';
          }
          else {
            $entityfields['bodyonly'] = '0';
          }
        }

        if (isset($notification->comment)) {
          if ((string)($notification->comment) == 'true') {
            $entityfields['comment'] = '1';
          }
          else {
            $entityfields['comment'] = '0';
          }
        }

        if (isset($notification->usergenerated)) {
          if ((string)($notification->usergenerated) == 'true') {
            $entityfields['usergenerated'] = '1';
          }
          else {
            $entityfields['usergenerated'] = '0';
          }
        }

        if (isset($notification->digest)) {
          $entityfields['digest'] = (string)($notification->digest);
        }

        if (isset($notification->process_attempts)) {
          $entityfields['process_attempts'] = (int)intval($notification->process_attempts);
        }

        if (isset($notification->censored)) {
          if ((string)($notification->censored) == 'true') {
            $entityfields['censored'] = '1';
          }
          else {
            $entityfields['censored'] = '0';
          }
        }

        if (isset($notification->reviewed)) {
          if ((string)($notification->reviewed) == 'true') {
            $entityfields['reviewed'] = '1';
          }
          else {
            $entityfields['reviewed'] = '0';
          }
        }

        if (isset($notification->privacy)) {
          $entityfields['privacy'] = (int)intval($notification->privacy);
        }

        if (isset($notification->viewcount)) {
          $entityfields['viewcount'] = (int)intval($notification->viewcount);
        }

        if (isset($notification->ratingdividend)) {
          $entityfields['ratingdividend'] = (int)intval($notification->ratingdividend);
        }

        if (isset($notification->ratingdivisor)) {
          $entityfields['ratingdivisor'] = (int)intval($notification->ratingdivisor);
        }

        if (isset($notification->ratings)) {
          $entityfields['ratings'] = (int)intval($notification->ratings);
        }

        if (!isset($notification->attributes()->key)) {
          if (twCanDebug()) debug('$entities');
          if (twCanDebug()) debug($entities);
        }

        // We are being notified, so we do not want to ship this out again as a notification...
        // Finally write the entity.
        $entity = entity_create('thumbwhere_mediaitem', $entityfields);
        $entity->do_not_propagate = TRUE;
        $entity->save();

        //
        // Send the final response
        //
        
        $duration = round(((microtime(TRUE) - $start_time) * 1000.0),3); 

        drupal_add_http_header('Content-Type', 'text/xml');
        print('<response duration="' . $duration . 'ms">');
        print('<mediaitem op="' . $op . '" id="' . $id . '" >');
        print('<status>success</status>');
        print('</mediaitem>');
        print('</response>');
        exit(0);			
		
//
//
//
////////////////////////////////////////////////////////////////////////
        }

        // Getting a reference to the entity we asked for
        $entity = $entities[$id];

        //
        // We are only updating what has been passed through.
        // No test for missing values.
        // TODO: Validate incoming values.
        //


        if (isset($notification->code)) {
          $entity->fk_code = intval($notification->code);
        }

        if (isset($notification->identity)) {
          $entity->fk_identity = intval($notification->identity);
        }

        if (isset($notification->format)) {
          $entity->fk_format = intval($notification->format);
        }

        if (isset($notification->server)) {
          $entity->fk_server = intval($notification->server);
        }

        if (isset($notification->license)) {
          $entity->fk_license = intval($notification->license);
        }

        if (isset($notification->content)) {
          $entity->fk_content = intval($notification->content);
        }

        if (isset($notification->target)) {
          $entity->fk_target = intval($notification->target);
        }

        if (isset($notification->title)) {

        }

        if (isset($notification->body)) {

        }

        if (isset($notification->bodyonly)) {
          if ((string)($notification->bodyonly) == 'true') {
            $entity->bodyonly = '1';
          }
          else {
            $entity->bodyonly = '0';
          }
        }

        if (isset($notification->comment)) {
          if ((string)($notification->comment) == 'true') {
            $entity->comment = '1';
          }
          else {
            $entity->comment = '0';
          }
        }

        if (isset($notification->usergenerated)) {
          if ((string)($notification->usergenerated) == 'true') {
            $entity->usergenerated = '1';
          }
          else {
            $entity->usergenerated = '0';
          }
        }

        if (isset($notification->digest)) {
          $entity->digest = (string)($notification->digest);
        }

        if (isset($notification->process_attempts)) {
          $entity->process_attempts = (int)intval($notification->process_attempts);
        }

        if (isset($notification->censored)) {
          if ((string)($notification->censored) == 'true') {
            $entity->censored = '1';
          }
          else {
            $entity->censored = '0';
          }
        }

        if (isset($notification->reviewed)) {
          if ((string)($notification->reviewed) == 'true') {
            $entity->reviewed = '1';
          }
          else {
            $entity->reviewed = '0';
          }
        }

        if (isset($notification->privacy)) {
          $entity->privacy = (int)intval($notification->privacy);
        }

        if (isset($notification->viewcount)) {
          $entity->viewcount = (int)intval($notification->viewcount);
        }

        if (isset($notification->ratingdividend)) {
          $entity->ratingdividend = (int)intval($notification->ratingdividend);
        }

        if (isset($notification->ratingdivisor)) {
          $entity->ratingdivisor = (int)intval($notification->ratingdivisor);
        }

        if (isset($notification->ratings)) {
          $entity->ratings = (int)intval($notification->ratings);
        }

        if (!isset($notification->attributes()->key)) {
          if (twCanDebug()) debug('$entities');
          if (twCanDebug()) debug($entities);
        }

        // We are being notified, so we do not want to ship this out again as a notification...
        $entity->do_not_propagate = TRUE;

        // Finally write the entity.
        entity_save('thumbwhere_mediaitem', $entity);

        //
        // Send the final response
        //
        
        $duration = round(((microtime(TRUE) - $start_time) * 1000.0),3);

        drupal_add_http_header('Content-Type', 'text/xml');
        print('<response  duration="' . $duration . '" >');
        print('<mediaitem op="' . $op . '" id="' . $id . '" >');
        print('<status>success</status>');
        print('</mediaitem>');
        print('</response>');
        exit(0);
      }
      
      

    default:
      {
        drupal_add_http_header('Status', '501 Unable to fulfull request. Unhandled operation \'' . $op . '\'.');
        print('We don\'t handle the \'' . $op . '\', notification operation on the \'mediaitem\', element.');
        exit(0);

      }
  }
}



/**
 * Menu callback: React to a notifications for ContentIngest.
 */
function _thumbwhere_mediaitem_notify() {

  if (twCanDebug()) {
    watchdog('thumbwhere_mediaitem', 'incoming notification.');
  }

  $numargs = func_num_args();
  $arg_list = func_get_args();

  
	if (twCanDebug()) debug('$numargs  = ' . $numargs);
	
	if (twCanDebug()) debug('$arg_list = ' . $arg_list);
	
	if (twCanDebug()) debug('$_REQUEST');
	if (twCanDebug()) debug($_REQUEST);
	
	if (twCanDebug()) debug('$_GET');
	if (twCanDebug()) debug($_GET);
	
	if (twCanDebug()) debug('$_POST');
	if (twCanDebug()) debug($_POST);
	
	//if (twCanDebug()) debug('$HTTP_RAW_POST_DATA');
	//if (twCanDebug()) debug($HTTP_RAW_POST_DATA);
	
	if (twCanDebug()) debug('$_SERVER');
	if (twCanDebug()) debug($_SERVER);
	
	//if (twCanDebug()) debug('$GLOBALS');
	//if (twCanDebug()) debug($GLOBALS);

  

  if (variable_get('thumbwhere_api_log_debug', 0) == 1) {
    foreach ($arg_list as $arg) {
      if (twCanDebug()) debug('$arg  = ' . $arg);
    }
  }


  //////////////////////////////////////////////////
  //
  //

  switch ($_SERVER['REQUEST_METHOD']) {

    case 'POST': {
        if ($numargs == 1) {
          if ($arg_list[0] == 'mediaitem') {

            //
            // Get the POST data as XML
            //

            $post_xml = new SimpleXMLElement('php://input', null, true);
            if (twCanDebug()) debug('$post_xml');
            if (twCanDebug()) debug($post_xml);

            if (twCanDebug()) debug('$post_xml->getName()');
            if (twCanDebug()) debug($post_xml->getName());

            // We expect 'action' as the root element. TODO: I would rather this was the more generic 'request'
            if ($post_xml->getName() != 'action') {
              drupal_add_http_header('Status', '404 not found.');
              print('We were expecting \'action\', as the root element, not \'' . $post_xml->getName() . '\'.');
              exit(0);
            }

            /*
             *  SimpleXMLElement::__set_state(array(
             *      'mediaitem' =>
             *      SimpleXMLElement::__set_state(array(
             *       '@attributes' =>
             *      array (
             *        'key' => '8e9ff318-3cd7-4593-99de-4c37c0fb335a',
             *        'op' => 'create',
             *      ),
             *    )),
             *  ))
             */


            // We expect 'action' as the root element.
            if (!isset($post_xml->mediaitem)) {
              drupal_add_http_header('Status', '404 not found.');
              print('We were expecting a \'mediaitem\', element in the action payload.');
              exit(0);
            }

            // Handle the incoming notification for content ingest
            _thumbwhere_handle_mediaitem_notify_xml($post_xml->mediaitem);

          }
          else {
            drupal_add_http_header('Status', '404 not found.');
            print('Invalid resource');
            exit(0);
          }
        }
        else {
          drupal_add_http_header('Status', '404 not found.');
          print('Invalid notification request url format.');
          exit(0);
        }
      }
      break;

    case 'GET': {

        if ($numargs == 1) {

          // If we are returning the index.xml file for the webservice.
          if ($arg_list[0] == 'index.xml') {
            // return index.xml
            drupal_add_http_header('Content-Type', 'text/xml');
            readfile(dirname(__FILE__) . '/index.xml');
            exit(0);
          }
          else {
            drupal_add_http_header('Status', '404 not found.');
            print('File not found.');
            exit(0);
          }
        }
        else {
          drupal_add_http_header('Status', '404 not found.');
          print('Invalid notification request url format.');
          exit(0);
        }
      }
      break;

    default: {
        drupal_add_http_header('Status', '404 not found.');
        exit(0);
      }
      break;
  }

  //
  //
  ////////////////////////////////////////////////////////////////////////////////////
 
}





/**
 * MediaItem UI controller.
 */
class ThumbWhereMediaItemUIController extends EntityDefaultUIController {

  /**
   * Overrides hook_menu() defaults. Main reason for doing this is that
   * parent class hook_menu() is optimized for entity type administration.
   */
  public function hook_menu() {

    $items = array();
    
    // Add a notification page...
    $items['thumbwhere/mediaitem/notify'] = array(
        'title' => 'Notifications Callback for "MediaItem" Entity',
        'page callback' => '_thumbwhere_mediaitem_notify',
        'access arguments' => array(
            'send thumbwhere mediaitem notifications'
        ),
        'file' => 'thumbwhere_mediaitem.admin.inc',
        'file path' => drupal_get_path('module', $this->entityInfo['module'])
    );    
    
    $id_count = count(explode('/', $this->path));
    $wildcard = isset($this->entityInfo['admin ui']['menu wildcard']) ? $this->entityInfo['admin ui']['menu wildcard'] : '%' . $this->entityType;

    $items[$this->path] = array(
      'title' => 'MediaItem',
      'description' => 'Add edit and update thumbwhere_mediaitems.',
      'page callback' => 'system_admin_menu_block_page',
      'access arguments' => array('access administration pages'),
      'file path' => drupal_get_path('module', 'system'),
      'file' => 'system.admin.inc',
    );

    // Change the overview menu type for the list of thumbwhere_mediaitems.
    $items[$this->path]['type'] = MENU_LOCAL_TASK;

    // Change the add page menu to multiple types of entities
    $items[$this->path . '/add'] = array(
      //'title' => 'Add a MediaItem',
      'title' => 'Add',
      'description' => 'Add a new MediaItem',
      'page callback' => 'thumbwhere_mediaitem_form_wrapper',
      'page arguments' => array(thumbwhere_mediaitem_create(array('type' => 'thumbwhere_mediaitem'))),
      'access callback' => 'thumbwhere_mediaitem_access',
      'access arguments' => array('edit', 'edit ' . 'thumbwhere_mediaitem'),
      'file' => 'thumbwhere_mediaitem.admin.inc',
      'file path' => drupal_get_path('module', $this->entityInfo['module'])
    );


/*
    // Change the add page menu to multiple types of entities
    $items[$this->path . '/add'] = array(
      //'title' => 'Add a MediaItem',
      'title' => 'Add',
	  'description' => 'Add a new MediaItem',
      'page callback'  => 'thumbwhere_mediaitem_add_page',
      'access callback'  => 'thumbwhere_mediaitem_access',
      'access arguments' => array('edit'),
      'type' => MENU_NORMAL_ITEM,
      'weight' => 20,
      'file' => 'thumbwhere_mediaitem.admin.inc',
      'file path' => drupal_get_path('module', $this->entityInfo['module'])

    );
*/    
/*
    $items[$this->path . '/add/' . 'thumbwhere_mediaitem'] = array(
      'title' => 'Add ' . 'ThumbWhereMediaItem',
      'page callback' => 'thumbwhere_mediaitem_form_wrapper',
      'page arguments' => array(thumbwhere_mediaitem_create(array('type' => 'thumbwhere_mediaitem'))),
      'access callback' => 'thumbwhere_mediaitem_access',
      'access arguments' => array('edit', 'edit ' . 'thumbwhere_mediaitem'),
      'file' => 'thumbwhere_mediaitem.admin.inc',
      'file path' => drupal_get_path('module', $this->entityInfo['module'])
    );
*/
    // Loading and editing thumbwhere_mediaitem entities
    $items[$this->path . '/thumbwhere_mediaitem/' . $wildcard] = array(
      'page callback' => 'thumbwhere_mediaitem_form_wrapper',
      'page arguments' => array($id_count + 1),
      'access callback' => 'thumbwhere_mediaitem_access',
      'access arguments' => array('edit', $id_count + 1),
      'weight' => 0,
      'context' => MENU_CONTEXT_PAGE | MENU_CONTEXT_INLINE,
      'file' => 'thumbwhere_mediaitem.admin.inc',
      'file path' => drupal_get_path('module', $this->entityInfo['module'])
    );
    $items[$this->path . '/thumbwhere_mediaitem/' . $wildcard . '/edit'] = array(
      'title' => 'Edit',
      'type' => MENU_DEFAULT_LOCAL_TASK,
      'weight' => -10,
      'context' => MENU_CONTEXT_PAGE | MENU_CONTEXT_INLINE,
    );

    $items[$this->path . '/thumbwhere_mediaitem/' . $wildcard . '/delete'] = array(
      'title' => 'Delete',
      'page callback' => 'thumbwhere_mediaitem_delete_form_wrapper',
      'page arguments' => array($id_count + 1),
      'access callback' => 'thumbwhere_mediaitem_access',
      'access arguments' => array('edit', $id_count + 1),
      'type' => MENU_LOCAL_TASK,
      'context' => MENU_CONTEXT_INLINE,
      'weight' => 10,
      'file' => 'thumbwhere_mediaitem.admin.inc',
      'file path' => drupal_get_path('module', $this->entityInfo['module'])
    );

    // Menu item for viewing thumbwhere_mediaitems
    $items['thumbwhere_mediaitem/' . $wildcard] = array(
      //'title' => 'Title',
      'title callback' => 'thumbwhere_mediaitem_page_title',
      'title arguments' => array(1),
      'page callback' => 'thumbwhere_mediaitem_page_view',
      'page arguments' => array(1),
      'access callback' => 'thumbwhere_mediaitem_access',
      'access arguments' => array('view', 1),
      'type' => MENU_CALLBACK,
    );
    return $items;
  }


  /**
   * Create the markup for the add ThumbWhereMediaItem Entities page within the class
   * so it can easily be extended/overriden.
   */
  public function addPage() {
    $item = menu_get_item();
    $content = system_admin_menu_block($item);

    if (count($content) == 1) {
      $item = array_shift($content);
      drupal_goto($item['href']);
    }

    return theme('thumbwhere_mediaitem_add_list', array('content' => $content));
  }

}


/**
 * Form callback wrapper: create or edit a thumbwhere_mediaitem.
 *
 * @param $thumbwhere_mediaitem
 *   The thumbwhere_mediaitem object being edited by this form.
 *
 * @see thumbwhere_mediaitem_edit_form()
 */
function thumbwhere_mediaitem_form_wrapper($thumbwhere_mediaitem) {
  // Add the breadcrumb for the form's location.
  thumbwhere_mediaitem_set_breadcrumb();
  return drupal_get_form('thumbwhere_mediaitem_edit_form', $thumbwhere_mediaitem);
}


/**
 * Form callback wrapper: delete a thumbwhere_mediaitem.
 *
 * @param $thumbwhere_mediaitem
 *   The thumbwhere_mediaitem object being edited by this form.
 *
 * @see thumbwhere_mediaitem_edit_form()
 */
function thumbwhere_mediaitem_delete_form_wrapper($thumbwhere_mediaitem) {
  // Add the breadcrumb for the form's location.
  //thumbwhere_mediaitem_set_breadcrumb();
  return drupal_get_form('thumbwhere_mediaitem_delete_form', $thumbwhere_mediaitem);
}


/**
 * Form callback: create or edit a thumbwhere_mediaitem.
 *
 * @param $thumbwhere_mediaitem
 *   The thumbwhere_mediaitem object to edit or for a create form an empty thumbwhere_mediaitem object
 *     with only a thumbwhere_mediaitem type defined.
 */
function thumbwhere_mediaitem_edit_form($form, &$form_state, $thumbwhere_mediaitem) {

  // Add the default field elements.
  $form['fk_code'] = array(
    '#type' => 'textfield',
    '#title' => t('ThumbWhereMediaItem fk_code'),
    '#default_value' => isset($thumbwhere_mediaitem->fk_code) ? $thumbwhere_mediaitem->fk_code : '',
    //'#maxlength' => 255,
   /// '#required' => TRUE,
    '#weight' => -5,
    '#autocomplete_path' => 'entity-autocomplete/thumbwhere_code',
  );
  // Add the default field elements.
  $form['fk_identity'] = array(
    '#type' => 'textfield',
    '#title' => t('ThumbWhereMediaItem fk_identity'),
    '#default_value' => isset($thumbwhere_mediaitem->fk_identity) ? $thumbwhere_mediaitem->fk_identity : '',
    //'#maxlength' => 255,
   /// '#required' => TRUE,
    '#weight' => -5,
    '#autocomplete_path' => 'entity-autocomplete/thumbwhere_identity',
  );
  // Add the default field elements.
  $form['fk_format'] = array(
    '#type' => 'textfield',
    '#title' => t('ThumbWhereMediaItem fk_format'),
    '#default_value' => isset($thumbwhere_mediaitem->fk_format) ? $thumbwhere_mediaitem->fk_format : '',
    //'#maxlength' => 255,
   /// '#required' => TRUE,
    '#weight' => -5,
    '#autocomplete_path' => 'entity-autocomplete/thumbwhere_format',
  );
  // Add the default field elements.
  $form['fk_server'] = array(
    '#type' => 'textfield',
    '#title' => t('ThumbWhereMediaItem fk_server'),
    '#default_value' => isset($thumbwhere_mediaitem->fk_server) ? $thumbwhere_mediaitem->fk_server : '',
    //'#maxlength' => 255,
   /// '#required' => TRUE,
    '#weight' => -5,
    '#autocomplete_path' => 'entity-autocomplete/thumbwhere_server',
  );
  // Add the default field elements.
  $form['fk_license'] = array(
    '#type' => 'textfield',
    '#title' => t('ThumbWhereMediaItem fk_license'),
    '#default_value' => isset($thumbwhere_mediaitem->fk_license) ? $thumbwhere_mediaitem->fk_license : '',
    //'#maxlength' => 255,
   /// '#required' => TRUE,
    '#weight' => -5,
    '#autocomplete_path' => 'entity-autocomplete/thumbwhere_license',
  );
  // Add the default field elements.
  $form['fk_content'] = array(
    '#type' => 'textfield',
    '#title' => t('ThumbWhereMediaItem fk_content'),
    '#default_value' => isset($thumbwhere_mediaitem->fk_content) ? $thumbwhere_mediaitem->fk_content : '',
    //'#maxlength' => 255,
   /// '#required' => TRUE,
    '#weight' => -5,
    '#autocomplete_path' => 'entity-autocomplete/thumbwhere_content',
  );
  // Add the default field elements.
  $form['fk_target'] = array(
    '#type' => 'textfield',
    '#title' => t('ThumbWhereMediaItem fk_target'),
    '#default_value' => isset($thumbwhere_mediaitem->fk_target) ? $thumbwhere_mediaitem->fk_target : '',
    //'#maxlength' => 255,
   /// '#required' => TRUE,
    '#weight' => -5,
    '#autocomplete_path' => 'entity-autocomplete/thumbwhere_target',
  );
  // Add the default field elements.
    $form['title'] = array(
    '#type' => 'textarea',
    '#title' => t('ThumbWhereMediaItem title'),

    // '#maxlength' => 255,
   // '#required' => TRUE,
    '#weight' => -5,
  );
  // Add the default field elements.
    $form['body'] = array(
    '#type' => 'textarea',
    '#title' => t('ThumbWhereMediaItem body'),

    // '#maxlength' => 255,
   // '#required' => TRUE,
    '#weight' => -5,
  );
  // Add the default field elements.
    $form['bodyonly'] = array(
    '#type' => 'checkbox',
    '#title' => t('ThumbWhereMediaItem bodyonly'),
    '#default_value' => isset($thumbwhere_mediaitem->bodyonly) ? $thumbwhere_mediaitem->bodyonly : 0,
    // '#maxlength' => 255,
   // '#required' => TRUE,
    '#weight' => -5,
  );
  // Add the default field elements.
    $form['comment'] = array(
    '#type' => 'checkbox',
    '#title' => t('ThumbWhereMediaItem comment'),
    '#default_value' => isset($thumbwhere_mediaitem->comment) ? $thumbwhere_mediaitem->comment : 0,
    // '#maxlength' => 255,
   // '#required' => TRUE,
    '#weight' => -5,
  );
  // Add the default field elements.
    $form['usergenerated'] = array(
    '#type' => 'checkbox',
    '#title' => t('ThumbWhereMediaItem usergenerated'),
    '#default_value' => isset($thumbwhere_mediaitem->usergenerated) ? $thumbwhere_mediaitem->usergenerated : 0,
    // '#maxlength' => 255,
   // '#required' => TRUE,
    '#weight' => -5,
  );
  // Add the default field elements.
    $form['digest'] = array(
    '#type' => 'textfield',
    '#title' => t('ThumbWhereMediaItem digest'),

    // '#maxlength' => 255,
   // '#required' => TRUE,
    '#weight' => -5,
  );
  // Add the default field elements.
    $form['process_attempts'] = array(
    '#type' => 'textfield',
    '#title' => t('ThumbWhereMediaItem process_attempts'),
    '#default_value' => isset($thumbwhere_mediaitem->process_attempts) ? $thumbwhere_mediaitem->process_attempts : 0,
    // '#maxlength' => 255,
   // '#required' => TRUE,
    '#weight' => -5,
  );
  // Add the default field elements.
    $form['censored'] = array(
    '#type' => 'checkbox',
    '#title' => t('ThumbWhereMediaItem censored'),
    '#default_value' => isset($thumbwhere_mediaitem->censored) ? $thumbwhere_mediaitem->censored : 0,
    // '#maxlength' => 255,
   // '#required' => TRUE,
    '#weight' => -5,
  );
  // Add the default field elements.
    $form['reviewed'] = array(
    '#type' => 'checkbox',
    '#title' => t('ThumbWhereMediaItem reviewed'),
    '#default_value' => isset($thumbwhere_mediaitem->reviewed) ? $thumbwhere_mediaitem->reviewed : 0,
    // '#maxlength' => 255,
   // '#required' => TRUE,
    '#weight' => -5,
  );
  // Add the default field elements.
    $form['privacy'] = array(
    '#type' => 'textfield',
    '#title' => t('ThumbWhereMediaItem privacy'),
    '#default_value' => isset($thumbwhere_mediaitem->privacy) ? $thumbwhere_mediaitem->privacy : 0,
    // '#maxlength' => 255,
   // '#required' => TRUE,
    '#weight' => -5,
  );
  // Add the default field elements.
    $form['viewcount'] = array(
    '#type' => 'textfield',
    '#title' => t('ThumbWhereMediaItem viewcount'),
    '#default_value' => isset($thumbwhere_mediaitem->viewcount) ? $thumbwhere_mediaitem->viewcount : 0,
    // '#maxlength' => 255,
   // '#required' => TRUE,
    '#weight' => -5,
  );
  // Add the default field elements.
    $form['ratingdividend'] = array(
    '#type' => 'textfield',
    '#title' => t('ThumbWhereMediaItem ratingdividend'),
    '#default_value' => isset($thumbwhere_mediaitem->ratingdividend) ? $thumbwhere_mediaitem->ratingdividend : 0,
    // '#maxlength' => 255,
   // '#required' => TRUE,
    '#weight' => -5,
  );
  // Add the default field elements.
    $form['ratingdivisor'] = array(
    '#type' => 'textfield',
    '#title' => t('ThumbWhereMediaItem ratingdivisor'),
    '#default_value' => isset($thumbwhere_mediaitem->ratingdivisor) ? $thumbwhere_mediaitem->ratingdivisor : 0,
    // '#maxlength' => 255,
   // '#required' => TRUE,
    '#weight' => -5,
  );
  // Add the default field elements.
    $form['ratings'] = array(
    '#type' => 'textfield',
    '#title' => t('ThumbWhereMediaItem ratings'),
    '#default_value' => isset($thumbwhere_mediaitem->ratings) ? $thumbwhere_mediaitem->ratings : 0,
    // '#maxlength' => 255,
   // '#required' => TRUE,
    '#weight' => -5,
  );



  $form['data']['#tree'] = TRUE;

  /*
  $form['data']['sample_data'] = array(
    '#type' => 'checkbox',
    '#title' => t('An interesting thumbwhere_mediaitem switch'),
    '#default_value' => isset($thumbwhere_mediaitem->data['sample_data']) ? $thumbwhere_mediaitem->data['sample_data'] : 1,
  );
  */


  // Add the field related form elements.
  $form_state['thumbwhere_mediaitem'] = $thumbwhere_mediaitem;
  field_attach_form('thumbwhere_mediaitem', $thumbwhere_mediaitem, $form, $form_state);

  $form['actions'] = array(
    '#type' => 'container',
    '#attributes' => array('class' => array('form-actions')),
    '#weight' => 400,
  );

  // We add the form's #submit array to this button along with the actual submit
  // handler to preserve any submit handlers added by a form callback_wrapper.
  $submit = array();

  if (!empty($form['#submit'])) {
    $submit += $form['#submit'];
  }

  $form['actions']['submit'] = array(
    '#type' => 'submit',
    '#value' => t('Save thumbwhere_mediaitem'),
    '#submit' => $submit + array('thumbwhere_mediaitem_edit_form_submit'),
  );

  // Do we show the delete button?
  if (!empty($thumbwhere_mediaitem->pk_mediaitem)) {
    $form['actions']['delete'] = array(
      '#type' => 'submit',
      '#value' => t('Delete thumbwhere_mediaitem'),
      '#suffix' => l(t('Cancel'), 'admin/thumbwhere/thumbwhere_mediaitems'),
      '#submit' => $submit + array('thumbwhere_mediaitem_form_submit_delete'),
      '#weight' => 45,
    );
  }

  // We append the validate handler to #validate in case a form callback_wrapper
  // is used to add validate handlers earlier.
  $form['#validate'][] = 'thumbwhere_mediaitem_edit_form_validate';
  return $form;
}


/**
 * Form API validate callback for the thumbwhere_mediaitem form
 */
function thumbwhere_mediaitem_edit_form_validate(&$form, &$form_state) {
   
  //if (twCanDebug()) debug($form);   
  //if (twCanDebug()) debug($form_state);


  // No validation for pk


  // Convert fk autocomplete for fk_code
  $value = $form['fk_code']['#value'];
  // If we have something, escape the auto-completion encoding.
  if (!empty($value)) { 
    $value = entity_autocomplete_get_id($value);
    form_set_value($form['fk_code'], $value,$form_state);
  }
  //if (twCanDebug()) debug($form['fk_code']);
   if (twCanDebug()) debug('fk_code = ' . $value);
  // Validate fk fk_code
  if (empty($value)) {
    form_set_error('fk_code', t('Validation error, \'fk_code\' is mandatory1.'));
    // throw new Exception('Field \'fk_code\' in is mandatory');
  }

  // Convert fk autocomplete for fk_identity
  $value = $form['fk_identity']['#value'];
  // If we have something, escape the auto-completion encoding.
  if (!empty($value)) { 
    $value = entity_autocomplete_get_id($value);
    form_set_value($form['fk_identity'], $value,$form_state);
  }
  //if (twCanDebug()) debug($form['fk_identity']);
   if (twCanDebug()) debug('fk_identity = ' . $value);
  // Validate fk fk_identity
  if (empty($value)) {
    form_set_error('fk_identity', t('Validation error, \'fk_identity\' is mandatory1.'));
    // throw new Exception('Field \'fk_identity\' in is mandatory');
  }

  // Convert fk autocomplete for fk_format
  $value = $form['fk_format']['#value'];
  // If we have something, escape the auto-completion encoding.
  if (!empty($value)) { 
    $value = entity_autocomplete_get_id($value);
    form_set_value($form['fk_format'], $value,$form_state);
  }
  //if (twCanDebug()) debug($form['fk_format']);
   if (twCanDebug()) debug('fk_format = ' . $value);
  // Validate fk fk_format
  if (empty($value)) {
    form_set_error('fk_format', t('Validation error, \'fk_format\' is mandatory1.'));
    // throw new Exception('Field \'fk_format\' in is mandatory');
  }

  // Convert fk autocomplete for fk_server
  $value = $form['fk_server']['#value'];
  // If we have something, escape the auto-completion encoding.
  if (!empty($value)) { 
    $value = entity_autocomplete_get_id($value);
    form_set_value($form['fk_server'], $value,$form_state);
  }
  //if (twCanDebug()) debug($form['fk_server']);
   if (twCanDebug()) debug('fk_server = ' . $value);
  // Validate fk fk_server
  if (empty($value)) {
    form_set_error('fk_server', t('Validation error, \'fk_server\' is mandatory1.'));
    // throw new Exception('Field \'fk_server\' in is mandatory');
  }

  // Convert fk autocomplete for fk_license
  $value = $form['fk_license']['#value'];
  // If we have something, escape the auto-completion encoding.
  if (!empty($value)) { 
    $value = entity_autocomplete_get_id($value);
    form_set_value($form['fk_license'], $value,$form_state);
  }
  //if (twCanDebug()) debug($form['fk_license']);
   if (twCanDebug()) debug('fk_license = ' . $value);
  // Validate fk fk_license


  // Convert fk autocomplete for fk_content
  $value = $form['fk_content']['#value'];
  // If we have something, escape the auto-completion encoding.
  if (!empty($value)) { 
    $value = entity_autocomplete_get_id($value);
    form_set_value($form['fk_content'], $value,$form_state);
  }
  //if (twCanDebug()) debug($form['fk_content']);
   if (twCanDebug()) debug('fk_content = ' . $value);
  // Validate fk fk_content


  // Convert fk autocomplete for fk_target
  $value = $form['fk_target']['#value'];
  // If we have something, escape the auto-completion encoding.
  if (!empty($value)) { 
    $value = entity_autocomplete_get_id($value);
    form_set_value($form['fk_target'], $value,$form_state);
  }
  //if (twCanDebug()) debug($form['fk_target']);
   if (twCanDebug()) debug('fk_target = ' . $value);
  // Validate fk fk_target


  // Validate normaltitle
  $value = $form['title']['#value'];
  // If we have no default value then we don't care much for checking for emptyness.


  // Validate normalbody
  $value = $form['body']['#value'];
  // If we have no default value then we don't care much for checking for emptyness.


  // Validate normalbodyonly
  $value = $form['bodyonly']['#value'];
  // If we have no default value then we don't care much for checking for emptyness.


  // Validate normalcomment
  $value = $form['comment']['#value'];
  // If we have no default value then we don't care much for checking for emptyness.


  // Validate normalusergenerated
  $value = $form['usergenerated']['#value'];
  // If we have no default value then we don't care much for checking for emptyness.


  // Validate normaldigest
  $value = $form['digest']['#value'];
  // If we have no default value then we don't care much for checking for emptyness.
  if (empty($value)) {
    form_set_error('digest', t('Validation error, \'digest\' is mandatory2.'));
    //throw new Exception('Field \'digest\' in is mandatory.');
  }

  // Validate normalprocess_attempts
  $value = $form['process_attempts']['#value'];
  // If we have no default value then we don't care much for checking for emptyness.


  // Validate normalcensored
  $value = $form['censored']['#value'];
  // If we have no default value then we don't care much for checking for emptyness.


  // Validate normalreviewed
  $value = $form['reviewed']['#value'];
  // If we have no default value then we don't care much for checking for emptyness.


  // Validate normalprivacy
  $value = $form['privacy']['#value'];
  // If we have no default value then we don't care much for checking for emptyness.


  // Validate normalviewcount
  $value = $form['viewcount']['#value'];
  // If we have no default value then we don't care much for checking for emptyness.


  // Validate normalratingdividend
  $value = $form['ratingdividend']['#value'];
  // If we have no default value then we don't care much for checking for emptyness.


  // Validate normalratingdivisor
  $value = $form['ratingdivisor']['#value'];
  // If we have no default value then we don't care much for checking for emptyness.


  // Validate normalratings
  $value = $form['ratings']['#value'];
  // If we have no default value then we don't care much for checking for emptyness.


              

  //form_set_value( array('#parents' => array('array_key_parent', 'array_key_to_replace')) , $value, $form_state);

   
   
  $thumbwhere_mediaitem = $form_state['thumbwhere_mediaitem'];

  // Notify field widgets to validate their data.
  field_attach_form_validate('thumbwhere_mediaitem', $thumbwhere_mediaitem, $form, $form_state);
    
    
  
}


/**
 * Form API submit callback for the thumbwhere_mediaitem form.
 *
 * @todo remove hard-coded link
 */
function thumbwhere_mediaitem_edit_form_submit(&$form, &$form_state) {

  $thumbwhere_mediaitem = entity_ui_controller('thumbwhere_mediaitem')->entityFormSubmitBuildEntity($form, $form_state);
  // Save the thumbwhere_mediaitem and go back to the list of thumbwhere_mediaitems

  // Add in created and changed times.
  if ($thumbwhere_mediaitem->is_new = isset($thumbwhere_mediaitem->is_new) ? $thumbwhere_mediaitem->is_new : 0) {
    $thumbwhere_mediaitem->created = time();
  }

  $thumbwhere_mediaitem->changed = time();

  $thumbwhere_mediaitem->save();
  $form_state['redirect'] = 'admin/thumbwhere/thumbwhere_mediaitems';
}

/**
 * Form API submit callback for the delete button.
 *
 * @todo Remove hard-coded path
 */
function thumbwhere_mediaitem_form_submit_delete(&$form, &$form_state) {
  $form_state['redirect'] = 'admin/thumbwhere/thumbwhere_mediaitems/thumbwhere_mediaitem/' . $form_state['thumbwhere_mediaitem']->pk_mediaitem . '/delete';
}





/**
 * Form callback: confirmation form for deleting a thumbwhere_mediaitem.
 *
 * @param $thumbwhere_mediaitem
 *   The thumbwhere_mediaitem to delete
 *
 * @see confirm_form()
 */
function thumbwhere_mediaitem_delete_form($form, &$form_state, $thumbwhere_mediaitem) {
  $form_state['thumbwhere_mediaitem'] = $thumbwhere_mediaitem;

  $form['#submit'][] = 'thumbwhere_mediaitem_delete_form_submit';

  $form = confirm_form($form,
    t('Are you sure you want to delete thumbwhere_mediaitem %name?', array('%name' => $thumbwhere_mediaitem->title)),
    'admin/thumbwhere/thumbwhere_mediaitems/thumbwhere_mediaitem',
    '<p>' . t('This action cannot be undone.') . '</p>',
    t('Delete'),
    t('Cancel'),
    'confirm'
  );

  return $form;
}

/**
 * Submit callback for thumbwhere_mediaitem_delete_form
 */
function thumbwhere_mediaitem_delete_form_submit($form, &$form_state) {
  $thumbwhere_mediaitem = $form_state['thumbwhere_mediaitem'];

  thumbwhere_mediaitem_delete($thumbwhere_mediaitem);

  drupal_set_message(t('The thumbwhere_mediaitem %name has been deleted.', array('%name' => $thumbwhere_mediaitem->title)));
  watchdog('thumbwhere_mediaitem', 'Deleted thumbwhere_mediaitem %name.', array('%name' => $thumbwhere_mediaitem->title));

  $form_state['redirect'] = 'admin/thumbwhere/thumbwhere_mediaitems';
}



/**
 * Page to add ThumbWhereMediaItem Entities.
 *
 * @todo Pass this through a proper theme function
 */
function thumbwhere_mediaitem_add_page() {
  $controller = entity_ui_controller('thumbwhere_mediaitem');
  return $controller->addPage();
}


/**
 * Displays the list of available thumbwhere_mediaitem types for thumbwhere_mediaitem creation.
 *
 * @ingroup themeable
 */
function theme_thumbwhere_mediaitem_add_list($variables) {


  //$content = $variables['content'];
  $output = '';
  /*
  if ($content) {
    $output = '<dl class="thumbwhere_mediaitem-type-list">';
    foreach ($content as $item) {
      $output .= '<dt>' . l($item['title'], $item['href']) . '</dt>';
      $output .= '<dd>' . filter_xss_admin($item['description']) . '</dd>';
    }
    $output .= '</dl>';
  }
  else {
    if (user_access('administer thumbwhere_mediaitem types')) {
      $output = '<p>' . t('ThumbWhereMediaItem Entities cannot be added because you have not created any thumbwhere_mediaitem types yet. Go to the <a href="@create-thumbwhere_mediaitem-type">thumbwhere_mediaitem type creation page</a> to add a new thumbwhere_mediaitem type.', array('@create-thumbwhere_mediaitem-type' => url('admin/structure/thumbwhere_mediaitem_types/add'))) . '</p>';
    }
    else {
      $output = '<p>' . t('No thumbwhere_mediaitem types have been created yet for you to use.') . '</p>';
    }
  }
  */
  return $output;
}





/**
 * Sets the breadcrumb for administrative thumbwhere_mediaitem pages.
 */
function thumbwhere_mediaitem_set_breadcrumb() {
  $breadcrumb = array(
    l(t('Home'), '<front>'),
    l(t('Administration'), 'admin'),
    l(t('MediaItem'), 'admin/content'),
    l(t('MediaItem'), 'admin/thumbwhere/thumbwhere_mediaitems'),
  );

  drupal_set_breadcrumb($breadcrumb);
}



