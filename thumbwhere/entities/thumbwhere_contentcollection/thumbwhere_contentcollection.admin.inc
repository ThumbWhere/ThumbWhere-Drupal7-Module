<?php

/**
 * @file
 * ThumbWhereContentCollection editing UI.
 *
 * We make very little use of the EntityAPI interface for this - preferring instead to use
 * views. That offers more flexibility to change a UI that will, more often than not,
 * be end-user facing.
 *
 * NOTE: This file was generated by autogen_model_implementation_drupal.xsl
 */




 
/**
 * Handle the notification for content_ingext
 */
function _thumbwhere_handle_content_collection_notify_xml($notification) {

  //
  // Get the operation
  //

  if (!isset($notification->attributes()->op)) {
    drupal_add_http_header('Status', '501 unable to fulfull request..');
    $err = 'We were expecting an \'op\', attribute on the \'content_collection\', element.';
    print($err);
   	watchdog('thumbwhere_contentcollection', $err ,array(), WATCHDOG_ERROR);
    exit(0);
  }

  $op = $notification->attributes()->op;

  if (twCanDebug()) debug('op');
  if (twCanDebug()) debug($op);

  //
  // Get the context
  //

  if (!isset($notification->attributes()->key)) {
    drupal_add_http_header('Status', '501 unable to fulfull request..');
    $err = 'We were expecting a \'key\', attribute on the \'content_collection\', element.';
    print($err);
    watchdog('thumbwhere_contentcollection', $err ,array(), WATCHDOG_ERROR);
    exit(0);
  }

  $context_key =  $notification->attributes()->key;

  //
  // Now based on the operation..
  //

  switch($op)
  {
    case 'update':
      {
        //
        // What resource are we updating?
        //
      
        //
        // Get the id
        //
		
        if (!isset($notification->attributes()->id)) {
          drupal_add_http_header('Status', '501 unable to fulfull request.');
          $err = 'We were expecting an \'id\', attribute on the \'content_collection\', element when we are performing an \'update\' notification..';
          print($err);
          watchdog('thumbwhere_contentcollection', $err ,array(), WATCHDOG_ERROR);
          exit(0);
        }

        $id = intval($notification->attributes()->id);
		

        if (twCanDebug()) debug('id');
        if (twCanDebug()) debug($id);

      
        //
        // Get the fields
        //
          
        $entities = entity_load('thumbwhere_contentcollection', array($id));

        if (twCanDebug()) debug('$entities');
        if (twCanDebug()) debug($entities);

        if (count($entities) == 0) {
          drupal_add_http_header('Status', '404 No such resource exists.');
          print('No such \'content_collection\' resource exists with an id of \'' . $id . '\'.');
	    		watchdog('thumbwhere_contentcollection', 'No such \'content_collection\' resource exists with an id of \'' . $id . '\'.' ,array(), WATCHDOG_ERROR);
exit(0);
        }

        // Getting a reference to the entity we asked for
        $entity = $entities[$id];

        //
        // We are only updating what has been passed through.
        // No test for missing values.
        // TODO: Validate incoming values.
        //


        if (isset($notification->actor)) {
          $entity->fk_actor = intval($notification->actor);
        }

        if (isset($notification->title)) {
          $entity->title = (string)($notification->title);
        }

        if (!isset($notification->attributes()->key)) {
          if (twCanDebug()) debug('$entities');
          if (twCanDebug()) debug($entities);
        }

        // Finally write the entity.
        entity_save('thumbwhere_contentcollection', $entity);

        //
        // Send the final response
        //

        drupal_add_http_header('Content-Type', 'text/xml');
        print('<response>');
        print('<content_collection op="' . $op . '" id="' . $id . '" >');
        print('<status>success</status>');
        print('</content_collection>');
        print('</response>');
        exit(0);
      }

    default:
      {
        drupal_add_http_header('Status', '501 Unable to fulfull request..');
        print('We were expecting a \'key\', attribute on the \'content_collection\', element.');
        exit(0);

      }
  }
}



/**
 * Menu callback: React to a notifications for ContentIngest.
 */
function _thumbwhere_content_collection_notify() {

  if (variable_get('thumbwhere_api_log_debug', 0) == 1) {
    watchdog('thumbwhere_content_collection', 'incoming notification.');
  }

  $numargs = func_num_args();
  $arg_list = func_get_args();

  
	if (twCanDebug()) debug('$numargs  = ' . $numargs);
	
	if (twCanDebug()) debug('$arg_list = ' . $arg_list);
	
	if (twCanDebug()) debug('$_REQUEST');
	if (twCanDebug()) debug($_REQUEST);
	
	if (twCanDebug()) debug('$_GET');
	if (twCanDebug()) debug($_GET);
	
	if (twCanDebug()) debug('$_POST');
	if (twCanDebug()) debug($_POST);
	
	//if (twCanDebug()) debug('$HTTP_RAW_POST_DATA');
	//if (twCanDebug()) debug($HTTP_RAW_POST_DATA);
	
	if (twCanDebug()) debug('$_SERVER');
	if (twCanDebug()) debug($_SERVER);
	
	//if (twCanDebug()) debug('$GLOBALS');
	//if (twCanDebug()) debug($GLOBALS);

  

  if (variable_get('thumbwhere_api_log_debug', 0) == 1) {
    foreach ($arg_list as $arg) {
      if (twCanDebug()) debug('$arg  = ' . $arg);
    }
  }


  //////////////////////////////////////////////////
  //
  //

  switch ($_SERVER['REQUEST_METHOD']) {

    case 'POST': {
        if ($numargs == 1) {
          if ($arg_list[0] == 'content_collection') {

            //
            // Get the POST data as XML
            //

            $post_xml = new SimpleXMLElement('php://input', null, true);
            if (twCanDebug()) debug('$post_xml');
            if (twCanDebug()) debug($post_xml);

            if (twCanDebug()) debug('$post_xml->getName()');
            if (twCanDebug()) debug($post_xml->getName());

            // We expect 'action' as the root element. TODO: I would rather this was the more generic 'request'
            if ($post_xml->getName() != 'action') {
              drupal_add_http_header('Status', '404 not found.');
              print('We were expecting \'action\', as the root element, not \'' . $post_xml->getName() . '\'.');
              exit(0);
            }

            /*
             *  SimpleXMLElement::__set_state(array(
             *      'content_collection' =>
             *      SimpleXMLElement::__set_state(array(
             *       '@attributes' =>
             *      array (
             *        'key' => '8e9ff318-3cd7-4593-99de-4c37c0fb335a',
             *        'op' => 'create',
             *      ),
             *    )),
             *  ))
             */


            // We expect 'action' as the root element.
            if (!isset($post_xml->content_collection)) {
              drupal_add_http_header('Status', '404 not found.');
              print('We were expecting a \'content_collection\', element in the action payload.');
              exit(0);
            }

            // Handle the incoming notification for content ingest
            _thumbwhere_handle_content_collection_notify_xml($post_xml->content_collection);

          }
          else {
            drupal_add_http_header('Status', '404 not found.');
            print('Invalid resource');
            exit(0);
          }
        }
        else {
          drupal_add_http_header('Status', '404 not found.');
          print('Invalid notification request url format.');
          exit(0);
        }
      }
      break;

    case 'GET': {

        if ($numargs == 1) {

          // If we are returning the index.xml file for the webservice.
          if ($arg_list[0] == 'index.xml') {
            // return index.xml
            drupal_add_http_header('Content-Type', 'text/xml');
            readfile(dirname(__FILE__) . '/index.xml');
            exit(0);
          }
          else {
            drupal_add_http_header('Status', '404 not found.');
            print('File not found.');
            exit(0);
          }
        }
        else {
          drupal_add_http_header('Status', '404 not found.');
          print('Invalid notification request url format.');
          exit(0);
        }
      }
      break;

    default: {
        drupal_add_http_header('Status', '404 not found.');
        exit(0);
      }
      break;
  }

  //
  //
  ////////////////////////////////////////////////////////////////////////////////////
 
}





/**
 * ContentCollection UI controller.
 */
class ThumbWhereContentCollectionUIController extends EntityDefaultUIController {

  /**
   * Overrides hook_menu() defaults. Main reason for doing this is that
   * parent class hook_menu() is optimized for entity type administration.
   */
  public function hook_menu() {

    $items = array();
    
    // Add a notification page...
    $items['thumbwhere/content_collection/notify'] = array(
        'title' => 'Notifications Callback for "ContentCollection" Entity',
        'page callback' => '_thumbwhere_content_collection_notify',
        'access arguments' => array(
            'send thumbwhere contentcollection notifications'
        ),
        'file' => 'thumbwhere_contentcollection.admin.inc',
        'file path' => drupal_get_path('module', $this->entityInfo['module'])
    );    
    
    $id_count = count(explode('/', $this->path));
    $wildcard = isset($this->entityInfo['admin ui']['menu wildcard']) ? $this->entityInfo['admin ui']['menu wildcard'] : '%' . $this->entityType;

    $items[$this->path] = array(
      'title' => 'ThumbWhereContentCollections',
      'description' => 'Add edit and update thumbwhere_contentcollections.',
      'page callback' => 'system_admin_menu_block_page',
      'access arguments' => array('access administration pages'),
      'file path' => drupal_get_path('module', 'system'),
      'file' => 'system.admin.inc',
    );

    // Change the overview menu type for the list of thumbwhere_contentcollections.
    $items[$this->path]['type'] = MENU_LOCAL_TASK;

    // Change the add page menu to multiple types of entities
    $items[$this->path . '/add'] = array(
      'title' => 'Add a thumbwhere_contentcollection',
      'description' => 'Add a new thumbwhere_contentcollection',
      'page callback'  => 'thumbwhere_contentcollection_add_page',
      'access callback'  => 'thumbwhere_contentcollection_access',
      'access arguments' => array('edit'),
      'type' => MENU_NORMAL_ITEM,
      'weight' => 20,
      'file' => 'thumbwhere_contentcollection.admin.inc',
      'file path' => drupal_get_path('module', $this->entityInfo['module'])

    );

    $items[$this->path . '/add/' . 'thumbwhere_contentcollection'] = array(
      'title' => 'Add ' . 'ThumbWhereContentCollection',
      'page callback' => 'thumbwhere_contentcollection_form_wrapper',
      'page arguments' => array(thumbwhere_contentcollection_create(array('type' => 'thumbwhere_contentcollection'))),
      'access callback' => 'thumbwhere_contentcollection_access',
      'access arguments' => array('edit', 'edit ' . 'thumbwhere_contentcollection'),
      'file' => 'thumbwhere_contentcollection.admin.inc',
      'file path' => drupal_get_path('module', $this->entityInfo['module'])
    );

    // Loading and editing thumbwhere_contentcollection entities
    $items[$this->path . '/thumbwhere_contentcollection/' . $wildcard] = array(
      'page callback' => 'thumbwhere_contentcollection_form_wrapper',
      'page arguments' => array($id_count + 1),
      'access callback' => 'thumbwhere_contentcollection_access',
      'access arguments' => array('edit', $id_count + 1),
      'weight' => 0,
      'context' => MENU_CONTEXT_PAGE | MENU_CONTEXT_INLINE,
      'file' => 'thumbwhere_contentcollection.admin.inc',
      'file path' => drupal_get_path('module', $this->entityInfo['module'])
    );
    $items[$this->path . '/thumbwhere_contentcollection/' . $wildcard . '/edit'] = array(
      'title' => 'Edit',
      'type' => MENU_DEFAULT_LOCAL_TASK,
      'weight' => -10,
      'context' => MENU_CONTEXT_PAGE | MENU_CONTEXT_INLINE,
    );

    $items[$this->path . '/thumbwhere_contentcollection/' . $wildcard . '/delete'] = array(
      'title' => 'Delete',
      'page callback' => 'thumbwhere_contentcollection_delete_form_wrapper',
      'page arguments' => array($id_count + 1),
      'access callback' => 'thumbwhere_contentcollection_access',
      'access arguments' => array('edit', $id_count + 1),
      'type' => MENU_LOCAL_TASK,
      'context' => MENU_CONTEXT_INLINE,
      'weight' => 10,
      'file' => 'thumbwhere_contentcollection.admin.inc',
      'file path' => drupal_get_path('module', $this->entityInfo['module'])
    );

    // Menu item for viewing thumbwhere_contentcollections
    $items['thumbwhere_contentcollection/' . $wildcard] = array(
      //'title' => 'Title',
      'title callback' => 'thumbwhere_contentcollection_page_title',
      'title arguments' => array(1),
      'page callback' => 'thumbwhere_contentcollection_page_view',
      'page arguments' => array(1),
      'access callback' => 'thumbwhere_contentcollection_access',
      'access arguments' => array('view', 1),
      'type' => MENU_CALLBACK,
    );
    return $items;
  }


  /**
   * Create the markup for the add ThumbWhereContentCollection Entities page within the class
   * so it can easily be extended/overriden.
   */
  public function addPage() {
    $item = menu_get_item();
    $content = system_admin_menu_block($item);

    if (count($content) == 1) {
      $item = array_shift($content);
      drupal_goto($item['href']);
    }

    return theme('thumbwhere_contentcollection_add_list', array('content' => $content));
  }

}


/**
 * Form callback wrapper: create or edit a thumbwhere_contentcollection.
 *
 * @param $thumbwhere_contentcollection
 *   The thumbwhere_contentcollection object being edited by this form.
 *
 * @see thumbwhere_contentcollection_edit_form()
 */
function thumbwhere_contentcollection_form_wrapper($thumbwhere_contentcollection) {
  // Add the breadcrumb for the form's location.
  thumbwhere_contentcollection_set_breadcrumb();
  return drupal_get_form('thumbwhere_contentcollection_edit_form', $thumbwhere_contentcollection);
}


/**
 * Form callback wrapper: delete a thumbwhere_contentcollection.
 *
 * @param $thumbwhere_contentcollection
 *   The thumbwhere_contentcollection object being edited by this form.
 *
 * @see thumbwhere_contentcollection_edit_form()
 */
function thumbwhere_contentcollection_delete_form_wrapper($thumbwhere_contentcollection) {
  // Add the breadcrumb for the form's location.
  //thumbwhere_contentcollection_set_breadcrumb();
  return drupal_get_form('thumbwhere_contentcollection_delete_form', $thumbwhere_contentcollection);
}


/**
 * Form callback: create or edit a thumbwhere_contentcollection.
 *
 * @param $thumbwhere_contentcollection
 *   The thumbwhere_contentcollection object to edit or for a create form an empty thumbwhere_contentcollection object
 *     with only a thumbwhere_contentcollection type defined.
 */
function thumbwhere_contentcollection_edit_form($form, &$form_state, $thumbwhere_contentcollection) {

  // Add the default field elements.
  $form['fk_actor'] = array(
    '#type' => 'textfield',
    '#title' => t('ThumbWhereContentCollection fk_actor'),
    '#default_value' => isset($thumbwhere_contentcollection->fk_actor) ? $thumbwhere_contentcollection->fk_actor : '',
    //'#maxlength' => 255,
   /// '#required' => TRUE,
    '#weight' => -5,
    '#autocomplete_path' => 'entity-autocomplete/thumbwhere_actor',
  );
  // Add the default field elements.
    $form['title'] = array(
    '#type' => 'textfield',
    '#title' => t('ThumbWhereContentCollection title'),
    '#default_value' => isset($thumbwhere_contentcollection->title) ? $thumbwhere_contentcollection->title : 'Hello',
    // '#maxlength' => 255,
   // '#required' => TRUE,
    '#weight' => -5,
  );



  $form['data']['#tree'] = TRUE;

  /*
  $form['data']['sample_data'] = array(
    '#type' => 'checkbox',
    '#title' => t('An interesting thumbwhere_contentcollection switch'),
    '#default_value' => isset($thumbwhere_contentcollection->data['sample_data']) ? $thumbwhere_contentcollection->data['sample_data'] : 1,
  );
  */


  // Add the field related form elements.
  $form_state['thumbwhere_contentcollection'] = $thumbwhere_contentcollection;
  field_attach_form('thumbwhere_contentcollection', $thumbwhere_contentcollection, $form, $form_state);

  $form['actions'] = array(
    '#type' => 'container',
    '#attributes' => array('class' => array('form-actions')),
    '#weight' => 400,
  );

  // We add the form's #submit array to this button along with the actual submit
  // handler to preserve any submit handlers added by a form callback_wrapper.
  $submit = array();

  if (!empty($form['#submit'])) {
    $submit += $form['#submit'];
  }

  $form['actions']['submit'] = array(
    '#type' => 'submit',
    '#value' => t('Save thumbwhere_contentcollection'),
    '#submit' => $submit + array('thumbwhere_contentcollection_edit_form_submit'),
  );

  // Do we show the delete button?
  if (!empty($thumbwhere_contentcollection->pk_contentcollection)) {
    $form['actions']['delete'] = array(
      '#type' => 'submit',
      '#value' => t('Delete thumbwhere_contentcollection'),
      '#suffix' => l(t('Cancel'), 'admin/content/thumbwhere_contentcollections'),
      '#submit' => $submit + array('thumbwhere_contentcollection_form_submit_delete'),
      '#weight' => 45,
    );
  }

  // We append the validate handler to #validate in case a form callback_wrapper
  // is used to add validate handlers earlier.
  $form['#validate'][] = 'thumbwhere_contentcollection_edit_form_validate';
  return $form;
}


/**
 * Form API validate callback for the thumbwhere_contentcollection form
 */
function thumbwhere_contentcollection_edit_form_validate(&$form, &$form_state) {
   
  //if (twCanDebug()) debug($form);   
  //if (twCanDebug()) debug($form_state);


  // No validation for pk


  // Convert fk autocomplete for fk_actor
  $value = $form['fk_actor']['#value'];
  // If we have something, escape the auto-completion encoding.
  if (!empty($value)) { 
    $value = entity_autocomplete_get_id($value);
    form_set_value($form['fk_actor'], $value,$form_state);
  }
  //if (twCanDebug()) debug($form['fk_actor']);
   if (twCanDebug()) debug('fk_actor = ' . $value);
  // Validate fk fk_actor
  if (empty($value)) {
    form_set_error('fk_actor', t('Validation error, \'fk_actor\' is mandatory1.'));
    // throw new Exception('Field \'fk_actor\' in is mandatory');
  }

  // Validate normaltitle
  $value = $form['title']['#value'];
  // If we have no default value then we don't care much for checking for emptyness.


              

  //form_set_value( array('#parents' => array('array_key_parent', 'array_key_to_replace')) , $value, $form_state);

   
   
  $thumbwhere_contentcollection = $form_state['thumbwhere_contentcollection'];

  // Notify field widgets to validate their data.
  field_attach_form_validate('thumbwhere_contentcollection', $thumbwhere_contentcollection, $form, $form_state);
    
    
  
}


/**
 * Form API submit callback for the thumbwhere_contentcollection form.
 *
 * @todo remove hard-coded link
 */
function thumbwhere_contentcollection_edit_form_submit(&$form, &$form_state) {

  $thumbwhere_contentcollection = entity_ui_controller('thumbwhere_contentcollection')->entityFormSubmitBuildEntity($form, $form_state);
  // Save the thumbwhere_contentcollection and go back to the list of thumbwhere_contentcollections

  // Add in created and changed times.
  if ($thumbwhere_contentcollection->is_new = isset($thumbwhere_contentcollection->is_new) ? $thumbwhere_contentcollection->is_new : 0) {
    $thumbwhere_contentcollection->created = time();
  }

  $thumbwhere_contentcollection->changed = time();

  $thumbwhere_contentcollection->save();
  $form_state['redirect'] = 'admin/content/thumbwhere_contentcollections';
}

/**
 * Form API submit callback for the delete button.
 *
 * @todo Remove hard-coded path
 */
function thumbwhere_contentcollection_form_submit_delete(&$form, &$form_state) {
  $form_state['redirect'] = 'admin/content/thumbwhere_contentcollections/thumbwhere_contentcollection/' . $form_state['thumbwhere_contentcollection']->pk_contentcollection . '/delete';
}





/**
 * Form callback: confirmation form for deleting a thumbwhere_contentcollection.
 *
 * @param $thumbwhere_contentcollection
 *   The thumbwhere_contentcollection to delete
 *
 * @see confirm_form()
 */
function thumbwhere_contentcollection_delete_form($form, &$form_state, $thumbwhere_contentcollection) {
  $form_state['thumbwhere_contentcollection'] = $thumbwhere_contentcollection;

  $form['#submit'][] = 'thumbwhere_contentcollection_delete_form_submit';

  $form = confirm_form($form,
    t('Are you sure you want to delete thumbwhere_contentcollection %name?', array('%name' => $thumbwhere_contentcollection->title)),
    'admin/content/thumbwhere_contentcollections/thumbwhere_contentcollection',
    '<p>' . t('This action cannot be undone.') . '</p>',
    t('Delete'),
    t('Cancel'),
    'confirm'
  );

  return $form;
}

/**
 * Submit callback for thumbwhere_contentcollection_delete_form
 */
function thumbwhere_contentcollection_delete_form_submit($form, &$form_state) {
  $thumbwhere_contentcollection = $form_state['thumbwhere_contentcollection'];

  thumbwhere_contentcollection_delete($thumbwhere_contentcollection);

  drupal_set_message(t('The thumbwhere_contentcollection %name has been deleted.', array('%name' => $thumbwhere_contentcollection->title)));
  watchdog('thumbwhere_contentcollection', 'Deleted thumbwhere_contentcollection %name.', array('%name' => $thumbwhere_contentcollection->title));

  $form_state['redirect'] = 'admin/content/thumbwhere_contentcollections';
}



/**
 * Page to add ThumbWhereContentCollection Entities.
 *
 * @todo Pass this through a proper theme function
 */
function thumbwhere_contentcollection_add_page() {
  $controller = entity_ui_controller('thumbwhere_contentcollection');
  return $controller->addPage();
}


/**
 * Displays the list of available thumbwhere_contentcollection types for thumbwhere_contentcollection creation.
 *
 * @ingroup themeable
 */
function theme_thumbwhere_contentcollection_add_list($variables) {


  //$content = $variables['content'];
  $output = '';
  /*
  if ($content) {
    $output = '<dl class="thumbwhere_contentcollection-type-list">';
    foreach ($content as $item) {
      $output .= '<dt>' . l($item['title'], $item['href']) . '</dt>';
      $output .= '<dd>' . filter_xss_admin($item['description']) . '</dd>';
    }
    $output .= '</dl>';
  }
  else {
    if (user_access('administer thumbwhere_contentcollection types')) {
      $output = '<p>' . t('ThumbWhereContentCollection Entities cannot be added because you have not created any thumbwhere_contentcollection types yet. Go to the <a href="@create-thumbwhere_contentcollection-type">thumbwhere_contentcollection type creation page</a> to add a new thumbwhere_contentcollection type.', array('@create-thumbwhere_contentcollection-type' => url('admin/structure/thumbwhere_contentcollection_types/add'))) . '</p>';
    }
    else {
      $output = '<p>' . t('No thumbwhere_contentcollection types have been created yet for you to use.') . '</p>';
    }
  }
  */
  return $output;
}





/**
 * Sets the breadcrumb for administrative thumbwhere_contentcollection pages.
 */
function thumbwhere_contentcollection_set_breadcrumb() {
  $breadcrumb = array(
    l(t('Home'), '<front>'),
    l(t('Administration'), 'admin'),
    l(t('ContentCollection'), 'admin/content'),
    l(t('ThumbWhereContentCollections'), 'admin/content/thumbwhere_contentcollections'),
  );

  drupal_set_breadcrumb($breadcrumb);
}



