<?php

/**
 * @file
 * ThumbWhereExternalRepositorySync editing UI.
 *
 * We make very little use of the EntityAPI interface for this - preferring instead to use
 * views. That offers more flexibility to change a UI that will, more often than not,
 * be end-user facing.
 *
 * NOTE: This file was generated by autogen_model_implementation_drupal.xsl
 */


	

 
/**
 * Handle the notification for content_ingext
 */
function _thumbwhere_handle_external_repository_sync_notify_xml($notification) {

  //
  // Get the operation
  //

  if (!isset($notification->attributes()->op)) {
    drupal_add_http_header('Status', '501 unable to fulfull request..');
    $err = 'We were expecting an \'op\', attribute on the \'external_repository_sync\', element.';
    print($err);
   	watchdog('thumbwhere_externalrepositorysync', $err ,array(), WATCHDOG_ERROR);
    exit(0);
  }

  $op = $notification->attributes()->op;

  if (twCanDebug()) debug('op');
  if (twCanDebug()) debug($op);

  //
  // Get the context
  //

  if (!isset($notification->attributes()->key)) {
    drupal_add_http_header('Status', '412 unable to fulfull request..');
    $err = 'We were expecting a \'key\', attribute on the \'external_repository_sync\', element.';
    print($err);
    watchdog('thumbwhere_externalrepositorysync', $err ,array(), WATCHDOG_ERROR);
    exit(0);
  }

  $context_key =  $notification->attributes()->key;

  //
  // Now based on the operation..
  //

  switch($op)
  {
  
  
      case 'create':
      {
      		
		
		//
		// We want to time this
		//
			
		$start_time = microtime(TRUE);
			
        //
        // What resource are we creating?
        //
      
        //
        // Get the id
        //
		
        if (!isset($notification->attributes()->id)) {
          drupal_add_http_header('Status', '412 Unable to fulfull request.');
          $err = 'We were expecting an \'id\', attribute on the \'external_repository_sync\', element when we are performing an \'update\' notification..';
          print($err);
          watchdog('thumbwhere_externalrepositorysync', $err ,array(), WATCHDOG_ERROR);
          exit(0);
        }

        $id = intval($notification->attributes()->id);
		

        if (twCanDebug()) debug('id');
        if (twCanDebug()) debug($id);

      
        //
        // Get the fields
        //
          
        $entities = entity_load('thumbwhere_externalrepositorysync', array($id));

        if (twCanDebug()) debug('$entities');
        if (twCanDebug()) debug($entities);

        if (count($entities) != 0) {
          //drupal_add_http_header('Status', '409 Resource already exists.');
          $err = '\'external_repository_sync\' resource already exists with an id of \'' . $id . '\'.';
           //print($err);
          watchdog('thumbwhere_externalrepositorysync', $err ,array(), WATCHDOG_ERROR);
          //exit(0);

          	
          drupal_add_http_header('Content-Type', 'text/xml');
          print('<response>');
          print('<external_repository_sync op="' . $op . '" id="' . $id . '" >');
          print('<status>success</status>');
          print('</external_repository_sync>');
          print('</response>');
          exit(0);          	
          
        }

        // Getting a reference to the entity we asked for
        $entityfields = array();

        //
        // We are only updating what has been passed through.
        // No test for missing values.
        // TODO: Validate incoming values.
        //


        $entityfields['pk_externalrepositorysync'] = $id;	

        if (isset($notification->externalsubscription)) {
          $entityfields['fk_externalsubscription'] = intval($notification->externalsubscription);
        }

        if (isset($notification->last_sync)) {
          $entityfields['last_sync'] = strtotime($notification->last_sync);
        }

        if (isset($notification->max)) {
          $entityfields['max'] = (int)intval($notification->max);
        }

        if (isset($notification->complete)) {
          if ((string)($notification->complete) == 'true') {
            $entityfields['complete'] = '1';
          }
          else {
            $entityfields['complete'] = '0';
          }
        }

        if (!isset($notification->attributes()->key)) {
          if (twCanDebug()) debug('$entities');
          if (twCanDebug()) debug($entities);
        }

        // We are being notified, so we do not want to ship this out again as a notification...
        // Finally write the entity.
        $entity = entity_create('thumbwhere_externalrepositorysync', $entityfields);
        $entity->do_not_propagate = TRUE;
        $entity->save();

        //
        // Send the final response
        //
        
        $duration = round(((microtime(TRUE) - $start_time) * 1000.0),3); 

        drupal_add_http_header('Content-Type', 'text/xml');
        print('<response duration="' . $duration . 'ms">');
        print('<external_repository_sync op="' . $op . '" id="' . $id . '" >');
        print('<status>success</status>');
        print('</external_repository_sync>');
        print('</response>');
        exit(0);			
		
      
      }
  
  
    case 'update':
      {
      
		//
		// We want to time this
		//
			
		$start_time = microtime(TRUE);
      
        //
        // What resource are we updating?
        //
        
      
        //
        // Get the id
        //
		
        if (!isset($notification->attributes()->id)) {
          drupal_add_http_header('Status', '412 Unable to fulfull request.');
          $err = 'We were expecting an \'id\', attribute on the \'external_repository_sync\', element when we are performing an \'update\' notification..';
          print($err);
          watchdog('thumbwhere_externalrepositorysync', $err ,array(), WATCHDOG_ERROR);
          exit(0);
        }

        $id = intval($notification->attributes()->id);
		

        if (twCanDebug()) debug('id');
        if (twCanDebug()) debug($id);

      
        //
        // Get the fields
        //
          
        $entities = entity_load('thumbwhere_externalrepositorysync', array($id));

        if (twCanDebug()) debug('$entities');
        if (twCanDebug()) debug($entities);

        if (count($entities) == 0) {
          //drupal_add_http_header('Status', '409 No such resource exists.');
          $err = 'No such \'external_repository_sync\' resource exists with an id of \'' . $id . '\'.';
          //print($err);
          watchdog('thumbwhere_externalrepositorysync',$err,array(), WATCHDOG_ERROR);
          //exit(0);

////////////////////////////////////////////////////////////////////////
//
// ... for now we will just create the local copy
//
          watchdog('thumbwhere_externalrepositorysync','creating local version of missing entity',array(), WATCHDOG_ERROR);
		
		
		//
		// We want to time this
		//
			
		$start_time = microtime(TRUE);
			
        //
        // What resource are we creating?
        //
      
        //
        // Get the id
        //
		
        if (!isset($notification->attributes()->id)) {
          drupal_add_http_header('Status', '412 Unable to fulfull request.');
          $err = 'We were expecting an \'id\', attribute on the \'external_repository_sync\', element when we are performing an \'update\' notification..';
          print($err);
          watchdog('thumbwhere_externalrepositorysync', $err ,array(), WATCHDOG_ERROR);
          exit(0);
        }

        $id = intval($notification->attributes()->id);
		

        if (twCanDebug()) debug('id');
        if (twCanDebug()) debug($id);

      
        //
        // Get the fields
        //
          
        $entities = entity_load('thumbwhere_externalrepositorysync', array($id));

        if (twCanDebug()) debug('$entities');
        if (twCanDebug()) debug($entities);

        if (count($entities) != 0) {
          //drupal_add_http_header('Status', '409 Resource already exists.');
          $err = '\'external_repository_sync\' resource already exists with an id of \'' . $id . '\'.';
           //print($err);
          watchdog('thumbwhere_externalrepositorysync', $err ,array(), WATCHDOG_ERROR);
          //exit(0);

          	
          drupal_add_http_header('Content-Type', 'text/xml');
          print('<response>');
          print('<external_repository_sync op="' . $op . '" id="' . $id . '" >');
          print('<status>success</status>');
          print('</external_repository_sync>');
          print('</response>');
          exit(0);          	
          
        }

        // Getting a reference to the entity we asked for
        $entityfields = array();

        //
        // We are only updating what has been passed through.
        // No test for missing values.
        // TODO: Validate incoming values.
        //


        $entityfields['pk_externalrepositorysync'] = $id;	

        if (isset($notification->externalsubscription)) {
          $entityfields['fk_externalsubscription'] = intval($notification->externalsubscription);
        }

        if (isset($notification->last_sync)) {
          $entityfields['last_sync'] = strtotime($notification->last_sync);
        }

        if (isset($notification->max)) {
          $entityfields['max'] = (int)intval($notification->max);
        }

        if (isset($notification->complete)) {
          if ((string)($notification->complete) == 'true') {
            $entityfields['complete'] = '1';
          }
          else {
            $entityfields['complete'] = '0';
          }
        }

        if (!isset($notification->attributes()->key)) {
          if (twCanDebug()) debug('$entities');
          if (twCanDebug()) debug($entities);
        }

        // We are being notified, so we do not want to ship this out again as a notification...
        // Finally write the entity.
        $entity = entity_create('thumbwhere_externalrepositorysync', $entityfields);
        $entity->do_not_propagate = TRUE;
        $entity->save();

        //
        // Send the final response
        //
        
        $duration = round(((microtime(TRUE) - $start_time) * 1000.0),3); 

        drupal_add_http_header('Content-Type', 'text/xml');
        print('<response duration="' . $duration . 'ms">');
        print('<external_repository_sync op="' . $op . '" id="' . $id . '" >');
        print('<status>success</status>');
        print('</external_repository_sync>');
        print('</response>');
        exit(0);			
		
//
//
//
////////////////////////////////////////////////////////////////////////
        }

        // Getting a reference to the entity we asked for
        $entity = $entities[$id];

        //
        // We are only updating what has been passed through.
        // No test for missing values.
        // TODO: Validate incoming values.
        //


        if (isset($notification->externalsubscription)) {
          $entity->fk_externalsubscription = intval($notification->externalsubscription);
        }

        if (isset($notification->last_sync)) {
          $entity->last_sync = (int)strtotime($notification->last_sync);
        }

        if (isset($notification->max)) {
          $entity->max = (int)intval($notification->max);
        }

        if (isset($notification->complete)) {
          if ((string)($notification->complete) == 'true') {
            $entity->complete = '1';
          }
          else {
            $entity->complete = '0';
          }
        }

        if (!isset($notification->attributes()->key)) {
          if (twCanDebug()) debug('$entities');
          if (twCanDebug()) debug($entities);
        }

        // We are being notified, so we do not want to ship this out again as a notification...
        $entity->do_not_propagate = TRUE;

        // Finally write the entity.
        entity_save('thumbwhere_externalrepositorysync', $entity);

        //
        // Send the final response
        //
        
        $duration = round(((microtime(TRUE) - $start_time) * 1000.0),3);

        drupal_add_http_header('Content-Type', 'text/xml');
        print('<response  duration="' . $duration . '" >');
        print('<external_repository_sync op="' . $op . '" id="' . $id . '" >');
        print('<status>success</status>');
        print('</external_repository_sync>');
        print('</response>');
        exit(0);
      }
      
      

    default:
      {
        drupal_add_http_header('Status', '501 Unable to fulfull request. Unhandled operation \'' . $op . '\'.');
        print('We don\'t handle the \'' . $op . '\', notification operation on the \'external_repository_sync\', element.');
        exit(0);

      }
  }
}



/**
 * Menu callback: React to a notifications for ContentIngest.
 */
function _thumbwhere_external_repository_sync_notify() {

  if (twCanDebug()) {
    watchdog('thumbwhere_external_repository_sync', 'incoming notification.');
  }

  $numargs = func_num_args();
  $arg_list = func_get_args();

  
	if (twCanDebug()) debug('$numargs  = ' . $numargs);
	
	if (twCanDebug()) debug('$arg_list = ' . $arg_list);
	
	if (twCanDebug()) debug('$_REQUEST');
	if (twCanDebug()) debug($_REQUEST);
	
	if (twCanDebug()) debug('$_GET');
	if (twCanDebug()) debug($_GET);
	
	if (twCanDebug()) debug('$_POST');
	if (twCanDebug()) debug($_POST);
	
	//if (twCanDebug()) debug('$HTTP_RAW_POST_DATA');
	//if (twCanDebug()) debug($HTTP_RAW_POST_DATA);
	
	if (twCanDebug()) debug('$_SERVER');
	if (twCanDebug()) debug($_SERVER);
	
	//if (twCanDebug()) debug('$GLOBALS');
	//if (twCanDebug()) debug($GLOBALS);

  

  if (variable_get('thumbwhere_api_log_debug', 0) == 1) {
    foreach ($arg_list as $arg) {
      if (twCanDebug()) debug('$arg  = ' . $arg);
    }
  }


  //////////////////////////////////////////////////
  //
  //

  switch ($_SERVER['REQUEST_METHOD']) {

    case 'POST': {
        if ($numargs == 1) {
          if ($arg_list[0] == 'external_repository_sync') {

            //
            // Get the POST data as XML
            //

            $post_xml = new SimpleXMLElement('php://input', null, true);
            if (twCanDebug()) debug('$post_xml');
            if (twCanDebug()) debug($post_xml);

            if (twCanDebug()) debug('$post_xml->getName()');
            if (twCanDebug()) debug($post_xml->getName());

            // We expect 'action' as the root element. TODO: I would rather this was the more generic 'request'
            if ($post_xml->getName() != 'action') {
              drupal_add_http_header('Status', '404 not found.');
              print('We were expecting \'action\', as the root element, not \'' . $post_xml->getName() . '\'.');
              exit(0);
            }

            /*
             *  SimpleXMLElement::__set_state(array(
             *      'external_repository_sync' =>
             *      SimpleXMLElement::__set_state(array(
             *       '@attributes' =>
             *      array (
             *        'key' => '8e9ff318-3cd7-4593-99de-4c37c0fb335a',
             *        'op' => 'create',
             *      ),
             *    )),
             *  ))
             */


            // We expect 'action' as the root element.
            if (!isset($post_xml->external_repository_sync)) {
              drupal_add_http_header('Status', '404 not found.');
              print('We were expecting a \'external_repository_sync\', element in the action payload.');
              exit(0);
            }

            // Handle the incoming notification for content ingest
            _thumbwhere_handle_external_repository_sync_notify_xml($post_xml->external_repository_sync);

          }
          else {
            drupal_add_http_header('Status', '404 not found.');
            print('Invalid resource');
            exit(0);
          }
        }
        else {
          drupal_add_http_header('Status', '404 not found.');
          print('Invalid notification request url format.');
          exit(0);
        }
      }
      break;

    case 'GET': {

        if ($numargs == 1) {

          // If we are returning the index.xml file for the webservice.
          if ($arg_list[0] == 'index.xml') {
            // return index.xml
            drupal_add_http_header('Content-Type', 'text/xml');
            readfile(dirname(__FILE__) . '/index.xml');
            exit(0);
          }
          else {
            drupal_add_http_header('Status', '404 not found.');
            print('File not found.');
            exit(0);
          }
        }
        else {
          drupal_add_http_header('Status', '404 not found.');
          print('Invalid notification request url format.');
          exit(0);
        }
      }
      break;

    default: {
        drupal_add_http_header('Status', '404 not found.');
        exit(0);
      }
      break;
  }

  //
  //
  ////////////////////////////////////////////////////////////////////////////////////
 
}





/**
 * ExternalRepositorySync UI controller.
 */
class ThumbWhereExternalRepositorySyncUIController extends EntityDefaultUIController {

  /**
   * Overrides hook_menu() defaults. Main reason for doing this is that
   * parent class hook_menu() is optimized for entity type administration.
   */
  public function hook_menu() {

    $items = array();
    
    // Add a notification page...
    $items['thumbwhere/external_repository_sync/notify'] = array(
        'title' => 'Notifications Callback for "ExternalRepositorySync" Entity',
        'page callback' => '_thumbwhere_external_repository_sync_notify',
        'access arguments' => array(
            'send thumbwhere externalrepositorysync notifications'
        ),
        'file' => 'thumbwhere_externalrepositorysync.admin.inc',
        'file path' => drupal_get_path('module', $this->entityInfo['module'])
    );    
    
    $id_count = count(explode('/', $this->path));
    $wildcard = isset($this->entityInfo['admin ui']['menu wildcard']) ? $this->entityInfo['admin ui']['menu wildcard'] : '%' . $this->entityType;

    $items[$this->path] = array(
      'title' => 'ExternalRepositorySync',
      'description' => 'Add edit and update thumbwhere_externalrepositorysyncs.',
      'page callback' => 'system_admin_menu_block_page',
      'access arguments' => array('access administration pages'),
      'file path' => drupal_get_path('module', 'system'),
      'file' => 'system.admin.inc',
    );

    // Change the overview menu type for the list of thumbwhere_externalrepositorysyncs.
    $items[$this->path]['type'] = MENU_LOCAL_TASK;

    // Change the add page menu to multiple types of entities
    $items[$this->path . '/add'] = array(
      //'title' => 'Add a ExternalRepositorySync',
      'title' => 'Add',
      'description' => 'Add a new ExternalRepositorySync',
      'page callback' => 'thumbwhere_externalrepositorysync_form_wrapper',
      'page arguments' => array(thumbwhere_externalrepositorysync_create(array('type' => 'thumbwhere_externalrepositorysync'))),
      'access callback' => 'thumbwhere_externalrepositorysync_access',
      'access arguments' => array('edit', 'edit ' . 'thumbwhere_externalrepositorysync'),
      'file' => 'thumbwhere_externalrepositorysync.admin.inc',
      'file path' => drupal_get_path('module', $this->entityInfo['module'])
    );


/*
    // Change the add page menu to multiple types of entities
    $items[$this->path . '/add'] = array(
      //'title' => 'Add a ExternalRepositorySync',
      'title' => 'Add',
	  'description' => 'Add a new ExternalRepositorySync',
      'page callback'  => 'thumbwhere_externalrepositorysync_add_page',
      'access callback'  => 'thumbwhere_externalrepositorysync_access',
      'access arguments' => array('edit'),
      'type' => MENU_NORMAL_ITEM,
      'weight' => 20,
      'file' => 'thumbwhere_externalrepositorysync.admin.inc',
      'file path' => drupal_get_path('module', $this->entityInfo['module'])

    );
*/    
/*
    $items[$this->path . '/add/' . 'thumbwhere_externalrepositorysync'] = array(
      'title' => 'Add ' . 'ThumbWhereExternalRepositorySync',
      'page callback' => 'thumbwhere_externalrepositorysync_form_wrapper',
      'page arguments' => array(thumbwhere_externalrepositorysync_create(array('type' => 'thumbwhere_externalrepositorysync'))),
      'access callback' => 'thumbwhere_externalrepositorysync_access',
      'access arguments' => array('edit', 'edit ' . 'thumbwhere_externalrepositorysync'),
      'file' => 'thumbwhere_externalrepositorysync.admin.inc',
      'file path' => drupal_get_path('module', $this->entityInfo['module'])
    );
*/
    // Loading and editing thumbwhere_externalrepositorysync entities
    $items[$this->path . '/thumbwhere_externalrepositorysync/' . $wildcard] = array(
      'page callback' => 'thumbwhere_externalrepositorysync_form_wrapper',
      'page arguments' => array($id_count + 1),
      'access callback' => 'thumbwhere_externalrepositorysync_access',
      'access arguments' => array('edit', $id_count + 1),
      'weight' => 0,
      'context' => MENU_CONTEXT_PAGE | MENU_CONTEXT_INLINE,
      'file' => 'thumbwhere_externalrepositorysync.admin.inc',
      'file path' => drupal_get_path('module', $this->entityInfo['module'])
    );
    $items[$this->path . '/thumbwhere_externalrepositorysync/' . $wildcard . '/edit'] = array(
      'title' => 'Edit',
      'type' => MENU_DEFAULT_LOCAL_TASK,
      'weight' => -10,
      'context' => MENU_CONTEXT_PAGE | MENU_CONTEXT_INLINE,
    );

    $items[$this->path . '/thumbwhere_externalrepositorysync/' . $wildcard . '/delete'] = array(
      'title' => 'Delete',
      'page callback' => 'thumbwhere_externalrepositorysync_delete_form_wrapper',
      'page arguments' => array($id_count + 1),
      'access callback' => 'thumbwhere_externalrepositorysync_access',
      'access arguments' => array('edit', $id_count + 1),
      'type' => MENU_LOCAL_TASK,
      'context' => MENU_CONTEXT_INLINE,
      'weight' => 10,
      'file' => 'thumbwhere_externalrepositorysync.admin.inc',
      'file path' => drupal_get_path('module', $this->entityInfo['module'])
    );

    // Menu item for viewing thumbwhere_externalrepositorysyncs
    $items['thumbwhere_externalrepositorysync/' . $wildcard] = array(
      //'title' => 'Title',
      'title callback' => 'thumbwhere_externalrepositorysync_page_title',
      'title arguments' => array(1),
      'page callback' => 'thumbwhere_externalrepositorysync_page_view',
      'page arguments' => array(1),
      'access callback' => 'thumbwhere_externalrepositorysync_access',
      'access arguments' => array('view', 1),
      'type' => MENU_CALLBACK,
    );
    return $items;
  }


  /**
   * Create the markup for the add ThumbWhereExternalRepositorySync Entities page within the class
   * so it can easily be extended/overriden.
   */
  public function addPage() {
    $item = menu_get_item();
    $content = system_admin_menu_block($item);

    if (count($content) == 1) {
      $item = array_shift($content);
      drupal_goto($item['href']);
    }

    return theme('thumbwhere_externalrepositorysync_add_list', array('content' => $content));
  }

}


/**
 * Form callback wrapper: create or edit a thumbwhere_externalrepositorysync.
 *
 * @param $thumbwhere_externalrepositorysync
 *   The thumbwhere_externalrepositorysync object being edited by this form.
 *
 * @see thumbwhere_externalrepositorysync_edit_form()
 */
function thumbwhere_externalrepositorysync_form_wrapper($thumbwhere_externalrepositorysync) {
  // Add the breadcrumb for the form's location.
  thumbwhere_externalrepositorysync_set_breadcrumb();
  return drupal_get_form('thumbwhere_externalrepositorysync_edit_form', $thumbwhere_externalrepositorysync);
}


/**
 * Form callback wrapper: delete a thumbwhere_externalrepositorysync.
 *
 * @param $thumbwhere_externalrepositorysync
 *   The thumbwhere_externalrepositorysync object being edited by this form.
 *
 * @see thumbwhere_externalrepositorysync_edit_form()
 */
function thumbwhere_externalrepositorysync_delete_form_wrapper($thumbwhere_externalrepositorysync) {
  // Add the breadcrumb for the form's location.
  //thumbwhere_externalrepositorysync_set_breadcrumb();
  return drupal_get_form('thumbwhere_externalrepositorysync_delete_form', $thumbwhere_externalrepositorysync);
}


/**
 * Form callback: create or edit a thumbwhere_externalrepositorysync.
 *
 * @param $thumbwhere_externalrepositorysync
 *   The thumbwhere_externalrepositorysync object to edit or for a create form an empty thumbwhere_externalrepositorysync object
 *     with only a thumbwhere_externalrepositorysync type defined.
 */
function thumbwhere_externalrepositorysync_edit_form($form, &$form_state, $thumbwhere_externalrepositorysync) {

  // Add the default field elements.
  $form['fk_externalsubscription'] = array(
    '#type' => 'textfield',
    '#title' => t('ThumbWhereExternalRepositorySync fk_externalsubscription'),
    '#default_value' => isset($thumbwhere_externalrepositorysync->fk_externalsubscription) ? $thumbwhere_externalrepositorysync->fk_externalsubscription : '',
    //'#maxlength' => 255,
   /// '#required' => TRUE,
    '#weight' => -5,
    '#autocomplete_path' => 'entity-autocomplete/thumbwhere_externalsubscription',
  );
  // Add the default field elements.
    $form['last_sync'] = array(
    '#type' => 'date',
    '#title' => t('ThumbWhereExternalRepositorySync last_sync'),

    // '#maxlength' => 255,
   // '#required' => TRUE,
    '#weight' => -5,
  );
  // Add the default field elements.
    $form['max'] = array(
    '#type' => 'textfield',
    '#title' => t('ThumbWhereExternalRepositorySync max'),

    // '#maxlength' => 255,
   // '#required' => TRUE,
    '#weight' => -5,
  );
  // Add the default field elements.
    $form['complete'] = array(
    '#type' => 'checkbox',
    '#title' => t('ThumbWhereExternalRepositorySync complete'),
    '#default_value' => isset($thumbwhere_externalrepositorysync->complete) ? $thumbwhere_externalrepositorysync->complete : 0,
    // '#maxlength' => 255,
   // '#required' => TRUE,
    '#weight' => -5,
  );



  $form['data']['#tree'] = TRUE;

  /*
  $form['data']['sample_data'] = array(
    '#type' => 'checkbox',
    '#title' => t('An interesting thumbwhere_externalrepositorysync switch'),
    '#default_value' => isset($thumbwhere_externalrepositorysync->data['sample_data']) ? $thumbwhere_externalrepositorysync->data['sample_data'] : 1,
  );
  */


  // Add the field related form elements.
  $form_state['thumbwhere_externalrepositorysync'] = $thumbwhere_externalrepositorysync;
  field_attach_form('thumbwhere_externalrepositorysync', $thumbwhere_externalrepositorysync, $form, $form_state);

  $form['actions'] = array(
    '#type' => 'container',
    '#attributes' => array('class' => array('form-actions')),
    '#weight' => 400,
  );

  // We add the form's #submit array to this button along with the actual submit
  // handler to preserve any submit handlers added by a form callback_wrapper.
  $submit = array();

  if (!empty($form['#submit'])) {
    $submit += $form['#submit'];
  }

  $form['actions']['submit'] = array(
    '#type' => 'submit',
    '#value' => t('Save thumbwhere_externalrepositorysync'),
    '#submit' => $submit + array('thumbwhere_externalrepositorysync_edit_form_submit'),
  );

  // Do we show the delete button?
  if (!empty($thumbwhere_externalrepositorysync->pk_externalrepositorysync)) {
    $form['actions']['delete'] = array(
      '#type' => 'submit',
      '#value' => t('Delete thumbwhere_externalrepositorysync'),
      '#suffix' => l(t('Cancel'), 'admin/thumbwhere/thumbwhere_externalrepositorysyncs'),
      '#submit' => $submit + array('thumbwhere_externalrepositorysync_form_submit_delete'),
      '#weight' => 45,
    );
  }

  // We append the validate handler to #validate in case a form callback_wrapper
  // is used to add validate handlers earlier.
  $form['#validate'][] = 'thumbwhere_externalrepositorysync_edit_form_validate';
  return $form;
}


/**
 * Form API validate callback for the thumbwhere_externalrepositorysync form
 */
function thumbwhere_externalrepositorysync_edit_form_validate(&$form, &$form_state) {
   
  //if (twCanDebug()) debug($form);   
  //if (twCanDebug()) debug($form_state);


  // No validation for pk


  // Convert fk autocomplete for fk_externalsubscription
  $value = $form['fk_externalsubscription']['#value'];
  // If we have something, escape the auto-completion encoding.
  if (!empty($value)) { 
    $value = entity_autocomplete_get_id($value);
    form_set_value($form['fk_externalsubscription'], $value,$form_state);
  }
  //if (twCanDebug()) debug($form['fk_externalsubscription']);
   if (twCanDebug()) debug('fk_externalsubscription = ' . $value);
  // Validate fk fk_externalsubscription
  if (empty($value)) {
    form_set_error('fk_externalsubscription', t('Validation error, \'fk_externalsubscription\' is mandatory1.'));
    // throw new Exception('Field \'fk_externalsubscription\' in is mandatory');
  }

  // Validate normallast_sync
  $value = $form['last_sync']['#value'];
  // If we have no default value then we don't care much for checking for emptyness.
  if (empty($value)) {
    form_set_error('last_sync', t('Validation error, \'last_sync\' is mandatory2.'));
    //throw new Exception('Field \'last_sync\' in is mandatory.');
  }

  // Validate normalmax
  $value = $form['max']['#value'];
  // If we have no default value then we don't care much for checking for emptyness.
  if (empty($value)) {
    form_set_error('max', t('Validation error, \'max\' is mandatory2.'));
    //throw new Exception('Field \'max\' in is mandatory.');
  }

  // Validate normalcomplete
  $value = $form['complete']['#value'];
  // If we have no default value then we don't care much for checking for emptyness.


              

  //form_set_value( array('#parents' => array('array_key_parent', 'array_key_to_replace')) , $value, $form_state);

   
   
  $thumbwhere_externalrepositorysync = $form_state['thumbwhere_externalrepositorysync'];

  // Notify field widgets to validate their data.
  field_attach_form_validate('thumbwhere_externalrepositorysync', $thumbwhere_externalrepositorysync, $form, $form_state);
    
    
  
}


/**
 * Form API submit callback for the thumbwhere_externalrepositorysync form.
 *
 * @todo remove hard-coded link
 */
function thumbwhere_externalrepositorysync_edit_form_submit(&$form, &$form_state) {

  $thumbwhere_externalrepositorysync = entity_ui_controller('thumbwhere_externalrepositorysync')->entityFormSubmitBuildEntity($form, $form_state);
  // Save the thumbwhere_externalrepositorysync and go back to the list of thumbwhere_externalrepositorysyncs

  // Add in created and changed times.
  if ($thumbwhere_externalrepositorysync->is_new = isset($thumbwhere_externalrepositorysync->is_new) ? $thumbwhere_externalrepositorysync->is_new : 0) {
    $thumbwhere_externalrepositorysync->created = time();
  }

  $thumbwhere_externalrepositorysync->changed = time();

  $thumbwhere_externalrepositorysync->save();
  $form_state['redirect'] = 'admin/thumbwhere/thumbwhere_externalrepositorysyncs';
}

/**
 * Form API submit callback for the delete button.
 *
 * @todo Remove hard-coded path
 */
function thumbwhere_externalrepositorysync_form_submit_delete(&$form, &$form_state) {
  $form_state['redirect'] = 'admin/thumbwhere/thumbwhere_externalrepositorysyncs/thumbwhere_externalrepositorysync/' . $form_state['thumbwhere_externalrepositorysync']->pk_externalrepositorysync . '/delete';
}





/**
 * Form callback: confirmation form for deleting a thumbwhere_externalrepositorysync.
 *
 * @param $thumbwhere_externalrepositorysync
 *   The thumbwhere_externalrepositorysync to delete
 *
 * @see confirm_form()
 */
function thumbwhere_externalrepositorysync_delete_form($form, &$form_state, $thumbwhere_externalrepositorysync) {
  $form_state['thumbwhere_externalrepositorysync'] = $thumbwhere_externalrepositorysync;

  $form['#submit'][] = 'thumbwhere_externalrepositorysync_delete_form_submit';

  $form = confirm_form($form,
    t('Are you sure you want to delete thumbwhere_externalrepositorysync %name?', array('%name' => $thumbwhere_externalrepositorysync->pk_externalrepositorysync)),
    'admin/thumbwhere/thumbwhere_externalrepositorysyncs/thumbwhere_externalrepositorysync',
    '<p>' . t('This action cannot be undone.') . '</p>',
    t('Delete'),
    t('Cancel'),
    'confirm'
  );

  return $form;
}

/**
 * Submit callback for thumbwhere_externalrepositorysync_delete_form
 */
function thumbwhere_externalrepositorysync_delete_form_submit($form, &$form_state) {
  $thumbwhere_externalrepositorysync = $form_state['thumbwhere_externalrepositorysync'];

  thumbwhere_externalrepositorysync_delete($thumbwhere_externalrepositorysync);

  drupal_set_message(t('The thumbwhere_externalrepositorysync %name has been deleted.', array('%name' => $thumbwhere_externalrepositorysync->pk_externalrepositorysync)));
  watchdog('thumbwhere_externalrepositorysync', 'Deleted thumbwhere_externalrepositorysync %name.', array('%name' => $thumbwhere_externalrepositorysync->pk_externalrepositorysync));

  $form_state['redirect'] = 'admin/thumbwhere/thumbwhere_externalrepositorysyncs';
}



/**
 * Page to add ThumbWhereExternalRepositorySync Entities.
 *
 * @todo Pass this through a proper theme function
 */
function thumbwhere_externalrepositorysync_add_page() {
  $controller = entity_ui_controller('thumbwhere_externalrepositorysync');
  return $controller->addPage();
}


/**
 * Displays the list of available thumbwhere_externalrepositorysync types for thumbwhere_externalrepositorysync creation.
 *
 * @ingroup themeable
 */
function theme_thumbwhere_externalrepositorysync_add_list($variables) {


  //$content = $variables['content'];
  $output = '';
  /*
  if ($content) {
    $output = '<dl class="thumbwhere_externalrepositorysync-type-list">';
    foreach ($content as $item) {
      $output .= '<dt>' . l($item['title'], $item['href']) . '</dt>';
      $output .= '<dd>' . filter_xss_admin($item['description']) . '</dd>';
    }
    $output .= '</dl>';
  }
  else {
    if (user_access('administer thumbwhere_externalrepositorysync types')) {
      $output = '<p>' . t('ThumbWhereExternalRepositorySync Entities cannot be added because you have not created any thumbwhere_externalrepositorysync types yet. Go to the <a href="@create-thumbwhere_externalrepositorysync-type">thumbwhere_externalrepositorysync type creation page</a> to add a new thumbwhere_externalrepositorysync type.', array('@create-thumbwhere_externalrepositorysync-type' => url('admin/structure/thumbwhere_externalrepositorysync_types/add'))) . '</p>';
    }
    else {
      $output = '<p>' . t('No thumbwhere_externalrepositorysync types have been created yet for you to use.') . '</p>';
    }
  }
  */
  return $output;
}





/**
 * Sets the breadcrumb for administrative thumbwhere_externalrepositorysync pages.
 */
function thumbwhere_externalrepositorysync_set_breadcrumb() {
  $breadcrumb = array(
    l(t('Home'), '<front>'),
    l(t('Administration'), 'admin'),
    l(t('ExternalRepositorySync'), 'admin/content'),
    l(t('ExternalRepositorySync'), 'admin/thumbwhere/thumbwhere_externalrepositorysyncs'),
  );

  drupal_set_breadcrumb($breadcrumb);
}



