<?php

// ******************************************************************
// This file was generated by autogen_model_implementation_drupal.xsl
// ******************************************************************

// Include the ThumbWhere SDK
require_once(dirname(__FILE__) . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . 'dependencies' . DIRECTORY_SEPARATOR . 'tw' . DIRECTORY_SEPARATOR . 'sdk.class.php');

/**
 * @file
 * Module for the ThumbWhere ExternalRepositorySync Entity
 *
 *
 * NOTE: This has been autogenerated.
 * 
 */

/**
 * Implement hook_entity_info().
 *
 * We define two entities here - the actual entity that will hold our domain
 * specific information and an entity that holds information about the different
 * types of entities. See here: http://drupal.org/node/977380 for a discussion on this
 * choice.
 */
function thumbwhere_externalrepositorysync_entity_info() {
  $return['thumbwhere_externalrepositorysync'] = array(

    // The human-readable name of the type.
    'label' => t('ThumbWhereExternalRepositorySync'),

    // (used by DrupalDefaultEntityController) The name of the entity type's base table.
    'base table' => 'thumbwhere_externalrepositorysync',

    // fieldable: Set to TRUE if you want your entity type to accept fields being attached to it.
    'fieldable' => TRUE,

  // Declare the endty keys..
    'entity keys' => array(
      'id' => 'pk_externalrepositorysync',
      'label' => 'pk_externalrepositorysync',
    ),

    // A function taking an entity and an entity type as arguments and returning the label of the entity.
    // The entity label is the main string associated with an entity; for example, the title of a node or
    // the subject of a comment. If there is an entity object property that defines the label, use the
    // 'label' element of the 'entity keys' return value component to provide this information (see below).
    // If more complex logic is needed to determine the label of an entity, you can instead specify a callback
    // function here, which will be called to determine the entity label. See also the entity_label()
    // function, which implements this logic.
    'label callback' => 'entity_class_label',

    // A function taking an entity as argument and returning the uri elements of the entity, e.g. 'path' and 'options'.
    // The actual entity uri can be constructed by passing these elements to url().
    'uri callback' => 'entity_class_uri',

    //
    // Entity API Components
    //

    // The module whwre we can find our classes
    'module' => 'thumbwhere_externalrepositorysync',

    // The entity class and controller class extend the classes provided by the Entity API
    'entity class' => 'ThumbWhereExternalRepositorySync',

    // Which Entity API contoller
    'controller class' => 'ThumbWhereExternalRepositorySyncController',

    // The information below is used by the ThumbWhereExternalRepositorySyncUIController (which extends the EntityDefaultUIController)
    'admin ui' => array(
      'path' => 'admin/thumbwhere/thumbwhere_externalrepositorysyncs',
      'file' => 'thumbwhere_externalrepositorysync.admin.inc',
      'controller class' => 'ThumbWhereExternalRepositorySyncUIController',
      'menu wildcard' => '%thumbwhere_externalrepositorysync',
    ),
  );

  return $return;
}


/**
 * Implements hook_permission().
 */
function thumbwhere_externalrepositorysync_permission() {

  // We set up permisssions to manage entity types, manage all entities and the
  // permissions for each individual entity
  $permissions = array(
    'administer thumbwhere_externalrepositorysyncs' => array(
      'title' => t('Administer thumbwhere_externalrepositorysyncs'),
      'description' => t('Edit and delete all thumbwhere_externalrepositorysyncs'),
    ),
  'send thumbwhere externalrepositorysync notifications' => array(
          'title' => t('Send ThumbWhere Notifications'),
          'description' => t('Allow the caller to call the ThumbWhere ExternalRepositorySync Entity Notifications Endpoint.'),
          'restrict access' => TRUE,
      ),    
  );

  return $permissions;
}


/**
 * Determines whether the given user has access to a thumbwhere_externalrepositorysync.
 *
 * @param $op
 *   The operation being performed. One of 'view', 'update', 'create', 'delete'
 *   or just 'edit' (being the same as 'create' or 'update').
 * @param $thumbwhere_externalrepositorysync
 *   Optionally a thumbwhere_externalrepositorysync or a thumbwhere_externalrepositorysync type to check access for. If nothing is
 *   given, access for all thumbwhere_externalrepositorysyncs is determined.
 * @param $account
 *   The user to check for. Leave it to NULL to check for the global user.
 * @return boolean
 *   Whether access is allowed or not.
 */
function thumbwhere_externalrepositorysync_access($op, $thumbwhere_externalrepositorysync = NULL, $account = NULL) {

  //if (user_access('administer thumbwhere_externalrepositorysyncs', $account)) {
  //  return TRUE;
 // }

  //return FALSE;
  return TRUE;
}




/**
 * Fetch a thumbwhere_externalrepositorysync object. Make sure that the wildcard you choose
 * in the thumbwhere_externalrepositorysync entity definition fits the function name here.
 *
 * @param $thumbwhere_externalrepositorysync_id
 *   Integer specifying the thumbwhere_externalrepositorysync id.
 * @param $reset
 *   A boolean indicating that the internal cache should be reset.
 * @return
 *   A fully-loaded $thumbwhere_externalrepositorysync object or FALSE if it cannot be loaded.
 *
 * @see thumbwhere_externalrepositorysync_load_multiple()
 */
function thumbwhere_externalrepositorysync_load($thumbwhere_externalrepositorysync_id, $reset = FALSE) {

  $thumbwhere_externalrepositorysyncs = entity_load('thumbwhere_externalrepositorysync', array($thumbwhere_externalrepositorysync_id), array(), $reset);

  return reset($thumbwhere_externalrepositorysyncs);
}


/**
 * Deletes a thumbwhere_externalrepositorysync.
 */
function thumbwhere_externalrepositorysync_delete(ThumbWhereExternalRepositorySync $thumbwhere_externalrepositorysync) {

  $thumbwhere_externalrepositorysync->delete();
}

/**
 * Create a thumbwhere_externalrepositorysync object.
 */
function thumbwhere_externalrepositorysync_create($values = array()) {

  return entity_get_controller('thumbwhere_externalrepositorysync')->create($values);
}


/**
 * URI callback for thumbwhere_externalrepositorysyncs
 */
function thumbwhere_externalrepositorysync_uri(ThumbWhereExternalRepositorySync $thumbwhere_externalrepositorysync) {

  return array(
    'path' => 'thumbwhere_externalrepositorysync/' . $thumbwhere_externalrepositorysync->pk_externalrepositorysync,
  );
}


/**
 * Menu title callback for showing individual entities
 */
function thumbwhere_externalrepositorysync_page_title(ThumbWhereExternalRepositorySync $thumbwhere_externalrepositorysync) {

	return $thumbwhere_externalrepositorysync->pk_externalrepositorysync;
}


/**
 * Sets up content to show an individual thumbwhere_externalrepositorysync
 * @todo - get rid of drupal_set_title();
 */
function thumbwhere_externalrepositorysync_page_view($thumbwhere_externalrepositorysync, $view_mode = 'full') {



  $controller = entity_get_controller('thumbwhere_externalrepositorysync');
  $content = $controller->view(array($thumbwhere_externalrepositorysync->pk_externalrepositorysync => $thumbwhere_externalrepositorysync));
  drupal_set_title($thumbwhere_externalrepositorysync->pk_externalrepositorysync);
  return $content;
}


/**
 * Implements hook_views_api().
 */
function thumbwhere_externalrepositorysync_views_api() {



  return array(
    'api' => 3,
    'path' => drupal_get_path('module', 'thumbwhere_externalrepositorysync') . '/views',
  );
}


/**
 * Implement hook_theme().
 */
function thumbwhere_externalrepositorysync_theme() {



  return array(
    'thumbwhere_externalrepositorysync_add_list' => array(
      'variables' => array('content' => array()),
      'file' => 'thumbwhere_externalrepositorysync.admin.inc',
    ),
    'thumbwhere_externalrepositorysync' => array(
      'render element' => 'elements',
      'template' => 'thumbwhere_externalrepositorysync',
    )
  );
}


/**
 * Implements hook_menu_local_tasks_alter().
 */
function thumbwhere_externalrepositorysync_menu_local_tasks_alter(&$data, $router_item, $root_path) {

  // Add action link 'admin/structure/thumbwhere_externalrepositorysyncs/add' on 'admin/structure/thumbwhere_externalrepositorysyncs'.
  if ($root_path == 'admin/thumbwhere/thumbwhere_externalrepositorysyncs') {
    $item = menu_get_item('admin/thumbwhere/thumbwhere_externalrepositorysyncs/add');
    if ($item['access']) {
      $data['actions']['output'][] = array(
        '#theme' => 'menu_local_action',
        '#link' => $item,
      );
    }
  }
}


/**
 * The class used for thumbwhere_externalrepositorysync entities
 */
class ThumbWhereExternalRepositorySync extends Entity {

  public function __construct($values = array()) {
    parent::__construct($values, 'thumbwhere_externalrepositorysync');
  }

  protected function defaultLabel() {
    return $this->pk_externalrepositorysync;
  }

  protected function defaultUri() {
    return array('path' => 'thumbwhere_externalrepositorysync/' . $this->pk_externalrepositorysync);
  }


}



/**
 * The Controller for ThumbWhereExternalRepositorySync entities
 */
class ThumbWhereExternalRepositorySyncController extends EntityAPIController {
  public function __construct($entityType) {
    parent::__construct($entityType);
  }
}



/**
* Implements hook_entity_presave().
*
* Act on an ExternalRepositorySync entity before it is about to be created or updated.
*
* @param $entity
*   The entity object.
* @param $entity_type
*   The type of entity being saved (i.e. node, user, comment).
*/
function thumbwhere_externalrepositorysync_entity_presave($entity, $entity_type) {

  // We only look after what we want...
  if ($entity_type == 'thumbwhere_externalrepositorysync')  {
  	
  	
  	
 	/////////////////////////////////////////////////////
    //
    // Should we not propagate this to ThumbWhere? 
    //
    if (isset($entity->do_not_propagate)) {
      if ($entity->do_not_propagate) {
        if (twCanDebug()) {
          if ($entity->is_new) {
            if (twCanDebug()){
              debug("DO NOT PROPAGATE 'externalrepositorysync' presave *CREATE*");
          	}
          }
          else {
            if (twCanDebug()) {
              debug("DO NOT PROPAGATE 'externalrepositorysync' presave *UPDATE*");
            }
          }
        }

        // To cause an infinite loop of updates, comment out this line.
        return;
      }
      else if (twCanDebug()) {
        debug("OK PROAOGATE 'externalrepositorysync' presave to ThumbWhere Server");
      }
    }
    else if (twCanDebug()) {
      debug("DO NOT PROPAGATE IS NOT SET");
    }
    //
    //
    ////////////////////////////////////////////////////////  	

  
    // .. then we create a new thumbwhere_externalrepositorysync entity...
      // Instantiate the ThumbWhereContent class

    $twAdminAPI = new ThumbWhereAPIAdmin();

    //if (twCanDebug()) debug($twContentAPI);

    //if (twCanDebug()) debug($entity);

    //
    // Collate and validate our fields
    //

    $fields = array();
    // Compulsory so..
    if (!isset($entity->fk_externalsubscription)) {
      throw new Exception('externalsubscription is mandatory.');
    }
    else {
      $fields['externalsubscription'] = $entity->fk_externalsubscription;
    }
    // Compulsory so..
    if (!isset($entity->last_sync)) {
      throw new Exception('last_sync is mandatory.');
    }
    else {
      $fields['last_sync'] = $entity->last_sync;
    }
    // Compulsory so..
    if (!isset($entity->max)) {
      throw new Exception('max is mandatory.');
    }
    else {
      $fields['max'] = $entity->max;
    }

    // Not nullable but we have a default value
    if (!isset($entity->complete)) {
      $fields['complete'] = 0;
    }
    else {
      $fields['complete'] = $entity->complete;
    }
		
	//
	// Ensure that we have the 'thumbwhere_api_key' as we are going to pass that through in a context
	//
    if (variable_get('thumbwhere_api_key') == '') {
    	throw new Exception('ThumbWhere resource for \'ExternalRepositorySync\' requires that variable \'thumbwhere_api_key\' is set.');
    }
    
              		
    //
    // If this externalrepositorysync entity is new....
    //
               
    if ($entity->is_new) {
                

      //
      // ... then we create the externalrepositorysync entity in ThumbWhere
      //

      $externalrepositorysync = $twAdminAPI->create_external_repository_sync($context = array(
                  'key' => variable_get('thumbwhere_api_key')
          ), $fields);
            
       if (twCanDebug()) debug($externalrepositorysync);

      //if (twCanDebug()) debug($external_repository_sync);

      $attributes = $externalrepositorysync->body->external_repository_sync->attributes();

      if (twCanDebug()) debug('newly created id = ' . $attributes['id']);

      // Assign new id.
      $entity->pk_externalrepositorysync = intval($attributes['id']);

      if (twCanDebug()) debug('$entity->pk_externalrepositorysync = ' . $entity->pk_externalrepositorysync);
    }
    else
    {
          	

      //
      // .. otherwise we update the externalrepositorysync entity in ThumbWhere
      //

      $externalrepositorysync = $twAdminAPI->update_external_repository_sync($id=$entity->pk_externalrepositorysync,$context = array(
                  'key' => variable_get('thumbwhere_api_key')
          ), $fields);
            
        
    }
          	
    
  }
}

/**
 * Implements thumbwhere_externalrepositorysync_entity_insert().
 *
 * Act on entities when inserted.
 *
 * @param $entity
 *   The entity object
 * @param $type
 *   The type of entity being inserted (i.e. node, user, comment).
 */
function thumbwhere_externalrepositorysync_entity_insert($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_externalrepositorysync') return;

}


/**
* Implements thumbwhere_externalrepositorysync_entity_delete().
*
* Act on entities when inserted.
*
* @param $entity
*   The entity object
* @param $type
*   The type of entity being deleted (i.e. node, user, comment).
*/
function thumbwhere_externalrepositorysync_entity_delete($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_externalrepositorysync') return;


}
