<?php

// ******************************************************************
// This file was generated by autogen_model_implementation_drupal.xsl
// ******************************************************************

// Include the ThumbWhere SDK
require_once(dirname(__FILE__) . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . 'dependencies' . DIRECTORY_SEPARATOR . 'tw' . DIRECTORY_SEPARATOR . 'sdk.class.php');

/**
 * @file
 * Module for the ThumbWhere HostSubRoleType Entity
 *
 *
 * NOTE: This has been autogenerated.
 * 
 */

/**
 * Implement hook_entity_info().
 *
 * We define two entities here - the actual entity that will hold our domain
 * specific information and an entity that holds information about the different
 * types of entities. See here: http://drupal.org/node/977380 for a discussion on this
 * choice.
 */
function thumbwhere_hostsubroletype_entity_info() {
  $return['thumbwhere_hostsubroletype'] = array(

    // The human-readable name of the type.
    'label' => t('ThumbWhereHostSubRoleType'),

    // (used by DrupalDefaultEntityController) The name of the entity type's base table.
    'base table' => 'thumbwhere_hostsubroletype',

    // fieldable: Set to TRUE if you want your entity type to accept fields being attached to it.
    'fieldable' => TRUE,

  // Declare the endty keys..
    'entity keys' => array(
      'id' => 'pk_hostsubroletype',
      'label' => 'name',
    ),

    // A function taking an entity and an entity type as arguments and returning the label of the entity.
    // The entity label is the main string associated with an entity; for example, the title of a node or
    // the subject of a comment. If there is an entity object property that defines the label, use the
    // 'label' element of the 'entity keys' return value component to provide this information (see below).
    // If more complex logic is needed to determine the label of an entity, you can instead specify a callback
    // function here, which will be called to determine the entity label. See also the entity_label()
    // function, which implements this logic.
    'label callback' => 'entity_class_label',

    // A function taking an entity as argument and returning the uri elements of the entity, e.g. 'path' and 'options'.
    // The actual entity uri can be constructed by passing these elements to url().
    'uri callback' => 'entity_class_uri',

    //
    // Entity API Components
    //

    // The module whwre we can find our classes
    'module' => 'thumbwhere_hostsubroletype',

    // The entity class and controller class extend the classes provided by the Entity API
    'entity class' => 'ThumbWhereHostSubRoleType',

    // Which Entity API contoller
    'controller class' => 'ThumbWhereHostSubRoleTypeController',

    // The information below is used by the ThumbWhereHostSubRoleTypeUIController (which extends the EntityDefaultUIController)
    'admin ui' => array(
      'path' => 'admin/thumbwhere/thumbwhere_hostsubroletypes',
      'file' => 'thumbwhere_hostsubroletype.admin.inc',
      'controller class' => 'ThumbWhereHostSubRoleTypeUIController',
      'menu wildcard' => '%thumbwhere_hostsubroletype',
    ),
  );

  return $return;
}


/**
 * Implements hook_permission().
 */
function thumbwhere_hostsubroletype_permission() {

  // We set up permisssions to manage entity types, manage all entities and the
  // permissions for each individual entity
  $permissions = array(
    'administer thumbwhere_hostsubroletypes' => array(
      'title' => t('Administer thumbwhere_hostsubroletypes'),
      'description' => t('Edit and delete all thumbwhere_hostsubroletypes'),
    ),
  'send thumbwhere hostsubroletype notifications' => array(
          'title' => t('Send ThumbWhere Notifications'),
          'description' => t('Allow the caller to call the ThumbWhere HostSubRoleType Entity Notifications Endpoint.'),
          'restrict access' => TRUE,
      ),    
  );

  return $permissions;
}


/**
 * Determines whether the given user has access to a thumbwhere_hostsubroletype.
 *
 * @param $op
 *   The operation being performed. One of 'view', 'update', 'create', 'delete'
 *   or just 'edit' (being the same as 'create' or 'update').
 * @param $thumbwhere_hostsubroletype
 *   Optionally a thumbwhere_hostsubroletype or a thumbwhere_hostsubroletype type to check access for. If nothing is
 *   given, access for all thumbwhere_hostsubroletypes is determined.
 * @param $account
 *   The user to check for. Leave it to NULL to check for the global user.
 * @return boolean
 *   Whether access is allowed or not.
 */
function thumbwhere_hostsubroletype_access($op, $thumbwhere_hostsubroletype = NULL, $account = NULL) {

  //if (user_access('administer thumbwhere_hostsubroletypes', $account)) {
  //  return TRUE;
 // }

  //return FALSE;
  return TRUE;
}




/**
 * Fetch a thumbwhere_hostsubroletype object. Make sure that the wildcard you choose
 * in the thumbwhere_hostsubroletype entity definition fits the function name here.
 *
 * @param $thumbwhere_hostsubroletype_id
 *   Integer specifying the thumbwhere_hostsubroletype id.
 * @param $reset
 *   A boolean indicating that the internal cache should be reset.
 * @return
 *   A fully-loaded $thumbwhere_hostsubroletype object or FALSE if it cannot be loaded.
 *
 * @see thumbwhere_hostsubroletype_load_multiple()
 */
function thumbwhere_hostsubroletype_load($thumbwhere_hostsubroletype_id, $reset = FALSE) {

  $thumbwhere_hostsubroletypes = entity_load('thumbwhere_hostsubroletype', array($thumbwhere_hostsubroletype_id), array(), $reset);

  return reset($thumbwhere_hostsubroletypes);
}


/**
 * Deletes a thumbwhere_hostsubroletype.
 */
function thumbwhere_hostsubroletype_delete(ThumbWhereHostSubRoleType $thumbwhere_hostsubroletype) {

  $thumbwhere_hostsubroletype->delete();
}

/**
 * Create a thumbwhere_hostsubroletype object.
 */
function thumbwhere_hostsubroletype_create($values = array()) {

  return entity_get_controller('thumbwhere_hostsubroletype')->create($values);
}


/**
 * URI callback for thumbwhere_hostsubroletypes
 */
function thumbwhere_hostsubroletype_uri(ThumbWhereHostSubRoleType $thumbwhere_hostsubroletype) {

  return array(
    'path' => 'thumbwhere_hostsubroletype/' . $thumbwhere_hostsubroletype->pk_hostsubroletype,
  );
}


/**
 * Menu title callback for showing individual entities
 */
function thumbwhere_hostsubroletype_page_title(ThumbWhereHostSubRoleType $thumbwhere_hostsubroletype) {

	return $thumbwhere_hostsubroletype->name;
}


/**
 * Sets up content to show an individual thumbwhere_hostsubroletype
 * @todo - get rid of drupal_set_title();
 */
function thumbwhere_hostsubroletype_page_view($thumbwhere_hostsubroletype, $view_mode = 'full') {



  $controller = entity_get_controller('thumbwhere_hostsubroletype');
  $content = $controller->view(array($thumbwhere_hostsubroletype->pk_hostsubroletype => $thumbwhere_hostsubroletype));
  drupal_set_title($thumbwhere_hostsubroletype->name);
  return $content;
}


/**
 * Implements hook_views_api().
 */
function thumbwhere_hostsubroletype_views_api() {



  return array(
    'api' => 3,
    'path' => drupal_get_path('module', 'thumbwhere_hostsubroletype') . '/views',
  );
}


/**
 * Implement hook_theme().
 */
function thumbwhere_hostsubroletype_theme() {



  return array(
    'thumbwhere_hostsubroletype_add_list' => array(
      'variables' => array('content' => array()),
      'file' => 'thumbwhere_hostsubroletype.admin.inc',
    ),
    'thumbwhere_hostsubroletype' => array(
      'render element' => 'elements',
      'template' => 'thumbwhere_hostsubroletype',
    )
  );
}


/**
 * Implements hook_menu_local_tasks_alter().
 */
function thumbwhere_hostsubroletype_menu_local_tasks_alter(&$data, $router_item, $root_path) {

  // Add action link 'admin/structure/thumbwhere_hostsubroletypes/add' on 'admin/structure/thumbwhere_hostsubroletypes'.
  if ($root_path == 'admin/thumbwhere/thumbwhere_hostsubroletypes') {
    $item = menu_get_item('admin/thumbwhere/thumbwhere_hostsubroletypes/add');
    if ($item['access']) {
      $data['actions']['output'][] = array(
        '#theme' => 'menu_local_action',
        '#link' => $item,
      );
    }
  }
}


/**
 * The class used for thumbwhere_hostsubroletype entities
 */
class ThumbWhereHostSubRoleType extends Entity {

  public function __construct($values = array()) {
    parent::__construct($values, 'thumbwhere_hostsubroletype');
  }

  protected function defaultLabel() {
    return $this->name;
  }

  protected function defaultUri() {
    return array('path' => 'thumbwhere_hostsubroletype/' . $this->pk_hostsubroletype);
  }


}



/**
 * The Controller for ThumbWhereHostSubRoleType entities
 */
class ThumbWhereHostSubRoleTypeController extends EntityAPIController {
  public function __construct($entityType) {
    parent::__construct($entityType);
  }
}



/**
* Implements hook_entity_presave().
*
* Act on an HostSubRoleType entity before it is about to be created or updated.
*
* @param $entity
*   The entity object.
* @param $entity_type
*   The type of entity being saved (i.e. node, user, comment).
*/
function thumbwhere_hostsubroletype_entity_presave($entity, $entity_type) {

  // We only look after what we want...
  if ($entity_type == 'thumbwhere_hostsubroletype')  {
  	
 	/////////////////////////////////////////////////////
    //
    // Should we not propagate this to ThumbWhere? 
    //
    if (isset($entity->do_not_propagate)) {
      if ($entity->do_not_propagate) {
        if (twCanDebug()) {
          if ($entity->is_new) {
            if (twCanDebug()){
              debug("DO NOT PROPAGATE 'hostsubroletype' presave *CREATE* not being sent to ThumbWhere server.");
          	}
          }
          else {
            if (twCanDebug()) {
              debug("DO NOT PROPAGATE 'hostsubroletype' presave *UPDATE* not being sent to ThumbWhere server.");
            }
          }
        }

        // To cause an infinite loop of updates, comment out this line.
        return;
      }
      else if (twCanDebug()) {
        debug("OK PROAOGATE 'hostsubroletype' presave to ThumbWhere server.");
      }
    }
    else if (twCanDebug()) {
      debug("DO NOT PROPAGATE IS NOT SET");
    }
    //
    //
    ////////////////////////////////////////////////////////  	

  
    // .. then we create a new thumbwhere_hostsubroletype entity...
      // Instantiate the ThumbWhereContent class

    $twAdminAPI = new ThumbWhereAPIAdmin();

    //if (twCanDebug()) debug($twContentAPI);

    //if (twCanDebug()) debug($entity);

    //
    // Collate and validate our fields
    //

    $fields = array();
    // Compulsory so..
    if (empty($entity->name)) {
      throw new Exception('name is mandatory.');
    }
    else {
      $fields['name'] = $entity->name;
    }

    // Nullable so we only pass it through to create_host_role_subrole_type if we have it..
    if (!empty($entity->config_xsl)) {
      $fields['config_xsl'] = $entity->config_xsl;
    }
		
	//
	// Ensure that we have the 'thumbwhere_api_key' as we are going to pass that through in a context
	//
    if (variable_get('thumbwhere_api_key') == '') {
    	throw new Exception('ThumbWhere resource for \'HostSubRoleType\' requires that variable \'thumbwhere_api_key\' is set.');
    }
    
              		
    //
    // If this hostsubroletype entity is new....
    //
               
    if ($entity->is_new) {
                

      //
      // ... then we create the hostsubroletype entity in ThumbWhere
      //

      $hostsubroletype = $twAdminAPI->create_host_role_subrole_type($context = array(
                  'key' => variable_get('thumbwhere_api_key')
          ), $fields);
            
       if (twCanDebug()) debug($hostsubroletype);

      //if (twCanDebug()) debug($host_role_subrole_type);

      $attributes = $hostsubroletype->body->host_role_subrole_type->attributes();

      if (twCanDebug()) debug('newly created id = ' . $attributes['id']);

      // Assign new id.
      $entity->pk_hostsubroletype = intval($attributes['id']);

      if (twCanDebug()) debug('$entity->pk_hostsubroletype = ' . $entity->pk_hostsubroletype);
    }
    else
    {
          	

      //
      // .. otherwise we update the hostsubroletype entity in ThumbWhere
      //

      $hostsubroletype = $twAdminAPI->update_host_role_subrole_type($id=$entity->pk_hostsubroletype,$context = array(
                  'key' => variable_get('thumbwhere_api_key')
          ), $fields);
            
        
    }
          	
    
  }
}

/**
 * Implements thumbwhere_hostsubroletype_entity_insert().
 *
 * Act on entities when inserted.
 *
 * @param $entity
 *   The entity object
 * @param $type
 *   The type of entity being inserted (i.e. node, user, comment).
 */
function thumbwhere_hostsubroletype_entity_insert($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_hostsubroletype') return;

}


/**
* Implements thumbwhere_hostsubroletype_entity_delete().
*
* Act on entities when inserted.
*
* @param $entity
*   The entity object
* @param $type
*   The type of entity being deleted (i.e. node, user, comment).
*/
function thumbwhere_hostsubroletype_entity_delete($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_hostsubroletype') return;


}
