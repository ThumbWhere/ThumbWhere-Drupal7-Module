<?php

// ******************************************************************
// This file was generated by autogen_model_implementation_drupal.xsl
// ******************************************************************

// Include the ThumbWhere SDK
require_once(dirname(__FILE__) . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . 'dependencies' . DIRECTORY_SEPARATOR . 'tw' . DIRECTORY_SEPARATOR . 'sdk.class.php');

/**
 * @file
 * Module for the ThumbWhere Thing Entity
 *
 *
 * NOTE: This has been autogenerated.
 * 
 */

/**
 * Implement hook_entity_info().
 *
 * We define two entities here - the actual entity that will hold our domain
 * specific information and an entity that holds information about the different
 * types of entities. See here: http://drupal.org/node/977380 for a discussion on this
 * choice.
 */
function thumbwhere_thing_entity_info() {
  $return['thumbwhere_thing'] = array(

    // The human-readable name of the type.
    'label' => t('ThumbWhereThing'),

    // (used by DrupalDefaultEntityController) The name of the entity type's base table.
    'base table' => 'thumbwhere_thing',

    // fieldable: Set to TRUE if you want your entity type to accept fields being attached to it.
    'fieldable' => TRUE,

  // Declare the endty keys..
    'entity keys' => array(
      'id' => 'pk_thing',
      'label' => 'pk_thing',
    ),

    // A function taking an entity and an entity type as arguments and returning the label of the entity.
    // The entity label is the main string associated with an entity; for example, the title of a node or
    // the subject of a comment. If there is an entity object property that defines the label, use the
    // 'label' element of the 'entity keys' return value component to provide this information (see below).
    // If more complex logic is needed to determine the label of an entity, you can instead specify a callback
    // function here, which will be called to determine the entity label. See also the entity_label()
    // function, which implements this logic.
    'label callback' => 'entity_class_label',

    // A function taking an entity as argument and returning the uri elements of the entity, e.g. 'path' and 'options'.
    // The actual entity uri can be constructed by passing these elements to url().
    'uri callback' => 'entity_class_uri',

    //
    // Entity API Components
    //

    // The module whwre we can find our classes
    'module' => 'thumbwhere_thing',

    // The entity class and controller class extend the classes provided by the Entity API
    'entity class' => 'ThumbWhereThing',

    // Which Entity API contoller
    'controller class' => 'ThumbWhereThingController',

    // The information below is used by the ThumbWhereThingUIController (which extends the EntityDefaultUIController)
    'admin ui' => array(
      'path' => 'admin/content/thumbwhere_things',
      'file' => 'thumbwhere_thing.admin.inc',
      'controller class' => 'ThumbWhereThingUIController',
      'menu wildcard' => '%thumbwhere_thing',
    ),
  );

  return $return;
}


/**
 * Implements hook_permission().
 */
function thumbwhere_thing_permission() {

  // We set up permisssions to manage entity types, manage all entities and the
  // permissions for each individual entity
  $permissions = array(
    'administer thumbwhere_things' => array(
      'title' => t('Administer thumbwhere_things'),
      'description' => t('Edit and delete all thumbwhere_things'),
    ),
  'send thumbwhere thing notifications' => array(
          'title' => t('Send ThumbWhere Notifications'),
          'description' => t('Allow the caller to call the ThumbWhere Thing Entity Notifications Endpoint.'),
          'restrict access' => TRUE,
      ),    
  );

  return $permissions;
}


/**
 * Determines whether the given user has access to a thumbwhere_thing.
 *
 * @param $op
 *   The operation being performed. One of 'view', 'update', 'create', 'delete'
 *   or just 'edit' (being the same as 'create' or 'update').
 * @param $thumbwhere_thing
 *   Optionally a thumbwhere_thing or a thumbwhere_thing type to check access for. If nothing is
 *   given, access for all thumbwhere_things is determined.
 * @param $account
 *   The user to check for. Leave it to NULL to check for the global user.
 * @return boolean
 *   Whether access is allowed or not.
 */
function thumbwhere_thing_access($op, $thumbwhere_thing = NULL, $account = NULL) {

  //if (user_access('administer thumbwhere_things', $account)) {
  //  return TRUE;
 // }

  //return FALSE;
  return TRUE;
}




/**
 * Fetch a thumbwhere_thing object. Make sure that the wildcard you choose
 * in the thumbwhere_thing entity definition fits the function name here.
 *
 * @param $thumbwhere_thing_id
 *   Integer specifying the thumbwhere_thing id.
 * @param $reset
 *   A boolean indicating that the internal cache should be reset.
 * @return
 *   A fully-loaded $thumbwhere_thing object or FALSE if it cannot be loaded.
 *
 * @see thumbwhere_thing_load_multiple()
 */
function thumbwhere_thing_load($thumbwhere_thing_id, $reset = FALSE) {

  //

  $thumbwhere_things = entity_load('thumbwhere_thing', array($thumbwhere_thing_id), array(), $reset);

  return reset($thumbwhere_things);
}





/**
 * Load multiple thumbwhere_things based on certain conditions.
 *
 * @param $thumbwhere_thing_ids
 *   An array of thumbwhere_thing IDs.
 * @param $conditions
 *   An array of conditions to match against the {thumbwhere_thing} table.
 * @param $reset
 *   A boolean indicating that the internal cache should be reset.
 * @return
 *   An array of thumbwhere_thing objects, indexed by thumbwhere_thing_id.
 *
 * @see entity_load()
 * @see thumbwhere_thing_load()
 */
/*
function thumbwhere_thing_load_multiple($thumbwhere_thing_ids = array(), $conditions = array(), $reset = FALSE) {



  $loaded = entity_load('thumbwhere_thing', $thumbwhere_thing_ids, $conditions, $reset);

  return $loaded;
}
*/

/**
 * Deletes a thumbwhere_thing.
 */
function thumbwhere_thing_delete(ThumbWhereThing $thumbwhere_thing) {

  $thumbwhere_thing->delete();
}


/**
 * Delete multiple thumbwhere_things.
 *
 * @param $thumbwhere_thing_ids
 *   An array of thumbwhere_thing IDs.
 */
/*
function thumbwhere_thing_delete_multiple(array $thumbwhere_thing_ids) {



  entity_get_controller('thumbwhere_thing')->delete($thumbwhere_thing_ids);
}
*/

/**
 * Create a thumbwhere_thing object.
 */
function thumbwhere_thing_create($values = array()) {

  return entity_get_controller('thumbwhere_thing')->create($values);
}


/**
 * Saves a thumbwhere_thing to the database.
 *
 * @param $thumbwhere_thing
 *   The thumbwhere_thing object.
 */
/*
function thumbwhere_thing_save(ThumbWhereThing $thumbwhere_thing) {



  return $thumbwhere_thing->save();
}*/



/**
 * Insert a thumbwhere_thing
 *
 * @param $thumbwhere_thing
 *   The thumbwhere_thing object.
 */
/*
function thumbwhere_thing_insert(array $values = array()) {



  //return $thumbwhere_thing->create($values);
  return entity_get_controller('thumbwhere_thing')->insert($values);
}
*/






/**
 * URI callback for thumbwhere_things
 */
function thumbwhere_thing_uri(ThumbWhereThing $thumbwhere_thing) {



  return array(
    'path' => 'thumbwhere_thing/' . $thumbwhere_thing->pk_thing,
  );
}


/**
 * Menu title callback for showing individual entities
 */
function thumbwhere_thing_page_title(ThumbWhereThing $thumbwhere_thing) {

  

  return $thumbwhere_thing->pk_thing;
}


/**
 * Sets up content to show an individual thumbwhere_thing
 * @todo - get rid of drupal_set_title();
 */
function thumbwhere_thing_page_view($thumbwhere_thing, $view_mode = 'full') {



  $controller = entity_get_controller('thumbwhere_thing');
  $content = $controller->view(array($thumbwhere_thing->pk_thing => $thumbwhere_thing));
  drupal_set_title($thumbwhere_thing->pk_thing);
  return $content;
}


/**
 * Implements hook_views_api().
 */
function thumbwhere_thing_views_api() {



  return array(
    'api' => 3,
    'path' => drupal_get_path('module', 'thumbwhere_thing') . '/views',
  );
}


/**
 * Implement hook_theme().
 */
function thumbwhere_thing_theme() {



  return array(
    'thumbwhere_thing_add_list' => array(
      'variables' => array('content' => array()),
      'file' => 'thumbwhere_thing.admin.inc',
    ),
    'thumbwhere_thing' => array(
      'render element' => 'elements',
      'template' => 'thumbwhere_thing',
    )
  );
}


/**
 * Implements hook_menu_local_tasks_alter().
 */
function thumbwhere_thing_menu_local_tasks_alter(&$data, $router_item, $root_path) {

  // Add action link 'admin/structure/thumbwhere_things/add' on 'admin/structure/thumbwhere_things'.
  if ($root_path == 'admin/content/thumbwhere_things') {
    $item = menu_get_item('admin/content/thumbwhere_things/add');
    if ($item['access']) {
      $data['actions']['output'][] = array(
        '#theme' => 'menu_local_action',
        '#link' => $item,
      );
    }
  }
}


/**
 * The class used for thumbwhere_thing entities
 */
class ThumbWhereThing extends Entity {

  public function __construct($values = array()) {
    parent::__construct($values, 'thumbwhere_thing');
  }

  protected function defaultLabel() {
    return $this->pk_thing;
  }

  protected function defaultUri() {
    return array('path' => 'thumbwhere_thing/' . $this->pk_thing);
  }


}



/**
 * The Controller for ThumbWhereThing entities
 */
class ThumbWhereThingController extends EntityAPIController {
  public function __construct($entityType) {
    parent::__construct($entityType);
  }


  /**
   * Create a thumbwhere_thing - we first set up the values that are specific
   * to our thumbwhere_thing schema but then also go through the EntityAPIController
   * function.
   *
   * @param $type
   *   The machine-readable type of the thumbwhere_thing.
   *
   * @return
   *   A thumbwhere_thing object with all default fields initialized.
   */
  /*
  public function create(array $values = array()) {



    //$values += array(
    //  'thumbwhere_id' => -1,
    //);

    return parent::create($values);
  }
  */


  /**
  * view
  * to our thumbwhere_thing schema but then also go through the EntityAPIController
  * function.
  *
  * @param $type
  *   The machine-readable type of the thumbwhere_thing.
  *
  * @return
  *   A thumbwhere_thing object with all default fields initialized.
  */
  /*
  public function view($entities, $view_mode = 'full', $langcode = NULL, $page = NULL) {



    $view = parent::view($entities, $view_mode, $langcode, $page);

    return $view;
  }
  */




  /**
  * query - Builds and executes the query for loading.
  *
  * @param $ids
  *   Array of ids
  *
  * @param $conditions
  *
  *
  *
  * @return
  *   A thumbwhere_thing object with all default fields initialized.
  */
  /*
  public function load($ids = array(), $conditions = array()) {



    $result = parent::load($ids, $conditions);

    //$result[1]->token = "my token";

    return $result;
  }
  */

  /**
  * Invoke
  *
  * @param $ids
  *   Array of ids
  *
  * @param $transaction
  *   If not null then the transaction this is taking part in
  *
  * @return
  *   A thumbwhere_thing object with all default fields initialized.
  */
  /*
  public function invoke($hook, $entity) {



    parent::invoke($hook, $entity);

  }
  */


  /**
  * Delete a thumbwhere_thing.
  *
  * @param $ids
  *   Array of ids
  *
  * @param $transaction
  *   If not null then the transaction this is taking part in
  *
  * @return
  *   A thumbwhere_thing object with all default fields initialized.
  */
  /*
  public function delete($ids, DatabaseTransaction $transaction = NULL) {



    parent::delete($ids, $transaction);

  }
  */


  /**
  * Save a thumbwhere_thing.
  *
  * @param $thumbwhere_thing
  *   The thumbwhere_thing being saved
  *
  * @param $transaction
  *   If not null then the transaction this is taking part in
  *
  * @return
  *   A thumbwhere_thing object with all default fields initialized.
  */
  /*
  public function save($thumbwhere_thing, DatabaseTransaction $transaction = NULL) {



    return parent::save($thumbwhere_thing, $transaction);
  }
  */


  /**
  * resetCache - Called when the cache is
  *
  * @param $ids
  *   Array of ids
  *
  * @return
  *   void
  */
  /*
  public function resetCache(array $ids = NULL) {



    parent::resetCache($ids);
  }
  */


  /**
  * query - Builds and executes the query for loading.
  *
  * @param $ids
  *   Array of ids
  *
  * @param $conditions
  *
  * @param $revision_id
  *
  *
  * @return
  *   A thumbwhere_thing object with all default fields initialized.
  */
  /*
  public function query($ids, $conditions, $revision_id = FALSE) {



    $result= parent::query($ids, $conditions, $revision_id);

    return $result;
  }
  */


  /**
   * Overriding the buldThing function to add entity specific fields
   */
  /*
  public function buildThing($entity, $view_mode = 'full', $langcode = NULL, $content = array()) {

    $content = parent::buildThing($entity, $view_mode, $langcode, $content);

    $content['thumbwhere_thing_sample_data'] =  array(
      '#markup' => theme('thumbwhere_thing_sample_data', array('thumbwhere_thing_sample_data' => check_plain($entity->data['sample_data']), 'thumbwhere_thing' => $entity)),
    );

    return $content;
  }*/
}


/**
 * Implements hook_field_storage_load().
 *
 * Load field from storage
 *
 * @todo This needs to be improved
 */
/*
function thumbwhere_thing_field_storage_load($entity_type, $entities, $age, $fields, $options) {


}
*/



/**
* Implements hook_entity_presave().
*
* Act on an Thing entity before it is about to be created or updated.
*
* @param $entity
*   The entity object.
* @param $entity_type
*   The type of entity being saved (i.e. node, user, comment).
*/
function thumbwhere_thing_entity_presave($entity, $entity_type) {

  // We only look after what we want...
  if ($entity_type == 'thumbwhere_thing')  {

    // If this is new
    if ($entity->is_new) {
  
      // .. then we create a new thumbwhere_thing entity...
      // .. then we create a new thumbwhere_Thing entity...
      // Instantiate the ThumbWhereContent class

      $twContentAPI = new ThumbWhereAPIContent();

      //if (twCanDebug()) debug($twContentAPI);

      //if (twCanDebug()) debug($entity);

      //
      // Collate and validate our fields
      //

      $fields = array();

      // Nullable so we only pass it through to create_thing if we have it..
      if (!empty($entity->fk_mediaitem)) {
        $fields['mediaitem'] = $entity->fk_mediaitem;
      }

      // Nullable so we only pass it through to create_thing if we have it..
      if (!empty($entity->fk_content)) {
        $fields['content'] = $entity->fk_content;
      }

      // Nullable so we only pass it through to create_thing if we have it..
      if (!empty($entity->fk_contentcollection)) {
        $fields['contentcollection'] = $entity->fk_contentcollection;
      }

      //
      // Now create the Entity in ThumbWhere
      //

      $thing = $twContentAPI->create_thing($context = array(
                  'key' => variable_get('thumbwhere_api_key')
          ), $fields);
            
       if (twCanDebug()) debug($thing);

      //if (twCanDebug()) debug($thing);

      $attributes = $thing->body->thing->attributes();

      if (twCanDebug()) debug('newly created id = ' . $attributes['id']);

      // Assign new id.
      $entity->pk_thing = intval($attributes['id']);

      if (twCanDebug()) debug('$entity->pk_thing = ' . $entity->pk_thing);
    }
    else
    {
        // .. otherwise we calculate the changes and ship them off to thumbwhere
    }
  }
}

/**
 * Implements thumbwhere_thing_entity_insert().
 *
 * Act on entities when inserted.
 *
 * @param $entity
 *   The entity object
 * @param $type
 *   The type of entity being inserted (i.e. node, user, comment).
 */
function thumbwhere_thing_entity_insert($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_thing') return;

}


/**
* Implements thumbwhere_thing_entity_delete().
*
* Act on entities when inserted.
*
* @param $entity
*   The entity object
* @param $type
*   The type of entity being deleted (i.e. node, user, comment).
*/
function thumbwhere_thing_entity_delete($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_thing') return;


}
