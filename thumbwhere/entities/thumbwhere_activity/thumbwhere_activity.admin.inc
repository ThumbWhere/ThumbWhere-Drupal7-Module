<?php

/**
 * @file
 * ThumbWhereActivity editing UI.
 *
 * We make very little use of the EntityAPI interface for this - preferring instead to use
 * views. That offers more flexibility to change a UI that will, more often than not,
 * be end-user facing.
 *
 * NOTE: This file was generated by autogen_model_implementation_drupal.xsl
 */

 
/**
 * Handle the notification for content_ingext
 */
function _thumbwhere_handle_activity_notify_xml($notification) {

  //
  // Get the operation
  //

  if (!isset($notification->attributes()->op)) {
    drupal_add_http_header('Status', '501 unable to fulfull request..');
    $err = 'We were expecting an \'op\', attribute on the \'activity\', element.';
    print($err);
   	watchdog('thumbwhere_activity', $err ,array(), WATCHDOG_ERROR);
    exit(0);
  }

  $op = $notification->attributes()->op;

  if (twCanDebug()) debug('op');
  if (twCanDebug()) debug($op);

  //
  // Get the context
  //

  if (!isset($notification->attributes()->key)) {
    drupal_add_http_header('Status', '412 unable to fulfull request..');
    $err = 'We were expecting a \'key\', attribute on the \'activity\', element.';
    print($err);
    watchdog('thumbwhere_activity', $err ,array(), WATCHDOG_ERROR);
    exit(0);
  }

  $context_key =  $notification->attributes()->key;

  //
  // Now based on the operation..
  //

  switch($op)
  {
  
  
      case 'create':
      {
      		
		
		//
		// We want to time this
		//
			
		$start_time = microtime(TRUE);
			
        //
        // What resource are we creating?
        //
      
        //
        // Get the id
        //
		
        if (!isset($notification->attributes()->id)) {
          drupal_add_http_header('Status', '412 Unable to fulfull request.');
          $err = 'We were expecting an \'id\', attribute on the \'activity\', element when we are performing an \'update\' notification..';
          print($err);
          watchdog('thumbwhere_activity', $err ,array(), WATCHDOG_ERROR);
          exit(0);
        }

        $id = intval($notification->attributes()->id);
		

        if (twCanDebug()) debug('id');
        if (twCanDebug()) debug($id);

      
        //
        // Get the fields
        //
          
        $entities = entity_load('thumbwhere_activity', array($id));

        if (twCanDebug()) debug('$entities');
        if (twCanDebug()) debug($entities);

        if (count($entities) != 0) {
          //drupal_add_http_header('Status', '409 Resource already exists.');
          $err = '\'activity\' resource already exists with an id of \'' . $id . '\'.';
           //print($err);
          watchdog('thumbwhere_activity', $err ,array(), WATCHDOG_ERROR);
          //exit(0);

          	
          drupal_add_http_header('Content-Type', 'text/xml');
          print('<response>');
          print('<activity op="' . $op . '" id="' . $id . '" >');
          print('<status>success</status>');
          print('</activity>');
          print('</response>');
          exit(0);          	
          
        }

        // Getting a reference to the entity we asked for
        $entityfields = array();

        //
        // We are only updating what has been passed through.
        // No test for missing values.
        // TODO: Validate incoming values.
        //


        $entityfields['pk_activity'] = $id;	

        if (isset($notification->actor)) {
          $entityfields['fk_actor'] = intval($notification->actor);
        }

        if (isset($notification->action)) {
          $entityfields['fk_action'] = intval($notification->action);
        }

        if (isset($notification->subject)) {
          $entityfields['fk_subject'] = intval($notification->subject);
        }

        if (isset($notification->place)) {
          $entityfields['fk_place'] = intval($notification->place);
        }

        if (!isset($notification->attributes()->key)) {
          if (twCanDebug()) debug('$entities');
          if (twCanDebug()) debug($entities);
        }

        // We are being notified, so we do not want to ship this out again as a notification...
        // Finally write the entity.
        $entity = entity_create('thumbwhere_activity', $entityfields);
        $entity->do_not_propagate = TRUE;
        $entity->save();

        //
        // Send the final response
        //
        
        $duration = round(((microtime(TRUE) - $start_time) * 1000.0),3); 

        drupal_add_http_header('Content-Type', 'text/xml');
        print('<response duration="' . $duration . 'ms">');
        print('<activity op="' . $op . '" id="' . $id . '" >');
        print('<status>success</status>');
        print('</activity>');
        print('</response>');
        exit(0);			
		
      
      }
  
  
    case 'update':
      {
      
		//
		// We want to time this
		//
			
		$start_time = microtime(TRUE);
      
        //
        // What resource are we updating?
        //
        
      
        //
        // Get the id
        //
		
        if (!isset($notification->attributes()->id)) {
          drupal_add_http_header('Status', '412 Unable to fulfull request.');
          $err = 'We were expecting an \'id\', attribute on the \'activity\', element when we are performing an \'update\' notification..';
          print($err);
          watchdog('thumbwhere_activity', $err ,array(), WATCHDOG_ERROR);
          exit(0);
        }

        $id = intval($notification->attributes()->id);
		

        if (twCanDebug()) debug('id');
        if (twCanDebug()) debug($id);

      
        //
        // Get the fields
        //
          
        $entities = entity_load('thumbwhere_activity', array($id));

        if (twCanDebug()) debug('$entities');
        if (twCanDebug()) debug($entities);

        if (count($entities) == 0) {
          //drupal_add_http_header('Status', '409 No such resource exists.');
          $err = 'No such \'activity\' resource exists with an id of \'' . $id . '\'.';
          //print($err);
          watchdog('thumbwhere_activity',$err,array(), WATCHDOG_ERROR);
          //exit(0);

////////////////////////////////////////////////////////////////////////
//
// ... for now we will just create the local copy
//
          watchdog('thumbwhere_activity','creating local version of missing entity',array(), WATCHDOG_ERROR);
		
		
		//
		// We want to time this
		//
			
		$start_time = microtime(TRUE);
			
        //
        // What resource are we creating?
        //
      
        //
        // Get the id
        //
		
        if (!isset($notification->attributes()->id)) {
          drupal_add_http_header('Status', '412 Unable to fulfull request.');
          $err = 'We were expecting an \'id\', attribute on the \'activity\', element when we are performing an \'update\' notification..';
          print($err);
          watchdog('thumbwhere_activity', $err ,array(), WATCHDOG_ERROR);
          exit(0);
        }

        $id = intval($notification->attributes()->id);
		

        if (twCanDebug()) debug('id');
        if (twCanDebug()) debug($id);

      
        //
        // Get the fields
        //
          
        $entities = entity_load('thumbwhere_activity', array($id));

        if (twCanDebug()) debug('$entities');
        if (twCanDebug()) debug($entities);

        if (count($entities) != 0) {
          //drupal_add_http_header('Status', '409 Resource already exists.');
          $err = '\'activity\' resource already exists with an id of \'' . $id . '\'.';
           //print($err);
          watchdog('thumbwhere_activity', $err ,array(), WATCHDOG_ERROR);
          //exit(0);

          	
          drupal_add_http_header('Content-Type', 'text/xml');
          print('<response>');
          print('<activity op="' . $op . '" id="' . $id . '" >');
          print('<status>success</status>');
          print('</activity>');
          print('</response>');
          exit(0);          	
          
        }

        // Getting a reference to the entity we asked for
        $entityfields = array();

        //
        // We are only updating what has been passed through.
        // No test for missing values.
        // TODO: Validate incoming values.
        //


        $entityfields['pk_activity'] = $id;	

        if (isset($notification->actor)) {
          $entityfields['fk_actor'] = intval($notification->actor);
        }

        if (isset($notification->action)) {
          $entityfields['fk_action'] = intval($notification->action);
        }

        if (isset($notification->subject)) {
          $entityfields['fk_subject'] = intval($notification->subject);
        }

        if (isset($notification->place)) {
          $entityfields['fk_place'] = intval($notification->place);
        }

        if (!isset($notification->attributes()->key)) {
          if (twCanDebug()) debug('$entities');
          if (twCanDebug()) debug($entities);
        }

        // We are being notified, so we do not want to ship this out again as a notification...
        // Finally write the entity.
        $entity = entity_create('thumbwhere_activity', $entityfields);
        $entity->do_not_propagate = TRUE;
        $entity->save();

        //
        // Send the final response
        //
        
        $duration = round(((microtime(TRUE) - $start_time) * 1000.0),3); 

        drupal_add_http_header('Content-Type', 'text/xml');
        print('<response duration="' . $duration . 'ms">');
        print('<activity op="' . $op . '" id="' . $id . '" >');
        print('<status>success</status>');
        print('</activity>');
        print('</response>');
        exit(0);			
		
//
//
//
////////////////////////////////////////////////////////////////////////
        }

        // Getting a reference to the entity we asked for
        $entity = $entities[$id];

        //
        // We are only updating what has been passed through.
        // No test for missing values.
        // TODO: Validate incoming values.
        //


        if (isset($notification->actor)) {
          $entity->fk_actor = intval($notification->actor);
        }

        if (isset($notification->action)) {
          $entity->fk_action = intval($notification->action);
        }

        if (isset($notification->subject)) {
          $entity->fk_subject = intval($notification->subject);
        }

        if (isset($notification->place)) {
          $entity->fk_place = intval($notification->place);
        }

        if (!isset($notification->attributes()->key)) {
          if (twCanDebug()) debug('$entities');
          if (twCanDebug()) debug($entities);
        }

        // We are being notified, so we do not want to ship this out again as a notification...
        $entity->do_not_propagate = TRUE;

        // Finally write the entity.
        entity_save('thumbwhere_activity', $entity);

        //
        // Send the final response
        //
        
        $duration = round(((microtime(TRUE) - $start_time) * 1000.0),3);

        drupal_add_http_header('Content-Type', 'text/xml');
        print('<response  duration="' . $duration . '" >');
        print('<activity op="' . $op . '" id="' . $id . '" >');
        print('<status>success</status>');
        print('</activity>');
        print('</response>');
        exit(0);
      }
      
      

    default:
      {
        drupal_add_http_header('Status', '501 Unable to fulfull request. Unhandled operation \'' . $op . '\'.');
        print('We don\'t handle the \'' . $op . '\', notification operation on the \'activity\', element.');
        exit(0);

      }
  }
}



/**
 * Menu callback: React to a notifications for ContentIngest.
 */
function _thumbwhere_activity_notify() {

  if (twCanDebug()) {
    watchdog('thumbwhere_activity', 'incoming notification.');
  }

  $numargs = func_num_args();
  $arg_list = func_get_args();

  
	if (twCanDebug()) debug('$numargs  = ' . $numargs);
	
	if (twCanDebug()) debug('$arg_list = ' . $arg_list);
	
	if (twCanDebug()) debug('$_REQUEST');
	if (twCanDebug()) debug($_REQUEST);
	
	if (twCanDebug()) debug('$_GET');
	if (twCanDebug()) debug($_GET);
	
	if (twCanDebug()) debug('$_POST');
	if (twCanDebug()) debug($_POST);
	
	//if (twCanDebug()) debug('$HTTP_RAW_POST_DATA');
	//if (twCanDebug()) debug($HTTP_RAW_POST_DATA);
	
	if (twCanDebug()) debug('$_SERVER');
	if (twCanDebug()) debug($_SERVER);
	
	//if (twCanDebug()) debug('$GLOBALS');
	//if (twCanDebug()) debug($GLOBALS);

  

  if (variable_get('thumbwhere_api_log_debug', 0) == 1) {
    foreach ($arg_list as $arg) {
      if (twCanDebug()) debug('$arg  = ' . $arg);
    }
  }


  //////////////////////////////////////////////////
  //
  //

  switch ($_SERVER['REQUEST_METHOD']) {

    case 'POST': {
        if ($numargs == 1) {
          if ($arg_list[0] == 'activity') {

            //
            // Get the POST data as XML
            //

            $post_xml = new SimpleXMLElement('php://input', null, true);
            if (twCanDebug()) debug('$post_xml');
            if (twCanDebug()) debug($post_xml);

            if (twCanDebug()) debug('$post_xml->getName()');
            if (twCanDebug()) debug($post_xml->getName());

            // We expect 'action' as the root element. TODO: I would rather this was the more generic 'request'
            if ($post_xml->getName() != 'action') {
              drupal_add_http_header('Status', '404 not found.');
              print('We were expecting \'action\', as the root element, not \'' . $post_xml->getName() . '\'.');
              exit(0);
            }

            /*
             *  SimpleXMLElement::__set_state(array(
             *      'activity' =>
             *      SimpleXMLElement::__set_state(array(
             *       '@attributes' =>
             *      array (
             *        'key' => '8e9ff318-3cd7-4593-99de-4c37c0fb335a',
             *        'op' => 'create',
             *      ),
             *    )),
             *  ))
             */


            // We expect 'action' as the root element.
            if (!isset($post_xml->activity)) {
              drupal_add_http_header('Status', '404 not found.');
              print('We were expecting a \'activity\', element in the action payload.');
              exit(0);
            }

            // Handle the incoming notification for content ingest
            _thumbwhere_handle_activity_notify_xml($post_xml->activity);

          }
          else {
            drupal_add_http_header('Status', '404 not found.');
            print('Invalid resource');
            exit(0);
          }
        }
        else {
          drupal_add_http_header('Status', '404 not found.');
          print('Invalid notification request url format.');
          exit(0);
        }
      }
      break;

    case 'GET': {

        if ($numargs == 1) {

          // If we are returning the index.xml file for the webservice.
          if ($arg_list[0] == 'index.xml') {
            // return index.xml
            drupal_add_http_header('Content-Type', 'text/xml');
            readfile(dirname(__FILE__) . '/index.xml');
            exit(0);
          }
          else {
            drupal_add_http_header('Status', '404 not found.');
            print('File not found.');
            exit(0);
          }
        }
        else {
          drupal_add_http_header('Status', '404 not found.');
          print('Invalid notification request url format.');
          exit(0);
        }
      }
      break;

    default: {
        drupal_add_http_header('Status', '404 not found.');
        exit(0);
      }
      break;
  }

  //
  //
  ////////////////////////////////////////////////////////////////////////////////////
 
}





/**
 * Activity UI controller.
 */
class ThumbWhereActivityUIController extends EntityDefaultUIController {

  /**
   * Overrides hook_menu() defaults. Main reason for doing this is that
   * parent class hook_menu() is optimized for entity type administration.
   */
  public function hook_menu() {

    $items = array();
    
    // Add a notification page...
    $items['thumbwhere/activity/notify'] = array(
        'title' => 'Notifications Callback for "Activity" Entity',
        'page callback' => '_thumbwhere_activity_notify',
        'access arguments' => array(
            'send thumbwhere activity notifications'
        ),
        'file' => 'thumbwhere_activity.admin.inc',
        'file path' => drupal_get_path('module', $this->entityInfo['module'])
    );    
    
    $id_count = count(explode('/', $this->path));
    $wildcard = isset($this->entityInfo['admin ui']['menu wildcard']) ? $this->entityInfo['admin ui']['menu wildcard'] : '%' . $this->entityType;

    $items[$this->path] = array(
      'title' => 'Activity',
      'description' => 'Add edit and update thumbwhere_activitys.',
      'page callback' => 'system_admin_menu_block_page',
      'access arguments' => array('access administration pages'),
      'file path' => drupal_get_path('module', 'system'),
      'file' => 'system.admin.inc',
    );

    // Change the overview menu type for the list of thumbwhere_activitys.
    $items[$this->path]['type'] = MENU_LOCAL_TASK;

    // Change the add page menu to multiple types of entities
    $items[$this->path . '/add'] = array(
      //'title' => 'Add a Activity',
      'title' => 'Add',
      'description' => 'Add a new Activity',
      'page callback' => 'thumbwhere_activity_form_wrapper',
      'page arguments' => array(thumbwhere_activity_create(array('type' => 'thumbwhere_activity'))),
      'access callback' => 'thumbwhere_activity_access',
      'access arguments' => array('edit', 'edit ' . 'thumbwhere_activity'),
      'file' => 'thumbwhere_activity.admin.inc',
      'file path' => drupal_get_path('module', $this->entityInfo['module'])
    );


/*
    // Change the add page menu to multiple types of entities
    $items[$this->path . '/add'] = array(
      //'title' => 'Add a Activity',
      'title' => 'Add',
	  'description' => 'Add a new Activity',
      'page callback'  => 'thumbwhere_activity_add_page',
      'access callback'  => 'thumbwhere_activity_access',
      'access arguments' => array('edit'),
      'type' => MENU_NORMAL_ITEM,
      'weight' => 20,
      'file' => 'thumbwhere_activity.admin.inc',
      'file path' => drupal_get_path('module', $this->entityInfo['module'])

    );
*/    
/*
    $items[$this->path . '/add/' . 'thumbwhere_activity'] = array(
      'title' => 'Add ' . 'ThumbWhereActivity',
      'page callback' => 'thumbwhere_activity_form_wrapper',
      'page arguments' => array(thumbwhere_activity_create(array('type' => 'thumbwhere_activity'))),
      'access callback' => 'thumbwhere_activity_access',
      'access arguments' => array('edit', 'edit ' . 'thumbwhere_activity'),
      'file' => 'thumbwhere_activity.admin.inc',
      'file path' => drupal_get_path('module', $this->entityInfo['module'])
    );
*/
    // Loading and editing thumbwhere_activity entities
    $items[$this->path . '/thumbwhere_activity/' . $wildcard] = array(
      'page callback' => 'thumbwhere_activity_form_wrapper',
      'page arguments' => array($id_count + 1),
      'access callback' => 'thumbwhere_activity_access',
      'access arguments' => array('edit', $id_count + 1),
      'weight' => 0,
      'context' => MENU_CONTEXT_PAGE | MENU_CONTEXT_INLINE,
      'file' => 'thumbwhere_activity.admin.inc',
      'file path' => drupal_get_path('module', $this->entityInfo['module'])
    );
    $items[$this->path . '/thumbwhere_activity/' . $wildcard . '/edit'] = array(
      'title' => 'Edit',
      'type' => MENU_DEFAULT_LOCAL_TASK,
      'weight' => -10,
      'context' => MENU_CONTEXT_PAGE | MENU_CONTEXT_INLINE,
    );

    $items[$this->path . '/thumbwhere_activity/' . $wildcard . '/delete'] = array(
      'title' => 'Delete',
      'page callback' => 'thumbwhere_activity_delete_form_wrapper',
      'page arguments' => array($id_count + 1),
      'access callback' => 'thumbwhere_activity_access',
      'access arguments' => array('edit', $id_count + 1),
      'type' => MENU_LOCAL_TASK,
      'context' => MENU_CONTEXT_INLINE,
      'weight' => 10,
      'file' => 'thumbwhere_activity.admin.inc',
      'file path' => drupal_get_path('module', $this->entityInfo['module'])
    );

    // Menu item for viewing thumbwhere_activitys
    $items['thumbwhere_activity/' . $wildcard] = array(
      //'title' => 'Title',
      'title callback' => 'thumbwhere_activity_page_title',
      'title arguments' => array(1),
      'page callback' => 'thumbwhere_activity_page_view',
      'page arguments' => array(1),
      'access callback' => 'thumbwhere_activity_access',
      'access arguments' => array('view', 1),
      'type' => MENU_CALLBACK,
    );
    return $items;
  }


  /**
   * Create the markup for the add ThumbWhereActivity Entities page within the class
   * so it can easily be extended/overriden.
   */
  public function addPage() {
    $item = menu_get_item();
    $content = system_admin_menu_block($item);

    if (count($content) == 1) {
      $item = array_shift($content);
      drupal_goto($item['href']);
    }

    return theme('thumbwhere_activity_add_list', array('content' => $content));
  }

}


/**
 * Form callback wrapper: create or edit a thumbwhere_activity.
 *
 * @param $thumbwhere_activity
 *   The thumbwhere_activity object being edited by this form.
 *
 * @see thumbwhere_activity_edit_form()
 */
function thumbwhere_activity_form_wrapper($thumbwhere_activity) {
  // Add the breadcrumb for the form's location.
  thumbwhere_activity_set_breadcrumb();
  return drupal_get_form('thumbwhere_activity_edit_form', $thumbwhere_activity);
}


/**
 * Form callback wrapper: delete a thumbwhere_activity.
 *
 * @param $thumbwhere_activity
 *   The thumbwhere_activity object being edited by this form.
 *
 * @see thumbwhere_activity_edit_form()
 */
function thumbwhere_activity_delete_form_wrapper($thumbwhere_activity) {
  // Add the breadcrumb for the form's location.
  //thumbwhere_activity_set_breadcrumb();
  return drupal_get_form('thumbwhere_activity_delete_form', $thumbwhere_activity);
}


/**
 * Form callback: create or edit a thumbwhere_activity.
 *
 * @param $thumbwhere_activity
 *   The thumbwhere_activity object to edit or for a create form an empty thumbwhere_activity object
 *     with only a thumbwhere_activity type defined.
 */
function thumbwhere_activity_edit_form($form, &$form_state, $thumbwhere_activity) {

  // Add the default field elements.
  $form['fk_actor'] = array(
    '#type' => 'textfield',
    '#title' => t('ThumbWhereActivity fk_actor'),
    '#default_value' => isset($thumbwhere_activity->fk_actor) ? $thumbwhere_activity->fk_actor : '',
    //'#maxlength' => 255,
   /// '#required' => TRUE,
    '#weight' => -5,
    '#autocomplete_path' => 'entity-autocomplete/thumbwhere_actor',
  );
  // Add the default field elements.
  $form['fk_action'] = array(
    '#type' => 'textfield',
    '#title' => t('ThumbWhereActivity fk_action'),
    '#default_value' => isset($thumbwhere_activity->fk_action) ? $thumbwhere_activity->fk_action : '',
    //'#maxlength' => 255,
   /// '#required' => TRUE,
    '#weight' => -5,
    '#autocomplete_path' => 'entity-autocomplete/thumbwhere_action',
  );
  // Add the default field elements.
  $form['fk_subject'] = array(
    '#type' => 'textfield',
    '#title' => t('ThumbWhereActivity fk_subject'),
    '#default_value' => isset($thumbwhere_activity->fk_subject) ? $thumbwhere_activity->fk_subject : '',
    //'#maxlength' => 255,
   /// '#required' => TRUE,
    '#weight' => -5,
    '#autocomplete_path' => 'entity-autocomplete/thumbwhere_subject',
  );
  // Add the default field elements.
  $form['fk_place'] = array(
    '#type' => 'textfield',
    '#title' => t('ThumbWhereActivity fk_place'),
    '#default_value' => isset($thumbwhere_activity->fk_place) ? $thumbwhere_activity->fk_place : '',
    //'#maxlength' => 255,
   /// '#required' => TRUE,
    '#weight' => -5,
    '#autocomplete_path' => 'entity-autocomplete/thumbwhere_place',
  );



  $form['data']['#tree'] = TRUE;

  /*
  $form['data']['sample_data'] = array(
    '#type' => 'checkbox',
    '#title' => t('An interesting thumbwhere_activity switch'),
    '#default_value' => isset($thumbwhere_activity->data['sample_data']) ? $thumbwhere_activity->data['sample_data'] : 1,
  );
  */


  // Add the field related form elements.
  $form_state['thumbwhere_activity'] = $thumbwhere_activity;
  field_attach_form('thumbwhere_activity', $thumbwhere_activity, $form, $form_state);

  $form['actions'] = array(
    '#type' => 'container',
    '#attributes' => array('class' => array('form-actions')),
    '#weight' => 400,
  );

  // We add the form's #submit array to this button along with the actual submit
  // handler to preserve any submit handlers added by a form callback_wrapper.
  $submit = array();

  if (!empty($form['#submit'])) {
    $submit += $form['#submit'];
  }

  $form['actions']['submit'] = array(
    '#type' => 'submit',
    '#value' => t('Save thumbwhere_activity'),
    '#submit' => $submit + array('thumbwhere_activity_edit_form_submit'),
  );

  // Do we show the delete button?
  if (!empty($thumbwhere_activity->pk_activity)) {
    $form['actions']['delete'] = array(
      '#type' => 'submit',
      '#value' => t('Delete thumbwhere_activity'),
      '#suffix' => l(t('Cancel'), 'admin/thumbwhere/thumbwhere_activitys'),
      '#submit' => $submit + array('thumbwhere_activity_form_submit_delete'),
      '#weight' => 45,
    );
  }

  // We append the validate handler to #validate in case a form callback_wrapper
  // is used to add validate handlers earlier.
  $form['#validate'][] = 'thumbwhere_activity_edit_form_validate';
  return $form;
}


/**
 * Form API validate callback for the thumbwhere_activity form
 */
function thumbwhere_activity_edit_form_validate(&$form, &$form_state) {
   
  //if (twCanDebug()) debug($form);   
  //if (twCanDebug()) debug($form_state);


  // No validation for pk


  // Convert fk autocomplete for fk_actor
  $value = $form['fk_actor']['#value'];
  // If we have something, escape the auto-completion encoding.
  if (!empty($value)) { 
    $value = entity_autocomplete_get_id($value);
    form_set_value($form['fk_actor'], $value,$form_state);
  }
  //if (twCanDebug()) debug($form['fk_actor']);
   if (twCanDebug()) debug('fk_actor = ' . $value);
  // Validate fk fk_actor
  if (empty($value)) {
    form_set_error('fk_actor', t('Validation error, \'fk_actor\' is mandatory1.'));
    // throw new Exception('Field \'fk_actor\' in is mandatory');
  }

  // Convert fk autocomplete for fk_action
  $value = $form['fk_action']['#value'];
  // If we have something, escape the auto-completion encoding.
  if (!empty($value)) { 
    $value = entity_autocomplete_get_id($value);
    form_set_value($form['fk_action'], $value,$form_state);
  }
  //if (twCanDebug()) debug($form['fk_action']);
   if (twCanDebug()) debug('fk_action = ' . $value);
  // Validate fk fk_action
  if (empty($value)) {
    form_set_error('fk_action', t('Validation error, \'fk_action\' is mandatory1.'));
    // throw new Exception('Field \'fk_action\' in is mandatory');
  }

  // Convert fk autocomplete for fk_subject
  $value = $form['fk_subject']['#value'];
  // If we have something, escape the auto-completion encoding.
  if (!empty($value)) { 
    $value = entity_autocomplete_get_id($value);
    form_set_value($form['fk_subject'], $value,$form_state);
  }
  //if (twCanDebug()) debug($form['fk_subject']);
   if (twCanDebug()) debug('fk_subject = ' . $value);
  // Validate fk fk_subject
  if (empty($value)) {
    form_set_error('fk_subject', t('Validation error, \'fk_subject\' is mandatory1.'));
    // throw new Exception('Field \'fk_subject\' in is mandatory');
  }

  // Convert fk autocomplete for fk_place
  $value = $form['fk_place']['#value'];
  // If we have something, escape the auto-completion encoding.
  if (!empty($value)) { 
    $value = entity_autocomplete_get_id($value);
    form_set_value($form['fk_place'], $value,$form_state);
  }
  //if (twCanDebug()) debug($form['fk_place']);
   if (twCanDebug()) debug('fk_place = ' . $value);
  // Validate fk fk_place


              

  //form_set_value( array('#parents' => array('array_key_parent', 'array_key_to_replace')) , $value, $form_state);

   
   
  $thumbwhere_activity = $form_state['thumbwhere_activity'];

  // Notify field widgets to validate their data.
  field_attach_form_validate('thumbwhere_activity', $thumbwhere_activity, $form, $form_state);
    
    
  
}


/**
 * Form API submit callback for the thumbwhere_activity form.
 *
 * @todo remove hard-coded link
 */
function thumbwhere_activity_edit_form_submit(&$form, &$form_state) {

  $thumbwhere_activity = entity_ui_controller('thumbwhere_activity')->entityFormSubmitBuildEntity($form, $form_state);
  // Save the thumbwhere_activity and go back to the list of thumbwhere_activitys

  // Add in created and changed times.
  if ($thumbwhere_activity->is_new = isset($thumbwhere_activity->is_new) ? $thumbwhere_activity->is_new : 0) {
    $thumbwhere_activity->created = time();
  }

  $thumbwhere_activity->changed = time();

  $thumbwhere_activity->save();
  $form_state['redirect'] = 'admin/thumbwhere/thumbwhere_activitys';
}

/**
 * Form API submit callback for the delete button.
 *
 * @todo Remove hard-coded path
 */
function thumbwhere_activity_form_submit_delete(&$form, &$form_state) {
  $form_state['redirect'] = 'admin/thumbwhere/thumbwhere_activitys/thumbwhere_activity/' . $form_state['thumbwhere_activity']->pk_activity . '/delete';
}





/**
 * Form callback: confirmation form for deleting a thumbwhere_activity.
 *
 * @param $thumbwhere_activity
 *   The thumbwhere_activity to delete
 *
 * @see confirm_form()
 */
function thumbwhere_activity_delete_form($form, &$form_state, $thumbwhere_activity) {
  $form_state['thumbwhere_activity'] = $thumbwhere_activity;

  $form['#submit'][] = 'thumbwhere_activity_delete_form_submit';

  $form = confirm_form($form,
    t('Are you sure you want to delete thumbwhere_activity %name?', array('%name' => $thumbwhere_activity->pk_activity)),
    'admin/thumbwhere/thumbwhere_activitys/thumbwhere_activity',
    '<p>' . t('This action cannot be undone.') . '</p>',
    t('Delete'),
    t('Cancel'),
    'confirm'
  );

  return $form;
}

/**
 * Submit callback for thumbwhere_activity_delete_form
 */
function thumbwhere_activity_delete_form_submit($form, &$form_state) {
  $thumbwhere_activity = $form_state['thumbwhere_activity'];

  thumbwhere_activity_delete($thumbwhere_activity);

  drupal_set_message(t('The thumbwhere_activity %name has been deleted.', array('%name' => $thumbwhere_activity->pk_activity)));
  watchdog('thumbwhere_activity', 'Deleted thumbwhere_activity %name.', array('%name' => $thumbwhere_activity->pk_activity));

  $form_state['redirect'] = 'admin/thumbwhere/thumbwhere_activitys';
}



/**
 * Page to add ThumbWhereActivity Entities.
 *
 * @todo Pass this through a proper theme function
 */
function thumbwhere_activity_add_page() {
  $controller = entity_ui_controller('thumbwhere_activity');
  return $controller->addPage();
}


/**
 * Displays the list of available thumbwhere_activity types for thumbwhere_activity creation.
 *
 * @ingroup themeable
 */
function theme_thumbwhere_activity_add_list($variables) {


  //$content = $variables['content'];
  $output = '';
  /*
  if ($content) {
    $output = '<dl class="thumbwhere_activity-type-list">';
    foreach ($content as $item) {
      $output .= '<dt>' . l($item['title'], $item['href']) . '</dt>';
      $output .= '<dd>' . filter_xss_admin($item['description']) . '</dd>';
    }
    $output .= '</dl>';
  }
  else {
    if (user_access('administer thumbwhere_activity types')) {
      $output = '<p>' . t('ThumbWhereActivity Entities cannot be added because you have not created any thumbwhere_activity types yet. Go to the <a href="@create-thumbwhere_activity-type">thumbwhere_activity type creation page</a> to add a new thumbwhere_activity type.', array('@create-thumbwhere_activity-type' => url('admin/structure/thumbwhere_activity_types/add'))) . '</p>';
    }
    else {
      $output = '<p>' . t('No thumbwhere_activity types have been created yet for you to use.') . '</p>';
    }
  }
  */
  return $output;
}





/**
 * Sets the breadcrumb for administrative thumbwhere_activity pages.
 */
function thumbwhere_activity_set_breadcrumb() {
  $breadcrumb = array(
    l(t('Home'), '<front>'),
    l(t('Administration'), 'admin'),
    l(t('Activity'), 'admin/content'),
    l(t('Activity'), 'admin/thumbwhere/thumbwhere_activitys'),
  );

  drupal_set_breadcrumb($breadcrumb);
}



