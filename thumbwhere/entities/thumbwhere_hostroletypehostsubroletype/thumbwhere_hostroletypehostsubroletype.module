<?php

// ******************************************************************
// This file was generated by autogen_model_implementation_drupal.xsl
// ******************************************************************

// Include the ThumbWhere SDK
require_once(dirname(__FILE__) . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . 'dependencies' . DIRECTORY_SEPARATOR . 'tw' . DIRECTORY_SEPARATOR . 'sdk.class.php');

/**
 * @file
 * Module for the ThumbWhere HostRoleTypeHostSubRoleType Entity
 *
 *
 * NOTE: This has been autogenerated.
 * 
 */

/**
 * Implement hook_entity_info().
 *
 * We define two entities here - the actual entity that will hold our domain
 * specific information and an entity that holds information about the different
 * types of entities. See here: http://drupal.org/node/977380 for a discussion on this
 * choice.
 */
function thumbwhere_hostroletypehostsubroletype_entity_info() {
  $return['thumbwhere_hostroletypehostsubroletype'] = array(

    // The human-readable name of the type.
    'label' => t('ThumbWhereHostRoleTypeHostSubRoleType'),

    // (used by DrupalDefaultEntityController) The name of the entity type's base table.
    'base table' => 'thumbwhere_hostroletypehostsubroletype',

    // fieldable: Set to TRUE if you want your entity type to accept fields being attached to it.
    'fieldable' => TRUE,

  // Declare the endty keys..
    'entity keys' => array(
      'id' => 'pk_hostroletypehostsubroletype',
      'label' => 'pk_hostroletypesubroletype',
    ),

    // A function taking an entity and an entity type as arguments and returning the label of the entity.
    // The entity label is the main string associated with an entity; for example, the title of a node or
    // the subject of a comment. If there is an entity object property that defines the label, use the
    // 'label' element of the 'entity keys' return value component to provide this information (see below).
    // If more complex logic is needed to determine the label of an entity, you can instead specify a callback
    // function here, which will be called to determine the entity label. See also the entity_label()
    // function, which implements this logic.
    'label callback' => 'entity_class_label',

    // A function taking an entity as argument and returning the uri elements of the entity, e.g. 'path' and 'options'.
    // The actual entity uri can be constructed by passing these elements to url().
    'uri callback' => 'entity_class_uri',

    //
    // Entity API Components
    //

    // The module whwre we can find our classes
    'module' => 'thumbwhere_hostroletypehostsubroletype',

    // The entity class and controller class extend the classes provided by the Entity API
    'entity class' => 'ThumbWhereHostRoleTypeHostSubRoleType',

    // Which Entity API contoller
    'controller class' => 'ThumbWhereHostRoleTypeHostSubRoleTypeController',

    // The information below is used by the ThumbWhereHostRoleTypeHostSubRoleTypeUIController (which extends the EntityDefaultUIController)
    'admin ui' => array(
      'path' => 'admin/thumbwhere/thumbwhere_hostroletypehostsubroletypes',
      'file' => 'thumbwhere_hostroletypehostsubroletype.admin.inc',
      'controller class' => 'ThumbWhereHostRoleTypeHostSubRoleTypeUIController',
      'menu wildcard' => '%thumbwhere_hostroletypehostsubroletype',
    ),
  );

  return $return;
}


/**
 * Implements hook_permission().
 */
function thumbwhere_hostroletypehostsubroletype_permission() {

  // We set up permisssions to manage entity types, manage all entities and the
  // permissions for each individual entity
  $permissions = array(
    'administer thumbwhere_hostroletypehostsubroletypes' => array(
      'title' => t('Administer thumbwhere_hostroletypehostsubroletypes'),
      'description' => t('Edit and delete all thumbwhere_hostroletypehostsubroletypes'),
    ),
  'send thumbwhere hostroletypehostsubroletype notifications' => array(
          'title' => t('Send ThumbWhere Notifications'),
          'description' => t('Allow the caller to call the ThumbWhere HostRoleTypeHostSubRoleType Entity Notifications Endpoint.'),
          'restrict access' => TRUE,
      ),    
  );

  return $permissions;
}


/**
 * Determines whether the given user has access to a thumbwhere_hostroletypehostsubroletype.
 *
 * @param $op
 *   The operation being performed. One of 'view', 'update', 'create', 'delete'
 *   or just 'edit' (being the same as 'create' or 'update').
 * @param $thumbwhere_hostroletypehostsubroletype
 *   Optionally a thumbwhere_hostroletypehostsubroletype or a thumbwhere_hostroletypehostsubroletype type to check access for. If nothing is
 *   given, access for all thumbwhere_hostroletypehostsubroletypes is determined.
 * @param $account
 *   The user to check for. Leave it to NULL to check for the global user.
 * @return boolean
 *   Whether access is allowed or not.
 */
function thumbwhere_hostroletypehostsubroletype_access($op, $thumbwhere_hostroletypehostsubroletype = NULL, $account = NULL) {

  //if (user_access('administer thumbwhere_hostroletypehostsubroletypes', $account)) {
  //  return TRUE;
 // }

  //return FALSE;
  return TRUE;
}




/**
 * Fetch a thumbwhere_hostroletypehostsubroletype object. Make sure that the wildcard you choose
 * in the thumbwhere_hostroletypehostsubroletype entity definition fits the function name here.
 *
 * @param $thumbwhere_hostroletypehostsubroletype_id
 *   Integer specifying the thumbwhere_hostroletypehostsubroletype id.
 * @param $reset
 *   A boolean indicating that the internal cache should be reset.
 * @return
 *   A fully-loaded $thumbwhere_hostroletypehostsubroletype object or FALSE if it cannot be loaded.
 *
 * @see thumbwhere_hostroletypehostsubroletype_load_multiple()
 */
function thumbwhere_hostroletypehostsubroletype_load($thumbwhere_hostroletypehostsubroletype_id, $reset = FALSE) {

  $thumbwhere_hostroletypehostsubroletypes = entity_load('thumbwhere_hostroletypehostsubroletype', array($thumbwhere_hostroletypehostsubroletype_id), array(), $reset);

  return reset($thumbwhere_hostroletypehostsubroletypes);
}


/**
 * Deletes a thumbwhere_hostroletypehostsubroletype.
 */
function thumbwhere_hostroletypehostsubroletype_delete(ThumbWhereHostRoleTypeHostSubRoleType $thumbwhere_hostroletypehostsubroletype) {

  $thumbwhere_hostroletypehostsubroletype->delete();
}

/**
 * Create a thumbwhere_hostroletypehostsubroletype object.
 */
function thumbwhere_hostroletypehostsubroletype_create($values = array()) {

  return entity_get_controller('thumbwhere_hostroletypehostsubroletype')->create($values);
}


/**
 * URI callback for thumbwhere_hostroletypehostsubroletypes
 */
function thumbwhere_hostroletypehostsubroletype_uri(ThumbWhereHostRoleTypeHostSubRoleType $thumbwhere_hostroletypehostsubroletype) {

  return array(
    'path' => 'thumbwhere_hostroletypehostsubroletype/' . $thumbwhere_hostroletypehostsubroletype->pk_hostroletypehostsubroletype,
  );
}


/**
 * Menu title callback for showing individual entities
 */
function thumbwhere_hostroletypehostsubroletype_page_title(ThumbWhereHostRoleTypeHostSubRoleType $thumbwhere_hostroletypehostsubroletype) {

	return $thumbwhere_hostroletypehostsubroletype->pk_hostroletypesubroletype;
}


/**
 * Sets up content to show an individual thumbwhere_hostroletypehostsubroletype
 * @todo - get rid of drupal_set_title();
 */
function thumbwhere_hostroletypehostsubroletype_page_view($thumbwhere_hostroletypehostsubroletype, $view_mode = 'full') {



  $controller = entity_get_controller('thumbwhere_hostroletypehostsubroletype');
  $content = $controller->view(array($thumbwhere_hostroletypehostsubroletype->pk_hostroletypehostsubroletype => $thumbwhere_hostroletypehostsubroletype));
  drupal_set_title($thumbwhere_hostroletypehostsubroletype->pk_hostroletypesubroletype);
  return $content;
}


/**
 * Implements hook_views_api().
 */
function thumbwhere_hostroletypehostsubroletype_views_api() {



  return array(
    'api' => 3,
    'path' => drupal_get_path('module', 'thumbwhere_hostroletypehostsubroletype') . '/views',
  );
}


/**
 * Implement hook_theme().
 */
function thumbwhere_hostroletypehostsubroletype_theme() {



  return array(
    'thumbwhere_hostroletypehostsubroletype_add_list' => array(
      'variables' => array('content' => array()),
      'file' => 'thumbwhere_hostroletypehostsubroletype.admin.inc',
    ),
    'thumbwhere_hostroletypehostsubroletype' => array(
      'render element' => 'elements',
      'template' => 'thumbwhere_hostroletypehostsubroletype',
    )
  );
}


/**
 * Implements hook_menu_local_tasks_alter().
 */
function thumbwhere_hostroletypehostsubroletype_menu_local_tasks_alter(&$data, $router_item, $root_path) {

  // Add action link 'admin/structure/thumbwhere_hostroletypehostsubroletypes/add' on 'admin/structure/thumbwhere_hostroletypehostsubroletypes'.
  if ($root_path == 'admin/thumbwhere/thumbwhere_hostroletypehostsubroletypes') {
    $item = menu_get_item('admin/thumbwhere/thumbwhere_hostroletypehostsubroletypes/add');
    if ($item['access']) {
      $data['actions']['output'][] = array(
        '#theme' => 'menu_local_action',
        '#link' => $item,
      );
    }
  }
}


/**
 * The class used for thumbwhere_hostroletypehostsubroletype entities
 */
class ThumbWhereHostRoleTypeHostSubRoleType extends Entity {

  public function __construct($values = array()) {
    parent::__construct($values, 'thumbwhere_hostroletypehostsubroletype');
  }

  protected function defaultLabel() {
    return $this->pk_hostroletypesubroletype;
  }

  protected function defaultUri() {
    return array('path' => 'thumbwhere_hostroletypehostsubroletype/' . $this->pk_hostroletypehostsubroletype);
  }


}



/**
 * The Controller for ThumbWhereHostRoleTypeHostSubRoleType entities
 */
class ThumbWhereHostRoleTypeHostSubRoleTypeController extends EntityAPIController {
  public function __construct($entityType) {
    parent::__construct($entityType);
  }
}



/**
* Implements hook_entity_presave().
*
* Act on an HostRoleTypeHostSubRoleType entity before it is about to be created or updated.
*
* @param $entity
*   The entity object.
* @param $entity_type
*   The type of entity being saved (i.e. node, user, comment).
*/
function thumbwhere_hostroletypehostsubroletype_entity_presave($entity, $entity_type) {

  // We only look after what we want...
  if ($entity_type == 'thumbwhere_hostroletypehostsubroletype')  {
  	
  	
  	
 	/////////////////////////////////////////////////////
    //
    // Should we not propagate this to ThumbWhere? 
    //
    if (isset($entity->do_not_propagate)) {
      if ($entity->do_not_propagate) {
        if (twCanDebug()) {
          if ($entity->is_new) {
            if (twCanDebug()){
              debug("DO NOT PROPAGATE 'hostroletypehostsubroletype' presave *CREATE*");
          	}
          }
          else {
            if (twCanDebug()) {
              debug("DO NOT PROPAGATE 'hostroletypehostsubroletype' presave *UPDATE*");
            }
          }
        }

        // To cause an infinite loop of updates, comment out this line.
        return;
      }
      else if (twCanDebug()) {
        debug("OK PROAOGATE 'hostroletypehostsubroletype' presave to ThumbWhere Server");
      }
    }
    else if (twCanDebug()) {
      debug("DO NOT PROPAGATE IS NOT SET");
    }
    //
    //
    ////////////////////////////////////////////////////////  	

  
    // .. then we create a new thumbwhere_hostroletypehostsubroletype entity...
      // Instantiate the ThumbWhereContent class

    $twAdminAPI = new ThumbWhereAPIAdmin();

    //if (twCanDebug()) debug($twContentAPI);

    //if (twCanDebug()) debug($entity);

    //
    // Collate and validate our fields
    //

    $fields = array();
    // Compulsory so..
    if (!isset($entity->fk_hostroletype)) {
      throw new Exception('hostroletype is mandatory.');
    }
    else {
      $fields['hostroletype'] = $entity->fk_hostroletype;
    }
    // Compulsory so..
    if (!isset($entity->fk_subroletype)) {
      throw new Exception('subroletype is mandatory.');
    }
    else {
      $fields['subroletype'] = $entity->fk_subroletype;
    }
		
	//
	// Ensure that we have the 'thumbwhere_api_key' as we are going to pass that through in a context
	//
    if (variable_get('thumbwhere_api_key') == '') {
    	throw new Exception('ThumbWhere resource for \'HostRoleTypeHostSubRoleType\' requires that variable \'thumbwhere_api_key\' is set.');
    }
    
              		
    //
    // If this hostroletypehostsubroletype entity is new....
    //
               
    if ($entity->is_new) {
                

      //
      // ... then we create the hostroletypehostsubroletype entity in ThumbWhere
      //

      $hostroletypehostsubroletype = $twAdminAPI->create_host_role_type_capable_of_host_role_subrole_type($context = array(
                  'key' => variable_get('thumbwhere_api_key')
          ), $fields);
            
       if (twCanDebug()) debug($hostroletypehostsubroletype);

      //if (twCanDebug()) debug($host_role_type_capable_of_host_role_subrole_type);

      $attributes = $hostroletypehostsubroletype->body->host_role_type_capable_of_host_role_subrole_type->attributes();

      if (twCanDebug()) debug('newly created id = ' . $attributes['id']);

      // Assign new id.
      $entity->pk_hostroletypehostsubroletype = intval($attributes['id']);

      if (twCanDebug()) debug('$entity->pk_hostroletypehostsubroletype = ' . $entity->pk_hostroletypehostsubroletype);
    }
    else
    {
          	

      //
      // .. otherwise we update the hostroletypehostsubroletype entity in ThumbWhere
      //

      $hostroletypehostsubroletype = $twAdminAPI->update_host_role_type_capable_of_host_role_subrole_type($id=$entity->pk_hostroletypehostsubroletype,$context = array(
                  'key' => variable_get('thumbwhere_api_key')
          ), $fields);
            
        
    }
          	
    
  }
}

/**
 * Implements thumbwhere_hostroletypehostsubroletype_entity_insert().
 *
 * Act on entities when inserted.
 *
 * @param $entity
 *   The entity object
 * @param $type
 *   The type of entity being inserted (i.e. node, user, comment).
 */
function thumbwhere_hostroletypehostsubroletype_entity_insert($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_hostroletypehostsubroletype') return;

}


/**
* Implements thumbwhere_hostroletypehostsubroletype_entity_delete().
*
* Act on entities when inserted.
*
* @param $entity
*   The entity object
* @param $type
*   The type of entity being deleted (i.e. node, user, comment).
*/
function thumbwhere_hostroletypehostsubroletype_entity_delete($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_hostroletypehostsubroletype') return;


}
