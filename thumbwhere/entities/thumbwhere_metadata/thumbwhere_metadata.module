<?php

// ******************************************************************
// This file was generated by autogen_model_implementation_drupal.xsl
// ******************************************************************

// Include the ThumbWhere SDK
require_once(dirname(__FILE__) . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . 'dependencies' . DIRECTORY_SEPARATOR . 'tw' . DIRECTORY_SEPARATOR . 'sdk.class.php');

/**
 * @file
 * Module for the ThumbWhere Metadata Entity
 *
 *
 * NOTE: This has been autogenerated.
 * 
 */

/**
 * Implement hook_entity_info().
 *
 * We define two entities here - the actual entity that will hold our domain
 * specific information and an entity that holds information about the different
 * types of entities. See here: http://drupal.org/node/977380 for a discussion on this
 * choice.
 */
function thumbwhere_metadata_entity_info() {
  $return['thumbwhere_metadata'] = array(

    // The human-readable name of the type.
    'label' => t('ThumbWhereMetadata'),

    // (used by DrupalDefaultEntityController) The name of the entity type's base table.
    'base table' => 'thumbwhere_metadata',

    // fieldable: Set to TRUE if you want your entity type to accept fields being attached to it.
    'fieldable' => TRUE,

  // Declare the endty keys..
    'entity keys' => array(
      'id' => 'pk_metadata',
      'label' => 'pk_metadata',
    ),

    // A function taking an entity and an entity type as arguments and returning the label of the entity.
    // The entity label is the main string associated with an entity; for example, the title of a node or
    // the subject of a comment. If there is an entity object property that defines the label, use the
    // 'label' element of the 'entity keys' return value component to provide this information (see below).
    // If more complex logic is needed to determine the label of an entity, you can instead specify a callback
    // function here, which will be called to determine the entity label. See also the entity_label()
    // function, which implements this logic.
    'label callback' => 'entity_class_label',

    // A function taking an entity as argument and returning the uri elements of the entity, e.g. 'path' and 'options'.
    // The actual entity uri can be constructed by passing these elements to url().
    'uri callback' => 'entity_class_uri',

    //
    // Entity API Components
    //

    // The module whwre we can find our classes
    'module' => 'thumbwhere_metadata',

    // The entity class and controller class extend the classes provided by the Entity API
    'entity class' => 'ThumbWhereMetadata',

    // Which Entity API contoller
    'controller class' => 'ThumbWhereMetadataController',

    // The information below is used by the ThumbWhereMetadataUIController (which extends the EntityDefaultUIController)
    'admin ui' => array(
      'path' => 'admin/thumbwhere/thumbwhere_metadatas',
      'file' => 'thumbwhere_metadata.admin.inc',
      'controller class' => 'ThumbWhereMetadataUIController',
      'menu wildcard' => '%thumbwhere_metadata',
    ),
  );

  return $return;
}


/**
 * Implements hook_permission().
 */
function thumbwhere_metadata_permission() {

  // We set up permisssions to manage entity types, manage all entities and the
  // permissions for each individual entity
  $permissions = array(
    'administer thumbwhere_metadatas' => array(
      'title' => t('Administer thumbwhere_metadatas'),
      'description' => t('Edit and delete all thumbwhere_metadatas'),
    ),
  'send thumbwhere metadata notifications' => array(
          'title' => t('Send ThumbWhere Notifications'),
          'description' => t('Allow the caller to call the ThumbWhere Metadata Entity Notifications Endpoint.'),
          'restrict access' => TRUE,
      ),    
  );

  return $permissions;
}


/**
 * Determines whether the given user has access to a thumbwhere_metadata.
 *
 * @param $op
 *   The operation being performed. One of 'view', 'update', 'create', 'delete'
 *   or just 'edit' (being the same as 'create' or 'update').
 * @param $thumbwhere_metadata
 *   Optionally a thumbwhere_metadata or a thumbwhere_metadata type to check access for. If nothing is
 *   given, access for all thumbwhere_metadatas is determined.
 * @param $account
 *   The user to check for. Leave it to NULL to check for the global user.
 * @return boolean
 *   Whether access is allowed or not.
 */
function thumbwhere_metadata_access($op, $thumbwhere_metadata = NULL, $account = NULL) {

  //if (user_access('administer thumbwhere_metadatas', $account)) {
  //  return TRUE;
 // }

  //return FALSE;
  return TRUE;
}




/**
 * Fetch a thumbwhere_metadata object. Make sure that the wildcard you choose
 * in the thumbwhere_metadata entity definition fits the function name here.
 *
 * @param $thumbwhere_metadata_id
 *   Integer specifying the thumbwhere_metadata id.
 * @param $reset
 *   A boolean indicating that the internal cache should be reset.
 * @return
 *   A fully-loaded $thumbwhere_metadata object or FALSE if it cannot be loaded.
 *
 * @see thumbwhere_metadata_load_multiple()
 */
function thumbwhere_metadata_load($thumbwhere_metadata_id, $reset = FALSE) {

  $thumbwhere_metadatas = entity_load('thumbwhere_metadata', array($thumbwhere_metadata_id), array(), $reset);

  return reset($thumbwhere_metadatas);
}


/**
 * Deletes a thumbwhere_metadata.
 */
function thumbwhere_metadata_delete(ThumbWhereMetadata $thumbwhere_metadata) {

  $thumbwhere_metadata->delete();
}

/**
 * Create a thumbwhere_metadata object.
 */
function thumbwhere_metadata_create($values = array()) {

  return entity_get_controller('thumbwhere_metadata')->create($values);
}


/**
 * URI callback for thumbwhere_metadatas
 */
function thumbwhere_metadata_uri(ThumbWhereMetadata $thumbwhere_metadata) {

  return array(
    'path' => 'thumbwhere_metadata/' . $thumbwhere_metadata->pk_metadata,
  );
}


/**
 * Menu title callback for showing individual entities
 */
function thumbwhere_metadata_page_title(ThumbWhereMetadata $thumbwhere_metadata) {

	return $thumbwhere_metadata->pk_metadata;
}


/**
 * Sets up content to show an individual thumbwhere_metadata
 * @todo - get rid of drupal_set_title();
 */
function thumbwhere_metadata_page_view($thumbwhere_metadata, $view_mode = 'full') {



  $controller = entity_get_controller('thumbwhere_metadata');
  $content = $controller->view(array($thumbwhere_metadata->pk_metadata => $thumbwhere_metadata));
  drupal_set_title($thumbwhere_metadata->pk_metadata);
  return $content;
}


/**
 * Implements hook_views_api().
 */
function thumbwhere_metadata_views_api() {



  return array(
    'api' => 3,
    'path' => drupal_get_path('module', 'thumbwhere_metadata') . '/views',
  );
}


/**
 * Implement hook_theme().
 */
function thumbwhere_metadata_theme() {



  return array(
    'thumbwhere_metadata_add_list' => array(
      'variables' => array('content' => array()),
      'file' => 'thumbwhere_metadata.admin.inc',
    ),
    'thumbwhere_metadata' => array(
      'render element' => 'elements',
      'template' => 'thumbwhere_metadata',
    )
  );
}


/**
 * Implements hook_menu_local_tasks_alter().
 */
function thumbwhere_metadata_menu_local_tasks_alter(&$data, $router_item, $root_path) {

  // Add action link 'admin/structure/thumbwhere_metadatas/add' on 'admin/structure/thumbwhere_metadatas'.
  if ($root_path == 'admin/thumbwhere/thumbwhere_metadatas') {
    $item = menu_get_item('admin/thumbwhere/thumbwhere_metadatas/add');
    if ($item['access']) {
      $data['actions']['output'][] = array(
        '#theme' => 'menu_local_action',
        '#link' => $item,
      );
    }
  }
}


/**
 * The class used for thumbwhere_metadata entities
 */
class ThumbWhereMetadata extends Entity {

  public function __construct($values = array()) {
    parent::__construct($values, 'thumbwhere_metadata');
  }

  protected function defaultLabel() {
    return $this->pk_metadata;
  }

  protected function defaultUri() {
    return array('path' => 'thumbwhere_metadata/' . $this->pk_metadata);
  }


}



/**
 * The Controller for ThumbWhereMetadata entities
 */
class ThumbWhereMetadataController extends EntityAPIController {
  public function __construct($entityType) {
    parent::__construct($entityType);
  }
}



/**
* Implements hook_entity_presave().
*
* Act on an Metadata entity before it is about to be created or updated.
*
* @param $entity
*   The entity object.
* @param $entity_type
*   The type of entity being saved (i.e. node, user, comment).
*/
function thumbwhere_metadata_entity_presave($entity, $entity_type) {

  // We only look after what we want...
  if ($entity_type == 'thumbwhere_metadata')  {
  	
 	/////////////////////////////////////////////////////
    //
    // Should we not propagate this to ThumbWhere? 
    //
    if (isset($entity->do_not_propagate)) {
      if ($entity->do_not_propagate) {
        if (twCanDebug()) {
          if ($entity->is_new) {
            if (twCanDebug()){
              debug("DO NOT PROPAGATE 'metadata' presave *CREATE* not being sent to ThumbWhere server.");
          	}
          }
          else {
            if (twCanDebug()) {
              debug("DO NOT PROPAGATE 'metadata' presave *UPDATE* not being sent to ThumbWhere server.");
            }
          }
        }

        // To cause an infinite loop of updates, comment out this line.
        return;
      }
      else if (twCanDebug()) {
        debug("OK PROAOGATE 'metadata' presave to ThumbWhere server.");
      }
    }
    else if (twCanDebug()) {
      debug("DO NOT PROPAGATE IS NOT SET");
    }
    //
    //
    ////////////////////////////////////////////////////////  	

  
    // .. then we create a new thumbwhere_metadata entity...
      // Instantiate the ThumbWhereContent class

    $twContentAPI = new ThumbWhereAPIContent();

    //if (twCanDebug()) debug($twContentAPI);

    //if (twCanDebug()) debug($entity);

    //
    // Collate and validate our fields
    //

    $fields = array();

    // Nullable so we only pass it through to create_metadata if we have it..
    if (isset($entity->fk_contentmetadata)) {
      $fields['contentmetadata'] = $entity->fk_contentmetadata;
    }

    // Nullable so we only pass it through to create_metadata if we have it..
    if (isset($entity->fk_trackmetadata)) {
      $fields['trackmetadata'] = $entity->fk_trackmetadata;
    }
		
	//
	// Ensure that we have the 'thumbwhere_api_key' as we are going to pass that through in a context
	//
    if (variable_get('thumbwhere_api_key') == '') {
    	throw new Exception('ThumbWhere resource for \'Metadata\' requires that variable \'thumbwhere_api_key\' is set.');
    }
    
              		
    //
    // If this metadata entity is new....
    //
               
    if ($entity->is_new) {
                

      //
      // ... then we create the metadata entity in ThumbWhere
      //

      $metadata = $twContentAPI->create_metadata($context = array(
                  'key' => variable_get('thumbwhere_api_key')
          ), $fields);
            
       if (twCanDebug()) debug($metadata);

      //if (twCanDebug()) debug($metadata);

      $attributes = $metadata->body->metadata->attributes();

      if (twCanDebug()) debug('newly created id = ' . $attributes['id']);

      // Assign new id.
      $entity->pk_metadata = intval($attributes['id']);

      if (twCanDebug()) debug('$entity->pk_metadata = ' . $entity->pk_metadata);
    }
    else
    {
          	

      //
      // .. otherwise we update the metadata entity in ThumbWhere
      //

      $metadata = $twContentAPI->update_metadata($id=$entity->pk_metadata,$context = array(
                  'key' => variable_get('thumbwhere_api_key')
          ), $fields);
            
        
    }
          	
    
  }
}

/**
 * Implements thumbwhere_metadata_entity_insert().
 *
 * Act on entities when inserted.
 *
 * @param $entity
 *   The entity object
 * @param $type
 *   The type of entity being inserted (i.e. node, user, comment).
 */
function thumbwhere_metadata_entity_insert($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_metadata') return;

}


/**
* Implements thumbwhere_metadata_entity_delete().
*
* Act on entities when inserted.
*
* @param $entity
*   The entity object
* @param $type
*   The type of entity being deleted (i.e. node, user, comment).
*/
function thumbwhere_metadata_entity_delete($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_metadata') return;


}
