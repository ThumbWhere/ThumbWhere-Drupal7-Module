<?php

// ******************************************************************
// This file was generated by autogen_model_implementation_drupal.xsl
// ******************************************************************

// Include the ThumbWhere SDK
require_once(dirname(__FILE__) . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . 'dependencies' . DIRECTORY_SEPARATOR . 'tw' . DIRECTORY_SEPARATOR . 'sdk.class.php');

/**
 * @file
 * Module for the ThumbWhere ExternalRepository Entity
 *
 *
 * NOTE: This has been autogenerated.
 * 
 */

/**
 * Implement hook_entity_info().
 *
 * We define two entities here - the actual entity that will hold our domain
 * specific information and an entity that holds information about the different
 * types of entities. See here: http://drupal.org/node/977380 for a discussion on this
 * choice.
 */
function thumbwhere_externalrepository_entity_info() {
  $return['thumbwhere_externalrepository'] = array(

    // The human-readable name of the type.
    'label' => t('ThumbWhereExternalRepository'),

    // (used by DrupalDefaultEntityController) The name of the entity type's base table.
    'base table' => 'thumbwhere_externalrepository',

    // fieldable: Set to TRUE if you want your entity type to accept fields being attached to it.
    'fieldable' => TRUE,

  // Declare the endty keys..
    'entity keys' => array(
      'id' => 'pk_externalrepository',
      'label' => 'pk_externalrepository',
    ),

    // A function taking an entity and an entity type as arguments and returning the label of the entity.
    // The entity label is the main string associated with an entity; for example, the title of a node or
    // the subject of a comment. If there is an entity object property that defines the label, use the
    // 'label' element of the 'entity keys' return value component to provide this information (see below).
    // If more complex logic is needed to determine the label of an entity, you can instead specify a callback
    // function here, which will be called to determine the entity label. See also the entity_label()
    // function, which implements this logic.
    'label callback' => 'entity_class_label',

    // A function taking an entity as argument and returning the uri elements of the entity, e.g. 'path' and 'options'.
    // The actual entity uri can be constructed by passing these elements to url().
    'uri callback' => 'entity_class_uri',

    //
    // Entity API Components
    //

    // The module whwre we can find our classes
    'module' => 'thumbwhere_externalrepository',

    // The entity class and controller class extend the classes provided by the Entity API
    'entity class' => 'ThumbWhereExternalRepository',

    // Which Entity API contoller
    'controller class' => 'ThumbWhereExternalRepositoryController',

    // The information below is used by the ThumbWhereExternalRepositoryUIController (which extends the EntityDefaultUIController)
    'admin ui' => array(
      'path' => 'admin/thumbwhere/thumbwhere_externalrepositorys',
      'file' => 'thumbwhere_externalrepository.admin.inc',
      'controller class' => 'ThumbWhereExternalRepositoryUIController',
      'menu wildcard' => '%thumbwhere_externalrepository',
    ),
  );

  return $return;
}


/**
 * Implements hook_permission().
 */
function thumbwhere_externalrepository_permission() {

  // We set up permisssions to manage entity types, manage all entities and the
  // permissions for each individual entity
  $permissions = array(
    'administer thumbwhere_externalrepositorys' => array(
      'title' => t('Administer thumbwhere_externalrepositorys'),
      'description' => t('Edit and delete all thumbwhere_externalrepositorys'),
    ),
  'send thumbwhere externalrepository notifications' => array(
          'title' => t('Send ThumbWhere Notifications'),
          'description' => t('Allow the caller to call the ThumbWhere ExternalRepository Entity Notifications Endpoint.'),
          'restrict access' => TRUE,
      ),    
  );

  return $permissions;
}


/**
 * Determines whether the given user has access to a thumbwhere_externalrepository.
 *
 * @param $op
 *   The operation being performed. One of 'view', 'update', 'create', 'delete'
 *   or just 'edit' (being the same as 'create' or 'update').
 * @param $thumbwhere_externalrepository
 *   Optionally a thumbwhere_externalrepository or a thumbwhere_externalrepository type to check access for. If nothing is
 *   given, access for all thumbwhere_externalrepositorys is determined.
 * @param $account
 *   The user to check for. Leave it to NULL to check for the global user.
 * @return boolean
 *   Whether access is allowed or not.
 */
function thumbwhere_externalrepository_access($op, $thumbwhere_externalrepository = NULL, $account = NULL) {

  //if (user_access('administer thumbwhere_externalrepositorys', $account)) {
  //  return TRUE;
 // }

  //return FALSE;
  return TRUE;
}




/**
 * Fetch a thumbwhere_externalrepository object. Make sure that the wildcard you choose
 * in the thumbwhere_externalrepository entity definition fits the function name here.
 *
 * @param $thumbwhere_externalrepository_id
 *   Integer specifying the thumbwhere_externalrepository id.
 * @param $reset
 *   A boolean indicating that the internal cache should be reset.
 * @return
 *   A fully-loaded $thumbwhere_externalrepository object or FALSE if it cannot be loaded.
 *
 * @see thumbwhere_externalrepository_load_multiple()
 */
function thumbwhere_externalrepository_load($thumbwhere_externalrepository_id, $reset = FALSE) {

  $thumbwhere_externalrepositorys = entity_load('thumbwhere_externalrepository', array($thumbwhere_externalrepository_id), array(), $reset);

  return reset($thumbwhere_externalrepositorys);
}


/**
 * Deletes a thumbwhere_externalrepository.
 */
function thumbwhere_externalrepository_delete(ThumbWhereExternalRepository $thumbwhere_externalrepository) {

  $thumbwhere_externalrepository->delete();
}

/**
 * Create a thumbwhere_externalrepository object.
 */
function thumbwhere_externalrepository_create($values = array()) {

  return entity_get_controller('thumbwhere_externalrepository')->create($values);
}


/**
 * URI callback for thumbwhere_externalrepositorys
 */
function thumbwhere_externalrepository_uri(ThumbWhereExternalRepository $thumbwhere_externalrepository) {

  return array(
    'path' => 'thumbwhere_externalrepository/' . $thumbwhere_externalrepository->pk_externalrepository,
  );
}


/**
 * Menu title callback for showing individual entities
 */
function thumbwhere_externalrepository_page_title(ThumbWhereExternalRepository $thumbwhere_externalrepository) {

	return $thumbwhere_externalrepository->pk_externalrepository;
}


/**
 * Sets up content to show an individual thumbwhere_externalrepository
 * @todo - get rid of drupal_set_title();
 */
function thumbwhere_externalrepository_page_view($thumbwhere_externalrepository, $view_mode = 'full') {



  $controller = entity_get_controller('thumbwhere_externalrepository');
  $content = $controller->view(array($thumbwhere_externalrepository->pk_externalrepository => $thumbwhere_externalrepository));
  drupal_set_title($thumbwhere_externalrepository->pk_externalrepository);
  return $content;
}


/**
 * Implements hook_views_api().
 */
function thumbwhere_externalrepository_views_api() {



  return array(
    'api' => 3,
    'path' => drupal_get_path('module', 'thumbwhere_externalrepository') . '/views',
  );
}


/**
 * Implement hook_theme().
 */
function thumbwhere_externalrepository_theme() {



  return array(
    'thumbwhere_externalrepository_add_list' => array(
      'variables' => array('content' => array()),
      'file' => 'thumbwhere_externalrepository.admin.inc',
    ),
    'thumbwhere_externalrepository' => array(
      'render element' => 'elements',
      'template' => 'thumbwhere_externalrepository',
    )
  );
}


/**
 * Implements hook_menu_local_tasks_alter().
 */
function thumbwhere_externalrepository_menu_local_tasks_alter(&$data, $router_item, $root_path) {

  // Add action link 'admin/structure/thumbwhere_externalrepositorys/add' on 'admin/structure/thumbwhere_externalrepositorys'.
  if ($root_path == 'admin/thumbwhere/thumbwhere_externalrepositorys') {
    $item = menu_get_item('admin/thumbwhere/thumbwhere_externalrepositorys/add');
    if ($item['access']) {
      $data['actions']['output'][] = array(
        '#theme' => 'menu_local_action',
        '#link' => $item,
      );
    }
  }
}


/**
 * The class used for thumbwhere_externalrepository entities
 */
class ThumbWhereExternalRepository extends Entity {

  public function __construct($values = array()) {
    parent::__construct($values, 'thumbwhere_externalrepository');
  }

  protected function defaultLabel() {
    return $this->pk_externalrepository;
  }

  protected function defaultUri() {
    return array('path' => 'thumbwhere_externalrepository/' . $this->pk_externalrepository);
  }


}



/**
 * The Controller for ThumbWhereExternalRepository entities
 */
class ThumbWhereExternalRepositoryController extends EntityAPIController {
  public function __construct($entityType) {
    parent::__construct($entityType);
  }
}



/**
* Implements hook_entity_presave().
*
* Act on an ExternalRepository entity before it is about to be created or updated.
*
* @param $entity
*   The entity object.
* @param $entity_type
*   The type of entity being saved (i.e. node, user, comment).
*/
function thumbwhere_externalrepository_entity_presave($entity, $entity_type) {

  // We only look after what we want...
  if ($entity_type == 'thumbwhere_externalrepository')  {
  	
  	
  	
 	/////////////////////////////////////////////////////
    //
    // Should we not propagate this to ThumbWhere? 
    //
    if (isset($entity->do_not_propagate)) {
      if ($entity->do_not_propagate) {
        if (twCanDebug()) {
          if ($entity->is_new) {
            if (twCanDebug()){
              debug("DO NOT PROPAGATE 'externalrepository' presave *CREATE*");
          	}
          }
          else {
            if (twCanDebug()) {
              debug("DO NOT PROPAGATE 'externalrepository' presave *UPDATE*");
            }
          }
        }

        // To cause an infinite loop of updates, comment out this line.
        return;
      }
      else if (twCanDebug()) {
        debug("OK PROAOGATE 'externalrepository' presave to ThumbWhere Server");
      }
    }
    else if (twCanDebug()) {
      debug("DO NOT PROPAGATE IS NOT SET");
    }
    //
    //
    ////////////////////////////////////////////////////////  	

  
    // .. then we create a new thumbwhere_externalrepository entity...
      // Instantiate the ThumbWhereContent class

    $twAdminAPI = new ThumbWhereAPIAdmin();

    //if (twCanDebug()) debug($twContentAPI);

    //if (twCanDebug()) debug($entity);

    //
    // Collate and validate our fields
    //

    $fields = array();
    // Compulsory so..
    if (!isset($entity->fk_host)) {
      throw new Exception('host is mandatory.');
    }
    else {
      $fields['host'] = $entity->fk_host;
    }
		
	//
	// Ensure that we have the 'thumbwhere_api_key' as we are going to pass that through in a context
	//
    if (variable_get('thumbwhere_api_key') == '') {
    	throw new Exception('ThumbWhere resource for \'ExternalRepository\' requires that variable \'thumbwhere_api_key\' is set.');
    }
    
              		
    //
    // If this externalrepository entity is new....
    //
               
    if ($entity->is_new) {
                

      //
      // ... then we create the externalrepository entity in ThumbWhere
      //

      $externalrepository = $twAdminAPI->create_external_repository($context = array(
                  'key' => variable_get('thumbwhere_api_key')
          ), $fields);
            
       if (twCanDebug()) debug($externalrepository);

      //if (twCanDebug()) debug($external_repository);

      $attributes = $externalrepository->body->external_repository->attributes();

      if (twCanDebug()) debug('newly created id = ' . $attributes['id']);

      // Assign new id.
      $entity->pk_externalrepository = intval($attributes['id']);

      if (twCanDebug()) debug('$entity->pk_externalrepository = ' . $entity->pk_externalrepository);
    }
    else
    {
          	

      //
      // .. otherwise we update the externalrepository entity in ThumbWhere
      //

      $externalrepository = $twAdminAPI->update_external_repository($id=$entity->pk_externalrepository,$context = array(
                  'key' => variable_get('thumbwhere_api_key')
          ), $fields);
            
        
    }
          	
    
  }
}

/**
 * Implements thumbwhere_externalrepository_entity_insert().
 *
 * Act on entities when inserted.
 *
 * @param $entity
 *   The entity object
 * @param $type
 *   The type of entity being inserted (i.e. node, user, comment).
 */
function thumbwhere_externalrepository_entity_insert($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_externalrepository') return;

}


/**
* Implements thumbwhere_externalrepository_entity_delete().
*
* Act on entities when inserted.
*
* @param $entity
*   The entity object
* @param $type
*   The type of entity being deleted (i.e. node, user, comment).
*/
function thumbwhere_externalrepository_entity_delete($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_externalrepository') return;


}
