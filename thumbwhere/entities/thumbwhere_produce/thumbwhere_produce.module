<?php

// ******************************************************************
// This file was generated by autogen_model_implementation_drupal.xsl
// ******************************************************************

// Include the ThumbWhere SDK
require_once(dirname(__FILE__) . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . 'dependencies' . DIRECTORY_SEPARATOR . 'tw' . DIRECTORY_SEPARATOR . 'sdk.class.php');

/**
 * @file
 * Module for the ThumbWhere Produce Entity
 *
 *
 * NOTE: This has been autogenerated.
 * 
 */

/**
 * Implement hook_entity_info().
 *
 * We define two entities here - the actual entity that will hold our domain
 * specific information and an entity that holds information about the different
 * types of entities. See here: http://drupal.org/node/977380 for a discussion on this
 * choice.
 */
function thumbwhere_produce_entity_info() {
  $return['thumbwhere_produce'] = array(

    // The human-readable name of the type.
    'label' => t('ThumbWhereProduce'),

    // (used by DrupalDefaultEntityController) The name of the entity type's base table.
    'base table' => 'thumbwhere_produce',

    // fieldable: Set to TRUE if you want your entity type to accept fields being attached to it.
    'fieldable' => TRUE,

  // Declare the endty keys..
    'entity keys' => array(
      'id' => 'pk_produce',
      'label' => 'pk_produce',
    ),

    // A function taking an entity and an entity type as arguments and returning the label of the entity.
    // The entity label is the main string associated with an entity; for example, the title of a node or
    // the subject of a comment. If there is an entity object property that defines the label, use the
    // 'label' element of the 'entity keys' return value component to provide this information (see below).
    // If more complex logic is needed to determine the label of an entity, you can instead specify a callback
    // function here, which will be called to determine the entity label. See also the entity_label()
    // function, which implements this logic.
    'label callback' => 'entity_class_label',

    // A function taking an entity as argument and returning the uri elements of the entity, e.g. 'path' and 'options'.
    // The actual entity uri can be constructed by passing these elements to url().
    'uri callback' => 'entity_class_uri',

    //
    // Entity API Components
    //

    // The module whwre we can find our classes
    'module' => 'thumbwhere_produce',

    // The entity class and controller class extend the classes provided by the Entity API
    'entity class' => 'ThumbWhereProduce',

    // Which Entity API contoller
    'controller class' => 'ThumbWhereProduceController',

    // The information below is used by the ThumbWhereProduceUIController (which extends the EntityDefaultUIController)
    'admin ui' => array(
      'path' => 'admin/thumbwhere/thumbwhere_produces',
      'file' => 'thumbwhere_produce.admin.inc',
      'controller class' => 'ThumbWhereProduceUIController',
      'menu wildcard' => '%thumbwhere_produce',
    ),
  );

  return $return;
}


/**
 * Implements hook_permission().
 */
function thumbwhere_produce_permission() {

  // We set up permisssions to manage entity types, manage all entities and the
  // permissions for each individual entity
  $permissions = array(
    'administer thumbwhere_produces' => array(
      'title' => t('Administer thumbwhere_produces'),
      'description' => t('Edit and delete all thumbwhere_produces'),
    ),
  'send thumbwhere produce notifications' => array(
          'title' => t('Send ThumbWhere Notifications'),
          'description' => t('Allow the caller to call the ThumbWhere Produce Entity Notifications Endpoint.'),
          //'restrict access' => TRUE,
      ),    
  );

  return $permissions;
}


/**
 * Determines whether the given user has access to a thumbwhere_produce.
 *
 * @param $op
 *   The operation being performed. One of 'view', 'update', 'create', 'delete'
 *   or just 'edit' (being the same as 'create' or 'update').
 * @param $thumbwhere_produce
 *   Optionally a thumbwhere_produce or a thumbwhere_produce type to check access for. If nothing is
 *   given, access for all thumbwhere_produces is determined.
 * @param $account
 *   The user to check for. Leave it to NULL to check for the global user.
 * @return boolean
 *   Whether access is allowed or not.
 */
function thumbwhere_produce_access($op, $thumbwhere_produce = NULL, $account = NULL) {

  //if (user_access('administer thumbwhere_produces', $account)) {
  //  return TRUE;
 // }

  //return FALSE;
  return TRUE;
}




/**
 * Fetch a thumbwhere_produce object. Make sure that the wildcard you choose
 * in the thumbwhere_produce entity definition fits the function name here.
 *
 * @param $thumbwhere_produce_id
 *   Integer specifying the thumbwhere_produce id.
 * @param $reset
 *   A boolean indicating that the internal cache should be reset.
 * @return
 *   A fully-loaded $thumbwhere_produce object or FALSE if it cannot be loaded.
 *
 * @see thumbwhere_produce_load_multiple()
 */
function thumbwhere_produce_load($thumbwhere_produce_id, $reset = FALSE) {

  $thumbwhere_produces = entity_load('thumbwhere_produce', array($thumbwhere_produce_id), array(), $reset);

  return reset($thumbwhere_produces);
}


/**
 * Deletes a thumbwhere_produce.
 */
function thumbwhere_produce_delete(ThumbWhereProduce $thumbwhere_produce) {

  $thumbwhere_produce->delete();
}

/**
 * Create a thumbwhere_produce object.
 */
function thumbwhere_produce_create($values = array()) {

  return entity_get_controller('thumbwhere_produce')->create($values);
}


/**
 * URI callback for thumbwhere_produces
 */
function thumbwhere_produce_uri(ThumbWhereProduce $thumbwhere_produce) {

  return array(
    'path' => 'thumbwhere_produce/' . $thumbwhere_produce->pk_produce,
  );
}


/**
 * Menu title callback for showing individual entities
 */
function thumbwhere_produce_page_title(ThumbWhereProduce $thumbwhere_produce) {

	return $thumbwhere_produce->pk_produce;
}


/**
 * Sets up content to show an individual thumbwhere_produce
 * @todo - get rid of drupal_set_title();
 */
function thumbwhere_produce_page_view($thumbwhere_produce, $view_mode = 'full') {



  $controller = entity_get_controller('thumbwhere_produce');
  $content = $controller->view(array($thumbwhere_produce->pk_produce => $thumbwhere_produce));
  drupal_set_title($thumbwhere_produce->pk_produce);
  return $content;
}


/**
 * Implements hook_views_api().
 */
function thumbwhere_produce_views_api() {



  return array(
    'api' => 3,
    'path' => drupal_get_path('module', 'thumbwhere_produce') . '/views',
  );
}


/**
 * Implement hook_theme().
 */
function thumbwhere_produce_theme() {



  return array(
    'thumbwhere_produce_add_list' => array(
      'variables' => array('content' => array()),
      'file' => 'thumbwhere_produce.admin.inc',
    ),
    'thumbwhere_produce' => array(
      'render element' => 'elements',
      'template' => 'thumbwhere_produce',
    )
  );
}


/**
 * Implements hook_menu_local_tasks_alter().
 */
function thumbwhere_produce_menu_local_tasks_alter(&$data, $router_item, $root_path) {

  // Add action link 'admin/structure/thumbwhere_produces/add' on 'admin/structure/thumbwhere_produces'.
  if ($root_path == 'admin/thumbwhere/thumbwhere_produces') {
    $item = menu_get_item('admin/thumbwhere/thumbwhere_produces/add');
    if ($item['access']) {
      $data['actions']['output'][] = array(
        '#theme' => 'menu_local_action',
        '#link' => $item,
      );
    }
  }
}


/**
 * The class used for thumbwhere_produce entities
 */
class ThumbWhereProduce extends Entity {

  public function __construct($values = array()) {
    parent::__construct($values, 'thumbwhere_produce');
  }

  protected function defaultLabel() {
    return $this->pk_produce;
  }

  protected function defaultUri() {
    return array('path' => 'thumbwhere_produce/' . $this->pk_produce);
  }


}



/**
 * The Controller for ThumbWhereProduce entities
 */
class ThumbWhereProduceController extends EntityAPIController {
  public function __construct($entityType) {
    parent::__construct($entityType);
  }
}



/**
* Implements hook_entity_presave().
*
* Act on an Produce entity before it is about to be created or updated.
*
* @param $entity
*   The entity object.
* @param $entity_type
*   The type of entity being saved (i.e. node, user, comment).
*/
function thumbwhere_produce_entity_presave($entity, $entity_type) {

  // We only look after what we want...
  if ($entity_type == 'thumbwhere_produce')  {
  	
 	/////////////////////////////////////////////////////
    //
    // Should we not propagate this to ThumbWhere? 
    //
    if (isset($entity->do_not_propagate)) {
      if ($entity->do_not_propagate) {
        if (twCanDebug()) {
          if ($entity->is_new) {
            if (twCanDebug()){
              debug("DO NOT PROPAGATE 'produce' presave *CREATE* not being sent to ThumbWhere server.");
          	}
          }
          else {
            if (twCanDebug()) {
              debug("DO NOT PROPAGATE 'produce' presave *UPDATE* not being sent to ThumbWhere server.");
            }
          }
        }

        // To cause an infinite loop of updates, comment out this line.
        return;
      }
      else if (twCanDebug()) {
        debug("OK PROAOGATE 'produce' presave to ThumbWhere server.");
      }
    }
    else if (twCanDebug()) {
      debug("DO NOT PROPAGATE IS NOT SET");
    }
    //
    //
    ////////////////////////////////////////////////////////  	

  
    // .. then we create a new thumbwhere_produce entity...
      // Instantiate the ThumbWhereContent class

    $twContentAPI = new ThumbWhereAPIContent();

    //if (twCanDebug()) debug($twContentAPI);

    //if (twCanDebug()) debug($entity);

    //
    // Collate and validate our fields
    //

    $fields = array();
    // Compulsory so..
    if (!isset($entity->fk_actor)) {
      throw new Exception('actor is mandatory.');
    }
    else {
      $fields['actor'] = $entity->fk_actor;
    }
    // Compulsory so..
    if (!isset($entity->fk_thing)) {
      throw new Exception('thing is mandatory.');
    }
    else {
      $fields['thing'] = $entity->fk_thing;
    }

    // Nullable so we only pass it through to create_produce if we have it..
    if (isset($entity->fk_place)) {
      $fields['place'] = $entity->fk_place;
    }

    // Nullable so we only pass it through to create_produce if we have it..
    if (isset($entity->fk_mediaitem)) {
      $fields['mediaitem'] = $entity->fk_mediaitem;
    }
    // Compulsory so..
    if (empty($entity->documentation)) {
      throw new Exception('documentation is mandatory.');
    }
    else {
      $fields['documentation'] = $entity->documentation;
    }
		
	//
	// Ensure that we have the 'thumbwhere_api_key' as we are going to pass that through in a context
	//
  if (variable_get('thumbwhere_api_key') == '') {
    throw new Exception('ThumbWhere resource calls for \'Produce\' requires that variable \'thumbwhere_api_key\' is set.');
  }

	//
	// Ensure that we have the 'thumbwhere_api_key' as we are going to pass that through in a context
	//
  if (!is_numeric(variable_get('thumbwhere_host_id'))) {
    throw new Exception('ThumbWhere resource calls for \'Produce\' requires that variable \'thumbwhere_host_id\' is set.');
  }


              		
    //
    // If this produce entity is new....
    //
               
    if ($entity->is_new) {
                

      //
      // ... then we create the produce entity in ThumbWhere
      //

      $produce = $twContentAPI->create_produce($context = array(
                  'key' => variable_get('thumbwhere_api_key'),
                  'origin' => variable_get('thumbwhere_host_id'),
          ), $fields);
            
       if (twCanDebug()) debug($produce);

      //if (twCanDebug()) debug($produce);

      $attributes = $produce->body->produce->attributes();

      if (twCanDebug()) debug('newly created id = ' . $attributes['id']);

      // Assign new id.
      $entity->pk_produce = intval($attributes['id']);

      if (twCanDebug()) debug('$entity->pk_produce = ' . $entity->pk_produce);
    }
    else
    {
          	

      //
      // .. otherwise we update the produce entity in ThumbWhere
      //

      $produce = $twContentAPI->update_produce($id=$entity->pk_produce,$context = array(
                  'key' => variable_get('thumbwhere_api_key'),
                  'origin' => variable_get('thumbwhere_host_id'),
          ), $fields);
            
        
    }
          	
    
  }
}

/**
 * Implements thumbwhere_produce_entity_insert().
 *
 * Act on entities when inserted.
 *
 * @param $entity
 *   The entity object
 * @param $type
 *   The type of entity being inserted (i.e. node, user, comment).
 */
function thumbwhere_produce_entity_insert($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_produce') return;

}


/**
* Implements thumbwhere_produce_entity_delete().
*
* Act on entities when inserted.
*
* @param $entity
*   The entity object
* @param $type
*   The type of entity being deleted (i.e. node, user, comment).
*/
function thumbwhere_produce_entity_delete($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_produce') return;


}
