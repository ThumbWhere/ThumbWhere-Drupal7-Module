<?php

// ******************************************************************
// This file was generated by autogen_model_implementation_drupal.xsl
// ******************************************************************

// Include the ThumbWhere SDK
require_once(dirname(__FILE__) . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . 'dependencies' . DIRECTORY_SEPARATOR . 'tw' . DIRECTORY_SEPARATOR . 'sdk.class.php');

/**
 * @file
 * Module for the ThumbWhere HostCommandTemplateType Entity
 *
 *
 * NOTE: This has been autogenerated.
 * 
 */

/**
 * Implement hook_entity_info().
 *
 * We define two entities here - the actual entity that will hold our domain
 * specific information and an entity that holds information about the different
 * types of entities. See here: http://drupal.org/node/977380 for a discussion on this
 * choice.
 */
function thumbwhere_hostcommandtemplatetype_entity_info() {
  $return['thumbwhere_hostcommandtemplatetype'] = array(

    // The human-readable name of the type.
    'label' => t('ThumbWhereHostCommandTemplateType'),

    // (used by DrupalDefaultEntityController) The name of the entity type's base table.
    'base table' => 'thumbwhere_hostcommandtemplatetype',

    // fieldable: Set to TRUE if you want your entity type to accept fields being attached to it.
    'fieldable' => TRUE,

  // Declare the endty keys..
    'entity keys' => array(
      'id' => 'pk_hostcommandtemplatetype',
      'label' => 'name',
    ),

    // A function taking an entity and an entity type as arguments and returning the label of the entity.
    // The entity label is the main string associated with an entity; for example, the title of a node or
    // the subject of a comment. If there is an entity object property that defines the label, use the
    // 'label' element of the 'entity keys' return value component to provide this information (see below).
    // If more complex logic is needed to determine the label of an entity, you can instead specify a callback
    // function here, which will be called to determine the entity label. See also the entity_label()
    // function, which implements this logic.
    'label callback' => 'entity_class_label',

    // A function taking an entity as argument and returning the uri elements of the entity, e.g. 'path' and 'options'.
    // The actual entity uri can be constructed by passing these elements to url().
    'uri callback' => 'entity_class_uri',

    //
    // Entity API Components
    //

    // The module whwre we can find our classes
    'module' => 'thumbwhere_hostcommandtemplatetype',

    // The entity class and controller class extend the classes provided by the Entity API
    'entity class' => 'ThumbWhereHostCommandTemplateType',

    // Which Entity API contoller
    'controller class' => 'ThumbWhereHostCommandTemplateTypeController',

    // The information below is used by the ThumbWhereHostCommandTemplateTypeUIController (which extends the EntityDefaultUIController)
    'admin ui' => array(
      'path' => 'admin/thumbwhere/thumbwhere_hostcommandtemplatetypes',
      'file' => 'thumbwhere_hostcommandtemplatetype.admin.inc',
      'controller class' => 'ThumbWhereHostCommandTemplateTypeUIController',
      'menu wildcard' => '%thumbwhere_hostcommandtemplatetype',
    ),
  );

  return $return;
}


/**
 * Implements hook_permission().
 */
function thumbwhere_hostcommandtemplatetype_permission() {

  // We set up permisssions to manage entity types, manage all entities and the
  // permissions for each individual entity
  $permissions = array(
    'administer thumbwhere_hostcommandtemplatetypes' => array(
      'title' => t('Administer thumbwhere_hostcommandtemplatetypes'),
      'description' => t('Edit and delete all thumbwhere_hostcommandtemplatetypes'),
    ),
  'send thumbwhere hostcommandtemplatetype notifications' => array(
          'title' => t('Send ThumbWhere Notifications'),
          'description' => t('Allow the caller to call the ThumbWhere HostCommandTemplateType Entity Notifications Endpoint.'),
          'restrict access' => TRUE,
      ),    
  );

  return $permissions;
}


/**
 * Determines whether the given user has access to a thumbwhere_hostcommandtemplatetype.
 *
 * @param $op
 *   The operation being performed. One of 'view', 'update', 'create', 'delete'
 *   or just 'edit' (being the same as 'create' or 'update').
 * @param $thumbwhere_hostcommandtemplatetype
 *   Optionally a thumbwhere_hostcommandtemplatetype or a thumbwhere_hostcommandtemplatetype type to check access for. If nothing is
 *   given, access for all thumbwhere_hostcommandtemplatetypes is determined.
 * @param $account
 *   The user to check for. Leave it to NULL to check for the global user.
 * @return boolean
 *   Whether access is allowed or not.
 */
function thumbwhere_hostcommandtemplatetype_access($op, $thumbwhere_hostcommandtemplatetype = NULL, $account = NULL) {

  //if (user_access('administer thumbwhere_hostcommandtemplatetypes', $account)) {
  //  return TRUE;
 // }

  //return FALSE;
  return TRUE;
}




/**
 * Fetch a thumbwhere_hostcommandtemplatetype object. Make sure that the wildcard you choose
 * in the thumbwhere_hostcommandtemplatetype entity definition fits the function name here.
 *
 * @param $thumbwhere_hostcommandtemplatetype_id
 *   Integer specifying the thumbwhere_hostcommandtemplatetype id.
 * @param $reset
 *   A boolean indicating that the internal cache should be reset.
 * @return
 *   A fully-loaded $thumbwhere_hostcommandtemplatetype object or FALSE if it cannot be loaded.
 *
 * @see thumbwhere_hostcommandtemplatetype_load_multiple()
 */
function thumbwhere_hostcommandtemplatetype_load($thumbwhere_hostcommandtemplatetype_id, $reset = FALSE) {

  $thumbwhere_hostcommandtemplatetypes = entity_load('thumbwhere_hostcommandtemplatetype', array($thumbwhere_hostcommandtemplatetype_id), array(), $reset);

  return reset($thumbwhere_hostcommandtemplatetypes);
}


/**
 * Deletes a thumbwhere_hostcommandtemplatetype.
 */
function thumbwhere_hostcommandtemplatetype_delete(ThumbWhereHostCommandTemplateType $thumbwhere_hostcommandtemplatetype) {

  $thumbwhere_hostcommandtemplatetype->delete();
}

/**
 * Create a thumbwhere_hostcommandtemplatetype object.
 */
function thumbwhere_hostcommandtemplatetype_create($values = array()) {

  return entity_get_controller('thumbwhere_hostcommandtemplatetype')->create($values);
}


/**
 * URI callback for thumbwhere_hostcommandtemplatetypes
 */
function thumbwhere_hostcommandtemplatetype_uri(ThumbWhereHostCommandTemplateType $thumbwhere_hostcommandtemplatetype) {

  return array(
    'path' => 'thumbwhere_hostcommandtemplatetype/' . $thumbwhere_hostcommandtemplatetype->pk_hostcommandtemplatetype,
  );
}


/**
 * Menu title callback for showing individual entities
 */
function thumbwhere_hostcommandtemplatetype_page_title(ThumbWhereHostCommandTemplateType $thumbwhere_hostcommandtemplatetype) {

	return $thumbwhere_hostcommandtemplatetype->name;
}


/**
 * Sets up content to show an individual thumbwhere_hostcommandtemplatetype
 * @todo - get rid of drupal_set_title();
 */
function thumbwhere_hostcommandtemplatetype_page_view($thumbwhere_hostcommandtemplatetype, $view_mode = 'full') {



  $controller = entity_get_controller('thumbwhere_hostcommandtemplatetype');
  $content = $controller->view(array($thumbwhere_hostcommandtemplatetype->pk_hostcommandtemplatetype => $thumbwhere_hostcommandtemplatetype));
  drupal_set_title($thumbwhere_hostcommandtemplatetype->name);
  return $content;
}


/**
 * Implements hook_views_api().
 */
function thumbwhere_hostcommandtemplatetype_views_api() {



  return array(
    'api' => 3,
    'path' => drupal_get_path('module', 'thumbwhere_hostcommandtemplatetype') . '/views',
  );
}


/**
 * Implement hook_theme().
 */
function thumbwhere_hostcommandtemplatetype_theme() {



  return array(
    'thumbwhere_hostcommandtemplatetype_add_list' => array(
      'variables' => array('content' => array()),
      'file' => 'thumbwhere_hostcommandtemplatetype.admin.inc',
    ),
    'thumbwhere_hostcommandtemplatetype' => array(
      'render element' => 'elements',
      'template' => 'thumbwhere_hostcommandtemplatetype',
    )
  );
}


/**
 * Implements hook_menu_local_tasks_alter().
 */
function thumbwhere_hostcommandtemplatetype_menu_local_tasks_alter(&$data, $router_item, $root_path) {

  // Add action link 'admin/structure/thumbwhere_hostcommandtemplatetypes/add' on 'admin/structure/thumbwhere_hostcommandtemplatetypes'.
  if ($root_path == 'admin/thumbwhere/thumbwhere_hostcommandtemplatetypes') {
    $item = menu_get_item('admin/thumbwhere/thumbwhere_hostcommandtemplatetypes/add');
    if ($item['access']) {
      $data['actions']['output'][] = array(
        '#theme' => 'menu_local_action',
        '#link' => $item,
      );
    }
  }
}


/**
 * The class used for thumbwhere_hostcommandtemplatetype entities
 */
class ThumbWhereHostCommandTemplateType extends Entity {

  public function __construct($values = array()) {
    parent::__construct($values, 'thumbwhere_hostcommandtemplatetype');
  }

  protected function defaultLabel() {
    return $this->name;
  }

  protected function defaultUri() {
    return array('path' => 'thumbwhere_hostcommandtemplatetype/' . $this->pk_hostcommandtemplatetype);
  }


}



/**
 * The Controller for ThumbWhereHostCommandTemplateType entities
 */
class ThumbWhereHostCommandTemplateTypeController extends EntityAPIController {
  public function __construct($entityType) {
    parent::__construct($entityType);
  }
}



/**
* Implements hook_entity_presave().
*
* Act on an HostCommandTemplateType entity before it is about to be created or updated.
*
* @param $entity
*   The entity object.
* @param $entity_type
*   The type of entity being saved (i.e. node, user, comment).
*/
function thumbwhere_hostcommandtemplatetype_entity_presave($entity, $entity_type) {

  // We only look after what we want...
  if ($entity_type == 'thumbwhere_hostcommandtemplatetype')  {
  	
  	
  	
 	/////////////////////////////////////////////////////
    //
    // Should we not propagate this to ThumbWhere? 
    //
    if (isset($entity->do_not_propagate)) {
      if ($entity->do_not_propagate) {
        if (twCanDebug()) {
          if ($entity->is_new) {
            if (twCanDebug()){
              debug("DO NOT PROPAGATE 'hostcommandtemplatetype' presave *CREATE*");
          	}
          }
          else {
            if (twCanDebug()) {
              debug("DO NOT PROPAGATE 'hostcommandtemplatetype' presave *UPDATE*");
            }
          }
        }

        // To cause an infinite loop of updates, comment out this line.
        return;
      }
      else if (twCanDebug()) {
        debug("OK PROAOGATE 'hostcommandtemplatetype' presave to ThumbWhere Server");
      }
    }
    else if (twCanDebug()) {
      debug("DO NOT PROPAGATE IS NOT SET");
    }
    //
    //
    ////////////////////////////////////////////////////////  	

  
    // .. then we create a new thumbwhere_hostcommandtemplatetype entity...
      // Instantiate the ThumbWhereContent class

    $twAdminAPI = new ThumbWhereAPIAdmin();

    //if (twCanDebug()) debug($twContentAPI);

    //if (twCanDebug()) debug($entity);

    //
    // Collate and validate our fields
    //

    $fields = array();
    // Compulsory so..
    if (empty($entity->name)) {
      throw new Exception('name is mandatory.');
    }
    else {
      $fields['name'] = $entity->name;
    }
		
	//
	// Ensure that we have the 'thumbwhere_api_key' as we are going to pass that through in a context
	//
    if (variable_get('thumbwhere_api_key') == '') {
    	throw new Exception('ThumbWhere resource for \'HostCommandTemplateType\' requires that variable \'thumbwhere_api_key\' is set.');
    }
    
              		
    //
    // If this hostcommandtemplatetype entity is new....
    //
               
    if ($entity->is_new) {
                

      //
      // ... then we create the hostcommandtemplatetype entity in ThumbWhere
      //

      $hostcommandtemplatetype = $twAdminAPI->create_host_command_template_type($context = array(
                  'key' => variable_get('thumbwhere_api_key')
          ), $fields);
            
       if (twCanDebug()) debug($hostcommandtemplatetype);

      //if (twCanDebug()) debug($host_command_template_type);

      $attributes = $hostcommandtemplatetype->body->host_command_template_type->attributes();

      if (twCanDebug()) debug('newly created id = ' . $attributes['id']);

      // Assign new id.
      $entity->pk_hostcommandtemplatetype = intval($attributes['id']);

      if (twCanDebug()) debug('$entity->pk_hostcommandtemplatetype = ' . $entity->pk_hostcommandtemplatetype);
    }
    else
    {
          	

      //
      // .. otherwise we update the hostcommandtemplatetype entity in ThumbWhere
      //

      $hostcommandtemplatetype = $twAdminAPI->update_host_command_template_type($id=$entity->pk_hostcommandtemplatetype,$context = array(
                  'key' => variable_get('thumbwhere_api_key')
          ), $fields);
            
        
    }
          	
    
  }
}

/**
 * Implements thumbwhere_hostcommandtemplatetype_entity_insert().
 *
 * Act on entities when inserted.
 *
 * @param $entity
 *   The entity object
 * @param $type
 *   The type of entity being inserted (i.e. node, user, comment).
 */
function thumbwhere_hostcommandtemplatetype_entity_insert($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_hostcommandtemplatetype') return;

}


/**
* Implements thumbwhere_hostcommandtemplatetype_entity_delete().
*
* Act on entities when inserted.
*
* @param $entity
*   The entity object
* @param $type
*   The type of entity being deleted (i.e. node, user, comment).
*/
function thumbwhere_hostcommandtemplatetype_entity_delete($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_hostcommandtemplatetype') return;


}
