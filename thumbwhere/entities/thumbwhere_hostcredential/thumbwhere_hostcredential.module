<?php

// ******************************************************************
// This file was generated by autogen_model_implementation_drupal.xsl
// ******************************************************************

// Include the ThumbWhere SDK
require_once(dirname(__FILE__) . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . 'dependencies' . DIRECTORY_SEPARATOR . 'tw' . DIRECTORY_SEPARATOR . 'sdk.class.php');

/**
 * @file
 * Module for the ThumbWhere HostCredential Entity
 *
 *
 * NOTE: This has been autogenerated.
 * 
 */

/**
 * Implement hook_entity_info().
 *
 * We define two entities here - the actual entity that will hold our domain
 * specific information and an entity that holds information about the different
 * types of entities. See here: http://drupal.org/node/977380 for a discussion on this
 * choice.
 */
function thumbwhere_hostcredential_entity_info() {
  $return['thumbwhere_hostcredential'] = array(

    // The human-readable name of the type.
    'label' => t('ThumbWhereHostCredential'),

    // (used by DrupalDefaultEntityController) The name of the entity type's base table.
    'base table' => 'thumbwhere_hostcredential',

    // fieldable: Set to TRUE if you want your entity type to accept fields being attached to it.
    'fieldable' => TRUE,

  // Declare the endty keys..
    'entity keys' => array(
      'id' => 'pk_hostcredential',
      'label' => 'pk_hostcredential',
    ),

    // A function taking an entity and an entity type as arguments and returning the label of the entity.
    // The entity label is the main string associated with an entity; for example, the title of a node or
    // the subject of a comment. If there is an entity object property that defines the label, use the
    // 'label' element of the 'entity keys' return value component to provide this information (see below).
    // If more complex logic is needed to determine the label of an entity, you can instead specify a callback
    // function here, which will be called to determine the entity label. See also the entity_label()
    // function, which implements this logic.
    'label callback' => 'entity_class_label',

    // A function taking an entity as argument and returning the uri elements of the entity, e.g. 'path' and 'options'.
    // The actual entity uri can be constructed by passing these elements to url().
    'uri callback' => 'entity_class_uri',

    //
    // Entity API Components
    //

    // The module whwre we can find our classes
    'module' => 'thumbwhere_hostcredential',

    // The entity class and controller class extend the classes provided by the Entity API
    'entity class' => 'ThumbWhereHostCredential',

    // Which Entity API contoller
    'controller class' => 'ThumbWhereHostCredentialController',

    // The information below is used by the ThumbWhereHostCredentialUIController (which extends the EntityDefaultUIController)
    'admin ui' => array(
      'path' => 'admin/thumbwhere/thumbwhere_hostcredentials',
      'file' => 'thumbwhere_hostcredential.admin.inc',
      'controller class' => 'ThumbWhereHostCredentialUIController',
      'menu wildcard' => '%thumbwhere_hostcredential',
    ),
  );

  return $return;
}


/**
 * Implements hook_permission().
 */
function thumbwhere_hostcredential_permission() {

  // We set up permisssions to manage entity types, manage all entities and the
  // permissions for each individual entity
  $permissions = array(
    'administer thumbwhere_hostcredentials' => array(
      'title' => t('Administer thumbwhere_hostcredentials'),
      'description' => t('Administer all thumbwhere_hostcredentials entities'),
      //'restrict access' => TRUE,
    ),
  'send thumbwhere hostcredential notifications' => array(
      'title' => t('Send ThumbWhere Notifications'),
      'description' => t('Allow the caller to call the ThumbWhere HostCredential Entity Notifications Endpoint.'),
      //'restrict access' => TRUE,
    ),    
  );

  return $permissions;
}


/**
 * Determines whether the given user has access to a thumbwhere_hostcredential.
 *
 * @param $op
 *   The operation being performed. One of 'view', 'update', 'create', 'delete'
 *   or just 'edit' (being the same as 'create' or 'update').
 * @param $thumbwhere_hostcredential
 *   Optionally a thumbwhere_hostcredential or a thumbwhere_hostcredential type to check access for. If nothing is
 *   given, access for all thumbwhere_hostcredentials is determined.
 * @param $account
 *   The user to check for. Leave it to NULL to check for the global user.
 * @return boolean
 *   Whether access is allowed or not.
 */
function thumbwhere_hostcredential_access($op, $thumbwhere_hostcredential = NULL, $account = NULL) {

  if (user_access('administer thumbwhere_hostcredentials', $account)) {
    return TRUE;
  }
 
  return FALSE;
}




/**
 * Fetch a thumbwhere_hostcredential object. Make sure that the wildcard you choose
 * in the thumbwhere_hostcredential entity definition fits the function name here.
 *
 * @param $thumbwhere_hostcredential_id
 *   Integer specifying the thumbwhere_hostcredential id.
 * @param $reset
 *   A boolean indicating that the internal cache should be reset.
 * @return
 *   A fully-loaded $thumbwhere_hostcredential object or FALSE if it cannot be loaded.
 *
 * @see thumbwhere_hostcredential_load_multiple()
 */
function thumbwhere_hostcredential_load($thumbwhere_hostcredential_id, $reset = FALSE) {

  $thumbwhere_hostcredentials = entity_load('thumbwhere_hostcredential', array($thumbwhere_hostcredential_id), array(), $reset);

  return reset($thumbwhere_hostcredentials);
}


/**
 * Deletes a thumbwhere_hostcredential.
 */
function thumbwhere_hostcredential_delete(ThumbWhereHostCredential $thumbwhere_hostcredential) {

  $thumbwhere_hostcredential->delete();
}

/**
 * Create a thumbwhere_hostcredential object.
 */
function thumbwhere_hostcredential_create($values = array()) {

  return entity_get_controller('thumbwhere_hostcredential')->create($values);
}


/**
 * URI callback for thumbwhere_hostcredentials
 */
function thumbwhere_hostcredential_uri(ThumbWhereHostCredential $thumbwhere_hostcredential) {

  return array(
    'path' => 'thumbwhere_hostcredential/' . $thumbwhere_hostcredential->pk_hostcredential,
  );
}


/**
 * Menu title callback for showing individual entities
 */
function thumbwhere_hostcredential_page_title(ThumbWhereHostCredential $thumbwhere_hostcredential) {

	return $thumbwhere_hostcredential->pk_hostcredential;
}


/**
 * Sets up content to show an individual thumbwhere_hostcredential
 * @todo - get rid of drupal_set_title();
 */
function thumbwhere_hostcredential_page_view($thumbwhere_hostcredential, $view_mode = 'full') {



  $controller = entity_get_controller('thumbwhere_hostcredential');
  $content = $controller->view(array($thumbwhere_hostcredential->pk_hostcredential => $thumbwhere_hostcredential));
  drupal_set_title($thumbwhere_hostcredential->pk_hostcredential);
  return $content;
}


/**
 * Implements hook_views_api().
 */
function thumbwhere_hostcredential_views_api() {



  return array(
    'api' => 3,
    'path' => drupal_get_path('module', 'thumbwhere_hostcredential') . '/views',
  );
}


/**
 * Implement hook_theme().
 */
function thumbwhere_hostcredential_theme() {



  return array(
    'thumbwhere_hostcredential_add_list' => array(
      'variables' => array('content' => array()),
      'file' => 'thumbwhere_hostcredential.admin.inc',
    ),
    'thumbwhere_hostcredential' => array(
      'render element' => 'elements',
      'template' => 'thumbwhere_hostcredential',
    )
  );
}


/**
 * Implements hook_menu_local_tasks_alter().
 */
function thumbwhere_hostcredential_menu_local_tasks_alter(&$data, $router_item, $root_path) {

  // Add action link 'admin/structure/thumbwhere_hostcredentials/add' on 'admin/structure/thumbwhere_hostcredentials'.
  if ($root_path == 'admin/thumbwhere/thumbwhere_hostcredentials') {
    $item = menu_get_item('admin/thumbwhere/thumbwhere_hostcredentials/add');
    if ($item['access']) {
      $data['actions']['output'][] = array(
        '#theme' => 'menu_local_action',
        '#link' => $item,
      );
    }
  }
}


/**
 * The class used for thumbwhere_hostcredential entities
 */
class ThumbWhereHostCredential extends Entity {

  public function __construct($values = array()) {
    parent::__construct($values, 'thumbwhere_hostcredential');
  }

  protected function defaultLabel() {
    return $this->pk_hostcredential;
  }

  protected function defaultUri() {
    return array('path' => 'thumbwhere_hostcredential/' . $this->pk_hostcredential);
  }


}



/**
 * The Controller for ThumbWhereHostCredential entities
 */
class ThumbWhereHostCredentialController extends EntityAPIController {
  public function __construct($entityType) {
    parent::__construct($entityType);
  }
}



/**
* Implements hook_entity_presave().
*
* Act on an HostCredential entity before it is about to be created or updated.
*
* @param $entity
*   The entity object.
* @param $entity_type
*   The type of entity being saved (i.e. node, user, comment).
*/
function thumbwhere_hostcredential_entity_presave($entity, $entity_type) {

  // We only look after what we want...
  if ($entity_type == 'thumbwhere_hostcredential')  {
  	
    // Should we not propagate this to ThumbWhere? 
    if (isset($entity->do_not_propagate)) {
      if ($entity->do_not_propagate) {
        // To cause an infinite loop of updates, comment out this line.
        return;
      }
    }

  
    // .. then we create a new thumbwhere_hostcredential entity...
      // Instantiate the ThumbWhereContent class

    $twAdminAPI = new ThumbWhereAPIAdmin();


    //
    // Collate and validate our fields
    //

    $fields = array();

    // Nullable so we only pass it through to create_host_credential if we have it..
    if (isset($entity->fk_hostprovideraccount)) {
      $fields['hostprovideraccount'] = $entity->fk_hostprovideraccount;
    }

    // Nullable so we only pass it through to create_host_credential if we have it..
    if (!empty($entity->username)) {
      $fields['username'] = $entity->username;
    }

    // Nullable so we only pass it through to create_host_credential if we have it..
    if (!empty($entity->password)) {
      $fields['password'] = $entity->password;
    }

    // Nullable so we only pass it through to create_host_credential if we have it..
    if (!empty($entity->sshprivatekey)) {
      $fields['sshprivatekey'] = $entity->sshprivatekey;
    }
		
	//
	// Ensure that we have the 'thumbwhere_api_key' as we are going to pass that through in a context
	//
  if (variable_get('thumbwhere_api_key') == '') {
    throw new Exception('ThumbWhere resource calls for \'HostCredential\' requires that variable \'thumbwhere_api_key\' is set.');
  }

	//
	// Ensure that we have the 'thumbwhere_api_key' as we are going to pass that through in a context
	//
  if (!is_numeric(variable_get('thumbwhere_host_id'))) {
    throw new Exception('ThumbWhere resource calls for \'HostCredential\' requires that variable \'thumbwhere_host_id\' is set.');
  }


              		
    //
    // If this hostcredential entity is new....
    //
               
    if ($entity->is_new) {
                

      //
      // ... then we create the hostcredential entity in ThumbWhere
      //

      $hostcredential = $twAdminAPI->create_host_credential($context = array(
                  'key' => variable_get('thumbwhere_api_key'),
                  'origin' => variable_get('thumbwhere_host_id'),
          ), $fields);

      $attributes = $hostcredential->body->host_credential->attributes();

      //if (twCanDebug()) debug('newly created id = ' . t($attributes['id']));

      // Assign new id.
      $entity->pk_hostcredential = intval($attributes['id']);

      //if (twCanDebug()) debug(t('$entity->pk_hostcredential = ' . $entity->pk_hostcredential));
    }
    else
    {
          	

      //
      // .. otherwise we update the hostcredential entity in ThumbWhere
      //

      $hostcredential = $twAdminAPI->update_host_credential($id=$entity->pk_hostcredential,$context = array(
                  'key' => variable_get('thumbwhere_api_key'),
                  'origin' => variable_get('thumbwhere_host_id'),
          ), $fields);
            
        
    }
          	
    
  }
}

/**
 * Implements thumbwhere_hostcredential_entity_insert().
 *
 * Act on entities when inserted.
 *
 * @param $entity
 *   The entity object
 * @param $type
 *   The type of entity being inserted (i.e. node, user, comment).
 */
function thumbwhere_hostcredential_entity_insert($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_hostcredential') return;

}


/**
* Implements thumbwhere_hostcredential_entity_delete().
*
* Act on entities when inserted.
*
* @param $entity
*   The entity object
* @param $type
*   The type of entity being deleted (i.e. node, user, comment).
*/
function thumbwhere_hostcredential_entity_delete($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_hostcredential') return;


}
