<?php

// ******************************************************************
// This file was generated by autogen_model_implementation_drupal.xsl
// ******************************************************************

// Include the ThumbWhere SDK
require_once(dirname(__FILE__) . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . 'dependencies' . DIRECTORY_SEPARATOR . 'tw' . DIRECTORY_SEPARATOR . 'sdk.class.php');

/**
 * @file
 * Module for the ThumbWhere Action Entity
 *
 *
 * NOTE: This has been autogenerated.
 * 
 */

/**
 * Implement hook_entity_info().
 *
 * We define two entities here - the actual entity that will hold our domain
 * specific information and an entity that holds information about the different
 * types of entities. See here: http://drupal.org/node/977380 for a discussion on this
 * choice.
 */
function thumbwhere_action_entity_info() {
  $return['thumbwhere_action'] = array(

    // The human-readable name of the type.
    'label' => t('ThumbWhereAction'),

    // (used by DrupalDefaultEntityController) The name of the entity type's base table.
    'base table' => 'thumbwhere_action',

    // fieldable: Set to TRUE if you want your entity type to accept fields being attached to it.
    'fieldable' => TRUE,

  // Declare the endty keys..
    'entity keys' => array(
      'id' => 'pk_action',
      'label' => 'pk_action',
    ),

    // A function taking an entity and an entity type as arguments and returning the label of the entity.
    // The entity label is the main string associated with an entity; for example, the title of a node or
    // the subject of a comment. If there is an entity object property that defines the label, use the
    // 'label' element of the 'entity keys' return value component to provide this information (see below).
    // If more complex logic is needed to determine the label of an entity, you can instead specify a callback
    // function here, which will be called to determine the entity label. See also the entity_label()
    // function, which implements this logic.
    'label callback' => 'entity_class_label',

    // A function taking an entity as argument and returning the uri elements of the entity, e.g. 'path' and 'options'.
    // The actual entity uri can be constructed by passing these elements to url().
    'uri callback' => 'entity_class_uri',

    //
    // Entity API Components
    //

    // The module whwre we can find our classes
    'module' => 'thumbwhere_action',

    // The entity class and controller class extend the classes provided by the Entity API
    'entity class' => 'ThumbWhereAction',

    // Which Entity API contoller
    'controller class' => 'ThumbWhereActionController',

    // The information below is used by the ThumbWhereActionUIController (which extends the EntityDefaultUIController)
    'admin ui' => array(
      'path' => 'admin/thumbwhere/thumbwhere_actions',
      'file' => 'thumbwhere_action.admin.inc',
      'controller class' => 'ThumbWhereActionUIController',
      'menu wildcard' => '%thumbwhere_action',
    ),
  );

  return $return;
}


/**
 * Implements hook_permission().
 */
function thumbwhere_action_permission() {

  // We set up permisssions to manage entity types, manage all entities and the
  // permissions for each individual entity
  $permissions = array(
    'administer thumbwhere_actions' => array(
      'title' => t('Administer thumbwhere_actions'),
      'description' => t('Administer all thumbwhere_actions entities'),
      //'restrict access' => TRUE,
    ),
  'send thumbwhere action notifications' => array(
      'title' => t('Send ThumbWhere Notifications'),
      'description' => t('Allow the caller to call the ThumbWhere Action Entity Notifications Endpoint.'),
      //'restrict access' => TRUE,
    ),    
  );

  return $permissions;
}


/**
 * Determines whether the given user has access to a thumbwhere_action.
 *
 * @param $op
 *   The operation being performed. One of 'view', 'update', 'create', 'delete'
 *   or just 'edit' (being the same as 'create' or 'update').
 * @param $thumbwhere_action
 *   Optionally a thumbwhere_action or a thumbwhere_action type to check access for. If nothing is
 *   given, access for all thumbwhere_actions is determined.
 * @param $account
 *   The user to check for. Leave it to NULL to check for the global user.
 * @return boolean
 *   Whether access is allowed or not.
 */
function thumbwhere_action_access($op, $thumbwhere_action = NULL, $account = NULL) {

  if (user_access('administer thumbwhere_actions', $account)) {
    return TRUE;
  }
 
  return FALSE;
}




/**
 * Fetch a thumbwhere_action object. Make sure that the wildcard you choose
 * in the thumbwhere_action entity definition fits the function name here.
 *
 * @param $thumbwhere_action_id
 *   Integer specifying the thumbwhere_action id.
 * @param $reset
 *   A boolean indicating that the internal cache should be reset.
 * @return
 *   A fully-loaded $thumbwhere_action object or FALSE if it cannot be loaded.
 *
 * @see thumbwhere_action_load_multiple()
 */
function thumbwhere_action_load($thumbwhere_action_id, $reset = FALSE) {

  $thumbwhere_actions = entity_load('thumbwhere_action', array($thumbwhere_action_id), array(), $reset);

  return reset($thumbwhere_actions);
}


/**
 * Deletes a thumbwhere_action.
 */
function thumbwhere_action_delete(ThumbWhereAction $thumbwhere_action) {

  $thumbwhere_action->delete();
}

/**
 * Create a thumbwhere_action object.
 */
function thumbwhere_action_create($values = array()) {

  return entity_get_controller('thumbwhere_action')->create($values);
}


/**
 * URI callback for thumbwhere_actions
 */
function thumbwhere_action_uri(ThumbWhereAction $thumbwhere_action) {

  return array(
    'path' => 'thumbwhere_action/' . $thumbwhere_action->pk_action,
  );
}


/**
 * Menu title callback for showing individual entities
 */
function thumbwhere_action_page_title(ThumbWhereAction $thumbwhere_action) {

	return $thumbwhere_action->pk_action;
}


/**
 * Sets up content to show an individual thumbwhere_action
 * @todo - get rid of drupal_set_title();
 */
function thumbwhere_action_page_view($thumbwhere_action, $view_mode = 'full') {



  $controller = entity_get_controller('thumbwhere_action');
  $content = $controller->view(array($thumbwhere_action->pk_action => $thumbwhere_action));
  drupal_set_title($thumbwhere_action->pk_action);
  return $content;
}


/**
 * Implements hook_views_api().
 */
function thumbwhere_action_views_api() {



  return array(
    'api' => 3,
    'path' => drupal_get_path('module', 'thumbwhere_action') . '/views',
  );
}


/**
 * Implement hook_theme().
 */
function thumbwhere_action_theme() {



  return array(
    'thumbwhere_action_add_list' => array(
      'variables' => array('content' => array()),
      'file' => 'thumbwhere_action.admin.inc',
    ),
    'thumbwhere_action' => array(
      'render element' => 'elements',
      'template' => 'thumbwhere_action',
    )
  );
}


/**
 * Implements hook_menu_local_tasks_alter().
 */
function thumbwhere_action_menu_local_tasks_alter(&$data, $router_item, $root_path) {

  // Add action link 'admin/structure/thumbwhere_actions/add' on 'admin/structure/thumbwhere_actions'.
  if ($root_path == 'admin/thumbwhere/thumbwhere_actions') {
    $item = menu_get_item('admin/thumbwhere/thumbwhere_actions/add');
    if ($item['access']) {
      $data['actions']['output'][] = array(
        '#theme' => 'menu_local_action',
        '#link' => $item,
      );
    }
  }
}


/**
 * The class used for thumbwhere_action entities
 */
class ThumbWhereAction extends Entity {

  public function __construct($values = array()) {
    parent::__construct($values, 'thumbwhere_action');
  }

  protected function defaultLabel() {
    return $this->pk_action;
  }

  protected function defaultUri() {
    return array('path' => 'thumbwhere_action/' . $this->pk_action);
  }


}



/**
 * The Controller for ThumbWhereAction entities
 */
class ThumbWhereActionController extends EntityAPIController {
  public function __construct($entityType) {
    parent::__construct($entityType);
  }
}



/**
* Implements hook_entity_presave().
*
* Act on an Action entity before it is about to be created or updated.
*
* @param $entity
*   The entity object.
* @param $entity_type
*   The type of entity being saved (i.e. node, user, comment).
*/
function thumbwhere_action_entity_presave($entity, $entity_type) {

  // We only look after what we want...
  if ($entity_type == 'thumbwhere_action')  {
  	
    // Should we not propagate this to ThumbWhere? 
    if (isset($entity->do_not_propagate)) {
      if ($entity->do_not_propagate) {
        // To cause an infinite loop of updates, comment out this line.
        return;
      }
    }

  
    // .. then we create a new thumbwhere_action entity...
      // Instantiate the ThumbWhereContent class

    $twContentAPI = new ThumbWhereAPIContent();


    //
    // Collate and validate our fields
    //

    $fields = array();

    // Nullable so we only pass it through to create_action if we have it..
    if (isset($entity->fk_contentingest)) {
      $fields['contentingest'] = $entity->fk_contentingest;
    }

    // Nullable so we only pass it through to create_action if we have it..
    if (isset($entity->fk_like)) {
      $fields['like'] = $entity->fk_like;
    }

    // Nullable so we only pass it through to create_action if we have it..
    if (isset($entity->fk_follow)) {
      $fields['follow'] = $entity->fk_follow;
    }

    // Nullable so we only pass it through to create_action if we have it..
    if (isset($entity->fk_fan)) {
      $fields['fan'] = $entity->fk_fan;
    }

    // Nullable so we only pass it through to create_action if we have it..
    if (isset($entity->fk_share)) {
      $fields['share'] = $entity->fk_share;
    }

    // Nullable so we only pass it through to create_action if we have it..
    if (isset($entity->fk_awardedtrophy)) {
      $fields['awardedtrophy'] = $entity->fk_awardedtrophy;
    }

    // Nullable so we only pass it through to create_action if we have it..
    if (isset($entity->fk_mediaitemrating)) {
      $fields['mediaitemrating'] = $entity->fk_mediaitemrating;
    }

    // Nullable so we only pass it through to create_action if we have it..
    if (isset($entity->fk_booking)) {
      $fields['booking'] = $entity->fk_booking;
    }

    // Nullable so we only pass it through to create_action if we have it..
    if (isset($entity->fk_checkin)) {
      $fields['checkin'] = $entity->fk_checkin;
    }

    // Nullable so we only pass it through to create_action if we have it..
    if (isset($entity->fk_consume)) {
      $fields['consume'] = $entity->fk_consume;
    }

    // Nullable so we only pass it through to create_action if we have it..
    if (isset($entity->fk_produce)) {
      $fields['produce'] = $entity->fk_produce;
    }

    // Nullable so we only pass it through to create_action if we have it..
    if (isset($entity->fk_consumermember)) {
      $fields['consumermember'] = $entity->fk_consumermember;
    }

    // Nullable so we only pass it through to create_action if we have it..
    if (isset($entity->fk_producermember)) {
      $fields['producermember'] = $entity->fk_producermember;
    }
		
	//
	// Ensure that we have the 'thumbwhere_api_key' as we are going to pass that through in a context
	//
  if (variable_get('thumbwhere_api_key') == '') {
    throw new Exception('ThumbWhere resource calls for \'Action\' requires that variable \'thumbwhere_api_key\' is set.');
  }

	//
	// Ensure that we have the 'thumbwhere_api_key' as we are going to pass that through in a context
	//
  if (!is_numeric(variable_get('thumbwhere_host_id'))) {
    throw new Exception('ThumbWhere resource calls for \'Action\' requires that variable \'thumbwhere_host_id\' is set.');
  }


              		
    //
    // If this action entity is new....
    //
               
    if ($entity->is_new) {
                

      //
      // ... then we create the action entity in ThumbWhere
      //

      $action = $twContentAPI->create_action($context = array(
                  'key' => variable_get('thumbwhere_api_key'),
                  'origin' => variable_get('thumbwhere_host_id'),
          ), $fields);

      $attributes = $action->body->action->attributes();

      //if (twCanDebug()) debug('newly created id = ' . t($attributes['id']));

      // Assign new id.
      $entity->pk_action = intval($attributes['id']);

      //if (twCanDebug()) debug(t('$entity->pk_action = ' . $entity->pk_action));
    }
    else
    {
          	

      //
      // .. otherwise we update the action entity in ThumbWhere
      //

      $action = $twContentAPI->update_action($id=$entity->pk_action,$context = array(
                  'key' => variable_get('thumbwhere_api_key'),
                  'origin' => variable_get('thumbwhere_host_id'),
          ), $fields);
            
        
    }
          	
    
  }
}

/**
 * Implements thumbwhere_action_entity_insert().
 *
 * Act on entities when inserted.
 *
 * @param $entity
 *   The entity object
 * @param $type
 *   The type of entity being inserted (i.e. node, user, comment).
 */
function thumbwhere_action_entity_insert($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_action') return;

}


/**
* Implements thumbwhere_action_entity_delete().
*
* Act on entities when inserted.
*
* @param $entity
*   The entity object
* @param $type
*   The type of entity being deleted (i.e. node, user, comment).
*/
function thumbwhere_action_entity_delete($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_action') return;


}
