<?php

// ******************************************************************
// This file was generated by autogen_model_implementation_drupal.xsl
// ******************************************************************

// Include the ThumbWhere SDK
require_once(dirname(__FILE__) . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . 'dependencies' . DIRECTORY_SEPARATOR . 'tw' . DIRECTORY_SEPARATOR . 'sdk.class.php');

/**
 * @file
 * Module for the ThumbWhere ContentIngest Entity
 *
 *
 * NOTE: This has been autogenerated.
 * 
 */

/**
 * Implement hook_entity_info().
 *
 * We define two entities here - the actual entity that will hold our domain
 * specific information and an entity that holds information about the different
 * types of entities. See here: http://drupal.org/node/977380 for a discussion on this
 * choice.
 */
function thumbwhere_contentingest_entity_info() {
  $return['thumbwhere_contentingest'] = array(

    // The human-readable name of the type.
    'label' => t('ThumbWhereContentIngest'),

    // (used by DrupalDefaultEntityController) The name of the entity type's base table.
    'base table' => 'thumbwhere_contentingest',

    // fieldable: Set to TRUE if you want your entity type to accept fields being attached to it.
    'fieldable' => TRUE,

  // Declare the endty keys..
    'entity keys' => array(
      'id' => 'pk_contentingest',
      'label' => 'pk_contentingest',
    ),

    // A function taking an entity and an entity type as arguments and returning the label of the entity.
    // The entity label is the main string associated with an entity; for example, the title of a node or
    // the subject of a comment. If there is an entity object property that defines the label, use the
    // 'label' element of the 'entity keys' return value component to provide this information (see below).
    // If more complex logic is needed to determine the label of an entity, you can instead specify a callback
    // function here, which will be called to determine the entity label. See also the entity_label()
    // function, which implements this logic.
    'label callback' => 'entity_class_label',

    // A function taking an entity as argument and returning the uri elements of the entity, e.g. 'path' and 'options'.
    // The actual entity uri can be constructed by passing these elements to url().
    'uri callback' => 'entity_class_uri',

    //
    // Entity API Components
    //

    // The module whwre we can find our classes
    'module' => 'thumbwhere_contentingest',

    // The entity class and controller class extend the classes provided by the Entity API
    'entity class' => 'ThumbWhereContentIngest',

    // Which Entity API contoller
    'controller class' => 'ThumbWhereContentIngestController',

    // The information below is used by the ThumbWhereContentIngestUIController (which extends the EntityDefaultUIController)
    'admin ui' => array(
      'path' => 'admin/content/thumbwhere_contentingests',
      'file' => 'thumbwhere_contentingest.admin.inc',
      'controller class' => 'ThumbWhereContentIngestUIController',
      'menu wildcard' => '%thumbwhere_contentingest',
    ),
  );

  return $return;
}


/**
 * Implements hook_permission().
 */
function thumbwhere_contentingest_permission() {

  // We set up permisssions to manage entity types, manage all entities and the
  // permissions for each individual entity
  $permissions = array(
    'administer thumbwhere_contentingests' => array(
      'title' => t('Administer thumbwhere_contentingests'),
      'description' => t('Edit and delete all thumbwhere_contentingests'),
    ),
  'send thumbwhere contentingest notifications' => array(
          'title' => t('Send ThumbWhere Notifications'),
          'description' => t('Allow the caller to call the ThumbWhere ContentIngest Entity Notifications Endpoint.'),
          'restrict access' => TRUE,
      ),    
  );

  return $permissions;
}


/**
 * Determines whether the given user has access to a thumbwhere_contentingest.
 *
 * @param $op
 *   The operation being performed. One of 'view', 'update', 'create', 'delete'
 *   or just 'edit' (being the same as 'create' or 'update').
 * @param $thumbwhere_contentingest
 *   Optionally a thumbwhere_contentingest or a thumbwhere_contentingest type to check access for. If nothing is
 *   given, access for all thumbwhere_contentingests is determined.
 * @param $account
 *   The user to check for. Leave it to NULL to check for the global user.
 * @return boolean
 *   Whether access is allowed or not.
 */
function thumbwhere_contentingest_access($op, $thumbwhere_contentingest = NULL, $account = NULL) {

  //if (user_access('administer thumbwhere_contentingests', $account)) {
  //  return TRUE;
 // }

  //return FALSE;
  return TRUE;
}




/**
 * Fetch a thumbwhere_contentingest object. Make sure that the wildcard you choose
 * in the thumbwhere_contentingest entity definition fits the function name here.
 *
 * @param $thumbwhere_contentingest_id
 *   Integer specifying the thumbwhere_contentingest id.
 * @param $reset
 *   A boolean indicating that the internal cache should be reset.
 * @return
 *   A fully-loaded $thumbwhere_contentingest object or FALSE if it cannot be loaded.
 *
 * @see thumbwhere_contentingest_load_multiple()
 */
function thumbwhere_contentingest_load($thumbwhere_contentingest_id, $reset = FALSE) {

  //

  $thumbwhere_contentingests = entity_load('thumbwhere_contentingest', array($thumbwhere_contentingest_id), array(), $reset);

  return reset($thumbwhere_contentingests);
}





/**
 * Load multiple thumbwhere_contentingests based on certain conditions.
 *
 * @param $thumbwhere_contentingest_ids
 *   An array of thumbwhere_contentingest IDs.
 * @param $conditions
 *   An array of conditions to match against the {thumbwhere_contentingest} table.
 * @param $reset
 *   A boolean indicating that the internal cache should be reset.
 * @return
 *   An array of thumbwhere_contentingest objects, indexed by thumbwhere_contentingest_id.
 *
 * @see entity_load()
 * @see thumbwhere_contentingest_load()
 */
/*
function thumbwhere_contentingest_load_multiple($thumbwhere_contentingest_ids = array(), $conditions = array(), $reset = FALSE) {



  $loaded = entity_load('thumbwhere_contentingest', $thumbwhere_contentingest_ids, $conditions, $reset);

  return $loaded;
}
*/

/**
 * Deletes a thumbwhere_contentingest.
 */
function thumbwhere_contentingest_delete(ThumbWhereContentIngest $thumbwhere_contentingest) {

  $thumbwhere_contentingest->delete();
}


/**
 * Delete multiple thumbwhere_contentingests.
 *
 * @param $thumbwhere_contentingest_ids
 *   An array of thumbwhere_contentingest IDs.
 */
/*
function thumbwhere_contentingest_delete_multiple(array $thumbwhere_contentingest_ids) {



  entity_get_controller('thumbwhere_contentingest')->delete($thumbwhere_contentingest_ids);
}
*/

/**
 * Create a thumbwhere_contentingest object.
 */
function thumbwhere_contentingest_create($values = array()) {

  return entity_get_controller('thumbwhere_contentingest')->create($values);
}


/**
 * Saves a thumbwhere_contentingest to the database.
 *
 * @param $thumbwhere_contentingest
 *   The thumbwhere_contentingest object.
 */
/*
function thumbwhere_contentingest_save(ThumbWhereContentIngest $thumbwhere_contentingest) {



  return $thumbwhere_contentingest->save();
}*/



/**
 * Insert a thumbwhere_contentingest
 *
 * @param $thumbwhere_contentingest
 *   The thumbwhere_contentingest object.
 */
/*
function thumbwhere_contentingest_insert(array $values = array()) {



  //return $thumbwhere_contentingest->create($values);
  return entity_get_controller('thumbwhere_contentingest')->insert($values);
}
*/






/**
 * URI callback for thumbwhere_contentingests
 */
function thumbwhere_contentingest_uri(ThumbWhereContentIngest $thumbwhere_contentingest) {



  return array(
    'path' => 'thumbwhere_contentingest/' . $thumbwhere_contentingest->pk_contentingest,
  );
}


/**
 * Menu title callback for showing individual entities
 */
function thumbwhere_contentingest_page_title(ThumbWhereContentIngest $thumbwhere_contentingest) {

  

  return $thumbwhere_contentingest->pk_contentingest;
}


/**
 * Sets up content to show an individual thumbwhere_contentingest
 * @todo - get rid of drupal_set_title();
 */
function thumbwhere_contentingest_page_view($thumbwhere_contentingest, $view_mode = 'full') {



  $controller = entity_get_controller('thumbwhere_contentingest');
  $content = $controller->view(array($thumbwhere_contentingest->pk_contentingest => $thumbwhere_contentingest));
  drupal_set_title($thumbwhere_contentingest->pk_contentingest);
  return $content;
}


/**
 * Implements hook_views_api().
 */
function thumbwhere_contentingest_views_api() {



  return array(
    'api' => 3,
    'path' => drupal_get_path('module', 'thumbwhere_contentingest') . '/views',
  );
}


/**
 * Implement hook_theme().
 */
function thumbwhere_contentingest_theme() {



  return array(
    'thumbwhere_contentingest_add_list' => array(
      'variables' => array('content' => array()),
      'file' => 'thumbwhere_contentingest.admin.inc',
    ),
    'thumbwhere_contentingest' => array(
      'render element' => 'elements',
      'template' => 'thumbwhere_contentingest',
    )
  );
}


/**
 * Implements hook_menu_local_tasks_alter().
 */
function thumbwhere_contentingest_menu_local_tasks_alter(&$data, $router_item, $root_path) {

  // Add action link 'admin/structure/thumbwhere_contentingests/add' on 'admin/structure/thumbwhere_contentingests'.
  if ($root_path == 'admin/content/thumbwhere_contentingests') {
    $item = menu_get_item('admin/content/thumbwhere_contentingests/add');
    if ($item['access']) {
      $data['actions']['output'][] = array(
        '#theme' => 'menu_local_action',
        '#link' => $item,
      );
    }
  }
}


/**
 * The class used for thumbwhere_contentingest entities
 */
class ThumbWhereContentIngest extends Entity {

  public function __construct($values = array()) {
    parent::__construct($values, 'thumbwhere_contentingest');
  }

  protected function defaultLabel() {
    return $this->pk_contentingest;
  }

  protected function defaultUri() {
    return array('path' => 'thumbwhere_contentingest/' . $this->pk_contentingest);
  }


}



/**
 * The Controller for ThumbWhereContentIngest entities
 */
class ThumbWhereContentIngestController extends EntityAPIController {
  public function __construct($entityType) {
    parent::__construct($entityType);
  }


  /**
   * Create a thumbwhere_contentingest - we first set up the values that are specific
   * to our thumbwhere_contentingest schema but then also go through the EntityAPIController
   * function.
   *
   * @param $type
   *   The machine-readable type of the thumbwhere_contentingest.
   *
   * @return
   *   A thumbwhere_contentingest object with all default fields initialized.
   */
  /*
  public function create(array $values = array()) {



    //$values += array(
    //  'thumbwhere_id' => -1,
    //);

    return parent::create($values);
  }
  */


  /**
  * view
  * to our thumbwhere_contentingest schema but then also go through the EntityAPIController
  * function.
  *
  * @param $type
  *   The machine-readable type of the thumbwhere_contentingest.
  *
  * @return
  *   A thumbwhere_contentingest object with all default fields initialized.
  */
  /*
  public function view($entities, $view_mode = 'full', $langcode = NULL, $page = NULL) {



    $view = parent::view($entities, $view_mode, $langcode, $page);

    return $view;
  }
  */




  /**
  * query - Builds and executes the query for loading.
  *
  * @param $ids
  *   Array of ids
  *
  * @param $conditions
  *
  *
  *
  * @return
  *   A thumbwhere_contentingest object with all default fields initialized.
  */
  /*
  public function load($ids = array(), $conditions = array()) {



    $result = parent::load($ids, $conditions);

    //$result[1]->token = "my token";

    return $result;
  }
  */

  /**
  * Invoke
  *
  * @param $ids
  *   Array of ids
  *
  * @param $transaction
  *   If not null then the transaction this is taking part in
  *
  * @return
  *   A thumbwhere_contentingest object with all default fields initialized.
  */
  /*
  public function invoke($hook, $entity) {



    parent::invoke($hook, $entity);

  }
  */


  /**
  * Delete a thumbwhere_contentingest.
  *
  * @param $ids
  *   Array of ids
  *
  * @param $transaction
  *   If not null then the transaction this is taking part in
  *
  * @return
  *   A thumbwhere_contentingest object with all default fields initialized.
  */
  /*
  public function delete($ids, DatabaseTransaction $transaction = NULL) {



    parent::delete($ids, $transaction);

  }
  */


  /**
  * Save a thumbwhere_contentingest.
  *
  * @param $thumbwhere_contentingest
  *   The thumbwhere_contentingest being saved
  *
  * @param $transaction
  *   If not null then the transaction this is taking part in
  *
  * @return
  *   A thumbwhere_contentingest object with all default fields initialized.
  */
  /*
  public function save($thumbwhere_contentingest, DatabaseTransaction $transaction = NULL) {



    return parent::save($thumbwhere_contentingest, $transaction);
  }
  */


  /**
  * resetCache - Called when the cache is
  *
  * @param $ids
  *   Array of ids
  *
  * @return
  *   void
  */
  /*
  public function resetCache(array $ids = NULL) {



    parent::resetCache($ids);
  }
  */


  /**
  * query - Builds and executes the query for loading.
  *
  * @param $ids
  *   Array of ids
  *
  * @param $conditions
  *
  * @param $revision_id
  *
  *
  * @return
  *   A thumbwhere_contentingest object with all default fields initialized.
  */
  /*
  public function query($ids, $conditions, $revision_id = FALSE) {



    $result= parent::query($ids, $conditions, $revision_id);

    return $result;
  }
  */


  /**
   * Overriding the buldContentIngest function to add entity specific fields
   */
  /*
  public function buildContentIngest($entity, $view_mode = 'full', $langcode = NULL, $content = array()) {

    $content = parent::buildContentIngest($entity, $view_mode, $langcode, $content);

    $content['thumbwhere_contentingest_sample_data'] =  array(
      '#markup' => theme('thumbwhere_contentingest_sample_data', array('thumbwhere_contentingest_sample_data' => check_plain($entity->data['sample_data']), 'thumbwhere_contentingest' => $entity)),
    );

    return $content;
  }*/
}


/**
 * Implements hook_field_storage_load().
 *
 * Load field from storage
 *
 * @todo This needs to be improved
 */
/*
function thumbwhere_contentingest_field_storage_load($entity_type, $entities, $age, $fields, $options) {


}
*/



/**
* Implements hook_entity_presave().
*
* Act on an ContentIngest entity before it is about to be created or updated.
*
* @param $entity
*   The entity object.
* @param $entity_type
*   The type of entity being saved (i.e. node, user, comment).
*/
function thumbwhere_contentingest_entity_presave($entity, $entity_type) {

  // We only look after what we want...
  if ($entity_type == 'thumbwhere_contentingest')  {

    // If this is new
    if ($entity->is_new) {
  
      // .. then we create a new thumbwhere_contentingest entity...
      // Instantiate the ThumbWhereContent class

      $twContentAPI = new ThumbWhereAPIContent();

      //if (twCanDebug()) debug($twContentAPI);

      //if (twCanDebug()) debug($entity);

      //
      // Collate and validate our fields
      //

      $fields = array();

      // Nullable so we only pass it through to create_content_ingest if we have it..
      if (!empty($entity->fk_mediaitem)) {
        $fields['mediaitem'] = $entity->fk_mediaitem;
      }

      // Nullable so we only pass it through to create_content_ingest if we have it..
      if (!empty($entity->fk_content)) {
        $fields['content'] = $entity->fk_content;
      }

      // Nullable so we only pass it through to create_content_ingest if we have it..
      if (!empty($entity->fk_member)) {
        $fields['member'] = $entity->fk_member;
      }
      // Compulsory so..
      if (empty($entity->url)) {
        throw new Exception('url is compulsory.');
      }
      else {
        $fields['url'] = $entity->url;
      }

      // Nullable so we only pass it through to create_content_ingest if we have it..
      if (!empty($entity->size)) {
        $fields['size'] = $entity->size;
      }

      // Not nullable but we have a default value
      if (empty($entity->uploaded)) {
        $fields['uploaded'] = $entity->uploaded = 0;
      }
      else {
        $fields['uploaded'] = $entity->uploaded;
      }

      // Not nullable but we have a default value
      if (empty($entity->transcoded)) {
        $fields['transcoded'] = $entity->transcoded = 0;
      }
      else {
        $fields['transcoded'] = $entity->transcoded;
      }

      // Not nullable but we have a default value
      if (empty($entity->encrypted)) {
        $fields['encrypted'] = $entity->encrypted = 0;
      }
      else {
        $fields['encrypted'] = $entity->encrypted;
      }

      // Not nullable but we have a default value
      if (empty($entity->deployed)) {
        $fields['deployed'] = $entity->deployed = 0;
      }
      else {
        $fields['deployed'] = $entity->deployed;
      }

      // Not nullable but we have a default value
      if (empty($entity->ready)) {
        $fields['ready'] = $entity->ready = 0;
      }
      else {
        $fields['ready'] = $entity->ready;
      }

      // Not nullable but we have a default value
      if (empty($entity->error)) {
        $fields['error'] = $entity->error = 0;
      }
      else {
        $fields['error'] = $entity->error;
      }

      // Nullable so we only pass it through to create_content_ingest if we have it..
      if (!empty($entity->status)) {
        $fields['status'] = $entity->status;
      }

      //
      // Now create the Entity in ThumbWhere
      //

      $contentingest = $twContentAPI->create_content_ingest($context = array(
                  'key' => variable_get('thumbwhere_api_key')
          ), $fields);
            
       if (twCanDebug()) debug($contentingest);

      //if (twCanDebug()) debug($content_ingest);

      $attributes = $contentingest->body->content_ingest->attributes();

      if (twCanDebug()) debug('newly created id = ' . $attributes['id']);

      // Assign new id.
      $entity->pk_contentingest = intval($attributes['id']);

      if (twCanDebug()) debug('$entity->pk_contentingest = ' . $entity->pk_contentingest);
    }
    else
    {
        // .. otherwise we calculate the changes and ship them off to thumbwhere
    }
  }
}

/**
 * Implements thumbwhere_contentingest_entity_insert().
 *
 * Act on entities when inserted.
 *
 * @param $entity
 *   The entity object
 * @param $type
 *   The type of entity being inserted (i.e. node, user, comment).
 */
function thumbwhere_contentingest_entity_insert($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_contentingest') return;

}


/**
* Implements thumbwhere_contentingest_entity_delete().
*
* Act on entities when inserted.
*
* @param $entity
*   The entity object
* @param $type
*   The type of entity being deleted (i.e. node, user, comment).
*/
function thumbwhere_contentingest_entity_delete($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_contentingest') return;


}
