<?php

/**
 * @file
 * ThumbWherePlace editing UI.
 *
 * We make very little use of the EntityAPI interface for this - preferring instead to use
 * views. That offers more flexibility to change a UI that will, more often than not,
 * be end-user facing.
 *
 * NOTE: This file was generated by autogen_model_implementation_drupal.xsl
 */


	

 
/**
 * Handle the notification for content_ingext
 */
function _thumbwhere_handle_place_notify_xml($notification) {

  //
  // Get the operation
  //

  if (!isset($notification->attributes()->op)) {
    drupal_add_http_header('Status', '501 unable to fulfull request..');
    $err = 'We were expecting an \'op\', attribute on the \'place\', element.';
    print($err);
   	watchdog('thumbwhere_place', $err ,array(), WATCHDOG_ERROR);
    exit(0);
  }

  $op = $notification->attributes()->op;

  if (twCanDebug()) debug('op');
  if (twCanDebug()) debug($op);

  //
  // Get the context
  //

  if (!isset($notification->attributes()->key)) {
    drupal_add_http_header('Status', '412 unable to fulfull request..');
    $err = 'We were expecting a \'key\', attribute on the \'place\', element.';
    print($err);
    watchdog('thumbwhere_place', $err ,array(), WATCHDOG_ERROR);
    exit(0);
  }

  $context_key =  $notification->attributes()->key;

  //
  // Now based on the operation..
  //

  switch($op)
  {
  
  
      case 'create':
      {
      
        //
        // What resource are we creating?
        //
      
        //
        // Get the id
        //
		
        if (!isset($notification->attributes()->id)) {
          drupal_add_http_header('Status', '412 Unable to fulfull request.');
          $err = 'We were expecting an \'id\', attribute on the \'place\', element when we are performing an \'update\' notification..';
          print($err);
          watchdog('thumbwhere_place', $err ,array(), WATCHDOG_ERROR);
          exit(0);
        }

        $id = intval($notification->attributes()->id);
		

        if (twCanDebug()) debug('id');
        if (twCanDebug()) debug($id);

      
        //
        // Get the fields
        //
          
        $entities = entity_load('thumbwhere_place', array($id));

        if (twCanDebug()) debug('$entities');
        if (twCanDebug()) debug($entities);

        if (count($entities) != 0) {
          //drupal_add_http_header('Status', '409 Resource already exists.');
          $err = '\'place\' resource already exists with an id of \'' . $id . '\'.';
           //print($err);
          watchdog('thumbwhere_place', $err ,array(), WATCHDOG_ERROR);
          //exit(0);

          	
          drupal_add_http_header('Content-Type', 'text/xml');
          print('<response>');
          print('<place op="' . $op . '" id="' . $id . '" >');
          print('<status>success</status>');
          print('</place>');
          print('</response>');
          exit(0);          	
          
        }

        // Getting a reference to the entity we asked for
        $entityfields = array();

        //
        // We are only updating what has been passed through.
        // No test for missing values.
        // TODO: Validate incoming values.
        //


        $entityfields['pk_place'] = $id;	

        if (isset($notification->location)) {
          $entityfields['fk_location'] = intval($notification->location);
        }

        if (!isset($notification->attributes()->key)) {
          if (twCanDebug()) debug('$entities');
          if (twCanDebug()) debug($entities);
        }

        // We are being notified, so we do not want to ship this out again as a notification...
        // Finally write the entity.
        $entity = entity_create('thumbwhere_place', $entityfields);
        $entity->do_not_propagate = TRUE;
        $entity->save();

        //
        // Send the final response
        //

        drupal_add_http_header('Content-Type', 'text/xml');
        print('<response>');
        print('<place op="' . $op . '" id="' . $id . '" >');
        print('<status>success</status>');
        print('</place>');
        print('</response>');
        exit(0);			
		
      
      }
  
  
    case 'update':
      {
        //
        // What resource are we updating?
        //
      
        //
        // Get the id
        //
		
        if (!isset($notification->attributes()->id)) {
          drupal_add_http_header('Status', '412 Unable to fulfull request.');
          $err = 'We were expecting an \'id\', attribute on the \'place\', element when we are performing an \'update\' notification..';
          print($err);
          watchdog('thumbwhere_place', $err ,array(), WATCHDOG_ERROR);
          exit(0);
        }

        $id = intval($notification->attributes()->id);
		

        if (twCanDebug()) debug('id');
        if (twCanDebug()) debug($id);

      
        //
        // Get the fields
        //
          
        $entities = entity_load('thumbwhere_place', array($id));

        if (twCanDebug()) debug('$entities');
        if (twCanDebug()) debug($entities);

        if (count($entities) == 0) {
          //drupal_add_http_header('Status', '409 No such resource exists.');
          $err = 'No such \'place\' resource exists with an id of \'' . $id . '\'.';
          //print($err);
          watchdog('thumbwhere_place',$err,array(), WATCHDOG_ERROR);
          //exit(0);

////////////////////////////////////////////////////////////////////////
//
// ... for now we will just create the local copy
//
          watchdog('thumbwhere_place','creating local version of missing entity',array(), WATCHDOG_ERROR);

        //
        // What resource are we creating?
        //
      
        //
        // Get the id
        //
		
        if (!isset($notification->attributes()->id)) {
          drupal_add_http_header('Status', '412 Unable to fulfull request.');
          $err = 'We were expecting an \'id\', attribute on the \'place\', element when we are performing an \'update\' notification..';
          print($err);
          watchdog('thumbwhere_place', $err ,array(), WATCHDOG_ERROR);
          exit(0);
        }

        $id = intval($notification->attributes()->id);
		

        if (twCanDebug()) debug('id');
        if (twCanDebug()) debug($id);

      
        //
        // Get the fields
        //
          
        $entities = entity_load('thumbwhere_place', array($id));

        if (twCanDebug()) debug('$entities');
        if (twCanDebug()) debug($entities);

        if (count($entities) != 0) {
          //drupal_add_http_header('Status', '409 Resource already exists.');
          $err = '\'place\' resource already exists with an id of \'' . $id . '\'.';
           //print($err);
          watchdog('thumbwhere_place', $err ,array(), WATCHDOG_ERROR);
          //exit(0);

          	
          drupal_add_http_header('Content-Type', 'text/xml');
          print('<response>');
          print('<place op="' . $op . '" id="' . $id . '" >');
          print('<status>success</status>');
          print('</place>');
          print('</response>');
          exit(0);          	
          
        }

        // Getting a reference to the entity we asked for
        $entityfields = array();

        //
        // We are only updating what has been passed through.
        // No test for missing values.
        // TODO: Validate incoming values.
        //


        $entityfields['pk_place'] = $id;	

        if (isset($notification->location)) {
          $entityfields['fk_location'] = intval($notification->location);
        }

        if (!isset($notification->attributes()->key)) {
          if (twCanDebug()) debug('$entities');
          if (twCanDebug()) debug($entities);
        }

        // We are being notified, so we do not want to ship this out again as a notification...
        // Finally write the entity.
        $entity = entity_create('thumbwhere_place', $entityfields);
        $entity->do_not_propagate = TRUE;
        $entity->save();

        //
        // Send the final response
        //

        drupal_add_http_header('Content-Type', 'text/xml');
        print('<response>');
        print('<place op="' . $op . '" id="' . $id . '" >');
        print('<status>success</status>');
        print('</place>');
        print('</response>');
        exit(0);			
		
//
//
//
////////////////////////////////////////////////////////////////////////
        }

        // Getting a reference to the entity we asked for
        $entity = $entities[$id];

        //
        // We are only updating what has been passed through.
        // No test for missing values.
        // TODO: Validate incoming values.
        //


        if (isset($notification->location)) {
          $entity->fk_location = intval($notification->location);
        }

        if (!isset($notification->attributes()->key)) {
          if (twCanDebug()) debug('$entities');
          if (twCanDebug()) debug($entities);
        }

        // We are being notified, so we do not want to ship this out again as a notification...
        $entity->do_not_propagate = TRUE;

        // Finally write the entity.
        entity_save('thumbwhere_place', $entity);

        //
        // Send the final response
        //

        drupal_add_http_header('Content-Type', 'text/xml');
        print('<response>');
        print('<place op="' . $op . '" id="' . $id . '" >');
        print('<status>success</status>');
        print('</place>');
        print('</response>');
        exit(0);
      }
      
      

    default:
      {
        drupal_add_http_header('Status', '501 Unable to fulfull request..');
        print('We don\'t handle the \'' . $op . '\', notification operation on the \'place\', element.');
        exit(0);

      }
  }
}



/**
 * Menu callback: React to a notifications for ContentIngest.
 */
function _thumbwhere_place_notify() {

  if (twCanDebug()) {
    watchdog('thumbwhere_place', 'incoming notification.');
  }

  $numargs = func_num_args();
  $arg_list = func_get_args();

  
	if (twCanDebug()) debug('$numargs  = ' . $numargs);
	
	if (twCanDebug()) debug('$arg_list = ' . $arg_list);
	
	if (twCanDebug()) debug('$_REQUEST');
	if (twCanDebug()) debug($_REQUEST);
	
	if (twCanDebug()) debug('$_GET');
	if (twCanDebug()) debug($_GET);
	
	if (twCanDebug()) debug('$_POST');
	if (twCanDebug()) debug($_POST);
	
	//if (twCanDebug()) debug('$HTTP_RAW_POST_DATA');
	//if (twCanDebug()) debug($HTTP_RAW_POST_DATA);
	
	if (twCanDebug()) debug('$_SERVER');
	if (twCanDebug()) debug($_SERVER);
	
	//if (twCanDebug()) debug('$GLOBALS');
	//if (twCanDebug()) debug($GLOBALS);

  

  if (variable_get('thumbwhere_api_log_debug', 0) == 1) {
    foreach ($arg_list as $arg) {
      if (twCanDebug()) debug('$arg  = ' . $arg);
    }
  }


  //////////////////////////////////////////////////
  //
  //

  switch ($_SERVER['REQUEST_METHOD']) {

    case 'POST': {
        if ($numargs == 1) {
          if ($arg_list[0] == 'place') {

            //
            // Get the POST data as XML
            //

            $post_xml = new SimpleXMLElement('php://input', null, true);
            if (twCanDebug()) debug('$post_xml');
            if (twCanDebug()) debug($post_xml);

            if (twCanDebug()) debug('$post_xml->getName()');
            if (twCanDebug()) debug($post_xml->getName());

            // We expect 'action' as the root element. TODO: I would rather this was the more generic 'request'
            if ($post_xml->getName() != 'action') {
              drupal_add_http_header('Status', '404 not found.');
              print('We were expecting \'action\', as the root element, not \'' . $post_xml->getName() . '\'.');
              exit(0);
            }

            /*
             *  SimpleXMLElement::__set_state(array(
             *      'place' =>
             *      SimpleXMLElement::__set_state(array(
             *       '@attributes' =>
             *      array (
             *        'key' => '8e9ff318-3cd7-4593-99de-4c37c0fb335a',
             *        'op' => 'create',
             *      ),
             *    )),
             *  ))
             */


            // We expect 'action' as the root element.
            if (!isset($post_xml->place)) {
              drupal_add_http_header('Status', '404 not found.');
              print('We were expecting a \'place\', element in the action payload.');
              exit(0);
            }

            // Handle the incoming notification for content ingest
            _thumbwhere_handle_place_notify_xml($post_xml->place);

          }
          else {
            drupal_add_http_header('Status', '404 not found.');
            print('Invalid resource');
            exit(0);
          }
        }
        else {
          drupal_add_http_header('Status', '404 not found.');
          print('Invalid notification request url format.');
          exit(0);
        }
      }
      break;

    case 'GET': {

        if ($numargs == 1) {

          // If we are returning the index.xml file for the webservice.
          if ($arg_list[0] == 'index.xml') {
            // return index.xml
            drupal_add_http_header('Content-Type', 'text/xml');
            readfile(dirname(__FILE__) . '/index.xml');
            exit(0);
          }
          else {
            drupal_add_http_header('Status', '404 not found.');
            print('File not found.');
            exit(0);
          }
        }
        else {
          drupal_add_http_header('Status', '404 not found.');
          print('Invalid notification request url format.');
          exit(0);
        }
      }
      break;

    default: {
        drupal_add_http_header('Status', '404 not found.');
        exit(0);
      }
      break;
  }

  //
  //
  ////////////////////////////////////////////////////////////////////////////////////
 
}





/**
 * Place UI controller.
 */
class ThumbWherePlaceUIController extends EntityDefaultUIController {

  /**
   * Overrides hook_menu() defaults. Main reason for doing this is that
   * parent class hook_menu() is optimized for entity type administration.
   */
  public function hook_menu() {

    $items = array();
    
    // Add a notification page...
    $items['thumbwhere/place/notify'] = array(
        'title' => 'Notifications Callback for "Place" Entity',
        'page callback' => '_thumbwhere_place_notify',
        'access arguments' => array(
            'send thumbwhere place notifications'
        ),
        'file' => 'thumbwhere_place.admin.inc',
        'file path' => drupal_get_path('module', $this->entityInfo['module'])
    );    
    
    $id_count = count(explode('/', $this->path));
    $wildcard = isset($this->entityInfo['admin ui']['menu wildcard']) ? $this->entityInfo['admin ui']['menu wildcard'] : '%' . $this->entityType;

    $items[$this->path] = array(
      'title' => 'Place',
      'description' => 'Add edit and update thumbwhere_places.',
      'page callback' => 'system_admin_menu_block_page',
      'access arguments' => array('access administration pages'),
      'file path' => drupal_get_path('module', 'system'),
      'file' => 'system.admin.inc',
    );

    // Change the overview menu type for the list of thumbwhere_places.
    $items[$this->path]['type'] = MENU_LOCAL_TASK;

    // Change the add page menu to multiple types of entities
    $items[$this->path . '/add'] = array(
      //'title' => 'Add a Place',
      'title' => 'Add',
      'description' => 'Add a new Place',
      'page callback' => 'thumbwhere_place_form_wrapper',
      'page arguments' => array(thumbwhere_place_create(array('type' => 'thumbwhere_place'))),
      'access callback' => 'thumbwhere_place_access',
      'access arguments' => array('edit', 'edit ' . 'thumbwhere_place'),
      'file' => 'thumbwhere_place.admin.inc',
      'file path' => drupal_get_path('module', $this->entityInfo['module'])
    );


/*
    // Change the add page menu to multiple types of entities
    $items[$this->path . '/add'] = array(
      //'title' => 'Add a Place',
      'title' => 'Add',
	  'description' => 'Add a new Place',
      'page callback'  => 'thumbwhere_place_add_page',
      'access callback'  => 'thumbwhere_place_access',
      'access arguments' => array('edit'),
      'type' => MENU_NORMAL_ITEM,
      'weight' => 20,
      'file' => 'thumbwhere_place.admin.inc',
      'file path' => drupal_get_path('module', $this->entityInfo['module'])

    );
*/    
/*
    $items[$this->path . '/add/' . 'thumbwhere_place'] = array(
      'title' => 'Add ' . 'ThumbWherePlace',
      'page callback' => 'thumbwhere_place_form_wrapper',
      'page arguments' => array(thumbwhere_place_create(array('type' => 'thumbwhere_place'))),
      'access callback' => 'thumbwhere_place_access',
      'access arguments' => array('edit', 'edit ' . 'thumbwhere_place'),
      'file' => 'thumbwhere_place.admin.inc',
      'file path' => drupal_get_path('module', $this->entityInfo['module'])
    );
*/
    // Loading and editing thumbwhere_place entities
    $items[$this->path . '/thumbwhere_place/' . $wildcard] = array(
      'page callback' => 'thumbwhere_place_form_wrapper',
      'page arguments' => array($id_count + 1),
      'access callback' => 'thumbwhere_place_access',
      'access arguments' => array('edit', $id_count + 1),
      'weight' => 0,
      'context' => MENU_CONTEXT_PAGE | MENU_CONTEXT_INLINE,
      'file' => 'thumbwhere_place.admin.inc',
      'file path' => drupal_get_path('module', $this->entityInfo['module'])
    );
    $items[$this->path . '/thumbwhere_place/' . $wildcard . '/edit'] = array(
      'title' => 'Edit',
      'type' => MENU_DEFAULT_LOCAL_TASK,
      'weight' => -10,
      'context' => MENU_CONTEXT_PAGE | MENU_CONTEXT_INLINE,
    );

    $items[$this->path . '/thumbwhere_place/' . $wildcard . '/delete'] = array(
      'title' => 'Delete',
      'page callback' => 'thumbwhere_place_delete_form_wrapper',
      'page arguments' => array($id_count + 1),
      'access callback' => 'thumbwhere_place_access',
      'access arguments' => array('edit', $id_count + 1),
      'type' => MENU_LOCAL_TASK,
      'context' => MENU_CONTEXT_INLINE,
      'weight' => 10,
      'file' => 'thumbwhere_place.admin.inc',
      'file path' => drupal_get_path('module', $this->entityInfo['module'])
    );

    // Menu item for viewing thumbwhere_places
    $items['thumbwhere_place/' . $wildcard] = array(
      //'title' => 'Title',
      'title callback' => 'thumbwhere_place_page_title',
      'title arguments' => array(1),
      'page callback' => 'thumbwhere_place_page_view',
      'page arguments' => array(1),
      'access callback' => 'thumbwhere_place_access',
      'access arguments' => array('view', 1),
      'type' => MENU_CALLBACK,
    );
    return $items;
  }


  /**
   * Create the markup for the add ThumbWherePlace Entities page within the class
   * so it can easily be extended/overriden.
   */
  public function addPage() {
    $item = menu_get_item();
    $content = system_admin_menu_block($item);

    if (count($content) == 1) {
      $item = array_shift($content);
      drupal_goto($item['href']);
    }

    return theme('thumbwhere_place_add_list', array('content' => $content));
  }

}


/**
 * Form callback wrapper: create or edit a thumbwhere_place.
 *
 * @param $thumbwhere_place
 *   The thumbwhere_place object being edited by this form.
 *
 * @see thumbwhere_place_edit_form()
 */
function thumbwhere_place_form_wrapper($thumbwhere_place) {
  // Add the breadcrumb for the form's location.
  thumbwhere_place_set_breadcrumb();
  return drupal_get_form('thumbwhere_place_edit_form', $thumbwhere_place);
}


/**
 * Form callback wrapper: delete a thumbwhere_place.
 *
 * @param $thumbwhere_place
 *   The thumbwhere_place object being edited by this form.
 *
 * @see thumbwhere_place_edit_form()
 */
function thumbwhere_place_delete_form_wrapper($thumbwhere_place) {
  // Add the breadcrumb for the form's location.
  //thumbwhere_place_set_breadcrumb();
  return drupal_get_form('thumbwhere_place_delete_form', $thumbwhere_place);
}


/**
 * Form callback: create or edit a thumbwhere_place.
 *
 * @param $thumbwhere_place
 *   The thumbwhere_place object to edit or for a create form an empty thumbwhere_place object
 *     with only a thumbwhere_place type defined.
 */
function thumbwhere_place_edit_form($form, &$form_state, $thumbwhere_place) {

  // Add the default field elements.
  $form['fk_location'] = array(
    '#type' => 'textfield',
    '#title' => t('ThumbWherePlace fk_location'),
    '#default_value' => isset($thumbwhere_place->fk_location) ? $thumbwhere_place->fk_location : '',
    //'#maxlength' => 255,
   /// '#required' => TRUE,
    '#weight' => -5,
    '#autocomplete_path' => 'entity-autocomplete/thumbwhere_location',
  );



  $form['data']['#tree'] = TRUE;

  /*
  $form['data']['sample_data'] = array(
    '#type' => 'checkbox',
    '#title' => t('An interesting thumbwhere_place switch'),
    '#default_value' => isset($thumbwhere_place->data['sample_data']) ? $thumbwhere_place->data['sample_data'] : 1,
  );
  */


  // Add the field related form elements.
  $form_state['thumbwhere_place'] = $thumbwhere_place;
  field_attach_form('thumbwhere_place', $thumbwhere_place, $form, $form_state);

  $form['actions'] = array(
    '#type' => 'container',
    '#attributes' => array('class' => array('form-actions')),
    '#weight' => 400,
  );

  // We add the form's #submit array to this button along with the actual submit
  // handler to preserve any submit handlers added by a form callback_wrapper.
  $submit = array();

  if (!empty($form['#submit'])) {
    $submit += $form['#submit'];
  }

  $form['actions']['submit'] = array(
    '#type' => 'submit',
    '#value' => t('Save thumbwhere_place'),
    '#submit' => $submit + array('thumbwhere_place_edit_form_submit'),
  );

  // Do we show the delete button?
  if (!empty($thumbwhere_place->pk_place)) {
    $form['actions']['delete'] = array(
      '#type' => 'submit',
      '#value' => t('Delete thumbwhere_place'),
      '#suffix' => l(t('Cancel'), 'admin/thumbwhere/thumbwhere_places'),
      '#submit' => $submit + array('thumbwhere_place_form_submit_delete'),
      '#weight' => 45,
    );
  }

  // We append the validate handler to #validate in case a form callback_wrapper
  // is used to add validate handlers earlier.
  $form['#validate'][] = 'thumbwhere_place_edit_form_validate';
  return $form;
}


/**
 * Form API validate callback for the thumbwhere_place form
 */
function thumbwhere_place_edit_form_validate(&$form, &$form_state) {
   
  //if (twCanDebug()) debug($form);   
  //if (twCanDebug()) debug($form_state);


  // No validation for pk


  // Convert fk autocomplete for fk_location
  $value = $form['fk_location']['#value'];
  // If we have something, escape the auto-completion encoding.
  if (!empty($value)) { 
    $value = entity_autocomplete_get_id($value);
    form_set_value($form['fk_location'], $value,$form_state);
  }
  //if (twCanDebug()) debug($form['fk_location']);
   if (twCanDebug()) debug('fk_location = ' . $value);
  // Validate fk fk_location


              

  //form_set_value( array('#parents' => array('array_key_parent', 'array_key_to_replace')) , $value, $form_state);

   
   
  $thumbwhere_place = $form_state['thumbwhere_place'];

  // Notify field widgets to validate their data.
  field_attach_form_validate('thumbwhere_place', $thumbwhere_place, $form, $form_state);
    
    
  
}


/**
 * Form API submit callback for the thumbwhere_place form.
 *
 * @todo remove hard-coded link
 */
function thumbwhere_place_edit_form_submit(&$form, &$form_state) {

  $thumbwhere_place = entity_ui_controller('thumbwhere_place')->entityFormSubmitBuildEntity($form, $form_state);
  // Save the thumbwhere_place and go back to the list of thumbwhere_places

  // Add in created and changed times.
  if ($thumbwhere_place->is_new = isset($thumbwhere_place->is_new) ? $thumbwhere_place->is_new : 0) {
    $thumbwhere_place->created = time();
  }

  $thumbwhere_place->changed = time();

  $thumbwhere_place->save();
  $form_state['redirect'] = 'admin/thumbwhere/thumbwhere_places';
}

/**
 * Form API submit callback for the delete button.
 *
 * @todo Remove hard-coded path
 */
function thumbwhere_place_form_submit_delete(&$form, &$form_state) {
  $form_state['redirect'] = 'admin/thumbwhere/thumbwhere_places/thumbwhere_place/' . $form_state['thumbwhere_place']->pk_place . '/delete';
}





/**
 * Form callback: confirmation form for deleting a thumbwhere_place.
 *
 * @param $thumbwhere_place
 *   The thumbwhere_place to delete
 *
 * @see confirm_form()
 */
function thumbwhere_place_delete_form($form, &$form_state, $thumbwhere_place) {
  $form_state['thumbwhere_place'] = $thumbwhere_place;

  $form['#submit'][] = 'thumbwhere_place_delete_form_submit';

  $form = confirm_form($form,
    t('Are you sure you want to delete thumbwhere_place %name?', array('%name' => $thumbwhere_place->pk_place)),
    'admin/thumbwhere/thumbwhere_places/thumbwhere_place',
    '<p>' . t('This action cannot be undone.') . '</p>',
    t('Delete'),
    t('Cancel'),
    'confirm'
  );

  return $form;
}

/**
 * Submit callback for thumbwhere_place_delete_form
 */
function thumbwhere_place_delete_form_submit($form, &$form_state) {
  $thumbwhere_place = $form_state['thumbwhere_place'];

  thumbwhere_place_delete($thumbwhere_place);

  drupal_set_message(t('The thumbwhere_place %name has been deleted.', array('%name' => $thumbwhere_place->pk_place)));
  watchdog('thumbwhere_place', 'Deleted thumbwhere_place %name.', array('%name' => $thumbwhere_place->pk_place));

  $form_state['redirect'] = 'admin/thumbwhere/thumbwhere_places';
}



/**
 * Page to add ThumbWherePlace Entities.
 *
 * @todo Pass this through a proper theme function
 */
function thumbwhere_place_add_page() {
  $controller = entity_ui_controller('thumbwhere_place');
  return $controller->addPage();
}


/**
 * Displays the list of available thumbwhere_place types for thumbwhere_place creation.
 *
 * @ingroup themeable
 */
function theme_thumbwhere_place_add_list($variables) {


  //$content = $variables['content'];
  $output = '';
  /*
  if ($content) {
    $output = '<dl class="thumbwhere_place-type-list">';
    foreach ($content as $item) {
      $output .= '<dt>' . l($item['title'], $item['href']) . '</dt>';
      $output .= '<dd>' . filter_xss_admin($item['description']) . '</dd>';
    }
    $output .= '</dl>';
  }
  else {
    if (user_access('administer thumbwhere_place types')) {
      $output = '<p>' . t('ThumbWherePlace Entities cannot be added because you have not created any thumbwhere_place types yet. Go to the <a href="@create-thumbwhere_place-type">thumbwhere_place type creation page</a> to add a new thumbwhere_place type.', array('@create-thumbwhere_place-type' => url('admin/structure/thumbwhere_place_types/add'))) . '</p>';
    }
    else {
      $output = '<p>' . t('No thumbwhere_place types have been created yet for you to use.') . '</p>';
    }
  }
  */
  return $output;
}





/**
 * Sets the breadcrumb for administrative thumbwhere_place pages.
 */
function thumbwhere_place_set_breadcrumb() {
  $breadcrumb = array(
    l(t('Home'), '<front>'),
    l(t('Administration'), 'admin'),
    l(t('Place'), 'admin/content'),
    l(t('Place'), 'admin/thumbwhere/thumbwhere_places'),
  );

  drupal_set_breadcrumb($breadcrumb);
}



