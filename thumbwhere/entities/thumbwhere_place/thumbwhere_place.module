<?php

// ******************************************************************
// This file was generated by autogen_model_implementation_drupal.xsl
// ******************************************************************

// Include the ThumbWhere SDK
require_once(dirname(__FILE__) . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . 'dependencies' . DIRECTORY_SEPARATOR . 'tw' . DIRECTORY_SEPARATOR . 'sdk.class.php');

/**
 * @file
 * Module for the ThumbWhere Place Entity
 *
 *
 * NOTE: This has been autogenerated.
 * 
 */

/**
 * Implement hook_entity_info().
 *
 * We define two entities here - the actual entity that will hold our domain
 * specific information and an entity that holds information about the different
 * types of entities. See here: http://drupal.org/node/977380 for a discussion on this
 * choice.
 */
function thumbwhere_place_entity_info() {
  $return['thumbwhere_place'] = array(

    // The human-readable name of the type.
    'label' => t('ThumbWherePlace'),

    // (used by DrupalDefaultEntityController) The name of the entity type's base table.
    'base table' => 'thumbwhere_place',

    // fieldable: Set to TRUE if you want your entity type to accept fields being attached to it.
    'fieldable' => TRUE,

  // Declare the endty keys..
    'entity keys' => array(
      'id' => 'pk_place',
      'label' => 'pk_place',
    ),

    // A function taking an entity and an entity type as arguments and returning the label of the entity.
    // The entity label is the main string associated with an entity; for example, the title of a node or
    // the subject of a comment. If there is an entity object property that defines the label, use the
    // 'label' element of the 'entity keys' return value component to provide this information (see below).
    // If more complex logic is needed to determine the label of an entity, you can instead specify a callback
    // function here, which will be called to determine the entity label. See also the entity_label()
    // function, which implements this logic.
    'label callback' => 'entity_class_label',

    // A function taking an entity as argument and returning the uri elements of the entity, e.g. 'path' and 'options'.
    // The actual entity uri can be constructed by passing these elements to url().
    'uri callback' => 'entity_class_uri',

    //
    // Entity API Components
    //

    // The module whwre we can find our classes
    'module' => 'thumbwhere_place',

    // The entity class and controller class extend the classes provided by the Entity API
    'entity class' => 'ThumbWherePlace',

    // Which Entity API contoller
    'controller class' => 'ThumbWherePlaceController',

    // The information below is used by the ThumbWherePlaceUIController (which extends the EntityDefaultUIController)
    'admin ui' => array(
      'path' => 'admin/content/thumbwhere_places',
      'file' => 'thumbwhere_place.admin.inc',
      'controller class' => 'ThumbWherePlaceUIController',
      'menu wildcard' => '%thumbwhere_place',
    ),
  );

  return $return;
}


/**
 * Implements hook_permission().
 */
function thumbwhere_place_permission() {

  // We set up permisssions to manage entity types, manage all entities and the
  // permissions for each individual entity
  $permissions = array(
    'administer thumbwhere_places' => array(
      'title' => t('Administer thumbwhere_places'),
      'description' => t('Edit and delete all thumbwhere_places'),
    ),
  'send thumbwhere place notifications' => array(
          'title' => t('Send ThumbWhere Notifications'),
          'description' => t('Allow the caller to call the ThumbWhere Place Entity Notifications Endpoint.'),
          'restrict access' => TRUE,
      ),    
  );

  return $permissions;
}


/**
 * Determines whether the given user has access to a thumbwhere_place.
 *
 * @param $op
 *   The operation being performed. One of 'view', 'update', 'create', 'delete'
 *   or just 'edit' (being the same as 'create' or 'update').
 * @param $thumbwhere_place
 *   Optionally a thumbwhere_place or a thumbwhere_place type to check access for. If nothing is
 *   given, access for all thumbwhere_places is determined.
 * @param $account
 *   The user to check for. Leave it to NULL to check for the global user.
 * @return boolean
 *   Whether access is allowed or not.
 */
function thumbwhere_place_access($op, $thumbwhere_place = NULL, $account = NULL) {

  //if (user_access('administer thumbwhere_places', $account)) {
  //  return TRUE;
 // }

  //return FALSE;
  return TRUE;
}




/**
 * Fetch a thumbwhere_place object. Make sure that the wildcard you choose
 * in the thumbwhere_place entity definition fits the function name here.
 *
 * @param $thumbwhere_place_id
 *   Integer specifying the thumbwhere_place id.
 * @param $reset
 *   A boolean indicating that the internal cache should be reset.
 * @return
 *   A fully-loaded $thumbwhere_place object or FALSE if it cannot be loaded.
 *
 * @see thumbwhere_place_load_multiple()
 */
function thumbwhere_place_load($thumbwhere_place_id, $reset = FALSE) {

  //

  $thumbwhere_places = entity_load('thumbwhere_place', array($thumbwhere_place_id), array(), $reset);

  return reset($thumbwhere_places);
}





/**
 * Load multiple thumbwhere_places based on certain conditions.
 *
 * @param $thumbwhere_place_ids
 *   An array of thumbwhere_place IDs.
 * @param $conditions
 *   An array of conditions to match against the {thumbwhere_place} table.
 * @param $reset
 *   A boolean indicating that the internal cache should be reset.
 * @return
 *   An array of thumbwhere_place objects, indexed by thumbwhere_place_id.
 *
 * @see entity_load()
 * @see thumbwhere_place_load()
 */
/*
function thumbwhere_place_load_multiple($thumbwhere_place_ids = array(), $conditions = array(), $reset = FALSE) {



  $loaded = entity_load('thumbwhere_place', $thumbwhere_place_ids, $conditions, $reset);

  return $loaded;
}
*/

/**
 * Deletes a thumbwhere_place.
 */
function thumbwhere_place_delete(ThumbWherePlace $thumbwhere_place) {

  $thumbwhere_place->delete();
}


/**
 * Delete multiple thumbwhere_places.
 *
 * @param $thumbwhere_place_ids
 *   An array of thumbwhere_place IDs.
 */
/*
function thumbwhere_place_delete_multiple(array $thumbwhere_place_ids) {



  entity_get_controller('thumbwhere_place')->delete($thumbwhere_place_ids);
}
*/

/**
 * Create a thumbwhere_place object.
 */
function thumbwhere_place_create($values = array()) {

  return entity_get_controller('thumbwhere_place')->create($values);
}


/**
 * Saves a thumbwhere_place to the database.
 *
 * @param $thumbwhere_place
 *   The thumbwhere_place object.
 */
/*
function thumbwhere_place_save(ThumbWherePlace $thumbwhere_place) {



  return $thumbwhere_place->save();
}*/



/**
 * Insert a thumbwhere_place
 *
 * @param $thumbwhere_place
 *   The thumbwhere_place object.
 */
/*
function thumbwhere_place_insert(array $values = array()) {



  //return $thumbwhere_place->create($values);
  return entity_get_controller('thumbwhere_place')->insert($values);
}
*/






/**
 * URI callback for thumbwhere_places
 */
function thumbwhere_place_uri(ThumbWherePlace $thumbwhere_place) {



  return array(
    'path' => 'thumbwhere_place/' . $thumbwhere_place->pk_place,
  );
}


/**
 * Menu title callback for showing individual entities
 */
function thumbwhere_place_page_title(ThumbWherePlace $thumbwhere_place) {

  

  return $thumbwhere_place->pk_place;
}


/**
 * Sets up content to show an individual thumbwhere_place
 * @todo - get rid of drupal_set_title();
 */
function thumbwhere_place_page_view($thumbwhere_place, $view_mode = 'full') {



  $controller = entity_get_controller('thumbwhere_place');
  $content = $controller->view(array($thumbwhere_place->pk_place => $thumbwhere_place));
  drupal_set_title($thumbwhere_place->pk_place);
  return $content;
}


/**
 * Implements hook_views_api().
 */
function thumbwhere_place_views_api() {



  return array(
    'api' => 3,
    'path' => drupal_get_path('module', 'thumbwhere_place') . '/views',
  );
}


/**
 * Implement hook_theme().
 */
function thumbwhere_place_theme() {



  return array(
    'thumbwhere_place_add_list' => array(
      'variables' => array('content' => array()),
      'file' => 'thumbwhere_place.admin.inc',
    ),
    'thumbwhere_place' => array(
      'render element' => 'elements',
      'template' => 'thumbwhere_place',
    )
  );
}


/**
 * Implements hook_menu_local_tasks_alter().
 */
function thumbwhere_place_menu_local_tasks_alter(&$data, $router_item, $root_path) {

  // Add action link 'admin/structure/thumbwhere_places/add' on 'admin/structure/thumbwhere_places'.
  if ($root_path == 'admin/content/thumbwhere_places') {
    $item = menu_get_item('admin/content/thumbwhere_places/add');
    if ($item['access']) {
      $data['actions']['output'][] = array(
        '#theme' => 'menu_local_action',
        '#link' => $item,
      );
    }
  }
}


/**
 * The class used for thumbwhere_place entities
 */
class ThumbWherePlace extends Entity {

  public function __construct($values = array()) {
    parent::__construct($values, 'thumbwhere_place');
  }

  protected function defaultLabel() {
    return $this->pk_place;
  }

  protected function defaultUri() {
    return array('path' => 'thumbwhere_place/' . $this->pk_place);
  }


}



/**
 * The Controller for ThumbWherePlace entities
 */
class ThumbWherePlaceController extends EntityAPIController {
  public function __construct($entityType) {
    parent::__construct($entityType);
  }


  /**
   * Create a thumbwhere_place - we first set up the values that are specific
   * to our thumbwhere_place schema but then also go through the EntityAPIController
   * function.
   *
   * @param $type
   *   The machine-readable type of the thumbwhere_place.
   *
   * @return
   *   A thumbwhere_place object with all default fields initialized.
   */
  /*
  public function create(array $values = array()) {



    //$values += array(
    //  'thumbwhere_id' => -1,
    //);

    return parent::create($values);
  }
  */


  /**
  * view
  * to our thumbwhere_place schema but then also go through the EntityAPIController
  * function.
  *
  * @param $type
  *   The machine-readable type of the thumbwhere_place.
  *
  * @return
  *   A thumbwhere_place object with all default fields initialized.
  */
  /*
  public function view($entities, $view_mode = 'full', $langcode = NULL, $page = NULL) {



    $view = parent::view($entities, $view_mode, $langcode, $page);

    return $view;
  }
  */




  /**
  * query - Builds and executes the query for loading.
  *
  * @param $ids
  *   Array of ids
  *
  * @param $conditions
  *
  *
  *
  * @return
  *   A thumbwhere_place object with all default fields initialized.
  */
  /*
  public function load($ids = array(), $conditions = array()) {



    $result = parent::load($ids, $conditions);

    //$result[1]->token = "my token";

    return $result;
  }
  */

  /**
  * Invoke
  *
  * @param $ids
  *   Array of ids
  *
  * @param $transaction
  *   If not null then the transaction this is taking part in
  *
  * @return
  *   A thumbwhere_place object with all default fields initialized.
  */
  /*
  public function invoke($hook, $entity) {



    parent::invoke($hook, $entity);

  }
  */


  /**
  * Delete a thumbwhere_place.
  *
  * @param $ids
  *   Array of ids
  *
  * @param $transaction
  *   If not null then the transaction this is taking part in
  *
  * @return
  *   A thumbwhere_place object with all default fields initialized.
  */
  /*
  public function delete($ids, DatabaseTransaction $transaction = NULL) {



    parent::delete($ids, $transaction);

  }
  */


  /**
  * Save a thumbwhere_place.
  *
  * @param $thumbwhere_place
  *   The thumbwhere_place being saved
  *
  * @param $transaction
  *   If not null then the transaction this is taking part in
  *
  * @return
  *   A thumbwhere_place object with all default fields initialized.
  */
  /*
  public function save($thumbwhere_place, DatabaseTransaction $transaction = NULL) {



    return parent::save($thumbwhere_place, $transaction);
  }
  */


  /**
  * resetCache - Called when the cache is
  *
  * @param $ids
  *   Array of ids
  *
  * @return
  *   void
  */
  /*
  public function resetCache(array $ids = NULL) {



    parent::resetCache($ids);
  }
  */


  /**
  * query - Builds and executes the query for loading.
  *
  * @param $ids
  *   Array of ids
  *
  * @param $conditions
  *
  * @param $revision_id
  *
  *
  * @return
  *   A thumbwhere_place object with all default fields initialized.
  */
  /*
  public function query($ids, $conditions, $revision_id = FALSE) {



    $result= parent::query($ids, $conditions, $revision_id);

    return $result;
  }
  */


  /**
   * Overriding the buldPlace function to add entity specific fields
   */
  /*
  public function buildPlace($entity, $view_mode = 'full', $langcode = NULL, $content = array()) {

    $content = parent::buildPlace($entity, $view_mode, $langcode, $content);

    $content['thumbwhere_place_sample_data'] =  array(
      '#markup' => theme('thumbwhere_place_sample_data', array('thumbwhere_place_sample_data' => check_plain($entity->data['sample_data']), 'thumbwhere_place' => $entity)),
    );

    return $content;
  }*/
}


/**
 * Implements hook_field_storage_load().
 *
 * Load field from storage
 *
 * @todo This needs to be improved
 */
/*
function thumbwhere_place_field_storage_load($entity_type, $entities, $age, $fields, $options) {


}
*/



/**
* Implements hook_entity_presave().
*
* Act on an Place entity before it is about to be created or updated.
*
* @param $entity
*   The entity object.
* @param $entity_type
*   The type of entity being saved (i.e. node, user, comment).
*/
function thumbwhere_place_entity_presave($entity, $entity_type) {

  // We only look after what we want...
  if ($entity_type == 'thumbwhere_place')  {

    // If this is new
    if ($entity->is_new) {
  
      // .. then we create a new thumbwhere_place entity...
      // .. then we create a new thumbwhere_Place entity...
      // Instantiate the ThumbWhereContent class

      $twContentAPI = new ThumbWhereAPIContent();

      //if (twCanDebug()) debug($twContentAPI);

      //if (twCanDebug()) debug($entity);

      //
      // Collate and validate our fields
      //

      $fields = array();

      // Nullable so we only pass it through to create_place if we have it..
      if (!empty($entity->fk_location)) {
        $fields['location'] = $entity->fk_location;
      }

      //
      // Now create the Entity in ThumbWhere
      //

      $place = $twContentAPI->create_place($context = array(
                  'key' => variable_get('thumbwhere_api_key')
          ), $fields);
            
       if (twCanDebug()) debug($place);

      //if (twCanDebug()) debug($place);

      $attributes = $place->body->place->attributes();

      if (twCanDebug()) debug('newly created id = ' . $attributes['id']);

      // Assign new id.
      $entity->pk_place = intval($attributes['id']);

      if (twCanDebug()) debug('$entity->pk_place = ' . $entity->pk_place);
    }
    else
    {
        // .. otherwise we calculate the changes and ship them off to thumbwhere
    }
  }
}

/**
 * Implements thumbwhere_place_entity_insert().
 *
 * Act on entities when inserted.
 *
 * @param $entity
 *   The entity object
 * @param $type
 *   The type of entity being inserted (i.e. node, user, comment).
 */
function thumbwhere_place_entity_insert($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_place') return;

}


/**
* Implements thumbwhere_place_entity_delete().
*
* Act on entities when inserted.
*
* @param $entity
*   The entity object
* @param $type
*   The type of entity being deleted (i.e. node, user, comment).
*/
function thumbwhere_place_entity_delete($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_place') return;


}
