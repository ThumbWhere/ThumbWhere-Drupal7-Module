<?php

// ******************************************************************
// This file was generated by autogen_model_implementation_drupal.xsl
// ******************************************************************

// Include the ThumbWhere SDK
require_once(dirname(__FILE__) . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . 'dependencies' . DIRECTORY_SEPARATOR . 'tw' . DIRECTORY_SEPARATOR . 'sdk.class.php');

/**
 * @file
 * Module for the ThumbWhere ProfileImage Entity
 *
 *
 * NOTE: This has been autogenerated.
 * 
 */

/**
 * Implement hook_entity_info().
 *
 * We define two entities here - the actual entity that will hold our domain
 * specific information and an entity that holds information about the different
 * types of entities. See here: http://drupal.org/node/977380 for a discussion on this
 * choice.
 */
function thumbwhere_profileimage_entity_info() {
  $return['thumbwhere_profileimage'] = array(

    // The human-readable name of the type.
    'label' => t('ThumbWhereProfileImage'),

    // (used by DrupalDefaultEntityController) The name of the entity type's base table.
    'base table' => 'thumbwhere_profileimage',

    // fieldable: Set to TRUE if you want your entity type to accept fields being attached to it.
    'fieldable' => TRUE,

  // Declare the endty keys..
    'entity keys' => array(
      'id' => 'pk_profileimage',
      'label' => 'pk_profileimage',
    ),

    // A function taking an entity and an entity type as arguments and returning the label of the entity.
    // The entity label is the main string associated with an entity; for example, the title of a node or
    // the subject of a comment. If there is an entity object property that defines the label, use the
    // 'label' element of the 'entity keys' return value component to provide this information (see below).
    // If more complex logic is needed to determine the label of an entity, you can instead specify a callback
    // function here, which will be called to determine the entity label. See also the entity_label()
    // function, which implements this logic.
    'label callback' => 'entity_class_label',

    // A function taking an entity as argument and returning the uri elements of the entity, e.g. 'path' and 'options'.
    // The actual entity uri can be constructed by passing these elements to url().
    'uri callback' => 'entity_class_uri',

    //
    // Entity API Components
    //

    // The module whwre we can find our classes
    'module' => 'thumbwhere_profileimage',

    // The entity class and controller class extend the classes provided by the Entity API
    'entity class' => 'ThumbWhereProfileImage',

    // Which Entity API contoller
    'controller class' => 'ThumbWhereProfileImageController',

    // The information below is used by the ThumbWhereProfileImageUIController (which extends the EntityDefaultUIController)
    'admin ui' => array(
      'path' => 'admin/thumbwhere/thumbwhere_profileimages',
      'file' => 'thumbwhere_profileimage.admin.inc',
      'controller class' => 'ThumbWhereProfileImageUIController',
      'menu wildcard' => '%thumbwhere_profileimage',
    ),
  );

  return $return;
}


/**
 * Implements hook_permission().
 */
function thumbwhere_profileimage_permission() {

  // We set up permisssions to manage entity types, manage all entities and the
  // permissions for each individual entity
  $permissions = array(
    'administer thumbwhere_profileimages' => array(
      'title' => t('Administer thumbwhere_profileimages'),
      'description' => t('Edit and delete all thumbwhere_profileimages'),
    ),
  'send thumbwhere profileimage notifications' => array(
          'title' => t('Send ThumbWhere Notifications'),
          'description' => t('Allow the caller to call the ThumbWhere ProfileImage Entity Notifications Endpoint.'),
          'restrict access' => TRUE,
      ),    
  );

  return $permissions;
}


/**
 * Determines whether the given user has access to a thumbwhere_profileimage.
 *
 * @param $op
 *   The operation being performed. One of 'view', 'update', 'create', 'delete'
 *   or just 'edit' (being the same as 'create' or 'update').
 * @param $thumbwhere_profileimage
 *   Optionally a thumbwhere_profileimage or a thumbwhere_profileimage type to check access for. If nothing is
 *   given, access for all thumbwhere_profileimages is determined.
 * @param $account
 *   The user to check for. Leave it to NULL to check for the global user.
 * @return boolean
 *   Whether access is allowed or not.
 */
function thumbwhere_profileimage_access($op, $thumbwhere_profileimage = NULL, $account = NULL) {

  //if (user_access('administer thumbwhere_profileimages', $account)) {
  //  return TRUE;
 // }

  //return FALSE;
  return TRUE;
}




/**
 * Fetch a thumbwhere_profileimage object. Make sure that the wildcard you choose
 * in the thumbwhere_profileimage entity definition fits the function name here.
 *
 * @param $thumbwhere_profileimage_id
 *   Integer specifying the thumbwhere_profileimage id.
 * @param $reset
 *   A boolean indicating that the internal cache should be reset.
 * @return
 *   A fully-loaded $thumbwhere_profileimage object or FALSE if it cannot be loaded.
 *
 * @see thumbwhere_profileimage_load_multiple()
 */
function thumbwhere_profileimage_load($thumbwhere_profileimage_id, $reset = FALSE) {

  $thumbwhere_profileimages = entity_load('thumbwhere_profileimage', array($thumbwhere_profileimage_id), array(), $reset);

  return reset($thumbwhere_profileimages);
}


/**
 * Deletes a thumbwhere_profileimage.
 */
function thumbwhere_profileimage_delete(ThumbWhereProfileImage $thumbwhere_profileimage) {

  $thumbwhere_profileimage->delete();
}

/**
 * Create a thumbwhere_profileimage object.
 */
function thumbwhere_profileimage_create($values = array()) {

  return entity_get_controller('thumbwhere_profileimage')->create($values);
}


/**
 * URI callback for thumbwhere_profileimages
 */
function thumbwhere_profileimage_uri(ThumbWhereProfileImage $thumbwhere_profileimage) {

  return array(
    'path' => 'thumbwhere_profileimage/' . $thumbwhere_profileimage->pk_profileimage,
  );
}


/**
 * Menu title callback for showing individual entities
 */
function thumbwhere_profileimage_page_title(ThumbWhereProfileImage $thumbwhere_profileimage) {

	return $thumbwhere_profileimage->pk_profileimage;
}


/**
 * Sets up content to show an individual thumbwhere_profileimage
 * @todo - get rid of drupal_set_title();
 */
function thumbwhere_profileimage_page_view($thumbwhere_profileimage, $view_mode = 'full') {



  $controller = entity_get_controller('thumbwhere_profileimage');
  $content = $controller->view(array($thumbwhere_profileimage->pk_profileimage => $thumbwhere_profileimage));
  drupal_set_title($thumbwhere_profileimage->pk_profileimage);
  return $content;
}


/**
 * Implements hook_views_api().
 */
function thumbwhere_profileimage_views_api() {



  return array(
    'api' => 3,
    'path' => drupal_get_path('module', 'thumbwhere_profileimage') . '/views',
  );
}


/**
 * Implement hook_theme().
 */
function thumbwhere_profileimage_theme() {



  return array(
    'thumbwhere_profileimage_add_list' => array(
      'variables' => array('content' => array()),
      'file' => 'thumbwhere_profileimage.admin.inc',
    ),
    'thumbwhere_profileimage' => array(
      'render element' => 'elements',
      'template' => 'thumbwhere_profileimage',
    )
  );
}


/**
 * Implements hook_menu_local_tasks_alter().
 */
function thumbwhere_profileimage_menu_local_tasks_alter(&$data, $router_item, $root_path) {

  // Add action link 'admin/structure/thumbwhere_profileimages/add' on 'admin/structure/thumbwhere_profileimages'.
  if ($root_path == 'admin/thumbwhere/thumbwhere_profileimages') {
    $item = menu_get_item('admin/thumbwhere/thumbwhere_profileimages/add');
    if ($item['access']) {
      $data['actions']['output'][] = array(
        '#theme' => 'menu_local_action',
        '#link' => $item,
      );
    }
  }
}


/**
 * The class used for thumbwhere_profileimage entities
 */
class ThumbWhereProfileImage extends Entity {

  public function __construct($values = array()) {
    parent::__construct($values, 'thumbwhere_profileimage');
  }

  protected function defaultLabel() {
    return $this->pk_profileimage;
  }

  protected function defaultUri() {
    return array('path' => 'thumbwhere_profileimage/' . $this->pk_profileimage);
  }


}



/**
 * The Controller for ThumbWhereProfileImage entities
 */
class ThumbWhereProfileImageController extends EntityAPIController {
  public function __construct($entityType) {
    parent::__construct($entityType);
  }
}



/**
* Implements hook_entity_presave().
*
* Act on an ProfileImage entity before it is about to be created or updated.
*
* @param $entity
*   The entity object.
* @param $entity_type
*   The type of entity being saved (i.e. node, user, comment).
*/
function thumbwhere_profileimage_entity_presave($entity, $entity_type) {

  // We only look after what we want...
  if ($entity_type == 'thumbwhere_profileimage')  {
  	
 	/////////////////////////////////////////////////////
    //
    // Should we not propagate this to ThumbWhere? 
    //
    if (isset($entity->do_not_propagate)) {
      if ($entity->do_not_propagate) {
        if (twCanDebug()) {
          if ($entity->is_new) {
            if (twCanDebug()){
              debug("DO NOT PROPAGATE 'profileimage' presave *CREATE* not being sent to ThumbWhere server.");
          	}
          }
          else {
            if (twCanDebug()) {
              debug("DO NOT PROPAGATE 'profileimage' presave *UPDATE* not being sent to ThumbWhere server.");
            }
          }
        }

        // To cause an infinite loop of updates, comment out this line.
        return;
      }
      else if (twCanDebug()) {
        debug("OK PROAOGATE 'profileimage' presave to ThumbWhere server.");
      }
    }
    else if (twCanDebug()) {
      debug("DO NOT PROPAGATE IS NOT SET");
    }
    //
    //
    ////////////////////////////////////////////////////////  	

  
    // .. then we create a new thumbwhere_profileimage entity...
      // Instantiate the ThumbWhereContent class

    $twContentAPI = new ThumbWhereAPIContent();

    //if (twCanDebug()) debug($twContentAPI);

    //if (twCanDebug()) debug($entity);

    //
    // Collate and validate our fields
    //

    $fields = array();
    // Compulsory so..
    if (!isset($entity->fk_actor)) {
      throw new Exception('actor is mandatory.');
    }
    else {
      $fields['actor'] = $entity->fk_actor;
    }
    // Compulsory so..
    if (!isset($entity->fk_mediaitem)) {
      throw new Exception('mediaitem is mandatory.');
    }
    else {
      $fields['mediaitem'] = $entity->fk_mediaitem;
    }
		
	//
	// Ensure that we have the 'thumbwhere_api_key' as we are going to pass that through in a context
	//
    if (variable_get('thumbwhere_api_key') == '') {
    	throw new Exception('ThumbWhere resource for \'ProfileImage\' requires that variable \'thumbwhere_api_key\' is set.');
    }
    
              		
    //
    // If this profileimage entity is new....
    //
               
    if ($entity->is_new) {
                

      //
      // ... then we create the profileimage entity in ThumbWhere
      //

      $profileimage = $twContentAPI->create_profileimage($context = array(
                  'key' => variable_get('thumbwhere_api_key')
          ), $fields);
            
       if (twCanDebug()) debug($profileimage);

      //if (twCanDebug()) debug($profileimage);

      $attributes = $profileimage->body->profileimage->attributes();

      if (twCanDebug()) debug('newly created id = ' . $attributes['id']);

      // Assign new id.
      $entity->pk_profileimage = intval($attributes['id']);

      if (twCanDebug()) debug('$entity->pk_profileimage = ' . $entity->pk_profileimage);
    }
    else
    {
          	

      //
      // .. otherwise we update the profileimage entity in ThumbWhere
      //

      $profileimage = $twContentAPI->update_profileimage($id=$entity->pk_profileimage,$context = array(
                  'key' => variable_get('thumbwhere_api_key')
          ), $fields);
            
        
    }
          	
    
  }
}

/**
 * Implements thumbwhere_profileimage_entity_insert().
 *
 * Act on entities when inserted.
 *
 * @param $entity
 *   The entity object
 * @param $type
 *   The type of entity being inserted (i.e. node, user, comment).
 */
function thumbwhere_profileimage_entity_insert($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_profileimage') return;

}


/**
* Implements thumbwhere_profileimage_entity_delete().
*
* Act on entities when inserted.
*
* @param $entity
*   The entity object
* @param $type
*   The type of entity being deleted (i.e. node, user, comment).
*/
function thumbwhere_profileimage_entity_delete($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_profileimage') return;


}
