<?php

/**
 * @file
 * ThumbWhereProducerMember editing UI.
 *
 * We make very little use of the EntityAPI interface for this - preferring instead to use
 * views. That offers more flexibility to change a UI that will, more often than not,
 * be end-user facing.
 *
 * NOTE: This file was generated by autogen_model_implementation_drupal.xsl
 */

 
/**
 * Handle the notification for content_ingext
 */
function _thumbwhere_handle_producer_member_notify_xml($notification) {

  //
  // Get the operation
  //

  if (!isset($notification->attributes()->op)) {
    drupal_add_http_header('Status', '501 unable to fulfil request..');
    $err = 'We were expecting an \'op\', attribute on the \'producer_member\', element.';
    print($err);
   	watchdog('thumbwhere_producermember', $err ,array(), WATCHDOG_ERROR);
    exit(0);
  }

  $op = $notification->attributes()->op;

  if (twCanDebug()) debug('op');
  if (twCanDebug()) debug($op);

  //
  // Get the context
  //

  if (!isset($notification->attributes()->key)) {
    drupal_add_http_header('Status', '412 unable to fulfil request..');
    $err = 'We were expecting a \'key\', attribute on the \'producer_member\', element.';
    print($err);
    watchdog('thumbwhere_producermember', $err ,array(), WATCHDOG_ERROR);
    exit(0);
  }

  $context_key =  $notification->attributes()->key;

  //
  // Now based on the operation..
  //

  switch($op)
  {
  
  
      case 'create':
      {
      		
		
		//
		// We want to time this
		//
			
		$start_time = microtime(TRUE);
			
        //
        // What resource are we creating?
        //
      
        //
        // Get the id
        //
		
        if (!isset($notification->attributes()->id)) {
          drupal_add_http_header('Status', '412 Unable to fulfil request.');
          $err = 'We were expecting an \'id\', attribute on the \'producer_member\', element when we are performing an \'update\' notification..';
          print($err);
          watchdog('thumbwhere_producermember', $err ,array(), WATCHDOG_ERROR);
          exit(0);
        }

        $id = intval($notification->attributes()->id);
		

        if (twCanDebug()) debug('id');
        if (twCanDebug()) debug($id);

      
        //
        // Get the fields
        //
          
        $entities = entity_load('thumbwhere_producermember', array($id));

        if (twCanDebug()) debug('$entities');
        if (twCanDebug()) debug($entities);

        if (count($entities) != 0) {
          //drupal_add_http_header('Status', '409 Resource already exists.');
          $err = '\'producer_member\' resource already exists with an id of \'' . $id . '\'.';
           //print($err);
          watchdog('thumbwhere_producermember', $err ,array(), WATCHDOG_ERROR);
          //exit(0);

          	
          drupal_add_http_header('Content-Type', 'text/xml');
          print('<response>');
          print('<producer_member op="' . $op . '" id="' . $id . '" >');
          print('<status>success</status>');
          print('</producer_member>');
          print('</response>');
          exit(0);          	
          
        }

        // Getting a reference to the entity we asked for
        $entityfields = array();

        //
        // We are only updating what has been passed through.
        // No test for missing values.
        // TODO: Validate incoming values.
        //


        $entityfields['pk_producermember'] = $id;	

        if (isset($notification->producer)) {
          $entityfields['fk_producer'] = intval($notification->producer);
        }

        if (isset($notification->member)) {
          $entityfields['fk_member'] = intval($notification->member);
        }

        if (isset($notification->admin)) {
          if ((string)($notification->admin) == 'true') {
            $entityfields['admin'] = '1';
          }
          else {
            $entityfields['admin'] = '0';
          }
        }

        if (!isset($notification->attributes()->key)) {
          if (twCanDebug()) debug('$entities');
          if (twCanDebug()) debug($entities);
        }

        // We are being notified, so we do not want to ship this out again as a notification...
        // Finally write the entity.
        $entity = entity_create('thumbwhere_producermember', $entityfields);
        $entity->do_not_propagate = TRUE;
        $entity->save();

        //
        // Send the final response
        //
        
        $duration = round(((microtime(TRUE) - $start_time) * 1000.0),3); 

        drupal_add_http_header('Content-Type', 'text/xml');
        print('<response duration="' . $duration . 'ms">');
        print('<producer_member op="' . $op . '" id="' . $id . '" >');
        print('<status>success</status>');
        print('</producer_member>');
        print('</response>');
        exit(0);			
		
      
      }
  
  
    case 'update':
      {
      
		//
		// We want to time this
		//
			
		$start_time = microtime(TRUE);
      
        //
        // What resource are we updating?
        //
        
      
        //
        // Get the id
        //
		
        if (!isset($notification->attributes()->id)) {
          drupal_add_http_header('Status', '412 Unable to fulfil request.');
          $err = 'We were expecting an \'id\', attribute on the \'producer_member\', element when we are performing an \'update\' notification..';
          print($err);
          watchdog('thumbwhere_producermember', $err ,array(), WATCHDOG_ERROR);
          exit(0);
        }

        $id = intval($notification->attributes()->id);
		

        if (twCanDebug()) debug('id');
        if (twCanDebug()) debug($id);

      
        //
        // Get the fields
        //
          
        $entities = entity_load('thumbwhere_producermember', array($id));

        if (twCanDebug()) debug('$entities');
        if (twCanDebug()) debug($entities);

        if (count($entities) == 0) {
          //drupal_add_http_header('Status', '409 No such resource exists.');
          $err = 'No such \'producer_member\' resource exists with an id of \'' . $id . '\'.';
          //print($err);
          watchdog('thumbwhere_producermember',$err,array(), WATCHDOG_ERROR);
          //exit(0);

////////////////////////////////////////////////////////////////////////
//
// ... for now we will just create the local copy
//
          watchdog('thumbwhere_producermember','creating local version of missing entity',array(), WATCHDOG_ERROR);
		
		
		//
		// We want to time this
		//
			
		$start_time = microtime(TRUE);
			
        //
        // What resource are we creating?
        //
      
        //
        // Get the id
        //
		
        if (!isset($notification->attributes()->id)) {
          drupal_add_http_header('Status', '412 Unable to fulfil request.');
          $err = 'We were expecting an \'id\', attribute on the \'producer_member\', element when we are performing an \'update\' notification..';
          print($err);
          watchdog('thumbwhere_producermember', $err ,array(), WATCHDOG_ERROR);
          exit(0);
        }

        $id = intval($notification->attributes()->id);
		

        if (twCanDebug()) debug('id');
        if (twCanDebug()) debug($id);

      
        //
        // Get the fields
        //
          
        $entities = entity_load('thumbwhere_producermember', array($id));

        if (twCanDebug()) debug('$entities');
        if (twCanDebug()) debug($entities);

        if (count($entities) != 0) {
          //drupal_add_http_header('Status', '409 Resource already exists.');
          $err = '\'producer_member\' resource already exists with an id of \'' . $id . '\'.';
           //print($err);
          watchdog('thumbwhere_producermember', $err ,array(), WATCHDOG_ERROR);
          //exit(0);

          	
          drupal_add_http_header('Content-Type', 'text/xml');
          print('<response>');
          print('<producer_member op="' . $op . '" id="' . $id . '" >');
          print('<status>success</status>');
          print('</producer_member>');
          print('</response>');
          exit(0);          	
          
        }

        // Getting a reference to the entity we asked for
        $entityfields = array();

        //
        // We are only updating what has been passed through.
        // No test for missing values.
        // TODO: Validate incoming values.
        //


        $entityfields['pk_producermember'] = $id;	

        if (isset($notification->producer)) {
          $entityfields['fk_producer'] = intval($notification->producer);
        }

        if (isset($notification->member)) {
          $entityfields['fk_member'] = intval($notification->member);
        }

        if (isset($notification->admin)) {
          if ((string)($notification->admin) == 'true') {
            $entityfields['admin'] = '1';
          }
          else {
            $entityfields['admin'] = '0';
          }
        }

        if (!isset($notification->attributes()->key)) {
          if (twCanDebug()) debug('$entities');
          if (twCanDebug()) debug($entities);
        }

        // We are being notified, so we do not want to ship this out again as a notification...
        // Finally write the entity.
        $entity = entity_create('thumbwhere_producermember', $entityfields);
        $entity->do_not_propagate = TRUE;
        $entity->save();

        //
        // Send the final response
        //
        
        $duration = round(((microtime(TRUE) - $start_time) * 1000.0),3); 

        drupal_add_http_header('Content-Type', 'text/xml');
        print('<response duration="' . $duration . 'ms">');
        print('<producer_member op="' . $op . '" id="' . $id . '" >');
        print('<status>success</status>');
        print('</producer_member>');
        print('</response>');
        exit(0);			
		
//
//
//
////////////////////////////////////////////////////////////////////////
        }

        // Getting a reference to the entity we asked for
        $entity = $entities[$id];

        //
        // We are only updating what has been passed through.
        // No test for missing values.
        // TODO: Validate incoming values.
        //


        if (isset($notification->producer)) {
          $entity->fk_producer = intval($notification->producer);
        }

        if (isset($notification->member)) {
          $entity->fk_member = intval($notification->member);
        }

        if (isset($notification->admin)) {
          if ((string)($notification->admin) == 'true') {
            $entity->admin = '1';
          }
          else {
            $entity->admin = '0';
          }
        }

        if (!isset($notification->attributes()->key)) {
          if (twCanDebug()) debug('$entities');
          if (twCanDebug()) debug($entities);
        }

        // We are being notified, so we do not want to ship this out again as a notification...
        $entity->do_not_propagate = TRUE;

        // Finally write the entity.
        entity_save('thumbwhere_producermember', $entity);

        //
        // Send the final response
        //
        
        $duration = round(((microtime(TRUE) - $start_time) * 1000.0),3);

        drupal_add_http_header('Content-Type', 'text/xml');
        print('<response  duration="' . $duration . '" >');
        print('<producer_member op="' . $op . '" id="' . $id . '" >');
        print('<status>success</status>');
        print('</producer_member>');
        print('</response>');
        exit(0);
      }
      
      

    default:
      {
        drupal_add_http_header('Status', '501 Unable to fulfil request. Unhandled operation \'' . $op . '\'.');
        print('We don\'t handle the \'' . $op . '\', notification operation on the \'producer_member\', element.');
        exit(0);

      }
  }
}



/**
 * Menu callback: React to a notification for producermember.
 */
function _thumbwhere_producer_member_notify() {

  if (twCanDebug()) {
    watchdog('thumbwhere_producer_member', 'incoming notification.');
  }

  $numargs = func_num_args();
  $arg_list = func_get_args();

  
	if (twCanDebug()) debug('$numargs  = ' . $numargs);
	
	if (twCanDebug()) debug('$arg_list = ' . $arg_list);
	
	if (twCanDebug()) debug('$_REQUEST');
	if (twCanDebug()) debug($_REQUEST);
	
	if (twCanDebug()) debug('$_GET');
	if (twCanDebug()) debug($_GET);
	
	if (twCanDebug()) debug('$_POST');
	if (twCanDebug()) debug($_POST);
	
	//if (twCanDebug()) debug('$HTTP_RAW_POST_DATA');
	//if (twCanDebug()) debug($HTTP_RAW_POST_DATA);
	
	if (twCanDebug()) debug('$_SERVER');
	if (twCanDebug()) debug($_SERVER);
	
	//if (twCanDebug()) debug('$GLOBALS');
	//if (twCanDebug()) debug($GLOBALS);

  

  if (variable_get('thumbwhere_api_log_debug', 0) == 1) {
    foreach ($arg_list as $arg) {
      if (twCanDebug()) debug('$arg  = ' . $arg);
    }
  }


  //////////////////////////////////////////////////
  //
  //

  switch ($_SERVER['REQUEST_METHOD']) {

    case 'POST': {
        if ($numargs == 1) {
          if ($arg_list[0] == 'producer_member') {

            //
            // Get the POST data as XML
            //

            $post_xml = new SimpleXMLElement('php://input', null, true);
            if (twCanDebug()) debug('$post_xml');
            if (twCanDebug()) debug($post_xml);

            if (twCanDebug()) debug('$post_xml->getName()');
            if (twCanDebug()) debug($post_xml->getName());

            // We expect 'action' as the root element. TODO: I would rather this was the more generic 'request'
            if ($post_xml->getName() != 'action') {
              drupal_add_http_header('Status', '404 not found.');
              print('We were expecting \'action\', as the root element, not \'' . $post_xml->getName() . '\'.');
              exit(0);
            }

            /*
             *  SimpleXMLElement::__set_state(array(
             *      'producer_member' =>
             *      SimpleXMLElement::__set_state(array(
             *       '@attributes' =>
             *      array (
             *        'key' => '8e9ff318-3cd7-4593-99de-4c37c0fb335a',
             *        'op' => 'create',
             *      ),
             *    )),
             *  ))
             */


            // We expect 'action' as the root element.
            if (!isset($post_xml->producer_member)) {
              drupal_add_http_header('Status', '404 not found.');
              print('We were expecting a \'producer_member\', element in the action payload.');
              exit(0);
            }

            // Handle the incoming notification for content ingest
            _thumbwhere_handle_producer_member_notify_xml($post_xml->producer_member);

          }
          else {
            drupal_add_http_header('Status', '404 not found.');
            print('Invalid resource');
            exit(0);
          }
        }
        else {
          drupal_add_http_header('Status', '404 not found.');
          print('Invalid notification request url format.');
          exit(0);
        }
      }
      break;

    case 'GET': {

        if ($numargs == 1) {

          // If we are returning the index.xml file for the webservice.
          if ($arg_list[0] == 'index.xml') {
            // return index.xml
            drupal_add_http_header('Content-Type', 'text/xml');
            readfile(dirname(__FILE__) . '/index.xml');
            exit(0);
          }
          else {
            drupal_add_http_header('Status', '404 not found.');
            print('File not found.');
            exit(0);
          }
        }
        else {
          drupal_add_http_header('Status', '404 not found.');
          print('Invalid notification request url format.');
          exit(0);
        }
      }
      break;

    default: {
        drupal_add_http_header('Status', '404 not found.');
        exit(0);
      }
      break;
  }

  //
  //
  ////////////////////////////////////////////////////////////////////////////////////
 
}





/**
 * ProducerMember UI controller.
 */
class ThumbWhereProducerMemberUIController extends EntityDefaultUIController {

  /**
   * Overrides hook_menu() defaults. Main reason for doing this is that
   * parent class hook_menu() is optimized for entity type administration.
   */
  public function hook_menu() {

    $items = array();
    
    // Add a notification page...
    $items['thumbwhere/producer_member/notify'] = array(
        'title' => 'Notifications Callback for "ProducerMember" Entity',
        'page callback' => '_thumbwhere_producer_member_notify',
        'access arguments' => array(
            'send thumbwhere producermember notifications'
        ),
        'file' => 'thumbwhere_producermember.admin.inc',
        'file path' => drupal_get_path('module', $this->entityInfo['module'])
    );    
    
    $id_count = count(explode('/', $this->path));
    $wildcard = isset($this->entityInfo['admin ui']['menu wildcard']) ? $this->entityInfo['admin ui']['menu wildcard'] : '%' . $this->entityType;

    $items[$this->path] = array(
      'title' => 'ProducerMember',
      'description' => 'Add edit and update thumbwhere_producermembers.',
      'page callback' => 'system_admin_menu_block_page',
      'access arguments' => array('access administration pages'),
      'file path' => drupal_get_path('module', 'system'),
      'file' => 'system.admin.inc',
    );

    // Change the overview menu type for the list of thumbwhere_producermembers.
    $items[$this->path]['type'] = MENU_LOCAL_TASK;

    // Change the add page menu to multiple types of entities
    $items[$this->path . '/add'] = array(
      //'title' => 'Add a ProducerMember',
      'title' => 'Add',
      'description' => 'Add a new ProducerMember',
      'page callback' => 'thumbwhere_producermember_form_wrapper',
      'page arguments' => array(thumbwhere_producermember_create(array('type' => 'thumbwhere_producermember'))),
      'access callback' => 'thumbwhere_producermember_access',
      'access arguments' => array('edit', 'edit ' . 'thumbwhere_producermember'),
      'file' => 'thumbwhere_producermember.admin.inc',
      'file path' => drupal_get_path('module', $this->entityInfo['module'])
    );


/*
    // Change the add page menu to multiple types of entities
    $items[$this->path . '/add'] = array(
      //'title' => 'Add a ProducerMember',
      'title' => 'Add',
	  'description' => 'Add a new ProducerMember',
      'page callback'  => 'thumbwhere_producermember_add_page',
      'access callback'  => 'thumbwhere_producermember_access',
      'access arguments' => array('edit'),
      'type' => MENU_NORMAL_ITEM,
      'weight' => 20,
      'file' => 'thumbwhere_producermember.admin.inc',
      'file path' => drupal_get_path('module', $this->entityInfo['module'])

    );
*/    
/*
    $items[$this->path . '/add/' . 'thumbwhere_producermember'] = array(
      'title' => 'Add ' . 'ThumbWhereProducerMember',
      'page callback' => 'thumbwhere_producermember_form_wrapper',
      'page arguments' => array(thumbwhere_producermember_create(array('type' => 'thumbwhere_producermember'))),
      'access callback' => 'thumbwhere_producermember_access',
      'access arguments' => array('edit', 'edit ' . 'thumbwhere_producermember'),
      'file' => 'thumbwhere_producermember.admin.inc',
      'file path' => drupal_get_path('module', $this->entityInfo['module'])
    );
*/
    // Loading and editing thumbwhere_producermember entities
    $items[$this->path . '/thumbwhere_producermember/' . $wildcard] = array(
      'page callback' => 'thumbwhere_producermember_form_wrapper',
      'page arguments' => array($id_count + 1),
      'access callback' => 'thumbwhere_producermember_access',
      'access arguments' => array('edit', $id_count + 1),
      'weight' => 0,
      'context' => MENU_CONTEXT_PAGE | MENU_CONTEXT_INLINE,
      'file' => 'thumbwhere_producermember.admin.inc',
      'file path' => drupal_get_path('module', $this->entityInfo['module'])
    );
    $items[$this->path . '/thumbwhere_producermember/' . $wildcard . '/edit'] = array(
      'title' => 'Edit',
      'type' => MENU_DEFAULT_LOCAL_TASK,
      'weight' => -10,
      'context' => MENU_CONTEXT_PAGE | MENU_CONTEXT_INLINE,
    );

    $items[$this->path . '/thumbwhere_producermember/' . $wildcard . '/delete'] = array(
      'title' => 'Delete',
      'page callback' => 'thumbwhere_producermember_delete_form_wrapper',
      'page arguments' => array($id_count + 1),
      'access callback' => 'thumbwhere_producermember_access',
      'access arguments' => array('edit', $id_count + 1),
      'type' => MENU_LOCAL_TASK,
      'context' => MENU_CONTEXT_INLINE,
      'weight' => 10,
      'file' => 'thumbwhere_producermember.admin.inc',
      'file path' => drupal_get_path('module', $this->entityInfo['module'])
    );

    // Menu item for viewing thumbwhere_producermembers
    $items['thumbwhere_producermember/' . $wildcard] = array(
      //'title' => 'Title',
      'title callback' => 'thumbwhere_producermember_page_title',
      'title arguments' => array(1),
      'page callback' => 'thumbwhere_producermember_page_view',
      'page arguments' => array(1),
      'access callback' => 'thumbwhere_producermember_access',
      'access arguments' => array('view', 1),
      'type' => MENU_CALLBACK,
    );
    return $items;
  }


  /**
   * Create the markup for the add ThumbWhereProducerMember Entities page within the class
   * so it can easily be extended/overriden.
   */
  public function addPage() {
    $item = menu_get_item();
    $content = system_admin_menu_block($item);

    if (count($content) == 1) {
      $item = array_shift($content);
      drupal_goto($item['href']);
    }

    return theme('thumbwhere_producermember_add_list', array('content' => $content));
  }

}


/**
 * Form callback wrapper: create or edit a thumbwhere_producermember.
 *
 * @param $thumbwhere_producermember
 *   The thumbwhere_producermember object being edited by this form.
 *
 * @see thumbwhere_producermember_edit_form()
 */
function thumbwhere_producermember_form_wrapper($thumbwhere_producermember) {
  // Add the breadcrumb for the form's location.
  thumbwhere_producermember_set_breadcrumb();
  return drupal_get_form('thumbwhere_producermember_edit_form', $thumbwhere_producermember);
}


/**
 * Form callback wrapper: delete a thumbwhere_producermember.
 *
 * @param $thumbwhere_producermember
 *   The thumbwhere_producermember object being edited by this form.
 *
 * @see thumbwhere_producermember_edit_form()
 */
function thumbwhere_producermember_delete_form_wrapper($thumbwhere_producermember) {
  // Add the breadcrumb for the form's location.
  //thumbwhere_producermember_set_breadcrumb();
  return drupal_get_form('thumbwhere_producermember_delete_form', $thumbwhere_producermember);
}


/**
 * Form callback: create or edit a thumbwhere_producermember.
 *
 * @param $thumbwhere_producermember
 *   The thumbwhere_producermember object to edit or for a create form an empty thumbwhere_producermember object
 *     with only a thumbwhere_producermember type defined.
 */
function thumbwhere_producermember_edit_form($form, &$form_state, $thumbwhere_producermember) {

  // Add the default field elements.
  $form['fk_producer'] = array(
    '#type' => 'textfield',
    '#title' => t('ThumbWhereProducerMember fk_producer'),
    '#default_value' => isset($thumbwhere_producermember->fk_producer) ? $thumbwhere_producermember->fk_producer : '',
    //'#maxlength' => 255,
   /// '#required' => TRUE,
    '#weight' => -5,
    '#autocomplete_path' => 'entity-autocomplete/thumbwhere_producer',
  );
  // Add the default field elements.
  $form['fk_member'] = array(
    '#type' => 'textfield',
    '#title' => t('ThumbWhereProducerMember fk_member'),
    '#default_value' => isset($thumbwhere_producermember->fk_member) ? $thumbwhere_producermember->fk_member : '',
    //'#maxlength' => 255,
   /// '#required' => TRUE,
    '#weight' => -5,
    '#autocomplete_path' => 'entity-autocomplete/thumbwhere_member',
  );
  // Add the default field elements.
    $form['admin'] = array(
    '#type' => 'checkbox',
    '#title' => t('ThumbWhereProducerMember admin'),
    '#default_value' => isset($thumbwhere_producermember->admin) ? $thumbwhere_producermember->admin : 0,
    // '#maxlength' => 255,
   // '#required' => TRUE,
    '#weight' => -5,
  );



  $form['data']['#tree'] = TRUE;

  /*
  $form['data']['sample_data'] = array(
    '#type' => 'checkbox',
    '#title' => t('An interesting thumbwhere_producermember switch'),
    '#default_value' => isset($thumbwhere_producermember->data['sample_data']) ? $thumbwhere_producermember->data['sample_data'] : 1,
  );
  */


  // Add the field related form elements.
  $form_state['thumbwhere_producermember'] = $thumbwhere_producermember;
  field_attach_form('thumbwhere_producermember', $thumbwhere_producermember, $form, $form_state);

  $form['actions'] = array(
    '#type' => 'container',
    '#attributes' => array('class' => array('form-actions')),
    '#weight' => 400,
  );

  // We add the form's #submit array to this button along with the actual submit
  // handler to preserve any submit handlers added by a form callback_wrapper.
  $submit = array();

  if (!empty($form['#submit'])) {
    $submit += $form['#submit'];
  }

  $form['actions']['submit'] = array(
    '#type' => 'submit',
    '#value' => t('Save thumbwhere_producermember'),
    '#submit' => $submit + array('thumbwhere_producermember_edit_form_submit'),
  );

  // Do we show the delete button?
  if (!empty($thumbwhere_producermember->pk_producermember)) {
    $form['actions']['delete'] = array(
      '#type' => 'submit',
      '#value' => t('Delete thumbwhere_producermember'),
      '#suffix' => l(t('Cancel'), 'admin/thumbwhere/thumbwhere_producermembers'),
      '#submit' => $submit + array('thumbwhere_producermember_form_submit_delete'),
      '#weight' => 45,
    );
  }

  // We append the validate handler to #validate in case a form callback_wrapper
  // is used to add validate handlers earlier.
  $form['#validate'][] = 'thumbwhere_producermember_edit_form_validate';
  return $form;
}


/**
 * Form API validate callback for the thumbwhere_producermember form
 */
function thumbwhere_producermember_edit_form_validate(&$form, &$form_state) {
   
  //if (twCanDebug()) debug($form);   
  //if (twCanDebug()) debug($form_state);


  // No validation for pk


  // Convert fk autocomplete for fk_producer
  $value = $form['fk_producer']['#value'];
  // If we have something, escape the auto-completion encoding.
  if (!empty($value)) { 
    $value = entity_autocomplete_get_id($value);
    form_set_value($form['fk_producer'], $value,$form_state);
  }
  //if (twCanDebug()) debug($form['fk_producer']);
   if (twCanDebug()) debug('fk_producer = ' . $value);
  // Validate fk fk_producer
  if (empty($value)) {
    form_set_error('fk_producer', t('Validation error, \'fk_producer\' is mandatory1.'));
    // throw new Exception('Field \'fk_producer\' in is mandatory');
  }

  // Convert fk autocomplete for fk_member
  $value = $form['fk_member']['#value'];
  // If we have something, escape the auto-completion encoding.
  if (!empty($value)) { 
    $value = entity_autocomplete_get_id($value);
    form_set_value($form['fk_member'], $value,$form_state);
  }
  //if (twCanDebug()) debug($form['fk_member']);
   if (twCanDebug()) debug('fk_member = ' . $value);
  // Validate fk fk_member
  if (empty($value)) {
    form_set_error('fk_member', t('Validation error, \'fk_member\' is mandatory1.'));
    // throw new Exception('Field \'fk_member\' in is mandatory');
  }

  // Validate normaladmin
  $value = $form['admin']['#value'];
  // If we have no default value then we don't care much for checking for emptyness.


              

  //form_set_value( array('#parents' => array('array_key_parent', 'array_key_to_replace')) , $value, $form_state);

   
   
  $thumbwhere_producermember = $form_state['thumbwhere_producermember'];

  // Notify field widgets to validate their data.
  field_attach_form_validate('thumbwhere_producermember', $thumbwhere_producermember, $form, $form_state);
    
    
  
}


/**
 * Form API submit callback for the thumbwhere_producermember form.
 *
 * @todo remove hard-coded link
 */
function thumbwhere_producermember_edit_form_submit(&$form, &$form_state) {

  $thumbwhere_producermember = entity_ui_controller('thumbwhere_producermember')->entityFormSubmitBuildEntity($form, $form_state);
  // Save the thumbwhere_producermember and go back to the list of thumbwhere_producermembers

  // Add in created and changed times.
  if ($thumbwhere_producermember->is_new = isset($thumbwhere_producermember->is_new) ? $thumbwhere_producermember->is_new : 0) {
    $thumbwhere_producermember->created = time();
  }

  $thumbwhere_producermember->changed = time();

  $thumbwhere_producermember->save();
  $form_state['redirect'] = 'admin/thumbwhere/thumbwhere_producermembers';
}

/**
 * Form API submit callback for the delete button.
 *
 * @todo Remove hard-coded path
 */
function thumbwhere_producermember_form_submit_delete(&$form, &$form_state) {
  $form_state['redirect'] = 'admin/thumbwhere/thumbwhere_producermembers/thumbwhere_producermember/' . $form_state['thumbwhere_producermember']->pk_producermember . '/delete';
}





/**
 * Form callback: confirmation form for deleting a thumbwhere_producermember.
 *
 * @param $thumbwhere_producermember
 *   The thumbwhere_producermember to delete
 *
 * @see confirm_form()
 */
function thumbwhere_producermember_delete_form($form, &$form_state, $thumbwhere_producermember) {
  $form_state['thumbwhere_producermember'] = $thumbwhere_producermember;

  $form['#submit'][] = 'thumbwhere_producermember_delete_form_submit';

  $form = confirm_form($form,
    t('Are you sure you want to delete thumbwhere_producermember %name?', array('%name' => $thumbwhere_producermember->pk_producermember)),
    'admin/thumbwhere/thumbwhere_producermembers/thumbwhere_producermember',
    '<p>' . t('This action cannot be undone.') . '</p>',
    t('Delete'),
    t('Cancel'),
    'confirm'
  );

  return $form;
}

/**
 * Submit callback for thumbwhere_producermember_delete_form
 */
function thumbwhere_producermember_delete_form_submit($form, &$form_state) {
  $thumbwhere_producermember = $form_state['thumbwhere_producermember'];

  thumbwhere_producermember_delete($thumbwhere_producermember);

  drupal_set_message(t('The thumbwhere_producermember %name has been deleted.', array('%name' => $thumbwhere_producermember->pk_producermember)));
  watchdog('thumbwhere_producermember', 'Deleted thumbwhere_producermember %name.', array('%name' => $thumbwhere_producermember->pk_producermember));

  $form_state['redirect'] = 'admin/thumbwhere/thumbwhere_producermembers';
}



/**
 * Page to add ThumbWhereProducerMember Entities.
 *
 * @todo Pass this through a proper theme function
 */
function thumbwhere_producermember_add_page() {
  $controller = entity_ui_controller('thumbwhere_producermember');
  return $controller->addPage();
}


/**
 * Displays the list of available thumbwhere_producermember types for thumbwhere_producermember creation.
 *
 * @ingroup themeable
 */
function theme_thumbwhere_producermember_add_list($variables) {


  //$content = $variables['content'];
  $output = '';
  /*
  if ($content) {
    $output = '<dl class="thumbwhere_producermember-type-list">';
    foreach ($content as $item) {
      $output .= '<dt>' . l($item['title'], $item['href']) . '</dt>';
      $output .= '<dd>' . filter_xss_admin($item['description']) . '</dd>';
    }
    $output .= '</dl>';
  }
  else {
    if (user_access('administer thumbwhere_producermember types')) {
      $output = '<p>' . t('ThumbWhereProducerMember Entities cannot be added because you have not created any thumbwhere_producermember types yet. Go to the <a href="@create-thumbwhere_producermember-type">thumbwhere_producermember type creation page</a> to add a new thumbwhere_producermember type.', array('@create-thumbwhere_producermember-type' => url('admin/structure/thumbwhere_producermember_types/add'))) . '</p>';
    }
    else {
      $output = '<p>' . t('No thumbwhere_producermember types have been created yet for you to use.') . '</p>';
    }
  }
  */
  return $output;
}





/**
 * Sets the breadcrumb for administrative thumbwhere_producermember pages.
 */
function thumbwhere_producermember_set_breadcrumb() {
  $breadcrumb = array(
    l(t('Home'), '<front>'),
    l(t('Administration'), 'admin'),
    l(t('ProducerMember'), 'admin/content'),
    l(t('ProducerMember'), 'admin/thumbwhere/thumbwhere_producermembers'),
  );

  drupal_set_breadcrumb($breadcrumb);
}



