<?php

// ******************************************************************
// This file was generated by autogen_model_implementation_drupal.xsl
// ******************************************************************

// Include the ThumbWhere SDK
require_once(dirname(__FILE__) . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . 'dependencies' . DIRECTORY_SEPARATOR . 'tw' . DIRECTORY_SEPARATOR . 'sdk.class.php');

/**
 * @file
 * Module for the ThumbWhere ProducerMember Entity
 *
 *
 * NOTE: This has been autogenerated.
 * 
 */

/**
 * Implement hook_entity_info().
 *
 * We define two entities here - the actual entity that will hold our domain
 * specific information and an entity that holds information about the different
 * types of entities. See here: http://drupal.org/node/977380 for a discussion on this
 * choice.
 */
function thumbwhere_producermember_entity_info() {
  $return['thumbwhere_producermember'] = array(

    // The human-readable name of the type.
    'label' => t('ThumbWhereProducerMember'),

    // (used by DrupalDefaultEntityController) The name of the entity type's base table.
    'base table' => 'thumbwhere_producermember',

    // fieldable: Set to TRUE if you want your entity type to accept fields being attached to it.
    'fieldable' => TRUE,

  // Declare the endty keys..
    'entity keys' => array(
      'id' => 'pk_producermember',
      'label' => 'pk_producermember',
    ),

    // A function taking an entity and an entity type as arguments and returning the label of the entity.
    // The entity label is the main string associated with an entity; for example, the title of a node or
    // the subject of a comment. If there is an entity object property that defines the label, use the
    // 'label' element of the 'entity keys' return value component to provide this information (see below).
    // If more complex logic is needed to determine the label of an entity, you can instead specify a callback
    // function here, which will be called to determine the entity label. See also the entity_label()
    // function, which implements this logic.
    'label callback' => 'entity_class_label',

    // A function taking an entity as argument and returning the uri elements of the entity, e.g. 'path' and 'options'.
    // The actual entity uri can be constructed by passing these elements to url().
    'uri callback' => 'entity_class_uri',

    //
    // Entity API Components
    //

    // The module whwre we can find our classes
    'module' => 'thumbwhere_producermember',

    // The entity class and controller class extend the classes provided by the Entity API
    'entity class' => 'ThumbWhereProducerMember',

    // Which Entity API contoller
    'controller class' => 'ThumbWhereProducerMemberController',

    // The information below is used by the ThumbWhereProducerMemberUIController (which extends the EntityDefaultUIController)
    'admin ui' => array(
      'path' => 'admin/content/thumbwhere_producermembers',
      'file' => 'thumbwhere_producermember.admin.inc',
      'controller class' => 'ThumbWhereProducerMemberUIController',
      'menu wildcard' => '%thumbwhere_producermember',
    ),
  );

  return $return;
}


/**
 * Implements hook_permission().
 */
function thumbwhere_producermember_permission() {

  // We set up permisssions to manage entity types, manage all entities and the
  // permissions for each individual entity
  $permissions = array(
    'administer thumbwhere_producermembers' => array(
      'title' => t('Administer thumbwhere_producermembers'),
      'description' => t('Edit and delete all thumbwhere_producermembers'),
    ),
  'send thumbwhere producermember notifications' => array(
          'title' => t('Send ThumbWhere Notifications'),
          'description' => t('Allow the caller to call the ThumbWhere ProducerMember Entity Notifications Endpoint.'),
          'restrict access' => TRUE,
      ),    
  );

  return $permissions;
}


/**
 * Determines whether the given user has access to a thumbwhere_producermember.
 *
 * @param $op
 *   The operation being performed. One of 'view', 'update', 'create', 'delete'
 *   or just 'edit' (being the same as 'create' or 'update').
 * @param $thumbwhere_producermember
 *   Optionally a thumbwhere_producermember or a thumbwhere_producermember type to check access for. If nothing is
 *   given, access for all thumbwhere_producermembers is determined.
 * @param $account
 *   The user to check for. Leave it to NULL to check for the global user.
 * @return boolean
 *   Whether access is allowed or not.
 */
function thumbwhere_producermember_access($op, $thumbwhere_producermember = NULL, $account = NULL) {

  //if (user_access('administer thumbwhere_producermembers', $account)) {
  //  return TRUE;
 // }

  //return FALSE;
  return TRUE;
}




/**
 * Fetch a thumbwhere_producermember object. Make sure that the wildcard you choose
 * in the thumbwhere_producermember entity definition fits the function name here.
 *
 * @param $thumbwhere_producermember_id
 *   Integer specifying the thumbwhere_producermember id.
 * @param $reset
 *   A boolean indicating that the internal cache should be reset.
 * @return
 *   A fully-loaded $thumbwhere_producermember object or FALSE if it cannot be loaded.
 *
 * @see thumbwhere_producermember_load_multiple()
 */
function thumbwhere_producermember_load($thumbwhere_producermember_id, $reset = FALSE) {

  //

  $thumbwhere_producermembers = entity_load('thumbwhere_producermember', array($thumbwhere_producermember_id), array(), $reset);

  return reset($thumbwhere_producermembers);
}





/**
 * Load multiple thumbwhere_producermembers based on certain conditions.
 *
 * @param $thumbwhere_producermember_ids
 *   An array of thumbwhere_producermember IDs.
 * @param $conditions
 *   An array of conditions to match against the {thumbwhere_producermember} table.
 * @param $reset
 *   A boolean indicating that the internal cache should be reset.
 * @return
 *   An array of thumbwhere_producermember objects, indexed by thumbwhere_producermember_id.
 *
 * @see entity_load()
 * @see thumbwhere_producermember_load()
 */
/*
function thumbwhere_producermember_load_multiple($thumbwhere_producermember_ids = array(), $conditions = array(), $reset = FALSE) {



  $loaded = entity_load('thumbwhere_producermember', $thumbwhere_producermember_ids, $conditions, $reset);

  return $loaded;
}
*/

/**
 * Deletes a thumbwhere_producermember.
 */
function thumbwhere_producermember_delete(ThumbWhereProducerMember $thumbwhere_producermember) {

  $thumbwhere_producermember->delete();
}


/**
 * Delete multiple thumbwhere_producermembers.
 *
 * @param $thumbwhere_producermember_ids
 *   An array of thumbwhere_producermember IDs.
 */
/*
function thumbwhere_producermember_delete_multiple(array $thumbwhere_producermember_ids) {



  entity_get_controller('thumbwhere_producermember')->delete($thumbwhere_producermember_ids);
}
*/

/**
 * Create a thumbwhere_producermember object.
 */
function thumbwhere_producermember_create($values = array()) {

  return entity_get_controller('thumbwhere_producermember')->create($values);
}


/**
 * Saves a thumbwhere_producermember to the database.
 *
 * @param $thumbwhere_producermember
 *   The thumbwhere_producermember object.
 */
/*
function thumbwhere_producermember_save(ThumbWhereProducerMember $thumbwhere_producermember) {



  return $thumbwhere_producermember->save();
}*/



/**
 * Insert a thumbwhere_producermember
 *
 * @param $thumbwhere_producermember
 *   The thumbwhere_producermember object.
 */
/*
function thumbwhere_producermember_insert(array $values = array()) {



  //return $thumbwhere_producermember->create($values);
  return entity_get_controller('thumbwhere_producermember')->insert($values);
}
*/






/**
 * URI callback for thumbwhere_producermembers
 */
function thumbwhere_producermember_uri(ThumbWhereProducerMember $thumbwhere_producermember) {



  return array(
    'path' => 'thumbwhere_producermember/' . $thumbwhere_producermember->pk_producermember,
  );
}


/**
 * Menu title callback for showing individual entities
 */
function thumbwhere_producermember_page_title(ThumbWhereProducerMember $thumbwhere_producermember) {

  

  return $thumbwhere_producermember->pk_producermember;
}


/**
 * Sets up content to show an individual thumbwhere_producermember
 * @todo - get rid of drupal_set_title();
 */
function thumbwhere_producermember_page_view($thumbwhere_producermember, $view_mode = 'full') {



  $controller = entity_get_controller('thumbwhere_producermember');
  $content = $controller->view(array($thumbwhere_producermember->pk_producermember => $thumbwhere_producermember));
  drupal_set_title($thumbwhere_producermember->pk_producermember);
  return $content;
}


/**
 * Implements hook_views_api().
 */
function thumbwhere_producermember_views_api() {



  return array(
    'api' => 3,
    'path' => drupal_get_path('module', 'thumbwhere_producermember') . '/views',
  );
}


/**
 * Implement hook_theme().
 */
function thumbwhere_producermember_theme() {



  return array(
    'thumbwhere_producermember_add_list' => array(
      'variables' => array('content' => array()),
      'file' => 'thumbwhere_producermember.admin.inc',
    ),
    'thumbwhere_producermember' => array(
      'render element' => 'elements',
      'template' => 'thumbwhere_producermember',
    )
  );
}


/**
 * Implements hook_menu_local_tasks_alter().
 */
function thumbwhere_producermember_menu_local_tasks_alter(&$data, $router_item, $root_path) {

  // Add action link 'admin/structure/thumbwhere_producermembers/add' on 'admin/structure/thumbwhere_producermembers'.
  if ($root_path == 'admin/content/thumbwhere_producermembers') {
    $item = menu_get_item('admin/content/thumbwhere_producermembers/add');
    if ($item['access']) {
      $data['actions']['output'][] = array(
        '#theme' => 'menu_local_action',
        '#link' => $item,
      );
    }
  }
}


/**
 * The class used for thumbwhere_producermember entities
 */
class ThumbWhereProducerMember extends Entity {

  public function __construct($values = array()) {
    parent::__construct($values, 'thumbwhere_producermember');
  }

  protected function defaultLabel() {
    return $this->pk_producermember;
  }

  protected function defaultUri() {
    return array('path' => 'thumbwhere_producermember/' . $this->pk_producermember);
  }


}



/**
 * The Controller for ThumbWhereProducerMember entities
 */
class ThumbWhereProducerMemberController extends EntityAPIController {
  public function __construct($entityType) {
    parent::__construct($entityType);
  }


  /**
   * Create a thumbwhere_producermember - we first set up the values that are specific
   * to our thumbwhere_producermember schema but then also go through the EntityAPIController
   * function.
   *
   * @param $type
   *   The machine-readable type of the thumbwhere_producermember.
   *
   * @return
   *   A thumbwhere_producermember object with all default fields initialized.
   */
  /*
  public function create(array $values = array()) {



    //$values += array(
    //  'thumbwhere_id' => -1,
    //);

    return parent::create($values);
  }
  */


  /**
  * view
  * to our thumbwhere_producermember schema but then also go through the EntityAPIController
  * function.
  *
  * @param $type
  *   The machine-readable type of the thumbwhere_producermember.
  *
  * @return
  *   A thumbwhere_producermember object with all default fields initialized.
  */
  /*
  public function view($entities, $view_mode = 'full', $langcode = NULL, $page = NULL) {



    $view = parent::view($entities, $view_mode, $langcode, $page);

    return $view;
  }
  */




  /**
  * query - Builds and executes the query for loading.
  *
  * @param $ids
  *   Array of ids
  *
  * @param $conditions
  *
  *
  *
  * @return
  *   A thumbwhere_producermember object with all default fields initialized.
  */
  /*
  public function load($ids = array(), $conditions = array()) {



    $result = parent::load($ids, $conditions);

    //$result[1]->token = "my token";

    return $result;
  }
  */

  /**
  * Invoke
  *
  * @param $ids
  *   Array of ids
  *
  * @param $transaction
  *   If not null then the transaction this is taking part in
  *
  * @return
  *   A thumbwhere_producermember object with all default fields initialized.
  */
  /*
  public function invoke($hook, $entity) {



    parent::invoke($hook, $entity);

  }
  */


  /**
  * Delete a thumbwhere_producermember.
  *
  * @param $ids
  *   Array of ids
  *
  * @param $transaction
  *   If not null then the transaction this is taking part in
  *
  * @return
  *   A thumbwhere_producermember object with all default fields initialized.
  */
  /*
  public function delete($ids, DatabaseTransaction $transaction = NULL) {



    parent::delete($ids, $transaction);

  }
  */


  /**
  * Save a thumbwhere_producermember.
  *
  * @param $thumbwhere_producermember
  *   The thumbwhere_producermember being saved
  *
  * @param $transaction
  *   If not null then the transaction this is taking part in
  *
  * @return
  *   A thumbwhere_producermember object with all default fields initialized.
  */
  /*
  public function save($thumbwhere_producermember, DatabaseTransaction $transaction = NULL) {



    return parent::save($thumbwhere_producermember, $transaction);
  }
  */


  /**
  * resetCache - Called when the cache is
  *
  * @param $ids
  *   Array of ids
  *
  * @return
  *   void
  */
  /*
  public function resetCache(array $ids = NULL) {



    parent::resetCache($ids);
  }
  */


  /**
  * query - Builds and executes the query for loading.
  *
  * @param $ids
  *   Array of ids
  *
  * @param $conditions
  *
  * @param $revision_id
  *
  *
  * @return
  *   A thumbwhere_producermember object with all default fields initialized.
  */
  /*
  public function query($ids, $conditions, $revision_id = FALSE) {



    $result= parent::query($ids, $conditions, $revision_id);

    return $result;
  }
  */


  /**
   * Overriding the buldProducerMember function to add entity specific fields
   */
  /*
  public function buildProducerMember($entity, $view_mode = 'full', $langcode = NULL, $content = array()) {

    $content = parent::buildProducerMember($entity, $view_mode, $langcode, $content);

    $content['thumbwhere_producermember_sample_data'] =  array(
      '#markup' => theme('thumbwhere_producermember_sample_data', array('thumbwhere_producermember_sample_data' => check_plain($entity->data['sample_data']), 'thumbwhere_producermember' => $entity)),
    );

    return $content;
  }*/
}


/**
 * Implements hook_field_storage_load().
 *
 * Load field from storage
 *
 * @todo This needs to be improved
 */
/*
function thumbwhere_producermember_field_storage_load($entity_type, $entities, $age, $fields, $options) {


}
*/



/**
* Implements hook_entity_presave().
*
* Act on an ProducerMember entity before it is about to be created or updated.
*
* @param $entity
*   The entity object.
* @param $entity_type
*   The type of entity being saved (i.e. node, user, comment).
*/
function thumbwhere_producermember_entity_presave($entity, $entity_type) {

  // We only look after what we want...
  if ($entity_type == 'thumbwhere_producermember')  {

    // If this is new
    if ($entity->is_new) {
  
      // .. then we create a new thumbwhere_producermember entity...
      // Instantiate the ThumbWhereContent class

      $twContentAPI = new ThumbWhereAPIContent();

      //if (twCanDebug()) debug($twContentAPI);

      //if (twCanDebug()) debug($entity);

      //
      // Collate and validate our fields
      //

      $fields = array();
      // Compulsory so..
      if (empty($entity->fk_producer)) {
        throw new Exception('producer is compulsory.');
      }
      else {
        $fields['producer'] = $entity->fk_producer;
      }
      // Compulsory so..
      if (empty($entity->fk_member)) {
        throw new Exception('member is compulsory.');
      }
      else {
        $fields['member'] = $entity->fk_member;
      }

      // Not nullable but we have a default value
      if (empty($entity->admin)) {
        $fields['admin'] = $entity->admin = 0;
      }
      else {
        $fields['admin'] = $entity->admin;
      }

      //
      // Now create the Entity in ThumbWhere
      //

      $producermember = $twContentAPI->create_producer_member($context = array(
                  'key' => variable_get('thumbwhere_api_key')
          ), $fields);
            
       if (twCanDebug()) debug($producermember);

      //if (twCanDebug()) debug($producer_member);

      $attributes = $producermember->body->producer_member->attributes();

      if (twCanDebug()) debug('newly created id = ' . $attributes['id']);

      // Assign new id.
      $entity->pk_producermember = intval($attributes['id']);

      if (twCanDebug()) debug('$entity->pk_producermember = ' . $entity->pk_producermember);
    }
    else
    {
        // .. otherwise we calculate the changes and ship them off to thumbwhere
    }
  }
}

/**
 * Implements thumbwhere_producermember_entity_insert().
 *
 * Act on entities when inserted.
 *
 * @param $entity
 *   The entity object
 * @param $type
 *   The type of entity being inserted (i.e. node, user, comment).
 */
function thumbwhere_producermember_entity_insert($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_producermember') return;

}


/**
* Implements thumbwhere_producermember_entity_delete().
*
* Act on entities when inserted.
*
* @param $entity
*   The entity object
* @param $type
*   The type of entity being deleted (i.e. node, user, comment).
*/
function thumbwhere_producermember_entity_delete($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_producermember') return;


}
