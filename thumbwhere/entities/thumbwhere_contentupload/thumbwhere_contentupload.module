<?php

// ******************************************************************
// This file was generated by autogen_model_implementation_drupal.xsl
// ******************************************************************

// Include the ThumbWhere SDK
require_once(dirname(__FILE__) . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . 'dependencies' . DIRECTORY_SEPARATOR . 'tw' . DIRECTORY_SEPARATOR . 'sdk.class.php');

/**
 * @file
 * Module for the ThumbWhere ContentUpload Entity
 *
 *
 * NOTE: This has been autogenerated.
 * 
 */

/**
 * Implement hook_entity_info().
 *
 * We define two entities here - the actual entity that will hold our domain
 * specific information and an entity that holds information about the different
 * types of entities. See here: http://drupal.org/node/977380 for a discussion on this
 * choice.
 */
function thumbwhere_contentupload_entity_info() {
  $return['thumbwhere_contentupload'] = array(

    // The human-readable name of the type.
    'label' => t('ThumbWhereContentUpload'),

    // (used by DrupalDefaultEntityController) The name of the entity type's base table.
    'base table' => 'thumbwhere_contentupload',

    // fieldable: Set to TRUE if you want your entity type to accept fields being attached to it.
    'fieldable' => TRUE,

  // Declare the endty keys..
    'entity keys' => array(
      'id' => 'pk_contentupload',
      'label' => 'pk_contentupload',
    ),

    // A function taking an entity and an entity type as arguments and returning the label of the entity.
    // The entity label is the main string associated with an entity; for example, the title of a node or
    // the subject of a comment. If there is an entity object property that defines the label, use the
    // 'label' element of the 'entity keys' return value component to provide this information (see below).
    // If more complex logic is needed to determine the label of an entity, you can instead specify a callback
    // function here, which will be called to determine the entity label. See also the entity_label()
    // function, which implements this logic.
    'label callback' => 'entity_class_label',

    // A function taking an entity as argument and returning the uri elements of the entity, e.g. 'path' and 'options'.
    // The actual entity uri can be constructed by passing these elements to url().
    'uri callback' => 'entity_class_uri',

    //
    // Entity API Components
    //

    // The module whwre we can find our classes
    'module' => 'thumbwhere_contentupload',

    // The entity class and controller class extend the classes provided by the Entity API
    'entity class' => 'ThumbWhereContentUpload',

    // Which Entity API contoller
    'controller class' => 'ThumbWhereContentUploadController',

    // The information below is used by the ThumbWhereContentUploadUIController (which extends the EntityDefaultUIController)
    'admin ui' => array(
      'path' => 'admin/thumbwhere/thumbwhere_contentuploads',
      'file' => 'thumbwhere_contentupload.admin.inc',
      'controller class' => 'ThumbWhereContentUploadUIController',
      'menu wildcard' => '%thumbwhere_contentupload',
    ),
  );

  return $return;
}


/**
 * Implements hook_permission().
 */
function thumbwhere_contentupload_permission() {

  // We set up permisssions to manage entity types, manage all entities and the
  // permissions for each individual entity
  $permissions = array(
    'administer thumbwhere_contentuploads' => array(
      'title' => t('Administer thumbwhere_contentuploads'),
      'description' => t('Edit and delete all thumbwhere_contentuploads'),
    ),
  'send thumbwhere contentupload notifications' => array(
          'title' => t('Send ThumbWhere Notifications'),
          'description' => t('Allow the caller to call the ThumbWhere ContentUpload Entity Notifications Endpoint.'),
          'restrict access' => TRUE,
      ),    
  );

  return $permissions;
}


/**
 * Determines whether the given user has access to a thumbwhere_contentupload.
 *
 * @param $op
 *   The operation being performed. One of 'view', 'update', 'create', 'delete'
 *   or just 'edit' (being the same as 'create' or 'update').
 * @param $thumbwhere_contentupload
 *   Optionally a thumbwhere_contentupload or a thumbwhere_contentupload type to check access for. If nothing is
 *   given, access for all thumbwhere_contentuploads is determined.
 * @param $account
 *   The user to check for. Leave it to NULL to check for the global user.
 * @return boolean
 *   Whether access is allowed or not.
 */
function thumbwhere_contentupload_access($op, $thumbwhere_contentupload = NULL, $account = NULL) {

  //if (user_access('administer thumbwhere_contentuploads', $account)) {
  //  return TRUE;
 // }

  //return FALSE;
  return TRUE;
}




/**
 * Fetch a thumbwhere_contentupload object. Make sure that the wildcard you choose
 * in the thumbwhere_contentupload entity definition fits the function name here.
 *
 * @param $thumbwhere_contentupload_id
 *   Integer specifying the thumbwhere_contentupload id.
 * @param $reset
 *   A boolean indicating that the internal cache should be reset.
 * @return
 *   A fully-loaded $thumbwhere_contentupload object or FALSE if it cannot be loaded.
 *
 * @see thumbwhere_contentupload_load_multiple()
 */
function thumbwhere_contentupload_load($thumbwhere_contentupload_id, $reset = FALSE) {

  $thumbwhere_contentuploads = entity_load('thumbwhere_contentupload', array($thumbwhere_contentupload_id), array(), $reset);

  return reset($thumbwhere_contentuploads);
}


/**
 * Deletes a thumbwhere_contentupload.
 */
function thumbwhere_contentupload_delete(ThumbWhereContentUpload $thumbwhere_contentupload) {

  $thumbwhere_contentupload->delete();
}

/**
 * Create a thumbwhere_contentupload object.
 */
function thumbwhere_contentupload_create($values = array()) {

  return entity_get_controller('thumbwhere_contentupload')->create($values);
}


/**
 * URI callback for thumbwhere_contentuploads
 */
function thumbwhere_contentupload_uri(ThumbWhereContentUpload $thumbwhere_contentupload) {

  return array(
    'path' => 'thumbwhere_contentupload/' . $thumbwhere_contentupload->pk_contentupload,
  );
}


/**
 * Menu title callback for showing individual entities
 */
function thumbwhere_contentupload_page_title(ThumbWhereContentUpload $thumbwhere_contentupload) {

	return $thumbwhere_contentupload->pk_contentupload;
}


/**
 * Sets up content to show an individual thumbwhere_contentupload
 * @todo - get rid of drupal_set_title();
 */
function thumbwhere_contentupload_page_view($thumbwhere_contentupload, $view_mode = 'full') {



  $controller = entity_get_controller('thumbwhere_contentupload');
  $content = $controller->view(array($thumbwhere_contentupload->pk_contentupload => $thumbwhere_contentupload));
  drupal_set_title($thumbwhere_contentupload->pk_contentupload);
  return $content;
}


/**
 * Implements hook_views_api().
 */
function thumbwhere_contentupload_views_api() {



  return array(
    'api' => 3,
    'path' => drupal_get_path('module', 'thumbwhere_contentupload') . '/views',
  );
}


/**
 * Implement hook_theme().
 */
function thumbwhere_contentupload_theme() {



  return array(
    'thumbwhere_contentupload_add_list' => array(
      'variables' => array('content' => array()),
      'file' => 'thumbwhere_contentupload.admin.inc',
    ),
    'thumbwhere_contentupload' => array(
      'render element' => 'elements',
      'template' => 'thumbwhere_contentupload',
    )
  );
}


/**
 * Implements hook_menu_local_tasks_alter().
 */
function thumbwhere_contentupload_menu_local_tasks_alter(&$data, $router_item, $root_path) {

  // Add action link 'admin/structure/thumbwhere_contentuploads/add' on 'admin/structure/thumbwhere_contentuploads'.
  if ($root_path == 'admin/thumbwhere/thumbwhere_contentuploads') {
    $item = menu_get_item('admin/thumbwhere/thumbwhere_contentuploads/add');
    if ($item['access']) {
      $data['actions']['output'][] = array(
        '#theme' => 'menu_local_action',
        '#link' => $item,
      );
    }
  }
}


/**
 * The class used for thumbwhere_contentupload entities
 */
class ThumbWhereContentUpload extends Entity {

  public function __construct($values = array()) {
    parent::__construct($values, 'thumbwhere_contentupload');
  }

  protected function defaultLabel() {
    return $this->pk_contentupload;
  }

  protected function defaultUri() {
    return array('path' => 'thumbwhere_contentupload/' . $this->pk_contentupload);
  }


}



/**
 * The Controller for ThumbWhereContentUpload entities
 */
class ThumbWhereContentUploadController extends EntityAPIController {
  public function __construct($entityType) {
    parent::__construct($entityType);
  }
}



/**
* Implements hook_entity_presave().
*
* Act on an ContentUpload entity before it is about to be created or updated.
*
* @param $entity
*   The entity object.
* @param $entity_type
*   The type of entity being saved (i.e. node, user, comment).
*/
function thumbwhere_contentupload_entity_presave($entity, $entity_type) {

  // We only look after what we want...
  if ($entity_type == 'thumbwhere_contentupload')  {
  	
  	
  	
 	/////////////////////////////////////////////////////
    //
    // Should we not propagate this to ThumbWhere? 
    //
    if (isset($entity->do_not_propagate)) {
      if ($entity->do_not_propagate) {
        if (twCanDebug()) {
          if ($entity->is_new) {
            if (twCanDebug()){
              debug("DO NOT PROPAGATE 'contentupload' presave *CREATE*");
          	}
          }
          else {
            if (twCanDebug()) {
              debug("DO NOT PROPAGATE 'contentupload' presave *UPDATE*");
            }
          }
        }

        // To cause an infinite loop of updates, comment out this line.
        return;
      }
      else if (twCanDebug()) {
        debug("OK PROAOGATE 'contentupload' presave to ThumbWhere Server");
      }
    }
    else if (twCanDebug()) {
      debug("DO NOT PROPAGATE IS NOT SET");
    }
    //
    //
    ////////////////////////////////////////////////////////  	

  
    // .. then we create a new thumbwhere_contentupload entity...
      // Instantiate the ThumbWhereContent class

    $twContentAPI = new ThumbWhereAPIContent();

    //if (twCanDebug()) debug($twContentAPI);

    //if (twCanDebug()) debug($entity);

    //
    // Collate and validate our fields
    //

    $fields = array();
    // Compulsory so..
    if (!isset($entity->mediaitemuploadpush)) {
      throw new Exception('mediaitemuploadpush is mandatory.');
    }
    else {
      $fields['mediaitemuploadpush'] = $entity->mediaitemuploadpush;
    }

    // Nullable so we only pass it through to create_content_upload if we have it..
    if (isset($entity->fk_mediaitem)) {
      $fields['mediaitem'] = $entity->fk_mediaitem;
    }

    // Nullable so we only pass it through to create_content_upload if we have it..
    if (isset($entity->fk_content)) {
      $fields['content'] = $entity->fk_content;
    }

    // Nullable so we only pass it through to create_content_upload if we have it..
    if (isset($entity->fk_member)) {
      $fields['member'] = $entity->fk_member;
    }

    // Nullable so we only pass it through to create_content_upload if we have it..
    if (isset($entity->fk_identity)) {
      $fields['identity'] = $entity->fk_identity;
    }

    // Nullable so we only pass it through to create_content_upload if we have it..
    if (isset($entity->fk_producer)) {
      $fields['producer'] = $entity->fk_producer;
    }

    // Nullable so we only pass it through to create_content_upload if we have it..
    if (isset($entity->fk_consumer)) {
      $fields['consumer'] = $entity->fk_consumer;
    }

    // Nullable so we only pass it through to create_content_upload if we have it..
    if (isset($entity->size)) {
      $fields['size'] = $entity->size;
    }

    // Not nullable but we have a default value
    if (!isset($entity->uploaded)) {
      $fields['uploaded'] = 0;
    }
    else {
      $fields['uploaded'] = $entity->uploaded;
    }

    // Not nullable but we have a default value
    if (!isset($entity->transcoded)) {
      $fields['transcoded'] = 0;
    }
    else {
      $fields['transcoded'] = $entity->transcoded;
    }

    // Not nullable but we have a default value
    if (!isset($entity->encrypted)) {
      $fields['encrypted'] = 0;
    }
    else {
      $fields['encrypted'] = $entity->encrypted;
    }

    // Not nullable but we have a default value
    if (!isset($entity->deployed)) {
      $fields['deployed'] = 0;
    }
    else {
      $fields['deployed'] = $entity->deployed;
    }

    // Not nullable but we have a default value
    if (!isset($entity->ready)) {
      $fields['ready'] = 0;
    }
    else {
      $fields['ready'] = $entity->ready;
    }

    // Not nullable but we have a default value
    if (!isset($entity->error)) {
      $fields['error'] = 0;
    }
    else {
      $fields['error'] = $entity->error;
    }

    // Nullable so we only pass it through to create_content_upload if we have it..
    if (!empty($entity->status)) {
      $fields['status'] = $entity->status;
    }
		
	//
	// Ensure that we have the 'thumbwhere_api_key' as we are going to pass that through in a context
	//
    if (variable_get('thumbwhere_api_key') == '') {
    	throw new Exception('ThumbWhere resource for \'ContentUpload\' requires that variable \'thumbwhere_api_key\' is set.');
    }
    
              		
    //
    // If this contentupload entity is new....
    //
               
    if ($entity->is_new) {
                

      //
      // ... then we create the contentupload entity in ThumbWhere
      //

      $contentupload = $twContentAPI->create_content_upload($context = array(
                  'key' => variable_get('thumbwhere_api_key')
          ), $fields);
            
       if (twCanDebug()) debug($contentupload);

      //if (twCanDebug()) debug($content_upload);

      $attributes = $contentupload->body->content_upload->attributes();

      if (twCanDebug()) debug('newly created id = ' . $attributes['id']);

      // Assign new id.
      $entity->pk_contentupload = intval($attributes['id']);

      if (twCanDebug()) debug('$entity->pk_contentupload = ' . $entity->pk_contentupload);
    }
    else
    {
          	

      //
      // .. otherwise we update the contentupload entity in ThumbWhere
      //

      $contentupload = $twContentAPI->update_content_upload($id=$entity->pk_contentupload,$context = array(
                  'key' => variable_get('thumbwhere_api_key')
          ), $fields);
            
        
    }
          	
    
  }
}

/**
 * Implements thumbwhere_contentupload_entity_insert().
 *
 * Act on entities when inserted.
 *
 * @param $entity
 *   The entity object
 * @param $type
 *   The type of entity being inserted (i.e. node, user, comment).
 */
function thumbwhere_contentupload_entity_insert($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_contentupload') return;

}


/**
* Implements thumbwhere_contentupload_entity_delete().
*
* Act on entities when inserted.
*
* @param $entity
*   The entity object
* @param $type
*   The type of entity being deleted (i.e. node, user, comment).
*/
function thumbwhere_contentupload_entity_delete($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_contentupload') return;


}
