<?php

/**
 * @file
 * Tests for thumbwhere_member_field_storage.module.
 *
 * Field_sql_storage.module implements the default back-end storage plugin
 * for the Field Strage API.
 *
 * NOTE: This has been autogenerated. 
 *
 */

/**
 * Tests field storage.
 */
class ThumbWhereMemberUnitTestCase extends DrupalUnitTestCase {
  public static function getInfo() {
    return array(
      'name'  => 'Member Entity Unit Tests',
      'description'  => "Test ThumbWhere Member Entity module.",
      'group' => 'ThumbWhereUnit'
    );
  }


  function setUp() {

    // Make sure we are using the testing profile
    $this->profile = 'testing';

    drupal_load('module', 'thumbwhere_member');
    parent::setUp();
  }


  /**
   * Test that the field storage works.
   */
  function testSomething() {

    $result = TRUE;
    $message = t("Test Passed.");
    $this->assertTrue($result, $message);

    }

}

