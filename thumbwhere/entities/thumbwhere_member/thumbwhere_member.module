<?php

// ******************************************************************
// This file was generated by autogen_model_implementation_drupal.xsl
// ******************************************************************

// Include the ThumbWhere SDK
require_once(dirname(__FILE__) . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . 'dependencies' . DIRECTORY_SEPARATOR . 'tw' . DIRECTORY_SEPARATOR . 'sdk.class.php');

/**
 * @file
 * Module for the ThumbWhere Member Entity
 *
 *
 * NOTE: This has been autogenerated.
 * 
 */

/**
 * Implement hook_entity_info().
 *
 * We define two entities here - the actual entity that will hold our domain
 * specific information and an entity that holds information about the different
 * types of entities. See here: http://drupal.org/node/977380 for a discussion on this
 * choice.
 */
function thumbwhere_member_entity_info() {
  $return['thumbwhere_member'] = array(

    // The human-readable name of the type.
    'label' => t('ThumbWhereMember'),

    // (used by DrupalDefaultEntityController) The name of the entity type's base table.
    'base table' => 'thumbwhere_member',

    // fieldable: Set to TRUE if you want your entity type to accept fields being attached to it.
    'fieldable' => TRUE,

  // Declare the endty keys..
    'entity keys' => array(
      'id' => 'pk_member',
      'label' => 'pk_member',
    ),

    // A function taking an entity and an entity type as arguments and returning the label of the entity.
    // The entity label is the main string associated with an entity; for example, the title of a node or
    // the subject of a comment. If there is an entity object property that defines the label, use the
    // 'label' element of the 'entity keys' return value component to provide this information (see below).
    // If more complex logic is needed to determine the label of an entity, you can instead specify a callback
    // function here, which will be called to determine the entity label. See also the entity_label()
    // function, which implements this logic.
    'label callback' => 'entity_class_label',

    // A function taking an entity as argument and returning the uri elements of the entity, e.g. 'path' and 'options'.
    // The actual entity uri can be constructed by passing these elements to url().
    'uri callback' => 'entity_class_uri',

    //
    // Entity API Components
    //

    // The module whwre we can find our classes
    'module' => 'thumbwhere_member',

    // The entity class and controller class extend the classes provided by the Entity API
    'entity class' => 'ThumbWhereMember',

    // Which Entity API contoller
    'controller class' => 'ThumbWhereMemberController',

    // The information below is used by the ThumbWhereMemberUIController (which extends the EntityDefaultUIController)
    'admin ui' => array(
      'path' => 'admin/content/thumbwhere_members',
      'file' => 'thumbwhere_member.admin.inc',
      'controller class' => 'ThumbWhereMemberUIController',
      'menu wildcard' => '%thumbwhere_member',
    ),
  );

  return $return;
}


/**
 * Implements hook_permission().
 */
function thumbwhere_member_permission() {

  // We set up permisssions to manage entity types, manage all entities and the
  // permissions for each individual entity
  $permissions = array(
    'administer thumbwhere_members' => array(
      'title' => t('Administer thumbwhere_members'),
      'description' => t('Edit and delete all thumbwhere_members'),
    ),
  'send thumbwhere member notifications' => array(
          'title' => t('Send ThumbWhere Notifications'),
          'description' => t('Allow the caller to call the ThumbWhere Member Entity Notifications Endpoint.'),
          'restrict access' => TRUE,
      ),    
  );

  return $permissions;
}


/**
 * Determines whether the given user has access to a thumbwhere_member.
 *
 * @param $op
 *   The operation being performed. One of 'view', 'update', 'create', 'delete'
 *   or just 'edit' (being the same as 'create' or 'update').
 * @param $thumbwhere_member
 *   Optionally a thumbwhere_member or a thumbwhere_member type to check access for. If nothing is
 *   given, access for all thumbwhere_members is determined.
 * @param $account
 *   The user to check for. Leave it to NULL to check for the global user.
 * @return boolean
 *   Whether access is allowed or not.
 */
function thumbwhere_member_access($op, $thumbwhere_member = NULL, $account = NULL) {

  //if (user_access('administer thumbwhere_members', $account)) {
  //  return TRUE;
 // }

  //return FALSE;
  return TRUE;
}




/**
 * Fetch a thumbwhere_member object. Make sure that the wildcard you choose
 * in the thumbwhere_member entity definition fits the function name here.
 *
 * @param $thumbwhere_member_id
 *   Integer specifying the thumbwhere_member id.
 * @param $reset
 *   A boolean indicating that the internal cache should be reset.
 * @return
 *   A fully-loaded $thumbwhere_member object or FALSE if it cannot be loaded.
 *
 * @see thumbwhere_member_load_multiple()
 */
function thumbwhere_member_load($thumbwhere_member_id, $reset = FALSE) {

  //

  $thumbwhere_members = entity_load('thumbwhere_member', array($thumbwhere_member_id), array(), $reset);

  return reset($thumbwhere_members);
}





/**
 * Load multiple thumbwhere_members based on certain conditions.
 *
 * @param $thumbwhere_member_ids
 *   An array of thumbwhere_member IDs.
 * @param $conditions
 *   An array of conditions to match against the {thumbwhere_member} table.
 * @param $reset
 *   A boolean indicating that the internal cache should be reset.
 * @return
 *   An array of thumbwhere_member objects, indexed by thumbwhere_member_id.
 *
 * @see entity_load()
 * @see thumbwhere_member_load()
 */
/*
function thumbwhere_member_load_multiple($thumbwhere_member_ids = array(), $conditions = array(), $reset = FALSE) {



  $loaded = entity_load('thumbwhere_member', $thumbwhere_member_ids, $conditions, $reset);

  return $loaded;
}
*/

/**
 * Deletes a thumbwhere_member.
 */
function thumbwhere_member_delete(ThumbWhereMember $thumbwhere_member) {

  $thumbwhere_member->delete();
}


/**
 * Delete multiple thumbwhere_members.
 *
 * @param $thumbwhere_member_ids
 *   An array of thumbwhere_member IDs.
 */
/*
function thumbwhere_member_delete_multiple(array $thumbwhere_member_ids) {



  entity_get_controller('thumbwhere_member')->delete($thumbwhere_member_ids);
}
*/

/**
 * Create a thumbwhere_member object.
 */
function thumbwhere_member_create($values = array()) {

  return entity_get_controller('thumbwhere_member')->create($values);
}


/**
 * Saves a thumbwhere_member to the database.
 *
 * @param $thumbwhere_member
 *   The thumbwhere_member object.
 */
/*
function thumbwhere_member_save(ThumbWhereMember $thumbwhere_member) {



  return $thumbwhere_member->save();
}*/



/**
 * Insert a thumbwhere_member
 *
 * @param $thumbwhere_member
 *   The thumbwhere_member object.
 */
/*
function thumbwhere_member_insert(array $values = array()) {



  //return $thumbwhere_member->create($values);
  return entity_get_controller('thumbwhere_member')->insert($values);
}
*/






/**
 * URI callback for thumbwhere_members
 */
function thumbwhere_member_uri(ThumbWhereMember $thumbwhere_member) {



  return array(
    'path' => 'thumbwhere_member/' . $thumbwhere_member->pk_member,
  );
}


/**
 * Menu title callback for showing individual entities
 */
function thumbwhere_member_page_title(ThumbWhereMember $thumbwhere_member) {

  

  return $thumbwhere_member->pk_member;
}


/**
 * Sets up content to show an individual thumbwhere_member
 * @todo - get rid of drupal_set_title();
 */
function thumbwhere_member_page_view($thumbwhere_member, $view_mode = 'full') {



  $controller = entity_get_controller('thumbwhere_member');
  $content = $controller->view(array($thumbwhere_member->pk_member => $thumbwhere_member));
  drupal_set_title($thumbwhere_member->pk_member);
  return $content;
}


/**
 * Implements hook_views_api().
 */
function thumbwhere_member_views_api() {



  return array(
    'api' => 3,
    'path' => drupal_get_path('module', 'thumbwhere_member') . '/views',
  );
}


/**
 * Implement hook_theme().
 */
function thumbwhere_member_theme() {



  return array(
    'thumbwhere_member_add_list' => array(
      'variables' => array('content' => array()),
      'file' => 'thumbwhere_member.admin.inc',
    ),
    'thumbwhere_member' => array(
      'render element' => 'elements',
      'template' => 'thumbwhere_member',
    )
  );
}


/**
 * Implements hook_menu_local_tasks_alter().
 */
function thumbwhere_member_menu_local_tasks_alter(&$data, $router_item, $root_path) {

  // Add action link 'admin/structure/thumbwhere_members/add' on 'admin/structure/thumbwhere_members'.
  if ($root_path == 'admin/content/thumbwhere_members') {
    $item = menu_get_item('admin/content/thumbwhere_members/add');
    if ($item['access']) {
      $data['actions']['output'][] = array(
        '#theme' => 'menu_local_action',
        '#link' => $item,
      );
    }
  }
}


/**
 * The class used for thumbwhere_member entities
 */
class ThumbWhereMember extends Entity {

  public function __construct($values = array()) {
    parent::__construct($values, 'thumbwhere_member');
  }

  protected function defaultLabel() {
    return $this->pk_member;
  }

  protected function defaultUri() {
    return array('path' => 'thumbwhere_member/' . $this->pk_member);
  }


}



/**
 * The Controller for ThumbWhereMember entities
 */
class ThumbWhereMemberController extends EntityAPIController {
  public function __construct($entityType) {
    parent::__construct($entityType);
  }


  /**
   * Create a thumbwhere_member - we first set up the values that are specific
   * to our thumbwhere_member schema but then also go through the EntityAPIController
   * function.
   *
   * @param $type
   *   The machine-readable type of the thumbwhere_member.
   *
   * @return
   *   A thumbwhere_member object with all default fields initialized.
   */
  /*
  public function create(array $values = array()) {



    //$values += array(
    //  'thumbwhere_id' => -1,
    //);

    return parent::create($values);
  }
  */


  /**
  * view
  * to our thumbwhere_member schema but then also go through the EntityAPIController
  * function.
  *
  * @param $type
  *   The machine-readable type of the thumbwhere_member.
  *
  * @return
  *   A thumbwhere_member object with all default fields initialized.
  */
  /*
  public function view($entities, $view_mode = 'full', $langcode = NULL, $page = NULL) {



    $view = parent::view($entities, $view_mode, $langcode, $page);

    return $view;
  }
  */




  /**
  * query - Builds and executes the query for loading.
  *
  * @param $ids
  *   Array of ids
  *
  * @param $conditions
  *
  *
  *
  * @return
  *   A thumbwhere_member object with all default fields initialized.
  */
  /*
  public function load($ids = array(), $conditions = array()) {



    $result = parent::load($ids, $conditions);

    //$result[1]->token = "my token";

    return $result;
  }
  */

  /**
  * Invoke
  *
  * @param $ids
  *   Array of ids
  *
  * @param $transaction
  *   If not null then the transaction this is taking part in
  *
  * @return
  *   A thumbwhere_member object with all default fields initialized.
  */
  /*
  public function invoke($hook, $entity) {



    parent::invoke($hook, $entity);

  }
  */


  /**
  * Delete a thumbwhere_member.
  *
  * @param $ids
  *   Array of ids
  *
  * @param $transaction
  *   If not null then the transaction this is taking part in
  *
  * @return
  *   A thumbwhere_member object with all default fields initialized.
  */
  /*
  public function delete($ids, DatabaseTransaction $transaction = NULL) {



    parent::delete($ids, $transaction);

  }
  */


  /**
  * Save a thumbwhere_member.
  *
  * @param $thumbwhere_member
  *   The thumbwhere_member being saved
  *
  * @param $transaction
  *   If not null then the transaction this is taking part in
  *
  * @return
  *   A thumbwhere_member object with all default fields initialized.
  */
  /*
  public function save($thumbwhere_member, DatabaseTransaction $transaction = NULL) {



    return parent::save($thumbwhere_member, $transaction);
  }
  */


  /**
  * resetCache - Called when the cache is
  *
  * @param $ids
  *   Array of ids
  *
  * @return
  *   void
  */
  /*
  public function resetCache(array $ids = NULL) {



    parent::resetCache($ids);
  }
  */


  /**
  * query - Builds and executes the query for loading.
  *
  * @param $ids
  *   Array of ids
  *
  * @param $conditions
  *
  * @param $revision_id
  *
  *
  * @return
  *   A thumbwhere_member object with all default fields initialized.
  */
  /*
  public function query($ids, $conditions, $revision_id = FALSE) {



    $result= parent::query($ids, $conditions, $revision_id);

    return $result;
  }
  */


  /**
   * Overriding the buldMember function to add entity specific fields
   */
  /*
  public function buildMember($entity, $view_mode = 'full', $langcode = NULL, $content = array()) {

    $content = parent::buildMember($entity, $view_mode, $langcode, $content);

    $content['thumbwhere_member_sample_data'] =  array(
      '#markup' => theme('thumbwhere_member_sample_data', array('thumbwhere_member_sample_data' => check_plain($entity->data['sample_data']), 'thumbwhere_member' => $entity)),
    );

    return $content;
  }*/
}


/**
 * Implements hook_field_storage_load().
 *
 * Load field from storage
 *
 * @todo This needs to be improved
 */
/*
function thumbwhere_member_field_storage_load($entity_type, $entities, $age, $fields, $options) {


}
*/



/**
* Implements hook_entity_presave().
*
* Act on an Member entity before it is about to be created or updated.
*
* @param $entity
*   The entity object.
* @param $entity_type
*   The type of entity being saved (i.e. node, user, comment).
*/
function thumbwhere_member_entity_presave($entity, $entity_type) {

  // We only look after what we want...
  if ($entity_type == 'thumbwhere_member')  {

    // If this is new
    if ($entity->is_new) {
  
      // .. then we create a new thumbwhere_member entity...
      // Instantiate the ThumbWhereContent class

      $twMemberAPI = new ThumbWhereAPIMember();

      //if (twCanDebug()) debug($twContentAPI);

      //if (twCanDebug()) debug($entity);

      //
      // Collate and validate our fields
      //

      $fields = array();

      //
      // Now create the Entity in ThumbWhere
      //

      $member = $twMemberAPI->create_($context = array(
          ), $fields);
            
       if (twCanDebug()) debug($member);

      //if (twCanDebug()) debug($member);

      $attributes = $member->body->->attributes();

      if (twCanDebug()) debug('newly created id = ' . $attributes['id']);

      // Assign new id.
      $entity->pk_member = intval($attributes['id']);

      if (twCanDebug()) debug('$entity->pk_member = ' . $entity->pk_member);
    }
    else
    {
        // .. otherwise we calculate the changes and ship them off to thumbwhere
    }
  }
}

/**
 * Implements thumbwhere_member_entity_insert().
 *
 * Act on entities when inserted.
 *
 * @param $entity
 *   The entity object
 * @param $type
 *   The type of entity being inserted (i.e. node, user, comment).
 */
function thumbwhere_member_entity_insert($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_member') return;

}


/**
* Implements thumbwhere_member_entity_delete().
*
* Act on entities when inserted.
*
* @param $entity
*   The entity object
* @param $type
*   The type of entity being deleted (i.e. node, user, comment).
*/
function thumbwhere_member_entity_delete($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_member') return;


}
