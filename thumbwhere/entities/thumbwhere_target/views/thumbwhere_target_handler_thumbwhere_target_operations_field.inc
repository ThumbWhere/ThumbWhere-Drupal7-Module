<?php

/**
 * @file
 * Provides list of operations that are possible in the thumbwhere_target editing view.
 *
 * NOTE: This file was generated by autogen_model_implementation_drupal.xsl
 *   
 */

/**
 * This field handler aggregates operations that can be done on a thumbwhere_target
 * under a single field providing a more flexible way to present them in a view
 */
class thumbwhere_target_handler_thumbwhere_target_operations_field extends views_handler_field {
  function construct() {
    parent::construct();

    $this->additional_fields['pk_target'] = 'pk_target';
  }

  function query() {
    $this->ensure_my_table();
    $this->add_additional_fields();
  }

  function render($values) {

    $links = menu_contextual_links('thumbwhere_target', 'admin/thumbwhere/thumbwhere_targets/thumbwhere_target', array($this->get_value($values, 'pk_target')));

    if (!empty($links)) {
      return theme('links', array('links' => $links, 'attributes' => array('class' => array('links', 'inline', 'operations'))));
    }
  }
}
