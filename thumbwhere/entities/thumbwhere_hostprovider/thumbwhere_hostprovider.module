<?php

// ******************************************************************
// This file was generated by autogen_model_implementation_drupal.xsl
// ******************************************************************

// Include the ThumbWhere SDK
require_once(dirname(__FILE__) . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . 'dependencies' . DIRECTORY_SEPARATOR . 'tw' . DIRECTORY_SEPARATOR . 'sdk.class.php');

/**
 * @file
 * Module for the ThumbWhere HostProvider Entity
 *
 *
 * NOTE: This has been autogenerated.
 * 
 */

/**
 * Implement hook_entity_info().
 *
 * We define two entities here - the actual entity that will hold our domain
 * specific information and an entity that holds information about the different
 * types of entities. See here: http://drupal.org/node/977380 for a discussion on this
 * choice.
 */
function thumbwhere_hostprovider_entity_info() {
  $return['thumbwhere_hostprovider'] = array(

    // The human-readable name of the type.
    'label' => t('ThumbWhereHostProvider'),

    // (used by DrupalDefaultEntityController) The name of the entity type's base table.
    'base table' => 'thumbwhere_hostprovider',

    // fieldable: Set to TRUE if you want your entity type to accept fields being attached to it.
    'fieldable' => TRUE,

  // Declare the endty keys..
    'entity keys' => array(
      'id' => 'pk_hostprovider',
      'label' => 'name',
    ),

    // A function taking an entity and an entity type as arguments and returning the label of the entity.
    // The entity label is the main string associated with an entity; for example, the title of a node or
    // the subject of a comment. If there is an entity object property that defines the label, use the
    // 'label' element of the 'entity keys' return value component to provide this information (see below).
    // If more complex logic is needed to determine the label of an entity, you can instead specify a callback
    // function here, which will be called to determine the entity label. See also the entity_label()
    // function, which implements this logic.
    'label callback' => 'entity_class_label',

    // A function taking an entity as argument and returning the uri elements of the entity, e.g. 'path' and 'options'.
    // The actual entity uri can be constructed by passing these elements to url().
    'uri callback' => 'entity_class_uri',

    //
    // Entity API Components
    //

    // The module whwre we can find our classes
    'module' => 'thumbwhere_hostprovider',

    // The entity class and controller class extend the classes provided by the Entity API
    'entity class' => 'ThumbWhereHostProvider',

    // Which Entity API contoller
    'controller class' => 'ThumbWhereHostProviderController',

    // The information below is used by the ThumbWhereHostProviderUIController (which extends the EntityDefaultUIController)
    'admin ui' => array(
      'path' => 'admin/thumbwhere/thumbwhere_hostproviders',
      'file' => 'thumbwhere_hostprovider.admin.inc',
      'controller class' => 'ThumbWhereHostProviderUIController',
      'menu wildcard' => '%thumbwhere_hostprovider',
    ),
  );

  return $return;
}


/**
 * Implements hook_permission().
 */
function thumbwhere_hostprovider_permission() {

  // We set up permisssions to manage entity types, manage all entities and the
  // permissions for each individual entity
  $permissions = array(
    'administer thumbwhere_hostproviders' => array(
      'title' => t('Administer thumbwhere_hostproviders'),
      'description' => t('Edit and delete all thumbwhere_hostproviders'),
    ),
  'send thumbwhere hostprovider notifications' => array(
          'title' => t('Send ThumbWhere Notifications'),
          'description' => t('Allow the caller to call the ThumbWhere HostProvider Entity Notifications Endpoint.'),
          'restrict access' => TRUE,
      ),    
  );

  return $permissions;
}


/**
 * Determines whether the given user has access to a thumbwhere_hostprovider.
 *
 * @param $op
 *   The operation being performed. One of 'view', 'update', 'create', 'delete'
 *   or just 'edit' (being the same as 'create' or 'update').
 * @param $thumbwhere_hostprovider
 *   Optionally a thumbwhere_hostprovider or a thumbwhere_hostprovider type to check access for. If nothing is
 *   given, access for all thumbwhere_hostproviders is determined.
 * @param $account
 *   The user to check for. Leave it to NULL to check for the global user.
 * @return boolean
 *   Whether access is allowed or not.
 */
function thumbwhere_hostprovider_access($op, $thumbwhere_hostprovider = NULL, $account = NULL) {

  //if (user_access('administer thumbwhere_hostproviders', $account)) {
  //  return TRUE;
 // }

  //return FALSE;
  return TRUE;
}




/**
 * Fetch a thumbwhere_hostprovider object. Make sure that the wildcard you choose
 * in the thumbwhere_hostprovider entity definition fits the function name here.
 *
 * @param $thumbwhere_hostprovider_id
 *   Integer specifying the thumbwhere_hostprovider id.
 * @param $reset
 *   A boolean indicating that the internal cache should be reset.
 * @return
 *   A fully-loaded $thumbwhere_hostprovider object or FALSE if it cannot be loaded.
 *
 * @see thumbwhere_hostprovider_load_multiple()
 */
function thumbwhere_hostprovider_load($thumbwhere_hostprovider_id, $reset = FALSE) {

  $thumbwhere_hostproviders = entity_load('thumbwhere_hostprovider', array($thumbwhere_hostprovider_id), array(), $reset);

  return reset($thumbwhere_hostproviders);
}


/**
 * Deletes a thumbwhere_hostprovider.
 */
function thumbwhere_hostprovider_delete(ThumbWhereHostProvider $thumbwhere_hostprovider) {

  $thumbwhere_hostprovider->delete();
}

/**
 * Create a thumbwhere_hostprovider object.
 */
function thumbwhere_hostprovider_create($values = array()) {

  return entity_get_controller('thumbwhere_hostprovider')->create($values);
}


/**
 * URI callback for thumbwhere_hostproviders
 */
function thumbwhere_hostprovider_uri(ThumbWhereHostProvider $thumbwhere_hostprovider) {

  return array(
    'path' => 'thumbwhere_hostprovider/' . $thumbwhere_hostprovider->pk_hostprovider,
  );
}


/**
 * Menu title callback for showing individual entities
 */
function thumbwhere_hostprovider_page_title(ThumbWhereHostProvider $thumbwhere_hostprovider) {

	return $thumbwhere_hostprovider->name;
}


/**
 * Sets up content to show an individual thumbwhere_hostprovider
 * @todo - get rid of drupal_set_title();
 */
function thumbwhere_hostprovider_page_view($thumbwhere_hostprovider, $view_mode = 'full') {



  $controller = entity_get_controller('thumbwhere_hostprovider');
  $content = $controller->view(array($thumbwhere_hostprovider->pk_hostprovider => $thumbwhere_hostprovider));
  drupal_set_title($thumbwhere_hostprovider->name);
  return $content;
}


/**
 * Implements hook_views_api().
 */
function thumbwhere_hostprovider_views_api() {



  return array(
    'api' => 3,
    'path' => drupal_get_path('module', 'thumbwhere_hostprovider') . '/views',
  );
}


/**
 * Implement hook_theme().
 */
function thumbwhere_hostprovider_theme() {



  return array(
    'thumbwhere_hostprovider_add_list' => array(
      'variables' => array('content' => array()),
      'file' => 'thumbwhere_hostprovider.admin.inc',
    ),
    'thumbwhere_hostprovider' => array(
      'render element' => 'elements',
      'template' => 'thumbwhere_hostprovider',
    )
  );
}


/**
 * Implements hook_menu_local_tasks_alter().
 */
function thumbwhere_hostprovider_menu_local_tasks_alter(&$data, $router_item, $root_path) {

  // Add action link 'admin/structure/thumbwhere_hostproviders/add' on 'admin/structure/thumbwhere_hostproviders'.
  if ($root_path == 'admin/thumbwhere/thumbwhere_hostproviders') {
    $item = menu_get_item('admin/thumbwhere/thumbwhere_hostproviders/add');
    if ($item['access']) {
      $data['actions']['output'][] = array(
        '#theme' => 'menu_local_action',
        '#link' => $item,
      );
    }
  }
}


/**
 * The class used for thumbwhere_hostprovider entities
 */
class ThumbWhereHostProvider extends Entity {

  public function __construct($values = array()) {
    parent::__construct($values, 'thumbwhere_hostprovider');
  }

  protected function defaultLabel() {
    return $this->name;
  }

  protected function defaultUri() {
    return array('path' => 'thumbwhere_hostprovider/' . $this->pk_hostprovider);
  }


}



/**
 * The Controller for ThumbWhereHostProvider entities
 */
class ThumbWhereHostProviderController extends EntityAPIController {
  public function __construct($entityType) {
    parent::__construct($entityType);
  }
}



/**
* Implements hook_entity_presave().
*
* Act on an HostProvider entity before it is about to be created or updated.
*
* @param $entity
*   The entity object.
* @param $entity_type
*   The type of entity being saved (i.e. node, user, comment).
*/
function thumbwhere_hostprovider_entity_presave($entity, $entity_type) {

  // We only look after what we want...
  if ($entity_type == 'thumbwhere_hostprovider')  {
  	
 	/////////////////////////////////////////////////////
    //
    // Should we not propagate this to ThumbWhere? 
    //
    if (isset($entity->do_not_propagate)) {
      if ($entity->do_not_propagate) {
        if (twCanDebug()) {
          if ($entity->is_new) {
            if (twCanDebug()){
              debug("DO NOT PROPAGATE 'hostprovider' presave *CREATE* not being sent to ThumbWhere server.");
          	}
          }
          else {
            if (twCanDebug()) {
              debug("DO NOT PROPAGATE 'hostprovider' presave *UPDATE* not being sent to ThumbWhere server.");
            }
          }
        }

        // To cause an infinite loop of updates, comment out this line.
        return;
      }
      else if (twCanDebug()) {
        debug("OK PROAOGATE 'hostprovider' presave to ThumbWhere server.");
      }
    }
    else if (twCanDebug()) {
      debug("DO NOT PROPAGATE IS NOT SET");
    }
    //
    //
    ////////////////////////////////////////////////////////  	

  
    // .. then we create a new thumbwhere_hostprovider entity...
      // Instantiate the ThumbWhereContent class

    $twAdminAPI = new ThumbWhereAPIAdmin();

    //if (twCanDebug()) debug($twContentAPI);

    //if (twCanDebug()) debug($entity);

    //
    // Collate and validate our fields
    //

    $fields = array();
    // Compulsory so..
    if (empty($entity->name)) {
      throw new Exception('name is mandatory.');
    }
    else {
      $fields['name'] = $entity->name;
    }
		
	//
	// Ensure that we have the 'thumbwhere_api_key' as we are going to pass that through in a context
	//
    if (variable_get('thumbwhere_api_key') == '') {
    	throw new Exception('ThumbWhere resource for \'HostProvider\' requires that variable \'thumbwhere_api_key\' is set.');
    }
    
              		
    //
    // If this hostprovider entity is new....
    //
               
    if ($entity->is_new) {
                

      //
      // ... then we create the hostprovider entity in ThumbWhere
      //

      $hostprovider = $twAdminAPI->create_host_provider($context = array(
                  'key' => variable_get('thumbwhere_api_key')
          ), $fields);
            
       if (twCanDebug()) debug($hostprovider);

      //if (twCanDebug()) debug($host_provider);

      $attributes = $hostprovider->body->host_provider->attributes();

      if (twCanDebug()) debug('newly created id = ' . $attributes['id']);

      // Assign new id.
      $entity->pk_hostprovider = intval($attributes['id']);

      if (twCanDebug()) debug('$entity->pk_hostprovider = ' . $entity->pk_hostprovider);
    }
    else
    {
          	

      //
      // .. otherwise we update the hostprovider entity in ThumbWhere
      //

      $hostprovider = $twAdminAPI->update_host_provider($id=$entity->pk_hostprovider,$context = array(
                  'key' => variable_get('thumbwhere_api_key')
          ), $fields);
            
        
    }
          	
    
  }
}

/**
 * Implements thumbwhere_hostprovider_entity_insert().
 *
 * Act on entities when inserted.
 *
 * @param $entity
 *   The entity object
 * @param $type
 *   The type of entity being inserted (i.e. node, user, comment).
 */
function thumbwhere_hostprovider_entity_insert($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_hostprovider') return;

}


/**
* Implements thumbwhere_hostprovider_entity_delete().
*
* Act on entities when inserted.
*
* @param $entity
*   The entity object
* @param $type
*   The type of entity being deleted (i.e. node, user, comment).
*/
function thumbwhere_hostprovider_entity_delete($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_hostprovider') return;


}
