<?php

// ******************************************************************
// This file was generated by autogen_model_implementation_drupal.xsl
// ******************************************************************

// Include the ThumbWhere SDK
require_once(dirname(__FILE__) . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . 'dependencies' . DIRECTORY_SEPARATOR . 'tw' . DIRECTORY_SEPARATOR . 'sdk.class.php');

/**
 * @file
 * Module for the ThumbWhere Host Entity
 *
 *
 * NOTE: This has been autogenerated.
 * 
 */

/**
 * Implement hook_entity_info().
 *
 * We define two entities here - the actual entity that will hold our domain
 * specific information and an entity that holds information about the different
 * types of entities. See here: http://drupal.org/node/977380 for a discussion on this
 * choice.
 */
function thumbwhere_host_entity_info() {
  $return['thumbwhere_host'] = array(

    // The human-readable name of the type.
    'label' => t('ThumbWhereHost'),

    // (used by DrupalDefaultEntityController) The name of the entity type's base table.
    'base table' => 'thumbwhere_host',

    // fieldable: Set to TRUE if you want your entity type to accept fields being attached to it.
    'fieldable' => TRUE,

  // Declare the endty keys..
    'entity keys' => array(
      'id' => 'pk_host',
      'label' => 'pk_host',
    ),

    // A function taking an entity and an entity type as arguments and returning the label of the entity.
    // The entity label is the main string associated with an entity; for example, the title of a node or
    // the subject of a comment. If there is an entity object property that defines the label, use the
    // 'label' element of the 'entity keys' return value component to provide this information (see below).
    // If more complex logic is needed to determine the label of an entity, you can instead specify a callback
    // function here, which will be called to determine the entity label. See also the entity_label()
    // function, which implements this logic.
    'label callback' => 'entity_class_label',

    // A function taking an entity as argument and returning the uri elements of the entity, e.g. 'path' and 'options'.
    // The actual entity uri can be constructed by passing these elements to url().
    'uri callback' => 'entity_class_uri',

    //
    // Entity API Components
    //

    // The module whwre we can find our classes
    'module' => 'thumbwhere_host',

    // The entity class and controller class extend the classes provided by the Entity API
    'entity class' => 'ThumbWhereHost',

    // Which Entity API contoller
    'controller class' => 'ThumbWhereHostController',

    // The information below is used by the ThumbWhereHostUIController (which extends the EntityDefaultUIController)
    'admin ui' => array(
      'path' => 'admin/thumbwhere/thumbwhere_hosts',
      'file' => 'thumbwhere_host.admin.inc',
      'controller class' => 'ThumbWhereHostUIController',
      'menu wildcard' => '%thumbwhere_host',
    ),
  );

  return $return;
}


/**
 * Implements hook_permission().
 */
function thumbwhere_host_permission() {

  // We set up permisssions to manage entity types, manage all entities and the
  // permissions for each individual entity
  $permissions = array(
    'administer thumbwhere_hosts' => array(
      'title' => t('Administer thumbwhere_hosts'),
      'description' => t('Edit and delete all thumbwhere_hosts'),
    ),
  'send thumbwhere host notifications' => array(
          'title' => t('Send ThumbWhere Notifications'),
          'description' => t('Allow the caller to call the ThumbWhere Host Entity Notifications Endpoint.'),
          //'restrict access' => TRUE,
      ),    
  );

  return $permissions;
}


/**
 * Determines whether the given user has access to a thumbwhere_host.
 *
 * @param $op
 *   The operation being performed. One of 'view', 'update', 'create', 'delete'
 *   or just 'edit' (being the same as 'create' or 'update').
 * @param $thumbwhere_host
 *   Optionally a thumbwhere_host or a thumbwhere_host type to check access for. If nothing is
 *   given, access for all thumbwhere_hosts is determined.
 * @param $account
 *   The user to check for. Leave it to NULL to check for the global user.
 * @return boolean
 *   Whether access is allowed or not.
 */
function thumbwhere_host_access($op, $thumbwhere_host = NULL, $account = NULL) {

  //if (user_access('administer thumbwhere_hosts', $account)) {
  //  return TRUE;
 // }

  //return FALSE;
  return TRUE;
}




/**
 * Fetch a thumbwhere_host object. Make sure that the wildcard you choose
 * in the thumbwhere_host entity definition fits the function name here.
 *
 * @param $thumbwhere_host_id
 *   Integer specifying the thumbwhere_host id.
 * @param $reset
 *   A boolean indicating that the internal cache should be reset.
 * @return
 *   A fully-loaded $thumbwhere_host object or FALSE if it cannot be loaded.
 *
 * @see thumbwhere_host_load_multiple()
 */
function thumbwhere_host_load($thumbwhere_host_id, $reset = FALSE) {

  $thumbwhere_hosts = entity_load('thumbwhere_host', array($thumbwhere_host_id), array(), $reset);

  return reset($thumbwhere_hosts);
}


/**
 * Deletes a thumbwhere_host.
 */
function thumbwhere_host_delete(ThumbWhereHost $thumbwhere_host) {

  $thumbwhere_host->delete();
}

/**
 * Create a thumbwhere_host object.
 */
function thumbwhere_host_create($values = array()) {

  return entity_get_controller('thumbwhere_host')->create($values);
}


/**
 * URI callback for thumbwhere_hosts
 */
function thumbwhere_host_uri(ThumbWhereHost $thumbwhere_host) {

  return array(
    'path' => 'thumbwhere_host/' . $thumbwhere_host->pk_host,
  );
}


/**
 * Menu title callback for showing individual entities
 */
function thumbwhere_host_page_title(ThumbWhereHost $thumbwhere_host) {

	return $thumbwhere_host->pk_host;
}


/**
 * Sets up content to show an individual thumbwhere_host
 * @todo - get rid of drupal_set_title();
 */
function thumbwhere_host_page_view($thumbwhere_host, $view_mode = 'full') {



  $controller = entity_get_controller('thumbwhere_host');
  $content = $controller->view(array($thumbwhere_host->pk_host => $thumbwhere_host));
  drupal_set_title($thumbwhere_host->pk_host);
  return $content;
}


/**
 * Implements hook_views_api().
 */
function thumbwhere_host_views_api() {



  return array(
    'api' => 3,
    'path' => drupal_get_path('module', 'thumbwhere_host') . '/views',
  );
}


/**
 * Implement hook_theme().
 */
function thumbwhere_host_theme() {



  return array(
    'thumbwhere_host_add_list' => array(
      'variables' => array('content' => array()),
      'file' => 'thumbwhere_host.admin.inc',
    ),
    'thumbwhere_host' => array(
      'render element' => 'elements',
      'template' => 'thumbwhere_host',
    )
  );
}


/**
 * Implements hook_menu_local_tasks_alter().
 */
function thumbwhere_host_menu_local_tasks_alter(&$data, $router_item, $root_path) {

  // Add action link 'admin/structure/thumbwhere_hosts/add' on 'admin/structure/thumbwhere_hosts'.
  if ($root_path == 'admin/thumbwhere/thumbwhere_hosts') {
    $item = menu_get_item('admin/thumbwhere/thumbwhere_hosts/add');
    if ($item['access']) {
      $data['actions']['output'][] = array(
        '#theme' => 'menu_local_action',
        '#link' => $item,
      );
    }
  }
}


/**
 * The class used for thumbwhere_host entities
 */
class ThumbWhereHost extends Entity {

  public function __construct($values = array()) {
    parent::__construct($values, 'thumbwhere_host');
  }

  protected function defaultLabel() {
    return $this->pk_host;
  }

  protected function defaultUri() {
    return array('path' => 'thumbwhere_host/' . $this->pk_host);
  }


}



/**
 * The Controller for ThumbWhereHost entities
 */
class ThumbWhereHostController extends EntityAPIController {
  public function __construct($entityType) {
    parent::__construct($entityType);
  }
}



/**
* Implements hook_entity_presave().
*
* Act on an Host entity before it is about to be created or updated.
*
* @param $entity
*   The entity object.
* @param $entity_type
*   The type of entity being saved (i.e. node, user, comment).
*/
function thumbwhere_host_entity_presave($entity, $entity_type) {

  // We only look after what we want...
  if ($entity_type == 'thumbwhere_host')  {
  	
 	/////////////////////////////////////////////////////
    //
    // Should we not propagate this to ThumbWhere? 
    //
    if (isset($entity->do_not_propagate)) {
      if ($entity->do_not_propagate) {
        if (twCanDebug()) {
          if ($entity->is_new) {
            if (twCanDebug()){
              debug("DO NOT PROPAGATE 'host' presave *CREATE* not being sent to ThumbWhere server.");
          	}
          }
          else {
            if (twCanDebug()) {
              debug("DO NOT PROPAGATE 'host' presave *UPDATE* not being sent to ThumbWhere server.");
            }
          }
        }

        // To cause an infinite loop of updates, comment out this line.
        return;
      }
      else if (twCanDebug()) {
        debug("OK PROAOGATE 'host' presave to ThumbWhere server.");
      }
    }
    else if (twCanDebug()) {
      debug("DO NOT PROPAGATE IS NOT SET");
    }
    //
    //
    ////////////////////////////////////////////////////////  	

  
    // .. then we create a new thumbwhere_host entity...
      // Instantiate the ThumbWhereContent class

    $twAdminAPI = new ThumbWhereAPIAdmin();

    //if (twCanDebug()) debug($twContentAPI);

    //if (twCanDebug()) debug($entity);

    //
    // Collate and validate our fields
    //

    $fields = array();
    // Compulsory so..
    if (!isset($entity->fk_hosttype)) {
      throw new Exception('hosttype is mandatory.');
    }
    else {
      $fields['hosttype'] = $entity->fk_hosttype;
    }
    // Compulsory so..
    if (!isset($entity->fk_hostcredential)) {
      throw new Exception('hostcredential is mandatory.');
    }
    else {
      $fields['hostcredential'] = $entity->fk_hostcredential;
    }
    // Compulsory so..
    if (empty($entity->address)) {
      throw new Exception('address is mandatory.');
    }
    else {
      $fields['address'] = $entity->address;
    }

    // Not nullable but we have a default value
    if (!isset($entity->online)) {
      $fields['online'] = 0;
    }
    else {
      $fields['online'] = $entity->online;
    }
		
	//
	// Ensure that we have the 'thumbwhere_api_key' as we are going to pass that through in a context
	//
  if (variable_get('thumbwhere_api_key') == '') {
    throw new Exception('ThumbWhere resource calls for \'Host\' requires that variable \'thumbwhere_api_key\' is set.');
  }

	//
	// Ensure that we have the 'thumbwhere_api_key' as we are going to pass that through in a context
	//
  if (!is_numeric(variable_get('thumbwhere_host_id'))) {
    throw new Exception('ThumbWhere resource calls for \'Host\' requires that variable \'thumbwhere_host_id\' is set.');
  }


              		
    //
    // If this host entity is new....
    //
               
    if ($entity->is_new) {
                

      //
      // ... then we create the host entity in ThumbWhere
      //

      $host = $twAdminAPI->create_host($context = array(
                  'key' => variable_get('thumbwhere_api_key'),
                  'origin' => variable_get('thumbwhere_host_id'),
          ), $fields);
            
       if (twCanDebug()) debug($host);

      //if (twCanDebug()) debug($host);

      $attributes = $host->body->host->attributes();

      if (twCanDebug()) debug('newly created id = ' . $attributes['id']);

      // Assign new id.
      $entity->pk_host = intval($attributes['id']);

      if (twCanDebug()) debug('$entity->pk_host = ' . $entity->pk_host);
    }
    else
    {
          	

      //
      // .. otherwise we update the host entity in ThumbWhere
      //

      $host = $twAdminAPI->update_host($id=$entity->pk_host,$context = array(
                  'key' => variable_get('thumbwhere_api_key'),
                  'origin' => variable_get('thumbwhere_host_id'),
          ), $fields);
            
        
    }
          	
    
  }
}

/**
 * Implements thumbwhere_host_entity_insert().
 *
 * Act on entities when inserted.
 *
 * @param $entity
 *   The entity object
 * @param $type
 *   The type of entity being inserted (i.e. node, user, comment).
 */
function thumbwhere_host_entity_insert($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_host') return;

}


/**
* Implements thumbwhere_host_entity_delete().
*
* Act on entities when inserted.
*
* @param $entity
*   The entity object
* @param $type
*   The type of entity being deleted (i.e. node, user, comment).
*/
function thumbwhere_host_entity_delete($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_host') return;


}
