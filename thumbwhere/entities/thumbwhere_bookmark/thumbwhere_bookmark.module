<?php

// ******************************************************************
// This file was generated by autogen_model_implementation_drupal.xsl
// ******************************************************************

// Include the ThumbWhere SDK
require_once(dirname(__FILE__) . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . 'dependencies' . DIRECTORY_SEPARATOR . 'tw' . DIRECTORY_SEPARATOR . 'sdk.class.php');

/**
 * @file
 * Module for the ThumbWhere Bookmark Entity
 *
 *
 * NOTE: This has been autogenerated.
 * 
 */

/**
 * Implement hook_entity_info().
 *
 * We define two entities here - the actual entity that will hold our domain
 * specific information and an entity that holds information about the different
 * types of entities. See here: http://drupal.org/node/977380 for a discussion on this
 * choice.
 */
function thumbwhere_bookmark_entity_info() {
  $return['thumbwhere_bookmark'] = array(

    // The human-readable name of the type.
    'label' => t('ThumbWhereBookmark'),

    // (used by DrupalDefaultEntityController) The name of the entity type's base table.
    'base table' => 'thumbwhere_bookmark',

    // fieldable: Set to TRUE if you want your entity type to accept fields being attached to it.
    'fieldable' => TRUE,

  // Declare the endty keys..
    'entity keys' => array(
      'id' => 'pk_bookmark',
      'label' => 'pk_bookmark',
    ),

    // A function taking an entity and an entity type as arguments and returning the label of the entity.
    // The entity label is the main string associated with an entity; for example, the title of a node or
    // the subject of a comment. If there is an entity object property that defines the label, use the
    // 'label' element of the 'entity keys' return value component to provide this information (see below).
    // If more complex logic is needed to determine the label of an entity, you can instead specify a callback
    // function here, which will be called to determine the entity label. See also the entity_label()
    // function, which implements this logic.
    'label callback' => 'entity_class_label',

    // A function taking an entity as argument and returning the uri elements of the entity, e.g. 'path' and 'options'.
    // The actual entity uri can be constructed by passing these elements to url().
    'uri callback' => 'entity_class_uri',

    //
    // Entity API Components
    //

    // The module whwre we can find our classes
    'module' => 'thumbwhere_bookmark',

    // The entity class and controller class extend the classes provided by the Entity API
    'entity class' => 'ThumbWhereBookmark',

    // Which Entity API contoller
    'controller class' => 'ThumbWhereBookmarkController',

    // The information below is used by the ThumbWhereBookmarkUIController (which extends the EntityDefaultUIController)
    'admin ui' => array(
      'path' => 'admin/thumbwhere/thumbwhere_bookmarks',
      'file' => 'thumbwhere_bookmark.admin.inc',
      'controller class' => 'ThumbWhereBookmarkUIController',
      'menu wildcard' => '%thumbwhere_bookmark',
    ),
  );

  return $return;
}


/**
 * Implements hook_permission().
 */
function thumbwhere_bookmark_permission() {

  // We set up permisssions to manage entity types, manage all entities and the
  // permissions for each individual entity
  $permissions = array(
    'administer thumbwhere_bookmarks' => array(
      'title' => t('Administer thumbwhere_bookmarks'),
      'description' => t('Edit and delete all thumbwhere_bookmarks'),
    ),
  'send thumbwhere bookmark notifications' => array(
          'title' => t('Send ThumbWhere Notifications'),
          'description' => t('Allow the caller to call the ThumbWhere Bookmark Entity Notifications Endpoint.'),
          'restrict access' => TRUE,
      ),    
  );

  return $permissions;
}


/**
 * Determines whether the given user has access to a thumbwhere_bookmark.
 *
 * @param $op
 *   The operation being performed. One of 'view', 'update', 'create', 'delete'
 *   or just 'edit' (being the same as 'create' or 'update').
 * @param $thumbwhere_bookmark
 *   Optionally a thumbwhere_bookmark or a thumbwhere_bookmark type to check access for. If nothing is
 *   given, access for all thumbwhere_bookmarks is determined.
 * @param $account
 *   The user to check for. Leave it to NULL to check for the global user.
 * @return boolean
 *   Whether access is allowed or not.
 */
function thumbwhere_bookmark_access($op, $thumbwhere_bookmark = NULL, $account = NULL) {

  //if (user_access('administer thumbwhere_bookmarks', $account)) {
  //  return TRUE;
 // }

  //return FALSE;
  return TRUE;
}




/**
 * Fetch a thumbwhere_bookmark object. Make sure that the wildcard you choose
 * in the thumbwhere_bookmark entity definition fits the function name here.
 *
 * @param $thumbwhere_bookmark_id
 *   Integer specifying the thumbwhere_bookmark id.
 * @param $reset
 *   A boolean indicating that the internal cache should be reset.
 * @return
 *   A fully-loaded $thumbwhere_bookmark object or FALSE if it cannot be loaded.
 *
 * @see thumbwhere_bookmark_load_multiple()
 */
function thumbwhere_bookmark_load($thumbwhere_bookmark_id, $reset = FALSE) {

  $thumbwhere_bookmarks = entity_load('thumbwhere_bookmark', array($thumbwhere_bookmark_id), array(), $reset);

  return reset($thumbwhere_bookmarks);
}


/**
 * Deletes a thumbwhere_bookmark.
 */
function thumbwhere_bookmark_delete(ThumbWhereBookmark $thumbwhere_bookmark) {

  $thumbwhere_bookmark->delete();
}

/**
 * Create a thumbwhere_bookmark object.
 */
function thumbwhere_bookmark_create($values = array()) {

  return entity_get_controller('thumbwhere_bookmark')->create($values);
}


/**
 * URI callback for thumbwhere_bookmarks
 */
function thumbwhere_bookmark_uri(ThumbWhereBookmark $thumbwhere_bookmark) {

  return array(
    'path' => 'thumbwhere_bookmark/' . $thumbwhere_bookmark->pk_bookmark,
  );
}


/**
 * Menu title callback for showing individual entities
 */
function thumbwhere_bookmark_page_title(ThumbWhereBookmark $thumbwhere_bookmark) {

	return $thumbwhere_bookmark->pk_bookmark;
}


/**
 * Sets up content to show an individual thumbwhere_bookmark
 * @todo - get rid of drupal_set_title();
 */
function thumbwhere_bookmark_page_view($thumbwhere_bookmark, $view_mode = 'full') {



  $controller = entity_get_controller('thumbwhere_bookmark');
  $content = $controller->view(array($thumbwhere_bookmark->pk_bookmark => $thumbwhere_bookmark));
  drupal_set_title($thumbwhere_bookmark->pk_bookmark);
  return $content;
}


/**
 * Implements hook_views_api().
 */
function thumbwhere_bookmark_views_api() {



  return array(
    'api' => 3,
    'path' => drupal_get_path('module', 'thumbwhere_bookmark') . '/views',
  );
}


/**
 * Implement hook_theme().
 */
function thumbwhere_bookmark_theme() {



  return array(
    'thumbwhere_bookmark_add_list' => array(
      'variables' => array('content' => array()),
      'file' => 'thumbwhere_bookmark.admin.inc',
    ),
    'thumbwhere_bookmark' => array(
      'render element' => 'elements',
      'template' => 'thumbwhere_bookmark',
    )
  );
}


/**
 * Implements hook_menu_local_tasks_alter().
 */
function thumbwhere_bookmark_menu_local_tasks_alter(&$data, $router_item, $root_path) {

  // Add action link 'admin/structure/thumbwhere_bookmarks/add' on 'admin/structure/thumbwhere_bookmarks'.
  if ($root_path == 'admin/thumbwhere/thumbwhere_bookmarks') {
    $item = menu_get_item('admin/thumbwhere/thumbwhere_bookmarks/add');
    if ($item['access']) {
      $data['actions']['output'][] = array(
        '#theme' => 'menu_local_action',
        '#link' => $item,
      );
    }
  }
}


/**
 * The class used for thumbwhere_bookmark entities
 */
class ThumbWhereBookmark extends Entity {

  public function __construct($values = array()) {
    parent::__construct($values, 'thumbwhere_bookmark');
  }

  protected function defaultLabel() {
    return $this->pk_bookmark;
  }

  protected function defaultUri() {
    return array('path' => 'thumbwhere_bookmark/' . $this->pk_bookmark);
  }


}



/**
 * The Controller for ThumbWhereBookmark entities
 */
class ThumbWhereBookmarkController extends EntityAPIController {
  public function __construct($entityType) {
    parent::__construct($entityType);
  }
}



/**
* Implements hook_entity_presave().
*
* Act on an Bookmark entity before it is about to be created or updated.
*
* @param $entity
*   The entity object.
* @param $entity_type
*   The type of entity being saved (i.e. node, user, comment).
*/
function thumbwhere_bookmark_entity_presave($entity, $entity_type) {

  // We only look after what we want...
  if ($entity_type == 'thumbwhere_bookmark')  {
  	
  	
  	
 	/////////////////////////////////////////////////////
    //
    // Should we not propagate this to ThumbWhere? 
    //
    if (isset($entity->do_not_propagate)) {
      if ($entity->do_not_propagate) {
        if (twCanDebug()) {
          if ($entity->is_new) {
            if (twCanDebug()){
              debug("DO NOT PROPAGATE 'bookmark' presave *CREATE*");
          	}
          }
          else {
            if (twCanDebug()) {
              debug("DO NOT PROPAGATE 'bookmark' presave *UPDATE*");
            }
          }
        }

        // To cause an infinite loop of updates, comment out this line.
        return;
      }
      else if (twCanDebug()) {
        debug("OK PROAOGATE 'bookmark' presave to ThumbWhere Server");
      }
    }
    else if (twCanDebug()) {
      debug("DO NOT PROPAGATE IS NOT SET");
    }
    //
    //
    ////////////////////////////////////////////////////////  	

  
    // .. then we create a new thumbwhere_bookmark entity...
      // Instantiate the ThumbWhereContent class

    $twContentAPI = new ThumbWhereAPIContent();

    //if (twCanDebug()) debug($twContentAPI);

    //if (twCanDebug()) debug($entity);

    //
    // Collate and validate our fields
    //

    $fields = array();
    // Compulsory so..
    if (!isset($entity->fk_actor)) {
      throw new Exception('actor is mandatory.');
    }
    else {
      $fields['actor'] = $entity->fk_actor;
    }
    // Compulsory so..
    if (!isset($entity->fk_thing)) {
      throw new Exception('thing is mandatory.');
    }
    else {
      $fields['thing'] = $entity->fk_thing;
    }
		
	//
	// Ensure that we have the 'thumbwhere_api_key' as we are going to pass that through in a context
	//
    if (variable_get('thumbwhere_api_key') == '') {
    	throw new Exception('ThumbWhere resource for \'Bookmark\' requires that variable \'thumbwhere_api_key\' is set.');
    }
    
              		
    //
    // If this bookmark entity is new....
    //
               
    if ($entity->is_new) {
                

      //
      // ... then we create the bookmark entity in ThumbWhere
      //

      $bookmark = $twContentAPI->create_bookmark($context = array(
                  'key' => variable_get('thumbwhere_api_key')
                  'member' => 
          ), $fields);
            
       if (twCanDebug()) debug($bookmark);

      //if (twCanDebug()) debug($bookmark);

      $attributes = $bookmark->body->bookmark->attributes();

      if (twCanDebug()) debug('newly created id = ' . $attributes['id']);

      // Assign new id.
      $entity->pk_bookmark = intval($attributes['id']);

      if (twCanDebug()) debug('$entity->pk_bookmark = ' . $entity->pk_bookmark);
    }
    else
    {
          	

      //
      // .. otherwise we update the bookmark entity in ThumbWhere
      //

      $bookmark = $twContentAPI->update_bookmark($id=$entity->pk_bookmark,$context = array(
                  'key' => variable_get('thumbwhere_api_key')
                  'member' => 
          ), $fields);
            
        
    }
          	
    
  }
}

/**
 * Implements thumbwhere_bookmark_entity_insert().
 *
 * Act on entities when inserted.
 *
 * @param $entity
 *   The entity object
 * @param $type
 *   The type of entity being inserted (i.e. node, user, comment).
 */
function thumbwhere_bookmark_entity_insert($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_bookmark') return;

}


/**
* Implements thumbwhere_bookmark_entity_delete().
*
* Act on entities when inserted.
*
* @param $entity
*   The entity object
* @param $type
*   The type of entity being deleted (i.e. node, user, comment).
*/
function thumbwhere_bookmark_entity_delete($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_bookmark') return;


}
