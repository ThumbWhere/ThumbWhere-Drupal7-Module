<?php

// ******************************************************************
// This file was generated by autogen_model_implementation_drupal.xsl
// ******************************************************************

// Include the ThumbWhere SDK
require_once(dirname(__FILE__) . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . 'dependencies' . DIRECTORY_SEPARATOR . 'tw' . DIRECTORY_SEPARATOR . 'sdk.class.php');

/**
 * @file
 * Module for the ThumbWhere HostRoleOrder Entity
 *
 *
 * NOTE: This has been autogenerated.
 * 
 */

/**
 * Implement hook_entity_info().
 *
 * We define two entities here - the actual entity that will hold our domain
 * specific information and an entity that holds information about the different
 * types of entities. See here: http://drupal.org/node/977380 for a discussion on this
 * choice.
 */
function thumbwhere_hostroleorder_entity_info() {
  $return['thumbwhere_hostroleorder'] = array(

    // The human-readable name of the type.
    'label' => t('ThumbWhereHostRoleOrder'),

    // (used by DrupalDefaultEntityController) The name of the entity type's base table.
    'base table' => 'thumbwhere_hostroleorder',

    // fieldable: Set to TRUE if you want your entity type to accept fields being attached to it.
    'fieldable' => TRUE,

  // Declare the endty keys..
    'entity keys' => array(
      'id' => 'pk_hostroleorder',
      'label' => 'pk_hostroleorder',
    ),

    // A function taking an entity and an entity type as arguments and returning the label of the entity.
    // The entity label is the main string associated with an entity; for example, the title of a node or
    // the subject of a comment. If there is an entity object property that defines the label, use the
    // 'label' element of the 'entity keys' return value component to provide this information (see below).
    // If more complex logic is needed to determine the label of an entity, you can instead specify a callback
    // function here, which will be called to determine the entity label. See also the entity_label()
    // function, which implements this logic.
    'label callback' => 'entity_class_label',

    // A function taking an entity as argument and returning the uri elements of the entity, e.g. 'path' and 'options'.
    // The actual entity uri can be constructed by passing these elements to url().
    'uri callback' => 'entity_class_uri',

    //
    // Entity API Components
    //

    // The module whwre we can find our classes
    'module' => 'thumbwhere_hostroleorder',

    // The entity class and controller class extend the classes provided by the Entity API
    'entity class' => 'ThumbWhereHostRoleOrder',

    // Which Entity API contoller
    'controller class' => 'ThumbWhereHostRoleOrderController',

    // The information below is used by the ThumbWhereHostRoleOrderUIController (which extends the EntityDefaultUIController)
    'admin ui' => array(
      'path' => 'admin/thumbwhere/thumbwhere_hostroleorders',
      'file' => 'thumbwhere_hostroleorder.admin.inc',
      'controller class' => 'ThumbWhereHostRoleOrderUIController',
      'menu wildcard' => '%thumbwhere_hostroleorder',
    ),
  );

  return $return;
}


/**
 * Implements hook_permission().
 */
function thumbwhere_hostroleorder_permission() {

  // We set up permisssions to manage entity types, manage all entities and the
  // permissions for each individual entity
  $permissions = array(
    'administer thumbwhere_hostroleorders' => array(
      'title' => t('Administer thumbwhere_hostroleorders'),
      'description' => t('Edit and delete all thumbwhere_hostroleorders'),
    ),
  'send thumbwhere hostroleorder notifications' => array(
          'title' => t('Send ThumbWhere Notifications'),
          'description' => t('Allow the caller to call the ThumbWhere HostRoleOrder Entity Notifications Endpoint.'),
          'restrict access' => TRUE,
      ),    
  );

  return $permissions;
}


/**
 * Determines whether the given user has access to a thumbwhere_hostroleorder.
 *
 * @param $op
 *   The operation being performed. One of 'view', 'update', 'create', 'delete'
 *   or just 'edit' (being the same as 'create' or 'update').
 * @param $thumbwhere_hostroleorder
 *   Optionally a thumbwhere_hostroleorder or a thumbwhere_hostroleorder type to check access for. If nothing is
 *   given, access for all thumbwhere_hostroleorders is determined.
 * @param $account
 *   The user to check for. Leave it to NULL to check for the global user.
 * @return boolean
 *   Whether access is allowed or not.
 */
function thumbwhere_hostroleorder_access($op, $thumbwhere_hostroleorder = NULL, $account = NULL) {

  //if (user_access('administer thumbwhere_hostroleorders', $account)) {
  //  return TRUE;
 // }

  //return FALSE;
  return TRUE;
}




/**
 * Fetch a thumbwhere_hostroleorder object. Make sure that the wildcard you choose
 * in the thumbwhere_hostroleorder entity definition fits the function name here.
 *
 * @param $thumbwhere_hostroleorder_id
 *   Integer specifying the thumbwhere_hostroleorder id.
 * @param $reset
 *   A boolean indicating that the internal cache should be reset.
 * @return
 *   A fully-loaded $thumbwhere_hostroleorder object or FALSE if it cannot be loaded.
 *
 * @see thumbwhere_hostroleorder_load_multiple()
 */
function thumbwhere_hostroleorder_load($thumbwhere_hostroleorder_id, $reset = FALSE) {

  $thumbwhere_hostroleorders = entity_load('thumbwhere_hostroleorder', array($thumbwhere_hostroleorder_id), array(), $reset);

  return reset($thumbwhere_hostroleorders);
}


/**
 * Deletes a thumbwhere_hostroleorder.
 */
function thumbwhere_hostroleorder_delete(ThumbWhereHostRoleOrder $thumbwhere_hostroleorder) {

  $thumbwhere_hostroleorder->delete();
}

/**
 * Create a thumbwhere_hostroleorder object.
 */
function thumbwhere_hostroleorder_create($values = array()) {

  return entity_get_controller('thumbwhere_hostroleorder')->create($values);
}


/**
 * URI callback for thumbwhere_hostroleorders
 */
function thumbwhere_hostroleorder_uri(ThumbWhereHostRoleOrder $thumbwhere_hostroleorder) {

  return array(
    'path' => 'thumbwhere_hostroleorder/' . $thumbwhere_hostroleorder->pk_hostroleorder,
  );
}


/**
 * Menu title callback for showing individual entities
 */
function thumbwhere_hostroleorder_page_title(ThumbWhereHostRoleOrder $thumbwhere_hostroleorder) {

	return $thumbwhere_hostroleorder->pk_hostroleorder;
}


/**
 * Sets up content to show an individual thumbwhere_hostroleorder
 * @todo - get rid of drupal_set_title();
 */
function thumbwhere_hostroleorder_page_view($thumbwhere_hostroleorder, $view_mode = 'full') {



  $controller = entity_get_controller('thumbwhere_hostroleorder');
  $content = $controller->view(array($thumbwhere_hostroleorder->pk_hostroleorder => $thumbwhere_hostroleorder));
  drupal_set_title($thumbwhere_hostroleorder->pk_hostroleorder);
  return $content;
}


/**
 * Implements hook_views_api().
 */
function thumbwhere_hostroleorder_views_api() {



  return array(
    'api' => 3,
    'path' => drupal_get_path('module', 'thumbwhere_hostroleorder') . '/views',
  );
}


/**
 * Implement hook_theme().
 */
function thumbwhere_hostroleorder_theme() {



  return array(
    'thumbwhere_hostroleorder_add_list' => array(
      'variables' => array('content' => array()),
      'file' => 'thumbwhere_hostroleorder.admin.inc',
    ),
    'thumbwhere_hostroleorder' => array(
      'render element' => 'elements',
      'template' => 'thumbwhere_hostroleorder',
    )
  );
}


/**
 * Implements hook_menu_local_tasks_alter().
 */
function thumbwhere_hostroleorder_menu_local_tasks_alter(&$data, $router_item, $root_path) {

  // Add action link 'admin/structure/thumbwhere_hostroleorders/add' on 'admin/structure/thumbwhere_hostroleorders'.
  if ($root_path == 'admin/thumbwhere/thumbwhere_hostroleorders') {
    $item = menu_get_item('admin/thumbwhere/thumbwhere_hostroleorders/add');
    if ($item['access']) {
      $data['actions']['output'][] = array(
        '#theme' => 'menu_local_action',
        '#link' => $item,
      );
    }
  }
}


/**
 * The class used for thumbwhere_hostroleorder entities
 */
class ThumbWhereHostRoleOrder extends Entity {

  public function __construct($values = array()) {
    parent::__construct($values, 'thumbwhere_hostroleorder');
  }

  protected function defaultLabel() {
    return $this->pk_hostroleorder;
  }

  protected function defaultUri() {
    return array('path' => 'thumbwhere_hostroleorder/' . $this->pk_hostroleorder);
  }


}



/**
 * The Controller for ThumbWhereHostRoleOrder entities
 */
class ThumbWhereHostRoleOrderController extends EntityAPIController {
  public function __construct($entityType) {
    parent::__construct($entityType);
  }
}



/**
* Implements hook_entity_presave().
*
* Act on an HostRoleOrder entity before it is about to be created or updated.
*
* @param $entity
*   The entity object.
* @param $entity_type
*   The type of entity being saved (i.e. node, user, comment).
*/
function thumbwhere_hostroleorder_entity_presave($entity, $entity_type) {

  // We only look after what we want...
  if ($entity_type == 'thumbwhere_hostroleorder')  {
  	
  	
  	
 	/////////////////////////////////////////////////////
    //
    // Should we not propagate this to ThumbWhere? 
    //
    if (isset($entity->do_not_propagate)) {
      if ($entity->do_not_propagate) {
        if (twCanDebug()) {
          if ($entity->is_new) {
            if (twCanDebug()){
              debug("DO NOT PROPAGATE 'hostroleorder' presave *CREATE*");
          	}
          }
          else {
            if (twCanDebug()) {
              debug("DO NOT PROPAGATE 'hostroleorder' presave *UPDATE*");
            }
          }
        }

        // To cause an infinite loop of updates, comment out this line.
        return;
      }
      else if (twCanDebug()) {
        debug("OK PROAOGATE 'hostroleorder' presave to ThumbWhere Server");
      }
    }
    else if (twCanDebug()) {
      debug("DO NOT PROPAGATE IS NOT SET");
    }
    //
    //
    ////////////////////////////////////////////////////////  	

  
    // .. then we create a new thumbwhere_hostroleorder entity...
      // Instantiate the ThumbWhereContent class

    $twAdminAPI = new ThumbWhereAPIAdmin();

    //if (twCanDebug()) debug($twContentAPI);

    //if (twCanDebug()) debug($entity);

    //
    // Collate and validate our fields
    //

    $fields = array();

    // Nullable so we only pass it through to create_host_role_order if we have it..
    if (isset($entity->fk_host)) {
      $fields['host'] = $entity->fk_host;
    }

    // Nullable so we only pass it through to create_host_role_order if we have it..
    if (isset($entity->fk_hostroletype)) {
      $fields['hostroletype'] = $entity->fk_hostroletype;
    }
    // Compulsory so..
    if (!isset($entity->processing)) {
      throw new Exception('processing is mandatory.');
    }
    else {
      $fields['processing'] = $entity->processing;
    }
    // Compulsory so..
    if (!isset($entity->completed)) {
      throw new Exception('completed is mandatory.');
    }
    else {
      $fields['completed'] = $entity->completed;
    }
		
	//
	// Ensure that we have the 'thumbwhere_api_key' as we are going to pass that through in a context
	//
    if (variable_get('thumbwhere_api_key') == '') {
    	throw new Exception('ThumbWhere resource for \'HostRoleOrder\' requires that variable \'thumbwhere_api_key\' is set.');
    }
    
              		
    //
    // If this hostroleorder entity is new....
    //
               
    if ($entity->is_new) {
                

      //
      // ... then we create the hostroleorder entity in ThumbWhere
      //

      $hostroleorder = $twAdminAPI->create_host_role_order($context = array(
                  'key' => variable_get('thumbwhere_api_key')
          ), $fields);
            
       if (twCanDebug()) debug($hostroleorder);

      //if (twCanDebug()) debug($host_role_order);

      $attributes = $hostroleorder->body->host_role_order->attributes();

      if (twCanDebug()) debug('newly created id = ' . $attributes['id']);

      // Assign new id.
      $entity->pk_hostroleorder = intval($attributes['id']);

      if (twCanDebug()) debug('$entity->pk_hostroleorder = ' . $entity->pk_hostroleorder);
    }
    else
    {
          	

      //
      // .. otherwise we update the hostroleorder entity in ThumbWhere
      //

      $hostroleorder = $twAdminAPI->update_host_role_order($id=$entity->pk_hostroleorder,$context = array(
                  'key' => variable_get('thumbwhere_api_key')
          ), $fields);
            
        
    }
          	
    
  }
}

/**
 * Implements thumbwhere_hostroleorder_entity_insert().
 *
 * Act on entities when inserted.
 *
 * @param $entity
 *   The entity object
 * @param $type
 *   The type of entity being inserted (i.e. node, user, comment).
 */
function thumbwhere_hostroleorder_entity_insert($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_hostroleorder') return;

}


/**
* Implements thumbwhere_hostroleorder_entity_delete().
*
* Act on entities when inserted.
*
* @param $entity
*   The entity object
* @param $type
*   The type of entity being deleted (i.e. node, user, comment).
*/
function thumbwhere_hostroleorder_entity_delete($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_hostroleorder') return;


}
