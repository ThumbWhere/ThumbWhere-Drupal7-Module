<?php

// ******************************************************************
// This file was generated by autogen_model_implementation_drupal.xsl
// ******************************************************************

// Include the ThumbWhere SDK
require_once(dirname(__FILE__) . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . 'dependencies' . DIRECTORY_SEPARATOR . 'tw' . DIRECTORY_SEPARATOR . 'sdk.class.php');

/**
 * @file
 * Module for the ThumbWhere Subject Entity
 *
 *
 * NOTE: This has been autogenerated.
 * 
 */

/**
 * Implement hook_entity_info().
 *
 * We define two entities here - the actual entity that will hold our domain
 * specific information and an entity that holds information about the different
 * types of entities. See here: http://drupal.org/node/977380 for a discussion on this
 * choice.
 */
function thumbwhere_subject_entity_info() {
  $return['thumbwhere_subject'] = array(

    // The human-readable name of the type.
    'label' => t('ThumbWhereSubject'),

    // (used by DrupalDefaultEntityController) The name of the entity type's base table.
    'base table' => 'thumbwhere_subject',

    // fieldable: Set to TRUE if you want your entity type to accept fields being attached to it.
    'fieldable' => TRUE,

  // Declare the endty keys..
    'entity keys' => array(
      'id' => 'pk_subject',
      'label' => 'pk_subject',
    ),

    // A function taking an entity and an entity type as arguments and returning the label of the entity.
    // The entity label is the main string associated with an entity; for example, the title of a node or
    // the subject of a comment. If there is an entity object property that defines the label, use the
    // 'label' element of the 'entity keys' return value component to provide this information (see below).
    // If more complex logic is needed to determine the label of an entity, you can instead specify a callback
    // function here, which will be called to determine the entity label. See also the entity_label()
    // function, which implements this logic.
    'label callback' => 'entity_class_label',

    // A function taking an entity as argument and returning the uri elements of the entity, e.g. 'path' and 'options'.
    // The actual entity uri can be constructed by passing these elements to url().
    'uri callback' => 'entity_class_uri',

    //
    // Entity API Components
    //

    // The module whwre we can find our classes
    'module' => 'thumbwhere_subject',

    // The entity class and controller class extend the classes provided by the Entity API
    'entity class' => 'ThumbWhereSubject',

    // Which Entity API contoller
    'controller class' => 'ThumbWhereSubjectController',

    // The information below is used by the ThumbWhereSubjectUIController (which extends the EntityDefaultUIController)
    'admin ui' => array(
      'path' => 'admin/thumbwhere/thumbwhere_subjects',
      'file' => 'thumbwhere_subject.admin.inc',
      'controller class' => 'ThumbWhereSubjectUIController',
      'menu wildcard' => '%thumbwhere_subject',
    ),
  );

  return $return;
}


/**
 * Implements hook_permission().
 */
function thumbwhere_subject_permission() {

  // We set up permisssions to manage entity types, manage all entities and the
  // permissions for each individual entity
  $permissions = array(
    'administer thumbwhere_subjects' => array(
      'title' => t('Administer thumbwhere_subjects'),
      'description' => t('Edit and delete all thumbwhere_subjects'),
    ),
  'send thumbwhere subject notifications' => array(
          'title' => t('Send ThumbWhere Notifications'),
          'description' => t('Allow the caller to call the ThumbWhere Subject Entity Notifications Endpoint.'),
          'restrict access' => TRUE,
      ),    
  );

  return $permissions;
}


/**
 * Determines whether the given user has access to a thumbwhere_subject.
 *
 * @param $op
 *   The operation being performed. One of 'view', 'update', 'create', 'delete'
 *   or just 'edit' (being the same as 'create' or 'update').
 * @param $thumbwhere_subject
 *   Optionally a thumbwhere_subject or a thumbwhere_subject type to check access for. If nothing is
 *   given, access for all thumbwhere_subjects is determined.
 * @param $account
 *   The user to check for. Leave it to NULL to check for the global user.
 * @return boolean
 *   Whether access is allowed or not.
 */
function thumbwhere_subject_access($op, $thumbwhere_subject = NULL, $account = NULL) {

  //if (user_access('administer thumbwhere_subjects', $account)) {
  //  return TRUE;
 // }

  //return FALSE;
  return TRUE;
}




/**
 * Fetch a thumbwhere_subject object. Make sure that the wildcard you choose
 * in the thumbwhere_subject entity definition fits the function name here.
 *
 * @param $thumbwhere_subject_id
 *   Integer specifying the thumbwhere_subject id.
 * @param $reset
 *   A boolean indicating that the internal cache should be reset.
 * @return
 *   A fully-loaded $thumbwhere_subject object or FALSE if it cannot be loaded.
 *
 * @see thumbwhere_subject_load_multiple()
 */
function thumbwhere_subject_load($thumbwhere_subject_id, $reset = FALSE) {

  $thumbwhere_subjects = entity_load('thumbwhere_subject', array($thumbwhere_subject_id), array(), $reset);

  return reset($thumbwhere_subjects);
}


/**
 * Deletes a thumbwhere_subject.
 */
function thumbwhere_subject_delete(ThumbWhereSubject $thumbwhere_subject) {

  $thumbwhere_subject->delete();
}

/**
 * Create a thumbwhere_subject object.
 */
function thumbwhere_subject_create($values = array()) {

  return entity_get_controller('thumbwhere_subject')->create($values);
}


/**
 * URI callback for thumbwhere_subjects
 */
function thumbwhere_subject_uri(ThumbWhereSubject $thumbwhere_subject) {

  return array(
    'path' => 'thumbwhere_subject/' . $thumbwhere_subject->pk_subject,
  );
}


/**
 * Menu title callback for showing individual entities
 */
function thumbwhere_subject_page_title(ThumbWhereSubject $thumbwhere_subject) {

	return $thumbwhere_subject->pk_subject;
}


/**
 * Sets up content to show an individual thumbwhere_subject
 * @todo - get rid of drupal_set_title();
 */
function thumbwhere_subject_page_view($thumbwhere_subject, $view_mode = 'full') {



  $controller = entity_get_controller('thumbwhere_subject');
  $content = $controller->view(array($thumbwhere_subject->pk_subject => $thumbwhere_subject));
  drupal_set_title($thumbwhere_subject->pk_subject);
  return $content;
}


/**
 * Implements hook_views_api().
 */
function thumbwhere_subject_views_api() {



  return array(
    'api' => 3,
    'path' => drupal_get_path('module', 'thumbwhere_subject') . '/views',
  );
}


/**
 * Implement hook_theme().
 */
function thumbwhere_subject_theme() {



  return array(
    'thumbwhere_subject_add_list' => array(
      'variables' => array('content' => array()),
      'file' => 'thumbwhere_subject.admin.inc',
    ),
    'thumbwhere_subject' => array(
      'render element' => 'elements',
      'template' => 'thumbwhere_subject',
    )
  );
}


/**
 * Implements hook_menu_local_tasks_alter().
 */
function thumbwhere_subject_menu_local_tasks_alter(&$data, $router_item, $root_path) {

  // Add action link 'admin/structure/thumbwhere_subjects/add' on 'admin/structure/thumbwhere_subjects'.
  if ($root_path == 'admin/thumbwhere/thumbwhere_subjects') {
    $item = menu_get_item('admin/thumbwhere/thumbwhere_subjects/add');
    if ($item['access']) {
      $data['actions']['output'][] = array(
        '#theme' => 'menu_local_action',
        '#link' => $item,
      );
    }
  }
}


/**
 * The class used for thumbwhere_subject entities
 */
class ThumbWhereSubject extends Entity {

  public function __construct($values = array()) {
    parent::__construct($values, 'thumbwhere_subject');
  }

  protected function defaultLabel() {
    return $this->pk_subject;
  }

  protected function defaultUri() {
    return array('path' => 'thumbwhere_subject/' . $this->pk_subject);
  }


}



/**
 * The Controller for ThumbWhereSubject entities
 */
class ThumbWhereSubjectController extends EntityAPIController {
  public function __construct($entityType) {
    parent::__construct($entityType);
  }
}



/**
* Implements hook_entity_presave().
*
* Act on an Subject entity before it is about to be created or updated.
*
* @param $entity
*   The entity object.
* @param $entity_type
*   The type of entity being saved (i.e. node, user, comment).
*/
function thumbwhere_subject_entity_presave($entity, $entity_type) {

  // We only look after what we want...
  if ($entity_type == 'thumbwhere_subject')  {
  	
 	/////////////////////////////////////////////////////
    //
    // Should we not propagate this to ThumbWhere? 
    //
    if (isset($entity->do_not_propagate)) {
      if ($entity->do_not_propagate) {
        if (twCanDebug()) {
          if ($entity->is_new) {
            if (twCanDebug()){
              debug("DO NOT PROPAGATE 'subject' presave *CREATE* not being sent to ThumbWhere server.");
          	}
          }
          else {
            if (twCanDebug()) {
              debug("DO NOT PROPAGATE 'subject' presave *UPDATE* not being sent to ThumbWhere server.");
            }
          }
        }

        // To cause an infinite loop of updates, comment out this line.
        return;
      }
      else if (twCanDebug()) {
        debug("OK PROAOGATE 'subject' presave to ThumbWhere server.");
      }
    }
    else if (twCanDebug()) {
      debug("DO NOT PROPAGATE IS NOT SET");
    }
    //
    //
    ////////////////////////////////////////////////////////  	

  
    // .. then we create a new thumbwhere_subject entity...
      // Instantiate the ThumbWhereContent class

    $twContentAPI = new ThumbWhereAPIContent();

    //if (twCanDebug()) debug($twContentAPI);

    //if (twCanDebug()) debug($entity);

    //
    // Collate and validate our fields
    //

    $fields = array();

    // Nullable so we only pass it through to create_subject if we have it..
    if (isset($entity->fk_actor)) {
      $fields['actor'] = $entity->fk_actor;
    }

    // Nullable so we only pass it through to create_subject if we have it..
    if (isset($entity->fk_thing)) {
      $fields['thing'] = $entity->fk_thing;
    }

    // Nullable so we only pass it through to create_subject if we have it..
    if (isset($entity->fk_action)) {
      $fields['action'] = $entity->fk_action;
    }

    // Nullable so we only pass it through to create_subject if we have it..
    if (isset($entity->fk_place)) {
      $fields['place'] = $entity->fk_place;
    }
		
	//
	// Ensure that we have the 'thumbwhere_api_key' as we are going to pass that through in a context
	//
    if (variable_get('thumbwhere_api_key') == '') {
    	throw new Exception('ThumbWhere resource for \'Subject\' requires that variable \'thumbwhere_api_key\' is set.');
    }
    
              		
    //
    // If this subject entity is new....
    //
               
    if ($entity->is_new) {
                

      //
      // ... then we create the subject entity in ThumbWhere
      //

      $subject = $twContentAPI->create_subject($context = array(
                  'key' => variable_get('thumbwhere_api_key')
          ), $fields);
            
       if (twCanDebug()) debug($subject);

      //if (twCanDebug()) debug($subject);

      $attributes = $subject->body->subject->attributes();

      if (twCanDebug()) debug('newly created id = ' . $attributes['id']);

      // Assign new id.
      $entity->pk_subject = intval($attributes['id']);

      if (twCanDebug()) debug('$entity->pk_subject = ' . $entity->pk_subject);
    }
    else
    {
          	

      //
      // .. otherwise we update the subject entity in ThumbWhere
      //

      $subject = $twContentAPI->update_subject($id=$entity->pk_subject,$context = array(
                  'key' => variable_get('thumbwhere_api_key')
          ), $fields);
            
        
    }
          	
    
  }
}

/**
 * Implements thumbwhere_subject_entity_insert().
 *
 * Act on entities when inserted.
 *
 * @param $entity
 *   The entity object
 * @param $type
 *   The type of entity being inserted (i.e. node, user, comment).
 */
function thumbwhere_subject_entity_insert($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_subject') return;

}


/**
* Implements thumbwhere_subject_entity_delete().
*
* Act on entities when inserted.
*
* @param $entity
*   The entity object
* @param $type
*   The type of entity being deleted (i.e. node, user, comment).
*/
function thumbwhere_subject_entity_delete($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_subject') return;


}
