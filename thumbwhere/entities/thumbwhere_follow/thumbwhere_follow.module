<?php

// ******************************************************************
// This file was generated by autogen_model_implementation_drupal.xsl
// ******************************************************************

// Include the ThumbWhere SDK
require_once(dirname(__FILE__) . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . 'dependencies' . DIRECTORY_SEPARATOR . 'tw' . DIRECTORY_SEPARATOR . 'sdk.class.php');

/**
 * @file
 * Module for the ThumbWhere Follow Entity
 *
 *
 * NOTE: This has been autogenerated.
 * 
 */

/**
 * Implement hook_entity_info().
 *
 * We define two entities here - the actual entity that will hold our domain
 * specific information and an entity that holds information about the different
 * types of entities. See here: http://drupal.org/node/977380 for a discussion on this
 * choice.
 */
function thumbwhere_follow_entity_info() {
  $return['thumbwhere_follow'] = array(

    // The human-readable name of the type.
    'label' => t('ThumbWhereFollow'),

    // (used by DrupalDefaultEntityController) The name of the entity type's base table.
    'base table' => 'thumbwhere_follow',

    // fieldable: Set to TRUE if you want your entity type to accept fields being attached to it.
    'fieldable' => TRUE,

  // Declare the endty keys..
    'entity keys' => array(
      'id' => 'pk_follow',
      'label' => 'pk_follow',
    ),

    // A function taking an entity and an entity type as arguments and returning the label of the entity.
    // The entity label is the main string associated with an entity; for example, the title of a node or
    // the subject of a comment. If there is an entity object property that defines the label, use the
    // 'label' element of the 'entity keys' return value component to provide this information (see below).
    // If more complex logic is needed to determine the label of an entity, you can instead specify a callback
    // function here, which will be called to determine the entity label. See also the entity_label()
    // function, which implements this logic.
    'label callback' => 'entity_class_label',

    // A function taking an entity as argument and returning the uri elements of the entity, e.g. 'path' and 'options'.
    // The actual entity uri can be constructed by passing these elements to url().
    'uri callback' => 'entity_class_uri',

    //
    // Entity API Components
    //

    // The module whwre we can find our classes
    'module' => 'thumbwhere_follow',

    // The entity class and controller class extend the classes provided by the Entity API
    'entity class' => 'ThumbWhereFollow',

    // Which Entity API contoller
    'controller class' => 'ThumbWhereFollowController',

    // The information below is used by the ThumbWhereFollowUIController (which extends the EntityDefaultUIController)
    'admin ui' => array(
      'path' => 'admin/thumbwhere/thumbwhere_follows',
      'file' => 'thumbwhere_follow.admin.inc',
      'controller class' => 'ThumbWhereFollowUIController',
      'menu wildcard' => '%thumbwhere_follow',
    ),
  );

  return $return;
}


/**
 * Implements hook_permission().
 */
function thumbwhere_follow_permission() {

  // We set up permisssions to manage entity types, manage all entities and the
  // permissions for each individual entity
  $permissions = array(
    'administer thumbwhere_follows' => array(
      'title' => t('Administer thumbwhere_follows'),
      'description' => t('Edit and delete all thumbwhere_follows'),
    ),
  'send thumbwhere follow notifications' => array(
          'title' => t('Send ThumbWhere Notifications'),
          'description' => t('Allow the caller to call the ThumbWhere Follow Entity Notifications Endpoint.'),
          'restrict access' => TRUE,
      ),    
  );

  return $permissions;
}


/**
 * Determines whether the given user has access to a thumbwhere_follow.
 *
 * @param $op
 *   The operation being performed. One of 'view', 'update', 'create', 'delete'
 *   or just 'edit' (being the same as 'create' or 'update').
 * @param $thumbwhere_follow
 *   Optionally a thumbwhere_follow or a thumbwhere_follow type to check access for. If nothing is
 *   given, access for all thumbwhere_follows is determined.
 * @param $account
 *   The user to check for. Leave it to NULL to check for the global user.
 * @return boolean
 *   Whether access is allowed or not.
 */
function thumbwhere_follow_access($op, $thumbwhere_follow = NULL, $account = NULL) {

  //if (user_access('administer thumbwhere_follows', $account)) {
  //  return TRUE;
 // }

  //return FALSE;
  return TRUE;
}




/**
 * Fetch a thumbwhere_follow object. Make sure that the wildcard you choose
 * in the thumbwhere_follow entity definition fits the function name here.
 *
 * @param $thumbwhere_follow_id
 *   Integer specifying the thumbwhere_follow id.
 * @param $reset
 *   A boolean indicating that the internal cache should be reset.
 * @return
 *   A fully-loaded $thumbwhere_follow object or FALSE if it cannot be loaded.
 *
 * @see thumbwhere_follow_load_multiple()
 */
function thumbwhere_follow_load($thumbwhere_follow_id, $reset = FALSE) {

  $thumbwhere_follows = entity_load('thumbwhere_follow', array($thumbwhere_follow_id), array(), $reset);

  return reset($thumbwhere_follows);
}


/**
 * Deletes a thumbwhere_follow.
 */
function thumbwhere_follow_delete(ThumbWhereFollow $thumbwhere_follow) {

  $thumbwhere_follow->delete();
}

/**
 * Create a thumbwhere_follow object.
 */
function thumbwhere_follow_create($values = array()) {

  return entity_get_controller('thumbwhere_follow')->create($values);
}


/**
 * URI callback for thumbwhere_follows
 */
function thumbwhere_follow_uri(ThumbWhereFollow $thumbwhere_follow) {

  return array(
    'path' => 'thumbwhere_follow/' . $thumbwhere_follow->pk_follow,
  );
}


/**
 * Menu title callback for showing individual entities
 */
function thumbwhere_follow_page_title(ThumbWhereFollow $thumbwhere_follow) {

	return $thumbwhere_follow->pk_follow;
}


/**
 * Sets up content to show an individual thumbwhere_follow
 * @todo - get rid of drupal_set_title();
 */
function thumbwhere_follow_page_view($thumbwhere_follow, $view_mode = 'full') {



  $controller = entity_get_controller('thumbwhere_follow');
  $content = $controller->view(array($thumbwhere_follow->pk_follow => $thumbwhere_follow));
  drupal_set_title($thumbwhere_follow->pk_follow);
  return $content;
}


/**
 * Implements hook_views_api().
 */
function thumbwhere_follow_views_api() {



  return array(
    'api' => 3,
    'path' => drupal_get_path('module', 'thumbwhere_follow') . '/views',
  );
}


/**
 * Implement hook_theme().
 */
function thumbwhere_follow_theme() {



  return array(
    'thumbwhere_follow_add_list' => array(
      'variables' => array('content' => array()),
      'file' => 'thumbwhere_follow.admin.inc',
    ),
    'thumbwhere_follow' => array(
      'render element' => 'elements',
      'template' => 'thumbwhere_follow',
    )
  );
}


/**
 * Implements hook_menu_local_tasks_alter().
 */
function thumbwhere_follow_menu_local_tasks_alter(&$data, $router_item, $root_path) {

  // Add action link 'admin/structure/thumbwhere_follows/add' on 'admin/structure/thumbwhere_follows'.
  if ($root_path == 'admin/thumbwhere/thumbwhere_follows') {
    $item = menu_get_item('admin/thumbwhere/thumbwhere_follows/add');
    if ($item['access']) {
      $data['actions']['output'][] = array(
        '#theme' => 'menu_local_action',
        '#link' => $item,
      );
    }
  }
}


/**
 * The class used for thumbwhere_follow entities
 */
class ThumbWhereFollow extends Entity {

  public function __construct($values = array()) {
    parent::__construct($values, 'thumbwhere_follow');
  }

  protected function defaultLabel() {
    return $this->pk_follow;
  }

  protected function defaultUri() {
    return array('path' => 'thumbwhere_follow/' . $this->pk_follow);
  }


}



/**
 * The Controller for ThumbWhereFollow entities
 */
class ThumbWhereFollowController extends EntityAPIController {
  public function __construct($entityType) {
    parent::__construct($entityType);
  }
}



/**
* Implements hook_entity_presave().
*
* Act on an Follow entity before it is about to be created or updated.
*
* @param $entity
*   The entity object.
* @param $entity_type
*   The type of entity being saved (i.e. node, user, comment).
*/
function thumbwhere_follow_entity_presave($entity, $entity_type) {

  // We only look after what we want...
  if ($entity_type == 'thumbwhere_follow')  {
  	
  	
  	
 	/////////////////////////////////////////////////////
    //
    // Should we not propagate this to ThumbWhere? 
    //
    if (isset($entity->do_not_propagate)) {
      if ($entity->do_not_propagate) {
        if (twCanDebug()) {
          if ($entity->is_new) {
            debug("DO NOT PROPAGATE 'follow' presave *CREATE*");
          }
          else {
            debug("DO NOT PROPAGATE 'follow' presave *UPDATE*");
          }
        }

        // To cause an infinite loop of updates, comment out this line.
        return;
      }
      else if (twCanDebug()) {
        debug("OK PROAOGATE 'follow' presave to ThumbWhere Server");
      }
    }
    else if (twCanDebug()) {
      debug("DO NOT PROPAGATE IS NOT SET");
    }
    //
    //
    ////////////////////////////////////////////////////////  	

  
    // .. then we create a new thumbwhere_follow entity...
      // Instantiate the ThumbWhereContent class

    $twContentAPI = new ThumbWhereAPIContent();

    //if (twCanDebug()) debug($twContentAPI);

    //if (twCanDebug()) debug($entity);

    //
    // Collate and validate our fields
    //

    $fields = array();
    // Compulsory so..
    if (!isset($entity->fk_actor)) {
      throw new Exception('actor is mandatory.');
    }
    else {
      $fields['actor'] = $entity->fk_actor;
    }
    // Compulsory so..
    if (!isset($entity->fk_thing)) {
      throw new Exception('thing is mandatory.');
    }
    else {
      $fields['thing'] = $entity->fk_thing;
    }

    // Nullable so we only pass it through to create_follow if we have it..
    if (isset($entity->fk_place)) {
      $fields['place'] = $entity->fk_place;
    }
    // Compulsory so..
    if (empty($entity->documentation)) {
      throw new Exception('documentation is mandatory.');
    }
    else {
      $fields['documentation'] = $entity->documentation;
    }
		
	//
	// Ensure that we have the 'thumbwhere_api_key' as we are going to pass that through in a context
	//
    if (variable_get('thumbwhere_api_key') == '') {
    	throw new Exception('ThumbWhere resource for \'Follow\' requires that variable \'thumbwhere_api_key\' is set.');
    }
    
              		
    //
    // If this follow entity is new....
    //
               
    if ($entity->is_new) {
                

      //
      // ... then we create the follow entity in ThumbWhere
      //

      $follow = $twContentAPI->create_follow($context = array(
                  'key' => variable_get('thumbwhere_api_key')
          ), $fields);
            
       if (twCanDebug()) debug($follow);

      //if (twCanDebug()) debug($follow);

      $attributes = $follow->body->follow->attributes();

      if (twCanDebug()) debug('newly created id = ' . $attributes['id']);

      // Assign new id.
      $entity->pk_follow = intval($attributes['id']);

      if (twCanDebug()) debug('$entity->pk_follow = ' . $entity->pk_follow);
    }
    else
    {
          	

      //
      // .. otherwise we update the follow entity in ThumbWhere
      //

      $follow = $twContentAPI->update_follow($id=$entity->pk_follow,$context = array(
                  'key' => variable_get('thumbwhere_api_key')
          ), $fields);
            
        
    }
          	
    
  }
}

/**
 * Implements thumbwhere_follow_entity_insert().
 *
 * Act on entities when inserted.
 *
 * @param $entity
 *   The entity object
 * @param $type
 *   The type of entity being inserted (i.e. node, user, comment).
 */
function thumbwhere_follow_entity_insert($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_follow') return;

}


/**
* Implements thumbwhere_follow_entity_delete().
*
* Act on entities when inserted.
*
* @param $entity
*   The entity object
* @param $type
*   The type of entity being deleted (i.e. node, user, comment).
*/
function thumbwhere_follow_entity_delete($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_follow') return;


}
