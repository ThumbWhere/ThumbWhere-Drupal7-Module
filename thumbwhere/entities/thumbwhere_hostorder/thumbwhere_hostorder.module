<?php

// ******************************************************************
// This file was generated by autogen_model_implementation_drupal.xsl
// ******************************************************************

// Include the ThumbWhere SDK
require_once(dirname(__FILE__) . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . 'dependencies' . DIRECTORY_SEPARATOR . 'tw' . DIRECTORY_SEPARATOR . 'sdk.class.php');

/**
 * @file
 * Module for the ThumbWhere HostOrder Entity
 *
 *
 * NOTE: This has been autogenerated.
 * 
 */

/**
 * Implement hook_entity_info().
 *
 * We define two entities here - the actual entity that will hold our domain
 * specific information and an entity that holds information about the different
 * types of entities. See here: http://drupal.org/node/977380 for a discussion on this
 * choice.
 */
function thumbwhere_hostorder_entity_info() {
  $return['thumbwhere_hostorder'] = array(

    // The human-readable name of the type.
    'label' => t('ThumbWhereHostOrder'),

    // (used by DrupalDefaultEntityController) The name of the entity type's base table.
    'base table' => 'thumbwhere_hostorder',

    // fieldable: Set to TRUE if you want your entity type to accept fields being attached to it.
    'fieldable' => TRUE,

  // Declare the endty keys..
    'entity keys' => array(
      'id' => 'pk_hostorder',
      'label' => 'pk_hostorder',
    ),

    // A function taking an entity and an entity type as arguments and returning the label of the entity.
    // The entity label is the main string associated with an entity; for example, the title of a node or
    // the subject of a comment. If there is an entity object property that defines the label, use the
    // 'label' element of the 'entity keys' return value component to provide this information (see below).
    // If more complex logic is needed to determine the label of an entity, you can instead specify a callback
    // function here, which will be called to determine the entity label. See also the entity_label()
    // function, which implements this logic.
    'label callback' => 'entity_class_label',

    // A function taking an entity as argument and returning the uri elements of the entity, e.g. 'path' and 'options'.
    // The actual entity uri can be constructed by passing these elements to url().
    'uri callback' => 'entity_class_uri',

    //
    // Entity API Components
    //

    // The module whwre we can find our classes
    'module' => 'thumbwhere_hostorder',

    // The entity class and controller class extend the classes provided by the Entity API
    'entity class' => 'ThumbWhereHostOrder',

    // Which Entity API contoller
    'controller class' => 'ThumbWhereHostOrderController',

    // The information below is used by the ThumbWhereHostOrderUIController (which extends the EntityDefaultUIController)
    'admin ui' => array(
      'path' => 'admin/thumbwhere/thumbwhere_hostorders',
      'file' => 'thumbwhere_hostorder.admin.inc',
      'controller class' => 'ThumbWhereHostOrderUIController',
      'menu wildcard' => '%thumbwhere_hostorder',
    ),
  );

  return $return;
}


/**
 * Implements hook_permission().
 */
function thumbwhere_hostorder_permission() {

  // We set up permisssions to manage entity types, manage all entities and the
  // permissions for each individual entity
  $permissions = array(
    'administer thumbwhere_hostorders' => array(
      'title' => t('Administer thumbwhere_hostorders'),
      'description' => t('Edit and delete all thumbwhere_hostorders'),
    ),
  'send thumbwhere hostorder notifications' => array(
          'title' => t('Send ThumbWhere Notifications'),
          'description' => t('Allow the caller to call the ThumbWhere HostOrder Entity Notifications Endpoint.'),
          'restrict access' => TRUE,
      ),    
  );

  return $permissions;
}


/**
 * Determines whether the given user has access to a thumbwhere_hostorder.
 *
 * @param $op
 *   The operation being performed. One of 'view', 'update', 'create', 'delete'
 *   or just 'edit' (being the same as 'create' or 'update').
 * @param $thumbwhere_hostorder
 *   Optionally a thumbwhere_hostorder or a thumbwhere_hostorder type to check access for. If nothing is
 *   given, access for all thumbwhere_hostorders is determined.
 * @param $account
 *   The user to check for. Leave it to NULL to check for the global user.
 * @return boolean
 *   Whether access is allowed or not.
 */
function thumbwhere_hostorder_access($op, $thumbwhere_hostorder = NULL, $account = NULL) {

  //if (user_access('administer thumbwhere_hostorders', $account)) {
  //  return TRUE;
 // }

  //return FALSE;
  return TRUE;
}




/**
 * Fetch a thumbwhere_hostorder object. Make sure that the wildcard you choose
 * in the thumbwhere_hostorder entity definition fits the function name here.
 *
 * @param $thumbwhere_hostorder_id
 *   Integer specifying the thumbwhere_hostorder id.
 * @param $reset
 *   A boolean indicating that the internal cache should be reset.
 * @return
 *   A fully-loaded $thumbwhere_hostorder object or FALSE if it cannot be loaded.
 *
 * @see thumbwhere_hostorder_load_multiple()
 */
function thumbwhere_hostorder_load($thumbwhere_hostorder_id, $reset = FALSE) {

  $thumbwhere_hostorders = entity_load('thumbwhere_hostorder', array($thumbwhere_hostorder_id), array(), $reset);

  return reset($thumbwhere_hostorders);
}


/**
 * Deletes a thumbwhere_hostorder.
 */
function thumbwhere_hostorder_delete(ThumbWhereHostOrder $thumbwhere_hostorder) {

  $thumbwhere_hostorder->delete();
}

/**
 * Create a thumbwhere_hostorder object.
 */
function thumbwhere_hostorder_create($values = array()) {

  return entity_get_controller('thumbwhere_hostorder')->create($values);
}


/**
 * URI callback for thumbwhere_hostorders
 */
function thumbwhere_hostorder_uri(ThumbWhereHostOrder $thumbwhere_hostorder) {

  return array(
    'path' => 'thumbwhere_hostorder/' . $thumbwhere_hostorder->pk_hostorder,
  );
}


/**
 * Menu title callback for showing individual entities
 */
function thumbwhere_hostorder_page_title(ThumbWhereHostOrder $thumbwhere_hostorder) {

	return $thumbwhere_hostorder->pk_hostorder;
}


/**
 * Sets up content to show an individual thumbwhere_hostorder
 * @todo - get rid of drupal_set_title();
 */
function thumbwhere_hostorder_page_view($thumbwhere_hostorder, $view_mode = 'full') {



  $controller = entity_get_controller('thumbwhere_hostorder');
  $content = $controller->view(array($thumbwhere_hostorder->pk_hostorder => $thumbwhere_hostorder));
  drupal_set_title($thumbwhere_hostorder->pk_hostorder);
  return $content;
}


/**
 * Implements hook_views_api().
 */
function thumbwhere_hostorder_views_api() {



  return array(
    'api' => 3,
    'path' => drupal_get_path('module', 'thumbwhere_hostorder') . '/views',
  );
}


/**
 * Implement hook_theme().
 */
function thumbwhere_hostorder_theme() {



  return array(
    'thumbwhere_hostorder_add_list' => array(
      'variables' => array('content' => array()),
      'file' => 'thumbwhere_hostorder.admin.inc',
    ),
    'thumbwhere_hostorder' => array(
      'render element' => 'elements',
      'template' => 'thumbwhere_hostorder',
    )
  );
}


/**
 * Implements hook_menu_local_tasks_alter().
 */
function thumbwhere_hostorder_menu_local_tasks_alter(&$data, $router_item, $root_path) {

  // Add action link 'admin/structure/thumbwhere_hostorders/add' on 'admin/structure/thumbwhere_hostorders'.
  if ($root_path == 'admin/thumbwhere/thumbwhere_hostorders') {
    $item = menu_get_item('admin/thumbwhere/thumbwhere_hostorders/add');
    if ($item['access']) {
      $data['actions']['output'][] = array(
        '#theme' => 'menu_local_action',
        '#link' => $item,
      );
    }
  }
}


/**
 * The class used for thumbwhere_hostorder entities
 */
class ThumbWhereHostOrder extends Entity {

  public function __construct($values = array()) {
    parent::__construct($values, 'thumbwhere_hostorder');
  }

  protected function defaultLabel() {
    return $this->pk_hostorder;
  }

  protected function defaultUri() {
    return array('path' => 'thumbwhere_hostorder/' . $this->pk_hostorder);
  }


}



/**
 * The Controller for ThumbWhereHostOrder entities
 */
class ThumbWhereHostOrderController extends EntityAPIController {
  public function __construct($entityType) {
    parent::__construct($entityType);
  }
}



/**
* Implements hook_entity_presave().
*
* Act on an HostOrder entity before it is about to be created or updated.
*
* @param $entity
*   The entity object.
* @param $entity_type
*   The type of entity being saved (i.e. node, user, comment).
*/
function thumbwhere_hostorder_entity_presave($entity, $entity_type) {

  // We only look after what we want...
  if ($entity_type == 'thumbwhere_hostorder')  {
  	
  	
  	
 	/////////////////////////////////////////////////////
    //
    // Should we not propagate this to ThumbWhere? 
    //
    if (isset($entity->do_not_propagate)) {
      if ($entity->do_not_propagate) {
        if (twCanDebug()) {
          if ($entity->is_new) {
            if (twCanDebug()){
              debug("DO NOT PROPAGATE 'hostorder' presave *CREATE*");
          	}
          }
          else {
            if (twCanDebug()) {
              debug("DO NOT PROPAGATE 'hostorder' presave *UPDATE*");
            }
          }
        }

        // To cause an infinite loop of updates, comment out this line.
        return;
      }
      else if (twCanDebug()) {
        debug("OK PROAOGATE 'hostorder' presave to ThumbWhere Server");
      }
    }
    else if (twCanDebug()) {
      debug("DO NOT PROPAGATE IS NOT SET");
    }
    //
    //
    ////////////////////////////////////////////////////////  	

  
    // .. then we create a new thumbwhere_hostorder entity...
      // Instantiate the ThumbWhereContent class

    $twAdminAPI = new ThumbWhereAPIAdmin();

    //if (twCanDebug()) debug($twContentAPI);

    //if (twCanDebug()) debug($entity);

    //
    // Collate and validate our fields
    //

    $fields = array();
    // Compulsory so..
    if (!isset($entity->fk_hostcredential)) {
      throw new Exception('hostcredential is mandatory.');
    }
    else {
      $fields['hostcredential'] = $entity->fk_hostcredential;
    }

    // Nullable so we only pass it through to create_host_order if we have it..
    if (isset($entity->fk_host)) {
      $fields['host'] = $entity->fk_host;
    }

    // Nullable so we only pass it through to create_host_order if we have it..
    if (!empty($entity->address)) {
      $fields['address'] = $entity->address;
    }
    // Compulsory so..
    if (!isset($entity->processing)) {
      throw new Exception('processing is mandatory.');
    }
    else {
      $fields['processing'] = $entity->processing;
    }
    // Compulsory so..
    if (!isset($entity->completed)) {
      throw new Exception('completed is mandatory.');
    }
    else {
      $fields['completed'] = $entity->completed;
    }
		
	//
	// Ensure that we have the 'thumbwhere_api_key' as we are going to pass that through in a context
	//
    if (variable_get('thumbwhere_api_key') == '') {
    	throw new Exception('ThumbWhere resource for \'HostOrder\' requires that variable \'thumbwhere_api_key\' is set.');
    }
    
              		
    //
    // If this hostorder entity is new....
    //
               
    if ($entity->is_new) {
                

      //
      // ... then we create the hostorder entity in ThumbWhere
      //

      $hostorder = $twAdminAPI->create_host_order($context = array(
                  'key' => variable_get('thumbwhere_api_key')
          ), $fields);
            
       if (twCanDebug()) debug($hostorder);

      //if (twCanDebug()) debug($host_order);

      $attributes = $hostorder->body->host_order->attributes();

      if (twCanDebug()) debug('newly created id = ' . $attributes['id']);

      // Assign new id.
      $entity->pk_hostorder = intval($attributes['id']);

      if (twCanDebug()) debug('$entity->pk_hostorder = ' . $entity->pk_hostorder);
    }
    else
    {
          	

      //
      // .. otherwise we update the hostorder entity in ThumbWhere
      //

      $hostorder = $twAdminAPI->update_host_order($id=$entity->pk_hostorder,$context = array(
                  'key' => variable_get('thumbwhere_api_key')
          ), $fields);
            
        
    }
          	
    
  }
}

/**
 * Implements thumbwhere_hostorder_entity_insert().
 *
 * Act on entities when inserted.
 *
 * @param $entity
 *   The entity object
 * @param $type
 *   The type of entity being inserted (i.e. node, user, comment).
 */
function thumbwhere_hostorder_entity_insert($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_hostorder') return;

}


/**
* Implements thumbwhere_hostorder_entity_delete().
*
* Act on entities when inserted.
*
* @param $entity
*   The entity object
* @param $type
*   The type of entity being deleted (i.e. node, user, comment).
*/
function thumbwhere_hostorder_entity_delete($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_hostorder') return;


}
