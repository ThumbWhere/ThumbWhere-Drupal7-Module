<?php

/**
 * @file
 * ThumbWhereHostOrder editing UI.
 *
 * We make very little use of the EntityAPI interface for this - preferring instead to use
 * views. That offers more flexibility to change a UI that will, more often than not,
 * be end-user facing.
 *
 * NOTE: This file was generated by autogen_model_implementation_drupal.xsl
 */

 
/**
 * Handle the notification for content_ingext
 */
function _thumbwhere_handle_host_order_notify_xml($notification) {

  if (twCanDebug()) debug('PROCESS NOTIFICATION');
  if (twCanDebug()) debug($notification);

  //
  // Get the operation
  //

  if (!isset($notification->attributes()->op)) {
    drupal_add_http_header('Status', '501 unable to fulfil request..');
    $err = 'We were expecting an \'op\', attribute on the \'host_order\', element.';
    print($err);
   	watchdog('thumbwhere_hostorder', $err ,array(), WATCHDOG_ERROR);
    exit(0);
  }

  $op = $notification->attributes()->op;

  if (twCanDebug()) debug('op');
  if (twCanDebug()) debug($op);

  //
  // If this is a notification then $op should be 'notify'
  //

  if (!isset($notification->attributes()->notification)) {
    drupal_add_http_header('Status', '501 unable to fulfil request..');
    $err = 'We were expecting an \'notification\', attribute on the \'host_order\', element.';
    print($err);
   	watchdog('thumbwhere_hostorder', $err ,array(), WATCHDOG_ERROR);
    exit(0);
  }

  $notification_op = $notification->attributes()->notification;

  if (twCanDebug()) debug('notification');
  if (twCanDebug()) debug($notification_op);


  //
  // Get the context
  //

  if (!isset($notification->attributes()->key)) {
    drupal_add_http_header('Status', '412 unable to fulfil request..');
    $err = 'We were expecting a \'key\', attribute on the \'host_order\', element.';
    print($err);
    watchdog('thumbwhere_hostorder', $err ,array(), WATCHDOG_ERROR);
    exit(0);
  }

  $context_key =  $notification->attributes()->key;

  //
  // Now based on the operation..
  //

  switch($notification_op)
  {
  
  
      case 'create':
      {
      		
		
		    //
		    // We want to time this
		    //
			
		    $start_time = microtime(TRUE);
			
        //
        // What resource are we creating?
        //
      
        //
        // Get the id
        //
		
        if (!isset($notification->attributes()->id)) {
          drupal_add_http_header('Status', '412 Unable to fulfil request.');
          $err = 'We were expecting an \'id\', attribute on the \'host_order\', element when we are handling a \'create\' notification..';
          print($err);
          watchdog('thumbwhere_hostorder', $err ,array(), WATCHDOG_ERROR);
          exit(0);
        }

        $id = intval($notification->attributes()->id);
		

        if (twCanDebug()) debug('id');
        if (twCanDebug()) debug($id);

      
        //
        // Get the fields
        //
          
        $entities = entity_load('thumbwhere_hostorder', array($id));

        if (twCanDebug()) debug('$entities');
        if (twCanDebug()) debug($entities);

        if (count($entities) != 0) {
          //drupal_add_http_header('Status', '409 Resource already exists.');
          $err = 'Error while servicing \'create\' notification. Resource \'host_order\' already exists with an id of \'' . $id . '\'.';
           //print($err);
          // When ORIGIN (TW-171) is sorted out then this can be re-anbled
          //watchdog('thumbwhere_hostorder', $err ,array(), WATCHDOG_ERROR);

          	
          // We act all idempotent like its all good. 'create', when it already exists, is ignored. 
          drupal_add_http_header('Content-Type', 'text/xml');
          print('<response>');
          print('<host_order op="' . $op . '" id="' . $id . '" >');
          print('<status>success</status>');
          print('</host_order>');
          print('</response>');
          exit(0);          	
          
        }

        // Getting a reference to the entity we asked for
        $entityfields = array();

        //
        // We are only updating what has been passed through.
        // No test for missing values.
        // TODO: Validate incoming values.
        //


        $entityfields['pk_hostorder'] = $id;	

        if (isset($notification->hostcredential)) {
          $entityfields['fk_hostcredential'] = intval($notification->hostcredential);
        }

        if (isset($notification->host)) {
          $entityfields['fk_host'] = intval($notification->host);
        }

        if (isset($notification->address)) {
          $entityfields['address'] = (string)($notification->address);
        }

        if (isset($notification->processing)) {
          if ((string)($notification->processing) == 'true') {
            $entityfields['processing'] = '1';
          }
          else {
            $entityfields['processing'] = '0';
          }
        }

        if (isset($notification->completed)) {
          if ((string)($notification->completed) == 'true') {
            $entityfields['completed'] = '1';
          }
          else {
            $entityfields['completed'] = '0';
          }
        }

        if (!isset($notification->attributes()->key)) {
          if (twCanDebug()) debug('$entities');
          if (twCanDebug()) debug($entities);
        }

        // We are being notified, so we do not want to ship this out again as a notification...
        // Finally write the entity.
        $entity = entity_create('thumbwhere_hostorder', $entityfields);
        $entity->do_not_propagate = TRUE;
        $entity->save();

        //
        // Send the final response
        //
        
        $duration = round(((microtime(TRUE) - $start_time) * 1000.0),3); 

        drupal_add_http_header('Content-Type', 'text/xml');
        print('<response duration="' . $duration . 'ms">');
        print('<host_order op="' . $op . '" id="' . $id . '" >');
        print('<status>success</status>');
        print('</host_order>');
        print('</response>');
        exit(0);			
		
      
      }
  
  
    case 'update':
      {
      
    		//
    		// We want to time this
    		//
    			
    		$start_time = microtime(TRUE);
      
        //
        // What resource are we updating?
        //
        
      
        //
        // Get the id
        //
		
        if (!isset($notification->attributes()->id)) {
          drupal_add_http_header('Status', '412 Unable to fulfil request.');
          $err = 'We were expecting an \'id\', attribute on the \'host_order\', element when we are performing an \'update\' notification..';
          print($err);
          watchdog('thumbwhere_hostorder', $err ,array(), WATCHDOG_ERROR);
          exit(0);
        }

        $id = intval($notification->attributes()->id);
		

        if (twCanDebug()) debug('id');
        if (twCanDebug()) debug($id);

      
        //
        // Get the fields
        //
          
        $entities = entity_load('thumbwhere_hostorder', array($id));

        if (twCanDebug()) debug('$entities');
        if (twCanDebug()) debug($entities);

        if (count($entities) == 0) {
          //drupal_add_http_header('Status', '409 No such resource exists.');
          $err = 'No such \'host_order\' resource exists with an id of \'' . $id . '\'.';
          //print($err);
          watchdog('thumbwhere_hostorder',$err,array(), WATCHDOG_ERROR);
          //exit(0);

////////////////////////////////////////////////////////////////////////
//
// ... for now we will just create the local copy
//
          watchdog('thumbwhere_hostorder','creating local version of missing entity',array(), WATCHDOG_ERROR);
		
		
		    //
		    // We want to time this
		    //
			
		    $start_time = microtime(TRUE);
			
        //
        // What resource are we creating?
        //
      
        //
        // Get the id
        //
		
        if (!isset($notification->attributes()->id)) {
          drupal_add_http_header('Status', '412 Unable to fulfil request.');
          $err = 'We were expecting an \'id\', attribute on the \'host_order\', element when we are handling a \'update\' notification..';
          print($err);
          watchdog('thumbwhere_hostorder', $err ,array(), WATCHDOG_ERROR);
          exit(0);
        }

        $id = intval($notification->attributes()->id);
		

        if (twCanDebug()) debug('id');
        if (twCanDebug()) debug($id);

      
        //
        // Get the fields
        //
          
        $entities = entity_load('thumbwhere_hostorder', array($id));

        if (twCanDebug()) debug('$entities');
        if (twCanDebug()) debug($entities);

        if (count($entities) != 0) {
          //drupal_add_http_header('Status', '409 Resource already exists.');
          $err = 'Error while servicing \'update\' notification. Resource \'host_order\' already exists with an id of \'' . $id . '\'.';
           //print($err);
          // When ORIGIN (TW-171) is sorted out then this can be re-anbled
          //watchdog('thumbwhere_hostorder', $err ,array(), WATCHDOG_ERROR);

          	
          // We act all idempotent like its all good. 'create', when it already exists, is ignored. 
          drupal_add_http_header('Content-Type', 'text/xml');
          print('<response>');
          print('<host_order op="' . $op . '" id="' . $id . '" >');
          print('<status>success</status>');
          print('</host_order>');
          print('</response>');
          exit(0);          	
          
        }

        // Getting a reference to the entity we asked for
        $entityfields = array();

        //
        // We are only updating what has been passed through.
        // No test for missing values.
        // TODO: Validate incoming values.
        //


        $entityfields['pk_hostorder'] = $id;	

        if (isset($notification->hostcredential)) {
          $entityfields['fk_hostcredential'] = intval($notification->hostcredential);
        }

        if (isset($notification->host)) {
          $entityfields['fk_host'] = intval($notification->host);
        }

        if (isset($notification->address)) {
          $entityfields['address'] = (string)($notification->address);
        }

        if (isset($notification->processing)) {
          if ((string)($notification->processing) == 'true') {
            $entityfields['processing'] = '1';
          }
          else {
            $entityfields['processing'] = '0';
          }
        }

        if (isset($notification->completed)) {
          if ((string)($notification->completed) == 'true') {
            $entityfields['completed'] = '1';
          }
          else {
            $entityfields['completed'] = '0';
          }
        }

        if (!isset($notification->attributes()->key)) {
          if (twCanDebug()) debug('$entities');
          if (twCanDebug()) debug($entities);
        }

        // We are being notified, so we do not want to ship this out again as a notification...
        // Finally write the entity.
        $entity = entity_create('thumbwhere_hostorder', $entityfields);
        $entity->do_not_propagate = TRUE;
        $entity->save();

        //
        // Send the final response
        //
        
        $duration = round(((microtime(TRUE) - $start_time) * 1000.0),3); 

        drupal_add_http_header('Content-Type', 'text/xml');
        print('<response duration="' . $duration . 'ms">');
        print('<host_order op="' . $op . '" id="' . $id . '" >');
        print('<status>success</status>');
        print('</host_order>');
        print('</response>');
        exit(0);			
		
//
//
//
////////////////////////////////////////////////////////////////////////
        }

        // Getting a reference to the entity we asked for
        $entity = $entities[$id];

        //
        // We are only updating what has been passed through.
        // No test for missing values.
        // TODO: Validate incoming values.
        //


        if (isset($notification->hostcredential)) {
          $entity->fk_hostcredential = intval($notification->hostcredential);
        }

        if (isset($notification->host)) {
          $entity->fk_host = intval($notification->host);
        }

        if (isset($notification->address)) {
          $entity->address = (string)($notification->address);
        }

        if (isset($notification->processing)) {
          if ((string)($notification->processing) == 'true') {
            $entity->processing = '1';
          }
          else {
            $entity->processing = '0';
          }
        }

        if (isset($notification->completed)) {
          if ((string)($notification->completed) == 'true') {
            $entity->completed = '1';
          }
          else {
            $entity->completed = '0';
          }
        }

        if (!isset($notification->attributes()->key)) {
          if (twCanDebug()) debug('$entities');
          if (twCanDebug()) debug($entities);
        }

        // We are being notified, so we do not want to ship this out again as a notification...
        $entity->do_not_propagate = TRUE;

        // Finally write the entity.
        entity_save('thumbwhere_hostorder', $entity);

        //
        // Send the final response
        //
        
        $duration = round(((microtime(TRUE) - $start_time) * 1000.0),3);

        drupal_add_http_header('Content-Type', 'text/xml');
        print('<response  duration="' . $duration . '" >');
        print('<host_order op="' . $op . '" notification="' . $notification_op . '" id="' . $id . '" >');
        print('<status>success</status>');
        print('</host_order>');
        print('</response>');
        exit(0);
      }

    default:
      {
        drupal_add_http_header('Status', '501 Unable to fulfil request. Unhandled operation \'' . $op . '\'.');
        print('We don\'t handle the \'' . $notification_op . '\', notification operation on the \'host_order\', element.');
        exit(0);
      }
  }
}



/**
 * Menu callback: React to a notification for hostorder.
 */
function _thumbwhere_host_order_notify() {

  if (twCanDebug()) {
    watchdog('thumbwhere_host_order', 'incoming notification.');
  }

  $numargs = func_num_args();
  $arg_list = func_get_args();

  
	if (twCanDebug()) debug('$numargs  = ' . $numargs);
	
	if (twCanDebug()) debug('$arg_list = ' . $arg_list);
	
	if (twCanDebug()) debug('$_REQUEST');
	if (twCanDebug()) debug($_REQUEST);
	
	if (twCanDebug()) debug('$_GET');
	if (twCanDebug()) debug($_GET);
	
	if (twCanDebug()) debug('$_POST');
	if (twCanDebug()) debug($_POST);
	
	//if (twCanDebug()) debug('$HTTP_RAW_POST_DATA');
	//if (twCanDebug()) debug($HTTP_RAW_POST_DATA);
	
	if (twCanDebug()) debug('$_SERVER');
	if (twCanDebug()) debug($_SERVER);
	
	//if (twCanDebug()) debug('$GLOBALS');
	//if (twCanDebug()) debug($GLOBALS);

  

  if (variable_get('thumbwhere_api_log_debug', 0) == 1) {
    foreach ($arg_list as $arg) {
      if (twCanDebug()) debug('$arg  = ' . $arg);
    }
  }


  //////////////////////////////////////////////////
  //
  //

  switch ($_SERVER['REQUEST_METHOD']) {

    case 'POST': {
        if ($numargs == 1) {
          if ($arg_list[0] == 'host_order') {

            //
            // Get the POST data as XML
            //

            $post_xml = new SimpleXMLElement('php://input', null, true);
            if (twCanDebug()) debug('$post_xml');
            if (twCanDebug()) debug($post_xml);

            if (twCanDebug()) debug('$post_xml->getName()');
            if (twCanDebug()) debug($post_xml->getName());

            // We expect 'action' as the root element. TODO: I would rather this was the more generic 'request'
            if ($post_xml->getName() != 'action') {
              drupal_add_http_header('Status', '404 not found.');
              print('We were expecting \'action\', as the root element, not \'' . $post_xml->getName() . '\'.');
              exit(0);
            }

            /*
             *  SimpleXMLElement::__set_state(array(
             *      'host_order' =>
             *      SimpleXMLElement::__set_state(array(
             *       '@attributes' =>
             *      array (
             *        'key' => '8e9ff318-3cd7-4593-99de-4c37c0fb335a',
             *        'op' => 'create',
             *      ),
             *    )),
             *  ))
             */


            // We expect 'action' as the root element.
            if (!isset($post_xml->host_order)) {
              drupal_add_http_header('Status', '404 not found.');
              print('We were expecting a \'host_order\', element in the action payload.');
              exit(0);
            }

            // Handle the incoming notification for content ingest
            _thumbwhere_handle_host_order_notify_xml($post_xml->host_order);

          }
          else {
            drupal_add_http_header('Status', '404 not found.');
            print('Invalid resource');
            exit(0);
          }
        }
        else {
          drupal_add_http_header('Status', '404 not found.');
          print('Invalid notification request url format.');
          exit(0);
        }
      }
      break;

    case 'GET': {

        if ($numargs == 1) {

          // If we are returning the index.xml file for the webservice.
          if ($arg_list[0] == 'index.xml') {
            // return index.xml
            drupal_add_http_header('Content-Type', 'text/xml');
            readfile(dirname(__FILE__) . '/index.xml');
            exit(0);
          }
          else {
            drupal_add_http_header('Status', '404 not found.');
            print('File not found.');
            exit(0);
          }
        }
        else {
          drupal_add_http_header('Status', '404 not found.');
          print('Invalid notification request url format.');
          exit(0);
        }
      }
      break;

    default: {
        drupal_add_http_header('Status', '404 not found.');
        exit(0);
      }
      break;
  }

  //
  //
  ////////////////////////////////////////////////////////////////////////////////////
 
}





/**
 * HostOrder UI controller.
 */
class ThumbWhereHostOrderUIController extends EntityDefaultUIController {

  /**
   * Overrides hook_menu() defaults. Main reason for doing this is that
   * parent class hook_menu() is optimized for entity type administration.
   */
  public function hook_menu() {

    $items = array();
    
    // Add a notification page...
    $items['thumbwhere/host_order/notify'] = array(
        'title' => 'Notifications Callback for "HostOrder" Entity',
        'page callback' => '_thumbwhere_host_order_notify',
        'access arguments' => array(
            'send thumbwhere hostorder notifications'
        ),
        'file' => 'thumbwhere_hostorder.admin.inc',
        'file path' => drupal_get_path('module', $this->entityInfo['module'])
    );    
    
    $id_count = count(explode('/', $this->path));
    $wildcard = isset($this->entityInfo['admin ui']['menu wildcard']) ? $this->entityInfo['admin ui']['menu wildcard'] : '%' . $this->entityType;

    $items[$this->path] = array(
      'title' => 'HostOrder',
      'description' => 'Add edit and update thumbwhere_hostorders.',
      'page callback' => 'system_admin_menu_block_page',
      'access arguments' => array('access administration pages'),
      'file path' => drupal_get_path('module', 'system'),
      'file' => 'system.admin.inc',
    );

    // Change the overview menu type for the list of thumbwhere_hostorders.
    $items[$this->path]['type'] = MENU_LOCAL_TASK;

    // Change the add page menu to multiple types of entities
    $items[$this->path . '/add'] = array(
      //'title' => 'Add a HostOrder',
      'title' => 'Add',
      'description' => 'Add a new HostOrder',
      'page callback' => 'thumbwhere_hostorder_form_wrapper',
      'page arguments' => array(thumbwhere_hostorder_create(array('type' => 'thumbwhere_hostorder'))),
      'access callback' => 'thumbwhere_hostorder_access',
      'access arguments' => array('edit', 'edit ' . 'thumbwhere_hostorder'),
      'file' => 'thumbwhere_hostorder.admin.inc',
      'file path' => drupal_get_path('module', $this->entityInfo['module'])
    );


/*
    // Change the add page menu to multiple types of entities
    $items[$this->path . '/add'] = array(
      //'title' => 'Add a HostOrder',
      'title' => 'Add',
	  'description' => 'Add a new HostOrder',
      'page callback'  => 'thumbwhere_hostorder_add_page',
      'access callback'  => 'thumbwhere_hostorder_access',
      'access arguments' => array('edit'),
      'type' => MENU_NORMAL_ITEM,
      'weight' => 20,
      'file' => 'thumbwhere_hostorder.admin.inc',
      'file path' => drupal_get_path('module', $this->entityInfo['module'])

    );
*/    
/*
    $items[$this->path . '/add/' . 'thumbwhere_hostorder'] = array(
      'title' => 'Add ' . 'ThumbWhereHostOrder',
      'page callback' => 'thumbwhere_hostorder_form_wrapper',
      'page arguments' => array(thumbwhere_hostorder_create(array('type' => 'thumbwhere_hostorder'))),
      'access callback' => 'thumbwhere_hostorder_access',
      'access arguments' => array('edit', 'edit ' . 'thumbwhere_hostorder'),
      'file' => 'thumbwhere_hostorder.admin.inc',
      'file path' => drupal_get_path('module', $this->entityInfo['module'])
    );
*/
    // Loading and editing thumbwhere_hostorder entities
    $items[$this->path . '/thumbwhere_hostorder/' . $wildcard] = array(
      'page callback' => 'thumbwhere_hostorder_form_wrapper',
      'page arguments' => array($id_count + 1),
      'access callback' => 'thumbwhere_hostorder_access',
      'access arguments' => array('edit', $id_count + 1),
      'weight' => 0,
      'context' => MENU_CONTEXT_PAGE | MENU_CONTEXT_INLINE,
      'file' => 'thumbwhere_hostorder.admin.inc',
      'file path' => drupal_get_path('module', $this->entityInfo['module'])
    );
    $items[$this->path . '/thumbwhere_hostorder/' . $wildcard . '/edit'] = array(
      'title' => 'Edit',
      'type' => MENU_DEFAULT_LOCAL_TASK,
      'weight' => -10,
      'context' => MENU_CONTEXT_PAGE | MENU_CONTEXT_INLINE,
    );

    $items[$this->path . '/thumbwhere_hostorder/' . $wildcard . '/delete'] = array(
      'title' => 'Delete',
      'page callback' => 'thumbwhere_hostorder_delete_form_wrapper',
      'page arguments' => array($id_count + 1),
      'access callback' => 'thumbwhere_hostorder_access',
      'access arguments' => array('edit', $id_count + 1),
      'type' => MENU_LOCAL_TASK,
      'context' => MENU_CONTEXT_INLINE,
      'weight' => 10,
      'file' => 'thumbwhere_hostorder.admin.inc',
      'file path' => drupal_get_path('module', $this->entityInfo['module'])
    );

    // Menu item for viewing thumbwhere_hostorders
    $items['thumbwhere_hostorder/' . $wildcard] = array(
      //'title' => 'Title',
      'title callback' => 'thumbwhere_hostorder_page_title',
      'title arguments' => array(1),
      'page callback' => 'thumbwhere_hostorder_page_view',
      'page arguments' => array(1),
      'access callback' => 'thumbwhere_hostorder_access',
      'access arguments' => array('view', 1),
      'type' => MENU_CALLBACK,
    );
    return $items;
  }


  /**
   * Create the markup for the add ThumbWhereHostOrder Entities page within the class
   * so it can easily be extended/overriden.
   */
  public function addPage() {
    $item = menu_get_item();
    $content = system_admin_menu_block($item);

    if (count($content) == 1) {
      $item = array_shift($content);
      drupal_goto($item['href']);
    }

    return theme('thumbwhere_hostorder_add_list', array('content' => $content));
  }

}


/**
 * Form callback wrapper: create or edit a thumbwhere_hostorder.
 *
 * @param $thumbwhere_hostorder
 *   The thumbwhere_hostorder object being edited by this form.
 *
 * @see thumbwhere_hostorder_edit_form()
 */
function thumbwhere_hostorder_form_wrapper($thumbwhere_hostorder) {
  // Add the breadcrumb for the form's location.
  thumbwhere_hostorder_set_breadcrumb();
  return drupal_get_form('thumbwhere_hostorder_edit_form', $thumbwhere_hostorder);
}


/**
 * Form callback wrapper: delete a thumbwhere_hostorder.
 *
 * @param $thumbwhere_hostorder
 *   The thumbwhere_hostorder object being edited by this form.
 *
 * @see thumbwhere_hostorder_edit_form()
 */
function thumbwhere_hostorder_delete_form_wrapper($thumbwhere_hostorder) {
  // Add the breadcrumb for the form's location.
  //thumbwhere_hostorder_set_breadcrumb();
  return drupal_get_form('thumbwhere_hostorder_delete_form', $thumbwhere_hostorder);
}


/**
 * Form callback: create or edit a thumbwhere_hostorder.
 *
 * @param $thumbwhere_hostorder
 *   The thumbwhere_hostorder object to edit or for a create form an empty thumbwhere_hostorder object
 *     with only a thumbwhere_hostorder type defined.
 */
function thumbwhere_hostorder_edit_form($form, &$form_state, $thumbwhere_hostorder) {

  // Add the default field elements.
  $form['fk_hostcredential'] = array(
    '#type' => 'textfield',
    '#title' => t('ThumbWhereHostOrder fk_hostcredential'),
    '#default_value' => isset($thumbwhere_hostorder->fk_hostcredential) ? $thumbwhere_hostorder->fk_hostcredential : '',
    //'#maxlength' => 255,
   /// '#required' => TRUE,
    '#weight' => -5,
    '#autocomplete_path' => 'entity-autocomplete/thumbwhere_hostcredential',
  );
  // Add the default field elements.
  $form['fk_host'] = array(
    '#type' => 'textfield',
    '#title' => t('ThumbWhereHostOrder fk_host'),
    '#default_value' => isset($thumbwhere_hostorder->fk_host) ? $thumbwhere_hostorder->fk_host : '',
    //'#maxlength' => 255,
   /// '#required' => TRUE,
    '#weight' => -5,
    '#autocomplete_path' => 'entity-autocomplete/thumbwhere_host',
  );
  // Add the default field elements.
    $form['address'] = array(
    '#type' => 'textfield',
    '#title' => t('ThumbWhereHostOrder address'),
    '#default_value' => isset($thumbwhere_hostorder->address) ? $thumbwhere_hostorder->address : '',
    // '#maxlength' => 255,
   // '#required' => TRUE,
    '#weight' => -5,
  );
  // Add the default field elements.
    $form['processing'] = array(
    '#type' => 'checkbox',
    '#title' => t('ThumbWhereHostOrder processing'),
    '#default_value' => isset($thumbwhere_hostorder->processing) ? $thumbwhere_hostorder->processing : 0,
    // '#maxlength' => 255,
   // '#required' => TRUE,
    '#weight' => -5,
  );
  // Add the default field elements.
    $form['completed'] = array(
    '#type' => 'checkbox',
    '#title' => t('ThumbWhereHostOrder completed'),
    '#default_value' => isset($thumbwhere_hostorder->completed) ? $thumbwhere_hostorder->completed : 0,
    // '#maxlength' => 255,
   // '#required' => TRUE,
    '#weight' => -5,
  );



  $form['data']['#tree'] = TRUE;

  /*
  $form['data']['sample_data'] = array(
    '#type' => 'checkbox',
    '#title' => t('An interesting thumbwhere_hostorder switch'),
    '#default_value' => isset($thumbwhere_hostorder->data['sample_data']) ? $thumbwhere_hostorder->data['sample_data'] : 1,
  );
  */


  // Add the field related form elements.
  $form_state['thumbwhere_hostorder'] = $thumbwhere_hostorder;
  field_attach_form('thumbwhere_hostorder', $thumbwhere_hostorder, $form, $form_state);

  $form['actions'] = array(
    '#type' => 'container',
    '#attributes' => array('class' => array('form-actions')),
    '#weight' => 400,
  );

  // We add the form's #submit array to this button along with the actual submit
  // handler to preserve any submit handlers added by a form callback_wrapper.
  $submit = array();

  if (!empty($form['#submit'])) {
    $submit += $form['#submit'];
  }

  $form['actions']['submit'] = array(
    '#type' => 'submit',
    '#value' => t('Save thumbwhere_hostorder'),
    '#submit' => $submit + array('thumbwhere_hostorder_edit_form_submit'),
  );

  // Do we show the delete button?
  if (!empty($thumbwhere_hostorder->pk_hostorder)) {
    $form['actions']['delete'] = array(
      '#type' => 'submit',
      '#value' => t('Delete thumbwhere_hostorder'),
      '#suffix' => l(t('Cancel'), 'admin/thumbwhere/thumbwhere_hostorders'),
      '#submit' => $submit + array('thumbwhere_hostorder_form_submit_delete'),
      '#weight' => 45,
    );
  }

  // We append the validate handler to #validate in case a form callback_wrapper
  // is used to add validate handlers earlier.
  $form['#validate'][] = 'thumbwhere_hostorder_edit_form_validate';
  return $form;
}


/**
 * Form API validate callback for the thumbwhere_hostorder form
 */
function thumbwhere_hostorder_edit_form_validate(&$form, &$form_state) {
   
  //if (twCanDebug()) debug($form);   
  //if (twCanDebug()) debug($form_state);


  // No validation for pk


  // Convert fk autocomplete for fk_hostcredential
  $value = $form['fk_hostcredential']['#value'];
  // If we have something, escape the auto-completion encoding.
  if (!empty($value)) { 
    $value = entity_autocomplete_get_id($value);
    form_set_value($form['fk_hostcredential'], $value,$form_state);
  }
  //if (twCanDebug()) debug($form['fk_hostcredential']);
   if (twCanDebug()) debug('fk_hostcredential = ' . $value);
  // Validate fk fk_hostcredential
  if (empty($value)) {
    form_set_error('fk_hostcredential', t('Validation error, \'fk_hostcredential\' is mandatory1.'));
    // throw new Exception('Field \'fk_hostcredential\' in is mandatory');
  }

  // Convert fk autocomplete for fk_host
  $value = $form['fk_host']['#value'];
  // If we have something, escape the auto-completion encoding.
  if (!empty($value)) { 
    $value = entity_autocomplete_get_id($value);
    form_set_value($form['fk_host'], $value,$form_state);
  }
  //if (twCanDebug()) debug($form['fk_host']);
   if (twCanDebug()) debug('fk_host = ' . $value);
  // Validate fk fk_host


  // Validate normaladdress
  $value = $form['address']['#value'];
  // If we have no default value then we don't care much for checking for emptyness.


  // Validate normalprocessing
  $value = $form['processing']['#value'];
  // If we have no default value then we don't care much for checking for emptyness.
  if (empty($value)) {
    form_set_error('processing', t('Validation error, \'processing\' is mandatory2.'));
    //throw new Exception('Field \'processing\' in is mandatory.');
  }

  // Validate normalcompleted
  $value = $form['completed']['#value'];
  // If we have no default value then we don't care much for checking for emptyness.
  if (empty($value)) {
    form_set_error('completed', t('Validation error, \'completed\' is mandatory2.'));
    //throw new Exception('Field \'completed\' in is mandatory.');
  }

              

  //form_set_value( array('#parents' => array('array_key_parent', 'array_key_to_replace')) , $value, $form_state);

   
   
  $thumbwhere_hostorder = $form_state['thumbwhere_hostorder'];

  // Notify field widgets to validate their data.
  field_attach_form_validate('thumbwhere_hostorder', $thumbwhere_hostorder, $form, $form_state);
    
    
  
}


/**
 * Form API submit callback for the thumbwhere_hostorder form.
 *
 * @todo remove hard-coded link
 */
function thumbwhere_hostorder_edit_form_submit(&$form, &$form_state) {

  $thumbwhere_hostorder = entity_ui_controller('thumbwhere_hostorder')->entityFormSubmitBuildEntity($form, $form_state);
  // Save the thumbwhere_hostorder and go back to the list of thumbwhere_hostorders

  // Add in created and changed times.
  if ($thumbwhere_hostorder->is_new = isset($thumbwhere_hostorder->is_new) ? $thumbwhere_hostorder->is_new : 0) {
    $thumbwhere_hostorder->created = time();
  }

  $thumbwhere_hostorder->changed = time();

  $thumbwhere_hostorder->save();
  $form_state['redirect'] = 'admin/thumbwhere/thumbwhere_hostorders';
}

/**
 * Form API submit callback for the delete button.
 *
 * @todo Remove hard-coded path
 */
function thumbwhere_hostorder_form_submit_delete(&$form, &$form_state) {
  $form_state['redirect'] = 'admin/thumbwhere/thumbwhere_hostorders/thumbwhere_hostorder/' . $form_state['thumbwhere_hostorder']->pk_hostorder . '/delete';
}





/**
 * Form callback: confirmation form for deleting a thumbwhere_hostorder.
 *
 * @param $thumbwhere_hostorder
 *   The thumbwhere_hostorder to delete
 *
 * @see confirm_form()
 */
function thumbwhere_hostorder_delete_form($form, &$form_state, $thumbwhere_hostorder) {
  $form_state['thumbwhere_hostorder'] = $thumbwhere_hostorder;

  $form['#submit'][] = 'thumbwhere_hostorder_delete_form_submit';

  $form = confirm_form($form,
    t('Are you sure you want to delete thumbwhere_hostorder %name?', array('%name' => $thumbwhere_hostorder->pk_hostorder)),
    'admin/thumbwhere/thumbwhere_hostorders/thumbwhere_hostorder',
    '<p>' . t('This action cannot be undone.') . '</p>',
    t('Delete'),
    t('Cancel'),
    'confirm'
  );

  return $form;
}

/**
 * Submit callback for thumbwhere_hostorder_delete_form
 */
function thumbwhere_hostorder_delete_form_submit($form, &$form_state) {
  $thumbwhere_hostorder = $form_state['thumbwhere_hostorder'];

  thumbwhere_hostorder_delete($thumbwhere_hostorder);

  drupal_set_message(t('The thumbwhere_hostorder %name has been deleted.', array('%name' => $thumbwhere_hostorder->pk_hostorder)));
  watchdog('thumbwhere_hostorder', 'Deleted thumbwhere_hostorder %name.', array('%name' => $thumbwhere_hostorder->pk_hostorder));

  $form_state['redirect'] = 'admin/thumbwhere/thumbwhere_hostorders';
}



/**
 * Page to add ThumbWhereHostOrder Entities.
 *
 * @todo Pass this through a proper theme function
 */
function thumbwhere_hostorder_add_page() {
  $controller = entity_ui_controller('thumbwhere_hostorder');
  return $controller->addPage();
}


/**
 * Displays the list of available thumbwhere_hostorder types for thumbwhere_hostorder creation.
 *
 * @ingroup themeable
 */
function theme_thumbwhere_hostorder_add_list($variables) {


  //$content = $variables['content'];
  $output = '';
  /*
  if ($content) {
    $output = '<dl class="thumbwhere_hostorder-type-list">';
    foreach ($content as $item) {
      $output .= '<dt>' . l($item['title'], $item['href']) . '</dt>';
      $output .= '<dd>' . filter_xss_admin($item['description']) . '</dd>';
    }
    $output .= '</dl>';
  }
  else {
    if (user_access('administer thumbwhere_hostorder types')) {
      $output = '<p>' . t('ThumbWhereHostOrder Entities cannot be added because you have not created any thumbwhere_hostorder types yet. Go to the <a href="@create-thumbwhere_hostorder-type">thumbwhere_hostorder type creation page</a> to add a new thumbwhere_hostorder type.', array('@create-thumbwhere_hostorder-type' => url('admin/structure/thumbwhere_hostorder_types/add'))) . '</p>';
    }
    else {
      $output = '<p>' . t('No thumbwhere_hostorder types have been created yet for you to use.') . '</p>';
    }
  }
  */
  return $output;
}





/**
 * Sets the breadcrumb for administrative thumbwhere_hostorder pages.
 */
function thumbwhere_hostorder_set_breadcrumb() {
  $breadcrumb = array(
    l(t('Home'), '<front>'),
    l(t('Administration'), 'admin'),
    l(t('HostOrder'), 'admin/content'),
    l(t('HostOrder'), 'admin/thumbwhere/thumbwhere_hostorders'),
  );

  drupal_set_breadcrumb($breadcrumb);
}



