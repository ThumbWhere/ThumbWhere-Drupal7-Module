<?php

// ******************************************************************
// This file was generated by autogen_model_implementation_drupal.xsl
// ******************************************************************

// Include the ThumbWhere SDK
require_once(dirname(__FILE__) . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . '..' . DIRECTORY_SEPARATOR . 'dependencies' . DIRECTORY_SEPARATOR . 'tw' . DIRECTORY_SEPARATOR . 'sdk.class.php');

/**
 * @file
 * Module for the ThumbWhere HostRoleType Entity
 *
 *
 * NOTE: This has been autogenerated.
 * 
 */

/**
 * Implement hook_entity_info().
 *
 * We define two entities here - the actual entity that will hold our domain
 * specific information and an entity that holds information about the different
 * types of entities. See here: http://drupal.org/node/977380 for a discussion on this
 * choice.
 */
function thumbwhere_hostroletype_entity_info() {
  $return['thumbwhere_hostroletype'] = array(

    // The human-readable name of the type.
    'label' => t('ThumbWhereHostRoleType'),

    // (used by DrupalDefaultEntityController) The name of the entity type's base table.
    'base table' => 'thumbwhere_hostroletype',

    // fieldable: Set to TRUE if you want your entity type to accept fields being attached to it.
    'fieldable' => TRUE,

  // Declare the endty keys..
    'entity keys' => array(
      'id' => 'pk_hostroletype',
      'label' => 'name',
    ),

    // A function taking an entity and an entity type as arguments and returning the label of the entity.
    // The entity label is the main string associated with an entity; for example, the title of a node or
    // the subject of a comment. If there is an entity object property that defines the label, use the
    // 'label' element of the 'entity keys' return value component to provide this information (see below).
    // If more complex logic is needed to determine the label of an entity, you can instead specify a callback
    // function here, which will be called to determine the entity label. See also the entity_label()
    // function, which implements this logic.
    'label callback' => 'entity_class_label',

    // A function taking an entity as argument and returning the uri elements of the entity, e.g. 'path' and 'options'.
    // The actual entity uri can be constructed by passing these elements to url().
    'uri callback' => 'entity_class_uri',

    //
    // Entity API Components
    //

    // The module whwre we can find our classes
    'module' => 'thumbwhere_hostroletype',

    // The entity class and controller class extend the classes provided by the Entity API
    'entity class' => 'ThumbWhereHostRoleType',

    // Which Entity API contoller
    'controller class' => 'ThumbWhereHostRoleTypeController',

    // The information below is used by the ThumbWhereHostRoleTypeUIController (which extends the EntityDefaultUIController)
    'admin ui' => array(
      'path' => 'admin/thumbwhere/thumbwhere_hostroletypes',
      'file' => 'thumbwhere_hostroletype.admin.inc',
      'controller class' => 'ThumbWhereHostRoleTypeUIController',
      'menu wildcard' => '%thumbwhere_hostroletype',
    ),
  );

  return $return;
}


/**
 * Implements hook_permission().
 */
function thumbwhere_hostroletype_permission() {

  // We set up permisssions to manage entity types, manage all entities and the
  // permissions for each individual entity
  $permissions = array(
    'administer thumbwhere_hostroletypes' => array(
      'title' => t('Administer thumbwhere_hostroletypes'),
      'description' => t('Edit and delete all thumbwhere_hostroletypes'),
    ),
  'send thumbwhere hostroletype notifications' => array(
          'title' => t('Send ThumbWhere Notifications'),
          'description' => t('Allow the caller to call the ThumbWhere HostRoleType Entity Notifications Endpoint.'),
          'restrict access' => TRUE,
      ),    
  );

  return $permissions;
}


/**
 * Determines whether the given user has access to a thumbwhere_hostroletype.
 *
 * @param $op
 *   The operation being performed. One of 'view', 'update', 'create', 'delete'
 *   or just 'edit' (being the same as 'create' or 'update').
 * @param $thumbwhere_hostroletype
 *   Optionally a thumbwhere_hostroletype or a thumbwhere_hostroletype type to check access for. If nothing is
 *   given, access for all thumbwhere_hostroletypes is determined.
 * @param $account
 *   The user to check for. Leave it to NULL to check for the global user.
 * @return boolean
 *   Whether access is allowed or not.
 */
function thumbwhere_hostroletype_access($op, $thumbwhere_hostroletype = NULL, $account = NULL) {

  //if (user_access('administer thumbwhere_hostroletypes', $account)) {
  //  return TRUE;
 // }

  //return FALSE;
  return TRUE;
}




/**
 * Fetch a thumbwhere_hostroletype object. Make sure that the wildcard you choose
 * in the thumbwhere_hostroletype entity definition fits the function name here.
 *
 * @param $thumbwhere_hostroletype_id
 *   Integer specifying the thumbwhere_hostroletype id.
 * @param $reset
 *   A boolean indicating that the internal cache should be reset.
 * @return
 *   A fully-loaded $thumbwhere_hostroletype object or FALSE if it cannot be loaded.
 *
 * @see thumbwhere_hostroletype_load_multiple()
 */
function thumbwhere_hostroletype_load($thumbwhere_hostroletype_id, $reset = FALSE) {

  $thumbwhere_hostroletypes = entity_load('thumbwhere_hostroletype', array($thumbwhere_hostroletype_id), array(), $reset);

  return reset($thumbwhere_hostroletypes);
}


/**
 * Deletes a thumbwhere_hostroletype.
 */
function thumbwhere_hostroletype_delete(ThumbWhereHostRoleType $thumbwhere_hostroletype) {

  $thumbwhere_hostroletype->delete();
}

/**
 * Create a thumbwhere_hostroletype object.
 */
function thumbwhere_hostroletype_create($values = array()) {

  return entity_get_controller('thumbwhere_hostroletype')->create($values);
}


/**
 * URI callback for thumbwhere_hostroletypes
 */
function thumbwhere_hostroletype_uri(ThumbWhereHostRoleType $thumbwhere_hostroletype) {

  return array(
    'path' => 'thumbwhere_hostroletype/' . $thumbwhere_hostroletype->pk_hostroletype,
  );
}


/**
 * Menu title callback for showing individual entities
 */
function thumbwhere_hostroletype_page_title(ThumbWhereHostRoleType $thumbwhere_hostroletype) {

	return $thumbwhere_hostroletype->name;
}


/**
 * Sets up content to show an individual thumbwhere_hostroletype
 * @todo - get rid of drupal_set_title();
 */
function thumbwhere_hostroletype_page_view($thumbwhere_hostroletype, $view_mode = 'full') {



  $controller = entity_get_controller('thumbwhere_hostroletype');
  $content = $controller->view(array($thumbwhere_hostroletype->pk_hostroletype => $thumbwhere_hostroletype));
  drupal_set_title($thumbwhere_hostroletype->name);
  return $content;
}


/**
 * Implements hook_views_api().
 */
function thumbwhere_hostroletype_views_api() {



  return array(
    'api' => 3,
    'path' => drupal_get_path('module', 'thumbwhere_hostroletype') . '/views',
  );
}


/**
 * Implement hook_theme().
 */
function thumbwhere_hostroletype_theme() {



  return array(
    'thumbwhere_hostroletype_add_list' => array(
      'variables' => array('content' => array()),
      'file' => 'thumbwhere_hostroletype.admin.inc',
    ),
    'thumbwhere_hostroletype' => array(
      'render element' => 'elements',
      'template' => 'thumbwhere_hostroletype',
    )
  );
}


/**
 * Implements hook_menu_local_tasks_alter().
 */
function thumbwhere_hostroletype_menu_local_tasks_alter(&$data, $router_item, $root_path) {

  // Add action link 'admin/structure/thumbwhere_hostroletypes/add' on 'admin/structure/thumbwhere_hostroletypes'.
  if ($root_path == 'admin/thumbwhere/thumbwhere_hostroletypes') {
    $item = menu_get_item('admin/thumbwhere/thumbwhere_hostroletypes/add');
    if ($item['access']) {
      $data['actions']['output'][] = array(
        '#theme' => 'menu_local_action',
        '#link' => $item,
      );
    }
  }
}


/**
 * The class used for thumbwhere_hostroletype entities
 */
class ThumbWhereHostRoleType extends Entity {

  public function __construct($values = array()) {
    parent::__construct($values, 'thumbwhere_hostroletype');
  }

  protected function defaultLabel() {
    return $this->name;
  }

  protected function defaultUri() {
    return array('path' => 'thumbwhere_hostroletype/' . $this->pk_hostroletype);
  }


}



/**
 * The Controller for ThumbWhereHostRoleType entities
 */
class ThumbWhereHostRoleTypeController extends EntityAPIController {
  public function __construct($entityType) {
    parent::__construct($entityType);
  }
}



/**
* Implements hook_entity_presave().
*
* Act on an HostRoleType entity before it is about to be created or updated.
*
* @param $entity
*   The entity object.
* @param $entity_type
*   The type of entity being saved (i.e. node, user, comment).
*/
function thumbwhere_hostroletype_entity_presave($entity, $entity_type) {

  // We only look after what we want...
  if ($entity_type == 'thumbwhere_hostroletype')  {
  	
 	/////////////////////////////////////////////////////
    //
    // Should we not propagate this to ThumbWhere? 
    //
    if (isset($entity->do_not_propagate)) {
      if ($entity->do_not_propagate) {
        if (twCanDebug()) {
          if ($entity->is_new) {
            if (twCanDebug()){
              debug("DO NOT PROPAGATE 'hostroletype' presave *CREATE* not being sent to ThumbWhere server.");
          	}
          }
          else {
            if (twCanDebug()) {
              debug("DO NOT PROPAGATE 'hostroletype' presave *UPDATE* not being sent to ThumbWhere server.");
            }
          }
        }

        // To cause an infinite loop of updates, comment out this line.
        return;
      }
      else if (twCanDebug()) {
        debug("OK PROAOGATE 'hostroletype' presave to ThumbWhere server.");
      }
    }
    else if (twCanDebug()) {
      debug("DO NOT PROPAGATE IS NOT SET");
    }
    //
    //
    ////////////////////////////////////////////////////////  	

  
    // .. then we create a new thumbwhere_hostroletype entity...
      // Instantiate the ThumbWhereContent class

    $twAdminAPI = new ThumbWhereAPIAdmin();

    //if (twCanDebug()) debug($twContentAPI);

    //if (twCanDebug()) debug($entity);

    //
    // Collate and validate our fields
    //

    $fields = array();
    // Compulsory so..
    if (empty($entity->name)) {
      throw new Exception('name is mandatory.');
    }
    else {
      $fields['name'] = $entity->name;
    }

    // Nullable so we only pass it through to create_host_role_type if we have it..
    if (!empty($entity->config_xsl)) {
      $fields['config_xsl'] = $entity->config_xsl;
    }
		
	//
	// Ensure that we have the 'thumbwhere_api_key' as we are going to pass that through in a context
	//
    if (variable_get('thumbwhere_api_key') == '') {
    	throw new Exception('ThumbWhere resource for \'HostRoleType\' requires that variable \'thumbwhere_api_key\' is set.');
    }
    
              		
    //
    // If this hostroletype entity is new....
    //
               
    if ($entity->is_new) {
                

      //
      // ... then we create the hostroletype entity in ThumbWhere
      //

      $hostroletype = $twAdminAPI->create_host_role_type($context = array(
                  'key' => variable_get('thumbwhere_api_key')
          ), $fields);
            
       if (twCanDebug()) debug($hostroletype);

      //if (twCanDebug()) debug($host_role_type);

      $attributes = $hostroletype->body->host_role_type->attributes();

      if (twCanDebug()) debug('newly created id = ' . $attributes['id']);

      // Assign new id.
      $entity->pk_hostroletype = intval($attributes['id']);

      if (twCanDebug()) debug('$entity->pk_hostroletype = ' . $entity->pk_hostroletype);
    }
    else
    {
          	

      //
      // .. otherwise we update the hostroletype entity in ThumbWhere
      //

      $hostroletype = $twAdminAPI->update_host_role_type($id=$entity->pk_hostroletype,$context = array(
                  'key' => variable_get('thumbwhere_api_key')
          ), $fields);
            
        
    }
          	
    
  }
}

/**
 * Implements thumbwhere_hostroletype_entity_insert().
 *
 * Act on entities when inserted.
 *
 * @param $entity
 *   The entity object
 * @param $type
 *   The type of entity being inserted (i.e. node, user, comment).
 */
function thumbwhere_hostroletype_entity_insert($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_hostroletype') return;

}


/**
* Implements thumbwhere_hostroletype_entity_delete().
*
* Act on entities when inserted.
*
* @param $entity
*   The entity object
* @param $type
*   The type of entity being deleted (i.e. node, user, comment).
*/
function thumbwhere_hostroletype_entity_delete($entity, $type) {

  // Flee if this is not the entity type we are looking for
  if ($type != 'thumbwhere_hostroletype') return;


}
